# 한 번만 돌리는 자료 작업

여기 있는 것은 **스키마가 아니라 자료 작업**입니다. 그때 한 번 넣거나 고치려고 쓴 SQL이고,
다시 돌리면 안 됩니다 — 지우고 다시 넣는 것이 섞여 있어서, 두 번째 실행은 그동안 사람이
손으로 고친 것까지 되돌립니다.

`supabase/migrations/` 에 두면 자동 실행이 언젠가 다시 돌립니다. 그래서 여기로 옮겼습니다.
필요하면 사람이 읽어보고 직접 실행합니다.
