// package.json의 "version" 값과 항상 같게 유지합니다(버전을 올릴 때마다 함께 갱신).
// 사이드바에 뜨는 현재 버전 표시는 이 상수를 씁니다 - Vercel 서버리스 함수는 실제로 쓰는
// 파일만 골라 담는 파일 트레이싱을 하는데, package.json이 런타임 번들에 항상 포함된다는
// 보장이 없어서(빌드 도구용 파일이라 트레이싱 대상에서 빠지기 쉬움) fs로 직접 읽는 대신
// 코드에 박아 넣는 쪽이 더 안전합니다.
export const APP_VERSION = "0.75.0";
