# 구글챗 실시간 푸시 설정 가이드

구글챗에 새 메시지가 올라오는 **그 순간** 우리 앱으로 밀어주도록 만드는 설정입니다.

## 왜 이걸 하나요

지금까지는 1분마다 구글챗에 "새 거 있나요?"를 물었습니다. 그런데 외부 스케줄러가 1분마다 부르는데
함수는 25초만 돌아서, **매 분 35초는 아무도 안 보고 있는 공백**이 남습니다.

```
0초 ─────25초──────────────────60초
  3초마다 확인      35초 공백      다시 시작
```

픽업 연락이 늦게 반영되면 아이가 이미 차에 타 있을 수 있습니다. 폴링으로는 이 공백을 없앨 수
없어서, 묻는 대신 알림을 받도록 뒤집습니다.

## 이 설정의 값들

| 항목 | 값 |
| --- | --- |
| GCP 프로젝트 | `gia-ops-chat-mirror` (구글챗 긁어오는 쪽. 운영 프로젝트 아님) |
| 앱 주소 | `https://gia-ops.vercel.app` |
| 주제 ID | `gia-chat-events` |
| 푸시 비밀값 | `mGnf00mzfusxTaibw-7Q9XvSCtr-3Ya2KJTMs4qB3qs` |

> **왜 운영 프로젝트가 아닌가요?** 구독을 승인하는 것은 앱이 아니라 **토큰**입니다. Workspace
> Events가 "이 구독을 만들어도 되나"를 판단할 때 보는 건 OAuth 토큰이고, 그 토큰이 나오는 곳이
> `gia-ops-chat-mirror`입니다. 운영 프로젝트는 이 그림에 안 들어옵니다 — Pub/Sub는 우리 앱으로
> 그냥 HTTPS POST를 쏠 뿐이라, 앱이 GCP에 속하거나 로그인할 필요가 없습니다.

---

## STEP 1 — 주제(Topic) 만들기

콘솔 상단 프로젝트 선택창이 **`gia-ops-chat-mirror`** 인지 먼저 확인하세요. 다른 프로젝트에
만들면 아무 일도 일어나지 않습니다.

1. 좌측 메뉴 → **Pub/Sub** → **주제(Topics)**
2. 상단 **주제 만들기(CREATE TOPIC)**
3. **주제 ID**: `gia-chat-events`
4. **"기본 구독 추가"(Add a default subscription) 체크를 해제**합니다
   - 체크된 채로 만들면 **가져오기(pull)** 방식 구독이 딸려옵니다. 우리는 **푸시**가 필요해서
     STEP 3에서 따로 만들 겁니다. 딸려온 구독을 그냥 두면 메시지를 아무도 안 가져가는 채로
     쌓이기만 하니, 애초에 만들지 않는 편이 깔끔합니다.
5. 나머지(암호화·스키마·보관 기간)는 **전부 기본값**으로 둡니다
6. **만들기**

만든 뒤 주제 상세 화면 위쪽에 전체 이름이 보입니다. **이걸 통째로 복사해 두세요** — STEP 4에서
씁니다.

```
projects/gia-ops-chat-mirror/topics/gia-chat-events
```

---

## STEP 2 — 구글이 이 주제에 글을 쓸 수 있게 허락하기

이 단계를 빼먹으면 구독은 만들어지는데 **알림이 한 통도 안 옵니다.** 조용히 실패하는 자리라
가장 놓치기 쉽습니다.

1. 방금 만든 주제 `gia-chat-events`를 **클릭**해서 상세 화면으로 들어갑니다
2. **권한(PERMISSIONS)** 탭
   - 탭이 안 보이면, 주제 목록에서 주제 왼쪽 **체크박스**를 선택하세요. 오른쪽에 정보 패널이
     열리고 거기에 권한 탭이 있습니다. (콘솔 버전에 따라 둘 중 하나입니다.)
3. **주 구성원 추가(ADD PRINCIPAL)**
4. 새 주 구성원:
   ```
   chat-api-push@system.gserviceaccount.com
   ```
5. 역할: **Pub/Sub** → **Pub/Sub 게시자(Pub/Sub Publisher)**
6. **저장**

> 이 주소는 구글 내부 계정이라 우리가 만드는 게 아닙니다. 그대로 붙여넣으면 됩니다.
> 권한이 실제로 먹히기까지 **몇 분** 걸릴 수 있습니다.

---

## STEP 3 — 푸시 구독 만들기

1. 주제 `gia-chat-events` 상세 화면 → **구독 만들기(CREATE SUBSCRIPTION)**
2. **구독 ID**: `gia-chat-events-push`
3. **Pub/Sub 주제**: `gia-chat-events` (자동으로 채워져 있습니다)
4. **전송 유형(Delivery type)**: **푸시(Push)**
5. **엔드포인트 URL**: 아래를 통째로 붙여넣기 (`?token=` 포함, 한 줄입니다)
   ```
   https://gia-ops.vercel.app/api/google-chat/push?token=mGnf00mzfusxTaibw-7Q9XvSCtr-3Ya2KJTMs4qB3qs
   ```
6. **인증 사용 설정(Enable authentication)** — **체크하지 않습니다**
   - 체크하면 서비스 계정을 따로 만들고 토큰 발급 역할까지 붙여야 합니다. 우리는 주소에 넣은
     비밀값으로 대신하고 있어서 불필요합니다.
7. **페이로드 래핑 해제(Enable payload unwrapping)** — **체크하지 않습니다** (기본값)
8. **확인 기한(Acknowledgement deadline)**: **60초** 로 올립니다
   - 기본값은 10초입니다. 우리 쪽이 알림을 받고 구글챗을 읽어오는 데 몇 초 걸릴 수 있는데,
     10초를 넘기면 Pub/Sub가 "못 받았나 보다" 하고 같은 알림을 다시 보냅니다. 중복 저장은
     막혀 있지만 괜히 두 번 일하게 되니 여유를 둡니다.
9. **재시도 정책(Retry policy)**: **지수 백오프로 재시도** 선택
   - 우리 앱이 잠깐 느릴 때 곧바로 연타하지 않고 간격을 늘려가며 다시 시도합니다.
10. **만료 기간(Expiration period)**: **만료되지 않음(Never expire)** 체크 ⚠️
    - 기본값은 "31일간 활동 없으면 삭제"입니다. **방학 때 한 달 넘게 메시지가 없으면 구독이
      조용히 사라집니다.** 학교 일정상 충분히 일어날 수 있는 일이라 꼭 꺼주세요.
11. 나머지는 기본값 → **만들기**

---

## STEP 4 — Vercel 환경변수

Vercel → 프로젝트 → **Settings → Environment Variables**.
**Production과 Preview 둘 다** 체크해서 추가합니다.

```
GOOGLE_CHAT_PUSH_SECRET = mGnf00mzfusxTaibw-7Q9XvSCtr-3Ya2KJTMs4qB3qs
GOOGLE_CHAT_PUBSUB_TOPIC = projects/gia-ops-chat-mirror/topics/gia-chat-events
```

> **넣은 뒤 재배포해야 적용됩니다.** Deployments → 최신 배포 → ⋯ → **Redeploy**.
> 환경변수는 빌드 시점에 함수로 들어가서, 저장만 하고 재배포를 안 하면 예전 값 그대로입니다.

---

## STEP 5 — 구독 걸기 (cron-job.org)

새 작업(Cronjob)을 만듭니다.

| 항목 | 값 |
| --- | --- |
| URL | `https://gia-ops.vercel.app/api/cron/chat-subscription-renew` |
| 헤더 | `Authorization: Bearer <CRON_SECRET>` |
| 주기 | 매일 06:30 |

만든 뒤 **Run now**를 누르면 그 자리에서 구독이 만들어집니다.

**성공하면** 이렇게 나옵니다.

```json
{
  "ok": true,
  "topic": "projects/gia-ops-chat-mirror/topics/gia-chat-events",
  "results": [
    { "spaceId": "AAAA...", "action": "created", "expireTime": "2026-09-..." }
  ]
}
```

이 크론은 첫 실행 때 구독을 만들고, 그 뒤로는 매일 기한을 최대로 되돌립니다. 구독이 어쩌다
사라져 있으면 알아서 다시 만듭니다.

---

## 확인하기

구글챗 출결알림 방에 아무 메시지나 하나 올려보세요. 업무보드 인박스에 **몇 초 안에** 뜨면
성공입니다.

Pub/Sub 쪽에서도 볼 수 있습니다 — 구독 `gia-chat-events-push` → **측정항목** 탭에서
`push request count`가 올라가는지 확인하세요.

---

## 안 될 때

| 증상 | 원인 | 할 일 |
| --- | --- | --- |
| `action: "failed"`, "not a member of the space" | 구독을 승인한 계정이 그 스페이스 멤버가 아님 | 그 계정을 출결알림 방에 초대 |
| `action: "failed"`, 토픽 권한 오류 | STEP 2를 빼먹었거나 아직 반영 전 | 권한 다시 확인, 5분 뒤 재시도 |
| `GOOGLE_CHAT_PUBSUB_TOPIC가 설정되지 않았습니다` | STEP 4 후 재배포 안 함 | Redeploy |
| 구독은 만들어졌는데 알림이 안 옴 | STEP 2 누락 (가장 흔함) | 주제 → 권한 탭 확인 |
| Pub/Sub 측정항목에 `4xx`가 쌓임 | 엔드포인트 URL의 `?token=` 값이 환경변수와 다름 | 두 값을 눈으로 대조 |

**어느 경우든 최악은 "예전만큼 느려지는 것"입니다.** 1분 폴링을 안전망으로 그대로 켜뒀기
때문에, 푸시가 안 되더라도 메시지를 놓치지는 않습니다.

---

## 이 설계에 대해

알림에 메시지 본문을 실을 수도 있었지만 **일부러 안 실었습니다**(`includeResource: false`).
벨이 울리면 문을 열어 직접 확인하지, 문 밖에서 들리는 말을 그대로 믿지 않는 방식입니다.

```
구글챗에 메시지 → 구글이 Pub/Sub에 발행 → 우리 /api/google-chat/push 호출
                                            ↓
                                "뭔가 왔다"만 받고 → 우리 토큰으로 구글챗을 직접 읽음
```

덕분에 주소에 넣은 비밀값이 새더라도, **가짜 알림으로 할 수 있는 최악이 "쓸데없이 한 번 더
읽게 만드는 것"** 입니다. 가짜 메시지가 인박스에 들어올 길이 없습니다. 덤으로 구독 만료 기한도
훨씬 길어집니다.
