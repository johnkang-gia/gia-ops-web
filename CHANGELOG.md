## v0.407.0 - 2026-09-04

**출석부 — "이 아이가 이번 학기에 며칠 결석했나"에 이제 답이 있습니다**

출결은 지금까지 **셔틀을 위한 것**이었습니다. 오늘 누가 결석인지는 알았지만, 학생 기준으로
모아본 적이 없어서 상급학교 서류·체류 증빙·상담에서 실제로 묻는 숫자를 낼 수 없었습니다.

**출결이 두 갈래로 쌓이고 있었고, 서로를 몰랐습니다.**

| 어디 | 무엇 | 누가 |
|---|---|---|
| `attendance_records` | 날짜별 학생 한 명의 출결 한 줄 | 담임이 화면에서 찍음 |
| `attendance_entries` | 연락에서 읽어낸 결석·지각(기간) | 토들·구글챗에서 자동 |

학부모가 토들에 "내일 결석합니다"를 쓰면 행정실은 그것을 등록하는데, **담임의 출석부는
그대로 비어 있었습니다.** 같은 결석을 두 번 적거나, 아무도 안 적어 출석부에 구멍이 났습니다.
이제 출석부를 열 때 연락에서 읽은 출결이 먼저 채워집니다. **사람이 정해둔 줄은 덮어쓰지
않고**, 자동으로 들어온 줄은 노란 줄로 떠서 담임이 한 번 눌러 확인합니다.

**분모가 없었습니다.** `terms`에 학기 시작일과 종료일만 있고 방학·공휴일·재량휴업일을 적을
자리가 없어서, 며칠 중 며칠인지를 아무도 몰랐습니다. 수업일 달력을 만들었습니다. 학기 평일을
자동으로 깔고 한국 공휴일은 미리 빼두었으니, 방학과 재량휴업일만 눌러서 빼면 됩니다.
100일을 하나씩 누르게 하면 그 일은 결국 안 하게 되고, 안 하면 출석부 전체가 멈춥니다.

**자료 없음과 결석 0을 갈라놨습니다.** 출석부를 쓰기 전 날짜에는 아무 기록도 없습니다. 그
날들을 그냥 세면 "전원 출석"으로 읽히고, 그 숫자가 서류에 그대로 나갑니다 — 없는 출석을
있다고 적는 것입니다. 기록 시작일을 따로 두고, 아직 안 찍은 날은 출석률 계산에서 뺍니다.
표에서는 가운뎃점(·)으로 표시됩니다.

- 결석 사유를 질병·인정·기타·무단으로 나눕니다. 서류에서 실제로 묻는 것은 무단 횟수입니다.
- 한 화면에서 오늘을 찍고, 같은 화면에서 전체·학년·반·학생별 현황을 봅니다. 나누면 매일
  여는 쪽만 쓰이고 나머지는 아무도 안 봅니다.
- 반별 출석부는 종이 출석부와 같은 학생 × 날짜 격자입니다. "이 아이가 언제 빠졌나"는 세로로
  읽어야 보이고, "그 날 몇 명이 빠졌나"는 가로로 읽어야 보입니다.
- 픽업은 출석부에 넣지 않습니다. 하원 방법이지 결석이 아니라, 옮겼다면 매일 픽업하는 아이가
  개근에서 최다결석으로 뒤집힙니다.
- 출석률의 분모는 수업일수가 아니라 **기록이 있는 날**입니다. 수업일수로 나누면 아직 안 찍은
  날이 전부 결석처럼 깎여 학기 초에 40% 같은 숫자가 나오고, 그것을 본 사람은 화면을 안 믿게
  됩니다.

## v0.406.0 - 2026-09-04

**학비외 항목: 끄기를 없애고 삭제로 바꿨습니다**

`끄기` 는 목록에서 사라지게만 할 뿐 줄은 남습니다. `꺼둔 것도 보기` 를 켜야 다시 보이는데,
평소에는 아무도 켜지 않습니다. 그래서 잘못 만든 항목이 **보이지도 지워지지도 않는 상태**로
쌓였습니다.

지우지 않고 끄기로 만들었던 이유는 "지난 인보이스가 왜 그 금액이었는지 설명할 수 있어야
한다" 였습니다. 그런데 그 걱정은 이미 다른 방식으로 풀려 있었습니다 — 인보이스는 항목을
가리키지 않고, 발행하는 순간의 이름과 금액을 `invoice_lines` 에 베껴 둡니다. **항목을 지워도
이미 보낸 청구서는 글자 하나 바뀌지 않습니다.**

- 항목 관리는 `고치기` 와 `삭제` 둘뿐입니다. 분류도 같습니다.
- 지우기 전에 무엇이 함께 없어지는지 보여주고 묻습니다. `정말 지울까요?` 만 띄우면 사람은
  읽지 않고 누르므로, 손으로 넣거나 뺀 아이가 몇 명인지를 숫자로 적었습니다.
- 단가 이력은 항목보다 오래 남습니다. 연결이 `on delete cascade` 라서 항목을 지우면
  "작년엔 얼마였나" 가 함께 지워지고 있었습니다. 연결만 끊고 이력은 남깁니다.
- 쓰는 항목이 있는 분류는 지울 수 없습니다. 분류가 없어지면 그 항목들이 어느 묶음에도
  속하지 않게 되어 탭에서 사라집니다.
- 꺼둔 채 남아 있던 항목은 정리했습니다. 화면에서 끄기를 없앤 뒤에도 그대로 두면, 그것이
  바로 보이지도 지워지지도 않는 줄이 됩니다.
- `active` 칸은 남기되 화면·계산 어디에서도 이 칸으로 거르지 않습니다. 거르는 곳이 하나라도
  남아 있으면 언젠가 같은 상태가 다시 생깁니다.

## v0.404.1 - 2026-09-03

**자동 실행이 왜 91번 실패했는지 — 원인을 찾아 하나씩 걷어냈습니다**

실패 화면을 처음으로 읽어봤고, 원인이 셋이었습니다.

**① 기록표에 유령이 하나 있었습니다** — 해결

```
Remote migration versions not found in local migrations directory.
```

원격 기록에는 `20260824160000` 이 있는데 로컬에는 그 파일이 없습니다. CLI 는 이런 어긋남이
하나라도 있으면 **아무것도 실행하지 않고 통째로 거절합니다.** 한 줄 때문에 79개가 멈춰
있었습니다. 이제 워크플로가 스스로 찾아 기록만 지웁니다(DB 는 건드리지 않습니다).

처음에는 `migration list` 가 그려주는 표를 잘라 썼는데, 그 표는 **사람이 보라고 그린 것**
이라 화면이냐 파일이냐에 따라 모양이 바뀝니다. 작업기계에서는 선 문자가 아예 안 나와서
찾은 것이 하나도 없었습니다. 기록은 그냥 표 하나이므로 **그 표를 직접 읽도록** 바꿨습니다.

**② 순서가 뒤섞여 있습니다** — 베이스라인이 필요합니다

기록에 없는 파일 33개가 시간 순서상 뒤죽박죽 흩어져 있습니다. 손으로 SQL 을 돌려온
기간이 길어서 그렇습니다.

`--include-all` 로 전부 실행해봤지만 실패했습니다 — 그 안에 **이미 적용된 자료 작업**이
섞여 있어서 두 번째 실행에서 앞뒤가 안 맞았습니다. 되돌렸습니다.

대신 **베이스라인** 모드를 넣었습니다. 지금 파일을 **실행하지 않고** "이미 돌린 것"으로만
표시합니다. 한 번 해두면 그다음부터는 새로 만든 파일만 순서대로 자동 적용됩니다.

**③ 한 번만 돌려야 하는 자료 작업이 섞여 있었습니다** — 분리

8월에 엑셀에서 하원 배정을 넣은 SQL 세 개를 `supabase/one-off/` 로 옮겼습니다.
지우고 다시 넣는 구조라, 두 번째 실행은 그동안 사람이 손으로 고친 배정까지 되돌립니다.
스키마가 아니라 자료 작업이므로 마이그레이션 폴더에 있을 것이 아니었습니다.

## v0.404.0 - 2026-09-03

**SQL 자동 실행이 91번 연속 실패하고 있었습니다**

마이그레이션 자동화는 만들어져 있고 시크릿도 등록돼 있었는데, **실행할 때마다 실패**하고
있었습니다. 아무도 실패를 안 봐서, 그동안 SQL 은 계속 손으로 붙여넣었습니다.

```
Remote migration versions not found in local migrations directory.
```

- 원격 기록표에는 `20260824160000` 이 있는데 **로컬에는 그 파일이 없습니다.** 언젠가
  SQL 편집기에서 직접 돌린 것으로 보입니다. CLI 는 이런 어긋남이 하나라도 있으면
  **아무것도 실행하지 않고 통째로 거절합니다.** 한 줄 때문에 79개가 전부 멈춰 있었습니다.
- 이제 워크플로가 **스스로 맞춥니다.** 로컬에 파일이 없는 원격 기록은 기록만 지웁니다
  (DB 는 건드리지 않습니다). 사람이 알아채기를 기다리면 그동안 전부 멈춥니다.
- **베이스라인** 모드를 넣었습니다. 손으로 돌려온 DB 를 자동 실행으로 넘길 때 한 번
  씁니다 — 지금 파일을 **실행하지 않고** "이미 돌린 것"으로만 표시합니다. 이걸 안 하면
  첫날에 36개가 한꺼번에 돌면서 이미 손으로 돌린 것까지 다시 실행됩니다.
- **한 번만 돌려야 하는 자료 작업 3개를 `supabase/one-off/` 로 옮겼습니다.**
  8월에 엑셀에서 하원 배정을 넣은 SQL 인데, 지우고 다시 넣는 구조라 두 번째 실행은
  그동안 사람이 손으로 고친 배정까지 되돌립니다. 스키마가 아니라 자료 작업이므로
  마이그레이션 폴더에 있을 것이 아니었습니다.

## v0.403.0 - 2026-09-03

**하원 체크표가 느리던 이유 — 여덟 번을 한 줄씩 기다렸습니다**

실측했습니다. 화면마다 서버가 답을 주기까지 걸린 시간입니다.

```
하원 체크표   1,973ms   ← 다른 화면의 두 배
학생 조회     1,168ms
셔틀 개요     1,159ms
업무 보드       949ms
인보이스        453ms
```

- 체크표는 자료를 **열네 번** 읽는데, 그것을 **한 줄씩 차례로** 기다리고 있었습니다.
  하나가 100ms 면 여덟 개는 800ms 이고, 그동안 화면은 비어 있습니다.
- 서로의 결과를 쓰지 않는 여덟 개(오늘 연락·특이사항·하원수단·구글챗·명부·활동기록·
  동승·토들주소)를 한꺼번에 보냅니다. **가장 느린 하나만큼만** 걸립니다.
- 순서를 지켜야 하는 것(노선 → 정류장 → 배정)은 그대로 뒀습니다. 앞의 답이 있어야
  다음을 물을 수 있는 자리입니다.

하루에 가장 많이 여는 화면이라, 이 기다림이 그대로 쌓입니다.

## v0.402.0 - 2026-09-03

**앞날 결석이 오늘 결석으로 등록되던 문제**

```
이연우 9/21-23 일결석입니다   →   9월 21일이 아니라 오늘 결석
```

- 날짜 계산을 **픽업에만** 하고 있었습니다. 결석 통보는 어느 날 이야기든 `service_date` 가
  받은 날로 굳었고, 하원 체크표는 그 날짜만 보고 오늘 결석으로 그었습니다. 앞날 이야기를
  오늘로 당겨 놓으면 **오늘 오는 아이가 명단에서 빠집니다.**
- 이제 픽업이 아닌 연락에서도 날짜를 읽습니다. 앞날이면 그날로 등록하고, 지난 날짜는
  쓰지 않습니다(이미 끝난 일이고 잘못 읽었을 가능성이 큽니다).
- **`9/21-23` 이 기간으로 안 잡히던 이유**: 기간으로 인정하려면 "까지" 같은 말이 있어야 했는데,
  날짜를 직접 두 개 적은 글에는 그 말이 없습니다. 가장 분명하게 적어준 형태를 못 읽고
  있었습니다. 이제 날짜가 두 개 적혀 있으면 그것만으로 기간입니다.
- `9월 21일**부터** 23일까지` 도 읽습니다. 사이를 잇는 말이 물결·붙임표만 있는 줄 알고
  있어서, 시작일이 글이 온 날로 잡히고 있었습니다.

**가르친 뒤에 화면이 안 바뀌던 문제**

- 가르치면 규칙만 다시 읽었습니다. 그런데 이미 '확인 필요' 로 **등록해 둔 줄**이 남아 있어서
  화면은 그대로였습니다. 규칙과 등록 상태를 함께 다시 읽습니다.
- 출결내역 칸에 **↻ 새로고침**을 넣었습니다. 이 칸만 다시 그립니다 — 페이지 전체를
  새로고침하면 하던 체크가 날아갑니다.

**학부모 문의에 동명이인의 반 표기**

- 김재이가 셋(G2C·G2A·G3JA)인데 문의 목록에는 그냥 "김재이" 로만 떴습니다. 어느 아이인지
  모른 채 셔틀·출결에 반영하면 엉뚱한 아이가 처리됩니다.
- 이름이 이미 한글이면 손대지 않고 있었던 것이 원인입니다. 이제 한글이어도 명부와 대조해
  **겹치는 이름에만** 반을 붙입니다(`김재이(G2C)`). 한 명뿐인 이름에는 붙이지 않습니다 —
  전부 붙이면 정작 구분이 필요한 이름이 묻힙니다.

## v0.401.0 - 2026-09-03

**형제자매를 묶었습니다 — 18가정 38명**

형제자매는 이 학교 실무에서 계속 걸리는 자리였습니다. 한 아이 차에 다른 아이를 태워달라는
연락, 형제 대화방에서 한 아이만 결석인 글(실제로 여명이가 결석으로 잘못 들어간 적이 있습니다),
같은 번호로 두 장 나가는 청구서. 지금까지는 **연락처가 같다는 것을 사람이 눈치채는 수밖에**
없었습니다.

- `wr_students.sibling_group_id` — 같은 집 아이들이 같은 값을 나눠 갖습니다. 관계를 줄 단위로
  적는 표를 두지 않은 이유: 형제가 셋이면 세 줄(A-B·B-C·A-C)이 필요하고 한 명이 전학 가면
  그 세 줄을 다 지워야 합니다. 하나 빠뜨리면 남은 둘이 서로를 모릅니다. 묶음 id 는 칸 하나만
  비우면 끝납니다.
- 학생 프로필에 **형제자매 줄**이 뜹니다. 이름을 누르면 그 아이 프로필로 넘어갑니다.
- 묶는 기준은 **보호자 연락처가 하나라도 같은 것**입니다. 눈으로 확인한 두 집(송윤진·송우진,
  장하영·장예나)이 이 기준으로 정확히 묶이는 것을 확인했습니다.
- 장예나(4학년 G4R)를 명부에 넣습니다. 전화번호부에는 (NEW) 로 있는데 앱 명부에만 없던
  아이입니다. 11학년 장하영의 동생입니다.
- 칸이 아직 없는 DB 에서도 프로필은 그대로 뜹니다. 형제자매 줄만 안 보입니다.

## v0.400.1 - 2026-09-03

**동승 표 만들기가 함수 하나 때문에 멈추던 문제**

```
ERROR: 42883: function public.stamp_current_term() does not exist
```

- 학기 도장 트리거를 거는 줄이 있었는데, 그 함수는 **학기 통합 SQL 에서 만들어집니다.**
  아직 안 돌린 DB 에서는 그 한 줄 때문에 표 자체가 안 만들어졌습니다.
- 도장은 있으면 좋은 것이지 없으면 안 되는 것이 아닙니다. 함수가 있을 때만 걸고, 없으면
  건너뛰면서 그 사실을 알립니다. 나중에 학기 SQL 을 돌린 뒤 이 파일을 다시 실행하면
  그때 걸립니다.
- 같은 이유로 `set_updated_at` 트리거, `terms` 외래키, 권한 함수(`is_giamicro_user`)도
  있을 때만 겁니다. 권한 함수가 없으면 로그인한 사용자 기준으로 정책을 세웁니다 —
  **정책 없이 RLS 만 켜두면 아무도 못 읽는데, 화면은 그냥 비어 보이고 오류도 안 납니다.**

## v0.400.0 - 2026-09-03

**"서이 셔틀에 하임이두 같이 보내주세요" 를 읽어서 오늘 명단에 올립니다**

이 한 문장에 세 가지가 겹쳐 있습니다. 김서이가 타는 차에 · 정하임을 같이 · 오늘 하루만.
정하임은 평소 셔틀을 타지 않습니다. 지금까지 이런 연락은 '문의' 로만 쌓였고, 사람이 읽고
기억해야 했습니다. 잊으면 아이가 차를 못 탑니다.

- **적을 자리가 없었습니다.** 오늘만 차를 바꾸는 기능은 이미 배정이 있는 아이에게만 걸 수
  있는데, 평소 안 타는 아이는 배정 자체가 없습니다. 그날 하루짜리 줄
  (`shuttle_ride_alongs`)을 새로 뒀습니다 — 정식 배정을 건드리지 않으므로 다음 날에는
  저절로 사라집니다. 배정에 넣었다 지우는 방식은 지우는 것을 잊으면 안 타는 아이가 계속
  명단에 남습니다.
- 연락이 들어오면 **규칙으로 먼저 읽습니다.** "<이름> 셔틀에 <이름>도/두/같이" 문형입니다.
  AI 호출은 실패할 수 있고 느린데, 이 문형은 뜻이 좁아 규칙으로 충분합니다.
- **양쪽 다 한 명으로 좁혀질 때만 자동 등록합니다.** 부모는 '서이' · '하임이' 처럼 짧게
  부르는데, 하임이는 명부에 **임하임과 정하임 둘**입니다. 이런 건 확인 대기로 두고 하원
  체크표에서 한 번 눌러 고릅니다. 엉뚱한 아이를 차에 태우는 것은 되돌릴 수 없습니다.
- 하원 체크표 맨 위에 **노란 칸**으로 뜹니다. 누가 · 몇 호차에 · 누구 차에 타는지와 원문이
  함께 보이고, 아니면 그 자리에서 취소합니다.
- 조사를 뗄 때 '서이' 를 '서' 로 만들지 않습니다. 뗀 모양과 안 뗀 모양을 모두 만들어 두고
  명부에서 맞는 것을 고릅니다 — 무조건 떼면 두 글자 이름이 전부 어긋납니다.

## v0.399.0 - 2026-09-03

**업무 보드가 옆으로 늘어나던 이유**

학부모 문의 탭을 열면 인박스는 물론 화면 전체가 옆으로 늘어나고, 출결내역 탭으로 옮기면
돌아왔습니다. 실제로 재보니 **화면은 1,366px 인데 문서는 9,412px** 이었습니다.

- 원인은 **문의 한 건**이었습니다. 1,553자짜리 긴 글이 들어와 있었고, 목록의 한 줄
  미리보기가 `truncate` 로 그려지고 있었습니다.
- `truncate` 는 글을 자르기 전에 **먼저 한 줄로 늘어놓습니다**(`white-space: nowrap`).
  그 한 줄의 길이가 곧 그 칸의 최소 너비가 되고, 바깥 칸까지 차례로 벌어집니다. 긴 글이
  문의 탭에만 있었으므로 그 탭에서만 늘어난 것입니다.
- 미리보기를 `line-clamp-1` 로 바꿨습니다. 줄바꿈을 허용한 채 첫 줄만 보여주므로, 아무리
  긴 글이 와도 최소 너비가 낱말 하나를 넘지 않습니다. 미리보기 글자 수도 200자에서 자릅니다.
- 같은 함정을 쓰던 자리 세 곳(계정 이름줄, 탑승 배정 두 곳)도 함께 고쳤습니다.
- `npm run build` 가 `scripts/check-truncate.mjs` 를 돌려 **flex 상자에 걸린 truncate** 를
  찾습니다. 자료 한 줄이 화면 전체를 밀어내는 종류의 고장이라, 눈으로는 원인을 찾기 어렵습니다.

**그밖에**

- 업무 보드의 칸 폭 조절 손잡이가 4px 짜리 선이라 잡히지 않았습니다. 10px 로 넓히고 손잡이
  점을 찍었으며, 두 번 누르면 원래 폭으로 돌아갑니다. 좁힐 수 있는 한계도 16% → 12% 로
  내렸습니다 - 넓은 모니터에서는 26%가 2,300px 이라 인박스 하나가 화면 4분의 1이었습니다.
- 출결 표기를 가르치는 창에서 학생을 고를 때 학년만 나와 동명이인을 가릴 수 없었습니다
  (김재이가 셋인데 둘이 2학년입니다). 반을 함께 적습니다.

## v0.398.0 - 2026-09-03

**인보이스 명단 표에서는 더 이상 바뀌지 않습니다**

표의 칸을 누르면 그 자리에서 항목이 붙고 떨어졌습니다. 칸이 작고 촘촘한 표라, 가로로
훑다가 옆 칸을 눌러도 알아채지 못합니다. 잘못 붙은 것은 청구서가 나간 뒤에야 드러납니다.

- 표는 **보는 곳**입니다. 칸을 눌러도 값이 바뀌지 않고, 그 학생 창이 열립니다.
- 넣고 빼는 것은 **이름을 눌러 나오는 창에서만** 합니다. 창에는 항목 이름·금액·수량이
  함께 있어서, 지금 무엇을 바꾸는지 보고 누르게 됩니다.
- 칸에 마우스를 올리면 그 항목과 금액이 그대로 뜹니다. 확인은 표에서, 수정은 창에서.

## v0.397.0 - 2026-09-03

**분류(교재 · 교복 …)를 나눠서 보고, 나눠서 청구합니다**

교재를 붙이는 일과 교복을 붙이는 일은 다른 날 하는 다른 일인데, 한 화면에 함께 세워두고
한 장으로 묶어 청구하고 있었습니다. 교복 치수가 안 끝난 아이 하나 때문에 교재비 청구까지
멈추는 구조였습니다.

- **인보이스 명단에 분류 탭**을 넣었습니다. 교재를 고르면 교재 열만, 교복을 고르면 교복 열만
  섭니다. 학생을 눌러 여는 창도 같은 분류만 보여줍니다 — 교재를 붙이려고 연 창에 교복이
  함께 있으면 잘못 눌러도 알아채기 어렵습니다.
- 합계·미발행 수도 그 분류 기준입니다. "교복 합계가 얼마인가"를 따로 물을 수 있습니다.
- **발행할 때 둘 중에 고릅니다.**
  · 한 장에 전부 — 학생당 한 장. 학부모가 한 번만 결제합니다.
  · 분류마다 따로 — 그 학생에게 붙은 분류마다 한 장씩. 교재비를 먼저 보내고 교복은 나중에
    보낼 수 있습니다.
  발행 전 확인 창에서 **모두 몇 장이 나가는지**를 먼저 보여줍니다.
- 분류 탭을 고른 상태에서는 그 분류만 나갑니다. 고를 것이 없으므로 묻지 않습니다.
- 청구서에 **어느 분류인지가 남습니다**(`invoices.category`, 비어 있으면 통합). 표에서 번호
  옆에 `교재`·`교복`·`통합`으로 보입니다. 총액만 남기면 나중에 "이 아이 교복은 나갔나"를
  물을 방법이 없습니다.
- 한 학생에게 청구서가 여러 장 생기므로, '발행됨/미발행'은 **보고 있는 분류 기준**으로
  판단합니다. 통합 청구서에는 그 분류도 들어 있으므로 나간 것으로 봅니다.
- 금액과 분류를 고르는 일은 서버가 다시 합니다. 화면이 고른 줄을 그대로 받아 넣으면,
  화면이 틀렸을 때 틀린 내역이 그대로 학부모에게 갑니다.

## v0.396.0 - 2026-09-03

**인보이스 명단이 통째로 안 뜨던 문제**

```
column wr_students.mother_phone does not exist
```

- 보호자 연락처 칸이 아직 DB에 없습니다. 그런데 **칸 하나가 없다고 137명 명단 전체가**
  **안 나왔습니다** — PostgREST 는 없는 칸을 하나라도 적으면 질의를 통째로 되돌립니다.
- 이제는 없는 칸만 빼고 읽고, **무엇이 없었는지를 화면 위에 띄웁니다.** 명단은 보이고,
  연락처 자리만 비며, 왜 비었는지가 함께 나옵니다.
- 인보이스 발행과 올톡페이 내보내기도 같은 방식으로 고쳤습니다. 연락처 없이도 발행은
  되고, 빠진 칸은 기록에 남습니다.
- 채우려면 `supabase/migrations/20260903060000_guardian_phones.sql` 을 실행하면 됩니다.

**상단 탭이 화면마다 다르던 문제**

- **재무 화면 다섯 개에는 상단 탭줄이 아예 없었습니다.** 대분류에 등록되지 않아서, 각
  페이지가 자기 본문 안에 따로 탭줄을 그리고 있었습니다. 그래서 재무로 들어가면 탭이
  사라졌다가 조금 아래 다른 모양으로 다시 나타났습니다. 하원 체크표도 같았습니다.
  둘 다 공용 탭줄로 올렸습니다(재무는 새 대분류, 셔틀명단은 하원 체크표의 하위 줄).
- **하위 줄은 없어도 자리를 지킵니다.** 하위 줄이 있는 탭과 없는 탭을 오갈 때마다 본문
  전체가 30px 씩 튀었습니다.
- 탭이 많은 대분류는 좁은 화면에서 두 줄로 접히며 또 튀었습니다. 이제 접히지 않고 옆으로
  흐릅니다. 어느 화면에서든 탭줄의 높이와 시작점이 같습니다.
- 탭줄이 없는 화면 다섯 곳을 더 찾아 붙였습니다: 출결 내역 · 학부모 문의 · 학교 개요 ·
  명부 가져오기(운영 대시보드는 큰 모니터 전용이라 그대로 둡니다).
- `npm run build` 가 `scripts/check-tabbar.mjs` 를 돌려, **탭에 걸리지 않은 새 화면**을
  찾습니다. 탭줄이 빠진 화면을 만들면 그 화면만 다르게 보이고, 만든 사람은 본문에 탭줄을
  또 그리게 됩니다. 그것이 지금까지 탭이 페이지마다 달라 보인 이유였습니다.

## v0.395.0 - 2026-09-03

**운행일지가 안 나오던 진짜 이유**

앞 판에서 오류를 화면에 내보내게 고쳤더니 이유가 드러났습니다.

```
Cannot convert argument to a ByteString because the character at index 18 has a value of 50868
```

- **HTTP 헤더에는 한글을 그대로 넣을 수 없습니다.** 파일 이름을
  `운행일지_27호_2026-09-03.pdf` 로 헤더에 담고 있었는데, 응답을 만드는 그 순간 터졌습니다.
  PDF는 다 만들어놓고 마지막 한 줄에서 죽은 것이라, 화면에는 아무 일도 없는 것처럼 보였습니다.
- 이름을 두 벌로 보냅니다. 영문·숫자만 남긴 이름과, 규격대로 감싼 한글 이름. 요즘 브라우저는
  한글 쪽을 쓰고, 오래된 것은 영문 쪽을 씁니다.
- **PDF를 만드는 다섯 라우트가 모두 같은 문제를 안고 있었습니다** — 운행일지·주간 관찰기록·
  매뉴얼·업무 보고·회의 보고. 전부 고쳤고, 실패하면 빈 화면 대신 이유가 글로 나옵니다.
- 앞 판에서 함께 넣은 글꼴 포함 설정은 그대로 둡니다. 이번 원인은 아니었지만, 배포본에서
  글꼴이 빠지면 똑같이 조용히 죽는 자리입니다.

## v0.394.0 - 2026-09-03

**오늘 운행일지가 안 나오던 문제**

- 눌러도 아무것도 안 뜨고 이유도 알 수 없었습니다. 실제로는 **빈 500**이 돌아오고 있었습니다.
- 원인은 **한글 글꼴 파일이 배포본에서 빠진 것**입니다. PDF를 만드는 라우트는 글꼴을 경로로
  읽는데, 코드에서 불러오지 않는 파일이라 배포할 때 통째로 제외됩니다 — 로컬에서는 잘 되고
  실제 배포에서만 죽는, 알아채기 어려운 종류의 고장입니다.
- 글꼴을 배포본에 넣도록 했습니다. PDF를 만드는 다섯 라우트(운행일지·주간 관찰기록·매뉴얼·
  업무 보고·회의 보고)가 모두 같은 문제를 안고 있었습니다.
- 글꼴 준비를 한 곳으로 모으고, **못 찾으면 그 사실을 말합니다.** 조용히 넘기면 글자가
  네모로 나간 종이를 기사님이 받게 됩니다.
- 운행일지가 실패하면 이제 빈 화면 대신 **이유가 글로** 나옵니다.

**GPS로 학습한 정류장 좌표**

- **호차 → 정류장 번호 순**으로 세웁니다. 예전에는 관측 횟수가 많은 순이라 한 호차의
  정류장이 목록 여기저기에 흩어졌고, 27호를 확인하려면 전체를 훑어야 했습니다. 확인은 노선
  단위로 하고, 1번부터 순서대로 봐야 어느 자리가 빠졌는지 보입니다.
- 호차별로 접고 펼칩니다. 접힌 줄에 **`3/7 반영`** 처럼 진행 상황이 뜹니다 — 그 노선을
  끝냈는지가 한눈에 보여야 다음 호차로 넘어갈 수 있습니다.
- 줄마다 **반영 여부(✓ / ○)** 를 표시합니다. 학습된 좌표와 지금 쓰는 좌표가 10m 안쪽이면
  반영된 것으로 봅니다. 이 표시가 없으면 무엇을 눌렀고 무엇이 남았는지 알 수 없어 같은 줄을
  몇 번씩 다시 누르게 됩니다.
- 이미 반영된 줄의 단추는 `반영됨` 으로 흐리게 둡니다.

## v0.393.0 - 2026-09-03

**어느 정류장인지 모르는 정차 지점을 호차별로 접습니다**

- 한 줄씩 늘어놓으면 서른 줄이 넘고, 27호를 정리하려면 다른 호차 줄을 계속 지나쳐야 했습니다.
  정리는 보통 **한 호차씩** 합니다 — 그 호차의 노선을 알아야 어느 정류장인지 판단할 수
  있기 때문입니다.
- 호차별로 묶고, 눌러서 펼칩니다. 기본은 전부 접힘입니다.
- 접힌 줄에 그 호차의 **개수와 날짜 범위**를 적어둡니다. 펼치지 않고도 어디를 봐야 할지
  고를 수 있습니다.
- 위쪽에 `모두 펼치기 / 모두 접기`.
- 호차는 숫자로 정렬합니다. 글자 순서로 두면 10호가 2호 앞에 옵니다.

## v0.392.0 - 2026-09-03

**근거 창의 말투**

- `이재훈가 체크표에서 픽업으로 표시했습니다` → `이재훈님이 체크표에서 픽업으로 표시했습니다`.
  받침에 따라 `이/가`를 고르는 것도 어색했지만, 그보다 이 문장은 **동료를 가리키는 말**이라
  높임이 맞습니다.
- 누가 눌렀는지 알 수 없을 때만 `담당자가` 로 둡니다.

## v0.391.0 - 2026-09-03

**하원 체크표 인쇄가 한 장으로 (다시)**

지난 판에서 종이를 화면 바깥으로 옮기면서 **높이를 재는 코드가 죽었습니다.** 옮긴 뒤로는
첫 화면에 종이가 아직 없어 재기를 그냥 건너뛰었고, 붙은 뒤에는 다시 잴 이유가 없어 축소가
1.0인 채로 남았습니다. 실제로 재보니 내용은 1105px인데 한 장은 1069px이었습니다.

- 종이가 붙은 뒤에 다시 재도록 고쳤습니다.
- 겉상자도 **한 장 높이로 못박았습니다.** 재기가 조금 틀려도 종이가 두 장이 되지 않게 하는
  마지막 빗장입니다.

## v0.390.0 - 2026-09-03

**"셔틀 안 탑니다"를 이제 읽습니다**

`오늘 이예나 셔틀 안탑니다` 라는 글이 통째로 무시됐고, 그 아이는 출결내역에 **아예 뜨지도
않았습니다.** 분류 낱말 목록에 이 표현이 하나도 없어서, 글 전체가 "출결과 무관한 글"로
버려졌기 때문입니다.

- `셔틀 안 탑니다` · `차 안 타요` · `버스 못 타요` · `태우지 마세요` 를 **픽업**으로 읽습니다.
  셔틀을 안 타면 누군가 데리러 옵니다.
- 결석이 함께 적힌 글은 결석이 이깁니다(`아파서 결석입니다, 셔틀도 안 타요`).
- `셔틀 탑니다` · `기존처럼` · `평소대로` 는 그대로 아무 일도 아닌 것으로 둡니다. 평소대로
  탄다는 말에 픽업을 걸면 매일 픽업이 쌓입니다.

**글을 문장 단위로 넓게 읽습니다**

- 예전에는 쉼표·연결어미에서 잘게 자른 뒤 **이름이 들어 있는 조각만** 봤습니다. 그래서
  `오늘 이예나, 셔틀 안탑니다` 처럼 이름과 서술이 갈라지면 이름 쪽 조각에 아무 단서가 없어
  분류가 안 됐습니다.
- 이제 **문장으로 먼저 읽습니다.** 한 문장에 아이가 여럿일 때만(`권수호는 픽업, 라원이는
  셔틀`) 다시 절로 나눠 각자 읽습니다 — 한 아이의 사정이 옆 아이에게 번지지 않아야 합니다.
- 문장을 나눌 때 마침표뿐 아니라 한국어 종결어미(`~습니다` `~해요` `~네요`)에서도 끊습니다.
  실제 글에는 마침표가 거의 없습니다.

**학비외 항목: 중고등부에 초등 학년·반이 뜨던 문제**

- 학년·반 목록을 한 벌만 만들어 초등·중고등에 똑같이 쓰고 있었습니다. 그래서 중고등부 항목을
  만들 때도 초등 학년(2~5)과 초등 반(G2A…)이 떴습니다. **고를 수 있는 것이 남의 부서 것뿐이면
  기본 대상을 정할 수 없고, 결국 아이마다 손으로 체크하게 됩니다.**
- 부서별로 나눠 그 부서의 학년·반만 보여줍니다.
- 목록을 반 표만이 아니라 **명부에서도** 모읍니다. 중고등부는 반이 없는 학년이 있어서 반
  표만 보면 그 학년이 통째로 빠집니다.
- `공통` 탭(교복처럼 학교 전체가 사는 것)에서는 양쪽을 다 보여줍니다.

## v0.389.0 - 2026-09-03

**인보이스 명단이 어머니·아버지 번호를 봅니다**

청구 대상을 어머니/아버지/보호자로 나누는 일은 되어 있었는데, **인보이스 명단은 아직 옛
`보호자` 칸 하나만 읽고 있었습니다.** 어머니 번호가 채워져 있어도 "연락처 없음"으로 세었고,
학생 창에서도 보이지 않았습니다.

- 명단이 세 칸을 모두 읽습니다. 청구서는 **어머니 → 아버지 → 보호자** 순으로 있는 번호로
  나갑니다.
- `연락처 없는 학생 N명` 도 세 칸을 모두 보고 셉니다.
- 학생 창에서 **세 번호를 그 자리에서 고칩니다.** 발행하려다 번호가 없는 것을 알게 되는데,
  그때 명부 화면으로 건너갔다 오면 하던 일을 놓칩니다. 지금 청구가 나갈 번호에는 점(●)을
  찍어 표시합니다.
- 저장에 실패하면 알려줍니다. 저장된 줄 알고 발행하면 그 아이만 청구가 안 나갑니다.

## v0.388.0 - 2026-09-03

**나머지 기록도 학기에 붙였습니다**

업무·회의·사건기록·관찰기록은 이미 학기에 붙어 있었고, 문서·메뉴얼·시간표·당번표·공지·출결은
아직이었습니다. 지금은 자료가 얼마 없어서 옮기기 쉽습니다 — 한 학기만 더 지나도 수천 줄을
나중에 갈라야 합니다.

- **새로 들어오는 줄은 저절로 지금 학기에 붙습니다.** 넣는 자리가 수십 곳이라 사람이 매번
  적게 하면 반드시 빠뜨리고, 빠뜨린 줄은 어느 학기에서도 안 보입니다. 화면이 아니라 DB가
  붙입니다.
- 지금 있는 줄은 전부 진행중인 학기로 넘어갑니다.
- `term_archive` 로 학기별로 무엇이 쌓였는지 한 줄로 봅니다 — 노선·학비외항목·청구서·업무·
  회의·사건기록·관찰기록·문서·메뉴얼·학생소속.

**보고 있는 학기를 바꿉니다**

- 사이드바에 학기 고르개가 생겼습니다. 바꾸면 업무·회의·사건기록·문서·메뉴얼이 그 학기 것으로
  바뀝니다. 선택은 쿠키에 남아 화면을 옮겨도 유지됩니다.
- **지난 학기를 보고 있으면 노랗게 표시**합니다. 지난 학기를 열어둔 채 새 기록을 남기려다
  "왜 안 보이지"가 되는 일이 가장 흔한 사고입니다.
- 학기 칸이 비어 있는 옛 줄은 어느 학기에서든 함께 보여줍니다. 학기를 붙이기 전에 쌓인
  기록이 갑자기 사라지면 안 됩니다.

**하원 체크표가 다시 한 장으로**

- 인쇄하면 빈 2·3장이 뒤에 붙었습니다. 원인은 표가 아니라 **화면 내용이 자리를 차지한 채로
  숨겨져 있던 것**입니다 — 안 보이게만 했지 자리는 그대로였고, 체크표 화면이 길어지면서 그
  빈 자리가 장수가 됐습니다.
- 인쇄용 종이를 화면 바깥(body 바로 아래)으로 옮기고, 인쇄할 때 나머지를 **자리까지** 없앱니다.

## v0.388.0 - 2026-09-02

**청구서를 누구 앞으로 보낼지 고를 수 있습니다**

지금까지 연락처는 학생 한 명에 한 칸뿐이었습니다. 그런데 결제하는 분은 집마다 다릅니다 -
어머니가 받는 집, 아버지가 결제하는 집, 부모가 아닌 분이 맡는 집이 다 있습니다. 한 칸에
하나만 담아두면 청구할 때마다 명부를 고쳐야 하고, 고치는 순간 출결 연락·셔틀에 쓰이는 번호까지
함께 바뀝니다.

- 명부의 연락처를 **어머니(M) · 아버지(F) · 보호자** 세 칸으로 나눴습니다. 마지막 칸은 부모가
  아닌 분(조부모·친척 등)을 위한 자리입니다.
- 청구서는 **어머니 → 아버지 → 보호자** 순으로 있는 번호를 씁니다. 어머니 칸을 채우기 전까지는
  지금과 똑같이 동작하고, 채우는 대로 어머니 번호로 넘어갑니다.
- 기존 번호를 어머니 칸으로 옮기지 않았습니다. 지금 들어 있는 번호가 어머니 것인지 아버지
  것인지 아무도 확인해 주지 않았기 때문입니다. 추측으로 옮기면 그 뒤로는 틀린 줄도 모르고
  씁니다.
- 발송 파일을 만드는 화면에서 **줄마다 M / F / 보호자를 눌러 바꿀 수 있습니다.** 명부는 그대로
  두고 이번 청구만 다른 분께 보낼 수 있습니다.
- 청구서에 어느 분 앞으로 보냈는지가 함께 남습니다. 번호만 남기면 명부가 바뀐 뒤에 그게 누구
  것이었는지 알 수 없습니다.
- 학생 관리 표·학생 상세·명부 가져오기에서 세 칸을 모두 넣고 고칠 수 있습니다. 예전처럼 다섯
  칸(이름·영어이름·학년·반·연락처)만 붙여넣으면 다섯 번째가 어머니 번호로 들어갑니다.

**발송 파일이 올톡페이 양식 그대로 나옵니다**

- 올톡페이가 준 **청구서등록 양식**과 시트 이름·열 이름·열 순서가 같습니다. 별표와 띄어쓰기까지
  같아서, 내려받아 대량발송에 **손대지 않고 그대로 올리면** 됩니다.
- 양식에 없는 열은 하나도 붙이지 않습니다. 우리 쪽 관리번호도 넣지 않습니다 - 열이 하나만 달라도
  업로드가 통째로 거절될 수 있습니다. 무엇을 내보냈는지는 청구서의 발송 표시로 남습니다.
- 결제만료시간을 화면에서 고릅니다(기본 23시). 이른 시각으로 잡으면 퇴근길에 결제하려던 학부모가
  못 냅니다.
- 휴대폰 번호는 글자로 굳혀 넣습니다. 숫자로 두면 엑셀이 앞의 0을 지워 01012345678이
  1012345678이 되고, 그 번호로는 청구서가 못 갑니다.

## v0.387.0 - 2026-09-02

**학기를 하나의 뿌리로 모았습니다**

학기 개념이 세 갈래로 나뉘어 있었습니다 — 업무·회의·관찰기록·반 편성이 붙어 있는 `terms`,
셔틀의 `정규학기`/`여름캠프2` 라는 **글자**, 그리고 어제 재무용으로 따로 만든 표. 셋이 따로
놀면 **"지금 학기"가 화면마다 다릅니다.**

- 가장 많이 붙어 있는 `terms` 를 뿌리로 삼고 나머지를 여기로 끌어왔습니다. 어제 만든 재무
  학기표는 없앴습니다 — 학기가 두 곳에 있으면 반드시 어긋납니다.
- 학비외 항목과 청구서가 이제 학교 학기에 붙습니다. 지금 것은 전부 **진행중인 학기**로
  들어갑니다.
- 셔틀은 `학기 표가 셔틀 이름을 들고 있는` 방식으로 이었습니다. 노선·정류장·탑승기록이
  전부 그 이름에 매달려 있어서, 한꺼번에 갈아엎으면 지난 기록이 미아가 됩니다.
- **진행중인 학기는 하나뿐**입니다(데이터 쪽에서 막습니다). 둘이 켜져 있으면 화면마다 다른
  학기를 현재로 보게 됩니다. 이미 여러 개가 켜져 있었다면 가장 최근 것만 남깁니다.

**새 학기 시작**

`학기` 화면에 `새 학기 시작` 이 생겼습니다(관리자 전용). 학기 이름·연도·기간을 적고, 무엇을
이어서 쓸지 고르면 됩니다.

- **셔틀 노선·정류장은 복사**합니다. 정규학기 것을 그대로 가져와 조금씩 고쳐 쓰는 방식이라
  처음부터 다시 그리지 않아도 됩니다. 다만 **탑승 배정(아이들)은 복사하지 않습니다** —
  캠프는 타는 아이가 달라집니다.
- 학비외 항목은 고를 수 있습니다. 캠프라면 대개 새로 만듭니다.
- 반 편성·학생 명단·업무·관찰기록·회의는 복사하지 않습니다. 새로 시작하고, 지난 학기 것은
  그대로 남아 학기를 바꿔 볼 수 있습니다.
- **지난 학기 기록은 지워지지 않습니다.** 그때 누가 몇 호차를 탔는지, 무슨 교재를 샀는지가
  전부 그 학기에 매달린 채로 남습니다.
- 새 학기를 만들다 실패하면 이전 학기를 되돌립니다. 진행중인 학기가 하나도 없는 상태가 되면
  거의 모든 화면이 빈 채로 뜹니다.

**학기를 바꿔 보기**

- 셔틀 학기 탭이 **학기 표에서 목록을 받습니다.** 예전에는 코드에 박아두어서, 새 학기를
  만들어도 탭에 나타나지 않았습니다.
- 재무 화면(학비외 항목·인보이스 명단)의 학기 고르개도 같은 학기 목록을 씁니다.

## v0.386.0 - 2026-09-02

**학비외 항목과 청구서를 학기로 묶습니다**

다음 학기에는 교재가 또 달라집니다. 학기를 나누지 않으면 지난 학기 교재가 새 학기 표에 계속
열로 서 있고, 그것을 하나씩 끄는 일이 매 학기 반복됩니다. **끄는 것을 빠뜨리면 안 사는 책이
청구됩니다.**

- 재무 전용 학기 표를 새로 두었습니다. 이미 있는 `terms` 는 사건기록(학기 돌아보기)에 매여
  있어서, 거기에 재무를 얹으면 한쪽을 고칠 때 다른 쪽이 흔들립니다.
- **지금 등록된 항목과 발행된 청구서는 전부 `2026 정규학기` 로 들어갑니다.**
- `학비외 항목` 과 `인보이스 명단` 위쪽에 학기 고르개가 생겼습니다. 학기를 바꾸면 그 학기의
  항목과 청구서만 보입니다.
- 두 화면이 **같은 학기**를 봅니다. 항목 화면은 이번 학기를 보는데 인보이스 명단은 지난
  학기를 보고 있으면, 열이 안 맞는 이유를 아무도 못 찾습니다. 고른 학기는 이 브라우저에
  기억해둡니다.
- 지난 학기를 보고 있으면 `지난 학기를 보는 중` 이 뜹니다. 모르고 지난 학기에 항목을
  등록하면 이번 학기 표에는 나타나지 않고, 왜 없는지 찾기 어렵습니다.
- 지난 학기 청구서는 이번 학기 표에서 `발행됨` 으로 보이지 않습니다. 그러면 이번 학기 것을
  안 보낸 채로 다 보낸 줄 알게 됩니다.
- 발행할 때 **그 학기 항목만** 계산합니다. 학기를 안 걸면 지난 학기 교재까지 청구서에
  붙습니다.

**새 학기 시작하기**

- `학비외 항목 → ＋ 새 학기 만들기` 에서 학기를 만듭니다. **지금 보는 학기의 항목을 복사해서
  시작**할 수 있습니다 — 처음부터 다시 적게 두면 결국 안 하고 지난 학기 것을 그대로 씁니다.
  복사한 뒤 값만 고치면 됩니다.
- 만들면 그 학기로 바로 넘어갑니다. 만들어놓고 안 넘어가면 지난 학기에 계속 등록합니다.
- 현재 학기는 **하나만** 켜집니다(데이터 쪽에서 막습니다). 둘이 켜져 있으면 화면마다 다른
  학기를 보여주게 됩니다.

## v0.385.0 - 2026-09-02

**인쇄가 화면과 같게 나옵니다**

- 인쇄하면 색이 전부 빠져서 남색 머리띠가 하얗게 나오고 그 위의 흰 글자는 아예 사라졌습니다.
  브라우저가 잉크를 아끼려고 **배경색을 기본으로 지우기** 때문입니다. 이 종이는 학부모에게
  가는 것이라 화면에서 본 그대로 나와야 합니다 — 색을 그대로 인쇄하도록 지정했습니다.
- 항목이 많아 두 장이 될 때, 줄이 반으로 잘리지 않게 하고 표 머리줄을 장마다 다시 찍습니다.
  둘째 장만 보면 무슨 칸인지 모르는 일이 없어야 합니다.
- 결제 안내와 꼬리말은 통째로 한 장에 오게 묶었습니다.
- 이 화면은 사이드바가 있는 큰 틀 안에 들어 있어서, 높이 제한 때문에 **둘째 장이 통째로
  사라질 수 있었습니다.** 인쇄할 때 그 제한을 풀어줍니다.

**인보이스를 그 자리에서 봅니다**

- 청구서 번호를 누르면 새 탭이 아니라 **보던 화면 위에 창으로** 뜹니다. 열 장을 훑을 때는
  탭을 열고 닫는 왕복이 일보다 오래 걸립니다.
- 창 안에서 바로 인쇄·PDF 저장이 됩니다. 크게 보고 싶으면 `새 탭 ↗` 이 따로 있습니다.
- 창 안쪽은 인쇄용 화면을 그대로 불러옵니다. 미리보기와 종이가 같은 화면이어야 "화면에서 본
  것과 인쇄된 것이 다르다"가 생기지 않습니다.

**부서 판정을 한 곳으로 모았습니다**

- 6학년을 중고등부로 옮겼는데 **학생 조회에서는 계속 초등부로 나왔습니다.** 판정이 세 곳에
  흩어져 있어서 한 곳만 고치고 나머지를 잊었기 때문입니다. 화면마다 다른 답이 나오면 어느
  쪽이 맞는지 아무도 모릅니다.
- 이제 모든 화면이 `departmentOf` 하나만 씁니다. 학생 조회가 스스로 만들던 판정을 지웠습니다.
- **학년이 읽히면 학년이 먼저입니다.** 명부의 부서 칸은 예전 자료에서 들어온 값이라 기준이
  바뀌면 어긋난 채 남습니다. 그래서 명부를 아직 안 고쳤어도 6학년은 중고등부로 보입니다.
  부서 칸은 학년을 읽을 수 없는 줄에서만 씁니다.
- `npm run build` 가 판정을 다시 만든 코드를 찾아 빌드를 실패시킵니다. 같은 실수가 다시
  일어나지 않게 하는 것이 이번 수정의 요점입니다.

## v0.384.0 - 2026-09-02

**학비외 항목을 원하는 순서로 봅니다**

- 표의 머리줄(`분류` · `이름` · `단가` · `기본 대상`)을 누르면 그 기준으로 정렬합니다. 한 번 더
  누르면 반대 방향입니다.
- 항목이 스무 개를 넘으면 눈으로 훑는 것이 안 됩니다. 무엇을 확인하려느냐에 따라 보고 싶은
  순서가 다릅니다 — 반별로 붙일 때는 대상, 이름이 헷갈릴 때는 이름, 교재끼리 견줄 때는 분류.
- 대상은 **학년 먼저, 그 다음 반**으로 줄 세웁니다. 대상이 없는 항목(개별로만 붙이는 것)은 맨
  뒤로 보냅니다 — 중간에 끼어 있으면 학년 흐름이 끊깁니다.

**발행을 되돌릴 수 있습니다**

- 인보이스 명단의 청구서 번호 옆 `↩` 로 발행을 취소합니다. 학생 창에서도 됩니다.
- **지우지 않습니다.** 상태를 취소로 바꾸고 누가·언제·왜 취소했는지를 남깁니다. 지워버리면
  나중에 "그 청구서 어디 갔냐"는 물음에 답할 방법이 없고, 번호도 비어 버립니다.
- 취소하면 그 학생은 다시 `미발행` 이 되어, 항목을 고친 뒤 새로 발행할 수 있습니다. **새
  번호가 붙습니다** — 같은 번호를 다시 쓰면 학부모가 받은 두 장이 같은 번호가 됩니다.
- 이미 **받은 돈이 붙어 있으면** 그냥 취소되지 않습니다. 그 입금이 어디에도 안 붙은 채로 남아
  장부가 안 맞기 때문입니다. 수납을 먼저 정리하라고 알려주고, 그래도 하겠다고 하면 한 번 더
  확인한 뒤에 진행합니다.
- 이미 올톡페이로 내보낸 청구서를 취소할 때는 **올톡페이 쪽도 취소해야 한다**고 알려줍니다.
  여기서 취소해도 그쪽은 그대로입니다.
- 취소된 인보이스를 열면 사유와 취소한 사람이 화면에 함께 보입니다(종이에는 나가지 않습니다).

## v0.383.0 - 2026-09-02

**6학년은 중고등부입니다**

- 이 학교는 6학년을 중고등부에서 운영합니다. 그동안은 학년 숫자만 보고 7학년부터 중고등부로
  나눠서, 6학년 9명이 초등부로 묶여 있었습니다.
- 명부의 6학년 학생과 반을 중고등부로 옮깁니다. 학년 표기가 `6` · `6학년` · `G6` 으로 섞여
  있어서 숫자만 뽑아 봅니다.
- 학년으로 부서를 추측하는 규칙도 6학년부터 중고등부로 고쳤습니다. 명부에 부서가 적혀 있으면
  그 값이 먼저입니다 — 추측은 부서 칸이 비어 있는 예전 줄에만 씁니다.
- 운영 대시보드·시간표가 보여주는 부서에 **중고등부를 더했습니다.** 이걸 안 하면 부서를 옮긴
  6학년이 그 화면들에서 조용히 사라집니다.

**학비외 항목을 부서별로 나눕니다**

- 초등과 중고등은 사는 교재가 아예 다릅니다. 한 목록에 섞어두면 등록할 때마다 남의 부서
  항목을 지나쳐 읽어야 하고, 인보이스 표의 열이 두 배로 늘어납니다.
- `학비외 항목` 에 `초등부 · 중고등부 · 공통` 탭이 생겼습니다. 새 항목은 지금 보고 있는
  부서로 시작합니다 — 매번 고르게 두면 결국 잘못된 부서에 등록합니다.
- `공통` 은 교복처럼 **학교 전체가 사는 것**입니다. 초등에서 보든 중고등에서 보든 함께
  나옵니다.
- 인보이스 명단도 부서 탭에 맞는 항목만 열로 세웁니다. 초등 표에서 만든 교재가 중고등 표에
  나타나지 않습니다.
- **부서가 다르면 아이별로 체크해둔 것이 있어도 붙지 않습니다.** 중고등 교재를 초등 아이에게
  실수로 체크해둔 것이 그대로 청구되면 안 됩니다.
- 지금까지 등록한 항목은 전부 초등부 것으로 넘깁니다(중고등부는 이제 시작합니다).

## v0.382.0 - 2026-09-02

**데모 학생과 실제 학생을 갈라놓았습니다**

오리엔테이션용 가짜 학생·가짜 반이 실제 표에 함께 들어 있고 `is_demo` 칸 하나로만 갈립니다.
읽는 곳마다 그 조건을 걸어야 하는데, 읽는 곳이 이미 일흔 곳이 넘어 **사람의 기억으로는 못
지킵니다.** 실제로 54곳이 빠져 있었습니다.

- `wr_students` · `wr_classes` 를 읽는 54곳에 조건을 걸었습니다.
  - 행정·관리자 화면은 실제 자료만(`is_demo = false`).
  - 교사 화면은 **보는 사람에 따라** 갈립니다 — 데모 계정에는 데모 자료만, 실제 계정에는
    실제 자료만. 한쪽으로 고정하면 데모 계정이 빈 화면을 보거나 실제 명단을 보게 됩니다.
- 새로 만들 때는 필터가 아니라 **값으로** 못박습니다(`is_demo: false`). 학생 등록·대량 등록·
  명부 가져오기·반 만들기 네 곳입니다. 기본값에 기대면 기본값이 바뀌는 날 조용히 섞입니다.
- 토큰으로 여는 화면(도착체크·운영 대시보드)은 보안규칙을 우회하는 자리라 특히 위험했습니다.
  거기서도 데모 학생이 빠집니다.

**빌드가 대신 봅니다**

- `npm run build` 가 `scripts/check-demo-isolation.mjs` 를 돌려, 조건을 빠뜨린 읽기를 찾아
  **빌드를 실패시킵니다.** 다음에 새 화면을 만들 때 같은 실수를 되풀이하지 않게 하는 것이
  이번 작업의 핵심입니다.
- 정말 걸면 안 되는 자리는 바로 위 줄에 `// demo-ok: 이유` 를 적으면 넘어갑니다. 끄는 것은
  쉬워야 하지만 왜 껐는지는 남아야 합니다.

**업무보드 학생 검색에 얼굴이 나옵니다**

- 검색 결과 왼쪽에 작은 사진이 붙습니다. 전화를 받거나 아이를 인계할 때 이름보다 얼굴이
  빠릅니다.
- 공용 명부 뷰에 사진 경로를 더했습니다. 업무보드 검색은 교직원 누구나 여는 화면이라 원본
  표를 못 읽고 이 뷰만 봅니다.
- 사진 주소는 **지금 보이는 것만** 받고, 한 번 받은 것은 기억합니다. 137명 것을 미리 다
  받으면 창이 그만큼 늦게 열립니다.

**데이터 쪽도 잠갔습니다**

- `is_demo` 는 **만든 뒤에 바꿀 수 없습니다.** 실제 학생을 데모로 바꾸면 명부에서 조용히
  사라지고, 반대로 바꾸면 가짜 학생이 실제 명단에 나타나는데 둘 다 화면에는 오류로 보이지
  않습니다.
- `demo_isolation_check` 로 섞였는지 언제든 확인합니다. 데모 학생이 셔틀 배정·청구서·사진·
  하원수단에 엮여 있으면 여기 잡힙니다. 데모 학생 수·데모 반 수 말고는 전부 0이어야 합니다.

## v0.381.0 - 2026-09-02

**머리가 잘리던 문제**

원인은 **눈 사이 거리로 머리 크기를 너무 작게 잡고 있던 것**입니다. 머리를 작게 보면 그만큼
과하게 확대되고, 정수리가 사진 밖으로 나갑니다.

- 머리 높이(정수리~턱)는 두 눈동자 사이 거리의 약 **3.6배**입니다. 어른은 눈 사이 62mm에
  머리 230mm, 아이는 55mm에 200mm쯤이라 비율은 거의 같습니다. 3배로 두었던 것을 3.6배로
  고쳤습니다.
- 머리 크기를 **두 가지로 재고 큰 쪽**을 씁니다. 눈 사이 거리는 고개를 옆으로 돌리면 짧게
  잡히는데, 얼굴 상자는 그때도 줄지 않습니다. 크게 잡아 틀리면 여백이 조금 넓을 뿐이고,
  작게 잡아 틀리면 잘립니다.
- 머리가 사진에서 차지하는 비율을 70% → **66%** 로 낮췄습니다. 여권 규격은 45mm 사진에서
  얼굴 길이를 32~36mm(71~80%)로 정하는데, 위쪽을 택하면 자동으로 잡은 크기가 조금만 작게
  나와도 정수리가 나갑니다. 조금 넉넉한 사진은 학생증에서 티가 안 나지만, 정수리가 잘린
  사진은 한눈에 보입니다.
- 눈높이를 44% → **40%** 로 올렸습니다. 머리 위 여백이 10%가 되는 자리입니다.
- 원본에 머리 위 여백이 거의 없는 사진은, 자르는 자리가 그림 밖으로 조금 나가는 것을
  허용하고 그 자리를 **흰색으로 채웁니다.** 억지로 안쪽에 붙이면 정수리가 잘립니다.

지금 값으로는 머리 66% · 위 여백 10% · 아래 24% · 눈높이 40%가 나옵니다.

## v0.380.0 - 2026-09-02

**이름이 하나도 안 붙던 진짜 원인**

- **맥에서 끌어온 파일명은 한글이 자모로 나뉘어(NFD) 옵니다.** `고서윤` 이 글자로는 같아
  보여도 명부의 `고서윤` 과 **다른 문자열**이라 어디에도 안 걸립니다. 눈으로는 절대 못 찾는
  종류의 어긋남입니다 — 화면에도 파일명은 멀쩡히 `고서윤.jpg` 로 보입니다.
- 파일명과 명부 이름을 **같은 방식으로 합쳐놓고** 비교합니다. 이제 맥에서 그대로 끌어다
  놓아도 붙습니다.

**동명이인은 반 표기로 가릅니다**

- `김재이(G3J).jpg` 처럼 괄호 안에 반을 적어두면 그것으로 가릅니다. 그전에는 괄호 안을
  **버리고** 있어서, 애써 적어둔 표기가 아무 소용이 없었습니다.
- 반 이름이 힌트로 **시작하기만 해도** 같은 것으로 봅니다(`G3J` → `G3JA`). 줄여 적는 것이
  자연스러운데 전체를 적으라고 하면 결국 안 적게 됩니다.
- 학년만 적어도(`김재이(3학년)`) 가려집니다.
- 표기로도 안 갈리면 후보만 보여주고 사람이 고릅니다. 잘못 붙이면 다른 아이 학생증에 남의
  얼굴이 들어갑니다.
- 명부 이름에 영문이 괄호로 붙은 줄(`강여명(Ryeomyeong Kang)`)도 대조합니다.

**두 눈으로 맞춥니다 — 학생증용**

신분증 사진은 사실 **눈높이로 맞춥니다.** 정수리는 머리 모양과 머리카락에 따라 들쭉날쭉하지만
눈은 그렇지 않아서, 눈을 기준으로 잡아야 아이들 사진이 실제로 똑같아집니다.

- 얼굴 인식 모델(BlazeFace)을 붙였습니다. 브라우저에 들어 있는 얼굴 찾기는 쓸 수 있는 곳이
  거의 없어서, 실제로 도는 모델을 한 번 받아 씁니다(약 230KB, 처음 한 번만).
- 두 눈을 찾으면 세 가지를 한 번에 정합니다.
  - **크기** — 눈 사이 거리로 머리 높이를 가늠해, 머리가 사진 높이의 70%가 되게.
  - **높이** — 눈높이가 사진 위에서 44%에 오도록. 여권 규격이 정하는 자리입니다.
  - **기울기** — 두 눈을 잇는 선이 수평이 되도록 돌립니다. 고개를 살짝 기울이고 찍은 아이가
    꼭 몇 명 있고, 한 판을 늘어놓으면 그 몇 장만 눈에 띕니다.
- 눈을 못 잡으면 얼굴 상자로, 그것도 안 되면 배경 빼기로, 다 안 되면 가운데로 둡니다.
  사진마다 `눈` · `얼굴` · `배경` · `기본` 중 무엇으로 잡았는지 표시합니다.
- 미리보기의 초록 선이 눈높이입니다. 눈이 그 선에 오면 맞습니다.
- 미리보기와 저장이 **같은 함수**로 그립니다. 따로 만들면 화면에서 본 것과 저장된 것이
  조금씩 달라지고, 그 차이는 늘 인쇄한 뒤에 발견됩니다.
- 기울여 자를 때 모서리에 생기는 빈 곳은 흰색으로 채웁니다.

## v0.379.0 - 2026-09-02

**하나도 안 붙던 문제**

- 사진 화면이 명부를 `status = 'active'` 인 줄만 읽고 있었습니다. 이 표에는 `active` 와
  `재학` 이 섞여 있어서, 한쪽만 보면 **명부가 통째로 비고** 사진마다 "명부에서 못
  찾았습니다"가 뜹니다. 원인은 사진이 아니라 명부인데 화면에서는 그렇게 보이지 않습니다.
  두 값을 모두 봅니다.
- 머리에 <b>명부 N명</b> 을 적어둡니다. 0명이면 빨갛게 경고합니다.
- 붙지 않은 사진에는 <b>파일명에서 무엇을 이름으로 읽었는지</b>를 같이 보여줍니다. 안 붙는
  이유가 파일명인지 명부인지 바로 보입니다.
- 명부 이름에 영문 표기가 괄호로 붙은 줄(`강여명(Ryeomyeong Kang)`)도 대조합니다. 그전에는
  `강여명.jpg` 가 어디에도 안 걸렸습니다.

**얼굴을 찾아 규격대로 자릅니다**

이제 모든 사진이 **같은 크기·같은 구도**로 나옵니다. 규격이 정하는 것은 두 가지입니다 —
머리(정수리~턱)가 사진 높이의 <b>70%</b>, 머리 위 여백이 <b>10%</b>. 이 둘을 고정하면 아이마다
찍힌 거리가 달라도 결과는 같아집니다. 학생증 한 판을 뽑았을 때 눈에 띄는 것이 그 차이입니다.

- 자리를 잡는 방법을 두 가지 두고 차례로 시도합니다.
  - **얼굴 찾기** — 브라우저가 지원하면 가장 정확합니다. 얼굴 상자는 이마부터 턱까지라
    머리카락 위쪽이 빠져 있어서, 그 위로 얼굴 높이의 30%를 더해 정수리를 잡습니다.
  - **배경 빼기** — 졸업앨범 사진은 배경이 한 가지 색입니다. 위에서 내려오다 배경이 아닌
    점이 처음 나오는 줄이 정수리입니다. 모델 없이도 꽤 맞습니다. 배경이 얼룩덜룩하면
    스스로 못 믿겠다고 하고 넘어갑니다.
- 사진마다 어떻게 잡았는지(`얼굴` · `배경` · `기본`)를 표시합니다. `기본` 은 둘 다 못 찾아
  가운데로 둔 것이라 사람이 봐야 합니다.
- 위쪽에 <b>전체 축소·확대·↑·↓</b> 를 두었습니다. 한 판을 뽑았을 때 눈에 띄는 것은 개별
  사진이 아니라 줄이 맞는가라서, 전체를 조금씩 함께 움직일 수 있어야 합니다.
- <b>자동으로 다시 잡기</b> 로 손으로 밀어놓은 것을 되돌릴 수 있습니다.

## v0.378.0 - 2026-09-02

**폴더를 끌어다 놓아도 아무 일이 없던 문제**

- 폴더를 놓으면 브라우저가 파일 목록을 **비워서** 넘깁니다. 폴더 안을 보려면 한 겹씩 직접
  들어가야 하는데 그렇게 하지 않고 있었습니다. 그래서 사진 폴더를 통째로 끌어다 놓으면
  오류도 안 뜨고 목록도 안 생겨 **되고 있는지 아닌지 알 수가 없었습니다.**
- 이제 폴더 안을 훑습니다. 학년별로 폴더를 나눠두셨어도 안쪽까지 찾아 들어갑니다.
- 폴더 목록은 한 번에 100개까지만 돌려주는 방식이라, 빈 배열이 올 때까지 반복해서 읽습니다.
  한 번만 읽으면 100장이 넘는 폴더에서 뒤쪽 사진이 통째로 빠집니다.
- 끌어다 놓기가 안 되는 환경을 위해 `📁 폴더 고르기` 단추도 두었습니다.

**진행 상황이 보입니다**

- 놓는 즉시 `폴더를 여는 중` 이 뜨고, 이어서 `사진을 읽는 중 12 / 137장` 과 막대가 나옵니다.
  137장을 읽는 데는 시간이 걸리는데, 그동안 화면이 그대로면 다시 끌어다 놓게 되고 같은
  사진이 두 벌 생깁니다.
- 다 읽으면 몇 장을 읽었고 그중 몇 장에 학생이 붙었는지 알려줍니다.

**넣지 않은 파일을 말해줍니다**

- 그림이 아닌 파일은 이름과 이유를 함께 보여줍니다. 조용히 빼면 "몇 장이 없네"가 되고, 왜
  없는지는 알 수 없습니다.
- HEIC(아이폰 사진)는 브라우저가 열지 못합니다. 그냥 실패로 두지 않고, JPG로 내보내 달라고
  적어줍니다.

## v0.377.0 - 2026-09-02

**학생 사진 (여권 규격)**

졸업앨범 사진을 그대로 두면 크기도 구도도 제각각이라 학생증·도서관 카드에 못 씁니다.
**넣을 때 여권 규격으로 잘라서** 보관합니다 — 35×45mm, 300dpi 기준 413×531px. 쓸 때 자르면
쓰는 화면마다 자르는 코드가 생기고, 그러면 화면마다 조금씩 다르게 잘립니다.

- `학생 조회 → 📸 사진 등록` 에 폴더를 통째로 끌어다 놓으면 됩니다. **파일명으로 아이를
  찾아** 붙이고, 규격에 맞게 잘라 미리 보여줍니다. 훑어본 뒤 한 번에 저장합니다.
- 파일명은 `고서윤.jpg` 처럼 이름만 있어도 되고, `2학년 고서윤 (2).JPG` `01_고서윤.png`
  `Jenny Go.jpeg` 처럼 앞뒤에 뭐가 붙어도 찾아냅니다. 한글 이름과 영문 이름 둘 다 봅니다.
- 동명이인이면 자동으로 붙이지 않고 **후보를 보여주고 고르게 합니다.** 김재이가 두 명인데
  한 명에게 붙여버리면, 다른 아이 카드에 남의 얼굴이 들어갑니다.
- 자르는 자리는 자동으로 잡습니다. 브라우저가 얼굴을 찾아줄 수 있으면 얼굴 높이가 사진의
  70%가 되도록 맞추고, 못 찾으면 가운데 위쪽에서 시작합니다. 어색하면 **끌어서 옮기고 휠로
  크기**를 맞춥니다. 앨범 사진은 대개 같은 구도라, 한 장을 맞춘 뒤 `전체 적용` 을 누르면
  나머지도 대개 맞습니다.
- **원본은 저장하지 않습니다.** 자른 것만 올립니다. 원본은 앨범 폴더에 이미 있고, 여기 또
  쌓으면 용량이 몇 배가 되며 지울 때도 두 번 지워야 합니다.
- 아래에 **아직 사진이 없는 학생** 명단이 나옵니다. 이게 없으면 137명 중 누가 빠졌는지
  세어봐야 압니다.
- 같은 아이에게 두 장이 붙었거나 이미 사진이 있는 아이를 덮어쓰는 경우, 저장하기 전에
  표시해 줍니다.

**사진이 보이는 곳**

- 학생 프로필 맨 위. 전화를 받거나 아이를 인계할 때 이름보다 얼굴이 빠릅니다. 눌러서 바로
  바꿀 수 있습니다.
- 학생 조회 카드의 작은 사진. 137장의 주소를 **한 번에 묶어** 받아옵니다 — 한 명씩 받으면
  137번을 부르게 되고 목록이 그만큼 늦게 뜹니다.

**볼 수 있는 사람**

- 아이 얼굴이라 공개 주소를 만들지 않습니다. 비공개 버킷에 두고, 볼 때마다 한 시간짜리
  주소를 새로 받습니다.
- 교직원(회사 계정으로 로그인한 사람) 전체가 보고 올릴 수 있습니다. 담임도 행정도 아이
  얼굴을 확인할 일이 있고, 하원 인계나 전화 응대에서 특히 그렇습니다.

## v0.376.0 - 2026-09-02

**청구서와 맞춰보기**

인보이스 명단에 `🔍 청구서와 대조` 가 생겼습니다. 올톡페이에서 받은 청구서관리목록 엑셀을
올리면, 이미 보낸 청구서 금액과 지금 등록한 항목의 합계를 아이별로 맞춰봅니다.

- **아무것도 저장하지 않습니다.** 읽고 보여주기만 합니다. 항목 등록이 다 맞은 뒤에 수납 탭에서
  입금을 넣습니다. 순서를 바꾸면, 틀린 등록 위에 입금까지 얹혀 무엇이 틀렸는지 알 수 없게
  됩니다.
- 네 갈래로 나눠 보여줍니다.
  - `금액 다름` — 등록이 잘못됐거나, 청구할 때 금액을 조정한 것입니다.
  - `금액 같음` — 맞은 것.
  - `명부에 없음` — 청구서에는 있는데 명부에서 못 찾은 이름.
  - `청구서 없음` — 우리 표에는 금액이 있는데 청구서가 안 나간 아이. 항목이 아예 없는 아이는
    여기 넣지 않습니다. 넣으면 목록이 명부 전체가 되어 아무도 안 봅니다.
- **한 아이의 여러 줄은 합쳐서** 비교합니다. 교재비와 교복을 따로 청구하고 `2차주문` 으로 또
  나가기 때문에, 줄 하나씩 비교하면 전부 안 맞는 것으로 나옵니다.
- 결제중단된 줄은 다시 청구한 줄과 겹치므로 합계에서 뺍니다.
- 금액이 다를 때는 **이름 칸에 붙은 사연을 같이 보여줍니다**. `강하라/치과진료비12,900원포함`
  처럼 다른 이유가 대개 거기 적혀 있습니다.
- 이름을 누르면 그 학생의 항목 창이 바로 열립니다. 다른 부서·다른 반에 있어도 열립니다 -
  대조 결과에서 눌렀는데 아무 일도 안 일어나면 고치러 갈 방법이 없습니다.

## v0.375.0 - 2026-09-02

**올톡페이 실제 파일에 맞췄습니다**

올톡페이에서 내려받은 `청구서관리목록` 178줄을 그대로 읽어 맞췄습니다. 그전까지는 칸 이름을
짐작으로 적어두었는데, 실제 파일과 세 군데가 어긋나 있었습니다.

- 칸 이름이 `고객명 · 청구핸드폰 · 청구사유 · 청구금액 · 상태 · 수납일자 · 승인번호` 입니다.
  `고객명` 은 예전 대조 목록의 어느 이름에도 안 걸려서, **입금자 이름이 통째로 비어 있었습니다.**
- 날짜 칸을 **적어준 순서대로** 찾도록 고쳤습니다. 전에는 먼저 만나는 칸을 썼는데, 이 파일은
  `등록일자` 가 `수납일자` 보다 앞에 있어서 **돈 들어온 날이 아니라 청구서 만든 날**이 들어갔습니다.
- **상태가 결제완료인 줄만** 넣습니다. 178줄 중 결제완료는 87줄이고 나머지 91줄은 아직 안 낸
  것(발송완료 71 · 결제중단 20)입니다. 그것까지 넣으면 안 받은 돈을 받은 것으로 기록하게 됩니다.
- 겹침 방지 열쇠를 **승인번호**로 바꿨습니다. 파일이름과 줄번호로 만들면, 내일 다시 받은 파일은
  줄 위치가 밀려 전부 새 입금으로 들어옵니다.

**입금과 학생을 붙이는 방법**

- 청구할 때 쓴 **연락처를 먼저** 봅니다. 이름 칸에는 사연이 붙어 있지만(`강하라/치과진료비
  12,900원포함`, `조장훈(13,000잔돈차감)`) 번호는 그대로입니다. 실제 파일에서 이름에 메모가
  붙은 것이 13줄이었습니다.
- 같은 연락처로 여러 건이면(형제) 금액으로 한 번 더 좁히고, 그래도 안 되면 후보만 보여주고
  사람에게 맡깁니다. 잘못 붙이면 낸 아이가 미납으로 남습니다.
- 이름을 대조할 때는 괄호·빗금·쉼표 뒤를 떼어냅니다. `송우진,윤진` 은 앞의 아이에게 붙고,
  나머지는 사람이 확인합니다.

**발송 엑셀 머리글을 올톡페이 표기와 같게**

- `고객명 · 청구핸드폰 · 청구금액 · 청구사유 · 만료일자` 로 바꿨습니다. 화면 표기와 같은 말을
  쓰면 올릴 때 어느 칸이 어느 칸인지 다시 맞춰볼 일이 없습니다. 양식이 다르면 여전히 화면에서
  고칠 수 있습니다.

## v0.374.0 - 2026-09-02

**인보이스에서 올톡페이 청구로 이어집니다**

- 올톡페이에는 외부에서 부를 수 있는 창구(API)가 없습니다. 가맹점이 청구서를 보내는 길은
  화면에서 한 건씩 넣거나 **엑셀을 올려 한 번에 보내는 것** 둘뿐입니다. 그래서 여기서 하는
  일은 "올려도 되는 파일을 정확히 만들어 주는 것"까지입니다 — 발송 자체는 사람이 올톡페이
  화면에서 합니다.
- 인보이스 명단에 `📤 올톡페이 청구` 가 생겼습니다. 고른 학생이 있으면 그 학생들만, 없으면
  지금 보이는 명단에서 **아직 안 보낸 것만** 잡습니다. 전부를 기본으로 잡으면 이미 보낸
  청구서까지 다시 올리게 됩니다.
- 발행 직후에도 바로 이어갈 수 있게 띄웁니다. 발행은 종이를 만든 것일 뿐 청구가 나간 것이
  아닌데, 여기서 끊기면 발행만 해놓고 안 보내는 일이 생깁니다.
- 파일을 만들기 전에 세 가지를 먼저 보여줍니다. 올린 뒤에는 되돌릴 수 없습니다.
  - 보호자 연락처가 없어 **빠지는 아이**. 빈 칸으로 섞어 올리면 그 아이만 조용히 청구가
    안 갑니다. 이름을 하나하나 적어줍니다.
  - 보호자 번호가 **같은 묶음**(형제). 합치지 않으면 그 집에 청구서가 두 번 갑니다. 합칠지는
    선택이고, 몇 쌍인지는 항상 셉니다. 합치면 납부기한은 빠른 쪽에 맞춥니다.
  - **이미 내보낸 청구서**. 다시 올리면 학부모에게 두 번 갑니다.
- 인보이스마다 `발행` 과 `발송` 을 따로 표시합니다. 같은 표시로 두면 발행만 해놓고 안 보낸
  것을 아무도 모릅니다.
- 엑셀 머리글(고객명·휴대폰번호·청구금액…)은 화면에서 고칠 수 있습니다. 올톡페이 업로드
  양식이 가맹점마다 다를 수 있어서, 한 번 맞춰두면 이 브라우저에 남습니다.
- 휴대폰 번호는 숫자만 남겨 넣되 **글자로** 굳힙니다. 숫자로 두면 엑셀이 앞의 0을 지워
  `01012345678` 이 `1012345678` 이 됩니다.

**인보이스가 학생 명부를 그대로 읽습니다**

- 청구서는 명부의 **보호자 연락처**로 나갑니다. 지금까지 인보이스 화면은 이름과 학년만
  알고 있어서, 연락처가 비어 있어도 발행이 되고 발송에서만 조용히 빠졌습니다. 이제 표 위에
  연락처 없는 학생 수와 이름이 먼저 뜹니다.
- 학생 창을 열면 학번·보호자 연락처·명부에 적힌 악기가 함께 나오고, `명부 열기` 로 바로
  건너갑니다.
- 명부의 악기와 같은 이름의 항목에는 `명부와 같음` 이 붙습니다. 악기는 아이마다 달라서, 이
  대조가 없으면 첼로 아이에게 바이올린 대여료가 붙어도 아무도 모릅니다.
- 발행 시점의 보호자 연락처를 인보이스에 굳혀둡니다. 명부의 번호가 나중에 바뀌어도 그때
  어디로 청구했는지가 남아야 대사가 됩니다.
- 학생 프로필에 `💳 학비외 청구` 칸이 생겼습니다. 청구서 번호·금액·완납/일부/미납·발송
  여부가 한 줄씩 나옵니다. 학부모가 "그거 냈는데요" 하고 전화하면 통화 중에 답이 나와야
  하는데, 재무 화면으로 건너가 이름을 다시 찾게 두면 못 합니다. 재무 열쇠를 가진 사람에게만
  보입니다.

**인보이스 명단을 초등/중고등으로 먼저 가릅니다**

- 위쪽에 부서 탭이 생겼습니다. 초등과 중고등은 반 이름 체계도, 사는 교재도 다릅니다. 한 표에
  섞으면 열이 서로의 몫까지 늘어나 가로로 한없이 길어지고, "2학년"이 초등 2학년인지 중2인지도
  헷갈립니다.
- 부서 안에서 **반이 먼저**, 그 다음이 학년입니다. 교재는 반 단위로 붙고 재무 담당자가 실제로
  여는 것도 반입니다. 학년은 악기처럼 반을 넘어 흩어지는 것을 볼 때 씁니다.
- 부서가 안 적힌 학생이 있으면 `부서 미상` 탭이 뜹니다. 조용히 사라지면 그 아이만 청구가
  안 됩니다.

## v0.373.0 - 2026-09-01

**분류를 직접 등록해서 씁니다**

- 지금까지 분류는 항목에 적힌 글자에서 거꾸로 모아 만들었습니다. 그래서 **항목이 하나도 없는
  분류는 존재할 수가 없었습니다** — `악기`를 먼저 세워두고 그 아래 첼로·바이올린을 채워 넣는
  순서로는 일을 못 했습니다.
- `학비외 항목 → 분류 관리` 에서 분류를 만들고 끕니다. 만들면 새 항목 폼의 분류 칸에 바로
  들어갑니다 — 만들자마자 그 분류로 항목을 만들게 되니까요.
- 쓰는 항목이 있는 분류는 끄지 못하게 막습니다. 끄면 그 항목들이 어느 분류에도 안 속하게
  됩니다.
- 예전에 항목에만 쓰이고 등록은 안 된 분류는 노란색으로 표시하고, 한 번 눌러 등록할 수
  있습니다.
- 분류 칸은 여전히 **직접 적을 수도** 있습니다. 등록된 것이 먼저 뜨고, 없는 이름을 적으면
  그대로 씁니다.

**이름을 누르면 그 학생만 크게 봅니다**

- 인보이스 명단에서 아이 이름을 누르면 창이 열립니다. 항목이 **분류별로 펼쳐져** 나오고,
  눌러서 넣고 빼고 수량을 고칩니다. 아래에 합계가 실시간으로 뜹니다.
- 표는 여럿을 훑을 때 좋고, 한 아이를 꼼꼼히 볼 때는 좁습니다. 악기처럼 아이마다 다른 것을
  고를 때는 분류별로 펼쳐놓고 보는 편이 낫습니다.
- 기본 세트로 들어온 것과 사람이 직접 넣은 것을 구분해 적습니다.
- 창 안에서 `이 학생만 발행` 으로 바로 갈 수 있습니다(발행 전 확인 창은 그대로 거칩니다).

**마이그레이션을 한 파일로 합쳤습니다**

- `outputs/재무_전체.sql` 하나로 학비외 항목 · 인보이스 · 수납 · 분류를 전부 겁니다. 이미
  일부를 실행했어도 그대로 다시 돌리면 됩니다 — 있는 것은 건너뛰고 없는 것만 만듭니다.

## v0.372.1 - 2026-09-01

**재무 열쇠만 주면 "변경할 값이 없습니다"로 막혀 있었습니다**

- 저장 전에 "바꿀 값이 하나라도 있는가"를 확인하는 자리에 **재무 열쇠가 빠져 있었습니다.**
  직위나 이름을 함께 고치면 통과했고, 열쇠만 주면 400으로 거절됐습니다. 화면에는 그 이유가
  드러나지 않아 권한 설정이 잘못된 것처럼 보였습니다.

**사유를 묻지 않습니다**

- 열쇠를 줄 때마다 이유를 물었는데, 재무 담당자라서 주는 것이라 매번 같은 말을 적게 됩니다.
  같은 말만 적히는 칸은 곧 아무 뜻도 없어집니다. 기록에 필요한 "누가 언제 누구에게"는 이미
  전부 남습니다.
- 회수할 때는 한 번 묻습니다 — 열쇠를 뺏으면 그 사람 화면에서 재무 메뉴가 통째로 사라집니다.

**주면 바로 보입니다**

- 사이드바 메뉴는 서버에서 그려집니다. 열쇠를 바꾼 뒤 화면을 다시 그리도록 해서, 내 열쇠를
  준 경우 재무 메뉴가 그 자리에서 나타납니다. 예전에는 새로고침해야 보여서 "줬는데 왜 안
  보이지"가 됐습니다.

## v0.372.0 - 2026-09-01

**수납 — 들어온 돈을 인보이스에 붙입니다 (A·B)**

- 인보이스를 보내는 데서 끝나면 **"누가 안 냈나"에 답할 수 없습니다.** 그러면 결국 통장을
  눈으로 훑게 되고, 눈으로 훑는 일은 매번 하지 않게 되고, 안 하면 미납이 쌓입니다.
- `재무 → 수납` 에서 올톡페이 엑셀(.xlsx · .csv)을 올리면 첫 시트를 읽습니다. 칸 이름은
  은행·서비스마다 다르므로 **날짜 · 금액 · 입금자 · 적요** 뜻의 칸을 이름이 조금 달라도
  찾아냅니다. 엑셀 날짜가 문자열이든 일련번호든 같게 읽습니다.
- **바로 저장하지 않습니다.** 읽은 줄을 화면에 세워두고 사람이 한 번 본 뒤 넣습니다.
- 대사는 확실한 것부터 봅니다. ① 적요에 인보이스 번호가 있으면 그것 ② 남은 잔액과 금액이
  정확히 같은 인보이스가 하나뿐이면 그것 ③ 입금자명이 학생 이름과 같고 잔액이 남은 것이
  하나뿐이면 그것.
- **하나로 좁혀지지 않으면 아무것도 고르지 않습니다.** 후보만 별표로 올려 사람이 고르게
  합니다. 어림짐작으로 붙이면 틀린 곳을 아무도 못 찾습니다.
- 같은 파일을 두 번 올려도 같은 줄은 한 번만 들어갑니다(파일명+순번+날짜+금액+입금자).
- **부분 납부와 잔액**을 함께 봅니다. 잔액은 저장하지 않고 `청구액 − 입금 합`으로 그때그때
  냅니다 — 같은 사실을 두 곳에 두면 반드시 어긋납니다.
- 기한이 지난 미납은 붉게 뜹니다. 못 붙인 입금도 따로 셉니다.
- 현금·계좌이체는 손으로 한 건씩 넣습니다. 인보이스를 고르면 **잔액이 기본값**으로 채워집니다.
- 17가지로 검증했습니다 — 잔액·부분납부·완납, 번호/금액/이름 매칭, 금액이 겹칠 때 안 고르는지,
  완납된 것이 후보에서 빠지는지, 날짜·금액 표기 네 가지.

**발행 전 검토 (D)**

- 발행 버튼이 이제 **확인 창**을 먼저 엽니다. 보내는 순간 되돌릴 수 없고, 학부모가 받은 뒤에
  고치면 신뢰가 깎입니다.
- 창에는 인원·합계·기한과 아이별 항목이 나오고, **같은 반 아이들과 금액이 다른 아이**를
  짚어줍니다. 맞는 경우도 많지만(악기·수리) 빠뜨린 것도 이 자리에서 드러납니다.
- 평균이 아니라 **최빈값**과 견줍니다. 한 명이 악기를 사면 평균이 통째로 끌려가서 멀쩡한
  아이들까지 전부 '다름'으로 표시됩니다. 같은 금액이 두 명 이상일 때만 '평소'라고 부릅니다.
- 이미 발행된 아이에게는 "한 장 더 나갑니다"라고 적습니다.

**발주 목록 (F)**

- 명단 표 합계 줄의 개수가 그대로 발주 수량입니다. `📦 발주 목록` 으로 분류·항목·수량·금액을
  뽑고, 목록을 복사해 서점에 보낼 수 있습니다. 새로 세는 것이 아니라 이미 있는 숫자를 꺼낼
  뿐입니다.

**항목 단가 이력 · 인보이스 취소 사유**

- 단가가 바뀌면 이전 값과 새 값이 자동으로 쌓입니다. 덮어쓰기만 하면 "작년엔 얼마였나"에
  답할 수 없습니다.
- 인보이스에 취소 사유 칸을 뒀습니다. 지우지 않고 남기는 것이 뜻이 있으려면 이유가 함께
  있어야 합니다.

**필요한 마이그레이션**

- `20260901180000_payments.sql` — 걸기 전까지 수납 탭은 열리지 않습니다.

## v0.371.0 - 2026-09-01

**재무에도 개요와 탭이 생겼습니다**

- `개요 · 인보이스 명단 · 학비외 항목 · 납부 항목·할인` 네 탭입니다. 다른 대분류(업무·학교·
  셔틀)와 같은 모양으로 맞췄습니다 — 화면마다 다르게 생기면 사람이 매번 "여기선 어디를
  눌러야 하지"를 다시 배웁니다.
- **개요**는 "지금 어디까지 됐나"에만 답합니다. 받아야 할 금액 · 발행한 금액 · 아직 발행 안 한
  인원 · 쓰는 항목 수, 그리고 **반별 진행**(대상·금액·미발행)과 최근 발행 목록입니다.
- 반별 진행은 **미발행이 많은 반부터** 위에 옵니다. 남은 일이 어디에 몰려 있는지가 다음
  행동을 정합니다.
- 개요의 금액은 명단·발행과 **같은 함수**로 계산합니다. 개요만 따로 세면 숫자가 어긋나고,
  어긋난 개요는 아무도 안 믿습니다.

**명단 표에서 바로 항목을 만듭니다**

- 표 위의 `＋ 여기서 바로 항목 만들기` 를 펴면 분류·이름·단가를 그 자리에서 넣습니다.
  만들면 **그 자리에서 열이 생깁니다** — 새로 고쳐야 보이면 결국 항목 화면으로 건너가게 됩니다.
- 반이나 학년을 보고 있으면 `이 반 기본으로 붙이기`가 함께 뜹니다. 켜두면 그 반 전원에게
  자동으로 들어가고, 나중에 전학 온 아이에게도 붙습니다 — 표에만 칠하는 것과 달리 **규칙으로**
  남습니다.

## v0.370.0 - 2026-09-01

**인보이스를 명단 표로 관리합니다**

- 재무 일은 한 아이가 아니라 **명단 단위**로 돕니다. 반 스무 명이 같은 책을 사는지 확인하고,
  악기만 아이마다 다른 것을 훑고, 누가 아직 발행 안 됐는지 봅니다. 한 명씩 여는 화면으로는
  그 일이 안 됩니다 — 스무 번 열고 스무 번 닫아야 하고, 그러면 빠뜨립니다.
- 그래서 **학생이 행, 항목이 열인 표**로 바꿨습니다. 칸을 눌러 넣고 뺍니다.

**결제·회계 프로그램들이 쓰는 모양을 그대로 가져왔습니다**

- 첫 열(학생)과 머리줄을 **고정**해서, 오른쪽으로 스크롤해도 누구 줄인지 잃지 않습니다.
- 맨 아래 **합계 줄**에 항목마다 몇 개·얼마가 뜹니다. 열을 보면 그 책이 몇 권인지 압니다 —
  그 숫자가 그대로 발주 수량입니다.
- 맨 오른쪽에 아이별 합계와 인보이스 번호. 발행 안 된 줄이 한눈에 보입니다.
- 열 머리의 `전체 +` 로 지금 보이는 명단 **전원에게 한 번에** 넣습니다. 반 단위로 교재를
  붙이는 일이 가장 잦은데, 이게 없으면 스무 번 눌러야 합니다.
- 고른 줄만 **한 번에 발행**합니다. 한 명이 실패해도 나머지는 계속하되, 누가 실패했는지
  반드시 말합니다 — 조용히 넘기면 그 아이만 인보이스 없이 남습니다.

**반별 · 학년별 · 전체**

- 기본은 반별입니다. 그런데 악기는 자기가 하고 싶은 것을 고르는 것이라 같은 반에서도 아이마다
  다릅니다. 그래서 학년별과 전체 보기를 함께 뒀습니다.
- 분류 단추로 열을 접고 폅니다. 지금 아무도 안 고른 항목은 열이 숨지만, 그 분류를 펴면 열이
  나와서 넣을 수 있습니다.
- `아직 발행 안 된 학생만` 을 켜면 남은 일만 보입니다.

**구글시트로 내보내기**

- `⬇ CSV (구글시트용)` 로 지금 보이는 표를 그대로 받습니다. 합계 줄까지 함께 나옵니다.
  한글이 깨지지 않도록 BOM을 붙였습니다.

**항목 분류를 자유롭게 적습니다**

- 교재·악기·악기수리·교복 다섯 가지로 고정돼 있던 것을 **자유 기입**으로 바꿨습니다. 학교에서
  받는 것이 그 다섯으로 끝나지 않고, 목록을 고정해두면 새 항목이 생길 때마다 개발자를 찾아야
  합니다.
- 이미 쓴 분류가 자동완성으로 먼저 뜨고, 처음 쓸 때를 위한 예시가 뒤에 붙습니다. 탭도 실제로
  쓰는 분류로만 만들어집니다.

## v0.369.0 - 2026-09-01

**교재·교복 인보이스를 손으로 만들지 않습니다**

- 지금까지는 구글독스 양식 하나를 아이마다 복사해서 항목을 갈아 끼우고, 금액을 손으로 넣고,
  합계도 사람이 계산했습니다. **그 방식이 실제로 틀리고 있었습니다** — 받은 양식의 항목 합은
  191,000원인데 총액 칸에는 191,200원이 적혀 있고, 금액에 `₩47,,000` 같은 오타도 남아
  있었습니다. 사람이 나빠서가 아니라, 사람에게 시키면 안 되는 일을 시키고 있어서입니다.

**세 화면으로 나눴습니다**

- `재무 → 학비외 항목` — 재무 담당자가 교재·악기·악기수리·교복을 등록합니다. **단가는 여기
  한 곳에만** 있습니다. 아이마다 금액을 고칠 수 있게 두면 그 자리가 곧 오타가 나는 자리입니다.
- 항목마다 **기본 학년·반**을 정해둡니다. `5학년`을 눌러두면 5학년 전원에게 자동으로 들어갑니다.
  137명을 하나씩 체크하게 두면 그 일은 결국 안 하게 되고, 안 하면 빠집니다.
- `재무 → 인보이스` — 아이를 고르면 그 아이가 사는 것이 **이미 채워진 채로** 나옵니다. 다른
  것만 눌러 넣고 뺍니다. 왼쪽 명단에 아이마다 합계가 미리 떠서 누가 얼마인지 한눈에 보입니다.
- 발행을 누르면 구글독스와 같은 모양의 인보이스가 새 창에 열립니다. 브라우저 인쇄에서
  `PDF로 저장`을 고르면 그대로 PDF입니다.

**틀릴 자리를 없앴습니다**

- 합계는 화면이 계산하고, **발행할 때 서버가 다시 계산합니다.** 화면이 보낸 총액을 그대로
  믿으면 화면이 틀렸을 때 틀린 금액이 그대로 학부모에게 갑니다.
- 아이별로는 **기본 세트와 다른 것만** 저장합니다. 5학년 스무 명에게 같은 책이 들어가면 스무
  줄을 만들지 않고, 한 명이 그 책을 안 산다는 사실 한 줄만 남깁니다. 줄이 적으면 틀릴 자리도
  적습니다.
- 발행한 인보이스는 **그 순간의 이름과 금액을 베껴 굳힙니다.** 나중에 책값이 올라도 이미 보낸
  종이의 금액은 흔들리지 않습니다.
- 번호(`2026-0001`)는 DB가 정합니다. 사람이 손으로 붙이면 반드시 겹칩니다.
- 인보이스 화면은 내역 합과 저장된 총액이 어긋나면 그 사실을 붉게 알립니다. 어긋난 채로
  나가는 것이 가장 나쁩니다.
- 항목을 지우지 않고 끕니다. 지난 인보이스가 왜 그 금액이었는지 설명할 수 있어야 합니다.
- 로직은 함수 하나(`resolveStudentItems`)에 모으고 9가지로 검증했습니다 — 학년·반 기본 세트,
  개별 추가·제외, 수량, 꺼둔 항목, 남의 기록이 새지 않는지, `5학년`과 `G5` 표기 차이까지.

**필요한 마이그레이션**

- `20260901160000_fee_items_invoices.sql` — 걸기 전까지 두 화면은 열리지 않습니다.

## v0.368.1 - 2026-09-01

**동명이인 학년 표기가 조용히 안 붙고 있었습니다**

- 도착체크에서 학생의 학년·반을 공용 명부 뷰(`wr_students_basic`)에서 읽고 있었습니다.
  그 뷰에는 `where is_giamicro_user()`가 박혀 있습니다 — 로그인한 교직원만 보라는 뜻입니다.
- 그런데 이 화면은 **로그인 없이 토큰으로 도는 화면**이라 서비스 키로 읽습니다. 서비스 키에는
  이메일이 없으니 그 조건이 거짓이 되고, **오류 없이 0행**이 돌아왔습니다. 학년이 그냥 안
  붙었고, 왜 안 붙는지 알 길이 없었습니다.
- 원본 표(`wr_students`)에서 읽도록 바꾸고, 실패하면 서버 로그에 남깁니다.

## v0.368.0 - 2026-09-01

**이준서의 집·기업은행 행선지가 월·금에만 열려 있었습니다**

- 도착체크 아래쪽 행선지 선택에 이준우는 두 가지가 다 뜨는데 이준서는 `학원` 하나만
  떴습니다. 이준서의 집·기업은행 배정만 요일이 **월·금**으로 잠겨 있었습니다. 화요일에는
  그 선택지가 아예 없으니, 따로 집에 가는 날 고를 방법이 없었습니다.
- 평일 전체로 열었습니다. 형제라 보통 같이 가지만 **따로 가는 날**을 위해 각자 두 가지를
  다 갖고 있어야 합니다. 이제 이준서·이준우 모두 `학원(4-2호)` · `집·기업은행(9호)`을
  매일 고를 수 있습니다.

**같은 이름이 두 명이면 학년을 붙입니다**

- 이 화면에 이준서가 둘 나옵니다. 7호를 매일 타는 초등 3학년(G3JA)과, 행선지를 그날 정하는
  중고등 9학년입니다. 둘 다 그냥 `이준서`로 뜨면 아이를 데리고 나가는 사람이 어느 쪽인지
  알 방법이 없습니다. 하원 시간의 착오는 되돌릴 수 없습니다.
- 이제 `이준서(3 G3JA)` · `이준서(9학년)`으로 갈라 보입니다. 행선지 선택과 호차 명단 양쪽에
  같은 규칙을 씁니다.
- **같은 이름이 두 사람 이상일 때만** 붙습니다. 한 명뿐인 이름까지 붙이면 좁은 칸이 글자로
  가득 찹니다. 배정이 여러 줄인 아이(행선지 선택)를 두 명으로 잘못 세지 않도록, 줄 수가
  아니라 사람 수로 셉니다.

## v0.367.0 - 2026-09-01

**GPS 현황: 연결된 차 2대인데 지도가 안 나왔습니다**

- 지도가 붙은 카드를 `GPS 연결`과 `GPS 미연결`로 나누는데, 그 기준이 **최근 15분 안에 들어온
  위치**였습니다. 하원이 끝나면 신호가 멎으니 오늘 종일 잘 돌던 27호·23호까지 미연결로
  접혀 들어갔고, 접힌 카드는 안 그려지니 **지도도 함께 사라졌습니다.**
- 15분은 "지금 살아 있는가"를 재는 자입니다. "오늘 연결된 차가 무엇인가"는 다른 물음이고,
  그건 기기가 오늘 마지막으로 신호를 보낸 시각으로 답해야 합니다. 두 물음에 같은 자를 대고
  있었습니다.
- 이제 **오늘 한 번이라도 신호가 있었으면 연결**입니다. 그 노선은 하루치 위치를 다시 읽어
  마지막 지점을 지도에 찍습니다. 운행이 끝난 뒤에도 어디서 멎었는지 볼 수 있습니다.
- 신선도·수신 주기·가장 긴 끊김은 그대로 최근 15분으로 잽니다 — 그건 원래 그 자로 재는
  것이 맞습니다.
- 하루치를 다시 읽는 것은 **오늘 신호가 있었는데 최근 15분에는 없는 노선만**입니다. 지금은
  두 대뿐이고, 늘어나도 읽는 양은 그날 실제로 움직인 차 수만큼만 늘어납니다.

## v0.366.0 - 2026-09-01

**도착체크: 색이 상태를 말합니다**

- 호차 왼쪽 동그라미 하나로 상태를 읽습니다. **청색=미도착 · 초록=도착 · 회색=출발.**
  `미도착` `도착함` `출발함` 글자는 뺐습니다 — 색으로 이미 구분되는데 글자로 또 적으면 좁은
  칸이 글자로 가득 차서 정작 호차가 안 보입니다.
- 📍는 **GPS가 붙어 신호가 오는 차에만** 뜹니다. 핀이 없으면 GPS 대기입니다. GPS 글자
  대신 핀 하나로 줄이고, 그 아래 아주 작게 연결 상태(`방금` · `3분 전` · `GPS 끊김`)만
  남겼습니다. 사람이 누른 게 아니라 GPS가 잡은 도착·출발이면 그 자리에 `자동·GPS`가 뜹니다.
- 카드 색도 같은 규칙을 따릅니다. 도착한 차는 이제 주황이 아니라 초록입니다.

**차번호 네 자리로 찾습니다**

- 맨 위 검색창에 뒤 네 자리를 치면 그 차만 남습니다. 차가 들어왔을 때 손에 있는 정보는
  번호판이고, 호차 번호는 차에 안 붙어 있습니다. 지금까지는 눈으로 표 전체를 훑어야 했습니다.
- 호차 번호와 노선 이름으로도 찾힙니다. 무엇이 손에 있든 통하게 했습니다.

**학생 이름 뱃지를 줄였습니다**

- 글자 12px → 10px, 높이 30px → 22px. 한 줄에 더 많이 들어가 카드가 짧아집니다. 누를 수 있는
  크기는 남겨뒀습니다 — 이름을 눌러 개별하원으로 바꾸는 자리라 너무 작으면 안 됩니다.

## v0.365.0 - 2026-09-01

**학생 검색이 명부를 못 읽고 있었습니다**

- `column wr_students_basic.student_no does not exist`. 학번 칸을 넣어 읽었는데 그 칸이
  없습니다. 없는 칸 하나가 조회를 통째로 막는 것은 오늘만 네 번째입니다.
- 학번은 뺐습니다. 대신 **반의 위치**를 붙였습니다.

**찾으면 어디로 가야 하는지까지 나옵니다**

- 이름 · 영문이름 · 학년 · 반 · **교실 위치**(`📍 A동 2F`)가 한 줄에 뜹니다. 아이를 찾아가야
  하는 사람에게는 반 이름보다 몇 동 몇 층인지가 필요합니다.
- 위치로도 검색됩니다. `A동`을 치면 그 건물 아이들이 모입니다.
- 누르면 그 학생의 기록 화면으로 바로 갑니다.
- 반 위치는 반 표(`wr_classes.room`)에 있어 따로 읽어 이어 붙입니다. 학생에게 반 연결이
  비어 있으면 학년+반 이름으로 한 번 더 찾습니다.
- 반 표를 못 읽어도 검색 자체는 됩니다. 위치만 비워 둡니다 — 위치 하나 때문에 학생 검색이
  통째로 막히면 손해가 더 큽니다.

## v0.364.0 - 2026-09-01

**안내보드: 떠나는 모션이 한 번도 안 보이고 있었습니다**

- 출발한 차는 슬라이드아웃 애니메이션 뒤에 사라지도록 만들어 뒀는데, 실제로는 그냥
  없어졌습니다. 출발하는 순간 공개 목록(revealedIds)에서도 함께 빼는데, 화면에 그릴 카드를
  고르는 조건이 그 목록을 **먼저** 보고 있었습니다. 애니메이션 대상이 그 자리에서 걸러졌습니다.
- 순서를 바꿔 출발 중인 카드를 먼저 통과시킵니다. 이제 GPS가 출발을 잡거나 사람이 출발을
  누르면 🚌💨 와 함께 미끄러지며 사라집니다.

**도착하고 20초가 지나면 화면이 재촉합니다**

- 차가 와 있는데 아이가 안 나오면 그 차는 서서 기다리고 뒤차도 같이 밀립니다. 화면이 조용하면
  아무도 서두르지 않습니다.
- 도착 후 20초가 지나도 안 탄 아이가 있으면 카드가 빨갛게 바뀌고 테두리가 천천히 깜빡이며,
  `⚠️ 차가 기다리고 있어요 — 얼른 타주세요!`와 몇 초째인지가 함께 뜹니다.
- 다 탄 차는 재촉하지 않습니다. 출발 표시를 기다리는 중일 뿐입니다.

**개별하원 아이의 이름이 안내보드에 뜹니다**

- 예전에는 `픽업 2명`이라는 숫자만 있었습니다. 숫자로는 화면을 보는 아이도 선생님도 누가
  그 아이인지 모릅니다. 이제 `🚗 개별하원` 표와 함께 이름이 나옵니다.

**도착체크에서 아이 이름을 눌러 개별하원으로 표시합니다**

- 하원 지도 중에 학부모님이 찾아오셔서 데려가시는 일이 하루에도 여러 번 있습니다. 그 자리에서
  못 누르면 나중에 옮겨 적어야 하고, 옮겨 적는 일은 반드시 언젠가 빠집니다.
- 이름 뱃지를 **손가락으로 누를 수 있는 크기**로 키웠습니다. 8px 글자에 여백이 거의 없던
  뱃지는 휴대폰에서 사실상 못 눌렀습니다.
- 누르면 바로 바꾸지 않고 한 번 묻습니다. 잘못 누르면 그 아이가 명단에서 사라지고, 사라진
  아이는 아무도 찾지 않습니다.
- 표시하면 그 아이는 명단에서 빠지고, 아이들이 보는 안내보드에 개별하원으로 이름이 뜹니다.

**도착체크 차번호는 뒤 네 자리만 크게**

- `77수 0340`을 통째로 넣으면 좁은 칸에서 전체가 작아져, 정작 사람이 외우고 부르는 네 자리가
  안 보였습니다. 이제 `0340`만 굵게 적습니다.

**GPS 현황에서 정류장별 도착 시각을 봅니다**

- `3/5` 같은 숫자만으로는 **어느 정류장에서 안 잡혔는지** 알 수 없습니다. 그 숫자를 누르면
  출발 시각과 정류장이 순서대로 펼쳐지고, 닿은 곳은 시각이, 못 닿은 곳은 회색으로 남습니다.
- 27호 하원은 정류장 다섯 곳 모두 좌표가 있어 도착이 잡힐 수 있는 상태입니다.

**업무보드**

- `🔎 학생 검색` 배지를 눌러 화면을 떠나지 않고 학생을 찾습니다. 업무를 보다가 학생 기록을
  남겨야 할 때 학교 메뉴로 옮겨 갔다가 돌아오면 보던 자리를 다시 찾아야 했습니다.
- 도착체크 QR이 `column shuttle_arrival_links.active does not exist`로 막혀 있었습니다.
  칸 이름은 `enabled`입니다. 없는 칸 하나가 조회를 통째로 막는 것은 오늘만 세 번째입니다.
- 하원 체크표에 `← 업무보드` 버튼을 붙였습니다. 오는 길만 있고 가는 길이 없었습니다.

**이준서 자리**

- 7호 리더스원(고진우 다음 자리)으로 배정했습니다. `정류장 미지정`이었습니다.

## v0.363.0 - 2026-09-01

**이준서가 두 명이었습니다**

- 초등 이준서(Justin Lee · 3학년 G3JA)와 중고등 이준서(Paul Lee · 9학년)가 셔틀 배정에서
  같은 `이준서` 한 사람처럼 묶여 있었습니다.
- 행선지를 물어서 정하는 쪽은 **중고등 이준서**입니다. 이준우(Roy Lee · 7학년)와 형제이고,
  학원(4-2호) · 집·기업은행(9호) 중 그날 정합니다.
- **초등 이준서는 7호를 매일 탑니다.** 물어볼 것이 없습니다. 그런데 7호 배정이 행선지 선택
  묶음에 함께 묶여 있어서, 아무도 정해주지 않으면 체크표에 나타나지 않았습니다. 셔틀명단에는
  있는데 체크표에는 없는 아이가 이렇게 생겼습니다.
- 다섯 줄의 배정을 각각 맞는 학생에게 연결하고, 7호는 선택 묶음에서 뺐습니다.
- 셔틀명단에는 있는데 체크표에 안 나오는 아이가 더 있는지 배정 105줄을 전부 훑었습니다.
  이준서·이준우 다섯 줄뿐이었습니다.

**반이 없는 학년은 "9"가 아니라 "9학년"으로 적습니다**

- 동명이인 표기가 중고등처럼 반이 없는 학년에서는 숫자 하나만 떴습니다. 무엇을 뜻하는지
  알 수 없습니다.

**업무보드에서 하원 체크표로 바로 갑니다**

- 머리줄에 `📋 하원 체크표` 배지를 붙였습니다. 업무와 하원, 이 둘이 하루 중 가장 자주 오가는
  두 화면인데 매번 셔틀 메뉴를 펼쳐 들어가고 있었습니다.
- `📱 도착체크 QR` 배지를 누르면 정규학기 도착체크 QR이 바로 뜹니다. 이 QR이 필요한 순간은
  대개 선생님이 옆에 서 계실 때인데, 지금까지는 관리 → 링크·기기까지 들어가야 했습니다.
  세 화면을 거치는 동안 그 자리는 지나갑니다.

**남은 것**

- 초등 이준서의 7호 정류장이 `정류장 미지정`입니다. 어디서 내리는지 적혀 있지 않아 기사님이
  알 수 없고, GPS 도착도 안 잡힙니다. 주소를 알려주시면 넣겠습니다.

## v0.362.0 - 2026-09-01

**없는 칸 하나가 하원 체크표의 픽업·결석을 통째로 지우고 있었습니다**

- 증상: 체크표에서 픽업·결석을 눌러도, 다시 열면 아무도 안 누른 것처럼 보였습니다.
  행선지 선택 학생(이준서·이준우)은 행선지를 정해줘도 체크표에 나타나지 않았습니다.
- 원인은 권한도 로직도 아니었습니다. `shuttle_boardings`에 **`updated_by`라는 칸이 없습니다.**
  그런데 코드 네 곳이 그 칸을 읽거나 쓰고 있었습니다.
  - 체크표가 오늘 탑승 기록을 읽는 조회에 그 칸이 들어 있어서, PostgREST가 **조회 전체를
    400으로 거절**했습니다. 오늘 눌러둔 것이 하나도 안 들어왔고, 화면은 조용했습니다.
  - 행선지 확인(도착체크 화면)이 그 칸에 이유를 적으려다 매번 실패했습니다. 그래서 행선지를
    정해줘도 그 아이는 어느 명단에도 나타나지 않았습니다.
  - 아침 크론의 기간 결석도 같은 이유로 하나도 안 걸리고 있었습니다.
- **재무 열쇠 때와 똑같은 모양입니다.** 없어도 되는 값 하나가 있어야 하는 값을 무너뜨렸고,
  화면에 오류가 없어서 원인을 엉뚱한 데서 찾게 됐습니다.

**고친 것**

- 누가 눌렀는지는 `checked_by`에 적습니다. 있는 칸을 씁니다.
- 탑승 기록 조회가 실패하면 서버 로그에 남깁니다. 눌러둔 것이 안 뜨는데 아무 말도 없는 상황을
  다시 만들지 않습니다.
- 픽업을 거는 함수가 실패를 삼키지 않습니다. 예전에는 "걸었다"고 답해놓고 실제로는 아무 일도
  일어나지 않았습니다.
- 진단 화면 기능별 점검에 `shuttle_boardings`를 넣었습니다. 이 조회가 막히면 체크표가
  통째로 비어 보이는데, 점검 목록에 없어서 초록불만 떠 있었습니다.

## v0.361.0 - 2026-09-01

**한 글에 아이가 여럿이면 아이마다 따로 읽습니다**

- 이렇게 왔습니다. `오늘 권수호, 황준호 픽업입니다, 라원 라윤이는 셔틀타요`
- 픽업은 권수호·황준호 둘인데 **황라원까지 픽업으로 들어가** 셔틀에서 빠졌습니다.
- 원인은 한 줄입니다. 지금까지 한 메시지에 상태 하나(픽업/결석)를 정하고, 그 메시지에서 찾은
  학생 **전원**에게 똑같이 붙였습니다. 한 글에 아이가 여럿이고 서로 다른 이야기를 하는 경우
  — 형제방에서는 예외가 아니라 보통입니다 — 이 전제가 그대로 깨집니다.
- 이제 **이름이 있는 절만** 보고 그 아이의 상태를 정합니다. 이름이 있는 절에 픽업·결석이
  적혀 있으면 그것, "셔틀 탄다"고 적혀 있으면 해당 없음, 아무 단서도 없으면 예전처럼 글 전체를
  따릅니다.
- `셔틀타요` · `기존처럼 스쿨버스` · `평소대로` · `as usual`을 평소대로 탄다는 신호로 읽습니다.
  다만 `셔틀 안 타요` · `태우지 말아주세요`는 그 반대로 읽습니다 — 셔틀이라는 말이 있다고
  타는 것이 아닙니다.
- 적힌 말이 "셔틀 탄다"보다 앞섭니다. `셔틀 대신 픽업합니다`는 픽업입니다.
- 이 판단은 하원 체크표 위젯, 업무 인박스, 출결 자동등록 세 곳이 **같은 함수**를 씁니다.
  세 곳이 따로 판단하면 한 화면에서는 타는 아이가 다른 화면에서는 빠집니다.

**틀린 방향의 무게**

- 오는 아이를 픽업으로 잘못 찍으면 셔틀 명단에서 빠져 **아이가 차를 못 탑니다.** 그래서 판단이
  갈릴 때는 타는 쪽으로 둡니다.

**검증**

- 위 문장과 `Bella와 sophia는 기존처럼 스쿨버스 태워주시면 됩니다`를 포함해 11가지로
  확인했습니다. 형제 결석·정상등원 판별, 한 명짜리 픽업, `수호는 픽업이고 준호는 결석`처럼
  절마다 다른 경우도 함께 봤습니다.

## v0.360.0 - 2026-09-01

**❓를 누르면 누가 바꿨는지 나옵니다**

- 근거 창에 `🕘 오늘 이 학생에게 있었던 일`이 붙습니다. **몇 시에 · 누가 · 무엇을 무엇으로**
  바꿨는지가 시간 순으로 적혀 있습니다.
- ❓가 **노선을 옮긴 학생에게도** 붙습니다. 지금까지는 픽업·결석에만 있었습니다. 차가 바뀌어
  있는데 누가 옮겼는지 물어볼 곳이 없으면 결국 전화로 확인하게 됩니다.
- ❓ 색이 뜻을 갖습니다. 파랑은 이유를 알고 있다는 뜻(연락에서 자동으로 붙었거나, 사람이
  눌렀고 그 기록이 있음), 주황은 사선이 그어져 있는데 이유를 모른다는 뜻입니다.
- 자동으로 붙은 뒤에 사람이 손댄 경우에는 자동 근거 아래에 그 순서가 이어 붙습니다. 순서가
  보여야 지금 화면이 왜 이 모양인지 설명이 됩니다.

**탑승 기록에 사람 이름이 남습니다**

- 픽업·결석 체크와 오늘만 노선 이동이 `shuttle_boardings`에 `updated_by`로 사람 이름을
  남깁니다. 예전에는 `"체크표"`만 남아서, 근거 창이 "체크표가 체크표에서 픽업으로
  표시했습니다"라고 말하고 있었습니다.
- 이 이름은 안내보드·도착체크처럼 활동 기록을 읽지 않는 화면에서도 그대로 보입니다.

## v0.359.0 - 2026-09-01

**하원수단이 체크표에 닿습니다**

- 학생 프로필의 🏠 하원수단(요일별)에서 **오늘이 셔틀이 아닌 아이**는 체크표에서 자동으로
  빠지고, 이름 옆에 무엇을 타고 가는지가 붙습니다(예: `1:55 메타프랩버스`).
- 지금까지는 프로필에 적어둬도 체크표는 그대로 "탄다"였습니다. 아무도 안 누르면 기사님은
  오지 않는 아이를 기다리고, 담임은 아이를 셔틀 줄에 세웁니다. 적어둔 사실이 화면에
  닿지 않으면 안 적은 것과 같습니다.
- 셔틀 배정은 지우지 않습니다. 요일마다 다르기 때문입니다 — 월요일에는 같은 아이가 같은
  차를 탑니다. 배정은 그대로 두고 그날 하루만 뺍니다.
- 사람이 이미 눌러둔 것이 있으면 손대지 않습니다. "오늘은 그래도 탄다"는 판단이 이깁니다.
- 왜 빠졌는지는 느낌표를 눌러 볼 수 있습니다 — 어느 요일 하원수단인지, 어디서 고치는지가
  함께 적혀 있습니다.
- 아침 크론도 같은 일을 합니다. 체크표는 이 표를 직접 읽지만 안내보드·도착체크·사무실
  대시보드는 `shuttle_boardings`만 봅니다. 화면마다 답이 다르면 결국 아무도 안 믿습니다.

**요일을 서버 시각으로 읽고 있었습니다**

- 체크표가 `new Date().getDay()`로 요일을 구하고 있었습니다. 서버는 세계표준시라 한국시간
  오전 9시 이전에는 **어제 요일**이 나옵니다. 월요일 새벽에 열면 일요일이 되어 아무도 안
  타는 표가 됩니다. 한국 요일을 쓰도록 고쳤습니다.

**체크표에 누가 무엇을 했는지 남습니다**

- 픽업·결석 체크, 노선 이동, 특이사항 메모 — 바뀔 때마다 **누가, 언제, 무엇을 무엇으로**
  한 줄씩 쌓입니다. 왼쪽 맨 위 `🕘 오늘 한 일`에서 봅니다.
- 이름을 누르면 표에서 그 아이를 바로 찾아줍니다. 시각에 마우스를 올리면 정확한 시각과
  계정이 나옵니다.
- 옆자리에서 누른 것도 실시간으로 올라옵니다. 서로의 손길이 늦게 보이면 같은 아이를 두 번
  고치게 됩니다.
- `shuttle_boardings`에도 마지막으로 손댄 사람이 남아 있지만 그것은 **마지막 상태 한 줄**
  입니다. "아침에 결석으로 찍혔다가 낮에 풀렸다"는 거기 안 남습니다. 되돌아본다는 것은
  순서를 본다는 뜻이라 따로 쌓습니다.
- 기록은 고칠 수도 지울 수도 없습니다. 수정·삭제 정책을 아예 만들지 않았습니다 — 고칠 수
  있는 기록은 기록이 아닙니다.
- 기록 저장이 실패해도 체크 자체는 되돌리지 않습니다. 픽업 표시는 아이가 차를 타느냐 마느냐의
  문제고, 기록은 나중에 되돌아보기 위한 것입니다. 다만 조용히 넘기지는 않습니다.

**필요한 마이그레이션**

- `20260901120000_checklist_activity_log.sql` — 걸기 전까지 활동 기록은 쌓이지 않습니다.
  체크·이동은 평소대로 동작합니다.

## v0.358.2 - 2026-09-01

**진단 화면이 새로 걸린 세 마이그레이션도 점검합니다**

- 하원수단 · 재무 열쇠 · 납부 항목 · 할인 · 학생 납부 선택 다섯 줄을 기능별 점검에
  넣었습니다. 이 셋은 마이그레이션이 안 걸린 채로 화면만 먼저 올라가 있었는데, 점검
  목록에 없어서 초록불 24개가 그대로 떠 있었습니다. 점검이 놓친 것은 사람도 놓칩니다.
- 재무 열쇠 줄에는 이 칸이 없을 때 무슨 일이 나는지 적었습니다. 없으면 돈 화면만 안 열리는
  것이 아니라, 예전에는 신분 조회 전체가 400으로 막혀 **모든 사람의 메뉴가 사라졌습니다**.

**하원수단 코드 주석에서 대화 인용을 걷어냈습니다**

- 요일별 하원수단이 필요한 이유는 사실로 남기고, 그 사실을 알려준 말은 지웠습니다.

## v0.358.1 - 2026-09-01

**아직 안 걸린 마이그레이션 3개를 한 파일로 묶었습니다**

- `outputs/미적용_마이그레이션_3개.sql` — 재무 권한 · 납부 항목과 할인 · 하원수단 순서로
  이어 붙였습니다. Supabase SQL Editor 에 통째로 붙여넣어 한 번에 실행합니다.
- 순서가 곧 의존 관계입니다. 납부 항목의 접근 제한은 재무 권한이 만드는 `has_finance_access()`
  를 쓰고, 하원수단은 `wr_students` 와 `set_updated_at()` 을 씁니다.
- 한 덩어리(begin/commit)로 묶었습니다. 중간에서 실패하면 아무것도 남지 않으므로 절반만
  걸린 상태를 걱정하지 않아도 됩니다.
- 끝에 실행 기록을 `supabase_migrations.schema_migrations` 에 직접 넣습니다. SQL Editor 로
  직접 실행하면 이력에 남지 않아서, 진단 화면이 계속 "파일은 있는데 DB에는 없다"고 경고하고
  다음에 CLI 가 같은 파일을 또 실행하려 듭니다.
- 세 표가 실제로 생겼는지 눈으로 볼 수 있는 확인 조회를 맨 아래 붙였습니다.

**마이그레이션 주석에서 대화 인용을 걷어냈습니다**

- 네 파일의 머리말이 요청 문장을 그대로 옮겨 적고 있었습니다. 몇 달 뒤 이 파일을 여는
  사람에게 필요한 것은 그때의 말투가 아니라 **무엇이 정해졌는가**입니다.
- 예를 들어 형제할인 항목의 주석은 이제 "형제할인은 운영하지 않기로 정해졌고, 그래서 할인
  목록은 고정될 수 없다"는 사실만 적습니다.

## v0.358.0 - 2026-09-01

**하원 대시보드 — 차가 두 대가 되면서 지도가 두 대를 못 보여주고 있었습니다**

- 왼쪽 지도는 노선 48개 전부에 화면을 맞추고 있었습니다. 서울 전체가 들어오니 정작 지금
  달리는 차는 점 두 개로 보였습니다. 이제 **지금 길 위에 있는 노선들**(GPS가 살아 있거나
  오늘 자취가 있는 차)에만 화면을 맞춥니다. 추적 중인 차가 하나도 없을 때만 예전처럼
  전체에 맞춥니다.
- 지금 달리는 차의 위치는 조건과 무관하게 항상 화면 맞춤에 넣습니다. 차를 보려고 지도를
  손으로 끌어야 한다면 이 지도는 제 몫을 못 하는 것입니다.
- 오른쪽 지도가 보고 있는 노선은 왼쪽에서도 경로선을 굵게 그립니다. 두 지도가 서로 어디를
  가리키는지 눈으로 이어집니다.

**오른쪽 지도를 한 노선에 고정할 수 있습니다**

- 아래 호차 띠의 호차를 누르면 그 노선에 고정되고 6초 순환이 멈춥니다. 한 번 더 누르거나
  지도 위의 고정 버튼을 누르면 풀립니다.
- 고정 중에는 지도 위에 📌 표시와 해제 버튼이 뜹니다. 순환이 멈춘 이유가 보이지 않으면
  고장으로 읽힙니다.
- 이 화면은 초 단위 시계 때문에 1초마다 다시 그려집니다. 노선 목록을 그대로 비교하면 내용이
  같아도 매번 지도를 통째로 다시 그리게 되므로, 내용을 문자열로 굳혀서 비교합니다.

**계속 다른 차를 타는 아이는 이제 '오늘 차량변경'에 뜨지 않습니다**

- 송우진·송윤진처럼 아예 다른 차로 옮겨 계속 타는 아이가 매일 "오늘 차량변경"으로 올라오고
  체크표에서도 계속 강조돼 있었습니다. 바뀐 상태가 그 아이의 평상시 상태인데 매일 예외로
  보이면, 정작 오늘 하루만 바뀐 아이가 묻힙니다.
- 이제 **오늘 하루만 옮긴 경우**에만 위젯에 오르고 ↔ 표시가 붙습니다. 계속 옮겨 다니는
  아이는 다른 아이들과 똑같이 보입니다.
- 위젯의 "어디서 왔는지"도 원래 배정된 호차가 아니라 **평소 타던 호차** 기준으로 적습니다.

## v0.357.0 - 2026-09-01

**칸 하나가 없어서 행정직원·관리자의 메뉴가 통째로 사라져 있었습니다**

- 증상: 행정직원 화면에서 **셔틀**과 **주간 학생 관찰기록**이 안 보였습니다. 관리자 화면에서는
  **관리**도 안 보였을 것입니다.
- 원인은 권한 설정이 아니었습니다. **신분 조회가 통째로 실패하고 있었습니다.**
  - 로그인한 사람의 이름·직위를 읽는 조회에 `finance_access`가 함께 들어 있었습니다.
  - 그 칸을 만드는 마이그레이션이 아직 안 걸려서, PostgREST가 조회 **전체**를 400으로
    거절했습니다 — `column app_users.finance_access does not exist`.
  - 그러면 직위가 `null`이 되고, 행정직원이 **직위 없는 사람**이 됩니다. 셔틀·주간 리포트는
    직위로 열리므로 그대로 사라지고, 업무·학교·문서는 직위와 무관해서 남습니다.
    보이는 그림이 정확히 그랬습니다.
- **개발자 계정에만 멀쩡해 보였습니다.** 개발자는 이메일로 우회하므로 직위를 읽지 못해도
  전부 보입니다. 그래서 화면을 만든 쪽에서는 아무 문제가 없었습니다.

**고친 것 — 없어도 되는 값이 있어야 하는 값을 무너뜨리지 못하게**

- 신분(이름·직위)과 재무 열쇠를 **따로** 읽습니다.
  - 이름·직위: 없으면 아무것도 못 하는 값입니다. 실패하면 서버 로그에 남깁니다.
  - 재무 열쇠: 없으면 돈 화면만 안 열리는 값입니다. 칸이 아직 없으면(42703) 조용히 false.
- 두 조회는 나란히 돌아가므로 왕복이 늘지 않습니다.
- 진단 화면의 설명도 고쳤습니다. 예전에는 "돈 화면이 안 열립니다"라고만 적혀 있어서, 이것이
  **모든 사람의 메뉴를 지우는 문제**라는 것이 드러나지 않았습니다.

**남은 것**

- `20260831180000_finance_role.sql`은 여전히 걸어야 합니다. 이번 수정은 그때까지 앱이
  버티게 한 것이지, 재무 기능을 살린 것이 아닙니다.

## v0.356.0 - 2026-09-01

**'요금제'를 학교에서 쓰는 말로 바꿨습니다**

- 문제: 재무 화면에 '요금제'라고 적혀 있었습니다. 통신사 말이라 학교에서 쓰지 않습니다.
- 학교 문서(`25-26학년도 학비 납부옵션`)의 말을 그대로 씁니다.
  - 무엇에 얼마를 받는가 → **납부 항목** (정규과정 · 방과후 5일반 · 셔틀 · 교재비)
  - 몇 회분을 묶고 몇 % 깎는가 → **납부 옵션** (월 납부 · 5개월 납부 · 10개월 납부).
    이건 원래도 맞는 말이라 그대로 뒀습니다.
- 메뉴 이름, 화면 제목, 통계 딱지, 버튼, 빈 화면 안내, 진단 화면 문구까지 함께 고쳤습니다.
- DB 표 이름(`fee_plans`)은 그대로입니다. 영문 내부 이름이라 사람이 보는 자리가 아니고,
  바꾸면 아직 걸리지도 않은 마이그레이션을 다시 써야 합니다.

## v0.355.0 - 2026-09-01

**형제방에서 한 아이만 쉬는 글을, 아이 단위로 나눠 읽습니다**

- 문제: 실제로 아이를 잘못 짚었습니다.

  > 오늘 여명인 괜찮은데, 이제는 여전히 열나고 아파 하루 더 쉬도록 하겠습니다.
  > 여명이는 정상등원 합니다!

  쉬는 아이는 **이제**이고 여명이는 정상등원인데, AI가 **여명이를 결석**으로 읽었습니다.
  근거 요약에도 "여명이의 결석 사유"라고 적혔습니다.

- 원인은 두 겹입니다.
  - 지금까지는 메시지 하나에 상태 하나(결석/픽업/지각)를 정하고, 그 메시지에서 찾은 학생
    전원에게 똑같이 붙였습니다. **형제방에서는 이 전제가 깨집니다** — 한 아이는 쉬고 한
    아이는 갑니다.
  - 형제방은 채널 이름만으로 둘 중 누구인지 가릴 수 없어 AI 답을 그대로 썼고, AI는 **먼저
    나온 이름**을 골랐습니다.

- **이름이 있는 절(節)만 읽어 아이마다 상태를 따로 정합니다**(`src/lib/attendanceIntent.ts`).
  "~한데", "~지만", 쉼표, 그리고 영어의 but/however/while에서 문장을 자릅니다.
  이 규칙 결과가 **AI 답보다 우선**입니다 — 규칙은 같은 문장에 늘 같은 답을 냅니다.

- **등원 쪽을 더 세게 봅니다.** 두 실수의 무게가 다르기 때문입니다.
  - 오는 아이를 결석으로 잘못 찍으면 → 셔틀 명단에서 빠져 **아이가 차를 못 탑니다.**
  - 안 오는 아이를 등원으로 두면 → 기사님이 잠깐 기다리고 사람이 확인 전화를 합니다.

  그래서 한 절에 "정상등원·괜찮다·등원합니다"가 적힌 아이는 결석 대상에서 뺍니다.
  AI가 그 아이를 골랐다면 그건 확실한 오답이므로 무시합니다.

- 9가지 문장으로 검증했습니다. 한국어·영어, 결석/픽업/등원, 부정형("안 괜찮아서 결석")까지
  전부 통과합니다.

- **형제가 서로 다르면 자동 확정을 막습니다.** 기계가 가장 자주 뒤집는 자리라 사람이 한 번
  봐야 합니다.

- **픽업 인박스에 형제 구분 안내를 답니다.** "여명은 정상등원, 이제는 결석"처럼 규칙이 읽은
  결과를 그대로 보여주고, AI 요약이 다른 아이를 가리키면 잘못 읽은 것이라고 알려줍니다.
  **이미 쌓여 있는 줄에도 붙습니다** — 고친 규칙이 어제 들어온 연락에 소용없다면 담당자는
  여전히 손으로 찾아야 합니다.

- AI 프롬프트에도 이 사례를 넣었습니다. 규칙과 모델 둘 다 같은 것을 보게 됩니다.

## v0.354.0 - 2026-08-31

**체인지로그에서 대화체를 전부 걷어내고, 다시 섞이지 않게 검사기를 걸었습니다**

- 문제: 체인지로그에 요청을 그대로 옮긴 문장이 계속 들어갔습니다. 한 번 정한 규칙인데
  그 뒤로도 반복해서 어긋났습니다.
- **4,377줄 전체를 훑어 224곳을 고쳤습니다.** 유형은 네 가지였습니다.
  - 괄호 안 인용 105곳 — 바깥 문장이 이미 변경 내용을 말하고 있어 괄호째 지웠습니다.
  - 문장 뒤에 붙은 인용 60곳 — 같은 이유로 잘라냈습니다.
  - 인용만으로 이루어진 항목 48곳 — 문제·기준·배경을 서술하는 문장으로 다시 썼습니다.
  - 표식 없이 통째로 옮긴 대화 11곳 — 사실만 남기고 말투를 버렸습니다.
- 고친 뒤 문장이 깨진 자리가 없는지 자동으로 점검했고, 공백이 먹힌 세 곳(`.ics`)을 되돌렸습니다.

**재발 방지 — 글이 아니라 검사로 막습니다**

- `CLAUDE.md` 신설. 이 저장소에서 지켜야 할 것을 한 곳에 모았습니다. 체인지로그 작성 규칙,
  코드 주석, 버전·배포 순서, 한국 날짜 처리, 조용한 실패 금지.
- `scripts/check-changelog.mjs` 신설. 금지 표현 네 가지를 검사하고, 걸리면 **무엇이 걸렸는지가
  아니라 대신 어떻게 쓰라는 것인지**를 알려줍니다.
  - 코드 블록 안은 검사하지 않습니다. 오류 메시지나 SQL 원문은 그대로 실어야 합니다.
  - 따옴표 **안**의 말투만 잡습니다. "보여줘서", "확인해주세요" 같은 설명문은 걸리지 않습니다.
    자꾸 헛걸리는 검사기는 사람이 꺼버리고, 꺼진 검사기는 없는 것과 같습니다.
- `npm run build` 가 이 검사를 **먼저** 돌립니다. 규칙을 어기면 빌드가 실패해 배포까지 가지
  않습니다. GitHub Actions에도 같은 검사를 걸어, 빌드를 건너뛴 커밋도 잡습니다.

**곁다리로 고친 것**

- 사람이 한 말은 인용하지 않고, 기계가 낸 말(오류 메시지·화면 문구·로그)은 그대로 인용합니다.
  이 구분을 `CLAUDE.md`에 명시했습니다.

## v0.353.0 - 2026-08-31

**차량번호 뒤 네 자리를 크게 · 탑승 학생 칸 앞 여백**

- 배경: 현장에서는 호차보다 차번호로 차를 구별하는 일이 더 잦습니다. 그런데 차번호가 작고
  앞뒤가 같은 크기라 멀리서 읽히지 않았고, 탑승 학생 칸은 격자선에 붙어 있었습니다.
- **차번호를 앞/뒤로 나눠 무게를 다르게 줍니다.** 사람이 외우고 부르는 것은 뒤 네 자리입니다 —
  "77수"는 거의 모든 차가 비슷해서 구별에 쓸모가 없습니다.
  - 인쇄본: 앞은 7.5pt 회색으로 위에, 뒤 네 자리는 **13pt 굵게** 아래 줄에.
  - 화면도 같은 규칙으로 맞췄습니다. 종이와 화면이 다르게 보이면 대조할 때 헷갈립니다.
  - 앞 글자가 없이 네 자리만 적힌 차(시트에 앞 칸이 비어 있는 11대)는 그 네 자리가 곧
    전부이므로 그대로 크게 나옵니다.
- 탑승 학생 칸 왼쪽에 여백 9px. 격자선에 첫 아이 이름이 붙어 먹히고 있었습니다.

## v0.352.0 - 2026-08-31

**하원 체크표 인쇄: 이름의 네모칸 제거, 글자 키움**

- 문제: 인쇄본에서 학생 이름마다 네모칸을 둘러 가시성이 떨어졌습니다.
- 이름마다 두르던 검은 테두리를 없앴습니다. 표에는 이미 격자선이 있어서, 이름에까지
  테두리를 두르면 **선이 이름보다 많아집니다.** 종이에서 눈이 찾아야 하는 것은 선이 아니라
  이름입니다.
- 테두리와 안쪽 여백이 빠진 만큼 글자를 **8.5pt → 10pt**로 키웠습니다. 게다가 표 높이가
  줄어 자동 축소도 덜 들어가므로, 종이에서는 그보다 더 크게 보입니다.
- 이름끼리 붙어 보이지 않도록 사이 간격을 넉넉히(10px) 두고, 줄 간격도 조금 넓혔습니다.
- 한 장에 맞추는 자동 축소는 그대로입니다 — 인원이 아무리 많아도 여전히 한 장입니다.

## v0.351.0 - 2026-08-31

**사이드바 버전 표시가 5번 배포 동안 안 바뀌고 있었습니다**

- 화면 왼쪽 아래 버전이 계속 `v0.345.0`이었습니다. `src/lib/version.ts`에 문자열을 손으로
  적어두고 "버전 올릴 때 같이 고치기"로 두었는데, 그 뒤로 **다섯 번 배포하는 동안 한 번도
  안 고쳐졌습니다.**
- 사소해 보이지만 사소하지 않습니다. 담당자가 "이게 반영된 건가?"를 확인할 유일한 표시라,
  틀려 있으면 배포가 됐는데도 안 됐다고 판단하게 됩니다.
- `package.json`에서 **빌드할 때 읽어오도록** 바꿨습니다. 두 곳에 같은 값을 적어두고 사람이
  맞추기로 하면 언젠가 반드시 어긋납니다.
- 예전 주석이 걱정하던 "런타임에 package.json을 못 찾는 문제"와는 상관없습니다 — `import`는
  파일을 읽는 게 아니라 빌드할 때 값이 코드 안으로 박히는 것입니다.

## v0.350.0 - 2026-08-31

**하원 체크표 인쇄 1장 강제 · 메뉴 구분선 복구**

### ① 인쇄가 계속 2장이던 진짜 이유

- 문제: 하원차량표를 인쇄하면 계속 두 장으로 나왔습니다. 한 장이어야 합니다.
- **표는 이미 한 장에 맞게 축소되고 있었습니다.** 브라우저에서 직접 재봤습니다 —
  표 높이 1175px, 자동 축소 0.90배 → **1063px**, 한 장이 1069px. 표만 놓고 보면 들어갑니다.
- 넘친 것은 **표가 아니라 껍데기**였습니다.
  - 화면 본문 여백(`main`의 위아래 24px씩) = **48px**
  - 인쇄에서 안 감춰지고 남아 있던 날짜 줄("📅 2026년 8월 31일 월 · 🧒 탑승예정 79명") = **44px**
  - 합쳐서 **92px** — 딱 그만큼이 두 번째 장이었습니다.
- 고침 세 가지.
  - 날짜 줄을 인쇄에서 뺐습니다. 같은 내용이 인쇄본 맨 윗줄에 이미 있습니다.
  - 인쇄할 때 `html`/`body`/`main`의 여백·높이 제한을 전부 없앱니다. 종이에서는 이 표 말고
    아무것도 자리를 차지하면 안 됩니다.
  - 안전 여유를 6px → **24px(약 6mm)** 로 늘렸습니다. 브라우저마다 인쇄 배율이 미세하게
    달라서 6px는 반올림 하나에 두 번째 장이 생길 만큼 빡빡했습니다.
- 글자도 줄였습니다 — 표 9pt → 8.5pt, 제목 11pt → 10pt,
  칸 여백 2px → 1px. 자동 축소가 덜 들어가서 오히려 더 또렷해집니다.

### ② 메뉴 구분이 사라진 이유

- 문제: 메뉴바의 항목 구분이 사라져 가시성이 크게 떨어졌습니다.
- **있는데 안 보이는 쪽이었습니다.** 글래스로 바꾸면서 `--shell-border`를
  `rgba(255,255,255,0.75)` — **흰색**으로 두었습니다. 그런데 이 변수는 유리판 가장자리
  하이라이트가 아니라 **메뉴 항목 사이의 구분선**으로 쓰이는 자리입니다(사이드바 `divide-y`,
  헤더 아랫줄, 플라이아웃 테두리 등 14곳). 밝은 바탕 위의 흰 선은 아무것도 아닙니다.
- 고침:
  - `--shell-border`를 **보이는 회보라 실선**(`rgba(58,63,99,0.14)`)으로 되돌렸습니다.
    유리 느낌을 내는 흰 하이라이트는 `--shell-edge`로 이름을 나눠 따로 뒀습니다 — 쓰임이
    다른 두 가지가 한 변수를 쓰고 있던 것이 원인이었습니다.
  - 흐린 글자색을 `#8e93b4` → `#5f6489`로 진하게. 흰 유리판 위에서 대비가 3:1 아래로
    떨어져 메뉴 이름이 "있는데 안 보이는" 상태였습니다.
  - 상단 탭에 **선반(2px 실선)** 을 깔았습니다. 켜진 탭이 그 선 위에 올라앉은 모양이 되어
    한눈에 읽힙니다. 꺼진 탭 글자도 slate-500 → 600, 마우스 올렸을 때 바탕을 반투명 흰색으로
    (기존 `slate-50`은 유리 위에서 보이지 않았습니다).
  - 사이드바 항목 높이를 한 칸 키웠습니다 — 구분선이 다시 보이게 되면 줄 간격이 좁을 때
    선이 글자에 붙어 답답해 보입니다.

## v0.349.0 - 2026-08-31

**리포트 저장 점검 중 발견한 버그 2건 수정**

리포트 작성·기록·저장이 되는지, 자료가 한꺼번에 들어가도 견디는지를 점검하며
저장 경로를 훑다가 두 가지를 찾았습니다. **둘 다 조용히 틀리는 종류**입니다.

### ① 같은 기간에 이어 쓰면 리포트가 두 줄로 갈라졌습니다

- 저장 열쇠가 `(학생, 과목, report_date)`인데, `report_date`에 **그날 날짜**를 넣고
  있었습니다. 월요일에 쓰고 수요일에 이어 쓰면 열쇠가 달라져 upsert가 안 묶이고 **새 줄**이
  생깁니다. 화면은 최근 것만 보여주므로 **월요일에 쓴 글이 사라진 것처럼 보이고**, 통계의
  작성 건수는 부풀려집니다.
- 주 단위일 때도 있던 문제인데, 2주로 늘리면 갈라질 수 있는 날이 7일 → **14일**이 됩니다.
- 고침: `report_date`를 **"쓴 날"이 아니라 "어느 기간의 리포트인가"** 로 바꿨습니다.
  - 이미 있는 리포트를 고치는 중이면 → 그 리포트의 날짜 그대로
  - 이 기간에 처음 쓰는 것이면 → 기간의 첫날
- 검증: 가짜 DB에 실제와 같은 고유 제약을 걸고 4일에 걸쳐 이어 쓰기를 돌렸습니다.
  **예전 4줄 → 지금 1줄.** 기간을 넘기면 정상적으로 새 줄이 됩니다(2줄).

### ② 반별 작성 현황이 1,000줄에서 말없이 잘렸습니다

- `StudentReportBoard`가 **한 학기치 전체**를 상한 없이 가져오고 있었습니다. Supabase는
  한 번에 돌려주는 줄 수에 상한(1,000)이 있어서, 넘으면 **잘렸다는 표시 없이** 잘립니다.
- 한 반 15명 × 과목 8 × 기간 9 ≈ 1,000줄. 학기 후반에 정확히 걸립니다. 잘리면 **이미 쓴
  리포트가 "미작성"으로 보입니다** — 선생님은 다 썼는데도 안 썼다는 화면을 보게 됩니다.
- 고침: ① 최근 60일치만 가져오고(이번 기간 + 직전 네 기간) ② 1,000줄씩 끝까지 나눠 읽습니다.
  통계 화면은 이미 이렇게 읽고 있었는데 여기만 빠져 있었습니다.

## v0.348.0 - 2026-08-31

**리포트 3항목 + 격주 · 하원수단 칸 · "every friday" 인식**

### ① 리포트 항목 5개 → 3개

- 배경: 선생님들이 리포트 항목이 너무 많다고 하십니다. 통합할 것은 통합해 3개로 줄입니다.
- **학업 / 생활 / 종합 의견** 세 가지입니다.
  - 학업 ← 학업 성취 + 보완점
  - 생활 ← 수업 참여 + 생활 태도 + 교우 관계
  - 종합 ← 예전의 '교사 종합 의견'(학부모께 그대로 나가는 글)
- **DB 칸 이름은 그대로 재사용했습니다**(academic / behavior / teacher_note). 새 칸을 만들면
  이미 쌓인 기록을 옮겨야 하는데, 옮기는 순간 원본과 다른 글이 생깁니다.
- **지난 기록은 그대로 남습니다.** 안 쓰게 된 세 칸(보완·참여·교우)은 **일부러 저장하지
  않습니다** — 빈 값으로 덮으면 선생님이 지난 학기에 적어둔 관찰이 사라집니다. 과거 기록
  보기와 인쇄본에서는 "이전 서식(5항목)에 적힌 내용"으로 밝히고 그대로 보여줍니다.

### ② 2주(격주) 주기

- 결정: 작성 주기를 2주로 바꿉니다. 화면 이름은 '위클리'로 두고 체크 판정만 격주로 해도 됩니다.
- 뒤쪽을 택했습니다. **이름·메뉴는 '위클리 리포트' 그대로**, "이번 기간에 이미 썼는가"를
  판정하는 자만 14일로 바꿨습니다. 이름까지 바꾸면 메뉴·제목·안내문·인쇄본을 다 손대야 하고
  되돌릴 때도 그만큼 듭니다.
- 리포트 창 제목 옆에 **지금 기간(예: 3월 2일 ~ 3월 15일)** 을 적었습니다. 2주에 한 번이면
  "이번 것을 이미 썼는지"를 날짜로 확인할 수 있어야 합니다.
- 겸사겸사 **날짜 계산의 UTC 버그**를 고쳤습니다. 예전 `getWeekRange`는 한국 시간 자정을
  `toISOString()`으로 찍어서 주 시작이 **하루 앞당겨져** 있었습니다(월요일인데 일요일로).

### ③ 하원수단 칸 (학생 기준·요일별) — 신규

- 사례: 백서아는 월요일만 셔틀이고 화·목은 13:55 메타프랩버스, 수요일은 16:30 블루웨일버스,
  금요일은 15:35 블루웨일버스를 탑니다. 이런 아이를 등록할 자리가 없었습니다.
- **지금까지 적을 자리가 아예 없었습니다.** "어떻게 집에 가는가"는 셔틀 배정에만 있었고,
  셔틀을 안 타는 날은 배정 자체가 없습니다. 학원 버스는 셔틀이 아니지만 **선생님이 아이를
  어디로 내보낼지 알아야 하는 정보**입니다.
- 새 표 `student_dismissal_plans` (학생 × 요일). 셔틀 / 외부버스 / 보호자픽업 / 도보 / 기타 +
  버스 이름 + 시각.
- **학생 프로필**에서 다섯 요일을 한 화면에 펼쳐 입력합니다. 한 요일만 봐서는 맞는지 알 수
  없어서입니다. [↑ 위와 같이]로 복사할 수 있습니다 — 화·목이 같고 수·금이 같은 식이 흔한데
  다섯 번 타이핑하면 오타가 섞이고, 오타 하나가 곧 "어느 버스인지 모른다"가 됩니다.
- **담임 선생님 화면(우리 반 픽업)** 에 아이 기준으로 오늘 수단이 뜹니다. 예전에는 "셔틀
  미탑승"만 떴는데, 그건 무엇을 **안** 타는지만 알려주고 무엇을 타는지는 안 알려줍니다.
- 시각은 `time`이 아니라 문자열로 둡니다. 학부모가 "1:55"라고만 알려주는 일이 많아서,
  시각 타입으로 바꾸면 **새벽 1시 55분**이 됩니다.

### ④ "every friday"를 반복으로 읽습니다

- 문제: `Theo is pick up everyfriday 3:10`이 **오늘 하루 픽업**으로 등록됐습니다.
  'everyfriday'가 무시되고 반복 약속이 한 번짜리로 처리된 것입니다.
- AI 프롬프트에는 'every Friday'를 반복으로 보라는 규칙이 **이미 있었는데도** 놓쳤습니다.
  붙여 쓴 'everyfriday', 앞에 길게 붙은 멘션, 영어·한국어 혼용 — 모델이 흔들릴 이유는 많고
  **놓쳤을 때 아무 표시도 안 남습니다.** 오늘 픽업으로 잘 들어간 것처럼 보이고, 다음 주
  금요일에 아이가 그냥 차를 탑니다.
- 그래서 **규칙으로 한 번 더 봅니다**(`src/lib/parentRecurrence.ts`). 둘 중 하나라도 반복이라
  하면 반복입니다. everyfriday / every friday / Fridays / each Monday / 매주 금요일 /
  금요일마다 / 매 화요일 / 매주 화·목 / 매일 — 16가지로 검증했습니다.
- 반복인데 **요일을 못 읽은 경우**(예: "앞으로 계속 제가 데리러 갈게요")는 자동 확정을
  막습니다. 기계가 정할 수 없으면 사람이 봐야 합니다.
- 픽업 인박스에 🔁 배지를 답니다. **이미 쌓여 있는 줄에도** 붙습니다 — 고친 규칙이 어제
  들어온 연락에는 소용없다면 담당자는 여전히 손으로 찾아야 합니다.

## v0.347.0 - 2026-08-31

**하원 체크표: 영어 이름으로도 검색**

- 문제: 하원 체크표 검색이 한글 이름만 찾아, 영어 이름으로는 아이를 못 찾았습니다.
- **배정표에는 한글 이름만 있습니다.** 영어 이름은 학생 명부(`wr_students.name_en`)에만
  있어서, 두 자료를 이름으로 이어붙여야 검색이 됩니다. 그 대조를 화면이 열릴 때 한 번만
  하고, 표에는 결과(배정 → 영어 이름)만 넘깁니다.
- 배정표 이름에는 "김재이(G2A)"처럼 구분 표기가 붙은 것이 섞여 있어 **괄호 안을 떼고**
  한 번 더 맞춰봅니다. 그래도 못 찾으면 비워둡니다 — 억지로 비슷한 사람을 붙이면 엉뚱한
  아이가 검색에 걸립니다.
- 대소문자·공백을 가리지 않습니다. "ella", "Ella Kim", "ellakim" 모두 같은 결과입니다.
- **찾은 뒤 영어 이름을 보여줍니다.** 표에는 한글만 나오므로, "Ella"로 찾았는데 노란 뱃지에
  한글만 있으면 같은 아이인지 확신할 수 없습니다. 강조된 동안만 밑에 작게 붙입니다.
- **영어 이름이 아예 없으면 그렇다고 말합니다.** 영어로 쳤는데 명부에 영어 이름이 한 건도
  없으면 검색창 옆에 주황색으로 알립니다. 아무 표시도 없으면 "이 아이가 없나"로 읽히는데
  실제로는 "자료가 없다"입니다 — 둘은 전혀 다른 이야기입니다.

## v0.346.0 - 2026-08-31

**하원 체크표: 호차 아래 차량번호**

- 문제: 하원 체크표 화면에 차번호가 없었습니다(인쇄본에만 있었습니다).
- **차량번호는 이미 들어와 있었습니다.** 체크표 화면이 노선을 읽어올 때 `vehicle_no`까지
  같이 가져오고 있었고, 다만 **인쇄본에서만** 그 값을 썼습니다. 정작 차를 찾는 일은 화면을
  보면서 하는데, 화면에는 호차 번호만 있었습니다. 이번 주에 몇 번 나온 그 모양 —
  자료는 있는데 안 보여주는 — 과 같습니다.
- **호차 옆이 아니라 아래 한 줄로 두었습니다.** 옆에 붙이면 이 칸의 폭이 늘어나고, 그만큼
  학생 이름이 들어갈 자리가 줄어듭니다. 체크표에서 가장 넓어야 하는 칸은 학생 칸입니다.
- **차량번호가 없는 호차는 "차번호 없음"이라고 적습니다.** 아무것도 안 적어두면 "아직
  등록을 안 한 건지, 원래 없는 차인지"를 볼 때마다 다시 확인하게 됩니다. 눌러서 바로
  노선 관리로 넘어가 채울 수 있게 링크로 걸었습니다(인쇄본에는 나오지 않습니다).

## v0.345.0 - 2026-08-31

**유리를 화면 안쪽까지 (101개 파일 236곳) + 동명이인 학년·반 표기**

- 문제: 글래스 디자인이 하원 체크표에는 적용되지 않아 화면이 거의 그대로였습니다.
  **맞습니다. 제가 셸(사이드바·헤더·바탕)만 바꿨습니다.** 화면 안쪽 카드는 전부 흰 배경
  그대로였고, 그래서 체크표를 열면 달라진 게 거의 없었습니다.
- **카드 겉면 236곳을 한 번에 유리판으로 바꿨습니다**(101개 파일). `rounded-xl border
  border-slate-200 bg-white` 조합을 `.g-panel-solid`로 치환했습니다.
  반투명한 `.g-panel`이 아니라 **솔리드 쪽**을 쓴 이유: 이 카드들 안에는 표·목록처럼 글자가
  빽빽한 것이 많습니다. 유리 느낌(blur·테두리 하이라이트·그림자)은 그대로 있으면서 읽기를
  잃지 않습니다.
- **인쇄본과 안내보드 TV는 건드리지 않았습니다.** 각자 흰 배경인 이유가 있습니다.

**동명이인 학년·반 표기**

- 문제: 동명이인(예: 김재이)이 화면마다 다르게 불려, 같은 아이인지 확신할 수 없었습니다.
- **판단을 한 곳으로 모았습니다**(`src/lib/studentLabel.tsx`). 지금까지 이 판단이 화면마다
  따로 있었습니다 — 출결 파서에는 `homonymLabel`, 탑승 배정에는 또 다른 표기, 픽업 인박스에는
  생년월일 괄호. 그래서 **같은 아이가 화면마다 다르게 불렸습니다.** 어떤 화면은
  "김재이(G3JA)", 어떤 화면은 그냥 "김재이". 같은 아이를 두 화면에서 다르게 부르면 그 둘이
  같은 아이인지조차 확신할 수 없습니다.
- **겹치는 이름에만 붙입니다.** 한 명뿐인 이름에까지 학년·반을 붙이면 표가 글자로 가득 차고
  정작 구분이 필요한 이름이 묻힙니다.
- **하원 체크표 화면과 인쇄본 둘 다** 적용했습니다. 종이에서 헷갈리는 게 더 위험합니다 —
  화면은 눌러서 확인할 수 있지만 종이는 그럴 수 없습니다.
- 이름 비교는 공백을 무시합니다("김 재이" = "김재이"). 교재비 시트에서 이런 표기 흔들림을
  실제로 8쌍 찾았습니다.

## v0.344.0 - 2026-08-31

**글래스 디자인 시스템 (시안 B 채택) + shadcn 기반**

- 시안 세 장 중 **B(부드러운형)** 를 고르셨고, 여기에 글래스모피즘을 얹었습니다.
- **shadcn 규약으로 공용 부품을 깔았습니다** — `Button` `Card` `Badge` `Input/Select/Textarea`
  (`src/components/ui/`), 그리고 `cn()` 유틸. 지금부터 **새로 만드는 화면은 전부 이걸 씁니다.**
- **시각화 부품 4종**(`src/components/viz/`): `StatCard`(숫자+추세선+증감) · `Donut`(비율) ·
  `BarRow`(가로 막대) · `Sparkline`. 담당자 요청("조금 더 시각화한 자료들이 많도록").
  숫자만 적어두면 "78%"가 좋은 건지 나쁜 건지 읽는 사람이 매번 판단해야 합니다.
- **기본 테마를 글래스로 갈아끼웠습니다.** 테마 고르는 구조는 그대로 두고 '라이트'의 값만
  바꿨습니다 — 이미 그 테마를 쓰시는 분들의 설정을 건드리지 않고 인상만 바뀝니다.
  사이드바·헤더·카드가 반투명이 되고 페이지 바탕에 옅은 얼룩이 깔립니다.
- **왜 얼룩(mesh)을 깔았나:** 반투명은 뒤에 뭔가 있어야 보입니다. **흰 배경 위의 반투명은
  그냥 흰색입니다.**
- **읽기를 먼저 지켰습니다.** 글래스는 예쁘지만 글자 대비를 갉아먹습니다. 이 앱은 하원 4시에
  여러 명이 동시에 보는 실무 화면이 중심이라, 판의 불투명도를 0.72 아래로 내리지 않았고
  글자가 빽빽한 판(표·목록)은 `.g-panel-solid`로 더 진하게 씁니다.
- **세 가지 안전장치**: ① `backdrop-filter`를 못 쓰는 구형 브라우저·안내보드 TV에서는
  **불투명으로 떨어집니다**(반투명인 채로 blur만 빠지면 글자를 읽을 수 없습니다)
  ② 인쇄할 때는 유리·그림자를 전부 끕니다(잉크 낭비) ③ 움직임 줄이기 설정을 켠 분에게는
  떠오르는 효과를 끕니다.
- **재무 화면을 새 부품으로 다시 만들었습니다** — 요약 카드 4개, 할인 켜둔 비율 도넛,
  요금제별 납부 옵션을 표 대신 **가로 막대**로. 표에 숫자만 늘어놓으면 "어디가 큰가"를 눈이
  아니라 머리로 계산하게 됩니다.
- 나머지 화면은 셸(사이드바·헤더·바탕)만 바뀌고 내부는 그대로입니다. 다음 배치부터 묶어서
  옮깁니다.

## v0.343.0 - 2026-08-31

**요금제 · 할인 (재무 전용) — 인보이스 2단계**

- 조건: 할인율과 할인 항목은 재무 권한자가 자유롭게 만들고 없앨 수 있어야 합니다. 형제할인처럼
  없애기로 한 항목이 있어 목록이 고정될 수 없습니다.
- **지우기 버튼이 없습니다. 켜고 끄기만 있습니다.** 지워버리면 작년 청구서가 왜 그
  금액이었는지 설명할 수 없게 됩니다. "지금 쓰는 할인"과 "그때 썼던 할인"은 다른 물음이고,
  둘 다 답할 수 있어야 합니다. 켜고 끈 기록은 **이유와 함께** 남습니다.
- **금액을 하나하나 적어두지 않습니다.** 납부 안내문의 숫자를 전부 검산했더니 하나도
  빠짐없이 규칙으로 떨어졌습니다 — 정규과정 11,000,000×3=33,000,000, 연납 ×0.9=29,700,000;
  방과후 5일반 450,000/월, ×5×0.95=2,137,500, ×10×0.9=4,050,000. 그래서 **기준 금액 1회분 +
  납부 옵션(몇 회분을 묶고 몇 % 깎는가)** 만 담습니다. 요금이 오르면 한 군데만 고칩니다.
- **학년도가 연도를 걸치는 문제는 새로 풀지 않았습니다.** 이미 있는 `terms` 표(26-27학년도
  1학기)를 그대로 가리킵니다 — 같은 개념을 두 군데서 다르게 정의하면 반드시 어긋납니다.
- 이 표들은 **전부 재무 권한 뒤에** 있습니다. 할인 이름조차 권한 없는 사람에게는 보이지
  않습니다("부이사장님만 아시는 할인"). 화면 단에서 한 번, DB(RLS)에서 또 한 번 막습니다.
- 사이드바 **💰 재무** 메뉴는 **직위가 아니라 열쇠로** 나옵니다 — 관리자여도 열쇠가 없으면
  메뉴 자체가 없습니다.

## v0.342.0 - 2026-08-31

**재무 권한 분리 (인보이스 시스템 1단계)**

- **'재무관리자'라는 직위를 만들지 않았습니다.** 처음엔 개발자 > 최고관리자 > 재무관리자 >
  관리자 로 한 줄 세우려 했는데, 재무는 위아래가 아니라 **다른 축**입니다. 재무를 보는 사람이
  셔틀이나 학사를 관리자보다 더 잘 알 이유가 없고, 한 줄로 세우면 "재무만 보고 학생 명부는
  못 보는 경리직원"을 나중에 못 만듭니다.
- 그래서 **계층 하나 + 열쇠 하나**로 나눴습니다.
  `position`(보이는 직위: 교사·행정직원·관리자·최고관리자·개발자) + `finance_access`(재무 열쇠).
  **재무관리자 = 관리자 + 재무 열쇠**이고, 그래서 남들에게는 그냥 관리자로 보입니다 —
  숨기는 게 아니라 **실제로 관리자**입니다. 화면 한 군데를 빠뜨려도 드러날 것이 없습니다.
- **열쇠 값은 서버에서 지워서 내려보냅니다.** 화면에서 버튼만 감추면 값은 브라우저까지 가서
  개발자도구에 그대로 보입니다. 감추는 것과 안 보내는 것은 다릅니다.
- **최고관리자**는 개발자 바로 밑이며 **개발자만 지정**할 수 있습니다. 관리자가 스스로를
  올릴 수 있으면 계층이 계층이 아닙니다.
- **열쇠는 최고관리자만 주고 뺏습니다.** 관리자끼리 서로 나눠 가질 수 있으면 "재무관리자만
  본다"가 아무 뜻이 없어집니다. 줄 때 **이유를 함께 적고**, 누가 언제 주고 뺏었는지가
  `finance_access_log`에 남습니다(지우지 않습니다).
- **`is_app_admin()`에 최고관리자를 포함**시켰습니다. 이걸 빠뜨리면 직위를 올려준 사람이
  오히려 관리자 권한을 잃습니다 — 알아채기 어려운 사고라 미리 막았습니다.
- 앞으로 만들 돈 관련 표의 RLS는 전부 `has_finance_access()` 하나만 봅니다. 판정 기준이 여러
  군데로 흩어지면 한 곳만 고치고 나머지를 잊습니다.

## v0.341.0 - 2026-08-31

**진단 화면: 넓게 · 두 단 · 다시 진단하기 · 빠진 SQL 바로 복사**

- **폭을 풀고 두 단으로 나눴습니다.** max-w-4xl(896px)은 글을 읽는 화면의 폭입니다. 진단은
  **훑어보는 화면**이라 세로로 쌓으면 아래 것을 보려고 계속 스크롤하게 되고, 그러면 "지금
  뭐가 빨간지"가 한눈에 안 들어옵니다. 왼쪽은 <b>지금 뭐가 잘못됐나</b>(연결·스키마·GPS·
  행선지·무결성), 오른쪽은 <b>돌아가고 있나</b>(마이그레이션·크론·오늘 들어온 것)입니다.
- **[🔄 다시 진단하기]** 버튼이 생겼습니다. 고치고 나서 결과를 보려면 브라우저 새로고침을
  눌러야 했는데, 그건 "이 화면이 다시 재는 것"이 아니라 "브라우저가 페이지를 다시 여는 것"
  이라 같은 일인지 확신이 안 섭니다. 이제 누르면 서버가 실제로 다시 재고 몇 시에 쟀는지가
  남습니다.
- **빨간 줄 옆에 [SQL 복사]·[보기]가 붙었습니다.** 지금까지는 `20260831120000_….sql 실행 필요`
  라고 **파일 이름만** 알려줬습니다. 그러면 깃허브에 들어가 폴더를 뒤지고 원문 보기를 눌러
  전체 선택해 복사해야 합니다 — 다섯 단계입니다. 화면이 이미 어느 파일인지 아는데 사람이
  찾아 헤맬 이유가 없습니다. 자동 적용이 어떤 이유로든 안 돌았을 때 **막히지 않고 넘어갈
  길**입니다.

## v0.340.0 - 2026-08-31

**기사님 안내문·동의서를 각 A4 한 장으로**

- 문제: 기사님 안내문·동의서가 너무 길었습니다. 안내문만 두 장 반이었고,
  기사님이 그걸 앉아서 다 읽으실 리가 없습니다. **안 읽히면 길게 쓴 것이 오히려 아무것도
  전달하지 못합니다.**
- **안내문(1장):** 뺀 것은 왜 필요한지에 대한 긴 설명, 설치 절차 서술(어차피 학교에서
  해드립니다), 그리고 **같은 말의 반복** — 수집 시간 제한이 세 군데에 적혀 있었습니다.
  남긴 것은 무엇이 수집되고 안 되는지, 시간 제한, 어디에 쓰지 않는지, 그리고 설정값 표입니다.
  설정값은 기사님이 직접 볼 일은 없지만 **신호가 끊겼을 때 되짚는 유일한 근거**라 남겼습니다.
- **동의서(1장):** 법적 문서라 **항목은 하나도 빼지 않았습니다** — 목적·수집항목·시간제한·
  보유기간·열람범위·제3자 제공·거부권·철회권. 줄인 것은 문장뿐이고, 안내문에 이미 있는
  설명을 옮겨 적은 부분을 걷어내 표 한 장으로 모았습니다.
- 서명란은 그대로입니다. 여전히 2부 인쇄해 한 부는 학교 보관, 한 부는 기사님께 드립니다.

## v0.339.0 - 2026-08-31

**지속 특이사항: 날짜 결석·여러 명 한 번에 (+ 조용히 안 듣던 버그 수정)**

- **버그였습니다.** 표에는 `effect_from`·`effect_to`(기간)와 `absent`(결석) 효과가 예전부터
  있었고, AI 수집기는 학부모 연락에서 "9월 23일~28일 결석"을 뽑아 **정확히 저장하고
  있었습니다.** 그런데 하원 체크표가 그 세 칸을 **조회조차 하지 않았습니다.** 저장은 되는데
  아무 일도 안 일어나는, 이번 주에 몇 번 겪은 그 모양입니다. 이제 읽고 반영합니다.
- **결석·픽업을 날짜로 미리 넣어둘 수 있습니다.** [결석 (그 날짜에 셔틀 제외)]를 고르고
  날짜를 넣으면, **그 날이 왔을 때만** 셔틀에서 빠지고 상태가 결석으로 바뀝니다. 오늘 넣어도
  오늘 셔틀은 그대로입니다 — 그게 "미리 넣어둔다"의 뜻입니다. 하루만이면 앞칸만 넣습니다.
- **여러 명을 한 번에** 넣습니다. 이름을 치고 엔터(또는 쉼표)를 누르면 칩으로 쌓이고, 명부에서
  고르면 곧바로 담깁니다. 강여명·강이제처럼 같이 여행 가는 아이들을 한 명씩 두 번 넣을 일이
  없습니다.
- **명부에서 못 찾은 이름은 조용히 넘기지 않습니다.** 이름이 명부와 다르면 그 줄은 아무
  아이에게도 안 붙는데, 지금까지는 "추가했습니다"만 뜨고 끝났습니다. 이제 어떤 이름을
  못 찾았는지 그 자리에서 알려줍니다.
- 요약 줄에 **9/23~9/28 예정** / **적용중** / **지남**이 색으로 구분돼 나옵니다. 앞날 것을
  넣어두고 "지금 빠진 건가?" 헷갈리지 않도록.
- **체크표의 '오늘'이 UTC로 계산되고 있었습니다.** 한국 시간 자정~오전 9시 사이에는 하루 전
  날짜를 봤습니다. 같은 실수를 네 번 반복한 뒤 만든 kst.ts로 갈아끼웠습니다 — 이 파일만
  빠져 있었습니다.

## v0.338.0 - 2026-08-31

**학사일정 자동화 (요청 ④⑤⑥) + GPS 표시 수정**

- **GPS 표시를 지붕 경광등으로 바꿨습니다.** 처음 그린 오른쪽 위 와이파이 아이콘은 담당자
  지적대로 잘 안 보였습니다 — 칸 하나가 84px인데 아이콘은 그중 12px이라, 스무 개를 늘어놓으면
  색이 있는지조차 안 보였습니다. 지붕 막대는 **그림 폭 전체**를 쓰므로 작게 줄여도 색이 먼저
  눈에 들어옵니다. 초록(연결) · 주황(신호 끊김) · 빨강(미연결).
- **항목 추가가 팝업으로** 바뀌었습니다. 예전에는 제목 한 칸 + 날짜 한 칸이 줄에 붙어 있어서
  기간도, 학기 기준도, 회의도 넣을 자리가 없었습니다.
- **기간**을 넣을 수 있습니다. 걸쳐 있는 일을 하루로 적으면 시작한 날부터 챙겨야 하는데
  마지막 날에야 눈에 띕니다. 기간 항목은 **걸쳐 있는 날 전부**에 달력에 표시됩니다.
- **학기 기준 상대 날짜**("학기 시작 2주 전까지")로 적을 수 있습니다. 이건 날짜가 아니라
  **규칙**이라, 학기가 바뀌면 그 학기 날짜로 저절로 다시 만들어집니다. 그래서 이 방식은 항상
  <b>매 학기 되풀이</b>로 저장됩니다 — 규칙을 한 학기짜리로 적어두면 다음 학기에 아무 일도
  안 일어나 결국 손으로 다시 적게 됩니다.
- **회의 자동 세팅.** [회의가 필요합니다]를 켜면 마지막 회의를 마감일에 두고 간격만큼 거슬러
  잡습니다(기본 2번 · 7일). 첫 모임에서 한 주 동안 누가 무엇을 맡을지 나누고, 다음 모임에서
  처리한 일과 결정한 일을 함께 봅니다. 횟수·간격은 자유롭게 바꿉니다.
- **업무보드 자동 등록.** 매일 자정 크론이 돌면서 **마감 N일 전(기본 7일)에 닥친 것만** 업무로
  올립니다. 너무 일찍 올리면 보드가 몇 달 뒤 일로 가득 차 오늘 할 일이 묻히고, 너무 늦게
  올리면 올라온 순간 이미 늦습니다. 회의도 각각 업무로 올라갑니다. 한 번 올린 것은 다시
  올라오지 않고, 업무를 지워도 되살아나지 않습니다.
- 달력 항목에 **📋 업무 등록됨** 표시와 **🗣 회의 1차·2차** 줄이 함께 나옵니다. 만들어만 두고
  안 보여주면 "회의 필요"를 켜도 아무 일도 안 일어난 것처럼 보입니다.
- 참고: **"학기 시작 며칠 전" 자체는 이미 돌아가고 있던 기능**입니다. 이번에 더한 것은 기간,
  회의, 업무보드 연계, 그리고 그것들을 한 팝업에서 정하는 화면입니다.

## v0.337.0 - 2026-08-31

**화면 정리 4가지 (요청 ①③⑦⑨)**

- **시간표 과목마다 색이 다릅니다.** 예전에는 모든 칸이 같은 보라색이라 가로로 훑을 때 "이
  줄에 같은 과목이 몇 개인지"가 눈에 안 들어왔습니다. 색은 **과목 이름으로 저절로** 정해집니다 —
  아무 설정 없이 바로 과목마다 다른 색이 나오고, 같은 과목은 어느 요일·어느 반에서든 늘
  같은 색입니다(무작위로 하면 새로고침할 때마다 바뀌어 아무 쓸모가 없습니다).
  마음에 안 드는 과목만 [🎨 과목 색]에서 눌러 바꾸면 되고, 언제든 자동으로 되돌릴 수 있습니다.
- **셔틀 개요 노선별 현황이 버스 앞모습 그림으로** 바뀌었습니다. 색칠한 네모 스무 개는 훑을 때
  다 같아 보입니다. 오늘 탈 아이가 있는 차는 **진하고 테두리가 있고**, 없는 차는 흐리고 테두리가
  없습니다. GPS는 그림 오른쪽 위 작은 와이파이로 — 초록(연결)·주황(신호 끊김)·빨강(미연결).
  **아이는 타는데 와이파이가 빨간 차**가 오늘 신경 쓸 차입니다.
- **하원 셔틀명단(탑승 배정)에서 이름 옆에 학년·반이 붙고, 명부와 이어졌는지 ✓로 보입니다.**
  ✓가 없으면 그 줄은 이름만 적힌 글자일 뿐이고 학년·반·연락처 어느 것도 따라오지 않습니다.
  반까지 붙인 이유는 학년만으로는 동명이인을 못 가르고, 픽업 연락이 오면 담임을 찾아야 하기
  때문입니다.
- **학생 조회에 셔틀 여부가 붙습니다.** 명부에 적어둔 값이 아니라 **실제 배정**을 보여줍니다
  (🚌 하원 9호). 명부에는 "탄다"고 되어 있는데 배정이 없으면 <b>🚌 배정 없음</b>이 빨갛게 뜹니다 —
  그 아이는 그날 아무 차에도 안 실립니다. 이게 실제로 사고가 나는 자리입니다.
- **교직원 정보 조회를 부서·권한별로 봅니다.** 이름 검색은 누구인지 이미 알 때 쓰는 도구인데,
  실제로 자주 필요한 물음은 "초등부 선생님이 누구누구지"처럼 묶어서 보는 것이었습니다.
  각 칸에 인원수가 함께 나오고, 퇴사자는 기본으로 감춥니다.

## v0.336.0 - 2026-08-31

**학기별 반·담임·과목 기록 (요청 ②)**

- 문제: 반/담임 배정과 과목이 학기에 속해 있지 않아, 학기를 바꿔도 지난 학기 반 세팅을 볼 수 없었습니다.
  지금까지 반 세팅은 **한 벌뿐**이었습니다. 새 학기 반을 짜는
  순간 지난 학기 모습은 아무 데도 남지 않고 사라졌습니다 — "작년 2학기에 3학년이 몇 반이었고
  담임이 누구였는지"를 물어볼 곳이 없었습니다.
- **반/담임 배정 관리**와 **과목반 세팅** 위에 학기 고르개가 생겼습니다. 진행중 학기를 고르면
  지금까지처럼 고칠 수 있고, 지난 학기를 고르면 그 학기의 반·담임·부담임·학생 명단(과목은
  담당 교사·수강 명단)이 **읽기 전용**으로 나옵니다. 지난 학기 배정을 이제 와서 고칠 일은
  없고, 고칠 수 있게 두면 "지금 반"과 헷갈려 엉뚱한 학기를 건드리게 됩니다.
- 기록은 **학기가 끝날 때 저절로** 저장됩니다(학기 전환 크론이 종료로 넘기기 직전에 뜹니다 —
  넘긴 뒤에는 이미 늦습니다). 학기 중에 지금 모습을 미리 남기고 싶으면 [📌 지금 세팅을 이
  학기 기록으로 저장] 버튼을 씁니다.
- **왜 `wr_classes`에 학기 칸을 넣지 않았는가:** 이 표를 읽는 화면이 31곳입니다(담임 화면,
  출결, 학생 프로필, 시간표, 픽업, 안내보드, 온보딩…). 거기에 학기 칸을 넣고 전부 "현재
  학기"로 거르게 만들면 **한 곳만 빠뜨려도 담임 선생님 화면이 통째로 비어 보입니다.** 그리고
  그 31곳이 묻는 것은 전부 "지금 반이 뭐냐"입니다. 그래서 지금 세팅은 건드리지 않고, 지난
  학기만 따로 보관하는 쪽을 골랐습니다 — 31개 파일 중 **하나도 고치지 않았습니다.**
- 기록은 참조가 아니라 **값**으로 적습니다. 반이 없어지거나 교사가 그만두어도 그 학기에
  실제로 어떤 반과 담임이 있었는지는 그대로 남아야 하기 때문입니다.
- 이 기능을 만들기 전에 지나간 학기는 남아 있는 것이 없습니다. **지금 진행중인 학기부터
  쌓입니다.**

## v0.335.0 - 2026-08-31

**화면 정리 5가지 (요청 ⑧⑩⑪⑫⑬)**

- **하원 체크표 인쇄가 또 잘리던 진짜 이유를 찾았습니다.** 인쇄본 자체는 A4 한 장에 맞게
  실측·축소까지 하고 있었는데, 잘라내던 것은 **앱 껍데기**였습니다. 대시보드 레이아웃이
  `h-screen`으로 높이를 한 화면에 묶어두고 있어서, 인쇄할 때도 딱 한 화면 높이까지만 찍고
  나머지를 버렸습니다. 인쇄 전용 규칙을 넣어 인쇄할 때만 그 껍데기를 풀고 인쇄본만 남깁니다.
  지금까지 인쇄본만 세 번 고쳤는데, 세 번 다 엉뚱한 곳을 고친 셈입니다.
- **하원 체크표에서 배정된 학생이 없는 호차는 표에 나오지 않습니다.** 빈 칸만 있는 줄이
  오늘 태울 아이를 찾는 눈을 가로막았습니다. (끌어다 옮기는 중에는 놓을 자리로 그대로 뜹니다.)
- **픽업 인박스가 화면을 넓게 씁니다.** 폭 제한(768px)을 풀고, 넓은 화면에서는 왼쪽에
  **손이 가는 것**(확인이 필요한 픽업), 오른쪽에 **눈으로 보는 것**(예정·오늘 확정·손접수)을
  나란히 둡니다. 확인할 건을 처리하다 오늘 명단을 보려고 스크롤을 오르내릴 일이 없어집니다.
- **링크·기기 탭**에서 안내보드 링크(왼쪽)와 교직원 도착체크 링크(오른쪽)를 나란히 놓았습니다.
  둘 다 "링크를 만들어 나눠주는" 같은 성격이라 위아래로 길게 늘일 이유가 없었습니다.
- **기사님 설정 현황에 검색을 붙이고 접을 수 있게 했습니다.** 기사님 성함·호차·차량번호·
  연락처로 찾습니다. 전화번호는 `010-1234`로 쳐도 `01012345678`과 맞도록 숫자만 남겨
  견줍니다. 목록은 기본으로 접혀 있고, 찾는 중이거나 탭을 고르면 저절로 펴집니다.
- **GPS 현황 탭에서 GPS 연결 부분이 맨 위로** 올라왔습니다. 지금 신호가 들어오는 차를 먼저
  보고, 전 호차 한 줄 요약표는 그 아래입니다. 어쩌다 쓰는 [누락된 노선 링크 추가]는 맨
  아래로 내려 접어두었습니다.

## v0.334.0 - 2026-08-30

**학부모 연락 검색 (업무 → 🔍 연락 검색)**

- 지금 화면들은 **최근 것만** 보여줍니다. 인박스는 3일치, 목록도 스크롤하면 끝입니다.
  그런데 학부모 연락은 대개 **나중에** 필요해집니다 - 상담 전에, 같은 일이 또 생겼을 때,
  지난 대화에서 정한 내용을 다시 찾아야 할 때. **쌓이기만 하고 못 찾았습니다.**
- 원문·요약·학생 이름·채팅방 이름을 **한 번에** 찾습니다. 사람은 어느 칸에 들어 있는지
  모른 채 기억나는 단어를 치기 때문입니다.
- 찾은 낱말에 표시가 붙습니다. 긴 글에서 어디가 걸렸는지 눈으로 못 찾으면 검색이 반쪽입니다.
- 분류(픽업·문의·기타)와 기간으로 좁힐 수 있고, 눌러서 원문 전체와 토들 링크로 갑니다.
- **열자마자 아무것도 안 보여줍니다.** 최근 것을 쏟아부으면 찾으러 온 사람이 그걸 훑다가
  원래 찾던 것을 잊습니다. 학부모 연락 전체를 이유 없이 화면에 올릴 이유도 없습니다.
- 새로 저장하는 것은 없습니다. 이미 들어와 있는 것을 찾을 수 있게 했을 뿐입니다.

## v0.333.0 - 2026-08-30

**정원 대비 탑승률 (셔틀 → 🪑 탑승률)**

- 차를 늘릴지 줄일지, 어느 노선을 합칠지는 지금까지 **기억과 인상**으로 정했습니다.
  "그 차는 늘 비어 보이던데"는 맞을 때도 있고 아닐 때도 있습니다.
- **두 숫자를 나란히 둡니다. 섞으면 잘못된 결론이 납니다.**
  · **배정 / 정원** — 계획. 명부상 몇 명을 태우기로 했는가.
  · **평균 탑승 / 정원** — 현실. 픽업·결석을 빼고 진짜 몇 명이 탔는가.
- 계획은 꽉 찼는데 현실이 절반이면 **차를 줄일 게 아니라 왜 안 타는지**를 봐야 합니다.
  둘 다 낮을 때가 노선을 합칠 때입니다. 화면이 판단을 대신하지 않고 두 숫자만 보여줍니다.
- 평균은 **기록이 있는 날로만** 나눕니다. 운행 안 한 날까지 나누면 실제보다 낮게 나옵니다.
  그래서 `운행일`을 함께 적습니다 - 이틀 기록으로 낸 평균과 20일 기록으로 낸 평균은
  무게가 다릅니다.
- 정원을 넘겨 태운 날은 **빨간 막대**로 뜹니다. 이건 통계가 아니라 안전 문제입니다.
- 정원이 안 적힌 차는 비율 대신 인원만 나오고, 몇 대인지 위에 알려줍니다.
- 그날만 다른 차로 옮긴 학생은 **옮겨간 차**로 셉니다.

## v0.332.0 - 2026-08-30

**결석 · 픽업 이력 (셔틀 → 📆 결석·픽업 이력)**

- 지금까지는 **오늘만** 볼 수 있었습니다. "이 아이 이번 달에 몇 번 빠졌지?"를 물으면
  아무도 답을 못 했습니다. **기록은 다 쌓여 있는데 꺼내 볼 방법이 없었을 뿐입니다.**
- 상담할 때, 학부모가 물어볼 때, 차량 인원을 다시 짤 때 전부 필요한 숫자입니다.
- 최근 7일 · 30일 · 90일 버튼과 직접 기간 지정. 이름·반·호차로 검색.
- **`기록 수`를 함께 보여줍니다.** "3번 빠짐"은 20일 중 3번과 5일 중 3번이 전혀 다른
  이야기입니다. 숫자만 있고 바탕이 없으면 잘못 읽습니다.
- 학생 단위로 셉니다(배정 단위가 아니라). 형제 행선지 선택처럼 한 학생이 배정을 여러 개
  가진 경우에도 한 줄로 나옵니다.
- **조회 전용입니다.** 지난 기록을 화면에서 손댈 수 있게 하면 "언제 누가 왜 바꿨나"를
  다시 쫓게 됩니다.
- 기간이 120일을 넘으면 잘라내고, **잘랐다고 화면에 적습니다**(조용히 자르지 않습니다).

## v0.331.0 - 2026-08-30

**진단 화면에 ⑦ 데이터 무결성 · ⑧ 마이그레이션 추가**

**⑦ 데이터 무결성** — 화면에서는 멀쩡해 보이는데 실제로는 틀린 것들을 셉니다.
이번 주에 겪은 문제가 전부 이 종류였고, **사고가 나거나 누가 말하기 전까지 아무도
몰랐습니다.** 여덟 가지를 봅니다.

- 같은 학생이 두 노선에 (양쪽 다 '탄다'로 보여 아무도 안 태울 수 있음)
- 좌표 없는 정류장 (GPS 도착이 영영 안 찍힘)
- 주소 없는 정류장 (기사님이 어디서 내릴지 모름)
- 명부에 연결 안 된 배정 (픽업·결석 자동 반영이 그 학생만 건너뜀)
- 정류장 없는 활성 노선 · 이름 없는 행선지 선택지
- 학생 없는 확정 픽업 · 학생 없는 지속 특이사항

**여기서 고치지 않습니다.** 세고, 왜 문제인지 말하고, 고치러 갈 링크만 줍니다.
자동으로 고치면 "왜 바뀌었지"를 또 찾아야 합니다.

**⑧ 마이그레이션** — 폴더의 파일과 DB 실행 기록을 대조합니다.

- `column... does not exist` 오류의 원인은 늘 같습니다. **무엇이 돌았고 무엇이 안
  돌았는지 아무도 모른다.** 파일은 폴더에, 기록은 DB 안쪽에 있어서 맞춰보려면 매번
  사람이 SQL을 쳐야 했습니다.
- 이제 `파일 56개 중 2개가 DB에 안 들어갔습니다` + 파일명까지 나옵니다.
- 뷰는 **버전·이름만** 엽니다. SQL 본문은 넣지 않습니다 - 스키마 변경 내역 전체를
  웹에 노출할 이유가 없습니다.

## v0.330.0 - 2026-08-30

**정리 한 번 — 실측하고, 위험한 것부터**

**① "오늘"이 UTC로 계산되던 곳 19군데**

- 코드 곳곳에 `new Date().toISOString().slice(0, 10)`이 흩어져 있었습니다. 이건
  **UTC 날짜**입니다. 한국 00:00~09:00 사이에는 **하루 전 날짜**가 나옵니다.
- 그래서 **아침 8시에 하원 체크표나 픽업 체크를 열면 어제 명단이 뜹니다.** 오후에만 쓰던
  화면이라 안 드러났을 뿐입니다. 담임 선생님들이 아침에 열기 시작하면 바로 나옵니다.
- 이 종류를 이미 **세 번** 고쳤습니다(출결 등록 / GPS 출발 판정 / 도착체크). 매번 그
  파일만 고쳤고, 그래서 매번 다시 나왔습니다. 이제 `src/lib/kst.ts` 하나로 모으고
  19개 파일을 모두 그것으로 바꿨습니다.
- 고쳐진 화면: 하원 체크표 · 교사 픽업 체크 · 우리 반 · 안내보드 · 셔틀 개요 ·
  학교 개요 · 파일럿 화면 4종 · 학기 전환 크론 · PDF 3종 · AI 2종.

**② 중복 제거**

- `timeAgo`("3분 전")가 **네 파일에 똑같이 복사**돼 있었습니다. 글자 하나만 달라져도
  화면마다 다른 말이 나옵니다. 하나로 모았습니다.
- 아무도 안 쓰는 파일 3개 삭제: `DashboardArea`(209줄) · `TaskListPane`(68줄) ·
  `/api/my-class`(119줄, 화면이 직접 조회하도록 바뀐 뒤 남아 있던 것).
- 껍데기만 남은 크론 2개 삭제: `shuttle-auto-arrive` · `shuttle-auto-depart`
  (`shuttle-auto`로 합쳐진 뒤 "옮겼습니다"만 답하던 라우트).
- **약 470줄이 줄었습니다.**

## v0.329.0 - 2026-08-30

**Pub/Sub 권한 오류도 '아직 안 됨'으로 넘깁니다**

방침: 토픽은 삭제하지 않고 남겨두되, 이 오류가 나면 건너뜁니다. 나중에 설정할 수도 있습니다.

- 맞습니다. 환경변수를 지우면 **나중에 다시 켤 때 무엇을 넣었는지 찾아야 합니다.**
  설정은 그대로 두고 이 오류만 넘기는 편이 낫습니다.
- `Pub/Sub 주제에 게시 권한 없음` 오류를 앞서 만든 "설정 안 됨" 분류에 넣었습니다.
  이 권한은 **구글 클라우드 조직 정책 때문에 최고관리자만 줄 수 있어**, 우리 쪽에서
  다시 시도해도 절대 달라지지 않는 종류입니다. 오류로 쌓을 이유가 없습니다.
- 진단 화면 크론 줄에 **무엇을 해야 하는지**가 그대로 적힙니다:
  `Pub/Sub 주제에 게시 권한이 없습니다(조직 정책상 최고관리자만 부여 가능).
  구글챗은 폴링으로 수집 중이라 지장 없습니다.`
- 나중에 권한을 받으면 진단 화면에서 `▶ 구글챗 구독 갱신`을 누르는 것만으로 켜집니다.
  설정을 지우지 않았으니 다시 만들 것이 없습니다.

## v0.328.0 - 2026-08-30

**Pub/Sub은 안 써도 됩니다 — 조직 정책에 막혔을 때의 정리**

- 구글 클라우드 조직 정책(`도메인 제한 공유`)이 `chat-api-push@system.gserviceaccount.com`
  추가를 막습니다. 이건 **최고관리자만 풀 수 있습니다.**
- 그런데 **구글챗 메시지는 Pub/Sub 없이도 이미 들어오고 있습니다.**
  `/api/cron/poll-chat-messages`가 몇 초마다 직접 읽어옵니다. Pub/Sub은 그걸 실시간
  푸시로 바꾸는 **선택 사항**이었습니다.
- `GOOGLE_CHAT_PUBSUB_TOPIC`을 비우면 이 크론은 조용히 건너뜁니다(예전에는 500 오류).
  안 쓰기로 한 기능이 매일 빨간 줄을 만들면 안 됩니다.
- 진단 화면에는 `설정 안 됨 · Pub/Sub 미사용(폴링으로 수집 중)`으로 표시됩니다.

## v0.327.1 - 2026-08-30

**같은 채팅방을 두 번 구독하던 것**

- 크론을 눌러 보니 **같은 스페이스(`spaces/AAAAsBOib5Y`)가 결과에 두 번** 나왔습니다.
  출결방·교사요청방 환경변수에 같은 방이 들어가 있습니다.
- 그대로 두면 같은 방에 구독을 두 번 만들고, 같은 오류도 두 번 기록되고, 메시지도 두 번
  들어옵니다. 설정이 잘못된 것이지만 **코드가 그걸 두 배로 키울 이유는 없어서** 중복을
  제거했습니다.
- 방을 실제로 나누실 거면 `GOOGLE_CHAT_SPACE_TEACHER_REQUESTS`를 따로 지정하시면 됩니다.

## v0.327.0 - 2026-08-30

**크론을 지금 눌러서 돌려보는 버튼 (개발자 → 진단)**

- 설정을 바꾸고 그게 먹었는지 보려면 **다음 예약 시각까지 기다려야** 했습니다. 하루 한 번짜리
  크론이면 하루입니다. 그 사이 다른 것을 건드리게 되고, 그러면 무엇 때문에 됐는지 안 됐는지도
  흐려집니다.
- **바꾸고 → 눌러보고 → 결과를 본다.** 이 세 걸음이 붙어 있어야 원인을 알 수 있습니다.
- 진단 화면 ⑤ 자동 작업에 다섯 개를 넣었습니다:
  구글챗 구독 갱신 · 구글챗 메시지 수집 · 셔틀 도착·출발 감지 · 오늘 픽업 예약 적용 ·
  정류장 좌표 학습
- **결과를 그대로 펼쳐 보여줍니다.** "성공"만 뜨면 무엇이 성공인지 알 수 없습니다 -
  구독이 만들어졌는지, 아직 설정이 안 됐는지, 몇 건이 처리됐는지가 답에 들어 있습니다.
- 돌릴 수 있는 작업은 **목록으로 제한**했습니다. 주소를 그대로 받으면 이 기능이
  "아무 내부 주소나 CRON_SECRET을 달고 불러주는 통로"가 됩니다. 개발자만 쓴다 해도
  열어둘 이유가 없습니다.

## v0.326.0 - 2026-08-30

**구글챗 구독 갱신이 매일 오류를 쌓던 것**

```
cron:chat-subscription-renew
Google Chat app not found. To create a Chat app, you must turn on the Chat API
and configure the app in the Google Cloud console.
```

- 이건 **고장이 아니라 아직 안 만든 것**입니다. 고칠 사람은 구글 클라우드 콘솔에 있고,
  코드가 몇 번을 다시 시도해도 달라지지 않습니다.
- 그런데 매일 오류로 기록되면서 두 가지를 망가뜨리고 있었습니다.
  · 오류 로그가 이걸로 채워져 **진짜 오류가 묻힙니다.**
  · "최근 24시간 오류 급증" 경고가 늘 켜져 있어 **경고가 무의미해집니다.**
- 이 종류(Chat API 미설정)는 오류로 남기지 않고, 진단 화면 크론 줄에
  **`설정 안 됨`** 으로 표시합니다. 숨기는 게 아니라 **있어야 할 자리로 옮긴 것**입니다.
- 설정이 아니라 진짜 실패면 예전처럼 오류로 남고 크론 상태가 빨강이 됩니다.

## v0.325.1 - 2026-08-29

**Sonnet 5 단가 인상 취소 확인 — 값은 그대로, 주석만 수정**

- 8월 31일에 끝날 예정이던 Sonnet 5 도입 특가($2/$10)가 **정가로 확정**됐습니다.
  예정돼 있던 9월 1일 $3/$15 인상은 **취소**되었습니다(Anthropic 공식 요금표 확인).
- 그래서 `pricing.ts`의 단가는 **건드리지 않았습니다**. AI 과금 화면 금액은 지금도 정확합니다.
- 대신 "곧 오릅니다"라고 적혀 있던 상단 주석만 고쳤습니다. 그대로 두면 나중에 누군가
  주석만 믿고 $3/$15로 잘못 올릴 수 있습니다.

## v0.325.0 - 2026-08-28

**개발자 메뉴를 개요 + 상단 탭으로**

개발자 메뉴만 다른 형식이라, 개요 메뉴와 같은 상단 탭으로 통일합니다.

- 지금까지 개발자 화면은 **한 장에 여섯 덩이**가 세로로 쌓여 있었습니다. 오류 로그는 맨
  아래라 **정작 오류가 났을 때 한참 스크롤해야** 보였습니다. 급할 때 찾는 것을 맨 아래
  두면 안 됩니다.
- 다른 대분류(업무·학교·셔틀)와 같은 모양으로 갈랐습니다.
  `📊 개요` · `🔎 진단` · `🚨 오류` · `🤖 AI 과금` · `💾 데이터` · `📜 변경 기록`
- 개요는 **데이터 현황 · 방치된 항목 · 시스템 상태**만 남겼습니다.
- 개요에서 AI 사용량 1000줄과 오류 20줄을 매번 읽던 것을 뺐습니다. **화면 하나 여는 데
  조회가 두 배로 들고 있었습니다.**
- **오류 급증 판정이 틀려 있었습니다.** 7일 평균을 "최근 20건 ÷ 7"로 계산해서, 오류가
  많을수록 평균이 오히려 낮게 나오는 구조였습니다. 7일 전체를 세도록 고쳤습니다.

## v0.324.0 - 2026-08-28

**진단 화면 (개발자 → 🔎 진단)**

문제: 스키마가 어긋날 때마다 SQL을 사람이 직접 붙여넣어야 했습니다. 무엇이 어긋났는지
앱과 Supabase 양쪽을 스스로 점검하는 화면이 필요합니다.

- 지금까지는 이상할 때마다 **SQL을 쓰고 → 사람이 돌리고 → 결과를 복사해 붙이고 →
  제가 읽는** 네 단계였습니다. 그중 셋이 사람 손입니다.
- 이 화면은 한 단계로 줄입니다. 자주 보는 것들을 미리 짜 넣어, **열면 바로 답이 나옵니다.**
- 원본 줄을 그대로 쏟지 않습니다. **판정과 그 이유만** 적습니다 - 표를 다시 해석해야 하면
  줄인 게 없습니다.

보는 것:

1. **Supabase 연결** — 붙는지, 응답이 몇 ms인지. 여기가 빨간색이면 아래는 볼 것도 없습니다.
2. **스키마** — 마이그레이션이 실제로 걸렸는지. information_schema를 뒤지지 않고 **앱과
   똑같이 그 칸을 읽어봅니다** (칸이 있어도 권한이 막으면 기능은 똑같이 안 되니까요).
   빠진 것이 있으면 어느 마이그레이션을 돌려야 하는지까지 적습니다.
3. **GPS 차량** — 노선별 마지막 신호, 오늘 도착·출발이 누구(사람/GPS)로 찍혔는지.
   좌표 없는 정류장, 주소 없는 정류장.
4. **행선지 선택 학생** — 선택지에 이름·주소가 다 붙었는지.
5. **자동 작업(크론)** — 마지막으로 돈 시각. 오래됐으면 그 기능은 지금 멈춰 있는 것입니다.
6. **오늘 학부모 연락** — 수집·픽업·미답변 건수.

새로 봐야 할 것이 생기면 이 화면에 한 덩이 더 붙입니다. 다음부터는 SQL 없이 보입니다.

## v0.323.0 - 2026-08-28

**갈 곳 이름이 없는 선택지는 회색으로**

- 이준서의 7호처럼 이름도 주소도 없는 줄이 남아 있습니다. 파란 버튼으로 나란히 두면
  **아는 것처럼 보여서**, 물어본 대답과 상관없이 눌러버릴 수 있습니다. 눌리면 그 호차에
  실제로 태워집니다.
- 이름이 없다는 건 **우리도 그게 뭔지 모른다**는 뜻이므로, 회색에 `7호?`로 둡니다.

## v0.322.0 - 2026-08-28

**① 막혀서 한 바퀴 돌고 온 차를 다시 도착으로 표시합니다**

문제: 하원 시간에도 길이 막혀 차가 한 바퀴 돌고 오는 일이 있습니다. 갔던 차가 다시 가까워지면
(이때는 반경을 더 좁혀서) 다시 도착으로 잡아야 합니다.

- 반경을 줄이자는 것이 맞습니다. 처음 도착은 "학교 근처에 왔다"만 알면 되지만, **돌아옴은
  정말 학교 앞에 다시 섰는지**를 봐야 합니다. 근처를 스쳐 지나가는 것까지 되돌리면
  안내보드가 켜졌다 꺼졌다 합니다. 돌아옴은 **60m**로 잡았습니다(출발은 250m).
- 도착을 새로 찍는 대신 **출발 기록을 지웁니다.** 도착 기록은 살아 있으므로 출발만
  없어지면 화면이 그대로 "도착함"으로 돌아갑니다.
- **사람이 직접 누른 출발은 되돌리지 않습니다.** 기계가 사람 판단을 뒤집으면 안 됩니다.

**② 행선지 버튼을 "학원 / 집·기업은행"으로**

기준: 형제의 행선지는 차량이 아니라 **장소**(학원 / 집·기업은행)로 묻고, 그 답에 따라
호차를 배정합니다. 실제로 물어보는 것이 장소이기 때문입니다.

- 맞습니다. 아이에게 **"몇 호차 타?"라고 묻지 않습니다. "어디 가?"라고 묻습니다.**
  화면이 물어보는 말과 다르게 생기면, 듣고 나서 머릿속으로 한 번 더 바꿔야 하고 그때
  틀립니다. 호차는 우리가 아는 것이지 아이가 아는 것이 아닙니다.
- 이제 `이준서 [학원] [집·기업은행] [✕]` 로 뜹니다. 누르면 호차 배정은 알아서 됩니다.
- 버튼에 적을 말은 배정마다 따로 둡니다(`choice_label`). 다른 학생에게 다른 사정이
  생겨도 코드를 고칠 필요가 없습니다. 비어 있으면 예전처럼 호차 번호를 씁니다.

## v0.321.0 - 2026-08-28

**판정 기준을 오후 4시 하나로 고정했습니다**

기준: 16시가 하원 기준 시각입니다. 16시에 학교 근처에 있으면 도착으로 보고, 그 뒤 멀어지면
출발로 봅니다. 신호는 15시 30분부터 받지만 그 사이에는 도착·미도착을 표시하지 않습니다.

- 바로 앞 판(v0.320)에서 저는 **노선별 출발 예정시각**으로 각각 다르게 쟀습니다. 그보다
  이 편이 낫습니다. 규칙이 하나면 화면에서 "왜 저 차만 다르지"가 없고, 예정시각이 비어
  있거나 틀린 노선에서도 똑같이 동작합니다.
- **오후 4시 전**: 도착·출발을 아예 판정하지 않습니다. 신호는 15시 30분부터 그대로 받아
  지도에 그리되, 화면에는 표시하지 않습니다. 일찍 와서 차를 대거나 자리를 옮기는 것은
  하원과 무관한 일입니다.
- **오후 4시부터**: 학교 근처에 있으면 도착, 멀어지면 출발.
- 벗어남 판정 거리는 250m를 유지합니다(100m는 주차 자리 옮기는 것과 진짜 떠나는 것을
  못 가립니다).

## v0.320.0 - 2026-08-28

**"일찍 와서 차를 옮긴 것"을 출발로 세던 문제**

문제: 27호 기사님이 일찍 오셔서 잠깐 차를 댔다가 4시 하원을 위해 옮기신 것이 **출발**로
집계됐습니다.

- **거리만으로는 절대 못 가립니다.** 차를 옮기든 진짜 떠나든 GPS에는 똑같이 "멀어졌다"로
  보입니다. 가르는 것은 거리가 아니라 **시각**입니다 - 하원 출발은 아이들을 태우고 가는
  일이라 예정시각 언저리에만 일어납니다. 3시에 멀어진 것은 무슨 일이든 하원 출발이 아닙니다.
- 노선의 **출발 예정시각**을 기준으로 삼았습니다.
  · 도착: 예정시각 **40분 전부터**만 인정합니다. 한 시간 일찍 온 차를 "도착함"으로 띄우면
    **안내보드를 보고 아이들이 그때 나가버립니다.**
  · 출발: 예정시각 **15분 전부터**만 인정합니다.
- 그보다 이른 시각에 학교를 벗어나면, 출발로 찍지 않고 앞서 찍힌 도착을 **되돌립니다.**
  그대로 두면 차가 없는데 안내보드에 "도착함"으로 계속 떠 있게 됩니다. 다시 들어오면
  도착이 새로 찍힙니다. (사람이 직접 누른 도착은 건드리지 않습니다.)
- 벗어남 판정 거리를 100m → **250m**로 넓혔습니다. 100m는 주차 자리를 옮기는 것과 진짜
  떠나는 것을 못 가릅니다 - 학교 앞에서 차를 빼 골목 한 바퀴만 돌아도 넘습니다.

## v0.319.0 - 2026-08-28

**인쇄에서 26-2호 뒤가 사라진 이유 — 제가 잘라내고 있었습니다**

- 다음 장으로 넘어간 게 아니라 **그냥 없어진 것**이었습니다. 앞 판에서 상자 높이를
  "잰 높이 × 축소비"로 잡고 `overflow: hidden`을 걸었는데, **잰 값이 실제보다 조금이라도
  작으면 상자가 내용보다 짧아지고 나머지를 잘라 없앱니다.**
- 상자 높이를 **A4 한 장 그대로**로 바꿨습니다. 재기가 틀려도 잘리지 않습니다 - 축소가
  조금 모자라면 한 장을 꽉 채울 뿐입니다.
- 인쇄 직전 다시 잴 때 **화면에 즉시 반영**되도록 했습니다(`flushSync`). 평소처럼 다음
  렌더를 기다리면 브라우저가 **그 전 상태를 그대로 종이에 찍습니다** - 계산은 맞는데
  종이만 틀린 상황이 됩니다.
- 한글 글꼴이 늦게 붙으면 줄 높이가 달라지므로, 글꼴이 붙은 뒤 한 번 더 잽니다.
- 축소 하한을 0.25까지 열었습니다. 작게 나오더라도 **한 명도 사라지지 않는 쪽**이 낫습니다.

## v0.318.0 - 2026-08-28

**① 안내보드에 GPS 도착이 안 뜬 이유 — 부르는 사람이 없었습니다**

문제: 27호가 실제로 도착했는데 안내보드에 표시되지 않았습니다. GPS로 잡힌 도착·출발이
곧바로 화면에 나와야 합니다.

- 도착·출발을 판단하는 코드는 **처음부터 있었습니다.** 문제는 **아무도 그걸 부르지 않았다**는
  것입니다. `/api/cron/shuttle-auto` 는 `vercel.json` 크론 목록에 없고, 바깥
  스케줄러(cron-job.org)가 1분마다 불러주기로 되어 있었습니다. 그게 안 돌면 GPS가 아무리
  들어와도 도착은 **영영 안 찍힙니다.**
- 이제 **GPS 신호가 들어온 그 자리에서** 바로 판단합니다. 바깥 스케줄러에 기대지 않습니다.
  방금 신호를 보낸 그 차 하나만 보므로 비용도 거의 없습니다.
- 크론이 살아 있어도 겹치지 않습니다 - 같은 줄은 DB 제약이 막습니다.
- **출발 판단이 UTC 날짜를 보고 있었습니다.** 도착은 한국 날짜로 찍는데 출발만 UTC라,
  두 함수가 서로 다른 날을 볼 수 있었습니다. 그러면 도착은 찍혔는데 출발은 "오늘 도착한
  차가 없다"며 안 찍힙니다. 한국 기준으로 맞췄습니다.
- 위치 수신 진단 줄에 `학교도착 기록` / `출발 기록`이 함께 남습니다. 안 보이면 또 처음부터
  찾게 됩니다.

**② 모바일 도착체크 정리**

- 문제: 행선지 선택 UI가 화면을 너무 많이 차지했습니다. 호차만 고르게 하고 아래로 내립니다.
- 맞습니다. 하루에 두 번 누르는 일이 화면 맨 위 절반을 잡아먹고 있었습니다. **차량 카드가
  먼저 보여야 합니다.** 한 줄짜리로 줄여 맨 아래로 내렸습니다(`이준서 [4-2] [9] [✕]`).
  정류장 주소는 버튼을 꾹 누르면 뜹니다.
- `눌러서 도착` 안내 글자를 뺐습니다.

## v0.317.0 - 2026-08-28

**인쇄가 "아직도 차량 짤린다" — 짐작을 그만두고 실제로 재서 맞춥니다**

- 앞 판은 인원 수를 보고 글자 크기를 **단계로 골랐습니다.** 노선 수도 인원도 날마다
  달라서, 어떤 단계를 잡아도 어떤 날은 넘칩니다. 짐작이었습니다.
- 이제 화면 밖에 **A4 폭(196mm) 그대로 그려두고 높이를 잽니다.** 한 장을 넘으면 넘친
  만큼 축소해서 인쇄합니다. 몇 명이 타든 반드시 한 장입니다.
- 축소만 하면 브라우저는 여전히 "넘친다"고 보고 다음 장을 만듭니다. 줄어든 만큼
  상자 높이도 함께 줄였습니다.

**요일 표시를 뺐습니다**

- 기준: 인쇄본에는 요일을 빼고 이름만 싣습니다. 요일은 화면에서 이미 계산되어 나옵니다.
- 맞습니다. 종이에는 **오늘 타는 아이만** 있으니 요일은 알려주는 것이 없고 줄만 길어집니다.

**맨 위 한 줄**

- 제목("하원 차량 체크표")을 뺐습니다. 들고 다니는 사람이 뭔지 모를 리 없습니다.
- `2026년 8월 28일 금요일 · 탑승 118명` 만 남겼습니다. 연도까지 넣은 이유는 종이가
  며칠 뒤에도 굴러다니기 때문입니다 - 언제 것인지 모르는 명단이 제일 위험합니다.

## v0.316.0 - 2026-08-28

**하원 체크표 인쇄를 A4 한 장으로 다시 만들었습니다**

기준: A4 한 장. 화면 위젯을 그대로 옮기지 않고 표로 정리하며, 오늘 타는 아이만 싣습니다.

- **이름 칸이 9개로 고정돼 있었습니다.** 이건 보기 문제가 아니라 사고입니다 - 열 명이 넘게
  타는 노선에서 **열 번째부터는 아무 표시 없이 그냥 사라졌습니다.** 종이만 보고 태우는
  분은 그 아이가 원래 없는 줄 압니다.
- 칸 수를 없앴습니다. 이름은 한 칸 안에 작은 상자로 늘어놓습니다. **몇 명이든 다 나오고**
  줄 높이만 늘어납니다.
- 오늘 안 타는 학생(다른 요일제·픽업·결석)은 뺐습니다. 대신 맨 위에 `탑승 118명 · 노선
  26대 · 픽업 2 · 결석 1`로 몇 명이 왜 빠졌는지 적습니다 - 종이에서 빠진 이유를 알 수
  없으면 "왜 얘가 없지"를 다시 물어보게 됩니다.
- 오늘 아무도 안 타는 노선은 종이에서 뺍니다. 빈 줄이 한 장을 잡아먹습니다.
- 인원에 따라 글자가 자동으로 작아집니다. **두 장으로 갈라지면 한 장만 들고 나가서 뒷장
  아이들을 놓칩니다.** 넘치느니 작게 뽑습니다.
- 노선 한 줄은 페이지 경계에서 잘리지 않습니다(`page-break-inside: avoid`).
- 타는 순서는 화면과 같은 **정류장 순서**입니다. 요일제 학생은 `(월수금)이름`으로 표시합니다.

## v0.315.0 - 2026-08-28

**행선지 선택 버튼에 정류장 주소를 함께 띄웁니다**

- 기준: 형제는 가는 곳이 같습니다. 정류장을 형제끼리 같게 두고, 둘 중 한 명이라도 타면
  그 노선으로 잡습니다.
- 호차 번호만 보고 누르면, **형제가 서로 다른 곳에 내리게 잘못 눌러도 아무도 모릅니다.**
  버튼 아래에 `4-2호 · 서초구 동광로 28`처럼 어디서 내리는지 적었습니다.
- 주소가 비어 있으면 `정류장 주소 없음`이라고 그대로 보여줍니다. 숨기면 문제가 있는
  줄인지 알 수 없습니다.
- "한 명씩 설정할 수 있도록"은 **이미 그렇게 되어 있습니다.** 묶음이 형제 각각
  따로(`lee-junseo` / `lee-junwoo`)라, 화면에도 이름별로 버튼 줄이 따로 뜹니다.
  준서는 집, 준우는 학원처럼 갈라지는 날도 그대로 누르시면 됩니다.

## v0.314.0 - 2026-08-28

**① 링크 삭제**

- 문제: 링크를 만들 수만 있고 지울 수는 없었습니다.
- 도착체크 링크·안내보드 링크 모두 `🗑 삭제`를 넣었습니다. 지우기 전에 한 번 묻습니다 -
  **이미 나눠준 링크를 지우면 받으신 분들 화면이 전부 막히고, 되돌릴 수 없습니다.**
- 삭제에 `.select()`를 붙였습니다. 권한이 막혀 있으면 **오류 없이 0줄이 지워집니다.**
  그대로 화면에서만 빼면 새로고침할 때 되살아나는데, 지웠다고 믿고 계속 쓰던 링크가
  살아 있는 것이 가장 나쁩니다. 이제 안 지워졌으면 안 지워졌다고 말합니다.

**② 도착체크 카드에 기사님 성함**

- 기준: 호차 / 차량번호 / 기사님 / 타는 학생 뱃지가 함께 보여야 합니다.
- 기사님 성함을 넣었습니다. 차량번호는 아직 못 적은 차가 있어서 **없으면 그냥 비워둡니다** -
  "미등록" 같은 글자를 넣으면 좁은 칸에서 진짜 번호처럼 읽힙니다.
- 카드에 `눌러서 도착` / `눌러서 출발` 한 줄을 넣었습니다. 여러 분이 같은 화면을 동시에
  보게 되는데, 처음 쓰는 분은 "도착함"만 보고 다음에 뭘 눌러야 할지 모릅니다.
- 꾹 누르면 기사님 전화, 한 번 누르면 도착·다시 누르면 출발, GPS 연결 차량 자동 감지는
  이미 되어 있습니다(GPS로 잡힌 것은 `자동·GPS 감지`로 구분해 표시합니다).

**③ 앞서 드린 SQL 두 개가 틀렸습니다**

- 행선지 SQL: 칸을 만드는 마이그레이션과 설정을 **두 파일로 나눠 드려서** 순서가 어긋났습니다
  (`column a.choice_group does not exist`). 한 파일로 합쳤습니다.
- GPS 점검 SQL: 위치 표 이름을 `shuttle_positions`로 잘못 적었습니다. 실제 이름은
  `shuttle_pilot_pings`입니다. 확인하지 않고 쓴 제 잘못입니다.

## v0.313.0 - 2026-08-28

**① 이준서·이준우 — 행선지를 그날 정합니다**

문제: 학원 요일이 고정이 아니라 자주 바뀝니다. 미리 특정해두면 놓치고, 둘 다 안 타는 날도
많습니다. 그래서 이 학생들은 배정을 비워두고, 그날 행선지를 물어본 뒤 그때 한쪽으로
배정합니다. 확인은 모바일 도착체크 단독 링크에서 합니다.

- 지금 상태가 가장 위험했습니다. 같은 아이가 두 노선에 각각 배정돼 **양쪽 다 '탄다'로
  보입니다.** 4-2호 기사님은 9호가 태웠겠거니, 9호 기사님은 4-2호가 태웠겠거니 하면
  **아무도 안 태웁니다.** 정원도 두 번 잡힙니다.
- 배정은 지우지 않습니다(정류장이 서로 다릅니다). 대신 `choice_group`으로 묶어
  **정하기 전까지 어느 명단에도 안 나오는 줄**로 만들었습니다.
- 도착체크 화면 맨 위에 `⚠ 행선지 확인 필요` 카드가 뜹니다. 물어보고 `4-2호` `9호`
  `안 탐` 중 하나를 누르면 그때 그 호차 명단에 나타납니다. 누르기 전까지 안 사라집니다.
- 체크표·안내보드·하원 대시보드에도 **같은 규칙 하나(`isUndecidedChoice`)를 씁니다.**
  한 화면에서만 숨기면 다른 화면에서 타는 것으로 보여, 지금 고치려는 위험이 그대로입니다.

**② 도착체크: 아무도 안 타는 호차를 없앴습니다**

- 문제: 학생이 배정되지 않은 호차까지 화면에 떴습니다. 누를 일 없는 버튼이 자리만 차지하고,
  급할 때 옆 칸을 잘못 누르게 만듭니다.

**③ 도착체크 화면의 날짜가 UTC였습니다**

- `toISOString()`과 `getDay()`를 쓰고 있었습니다. 서버가 UTC라 **한국 아침 9시 전에는
  어제 명단이 뜨고, 요일도 하루 밀립니다.** 한국 기준으로 고쳤습니다.
- 픽업·결석은 이미 제대로 반영되고 있었습니다(대기 인원에서 빼고 별도로 셉니다).

## v0.312.0 - 2026-08-28

**① 문의 글이 회색으로 흐려진 이유**

- 어제 넣은 "답글 자동 감지"가 `answered_at`을 채웁니다. 그런데 화면 곳곳이 **`answered_at`만
  보고 "끝난 건"으로 판단**하고 있었습니다 - 회색 처리(opacity-40), 급한 건 빨간 점, 출결
  처리 버튼까지 전부.
- 목록에서만 안 숨기게 고쳤지, **나머지 여섯 군데를 놓쳤습니다.**
- 이제 판단 기준을 함수 하나(`isDone`)로 모았습니다. **사람이 체크한 것만** 끝난 것입니다.

**② 업무 대시보드에 문의가 안 뜬 이유**

- 대시보드 조회가 `answered_at is null`이었습니다. 답글이 감지된 건은 통째로 빠졌습니다.
  같은 원인입니다.

**③ 하원 픽업에 없는 아이가 계속 뜬 이유 (이예온·심규민)**

- 이 자리가 **원문을 매번 다시 읽어 스스로 판단**하고 있었습니다. 14일치 학부모 연락을
  긁어와 픽업인지 다시 정하고, 날짜도 다시 계산했습니다. 그래서
  · 하원 체크표에서 지워도 원문은 그대로니 **다음 조회에서 되살아납니다.**
  · 며칠 전 "금요일에 데리러 갈게요"가 **오늘도 금요일이면 다시 오늘 것이 됩니다.**
- 바로 위 결석 칸에 **똑같은 실수를 적어놓고도** 픽업은 그대로 뒀습니다:
  "원본을 매번 다시 읽는 방식으로는 '처리했음'을 남길 데가 없어, 지운 것이 계속 되살아났습니다."
- 이제 원문을 다시 읽지 않습니다. **하원 체크표가 유일한 기준**입니다. 체크표에서 되돌리면
  대시보드에서도 사라집니다 - 되살아날 곳이 없습니다.
- 차량을 안 타는 학생만 예외로 확정된 오늘 픽업에서 가져옵니다(재해석 없음).

## v0.311.0 - 2026-08-28

**① 답글이 달려도 목록에서 내리지 않습니다**

기준: 처리된 건도 목록에는 남기되 초록색 체크로 표시합니다. 제대로 체크됐는지 한 번 더
확인한 뒤 업무보드에서 정리하기 위해서입니다.

- 맞는 순서입니다. 답글 감지는 **기계의 판단**입니다. 선생님이 "네~"라고 한 것을 답으로
  셌을 수도 있고, 다른 이야기에 단 댓글을 셌을 수도 있습니다. 그걸 **사람이 확인하기도 전에**
  목록에서 빼버리면, 틀렸을 때 알아챌 방법이 없습니다.
- 이제 자동 감지된 건은 이름 뒤에 **초록 ✓** 를 달고 목록에 그대로 남습니다.
- 사람이 왼쪽 네모를 체크해야 내려갑니다(`answered_via='수동'`). 기계가 찾은 것과
  사람이 확인한 것을 칸 하나로 확실히 갈라놓았습니다.

**② 반복 픽업은 명단을 고쳐야 끝납니다**

기준: 매주 금요일 건은 특정 날짜가 아니라 셔틀 배정 자체가 바뀌는 일입니다. 근본 조치는
하원 셔틀 명단에서 그 요일 체크를 푸는 것입니다.

- 그게 근본입니다. 지속 특이사항은 매일 아침 픽업으로 **덮어쓸** 뿐, 명단에는 그 아이가
  계속 타는 것으로 남습니다.
- 그래서 반복 픽업을 찾으면 **[셔틀 명단 수정] 업무를 함께 만듭니다.** 이걸 안 남기면
  "특이사항으로 처리됐으니 됐지" 하고 명단은 영영 안 고쳐집니다.
- 특이사항 자동 등록은 그대로 둡니다 - 명단을 고치기 전까지의 **안전망**입니다.

# CHANGELOG

이 파일은 `gia-ops-web`의 버전별 변경 이력을 기록합니다. 버전 번호는 `package.json`의
`version` 값과 항상 일치시킵니다. 업데이트할 때마다 이 파일 맨 위에 새 항목을 추가하고,
같은 내용을 GitHub Desktop의 커밋 Summary/Description에도 그대로 사용하면 됩니다.

## v0.310.0 - 2026-08-28

**① "10건이 안 보여, 노유겸만 떠 있어" — 숨긴 것이지 없는 것이 아니었습니다**

- 10건 다 들어와 있었습니다. **선생님이 토들에서 답을 다신 9건이 자동으로 '처리됨'이 되어
  목록에서 빠진 것**입니다(수집기가 답글을 읽어 표시합니다).
- 동작 자체는 맞습니다. 문제는 **말해주지 않는다**는 것이었습니다. 목록이 스스로 줄어드는데
  아무 표시가 없으면 사람은 **수집이 안 된다고 생각합니다.**
- `기록` 버튼에 **`+9`** 처럼 숨긴 개수를 붙였습니다. 눌러서 바로 볼 수 있습니다.

**② 매주 반복되는 픽업을 셔틀 배정에 반영합니다**

기준: '매주 금요일'은 오늘이 금요일이면 오늘 건이면서 동시에 **앞으로의 모든 금요일**입니다.
오늘을 포함해 매주 금요일 셔틀을 타지 않도록 해야 합니다.

- 정확합니다. "매주 금요일 픽업"은 그날 하루짜리 연락이 아니라 **셔틀 배정을 바꾸는
  일**입니다. 한 번짜리로 처리하면 **다음 주 금요일에 아이가 그냥 차에 타버립니다.**
- AI가 `recurring_weekdays`를 함께 뽑도록 했습니다("매주 금요일", "금요일마다",
  "앞으로 화·목은", "every Friday").
- 반복이면 **지속 특이사항으로 자동 등록**합니다. 매일 아침 크론이 읽어 그 요일이면 픽업으로
  찍으므로, 사람이 매주 다시 입력하지 않아도 됩니다.
- **크론에 두 군데 구멍이 있었습니다.**
  · 끝날(`effect_to`)이 비어 있으면 아예 걸러냈습니다 — "매주 금요일"에는 끝나는 날이
    없으니, 반복 픽업은 **한 번도 적용된 적이 없었습니다.**
  · 요일(`effect_days`)을 픽업·결석에는 안 보고 있었습니다. 이제 정해진 요일에만 적용합니다.

## v0.309.0 - 2026-08-28

- **'기타'로 버린 글에도 요약 한 줄을 남깁니다.**
  · 24시간에 들어온 토들 31건을 실측했더니 **9건이 '기타'로 조용히 사라지고** 있었습니다.
    본문도 요약도 없어서 **무엇을 왜 버렸는지 확인할 방법이 아예 없었습니다** — 진짜
    인사말이었는지 잘못 버린 것인지 사람이 검증할 수 없다는 뜻입니다.
  · 본문은 그대로 안 남깁니다(학부모 대화를 쌓아두지 않는다는 원칙). 대신 **요약과 판단
    근거 한 줄**만 남깁니다. 원문이 아니라 "무엇에 관한 글이었나"라 쌓아두는 부담이 훨씬
    작고, 잘못 버린 것을 찾아낼 수 있게 됩니다.

## v0.308.0 - 2026-08-28

- **출결알림에서 새 메시지가 맨 위로.** 채팅창은 보통 아래로 쌓이지만, 이 칸은 대화를 나누는
  곳이 아니라 **오늘 처리할 것을 훑는 곳**입니다. 아침에 열자마자 방금 온 결석 통보가 보여야
  하는데 아래로 쌓이면 매번 끝까지 스크롤해야 했습니다.
- **구글챗 미러 보관 기간 180일 → 30일.** 이건 우리 자료가 아니라 구글챗에 있는 대화를
  복사해 온 것입니다. 원본은 구글에 그대로 남아 있으니 오래 쥐고 있을 이유가 없고, 학부모·
  학생 이야기가 담긴 글이라 필요 이상으로 오래 두는 것 자체가 위험합니다.
  · 출결 판단은 이미 `attendance_entries`에 남으므로 **원문이 지워져도 기록은 남습니다.**
- **출결내역 이름 옆 빈 네모(⬜)를 없앴습니다.** 맞습니다 —
  ⬜는 "아직 등록 대상이 아니다"라는 **없음**을 굳이 칸을 잡아 그린 것이었고, 그만큼 이름이
  밀렸습니다. 정말 필요한 건 "내릴 수 있다"뿐이라 `✕`만 남깁니다.

## v0.307.0 - 2026-08-28

**AI 오분류를 그 자리에서 가르칩니다.**

담당자가 실제로 틀린 두 건을 짚어주셨습니다.
- `선생님 55분 도착입니다^^` → 픽업으로 분류됨. **아이를 데려간다는 말이 없습니다.**
- `첼로 가지러 오피스로 갈게요` → 픽업으로 분류됨. **가지러 가는 대상이 물건입니다.**

- **AI 지침에 '픽업이 아닌 것'을 못 박았습니다.** 두 사례를 그대로 넣고, 판단 기준을 한 줄로
  줬습니다 — *"이 문장에 '아이를' + '데려간다'가 둘 다 있는가?"* 하나라도 없으면 문의입니다.
  · 애매할 때 어느 쪽으로 틀리는 게 나은지도 적었습니다: **픽업으로 잘못 넣으면 아이가 차를
    못 타지만, 문의로 잘못 두면 사람이 한 번 더 읽을 뿐입니다.**
- **`🎓 픽업 아님으로 가르치기`** 를 근거 창에 넣었습니다. `표시만 지우기`는 오늘 하루만
  고칩니다 — 원래 연락이 남아 내일 또 올라오고, 사람은 같은 걸 매일 지우다 지쳐서 그냥
  둡니다. 이 버튼은 **세 가지를 한 번에** 합니다.
  · 그 연락을 '무시'로(문의함에는 남아 답을 놓치지 않습니다)
  · 발신자별 정정 기록 +1 → 같은 발신자의 다음 픽업은 자동확정 대신 사람 확인으로
  · 그 연락에서 만들어진 출결 등록도 함께 내림(한 곳만 고치면 다른 곳이 어긋납니다)

**사선에는 반드시 이유가 붙습니다.**

문제: 사선(픽업·결석) 표시에 근거가 없는 건이 있었습니다. 사선에는 반드시 사유가 있어야 합니다.

- 이제 픽업·결석 뱃지에는 **예외 없이** 왼쪽 아래 `?` 가 붙습니다.
- 근거를 못 찾은 것은 **주황색**입니다. 그냥 정보가 없는 게 아니라 **확인해야 할 일**이라
  눈에 띄어야 합니다. 눌러도 빈 창이 아니라 "왜 붙었는지 찾지 못했습니다"라고 적고,
  지금 할 수 있는 일(표시 지우기)을 함께 둡니다.
- 받은 날이 오늘이 아니면 **`← 오늘 온 연락이 아닙니다`** 를 붙입니다.

## v0.306.0 - 2026-08-28

- **`9/16~23` 같은 날짜 기간을 읽습니다.**
  · 이 형태를 못 읽어 기간이 통째로 무시되고 **글이 온 날 하루짜리**로 등록됐습니다.
    9월 얘기인데 오늘 결석으로 뜬 이유입니다 — 가장 명확하게 적어준 형태를 못 읽고 있었으니,
    다른 어떤 추측보다 먼저 봐야 할 자리였습니다.
  · `9/16~23` · `9/16~9/23` · `9.16 - 9.23` · `9월16일~23일` 네 가지로 확인했습니다.
    뒤쪽 달이 생략되면 앞의 달을 씁니다(사람이 그렇게 읽으니까요).
- **멘션 예외 세 분을 넣었습니다.**
  · 임시 목록입니다. 계정이 생기면 저절로 들어오므로 겹쳐도 문제없고, 다 등록되신 뒤엔
    지워도 됩니다.
  · **직장 메일 아이디(@giamicro.com 앞부분)도 함께** 봅니다. 성함 칸이 비어
    있어도 메일 아이디는 반드시 있으니, 등록이 늘수록 이쪽이 주력이 됩니다.
- **체크표의 모든 픽업·결석에 근거를 답니다.**
  · 예전에는 토들에서 자동으로 붙은 것만 `?` 표시가 있었습니다. 아침 크론이 지속 특이사항·
    예약 픽업으로 찍어놓은 것, 담당자가 직접 누른 것도 "누가 왜 찍었는지" 모르기는
    마찬가지입니다. 사선이 그어져 있는데 이유를 모르면 결국 전화로 확인하게 됩니다.
  · 이제 누가(사람 이름 / `AI(기간 특이사항)` 등) 언제 찍었는지가 함께 뜹니다.

## v0.305.0 - 2026-08-27

**① 세 낱말짜리 선생님 성함을 제대로 지웁니다**

문제: `@Carina Ann John`에서 이름을 'carina ann'까지만 읽어 'john'이 요한이로 잘못 붙었습니다.

- 멘션은 두 낱말까지만 지우고 있었습니다. 더 먹게 하면 `@Janelle Story Maya is absent`에서
  **마야가 사라집니다.** 낱말 수로는 `Story Maya`와 `Ann John`을 구별할 수 없습니다.
- 구별할 수 있는 건 **그게 실제 선생님 성함인지**뿐입니다. 그래서 계정 명단(app_users)을
  받아, 멘션 뒤 낱말이 실제 성함과 맞아떨어지는 만큼만 더 먹습니다. 명단에 없으면 예전처럼
  두 낱말에서 멈춥니다 — 모르면 덜 먹는 쪽이 안전합니다(학생을 지우는 것보다 낫습니다).
- 선생님이 새로 오시면 계정이 생기면서 **저절로 반영됩니다.** 목록을 따로 관리하지 않습니다.
- 네 가지로 확인했습니다: `@Carina Ann John … 임예나` → 임예나만 /
  `@Janelle Story Maya is absent` → 마야만 / `@Jueun Cho @Paul Lee @John Kang … 임예나` →
  임예나만 / `Peyton is absent` → 정하임만.

**② 기간이 길게 읽힌 출결을 자동으로 밀지 않습니다**

문제: 출결과 픽업이 날마다 갱신되지 않고 쌓이고 있었습니다.

- 대시보드는 "기간이 오늘을 품는 것"을 올립니다. 그래서 한 글에서 뽑은 기간이 잘못
  길어지면 그 아이가 **매일** 목록에 남습니다 — 지운 적도 없는데 쌓이는 것처럼 보입니다.
- 21일이 넘게 읽히면 **하루로 줄이고 '확인필요'로** 돌립니다. 버리지는 않습니다(진짜 한 달을
  쉬는 경우도 있으니). 자동이 길게 미는 것보다 사람이 짧게 시작하는 편이 되돌리기 쉽습니다.
- 무엇이 오늘 목록을 채우는지 낱낱이 보는 조회를 함께 드립니다
  (`outputs/대시보드_오늘출결픽업_왜쌓이나.sql`).

## v0.304.0 - 2026-08-27

**① 노선 지도에 기사님이 실제로 다니신 길을 그립니다**

문제: 지도에 그려진 경로가 계산값이라, 기사님이 실제 다니시는 길과 달랐습니다.

- 지금까지 그려진 선은 카카오에 "이 정류장들을 도는 최단 경로"를 물어본 **계산 결과**였습니다.
  기사님은 상습 정체 구간을 피하시고, 좌회전이 어려운 교차로를 돌아가시고, 아파트도
  들어가는 문이 정해져 있습니다. **그 차이는 지도만 봐서는 알 수 없고 자취에만 남습니다.**
- 가장 최근 **운행일**의 GPS 자취를 진한 초록 선으로 그립니다("어제"로 못 박지 않습니다 —
  공휴일·방학이면 빈 지도가 되니, 최근 30일 안에서 마지막으로 실제 다닌 날을 찾습니다).
- 계산한 선은 지우지 않고 **옅은 점선**으로 뒤에 남깁니다. 둘을 겹쳐 보면 **어디서 갈리는지**가
  그대로 보이고, 그게 정류장 순서를 고칠 근거가 됩니다.
- 800m 넘게 벌어진 구간은 잇지 않습니다(터널·신호 끊김). 없는 길을 그리지 않습니다.

**② 셔틀 자료를 학기로 갈랐습니다**

문제: 배차표에 여름캠프 차량이 그대로 남아 있었습니다. 학기 기록은 유지하되 차량은 학기에
맞게 갈려야 합니다.

- 지난 학기 노선은 **지우지 않습니다.** 그때 누가 몇 호차를 탔는지, 몇 시에 어느 정류장에
  섰는지가 전부 그 노선에 매달려 있어서, 지우는 순간 지난 학기 기록이 통째로 미아가 됩니다.
- 대신 **기본은 현재 학기만** 보고, 배차표 위 탭으로 지난 학기를 꺼내 봅니다. 지난 학기를
  보고 있을 때는 `지난 학기 — 기록 보기용` 이 계속 붙어 있습니다.
- 배차표 · 지역별 · 개요 지도도 이제 학기로 거릅니다(노선관리·탑승배정은 v0.297.0에서 완료).
- **새 노선을 만들 때 학기를 명시**합니다. 예전에는 데이터베이스 기본값에 맡겨서, 값이
  다르면 방금 만든 노선이 화면에서 바로 사라졌습니다. 만든 사람은 "저장이 안 됐나?" 하고
  또 만들게 되고 — 그렇게 중복이 쌓입니다.

## v0.303.0 - 2026-08-27

**① GPS 현황에 지도·수신 지표 패널을 함께 뒀습니다**

문제: 링크·기기 화면에 'GPS 연결 1'이라는 숫자만 떠서, 어느 차가 연결됐는지 알 수 없었습니다.
위에는 전 호차 한 줄 요약, 아래에는 운행 중인 차의 지도와 수신 지표. 이제 "어느 차가
이상한가"에서 "그 차가 지금 어디인가"로 눈만 내리면 됩니다.

**② 주간 관찰기록 — 담임 선생님 대거 사용 대비 점검**

배경: 다음 주부터 담임 선생님들이 한꺼번에 쓰기 시작해 대량의 자료가 들어옵니다. 그때
오류가 나면 안 됩니다.
지금까지 몇 분이 띄엄띄엄 쓰셔서 안 드러난 것들이 있었습니다. 넷을 찾아 고쳤습니다.

- **저장 실패를 화면이 삼키고 있었습니다.** 실패하면 `return null`뿐이라 "자동 저장 실패"
  넉 자만 떴습니다. 선생님은 이유도, 다시 해야 하는지도 모르고 창을 닫으면 **쓴 글이
  사라집니다.** 이제 이유를 그 자리에 적고 "창을 닫지 마시라"고 말합니다.
  · 특히 `42P10`(유일 인덱스 없음)은 **모든 선생님의 모든 저장이 실패**하는 경우라
    사람이 바로 알아볼 문구로 따로 씁니다.
- **날짜가 세계표준시(UTC)였습니다.** `toISOString()`은 UTC라 한국 오전 9시 이전에는
  **어제 날짜**가 나옵니다. 두 가지가 한꺼번에 깨집니다 — 아침에 쓴 기록이 어제 것으로
  남고, 8시 50분과 9시 10분에 저장하면 `report_date`가 달라져 **같은 주 같은 과목이
  두 줄로 갈라집니다**(열쇠가 달라 upsert가 안 묶임). 선생님들이 등교 직후 쓰시는 일이
  많아 반드시 걸릴 자리였습니다.
- **자동 저장이 타이핑을 덮어썼습니다.** 저장이 끝나면 부모가 갱신되고, 그 여파로 폼이
  **서버 값으로 되돌려집니다.** 저장이 오가는 1~2초 사이에 친 글자가 사라지고, 사람은
  자기가 오타를 낸 줄 압니다. 이제 쓰는 중이면 덮어쓰지 않습니다.
- **통계 화면이 조용히 잘리고 있었습니다.** 한 주치라도 137명 × 과목이면 1,000줄을
  넘는데, Supabase는 한 번에 돌려주는 줄 수에 상한이 있어 `select("*")`는 **말없이 잘린
  채로** 옵니다. "이번주 발행됨"이 실제보다 작게 나오고 틀렸다는 표시도 없습니다.
  필요한 칸만, 1,000줄씩 끝까지 나눠 읽습니다.
- **실행할 것: `20260828000000_wr_reports_scale.sql`** — 저장이 기대는 유일 인덱스와
  조회 인덱스 4종. 중복이 이미 있으면 인덱스를 걸지 않고 멈춰 알려줍니다.
- 더미 4,400줄(137명 × 8과목 × 4주)로 넣어보고 재보고 지우는 **부하 테스트 SQL**을
  함께 드립니다.

## v0.302.0 - 2026-08-27

**하원 대시보드 오른쪽 추적 화면이 부드럽게 흐릅니다.**

문제: 차량 이동이 띄엄띄엄 끊겨 자주 멈춘 것처럼 보였습니다. 운행 동선처럼 이어져 보여야 합니다.

- 원인은 GPS가 아니라 **그리는 방식**이었습니다. 화면이 10초마다 차를 마지막 위치로
  **순간이동**시켰습니다. 서 있다가 툭 뛰고 다시 서 있는 것처럼 보이지만, 실제로는 그
  10초 동안에도 계속 달리고 있었습니다.
- 내비게이션과 같은 방법을 씁니다 — **20초 뒤처져서 재생**합니다. 지금 이 순간이 아니라
  20초 전을 그리면, 그 뒤에 **이미 도착해 있는 점들** 사이를 시간에 비례해 채우며 지날
  수 있습니다. 매 프레임(초당 60번) 위치를 다시 계산해 밴과 지도 중심을 함께 옮깁니다.
- **위치를 지어내지 않습니다.** 이미 지나간 두 점 사이를 시간에 맞춰 채우는 것뿐이라,
  20초 늦지만 실제로 지난 길 위를 벗어나지 않습니다. 신호가 3분 넘게 끊긴 구간은 잇지
  않고 앞 점에 머뭅니다 — 터널에서 없는 길을 그리지 않기 위해서입니다.
- 오른쪽 아래에 **`▶ 부드럽게 재생 중 (20초 지연)`** 을 적었습니다. 이 화면으로 도착
  시각을 재는 분이 있을 수 있어, 몇 초 차이가 나는지 알고 봐야 합니다.
- 왼쪽 전체 노선 지도는 그대로입니다(축소된 화면이라 점 하나가 몇 픽셀이고, 거기서는
  끊김이 눈에 띄지 않습니다).

## v0.301.0 - 2026-08-27

**① 셔틀 → 링크·기기 → `GPS 현황` 화면 신설**

문제: GPS 연결 차량이 링크·기기 화면에 섞여 있어, 연결 상태만 훑어볼 방법이 없었습니다.

- 링크·기기는 **발급하는 곳**, GPS 현황은 **지켜보는 곳**으로 나눴습니다. 운행 중에 필요한
  건 카드가 아니라 한 줄씩 늘어선 표입니다.
- 호차마다: 마지막 신호 · 오늘 위치 건수 · **가장 긴 끊김** · 오늘 정류장 도착 수 ·
  정류장 좌표 학습 진행도 · 서버의 마지막 판정. 10초마다 자동 갱신.
- 추적 시간대가 아니면 화면이 먼저 그렇다고 말합니다. 안 그러면 "왜 다 죽었지?" 하고
  멀쩡한 기기를 뒤지게 됩니다.

**② "수신 성공률 47%"는 지표가 틀린 것이었습니다**

- 그 분모는 **웹 체크인 페이지가 5초마다 보내던 시절**의 값(10분=120건)이었습니다.
  지금은 Traccar가 30초 간격 + 30m 거리 필터로 보냅니다 — **일정한 주기가 없습니다.**
  주기가 없는 자료에 성공률은 쓸 수 없는데, 화면이 없는 문제를 만들어내고 있었습니다.
- 평균 간격 12.4초는 오히려 **설정한 30초보다 빠른** 값입니다. 달리는 중에는 30m를 몇 초
  만에 지나가니 그때마다 보냅니다. 30m 설정은 정상이고 의도한 동작입니다.
- 성공률을 빼고 **`최근 10분 수신 N건`** + **`가장 긴 끊김`** 으로 바꿨습니다. 끊김이
  길면 그 사이 정류장을 통째로 놓치므로, 그게 진짜 봐야 할 숫자입니다.

**③ 정류장 반경을 더 넓혔습니다**

문제: 정류장으로 설정한 곳이 실제 정차 위치가 아니라 그냥 아파트 주소여서, 실제와 차이가
클 수 있었습니다.

- 맞습니다. 지금 좌표는 **아파트 주소를 지오코딩한 값**이라 단지 한가운데를 가리키고,
  차는 단지 안으로 안 들어가고 정문·후문·상가 앞에 섭니다. 큰 단지면 400m를 넘습니다.

  | 좌표 상태 | 이전 | 지금 |
  |---|---|---|
  | 주소만 | 250m | **500m** |
  | 학습 중 | 150m | **200m** |
  | 학습 완료 | 80m | 80m |

- 500m에서는 속도 조건이 유일한 방어선이라 **15km/h → 10km/h**로 조였습니다.
  정류장에 서는 차는 거의 멈춰 있습니다.

## v0.300.0 - 2026-08-27

- 넓은 반경에서는 **지나가는 것을 도착으로 잡지 않습니다.**
  · 첫 도착이 `228m / 주소·250m`로 찍혔습니다. 넓히길 잘했지만, 250m는 차로 **20초 거리**라
    정류장 옆 도로를 달려 지나가기만 해도 도착이 됩니다. 그 기록으로 만드는 시간표와
    통계가 조용히 틀어집니다.
  · 정류장에 서는 차는 반드시 속도를 줄이므로, 반경이 80m보다 넓을 때는 **15km/h 이하**일
    때만 인정합니다. 아니면 `228m 지나가는 중(34km/h)`으로 남습니다.
  · 학습이 끝난 80m 정류장에는 이 조건을 걸지 않습니다 — 그 거리면 사실상 그 자리이고,
    신호에 걸려 선 것과 구분할 필요도 없습니다.

## v0.299.0 - 2026-08-27

**정류장 반경을 좌표 신뢰도에 따라 다르게 두고, 학습이 쌓이면 저절로 좁아지게 했습니다.**

방침: 반경을 처음부터 빡빡하게 잡지 않습니다. 조금이라도 길게 정차한 곳의 자료를 모아
날짜별로 대조하면서 점차 좁혀갑니다.

- 맞는 순서입니다. 좁게 잡으면 **아무것도 안 들어와서 줄여나갈 재료조차 안 생깁니다.**

  | 좌표 상태 | 반경 |
  |---|---|
  | 주소 지오코딩만 있음 | **250m** — 넓게 열어 재료부터 모읍니다 |
  | GPS로 배우는 중(3일 미만) | **150m** |
  | 여러 날 같은 자리에서 확인됨 | **80m** — 원래 목표치 |

- **정류장마다 반경이 다르므로**, "가장 가까운 곳"이 아니라 **"자기 반경 안에 들어온 곳 중
  가장 가까운 곳"** 을 찾습니다. 학습된 정류장 옆에 아직 주소뿐인 정류장이 있을 때
  가까운 쪽만 보고 반경을 적용하면 엉뚱한 판정이 납니다.
- 도착 기록에 **`matched_by`**(예: `주소/250m`, `학습됨/80m`)를 남깁니다. 줄이는 것은
  눈으로 보면서 해야 하고, 안 줄어들면 그것도 그 자리에서 드러나야 합니다.
  · **실행할 것: `20260827235500_stop_arrival_matched_by.sql`**
- 기기 진단 문구도 `stored · 정류장 도착(주소 190m)`처럼 근거를 함께 적습니다.

## v0.298.0 - 2026-08-27

**위치는 들어오는데 정류장만 안 찍힐 때, 이유가 화면에 남습니다.**

- 정류장 도착 저장은 오류를 **통째로 삼키고** 있었습니다(`await...insert(...)` — 결과를
  아예 안 봤습니다). 그래서 "GPS는 보이는데 정류장 체크가 안 된다"가 되어도 단서가 없었습니다.
  · 오늘로 세 번째 같은 실수입니다 — 출결 저장, 자동 스캔, 그리고 여기.
- 기기 진단(`last_hit_reason`)에 **판정을 함께** 적습니다.

  | 남는 말 | 뜻 |
  |---|---|
  | `stored · 정류장 도착 기록` | 정상 |
  | `stored · 가장 가까운 정류장 320m` | 아직 안 갔거나 정류장 좌표가 실제 정차 지점과 다름 |
  | `stored · 정류장 좌표 없음` | 그 노선 정류장에 좌표가 하나도 없음 |
  | `stored · 정류장 없음` | **기기가 정류장 없는 노선에 붙어 있음** |
  | `stored · 정류장 저장 실패: …` | 표·권한 문제 |

  마지막 두 줄이 특히 중요합니다. 27호가 여러 개인 상황에서 기기가 **엉뚱한 27호**에
  붙어 있으면 다른 노선의 정류장과 거리를 재게 되어 영원히 안 찍힙니다.
- 중복(`23505`)은 그대로 정상으로 봅니다 — 같은 정류장을 다시 지나간 것뿐입니다.

## v0.297.0 - 2026-08-27

**같은 호차가 두 개로 보이던 이유 — 화면이 학기를 안 가렸습니다.**

- 탑승 배정과 노선 관리가 **모든 학기, 꺼둔 노선까지 전부** 불러오고 있었습니다.
  여름캠프2 노선이 남아 있으면 같은 호차가 두 번씩 보입니다.
  · 하원 체크표는 `term`으로 거르는데 이 두 화면만 안 걸렀습니다 — **같은 자료를 세 화면이
    서로 다르게 보고 있었던 셈**입니다.
  · 이제 `정규학기` + `사용중`만 봅니다.
- 노선 관리에 **방향 어긋남 경고**를 넣었습니다.
  · 등원은 오전, 하원은 오후입니다. `하원`인데 출발이 `08:00`이면 방향이 잘못 박힌 것이고,
    그러면 하원 목록에 등원 차가 끼어듭니다.
  · 노란 띠로 짚어주고 **[등원으로 바꾸기]** 한 번으로 옮깁니다. 배정된 학생과 정류장은
    그대로 따라갑니다.

## v0.296.0 - 2026-08-27

- 노선 관리 정류장 표의 `인원` 칸을 **`타는 학생`** 으로 바꿨습니다.
  · `3명`만으로는 정류장을 지울 때 **무엇을 잃는지**도, 명단이 맞는지도 알 수 없어서
    매번 탑승 배정이나 하원 체크표로 넘어가 대조해야 했습니다.
  · 인원 수는 앞에 회색 알약으로 남겨 한눈에 세는 용도는 그대로 둡니다.
- **요일이 빠지는 학생은 노란 배경 + `(월수금)`** 으로 구분합니다. 월~금 전부 타는 학생에게는
  아무것도 붙이지 않습니다 — 대부분이 그렇고, 눈에 띄어야 할 것만 눈에 띄어야 합니다.

## v0.295.0 - 2026-08-27

- 운영 대시보드에 **`🚌 하원 화면 미리 보기`** 버튼.
  · 시각에만 맡기면 미리 확인하거나 늦게 다시 볼 방법이 없었습니다.
- 하원 시간이 아닌데 켠 경우, 화면 위에 **`⚠️ 지금은 하원 시간이 아닙니다 · 하원 16:00~17:30`**
  이 뜹니다. 공용 모니터라 낮에 켜져 있으면 지나가는 분들이 "지금 차가 나가나?" 하고
  오해합니다. 이 시간대는 GPS가 아예 저장되지 않으므로(15:30~18:30만) **지도가 비어 있는
  것도 정상**입니다.
- 수동으로 켠 것을 끌 때는 '오늘 하원 종료' 기록을 남기지 않습니다. 남기면 정작 4시에
  자동으로 떠야 할 화면이 안 뜹니다.

## v0.294.0 - 2026-08-27

**기계가 붙인 표시 옆에 그 근거를 뒀습니다.**

- 자동으로 픽업·결석이 붙은 학생 뱃지 **왼쪽 아래에 파란 `?`** 가 생깁니다.
  문제: 픽업·결석이 토들에서 온 것인지 구글챗에서 온 것인지, 왜 그렇게 분류됐는지
  확인할 방법이 없었습니다.
- 누르면 **원문 채팅**과 함께 어디서 왔는지가 뜹니다 — 출처(토들·구글챗·전화·교사),
  채널 이름, 보낸 분, 받은 시각, 연결된 명부 이름, AI가 남긴 판단 근거 한 줄.
  명부와 대조되지 않고 이름만 비교한 경우에는 그 사실을 노란 글씨로 말합니다.
- **토들에서 보기** 링크와 **픽업 인박스** 링크를 함께 뒀습니다. 창 안에서 바로
  `✕ 표시 지우기`도 됩니다 — 확인과 조치가 갈라져 있으면 확인만 하고 넘어가게 됩니다.
- 표시가 **있다는 것 자체가 "이건 기계가 붙였다"는 뜻**입니다. 사람이 직접 누른 것에는
  근거가 없으니 `?`도 없습니다. 오른쪽 위 특이사항(`!`)과 헷갈리지 않도록 반대쪽 모서리에
  뒀습니다.

## v0.293.0 - 2026-08-27

**가장 지워야 할 것을 가장 지울 수 없었습니다.**

- `null value in column "student_id" ... violates not-null constraint`
  · 출결 등록표는 학생 연결을 NOT NULL로 잠가뒀습니다. 그 판단 자체는 맞습니다 —
    학생과 이어지지 않은 **출결 기록**은 쓸모가 없습니다.
  · 그런데 '무시'는 출결 기록이 아니라 **"이건 출결이 아니다"라는 사람의 판단**입니다.
    그리고 내려야 하는 것들은 대부분 **애초에 학생을 못 찾은 것들**입니다 — 멘션에서
    잘못 잡힌 이준서·김요한 같은. 명부에 없는 출결이니 채울 학생이 없고, 그래서 막혔습니다.
  · NOT NULL을 조건부로 바꿨습니다. 출결로 쓰이는 줄(등록·확인필요)은 여전히 학생이
    반드시 있어야 하고, '무시'만 예외입니다. 외래키는 그대로 — 값이 있다면 실제 학생입니다.
  · **실행할 것: `20260827234500_attendance_dismiss_allows_no_student.sql`**
- 내릴 때 이름으로 학생을 찾을 수 있으면 채워둡니다(나중에 되짚을 수 있도록).
  동명이인이면 비워둡니다 — 엉뚱한 아이에 붙는 것이 더 나쁩니다.

## v0.292.0 - 2026-08-27

**잘못 붙은 픽업·결석을 되돌리는 길을 냈습니다.**

- 하원체크표에서 **이름을 더블클릭하면** 픽업·결석·탑승 표시가 지워지고 원래대로 돌아옵니다.
  문제: 멘션까지 읽어 김요한·이준서·임예나 세 명을 골라내면서 김요한과 이준서가 결석으로
  잘못 표시됐습니다. 작은 🚗/🚫 단추를 정확히 다시 누르는 것보다 이름을 두 번 치는 쪽이 빠릅니다.
- **오늘 픽업·결석 위젯에 ✕를 달았습니다.** 여기서 지우면 **두 곳이 함께** 돌아옵니다.
  · 출결 등록표에 '무시'로 남김 → 위젯·업무보드·운영 대시보드에서 사라짐
  · 그 아이의 오늘 셔틀 표시를 '예정'으로 → 체크표·안내보드·도착체크 원복
  목록에서만 지우면 표에는 결석 사선이 그대로 남아 **기사님이 아이를 안 태웁니다.**
- **되돌린 것이 다시 살아나던 자리를 막았습니다.** 체크표는 "값이 '예정'이면 아무도 안 누른
  것"으로 보고 토들 픽업·결석을 자동으로 덧씌우고 있었습니다. 그런데 되돌리기도 '예정'을
  남기므로, 사람이 지운 것을 다음에 화면을 열 때 기계가 도로 붙였습니다. 이제 **값이 아니라
  줄이 있는지**를 봅니다 - 줄이 하나라도 있으면 사람 판단입니다.

## v0.291.0 - 2026-08-27

**✕가 안 먹히고 전부 ⬜였던 진짜 이유 — 저장이 매번 실패하고 있었습니다.**

- 출결 등록표의 유일 인덱스에 **조건이 붙어 있었습니다**(`where source_message_id is not null`).
  Postgres는 조건이 붙은 유일 인덱스를 쓸 때 명령문에도 같은 조건을 적어야 그 인덱스를
  찾아냅니다. 앱의 upsert는 열쇠 칸 이름만 보내니 매번 `42P10`으로 튕겼습니다.
  · **자동 스캔이 한 줄도 못 만들었습니다** → 모든 항목이 ⬜
  · **'무시'로 남길 줄도 못 만들었습니다** → ✕를 눌러도 안 사라짐
  · 조건을 떼도 안전합니다. NULL끼리는 서로 다른 값이라, 수기 등록은 그대로 여러 줄 허용됩니다.
  · **실행할 것: `20260827230000_attendance_entries_uniq_fix.sql`**
- **오류를 삼키던 두 곳을 막았습니다.** 저장 코드는 `if (!error)`만 보고 실패를 버렸고,
  화면은 응답을 확인하지 않았습니다. 눌렀는데 아무 반응이 없으면 사람은 자기가 잘못
  눌렀다고 생각합니다. 이제 실패하면 빨간 줄로 이유가 뜹니다.
- **데이터베이스 점검이 이걸 못 잡았습니다.** 칸만 보고 있었으니까요. 이제 점검이
  **실제로 한 줄을 넣어보고 곧바로 지웁니다.** 되는지 안 되는지는 해봐야 압니다.
- 원문을 **눌러서 전체를 봅니다.** 한 줄로 잘린 미리보기로는 "정말 픽업 얘기인지" 판단할 수
  없어 매번 다른 화면으로 넘어가야 했습니다. 창 안에서 바로 `✕ 출결 아님`도 됩니다.
- ✕를 누르면 **서버 응답 즉시 화면에서 내립니다**(다시 불러오기를 기다리지 않음).
- 아래 '예정' 칸에도 ✕를 달았습니다. 지금까지 예정 건은 내릴 방법이 없었습니다.

## v0.290.0 - 2026-08-27

- 출결 이름 대조 전에 **멘션(@…)을 먼저 지웁니다.**
  · `stripLeadingMention`은 **이미 있었지만 화면에 글을 보여줄 때만** 쓰였고, 대조하는
    쪽에는 원문이 그대로 들어갔습니다. **두 곳이 서로 다른 글을 보고 있었던 셈**입니다.
  · 그래서 `@Paul Lee @John Kang 오늘 임예나 3시 픽업입니다`가 임예나·이준서(Paul Lee)·
    김요한(John Kim) **세 명**으로 잡혔습니다. 선생님 멘션 이름이 학생 영문명과 겹칩니다.
  · 유령 학생은 실제 등록 줄과 이어지지 않으니 그 항목은 계속 ⬜로 남았습니다. 출결내역이
    온통 ⬜였던 원인입니다.
- 지우지 않고 **같은 길이의 공백으로 바꿉니다.** 뒷단계가 "이름 바로 뒤 괄호"에서 생일·반을
  읽는데, 글자를 없애면 위치가 밀려 그 힌트를 놓칩니다.

## v0.289.0 - 2026-08-27

- 출결내역에 `✕ 출결 아님` 버튼.
  · '무시' 상태는 **이미 있었는데 그리로 갈 버튼이 없었습니다.** ❓에서 할 수 있는 일이
    등록뿐이라, 아닌 것도 계속 물음표로 남아 세어졌습니다.
  · 등록 대상으로 잡히지 않은 항목(⬜)도 내릴 수 있게 했습니다. 고칠 줄이 없으면 줄을 새로
    만들면서 곧바로 '무시'로 둡니다 - 등록은 못 해도 내리는 것만은 되어야 합니다.
- 내린 것이 **하원체크표 위젯에서도 사라집니다.** 그 위젯은 등록 상태를 아예 안 보고 메시지를
  매번 직접 세고 있었습니다. 운영 대시보드는 등록된 것만 읽도록 이미 고쳤는데 여기만 옛 방식으로
  남아, 업무 인박스에서 내려도 계속 세어졌습니다 - 같은 판단을 두 곳이 다르게 하고 있었습니다.
- 출결내역 목록에서도 내린 항목을 뺍니다. API가 무시된 키를 함께 돌려주도록 했습니다.

## v0.288.0 - 2026-08-27

- 데이터베이스 점검 목록에 최근 만든 것 여섯 가지를 더했습니다 - 지속 특이사항, 기간 픽업·결석,
  출결 등록 상태, 출결 이름 가르치기, 셔틀 배정 미연결 사유, 구글챗 실시간 푸시.
  · 점검 목록에 없으면 화면이 못 잡습니다. 실제로 `shuttle_persistent_notes` 표가 통째로
    없는 채로 며칠이 지났는데 아무도 몰랐습니다 - 위젯에 적으면 조용히 사라졌을 겁니다.
  · 새 마이그레이션을 만들 때는 `src/lib/schemaChecks.ts`에도 한 줄 적어야 합니다.

## v0.287.0 - 2026-08-27

- 픽업·결석을 **하루짜리와 기간**으로 나눠 다룹니다(담당자 원칙).

  ```
  "오늘 3시에 데리러 갈게요"        → 그날 하루. 예약 한 줄. (지금까지와 같음)
  "금요일까지 픽업하겠습니다"        → 지속 특이사항으로. 그 기간 매일 반영.
  "다음주 화요일까지 결석합니다"     → 지속 특이사항으로. 그 기간 매일 결석.
  ```

  기간짜리를 날짜마다 예약으로 흩뿌리면 **중간에 한 줄이 빠져도 아무도 모릅니다.** 그리고
  체크표를 보는 사람은 "이 아이가 언제까지 이런 상태인지"를 알 수 없습니다 - 오늘 한 줄만
  보이니까요. 특이사항 한 줄로 두고 매일 아침 그날 해당하는지만 봅니다.
- `shuttle_persistent_notes`에 `effect_from`·`effect_to`(기간)와 `effect_kind` 두 값
  (`pickup`·`absent`), `request_id`(원문 추적)를 더했습니다.
  → **마이그레이션 `20260827200000_persistent_note_period.sql` 실행이 필요합니다.**
- 기간 판정은 출결 인박스가 쓰는 `extractTargetRange`를 그대로 씁니다. 같은 문장을 두 곳이
  다르게 읽으면 언젠가 답이 갈립니다. "까지"가 없으면 기간으로 보지 않습니다 - 원칙은 하루.
- 기간으로 잡힌 건은 예약을 따로 만들지 않습니다. 두 곳이 같은 날을 각각 반영하면 화면에
  두 번 뜨고, 하나를 취소해도 다른 하나가 남습니다.

## v0.286.0 - 2026-08-27

- 토들 링크가 늘 비어 있던 진짜 원인. **수집기는 방 번호와 주소를 처음부터 보내고 있었는데,
  받는 쪽(`/api/pickup/ingest`)에서 옮겨 담지 않아 버려지고 있었습니다.** 저장 함수도, DB 칸도,
  화면 버튼도 다 준비되어 있었는데 이 한 곳이 비어서 `source_url`·`source_chat_id`가 항상
  null이었습니다.
  · 새 칸을 만들 때는 "보내는 쪽 → 받는 쪽 → 저장 → 화면" 네 곳을 다 고쳐야 하는데, 받는 쪽만
    빠뜨려도 **오류 없이 조용히 아무 일도 일어나지 않습니다.** 그래서 화면만 세 번 고쳤습니다.

## v0.285.0 - 2026-08-27

- 예정된 픽업에도 `🔁 규칙으로 다시 맞추기`. 지난번에 만든 재매칭 단추는 픽업
  인박스(`pickup_requests`) 쪽에만 있었고, 예정된 픽업은 다른 표(`pickup_schedules`)라
  그 단추가 닿지 않았습니다. 화면에 나란히 있으니 하나로 보이지만 안에서는 별개입니다.
- 재매칭에 **생년월일을 안 싣고 있었습니다.** 김재이 셋이 영문명을 모두 'Jay Kim'으로 쓰기
  때문에, 이 칸이 없으면 `jay kim(190828)`의 괄호를 읽어도 아무도 못 고릅니다. 규칙은
  맞았는데 재료가 빠져 있었습니다. 두 화면 모두 `birth_date`를 함께 읽습니다.

## v0.284.0 - 2026-08-27

- 학부모 문의 상세 창 맨 위에 `토들에서 열기 ↗`. 문의를 읽은 다음 하는 일이 거의 항상
  "토들에서 답하기"라, 아래로 스크롤해서 찾게 하면 안 됩니다.
- 그 버튼이 아예 안 뜨던 경우도 고쳤습니다. 토들 주소는 "학교 주소 + 방 id"로 되살리는데,
  학교 주소를 **지금 화면에 뜬 줄에서만** 찾고 있었습니다. 문의만 걸러 보면 그 줄들에
  주소가 하나도 없을 수 있고, 그러면 버튼이 통째로 사라집니다. 화면 밖까지 포함해 주소가
  있는 가장 최근 기록 하나를 따로 읽어 받쳐둡니다.

## v0.283.0 - 2026-08-27

- 픽업 인박스 학생 연결을 **검색**으로 바꿨습니다. 137명이 든 목록을 눈으로 훑는 대신
  두 글자만 쳐도 좁혀지고, 한글·영문·학번 어느 쪽으로 쳐도 찾습니다. AI가 읽은 이름을 미리
  넣어두므로 대개 열자마자 좁혀져 있습니다. 예정된 픽업에도 같은 것을 씁니다.
- 이름 매칭이 **성이 잘려 와도** 이어집니다. `dongha k` → 김동하, `Seohu K` → 강서후.
  지금까지는 완전히 같은 글자만 인정해서, 채널 이름이 성을 이니셜로 쓰면 한 글자도 안 맞는
  것으로 취급됐습니다. 앞에서부터 이어지면 인정하되 **한 명으로 좁혀질 때만** 입니다
  (`kim`만 적혀 있으면 수십 명이라 사람에게 넘깁니다).
- `🔁 규칙으로 다시 맞추기` 단추. 규칙을 고쳐도 **이미 저장된 줄에는 소용이 없었습니다** -
  학생 연결은 들어오는 순간 한 번만 하기 때문입니다. jay kim(190828)이 계속 안 이어지던
  이유가 이것입니다. 지금 규칙으로 미확정 건을 다시 훑되, 한 명으로 확정된 것만 저장합니다.
- 예정된 픽업에서 💬 로 **원문**을 봅니다. 맞다/아니다를 정하려면 원문을 봐야 하는데 그걸
  보려고 업무보드를 갔다 오게 하면 아무도 확인을 안 하게 됩니다. 토들 링크도 함께 답니다.
- 확인이 필요한 픽업에서 **결석으로도 처리**할 수 있습니다. 선택지가 픽업이냐 아니냐 둘뿐이라
  결석 연락을 받아도 할 수 있는 게 없었습니다. 출결 인박스가 쓰는 같은 도구로 결석 여부와
  기간("오늘부터 금요일까지")을 읽어 그 사이 평일을 하루씩 처리합니다.

## v0.282.0 - 2026-08-27

- 연동 상태 화면이 홈으로 튕기던 문제. 권한을 `position === "관리자"`로 직접 비교했는데,
  앱 전체가 쓰는 `isAdminUser`는 개발자 계정과 역할 미리보기도 통과시킵니다. 같은 판단을
  두 군데서 다르게 하면 이런 일이 생깁니다. `isAdminUser`로 맞췄습니다.
- 업무보드 상단 활동 로그를 최대 22rem으로 줄였습니다. 로그는 훑는 자리지 읽는 자리가
  아닌데 남는 폭을 전부 먹고 있었습니다.
- 그 옆에 **크론 연동 요약 배지**(🟢 N / 🔴 N)를 붙였습니다. 자세한 판은 관리 → 연동 상태에
  있지만, 들어가야만 보이는 정보는 결국 아무도 안 봅니다. 빨간 숫자는 0이면 안 그립니다 -
  0이 늘 떠 있으면 눈이 그 자리를 무시하게 되고 정작 1이 됐을 때도 안 보입니다.
- 학부모 문의의 `토들 ↗` 버튼이 안 뜨던 줄들. `source_url` 칸은 8월 24일에 생겨서 그 전
  기록에는 비어 있습니다. 방 id(`source_chat_id`)는 처음부터 저장하고 있었고 토들 주소는
  "학교 주소 + /messaging/ + 방 id" 형태라, 주소가 있는 줄 하나에서 학교 주소를 뽑아
  나머지에 적용합니다.

## v0.281.0 - 2026-08-27

메뉴 점검에서 나온 다섯 가지를 순서대로 반영했습니다.

- **연동 상태 화면**(관리 → 연동 상태). 크론 12개·토들 수집기·구글챗·GPS가 실제로 돌고 있는지
  한 화면에서 봅니다. 27호 GPS가 3일간 조용히 죽어 있었는데 아무도 몰랐던 일이 계기입니다.
  이런 실패는 화면이 깨지지 않고 그냥 새 데이터가 안 들어올 뿐이라, 조용한 날과 고장난 날이
  똑같이 보입니다. 각 크론이 실행될 때 `integration_heartbeats`에 신호를 남기고, 정해진 주기의
  3배가 지나면 🔴로 표시합니다. 신호만으로는 "돌긴 도는데 데이터가 없는" 상태를 못 잡으므로
  실제 수신 건수(GPS·출발도착·구글챗·문의)와 30일 저장량도 함께 보여줍니다.
  · GPS는 오는데 출발·도착 판단이 0이면 경고를 띄웁니다 - 27호 때가 정확히 그 상태였습니다.
- **학생 프로필에 셔틀**. 몇 호차를 무슨 요일에 타는지, 정류장과 기사님까지. 지금까지는
  학부모 전화를 받으면 학생 조회 → 셔틀 → 노선·배정으로 세 번 옮겨 다녀야 했습니다.
- 학부모 문의 상세에서 학생 이름을 눌러 통합 프로필로 갑니다. 이름은 이미 명부와 연결돼
  있었는데 누를 수가 없었습니다.
- 하원체크표 픽업·결석 위젯에서 픽업 인박스로 바로 가는 링크.
- **정류장 미지정** 배지(노선·배정). 하원 명단을 PDF 기준으로 다시 넣을 때 생긴 상태를
  한 자리에 모아 채울 수 있습니다. 다 채우면 배지가 사라집니다.
- 끊겨 있던 화면 두 개를 이었습니다 - 기사님 안내문·동의서 인쇄(링크·기기 화면),
  과목별 관찰기록(주간 학생 관찰기록 메뉴). 둘 다 주소를 직접 쳐야만 열렸습니다.
- 학생 조회에서 결과가 한 명이면 Enter로 바로 프로필로. 교직원 목록에 담임 반 표시.

## v0.280.0 - 2026-08-27

- 학부모 문의 목록의 각 줄에 `토들 ↗` 버튼을 붙였습니다. 예전에는 상세 창을 한 번 더 열어야
  보였는데, 답을 하려면 어차피 토들로 가야 하니 한 번 더 누르게 할 이유가 없습니다.
- 가르치기 창이 옆 위젯에 가려지던 문제. `fixed`는 조상에 transform·filter·sticky가 있으면
  화면이 아니라 **그 조상** 기준이 되고 z-index도 그 안에서만 셉니다. 하원체크표 사이드바가
  그런 구조라 z-[80]을 줘도 못 이겼습니다. createPortal로 body에 직접 그리도록 바꿨습니다
  (같은 폴더의 규칙 목록 창은 이미 portal을 써서 멀쩡했습니다).
- 하원체크표에서 이름을 눌러 켠 찾기(하이라이트)를 풀 수 있게 했습니다. 화면 빈 곳 클릭·Esc·
  `✕ 해제` 세 가지. 버튼·입력칸·표의 줄을 누른 것은 "일하려고 누른 것"이라 유지합니다.

## v0.279.0 - 2026-08-27

- 출결 매칭이 **한글·영문을 함께** 봅니다. 영문 이름은 'absent' 같은 영어 문구 앞만 뒤졌기
  때문에, "Jay Kim 오늘 결석합니다"처럼 영문 이름 + 한국어 문장이면 후보를 하나도 못 뽑았습니다.
  영어 이름을 쓰는 학부모가 문장은 한국어로 적는 경우가 흔한데 그게 통째로 빠져 있었습니다.
- 같은 맥락에서 두 가지를 더 고쳤습니다.
  · 이름 뒤 괄호의 **반 이름**("김재이(G3JA)")을 읽습니다. 선생님들이 가장 많이 쓰는 표기인데
    생일 판정기는 숫자만, 학년 판정기는 숫자로 시작할 때만 봐서 어느 쪽에도 안 걸렸습니다.
  · 영문 후보를 **긴 것부터** 봅니다. 'Jay'를 먼저 처리해 아무나 골라버리고, 정작 생일이 붙은
    'Jay Kim'은 건너뛰고 있었습니다. 성까지 적어줘도 소용이 없었습니다.
- 학부모 문의사항: 날짜 묶기는 UTC, 라벨은 한국 시각을 보고 있었습니다. 아침 8시(KST) 문의는
  UTC로 전날이라 전날로 묶이면서 "오늘"이라 적혔습니다 - '오늘' 칸에 수요일 것이 들어앉던
  원인입니다. 둘 다 한국 시각으로 맞췄습니다. 정렬도 "답 안 한 것 먼저"를 빼고 **날짜순**으로만
  세웁니다(그 정렬이 날짜 구분선을 오르내리게 했습니다).
- 학생조회: 초등부·중고등부 탭 + 학년 탭(전체/2학년/3학년…). 학년 오름차순 → 이름 오름차순.
  예전 학년별 막대그래프는 보기만 되고 누를 수가 없어서, 인원수는 그대로 두고 누르면 걸러지게
  바꿨습니다.
- 교직원(승인됨): 전체·교사·행정직원·관리자·미지정 탭.
- 기록분석 정류장 도착 시간을 두 칸으로 배치했습니다.

## v0.278.0 - 2026-08-27

- 동명이인을 화면에 **반 이름**으로 구별합니다("김재이(G3JA)"). 학년만으로는 같은 학년
  동명이인이 안 갈라지고, 생일("190828")은 사람이 보고 누군지 떠올리기 어렵습니다.
  반은 담임 선생님도 기사님도 아는 말입니다. 명부에 반이 없을 때만 예전처럼 생일·학년으로.
- 픽업 인박스가 **영문 이름 뒤 생일**을 읽습니다. `jay kim(190828)` 형태가 무시되고 있었습니다.
  한글 이름 경로에는 이 처리가 있었는데 영문 경로에만 빠져 있었습니다 - 영문 후보를 위치 없이
  문자열로만 뽑아 와서 "이름 바로 뒤"를 볼 수가 없었습니다. 김재이 세 명이 영문명을 모두
  'Jay Kim'으로 쓰고 그중 둘이 같은 2학년이라, 생일 없이는 영원히 확인 대기였습니다.
- 하원체크표의 오늘 픽업·결석에서 이름 옆 🔎로 바로 가르칠 수 있고, '가르친 목록'으로
  고치거나 지울 수 있습니다. 이름이 틀리게 읽힌 걸 발견하는 자리가 업무 인박스가 아니라
  여기라서, 발견한 자리에서 고칠 수 있어야 합니다. 이 위젯이 학습 규칙을 아예 안 읽고 있던
  것도 함께 고쳤습니다(같은 문장이 두 화면에서 다르게 읽히고 있었습니다).
- 앞으로 예정된 픽업에서 '학생 연결' 버튼으로 어느 학생인지 직접 고를 수 있습니다. 명부에
  붙지 않은 줄은 '미연결'로 표시합니다 - 이 상태로 당일이 되면 체크표에 아무것도 걸리지 않고
  조용히 지나갑니다.
- 셔틀관리·노선관리 노선 목록을 **호수 오름차순**으로 정렬하고 두 칸으로 배치했습니다.
  sort_order(엑셀의 지역별 순서)로 세워서 "4-2호 다음 12호"처럼 튀었습니다.

## v0.277.0 - 2026-08-27

- 하원운행 오른쪽 트래킹 지도: **차량을 화면 정중앙에 고정**했습니다. 이전에는 자취와 주변
  정류장을 모두 담는 `setBounds`를 써서 화면 중심이 그 덩어리의 한가운데가 되었고, 차가
  움직일수록 자취가 뒤로 길어져 차가 점점 더 구석으로 밀렸습니다. 트래킹 화면인데 정확히
  반대로 동작하고 있었습니다. 운행 중에는 level 3 고정 + 현재 위치를 중심으로 잡고, 출발
  전에만 노선 전체를 맞춥니다.
- 하원 명단 재등록 SQL(`outputs/하원_등록.sql`): 스스로 검증하고 어긋나면 통째로 되돌립니다.
  PDF 105줄을 명부와 대조해 한 명으로 확정되지 않으면 `raise exception`으로 중단합니다.
  요일은 새 칸을 만들지 않고 기존 `weekdays`(1=월…5=금)에 넣어 화면 6곳이 바로 쓰도록 했습니다.

## v0.276.0 - 2026-08-27

- **기록분석을 GPS 연결 / 미연결 두 줄로 나눴습니다.** 담당자 요청. 지금은 기기가 1대뿐이라 목록 대부분이 빈 카드인데, 섞여 있으면 실제로 신호가 들어오는 노선을 찾는 데만 한참 걸립니다.
  - 🟢 GPS 연결 — 최근 신호가 들어온 노선만. 펼쳐진 상태.
  - ⚪ GPS 미연결 — 기본 접힘, 숫자만 표시.
- 27호 트래킹 진단 SQL 추가(`outputs/27호_트래킹_진단.sql`, 읽기 전용). 위치 신호의 사슬(기기 → ping → 도착·출발 event → 화면)을 한 칸씩 짚어 어디서 끊겼는지 찾습니다.

## v0.275.0 - 2026-08-27

- **하원 추적 지도를 오른쪽 한 칸으로.** 두 칸으로 나누면 각 칸이 작아져 골목이 안 보입니다. 넓게 보는 일은 왼쪽이 이미 하므로 오른쪽은 한 칸을 크게 쓰고 순환으로 여러 노선을 훑습니다(6:4 분할, 6초 순환, 배율 2~3).
- **하원 재구성 SQL을 "명부에 연결된 학생만"으로 고쳤습니다.**
  - 제 잘못입니다. 엑셀 시트를 통째로 밀어 넣었습니다. 그 시트에는 유치부 아이와 등교 구간까지 섞여 있는데 아무 조건 없이 다 넣어서, 정리해 두신 하원 명단을 덮었습니다.
  - 이제 **정류장은 엑셀대로 다 만들되(기사님이 들르는 이유가 남아야 하니까), 체크표에 오르는 사람은 명부의 우리 학생뿐**입니다.

## v0.274.0 - 2026-08-27

- 하원 추적 지도 배율을 **3~4**로 더 좁혔습니다(5 → 4). level 4를 넘기면 큰길만 남아 "지금 어느 골목인지"가 안 보입니다.
- **GPS 미시작 경고를 접을 수 있게** 했습니다(기본 접힘). 거의 모든 노선이 걸려 있으면 경고가 아니라 배경 소음입니다 — 화면에서 가장 큰 자리를 차지하면서 정작 알려주는 게 없습니다. 접어두고 `⚠ GPS 미시작 46대` 처럼 숫자만 남깁니다. 기기를 다 나눠주면 몇 대만 남을 테니 그때 펴두고 쓰시면 됩니다.

## v0.273.0 - 2026-08-27

- **하원 추적 지도를 도로가 보이는 배율로 고정.**
  - GIA를 화면 맞추기에서 뺐습니다. 하원 차는 학교에서 **점점 멀어지므로** GIA까지 한 화면에 넣으려 하면 시간이 갈수록 계속 축소되고, 도로 이름이 사라져 "지금 어디쯤인지"를 알 수 없게 됩니다. 이 화면의 목적은 학교와의 거리가 아니라 **차가 지금 어느 길에 있는지**입니다.
  - 배율을 3~5로 묶었습니다 — 아래로는 3(멈춰 있으면 한 점이라 과확대되는 것 방지), 위로는 5(그 이상 축소되면 도로 이름이 사라짐).
- **하원 엑셀 재구성 v2** (`20260827150000_haw_from_excel_v2.sql`). 첫 판의 퇴소 95건 원인 두 가지를 고쳤습니다.
  - `하원 31 - 건대` 가 **"호" 없이** 적혀 있어 노선으로 인식되지 못했고, 그 제목 글자가 학생 이름("하원", "건대")으로 섞여 들어갔습니다. 이제 "호"가 없어도 잡습니다 — **노선 47 → 48개.**
  - 반 표기 `유치원`·`초등`을 못 읽어 퇴소로 떨어졌습니다. 판독 규칙에 넣었습니다.
  - 반이 **비어 있는** 줄은 퇴소로 단정하지 않고 `확인필요`로 남깁니다.

## v0.272.0 - 2026-08-27

- **위치를 못 찾은 정류장을 장소 이름으로 검색해서 지정합니다.** 카카오 지도 검색으로 찾아
  넣을 수 있습니다.
  - 주소 변환이 실패하는 정류장은 대개 **주소가 아니라 장소 이름**으로 적혀 있습니다 — "반포 자이 후문", "○○아파트 놀이터 앞". 기사님과 학부모가 실제로 쓰는 말이라, 정식 주소로 바꾸라고 하는 것보다 그 말 그대로 검색해 고르게 하는 편이 맞습니다.
  - 각 줄에 **🔍 검색** / **📍 지도** 두 가지. 검색창에는 원래 적힌 주소가 미리 들어가 있어 대개 [찾기] 한 번이면 끝납니다.
  - 좌표뿐 아니라 **구·동도 함께** 저장합니다 — 지역별 대시보드가 구/동으로 묶기 때문에 좌표만 채우면 지도엔 점이 찍혀도 지역 집계에서는 여전히 빠집니다.
  - 원래 적혀 있던 말은 **지우지 않습니다.** 정식 주소로 덮어쓰면 현장에서 알아보지 못합니다.
  - 카카오 지도 SDK의 Places를 쓰므로 **새로 발급받을 키가 없습니다.**

## v0.271.0 - 2026-08-27

- **노선관리에서 기사님 정보가 전부 같은 사람으로 뜨던 버그 수정.**
  - 원인: 입력칸이 `defaultValue`를 쓰는데, 이 값은 **처음 그려질 때 한 번만** 읽힙니다. 노선을 바꿔도 React가 보기엔 같은 자리의 같은 입력칸이라 새로 만들지 않고, 처음 열었던 노선의 값이 계속 남아 있었습니다.
  - **데이터는 멀쩡했고 화면만 낡은 값을 붙들고 있었습니다.** 그 상태로 저장했다면 다른 노선의 기사님 정보를 덮어쓸 뻔했습니다.
  - `key`에 노선 id를 넣어 노선이 바뀌면 입력칸을 새로 만들게 했습니다. 기사님·연락처·차량번호·동승선생님·출발시각·정원·권역 전부.
- **매칭 SQL을 자기완결형으로.** `unlinked_reason` 칸을 스스로 만들고 제약·외래키·인덱스까지 갖춥니다. 순서를 지켜야 하는 SQL이 여러 개면 결국 하나는 빠집니다(실제로 여기서 막혔습니다).

## v0.270.0 - 2026-08-27

- **배차표가 "중고등부·미상"으로 뜨던 원인 두 겹을 고쳤습니다.**
  - ① `shuttleDivision.ts` 가 **옛 규칙**을 쓰고 있었습니다 — 초등부를 "학교"라는 **글자**로만 알아봤습니다. 실제 표기인 `G5A`·`6TH GRADE` 는 몰라서 전부 "중고등부·미상"으로 떨어졌습니다. 셔틀 매칭에서 쓰는 규칙(24개 문형 통과)으로 통합했습니다. **같은 데이터를 두 곳에서 다르게 읽으면 어느 쪽이 맞는지 아무도 알 수 없습니다.**
  - ② 명부에 **연결돼 있어도 반 문자열만** 보고 있었습니다. 이제 `divisionFor` 가 연결된 아이는 명부의 학년·부서를 우선합니다 — 반 문자열은 사람이 손으로 적은 것이고 명부는 확정된 사실이라, 둘이 다르면 명부가 맞습니다.
  - 중고등부에 자체 색을 줬습니다(예전엔 미상과 같은 회색이라 구분이 안 됐습니다).
- **SQL 한 방 매칭 신설**: `20260827130000_shuttle_match_all.sql`. 브라우저로 API를 두 번 여는 대신 SQL Editor에서 한 번에 끝냅니다. 규칙은 API와 같고(명부 절대기준·별칭·유치부·퇴소·동명이인 보류), **이미 연결된 줄은 건드리지 않습니다** — 사람이 화면에서 손으로 지정한 것을 덮어쓰지 않기 위해서입니다. 명부가 137명이 아니면 예외를 던지고 멈춥니다.

## v0.269.0 - 2026-08-27

- **노선·배정 화면이 명부와 연결된 상태를 그대로 비춥니다.**
  - 원인: 이 화면이 **`student_name_raw`(적힌 그대로의 문자열)만 보고 있었습니다.** 배정에 `student_id`를 채워 넣어도 화면은 여전히 원문을 그리니, 연결이 됐는지 안 됐는지 알 방법이 없었습니다. 매칭을 아무리 돌려도 화면은 그대로였던 겁니다.
  - 이제 연결된 아이는 **명부의 이름·학년**으로 뜨고, 안 된 아이는 흐린 글씨 + 이유 꼬리표(`유치` `퇴소` `확인`)가 붙습니다.
  - **이름을 눌러 그 자리에서 학생을 지정**할 수 있습니다. 다른 화면으로 넘어가야 하면 아무도 안 하니까요. `student_id`와 `unlinked_reason`을 항상 함께 바꿔 CHECK 제약을 지킵니다.
  - 화면 위에 **명부연결 N · 유치부 N · 퇴소 N · 확인필요 N** 요약을 답니다.
  - 검색은 원문과 명부 이름 **둘 다**로 찾습니다 — 선생님이 어느 쪽으로 기억하실지 모르니까요.
  - 정렬도 명부 이름 기준으로 바꿨습니다(예전엔 원문 문자열 기준이라 같은 아이가 화면마다 다른 자리에 있었습니다).
- 배정 화면의 학생 목록에서 데모 계정 학생을 제외합니다.

## v0.268.0 - 2026-08-27

- 셔틀 매칭 반영 전에 `unlinked_reason` 칸이 실제로 있는지 확인합니다. 없으면 update가 줄마다 실패하는데 Supabase는 예외를 던지지 않아서 **반은 붙고 반은 안 붙은 채로 "완료"라고 뜹니다** — 제일 나쁜 결과입니다. 마이그레이션 미실행이면 그 사실을 그대로 알려주고 멈춥니다(409).

## v0.267.0 - 2026-08-27

- **별칭 SQL 오류 수정** — `min(uuid)` 는 없는 함수입니다. 세는 것과 고르는 것을 나눴습니다.
- **가르친 규칙 목록·수정 창 신설.**
  - 맞는 지적입니다. 지금까지는 **넣기만 되고 꺼내 볼 수가 없었습니다.** 한 번 잘못 가르치면 그 뒤로 계속 틀린 채 자동 적용되는데 어디가 잘못됐는지 볼 방법이 없었습니다. 학습하는 기능에는 **되돌리는 자리가 반드시 함께** 있어야 합니다.
  - 출결내역 제목 옆 **[가르친 규칙]** → 목록·연결 학생 변경·삭제. 별칭은 드롭다운으로 바로 다른 학생에 다시 붙일 수 있습니다(잘못 가르친 것의 대부분이 "다른 아이로 연결한" 경우라서).
  - 규칙을 지우면 그 표기는 다시 🔎 확인 필요로 돌아옵니다.
- **학부모 문의사항에 날짜 구분선.**
  - 줄 끝 작은 회색 글씨만으로는 어제 것과 오늘 것이 한눈에 안 갈립니다. 출결·픽업은 **날짜를 잘못 읽으면 아이가 잘못된 날 차를 타는** 문제라 경계가 눈에 띄어야 합니다.
  - `오늘`(파란색) / `어제` / `8월 25일 (월)` + 그날 건수. 스크롤해도 위에 붙어 있습니다.
  - 기존 정렬("답 안 한 것 먼저")은 그대로 두고 **날짜가 바뀌는 지점에만** 선을 넣습니다 — 날짜가 순서를 흔들면 안 되니까요.

## v0.266.0 - 2026-08-27

- **셔틀 매칭이 별칭을 읽습니다.**
  - 이 세 명은 **퇴소로 잘못 넘어갈 뻔했습니다.** 명부에는 영문으로 있는데 셔틀 명단에는 한글로 적혀 있어 기계 눈에는 없는 아이였습니다.
  - 규칙으로는 못 풉니다 — Maya를 '마야'로 옮길지 '마이아'로 옮길지는 사람이 정하는 것이지 규칙이 아닙니다. 그래서 **코드에 세 명을 박아 넣지 않고**, 출결에서 쓰던 별칭 표(`attendance_learning_rules`)를 셔틀에서도 읽게 했습니다. 네 번째 아이에게 또 막히지 않기 위해서입니다.
  - 앞으로 같은 일이 생기면 출결 인박스에서 🔎 를 눌러 가르치면 **셔틀에도 함께 적용됩니다.**
  - 별칭은 동명이인 후보에 더하는 게 아니라 **덮어씁니다** — 사람이 "이건 이 아이"라고 못 박은 것이니까요.
- ⚠️ **SQL 실행 필요**: `20260827120000_name_aliases.sql` (세 명의 별칭을 영문 이름으로 찾아 등록. 딱 한 명일 때만 걸고, 둘 이상 걸리면 건너뜁니다)

## v0.265.0 - 2026-08-27

- **`퇴소` 상태 신설.**
  - 명부가 절대 기준이므로, 유치부 반이 아닌데 명부에 없다면 답은 하나뿐입니다 — 나간 아이입니다. `확인필요`로 두면 아무도 손대지 않은 채 영영 남습니다. 사실을 그대로 적어두는 편이 낫습니다.
  - `확인필요` 에는 이제 **동명이인처럼 진짜로 사람 판단이 필요한 것만** 남습니다(74건 → 2건 예상). 목록이 짧아야 실제로 처리됩니다.

## v0.264.0 - 2026-08-27

- 셔틀 매칭 미리보기에 **정류장 단서** 추가. 남은 확인필요 74건이 전부 이번 달(2026-08) 생성이라 지난 학기 잔재가 아닙니다 — 그러면 "누구인가"를 판단할 다른 근거가 필요합니다. 같은 정류장에 이미 유치부로 확정된 줄이 있는지를 셉니다. 기사님이 그 정류장에 들르는 이유가 같다면 반 칸만 비어 있을 뿐 그 아이도 유치부일 가능성이 큽니다.

## v0.263.0 - 2026-08-27

- 셔틀 매칭: 반 코드 힌트가 명부 표기와 안 맞을 때 **학년으로 좁힙니다**. `김재이(G3J)` 의 `G3J` 가 명부 반 이름과 표기가 달라 못 맞추더라도, 거기서 학년 `3` 은 읽을 수 있고 그것만으로 대개 한 명으로 좁혀집니다.
- 확인필요 목록에 **생성일(`when`)과 월별 분포**를 함께 냅니다. 남은 줄들이 전부 오래된 날짜면 지난 학기 배정이라는 뜻이라, 이름을 아무리 들여다봐도 명부에 없는 게 당연합니다. 그 판단을 추측이 아니라 숫자로 하기 위해서입니다.

## v0.262.0 - 2026-08-27

- **반 이름 판독을 뒤집었습니다.** 실제 데이터가 규칙보다 훨씬 지저분했습니다 — `5Falcon`(공백 없음), `Pelican 4`(영단어가 앞), `7Crane/5Toucan`(두 반 겹침), `Swan/`(잘림), `7 Albatorss`·`6 seahwak`(오타). 이걸 전부 규칙으로 잡으려 하면 끝이 없어서, **초등부·중고등부 형식이 아니면서 영문이 들어 있으면 유치부**로 바꿨습니다. 반 이름 형식이 세 가지뿐이니 앞의 둘만 정확히 알면 됩니다. 유치부에 새 반 이름이 생겨도 규칙을 안 고쳐도 됩니다. **24개 문형 테스트 통과.**
  - 중고등 판정에 단어 경계를 넣었습니다 — 없으면 `5Starling`의 `5St`가 서수로 걸립니다.
- **괄호 힌트를 생일 말고도 읽습니다.** `김재이(G3J)`(반 코드), `이준서(중등)`(부서), `김재이(190510)`(생일) — 사람은 그때그때 편한 걸 씁니다. 힌트로 좁혀서 하나도 안 남으면 힌트가 틀린 것이므로 원래대로 두고 사람에게 넘깁니다. 엉뚱한 학생에게 붙이는 것보다 낫습니다.

## v0.261.0 - 2026-08-27

- **셔틀 매칭 미리보기에서 버그 2개 발견·수정** (미리보기로 돌려서 실데이터는 무사했습니다).
  - **반 칸을 하나도 못 읽고 있었습니다.** 실제 칸 이름이 `class_raw` 인데 후보 목록에 없었습니다. 그래서 유치부가 0건으로 나왔고, 256건이 전부 "명부에 없음"으로 몰렸습니다.
  - **명부에 재학 조건이 빠져 있었습니다.** `status='active'` 를 안 걸어서 454행 중 292명이 대조 대상이 됐습니다(실제 명부는 137명). 지난 학기 학생과 중복 행이 섞인 상태였고, 그대로 연결했다면 셔틀 배정이 **이미 나간 학생에게 붙었을** 수 있습니다.
- **명부 검산을 반영의 전제 조건으로.** 대조 대상이 137명(초등 101 + 중고등 36)이 아니면 `?apply=1` 을 **거부합니다**(409). 명부가 흐트러진 채 연결하면 조용히 잘못되는 종류라, 숫자가 맞을 때만 진행합니다.

## v0.260.0 - 2026-08-27

- **셔틀 매칭에 "명부가 절대 기준" 원칙 적용.**
  - 대조 대상을 **초등부·중고등부로 한정**합니다. 명부에 있으면 우리 학생, 없으면 아닙니다. 코드가 추측해서 없는 아이를 만들어내지 않습니다.
  - 반 이름 규칙을 판독기로 추가(12개 문형 테스트 통과): `4 sparrow`→유치부 / `G3A` `G5AB`→초등부 / `6TH GRADE`→중고등부. 반 이름은 **명부에 없는 줄이 왜 없는지를 설명하는 데만** 씁니다.
  - 반이 유치부 형식이면 명부를 볼 것도 없이 `유치부`로 표시하고 넘어갑니다.
  - 한 칸에 이름이 둘인 경우("이름은 두 개인데 반이 한 개") 처리 — 통째로 먼저 대조하고, 안 되면 쪼개서 각각. 통째로 맞는 이름을 괜히 쪼개지 않습니다.
  - 미리보기가 `shuttle_assignments`의 **실제 컬럼 목록**을 함께 돌려줍니다. 이 표는 마이그레이션 밖에서 만들어져 반 칸 이름을 코드로 확신할 수 없어서, 무엇을 읽었는지 눈으로 확인할 수 있게 했습니다.

## v0.259.0 - 2026-08-27

- **셔틀 배정 학생 연결 준비.** 462줄 중 444줄이 `student_name_raw` 문자열로만 붙어 있었습니다. 문자열로는 6년을 이을 수 없습니다 — 동명이인·개명·오탈자에 다 끊깁니다.
  - 확인된 사실: 유치부는 셔틀 기사님이 그 정류장에 들르는 이유로만 데이터베이스에 들어와 있고, 학생 명부로는 다루지 않습니다.
  - 그래서 이 표만 **NOT NULL을 걸 수 없습니다** — 유치부 줄은 정당하게 학생과 안 붙습니다. 억지로 잠그면 그 줄이 저장되지 않고, 기사님이 그 정류장에 들르는 이유가 사라집니다.
  - 대신 `unlinked_reason` 칸과 CHECK 제약을 뒀습니다: **student_id가 없으면 왜 없는지를 반드시 적어야 합니다**(`유치부` / `확인필요` / `외부`). NOT NULL을 못 걸어도 같은 보장을 얻습니다 — 말 없이 비어 있는 줄이 존재할 수 없습니다.
- `/api/shuttle/link-students` — 이름 문자열을 학생 ID로 승격. 생일 힌트("김재이(190510)")와 영문명도 대조합니다. **기본이 미리보기**라 `?apply=1` 을 붙이기 전에는 아무것도 바꾸지 않습니다.
- ⚠️ **SQL 실행 필요**: `20260827110000_shuttle_link_reason.sql`

## v0.258.0 - 2026-08-27

- **업무보드가 토들 새 문의를 바로 받습니다**(새로고침 불필요).
  - 원인: 업무보드는 `pickup_requests`를 **처음부터 구독하고 있었습니다.** 코드는 맞았는데 그 표가 `supabase_realtime` 발행목록에 없어서, 구독은 성립하는데 **소식이 한 번도 오지 않는** 상태였습니다. 조용히 아무 일도 안 일어나는 종류의 고장이라 알아채기 어려웠습니다. 다른 표들은 만들 때 각자 넣었는데 이 표만 그 단계가 빠져 있었습니다.
  - 반면 대시보드는 30초마다 스스로 다시 불러오는 화면이라 멀쩡히 떴습니다 — 그래서 뷰어가 메인보다 빠른 뒤집힌 상태가 됐습니다.
  - 고친 방법 세 겹: ① 발행목록에 `pickup_requests` `google_chat_mirror_messages` `attendance_entries` 추가 ② 수집 라우트가 끝날 때 서버에서 방송 발신(`notifyRefreshFromServer` — Realtime HTTP 창구를 써서 웹소켓을 새로 열지 않습니다) ③ 업무보드가 표 구독과 **별개로** 방송도 듣고, 창 포커스가 돌아올 때도 한 번 새로 부릅니다.
  - 한 겹이 어긋나도 화면이 멈추지 않습니다.
- ⚠️ **SQL 실행 필요**: `20260827100000_realtime_inbox_tables.sql`

## v0.257.0 - 2026-08-27

- **학생 연결 잠그기 1단계** — 실측 결과 미연결·고아가 0인 표 7개에 `student_id NOT NULL` + `wr_students(id)` 외래키를 겁니다. 학생과 연결되지 않은 행은 **저장 자체가 안 됩니다.** "학생 기준으로 기록한다"가 관례에서 제약조건이 됩니다.
  - 대상: `attendance_entries` `attendance_records` `wr_comments` `wr_reports` `incident_students` `lib_visits` `lib_loans`
  - **`ON DELETE RESTRICT`** — CASCADE로 두면 학생 한 명을 지울 때 그 아이의 6년치 기록이 조용히 사라집니다. 성장기록을 남기는 목적과 정면으로 어긋나서, 기록이 있는 학생은 지워지지 않게 했습니다. 지우려면 기록을 어떻게 할지 사람이 먼저 결정해야 합니다.
  - 안전장치: 표마다 잠그기 직전에 미연결·고아를 다시 셉니다. 하나라도 있으면 그 표는 건너뜁니다. 여러 번 실행해도 결과가 같습니다.
- **`scanIntoEntries`가 학생 없는 줄을 만들지 않도록 수정.** 이름만 들고 다니다 저장 시점에 다시 찾는 구조였는데, 그때 못 찾으면 `student_id`가 빈 줄이 생겼습니다. NOT NULL로 잠그면 그 줄은 저장이 실패하므로, 매칭 단계에서 명부 id를 함께 들고 나오고 id 없는 건 아예 거릅니다.
- 문서 2건: `데이터_분류규칙.md`(80개 표를 학생기록/입구/무관 3갈래로 분류 + 새 데이터 판단 흐름), `학생중심_학적시스템_제안서.md`
- ⚠️ **SQL 실행 필요**: `20260827090000_lock_student_fk.sql`

## v0.256.0 - 2026-08-26

- **대시보드에서 지운 출결이 되살아나던 문제를 고쳤습니다.** 화면 문제가 아니라 구조 문제였습니다 — 대시보드가 구글챗·토들 **원본 메시지를 매번 다시 파싱**해서 올리고 있었는데, 원본에는 "처리했다"를 적을 자리가 없어 새로고침마다 되살아났습니다. 업무보드의 체크는 `pickup_requests.answered_at`만 바꿨고 대시보드는 그 칸을 보지도 않았습니다.
  - 새 표 `attendance_entries` — "행정실이 등록한 출결"을 담는 단일 진실. 대시보드는 이제 **이 표만** 봅니다. 지우면 지워진 채로 남습니다(`state='무시'`).
  - `touched_by_human` — 사람이 손댄 줄은 자동 스캔이 다시 건드리지 않습니다. 자동 판단이 사람 판단을 이기면 안 됩니다.
- **출결 기간 인식**. `extractTargetRange` 신설 — "수요일까지", "이번주 금요일까지", "내일부터 금요일까지", "8/28까지", "3일간", "이번주 내내" 모두 시작~끝으로 잡습니다. 주말은 잘라내고, 최대 21일로 제한합니다(날짜 파싱이 어긋났을 때 몇 주짜리 결석이 조용히 굳는 것보다 잘려서 눈에 띄는 편이 낫습니다). 11개 문형 테스트 통과.
- **인박스에서 등록 여부가 보이고, 거기서 바로 등록됩니다**.
  - ✅ 초록 체크 — 이미 등록됨(대시보드에 떠 있음). 누르면 내립니다.
  - ❓ 물음표 — 자동 판단이 애매해 대기 중. 왜 애매한지가 툴팁에 뜹니다("명부에서 학생을 찾지 못했습니다" 등). 누르면 등록합니다.
  - 기간이 여러 날이면 이름 옆에 `~8/28` 칩이 붙습니다.
  - 확실한 건(학생 한 명 + 종류 분명 + 날짜 있음)은 **자동으로 등록**되어 처음부터 초록 체크입니다.
- ⚠️ **SQL 1개 실행 필요**: `20260826230000_attendance_entries.sql`

## v0.255.0 - 2026-08-26

- **구글챗을 폴링에서 푸시로.** 외부 스케줄러가 1분마다 부르는데 함수는 25초만 돌아서, **매 분 35초는 아무도 안 보고 있는 공백**이 남았습니다. 폴링으로는 이 공백을 없앨 수 없어서, 구글이 새 메시지를 올라오는 즉시 알려주도록 뒤집었습니다(Google Workspace Events API + Pub/Sub).
  - `src/lib/googleChatEvents.ts` — 구독 생성·갱신·조회. 인증은 지금 쓰는 사용자 OAuth 토큰 그대로(스페이스 구독은 관리자 권한이 아니라 "멤버일 것"만 요구하고, 필요한 `chat.messages.readonly`는 이미 받아둔 상태).
  - `/api/google-chat/push` — **알림을 초인종으로만 씁니다.** 알림에 실린 내용은 쓰지 않고, 신호만 받아 우리 토큰으로 구글챗을 직접 읽습니다(`includeResource: false`). 그래서 가짜 알림으로 할 수 있는 최악이 "쓸데없이 한 번 더 읽게 만드는 것"이고, 가짜 메시지가 인박스에 들어올 길은 없습니다.
  - `/api/cron/chat-subscription-renew` — 하루 한 번 기한을 최대로 되돌립니다. 구독이 사라져 있으면 알아서 새로 만듭니다. 구글 권고대로 만료 임박 알림에 기대지 않고 만료 시각을 직접 추적합니다.
  - 1분 폴링은 **안전망으로 그대로 둡니다.** 푸시가 검증된 뒤에 줄일 예정입니다.
- 새 표 `google_chat_event_subscriptions` — RLS를 켜고 정책을 하나도 두지 않아 서버 라우트(크론)만 접근합니다.

## v0.254.0 - 2026-08-26

- **토들 수집기가 새 메시지를 기다리지 않고 즉시 받아옵니다**(확장 2.5.0). 지금까지는 배경 일꾼이 1분마다 물었는데, **1분은 크롬 `chrome.alarms`가 허용하는 가장 짧은 주기**라 그 아래로 내릴 방법이 없었습니다. 시계를 더 빨리 돌릴 수 없으니, 묻는 대신 알림을 받도록 바꿨습니다.
  - `content.js`가 토들 탭의 화면 변화를 지켜보다가(`MutationObserver`) 배경 일꾼의 어깨를 두드립니다. ① 탭 제목(안 읽은 개수 "(3)") 변화 — 가장 확실한 신호라 즉시, 쿨다운 3초 ② 본문 변화 — 보조 신호라 1.2초 잠잠해진 뒤, 쿨다운 20초.
  - 1분 주기는 **안전망으로 그대로 둡니다.** 토들 화면 구조가 바뀌어 이 감지가 헛돌더라도 예전만큼은 동작합니다.
  - 근무시간(평일 07~19시) 밖의 두드림은 무시합니다 — 밤에 토들 탭을 스크롤하는 것만으로 수집이 도는 일을 막습니다.
- 구글챗 촘촘한 구간을 07:00~**19:00**으로(20시 → 19시).
- ⚠️ **사무실 PC에서 확장 프로그램을 새로 넣어주세요**(`chrome://extensions` → 새로고침). 확장이 올라오면 열려 있는 토들 탭은 알아서 새로고침됩니다.

## v0.253.0 - 2026-08-26

- **셔틀 자동 도착·출발 크론을 하나로 통합**(`/api/cron/shuttle-auto`). 두 판단은 같은 시각에 같은 표(`shuttle_run_events`)를 보고 이뤄지는데, 예전에는 두 라우트가 각자 1분마다 불려 각자 그 표를 조회하고 각자 25초씩 함수를 붙잡았습니다. 한 루프 안에서 도착 → 출발 순서로 처리합니다(방금 도착으로 찍힌 노선이 같은 회차에서 출발 후보가 되도록). **65h → 33h.**
  - 예전 두 주소는 남겨두되 "여기로 옮겼습니다"만 알려주고 아무 일도 하지 않습니다 — 스케줄러를 바꾸기 전에 두 번 도는 것보다 한쪽이 조용히 쉬는 편이 안전합니다.
  - ⚠️ **cron-job.org에서 예전 두 작업을 지우고 `/api/cron/shuttle-auto` 하나만 1분마다 부르도록 바꿔주세요.**
- **구글챗 미러링을 근무시간 전체(평일 07:00~20:00) 실시간으로.** 구글챗·토들은 업무시간 내내 바로바로 반영되어야 하는 통로입니다. 아침·하원 두 토막으로 나누던 것을 근무시간 전체로 넓혔습니다 — 출결·픽업 연락은 예고 없이 들어오고, 놓쳤을 때의 대가가 비용보다 큽니다.
- 전체 함수 실행시간: **928h → 182h (-80%)**. 구글챗을 넓히면서 126h를 다시 쓰지만, 문지기·통합으로 비운 800h 안에서 충분히 감당됩니다.

## v0.252.0 - 2026-08-26

**크론 문지기 — 앱 전체 함수 실행시간을 928h → 139h(-85%)로 줄였습니다.** 실측해 보니 매분 크론 3개가 전체의 97%를 쓰고 있었습니다. 셋 다 1분마다 불리면서 한 번에 25초씩 함수를 붙잡고, 그것을 24시간 내내·주말에도 했습니다.

- **셔틀 자동 도착·출발(2종): 600h → 65h.** 운행 시간대(평일 15:30~19:00, 마지막 차 마무리용 30분 꼬리 포함) 밖이면 루프에 들어가지 않고 즉시 반환합니다. 창 밖에는 애초에 볼 위치 자체가 저장되지 않으므로(`/api/shuttle/track`이 버립니다) 없는 데이터를 5초마다 다시 확인하고 있었던 셈입니다.
- **구글챗 미러링: 300h → 52h.** 담당자 확인에 따라 **촘촘히 봐야 하는 시간을 특정**했습니다 — 아침 출결(07:00~09:00)과 하원 준비·지도(14:00~16:30). 특히 후자는 비용을 아낄 구간이 아니라 **가장 촘촘해야 할 구간**입니다: 그 시간에 직원들이 나가서 하원지도를 하는데, 픽업 연락이 늦게 반영되면 알림이 오기 전에 아이를 차에 태워버리는 일이 생깁니다.
  - 그 밖의 시간은 아예 건너뛰지 않고 **한 번만** 확인합니다(약 1초). 셔틀 위치와 달리 채팅은 안 보면 영영 놓치는 자료라, 늦어도 1분 안에는 들어옵니다.
- **안내보드·도착체크: 하원 시간대 밖 60초 → 15분.** 하원 대시보드와 도착체크는 하원 시간에만 쓰입니다. 활성 시간대도 14~19시에서 15~19시로 좁혔습니다.
- `pollDelay`도 **다음 시작 시각을 넘겨서 자지 않도록** 고쳤습니다 — 15분씩 자면 15:50 하원 시작을 놓치고 16:05에 깨어납니다.

## v0.251.0 - 2026-08-26

- **운영 대시보드 호출량 절반으로**.
  - 근무 시간(평일 07~20시) **15초 → 30초**, 그 밖의 시간·주말 **30분 → 3시간**.
  - 야간만 늘려서는 소용이 없습니다 — 호출의 **98.8%가 근무 시간 폴링**에서 나오고, 야간을 30분에서 3시간으로 늘려 아끼는 양은 전체의 1.1%뿐입니다. 실제로 줄이려면 근무 시간 주기를 건드려야 했습니다.
  - 30초로 늘려도 체감이 없는 이유: 급한 두 가지가 이미 주기와 무관하게 처리됩니다. **교시 전환**은 끝나는 시각에 맞춘 별도 타이머가 정확히 그 순간 갱신하고, **문의 처리**는 업무 보드에서 체크하는 즉시 방송 신호로 갱신합니다(v0.249). 주기가 담당하는 것은 "토들에 새 문의가 들어왔나" 정도입니다.
  - 몇 시간을 자든 **07:00 정각에는 반드시 깨어나** 그날 첫 화면을 미리 받아둡니다.
  - 결과: 월 **69,508회 → 34,450회**. Vercel Pro 포함량의 3.4%.

## v0.250.0 - 2026-08-26

- **운영 대시보드 갱신 주기를 근무 시간 기준으로 정리**.
  - 평일 **07:00~20:00 → 15초** · 그 밖의 시간과 주말 → **30분**(예전 2분).
  - **아예 멈추지는 않았습니다.** 이 화면은 벽에 걸린 모니터라 아무도 탭을 다시 누르지 않아서, 타이머가 끊기면 누가 새로고침할 때까지 어제 화면이 그대로 남습니다. 30분에 한 번은 밤새 22번이라 사실상 없는 부담이면서, 스스로 깨어날 수 있습니다.
  - **근무 시작 시각을 넘겨서 자지 않습니다.** 그냥 30분씩 자면 06:45에 잠든 화면이 07:15에야 깨어나 근무 시작 15분간 낡은 화면을 띄웁니다. 다음 근무 시작까지 남은 시간과 견줘 짧은 쪽으로 잡아 **07:00 정각에** 깨어납니다(06:45 → 15분 뒤, 06:59 → 1분 뒤).
  - 결과: 대시보드 호출이 월 약 **7만 회**(근무시간 3,120/일 + 야간 22/일). Vercel Pro 포함량의 7%입니다.

## v0.249.0 - 2026-08-26

**업무 보드에서 처리한 문의가 운영 대시보드에 늦게 반영되던 문제 — 진짜 원인을 찾았습니다.** 지난 버전에서 고친 응답 캐시는 원인의 절반이었고, 나머지 절반은 **폴링 주기**였습니다.

- **빠른 갱신 시간대를 학교가 돌아가는 내내(평일 07~20시)로 확대.** v0.234에서 Vercel 사용량을 줄이며 "평일 14~19시에만 15초, 그 외에는 120초"로 걸어뒀는데, 이 화면은 사무실 벽에 하루 종일 띄워두고 계속 쳐다보는 모니터입니다. 오전에 문의를 처리해도 **2분이 지나야** 사라지니 "반영이 안 된다"고 느껴질 수밖에 없었습니다. 사람이 없는 밤·주말에만 느리게 돕니다.
- **즉시 반영 추가.** 업무 보드에서 문의를 체크하는 순간 대시보드가 바로 다시 불러옵니다. 폴링만으로는 아무리 촘촘해도 최대 한 주기(15초)만큼 늦고, 그동안 이미 처리한 문의가 남아 있어 다른 사람이 또 처리하려 들 수 있습니다.
  - 표를 직접 구독하지 않고 **방송(broadcast)** 신호를 씁니다 — 대시보드는 로그인 없이 토큰으로만 열리는 화면이라 `pickup_requests` 구독 권한이 없어 아무 소식도 못 받습니다. 방송은 표 권한과 무관한 단순 신호라 이 화면에서도 받습니다. 내용은 담지 않고 "다시 불러와라"만 알립니다.
  - 문의 체크뿐 아니라 **[결석]·[픽업]·[탑승] 원클릭 처리**에도 같은 신호가 붙습니다.
  - 신호 전송이 실패해도 조용히 넘어갑니다(다음 폴링에서 반영되므로 하던 일이 막히지 않습니다).

## v0.248.0 - 2026-08-26

- **교시 이름이 영어 화면에서도 한글로 남던 문제 수정.** `1교시` → `Period 1`, `점심` → `Lunch`, `방과후` → `After School`. 교시 이름은 DB(`wr_periods.label`)에 한 벌만 저장하고 화면에 낼 때만 언어에 맞춰 바꿉니다 — 한/영 두 벌로 저장하면 학기마다 교시를 고칠 때 한쪽만 고쳐져 어긋납니다. 규칙에 없는 자유 입력 라벨은 임의로 번역하지 않고 그대로 둡니다.
- **오리엔테이션 데모 반에 실제와 똑같은 시간표 추가**(`20260826190000_demo_timetable.sql`). 신입 선생님에게 "이 화면이 이렇게 생겼습니다"를 보여주는 계정인데 시간표가 비어 있어 정작 매일 보게 될 화면을 못 보여줬습니다.
  - 초등부 실제 반 중 **시간표가 가장 많이 채워진 반**을 자동으로 골라 그대로 복사합니다. 반 이름을 코드에 박아두면 그 반이 없어지거나 담임이 바뀔 때 조용히 아무것도 안 하게 되므로, 늘 존재하는 기준으로 고릅니다.
  - 여러 번 실행해도 안전합니다(데모 반 시간표를 비우고 다시 채웁니다).

## v0.247.0 - 2026-08-26

- **출결내역: 고치면 그것이 규칙이 됩니다.** 🔎(명부 대조 실패)·⚠️(동명이인 미확정) 이름을 누르면 "가르치기" 창이 뜨고, 어느 학생인지 한 번 알려주면 규칙으로 저장되어 **다음부터 자동으로 연결**됩니다.
  - 배우는 것 세 가지: **별칭**(`Maya` → 김마야, `조영운` → 조영윤 오탈자) · **분류**(`일찍 데려갈게요` → 조퇴) · **제외**(`선생님` 같은 낱말은 이름이 아님).
  - **AI를 부르지 않습니다.** 출결내역은 원래 규칙 기반이라 비용이 0이고, 저장하는 순간 바로 적용되며, 왜 그렇게 분류됐는지 규칙 목록에서 그대로 볼 수 있습니다.
  - 사람이 가르친 분류가 기본 키워드보다 **우선**합니다 — 기본 규칙이 틀려서 고치는 것인데 기본이 이기면 아무리 가르쳐도 안 바뀝니다.
  - 규칙이 바뀌면 열려 있는 화면들이 실시간으로 따라옵니다. 같은 표기를 두 번 가르치면 마지막 것으로 덮어씁니다.
  - 가르치기 전/후를 네 가지 문장으로 검증(4/4 통과) — 영문 이름·오탈자·제외 낱말·분류 규칙.

## v0.246.0 - 2026-08-26

- **업무 보드에서 처리한 학부모 문의가 운영 대시보드에 반영되지 않던 문제 수정.** 원인은 데이터가 아니라 **응답 캐시**였습니다. 대시보드는 늘 같은 주소(`/api/ops-board/{토큰}`)로 물어보는데 `cache: "no-store"`가 없어서 브라우저·CDN이 지난 응답을 그대로 다시 내주고 있었습니다. `dynamic = "force-dynamic"`은 "미리 만들어두지 말라"는 뜻일 뿐, 만들어진 응답이 캐시되는 것까지 막지는 않습니다. 요청 쪽과 응답 헤더(`Cache-Control: no-store`) 양쪽 모두 막았습니다.
- **출결·픽업 칸 이름을 짧게.** 칸이 작은데 `Maya Rodriguez Kim` 같은 이름이 들어오면 배지 하나가 줄을 통째로 먹어서 정작 몇 명인지가 안 보였습니다. 영문 이름은 **첫 이름만**(Maya) 보여주고, 전체 이름과 메모는 마우스를 올리면 뜹니다. 한글 이름은 원래 짧아 그대로 둡니다.
- 결석 이름 옆 **[연락전] 꼬리표 제거** — 이 화면은 지나가며 훑는 현황판이고 연락 여부는 업무 보드에서 처리하는 일이라, 여기 붙어 있어도 손이 가지 않았습니다.

## v0.245.0 - 2026-08-26

- **정류장과 신호대기를 반복성으로 구별합니다.** 하루치 기록만 보면 둘은 구별이 안 됩니다 — 신호에 걸려 60초 선 것과 정류장에서 60초 선 것은 GPS만 보면 똑같이 생겼습니다. 여러 날을 겹쳐야 갈라집니다.
  - 최근 60일 정차를 자리별로(40m) 묶고, 묶음마다 **"운행한 날 중 며칠이나 여기서 섰는가"**를 셉니다. 50% 이상 · 3일 이상인 묶음만 정류장으로 인정합니다.
  - 예전에는 반경 400m 안의 모든 정차를 그냥 평균 내서, **정류장 300m 앞 신호등에 걸린 기록이 정류장 좌표를 그쪽으로 끌고 갔습니다.**
  - 20일 운행 모의 데이터(정류장 18/19일, 신호등 7/19일)로 검증 — 정류장은 4m 오차로 인정, 신호등은 제외. 2/2 통과.
  - [셔틀 → 링크·기기]의 학습 좌표 목록에 **`18일 관측 · 95%`** 배지와 평균 체류시간이 뜹니다(80% 이상 초록 / 50% 이상 주황). 반영 전에 근거를 보고 판단하실 수 있습니다.
- **운영 대시보드 시간표 위젯 크기 완전 고정.** 과목명이 한 줄(`Math`)이냐 두 줄(`Computer Science`)이냐에 따라 카드 높이가 달라져 교시마다 위젯이 출렁였습니다. 과목명 자리를 **항상 두 줄 높이**로, 선생님 이름 줄도 이름이 없어도 자리를 지키도록 고정했습니다.

## v0.244.0 - 2026-08-26

**정류장 학습을 깨지 않으면서 GPS 전송량을 줄입니다.** 직전 버전에서 권한 `Distance = 80m`는 잘못된 제안이었습니다 — 정차 감지가 "멈춰 있는 동안 쌓이는 점"에 의존하는데, 거리 필터를 걸면 멈춘 동안 전송이 아예 없어 **정류장 학습이 통째로 죽습니다**(나중에 학부모께 "몇 분 뒤 도착"을 안내하려면 이 학습 좌표가 근거입니다).

- **정차 감지에 '기록이 비는 모습'을 추가.** 이제 두 가지를 모두 잡습니다.
  - ① 점이 뭉치는 모습(거리 필터 없음) — 기존 방식. 여러 점을 평균 내 좌표가 가장 정확합니다.
  - ② 기록이 비는 모습(거리 필터 있음) — 점 사이가 45초 이상 벌어지고, 그 앞뒤가 400m 안이며, 직전 속도가 30km/h 이하(감속 중)면 그 자리를 정차로 봅니다.
  - 터널·음영지역(고속 주행 중 신호 끊김)과 30초 신호대기는 정차로 잡지 않도록 걸러냅니다. 실제 소스의 함수를 그대로 꺼내 네 가지 주행 패턴으로 검증했습니다(4/4 통과).
- **기사님 안내문 `Distance = 30m`.** 80m가 아니라 30m입니다. 운행 중에는 30초 간격이 그대로 유지되고(시속 40km면 30m를 2.7초에 지나므로 간격이 제약), 정차·야간·주말에는 전송이 멈춥니다. 정차 지점 오차도 30m 안입니다.
- **[24h 테스트] 기기가 정류장 학습을 오염시키던 문제 수정.** 그 기기는 하루 종일 위치가 저장되므로 담당자의 출퇴근 신호대기·집·사무실 주차가 전부 '정차'로 잡히고, 반경 400m 안에 정류장이 있으면 그 정류장의 학습 좌표 평균까지 끌고 갔습니다. 학습에 쓰는 기록을 **하원 운행 시간대(평일 15:30~18:30)로 못 박았습니다.**

## v0.243.0 - 2026-08-26

- **운영 대시보드 시간표 칸이 잘려 보이던 문제 수정.** 시간표 높이를 픽셀로 못 박았더니 학년 두 줄에 모자랐습니다. 고정하는 쪽을 반대로 바꿔서 — 날마다 인원이 달라지는 **[출결·픽업] 쪽을 고정 높이로 두고, 시간표가 남는 공간을 전부** 가져갑니다. 시간표가 늘 가장 크고, 동시에 옆 칸 변화에 흔들리지도 않습니다.
- **GPS 35대 확대 대비 사전 정리**(아래 용량 예측에서 나온 병목).
  - 신호 도달 기록(`last_hit_at`)을 **1분에 한 번만** 씁니다. 예전에는 핑마다 기기 행을 UPDATE해서 저장할 위치와 같은 수만큼 쓰기가 더 생겼습니다(35대면 월 25만 건 → 12만 건). 상태(reason)가 바뀌는 순간은 주기와 무관하게 남겨 진단은 그대로입니다.
  - **위치 원본 보관 90일 → 30일.** 며칠 지나면 실제로 보는 것은 원본 점이 아니라 요약(정류장 도착·정차 관측·운행 이벤트)이고, 그 요약은 그대로 90일 남습니다. 저장 용량 135MB → 45MB. 동의서의 "90일 뒤 삭제" 약속보다 짧으므로 약속을 어기는 방향이 아닙니다.
  - **기사님 안내문에 `Distance = 80m` 설정 추가.** 지금은 `Distance 0`이라 차가 서 있는 밤·주말에도 30초마다 계속 보냅니다 — 35대로 늘리면 **월 302만 회 호출 중 92%가 시간대 밖이라 서버가 그냥 버리는 헛호출**이 됩니다. 80m 이상 움직였을 때만 보내게 하면 운행 중 해상도는 그대로(시속 40km면 7초에 80m)이면서 정차 중에는 전송이 멈춥니다.

## v0.242.0 - 2026-08-26

- **문의 자리에서 바로 셔틀 출결 처리(화면 왕복 제거).** "오늘 지호 결석해요"를 읽고 [셔틀 → 하원 체크표]로 가서 다시 찾아 체크하고 돌아오던 왕복을 없앴습니다.
  - 출결·차량 관련 문의 줄에 **[결석] [픽업] [탑승]** 버튼이 붙습니다. 누르면 하원 체크표가 쓰는 표(`shuttle_boardings`)에 같은 모양으로 기록되어, 체크표·안내보드·도착체크·운영 대시보드에 그대로 반영되고 문의도 처리 완료로 표시됩니다.
  - 요일제 학생을 감안해 **그날 실제로 차를 타기로 된 배정만** 찾습니다. 같은 이름이 두 노선에 걸쳐 있으면 양쪽 모두 처리합니다(한쪽만 두면 반대쪽 차가 기다립니다).
  - 셔틀 배정이 없는 학생(도보·자차 하원)은 조용히 성공한 척하지 않고 그 사실을 알립니다.
  - 학습 상담 같은 문의에는 버튼이 붙지 않습니다 — 오조작만 늘어납니다.
- **학부모 문의 분류 탭.** AI가 이미 매긴 분류(출결·수업·학습·생활·교우·건강·안전·차량·하원·행사·일정·납부·행정)별로 칩을 눌러 걸러 봅니다. 건수는 분류 필터를 뺀 값이라, 탭을 눌러도 다른 탭 숫자가 변하지 않습니다.
- **운영 대시보드 재구성.**
  - 화면을 세로로 반 갈라 **왼쪽 = 시간표 + 오늘 출결, 오른쪽 = 학부모 문의(세로 전체)**. 문의가 한 번에 보이는 수가 두 배가 됐습니다.
  - [오늘 업무] 칸 제거. **결석 · 지각/조퇴 · 하원 픽업을 한 칸**에 색 머리표로 나눠 담았습니다(예전에는 픽업과 출결이 따로 있어 같은 아이가 두 칸에 나뉘어 떴습니다).
  - 날짜 옆에 **요일** 표시 — 요일제 셔틀·시간표를 볼 때 날짜만 보고 요일을 세던 것을 없앴습니다.
  - **시간표 위젯 높이 고정** — 예전에는 남는 공간을 비율로 나눠 가져서 출결·문의 건수가 바뀔 때마다 시간표 칸이 출렁였습니다. 학년 줄 두 개가 들어갈 높이로 못 박았습니다.

## v0.241.0 - 2026-08-26

- **동명이인을 생년월일로 구분.** 김재이처럼 같은 이름이 3명이고 그중 둘은 학년까지 같으면 학년으로는 갈라지지 않습니다. 선생님들이 실제로 쓰는 `김재이(190510)` 표기를 알아듣도록 했습니다.
  - 받아들이는 형태: `(190510)` 6자리 · `(20190510)`·`(2019-05-10)` 8자리 · `(0828)` 월일 4자리.
  - 생일이 학년보다 구체적이므로 생일을 먼저 봅니다. 확정하면 화면에 `김재이(190510)`으로 뜨고, 힌트가 없어 못 고르면 임의로 한 명을 찍지 않고 `김재이(확인 필요)`로 표시합니다.
  - 같은 사람을 학년으로 찾든 생일로 찾든 중복 제거 키가 같아지도록 통일했습니다(같은 학생이 두 줄로 쌓이지 않습니다).
  - 곁들여 고친 버그: `김재이(190510)`의 앞 두 자리를 **학년 19로 잘못 읽던 문제**(학년 힌트 정규식이 뒤에 숫자가 더 이어져도 잘라 읽었습니다).
  - 출결내역·픽업·하원 체크표·운영 대시보드의 명부 조회에 `birth_date`를 함께 싣도록 4곳 수정.
- **인덱스 SQL이 중간에 멈추던 문제 수정** — `shuttle_safety_events`처럼 아직 만들어지지 않은 표가 있으면 `relation does not exist`로 스크립트 전체가 실패해서 만들 수 있는 인덱스까지 하나도 안 만들어졌습니다. 표가 실제로 있을 때만 만들도록 `to_regclass`로 확인하고 건너뜁니다.

## v0.240.0 - 2026-08-26

전체 코드베이스(378개 파일 · 62,000줄 · API 77개 · 크론 12개) 점검 후 최적화했습니다.

**① 코드**

- 참조되지 않는 죽은 파일 4개 제거(343줄): `MyClassClient`(교사 화면이 `TeacherOverviewClient`로 대체됨), `MyTasksWidget`·`AllTasksWidget`(흐름판 [내 업무만|전체] 토글로 흡수됨), `kakaoDirections`(학부모 ETA 기능 제거 때 남은 잔재).
- 미사용 npm 의존성 `@tiptap/pm` 제거.

**② API 토큰**

- **웹 검색 횟수 축소** — 입력 토큰의 대부분은 검색 결과 본문입니다. 교육뉴스 8→4, GIA시스템 제안 8→4, 학기준비 분석 5→3, 기본값 6→4. 검색 기반 기능의 입력 토큰이 대략 절반으로 줄어듭니다.
- **헛돈 나가던 프롬프트 캐시 정리** — 라우트별 지시문에도 `cache_control`을 달고 있었는데, Anthropic은 캐시 첫 기록 시 입력 토큰을 1.25배로 청구하고 5분 안에 같은 접두사로 다시 불러야 본전입니다. 라우트별 뒷부분은 그 기능 전용이라 하루 몇 번 부를까 말까 해서 **적중은 거의 없고 1.25배만 매번 냈습니다.** 여러 라우트가 글자까지 공유하는 앞부분에만 캐시를 답니다.
- **AI 호출당 DB 왕복 축소** — 기능 스위치 확인 + 사용량 기록이 각각 Supabase 클라이언트를 새로 만들었습니다(호출당 2~3개). 하나로 합치고, 스위치 값은 60초 프로세스 캐시를 둬 호출마다 하던 조회를 없앴습니다(스캔·일괄분류처럼 한 요청에서 수십 번 호출하는 기능에서 특히 효과).

**③ 데이터**

- **보관 기간 없이 무한히 쌓이던 표 5개에 주기 부여** — Supabase 무료 플랜 DB 상한이 500MB인데 이 표들은 매일 늘기만 했습니다. 야간 크론에 추가: 운행 이벤트·안전운행 90일, AI 사용기록 90일, 오류 로그 30일, 구글챗 미러링 180일(원본은 구글챗에 남아 있음).
- 각 표가 실패해도 나머지 정리는 진행되도록 개별 격리(표가 아직 없는 상태에서도 크론 전체가 죽지 않음).
- **인덱스 5개 추가**(`supabase/migrations/20260826120000_log_retention_indexes.sql`) — 정리 작업과 개발자 대시보드의 "최근 순" 조회가 표 전체를 훑지 않도록.

## v0.239.0 - 2026-08-25

- **인박스 탭에 새 메시지 알림 배지.** 학부모 문의·출결내역·출결알림 탭에 "마지막으로 그 탭을 본 뒤" 들어온 항목 수가 모바일 앱 아이콘처럼 탭 오른쪽 위 빨간 원으로 뜹니다. 탭을 열면 본 것으로 치고 사라집니다(확인 시각은 브라우저에 저장).
  - [선생님요청]은 안읽음이 아니라 **미완료 건수**입니다 — 요청은 읽는 게 아니라 처리해야 끝나는 것이라, 완료를 눌러야 숫자가 내려갑니다.
  - 학부모 문의 배지는 목록 패널이 닫혀 있어도 갱신되도록 접수시각만 가볍게 따로 구독합니다(채널명 분리로 중복 구독 없음).
- **토들·구글챗 연결상태를 인박스 머리글에 상시 표시.** `토들 🟢 구글챗 🟢` 형식으로, 마우스를 올리면 마지막 신호 시각·상세가 뜹니다. 연동이 조용히 멈추면 화면에는 "새 소식이 없는 것"처럼 보여 놓치기 쉬운데, 3분 넘게 신호가 없으면 🔴, 신호는 오는데 오류 상태(재로그인 필요 등)면 🟡로 구분합니다.
  - 구글챗 폴링 크론이 회차마다 `integration_heartbeats`(key `google-chat-poll`)에 신호를 남기도록 추가 — 토들 수집기(`toddle-collector`)와 같은 방식입니다.
  - 상태 확인은 60초에 한 번, 화면이 보일 때만(서버 호출 절감 원칙 유지).

## v0.238.0 - 2026-08-25

- **업무 보드 칸 배치를 실사용 빈도 기준으로 재배열.** 가장 자주 쓰는 것은 업무등록(등록창+채팅창)과 인박스인데 업무흐름판이 가운데를 차지하고 있어 실용성이 떨어졌습니다. 각 칸의 용도를 다시 따져 배치했습니다.
  - 등록·채팅 = 하루 종일 손이 가는 **입력 도구** → 가운데, 가장 넓게.
  - 인박스 = 수시로 확인하는 **수신함** → 왼쪽(읽는 흐름의 시작점). [→업무등록]을 누르면 바로 옆 가운데에서 이어서 처리.
  - 흐름판 = 드래그로 진행상황을 옮기고 훑어보는 **현황판** → 오른쪽 좁은 칸. 안 볼 때는 접어서 막대만.
  - 새 배치: **📥 인박스 | 💬 등록·채팅(최대) | 🔀 흐름판(접기 가능)**
- 흐름판이 옆 칸이 되면서 3열 대신 **예정→진행중→완료를 위에서 아래로 쌓는 세로 배치**(compact)를 씁니다 — 좁은 폭에 3열을 욱여넣으면 카드 제목이 다 잘립니다. 드래그 이동은 그대로 됩니다.
- 모바일 기본 탭도 등록·채팅으로. 저장 키를 v3로 올려 예전 레이아웃 저장값이 새 배치에 잘못 적용되지 않게 했습니다.

## v0.237.0 - 2026-08-25

- **업무 보드를 3존 관제탑으로 재설계.** 예전 구조는 이름만 3구역이었지 실제로는 2단이었습니다 — 왼쪽 한 칸에 인박스와 채팅을 위아래로 욱여넣어 둘 다 좁았고, 하루 종일 보는 흐름판은 화면의 절반뿐이었습니다.
  - **📥 인박스 | 🔀 업무 흐름판 | 💬 소통** 세 칸으로 분리. 들어오는 것을 왼쪽에서 받고, 가운데에서 처리하고, 오른쪽에서 이야기합니다(Front·Missive·Linear 등 관제 화면의 공통 배치).
  - 세 칸 모두 같은 모양·같은 높이(32px)의 머리글을 씁니다. 흐름판의 [🙋 내 업무만 | 🗂️ 전체] 토글과 '진행 N건'은 스크롤되는 본문에서 머리글로 올려 항상 같은 자리에 고정.
  - **양옆 칸 접기**: 머리글 ✕를 누르면 세로 막대만 남고 흐름판이 화면을 거의 다 씁니다(집중 모드). 폭·접힘 상태는 브라우저에 저장되어 다음에도 유지됩니다.
  - 칸 폭 드래그를 실제 컨테이너 폭 기준으로 계산하도록 수정 — 예전에는 `window.innerWidth`에서 사이드바 폭을 어림잡아 빼는 방식이라 화면 크기에 따라 손끝과 경계선이 어긋났습니다.
- **머리줄 3겹 → 1겹.** 대분류 상단탭 + 페이지 헤더(학기·재학생·가이드) + 부서 줄이 쌓여 세로 100px 넘게 먹던 것을 한 줄로 합쳤습니다.
  - 부서 줄에 있던 [업무기록]·[휴지통]·[업무 보고서] 링크 제거 — 대분류 상단탭의 [지난 업무]·[휴지통]·[보고서]와 같은 곳으로 가는 중복이었습니다.
  - "채팅 위 업무등록 위젯에서…" 상시 안내 문구 제거, 가이드 버튼 2개를 1개로.
- **중복 위젯 정리.** 흐름판 위 부서 메모(`ActivityLog`)가 [소통] 칸의 고정 메모(`PinnedMemo`)와 `department_memos`의 같은 행을 보고 있어 화면에 같은 글이 두 번 떠 있었습니다. 소통 칸 한 곳으로 통합(실시간 구독도 하나 줄었습니다).
- 사용 가이드(❓)를 새 3존 구조 기준으로 다시 썼습니다.

## v0.236.0 - 2026-08-25

- **상단탭 자리·크기를 전 화면에서 고정.** 예전에는 각 페이지가 본문 안에서 직접 탭바를 그렸는데, 페이지마다 바깥 상자가 달라서(`max-w-2xl`~`max-w-none`, `p-4 sm:p-6`~`sm:p-8`, 패딩 없음) 화면을 옮길 때마다 탭의 시작점·폭·간격이 널뛰었습니다. 이제 대시보드 레이아웃이 본문 위 한 자리에서 `SectionTabs` 하나만 그립니다.
  - 흩어져 있던 `WorkTabs`/`SchoolTabs`/`DocsTabs`/`ShuttleTabs`/`TeacherTabs` 5개를 삭제하고 공용 컴포넌트로 통합(35개 파일에서 탭 렌더 제거).
  - 본문 좌우 여백을 탭바와 같은 `sm:px-6`으로 통일. 업무 보드에는 여백이 아예 없어 탭이 내용에 붙어 있던 것도 해소.
- **학교 메뉴 9개 → 5개, 문서·매뉴얼 17개 → 7개로 통합.** 사이드바 서브메뉴와 상단탭이 항목·순서까지 정확히 같습니다.
  - 학교: 개요 · 학생 · 교직원 · 반·시간표 · 학사운영
  - 흡수된 화면(명부 관리, 명부 점검, 과목, 수업 시간표, 당번표, 학기 준비, 학기 관리, 서류함, 회의 보고서, 제안함·채택예정 등 19개)은 그 탭이 열렸을 때 상단탭 바로 아래 작은 줄로 펼쳐집니다. **사라진 화면은 없습니다.**
- **셔틀 노선 지도를 좌우 2단으로.** 왼쪽에 지도만 크게(460px), 오른쪽에 [위치 못 찾은 정류장]과 [정류장별 출발·도착 시간표]를 모았습니다. 예전에는 위아래로 쌓여 지도가 얇은 띠로 눌렸습니다.
  - GIA 기준점 좌표에 고정 폴백을 둬서, 주소 지오코딩이 실패해도 **등원은 마지막 지점 = GIA, 하원은 첫 출발 지점 = GIA**가 항상 표시됩니다.
- **기사님 안내문·동의서 바로 인쇄.** [셔틀 → 링크·기기]에서 버튼 한 번으로 A4 인쇄본이 열립니다(`/print/driver-docs`). 호차별 인쇄 버튼은 기사님 성함·연락처·차량번호를 동의서에 미리 채워줍니다.
- **링크·기기 화면에 [전체 / ✓ 연결됨 / 미연결] 탭.** 38개 노선을 한 줄씩 훑지 않고 연결된 차량만 골라 볼 수 있습니다.

## v0.235.0 - 2026-08-25

- Vercel Pro 전환 후 첫 production 배포(v0.233~v0.234의 메뉴 정리·폴링 절감이 이때 실제로 반영되었습니다).

## v0.234.0 - 2026-08-25

- **서버 호출량 대폭 절감(Vercel 무료 한도 초과 대응).** 하루 종일 켜두는 화면들이 쉬지 않고 서버를 부르던 것을 정리했습니다.
  - 공용 폴링 훅 `useSmartPoll` 신설: ① 화면이 안 보이면(다른 탭·최소화) 호출 중단 ② 하원 시간대(평일 14~19시 KST)에만 빠른 주기, 그 외에는 느린 주기 ③ 창을 다시 보면 즉시 1회 새로고침.
  - 안내보드·도착체크: 3초 → 하원 시간대만 3초, 그 밖에는 60초. 운영 대시보드·하원 운영화면: 10~15초 → 그 밖의 시간 120초.
  - 업무 인박스·교사 화면·하원 체크표·실시간 셔틀의 폴링도 화면이 보일 때만 실행.

## v0.233.0 - 2026-08-25

- **사이드바 서브메뉴 = 상단탭 일치.**
  - **업무**: 업무 보드 · 보고서 · 지난 업무 · 휴지통 4개로 정리. 사건·회의·행사 기록과 등록사건목록·회의 보고서·AI 매뉴얼·제안함·채택예정은 **[문서·매뉴얼]로 되돌림**(원래 있던 자리).
  - **문서·매뉴얼**: 실무자 매뉴얼 · 문서함 · 기록 드라이브 · 사건 · 회의 · 행사 · 제안·채택. 상단탭 `DocsTabs` 신설해 13개 화면에 적용.
  - **학교**: 상단탭과 1:1 동일(개요 · 학생 조회 · 교직원 · 명부 관리 · 반·과목 · 수업·시간표 · 학사일정 · 당번표 · 학기 준비). 탭에 없는 보조 화면(명부 점검 · 학기 관리 · 운영 대시보드)은 맨 아래 **[설정]** 구분선으로 분리.

## v0.232.0 - 2026-08-25

- **하원 체크표 인쇄본을 실제 "하원차량 체크" 서식으로.** 인쇄 전용 표 신설 — [차번호 | 기사님·연락처 | 호차 | 아동이름 9칸] 흑백 실선 격자, A4 세로 8mm 여백, 요일제 학생은 `(월수금)이름` 표기, 픽업·결석은 사선 실선, 오늘 안 타는 학생은 회색. 인쇄 시 화면용 표·사이드바·헤더는 감춰 종이 폭을 온전히 사용.

## v0.231.0 - 2026-08-25

- **상단탭 전체 커버리지.** 사건기록·회의기록·행사기록·회의 보고서·AI 매뉴얼·제안함·채택예정·휴지통에 WorkTabs, 학기 관리에 SchoolTabs 추가 — 어느 화면으로 들어가도 상단탭이 유지되어 통합 메뉴 구조가 끊기지 않음(요청 1).
- **하원 체크표 스위치 전환.** 요일별로 여러 차에 등록된 학생(곽호율 19·20호 등)은 한 차에서 탑승 체크하면 다른 차의 뱃지가 자동으로 흐려짐(요청 2).
- **픽업·결석 사선 표시.** 픽업(분홍)·결석(빨강) 뱃지에 대각선 실선을 그어 "이 차 안 탄다"가 한눈에 보이도록(요청 3).

## v0.230.0 - 2026-08-25

- **업무 나머지 3종 완성.**
  - ⓑ 인박스 → 업무등록: 선생님요청 탭 각 항목에 [→업무등록] 버튼(제목에 반·요청 요약, 본문에 원문). 학부모 문의는 상세 창의 [+업무로 등록] 그대로.
  - ⓒ 보고서 시각화: 업무 보고서에 담당자별·부서별·일자별 완료 막대 차트 추가(기간·부서 필터 연동). 보고서에도 WorkTabs 고정.
  - ⓓ 기록 드라이브: `/records/drive` 신설 — 사건·회의·행사를 연→월 폴더 트리(건수 배지)로 탐색, 종류 필터 칩, 전체 기간 검색, 항목 클릭 시 내용 미리보기+원본 화면 이동. 업무 메뉴·기록 탭에 연결.

## v0.229.0 - 2026-08-25

- **하원 체크표 = 보내주신 명단 그대로.** 예전 초등부 명부 대조 + 중고등 허용목록 필터를 제거 — 명단 재세팅 이후에는 배정 테이블 자체가 확정 명단인데, 필터가 구분표기 이름(김재이(G2A)·이준서(중등)·에이바(일라이아나) 등)을 떨어뜨려 체크표가 명단과 달라 보이던 문제 수정.

## v0.228.0 - 2026-08-25

- **하원 셔틀명단 재세팅 SQL** (`2026_하원명단_재세팅.sql`): 전달받은 명단(1-1호~31호) 그대로 38개 노선·106명 배정. (요일)이름=그 요일만, 이름(요일x)=제외 규칙 반영, 요일별로 다른 차를 타는 학생(임지효·박준후·이예온·민송희·박지음·남가인·정서우·곽호율·이한범·김재이G3J)은 양쪽 노선에 해당 요일로 분리 등록. 이준서·이준우(중등) 형제는 4-2호(학원)·9호(집·기업은행) 양쪽 등록 + 당일 확인 노트, 7호 이준서(초등)는 별도. 고등부 학년(G9~G12+)도 명부 Grade 컬럼대로 채움(김에스더·이하은·이준서 9 / 정에린·노다은·Mohammed 10 / 장하영·정하담 11 / 한이준·김태훈 12 / 신혁·강윤영 12+).
- **하원 체크표 탭 분리.** [하원 체크표|하원 셔틀명단] 탭 신설 - 셔틀명단 탭에서 노선별 카드로 누가 무슨 요일에 타는지 보고, 요일 버튼(월~금)으로 바로 켜고 끄기·학생 추가/빼기(즉시 저장, 체크표·안내보드 반영). 요일제 학생은 "월수금만" 배지로 표시.

## v0.227.0 - 2026-08-25

- **학생 명부 전면 리셋 대응.** 재학생 = 명부의 초등 101명(반배정) + 중고등 36명(학년만)뿐이 되도록: 기존 재학생 전원 보관(inactive) 후 137명만 재학으로 연결하는 SQL(`2026_명부리셋_초등101_중고등36.sql`). 화면도 정리 — 학생 조회에서 보관·유치부는 아예 표시 안 함(전출예정은 퇴학·전출 탭), 학교 개요에서 유치부 카운트 제외, 시간표·교사 개요의 반 인원은 재학(active)만 계산.
- **채팅 메시지 호버 → "→업무" 버튼.** 메시지에 마우스를 올리면 바로 업무 등록 버튼이 보입니다(기존 클릭 팝업과 같은 AI 분석 흐름, 이미 업무로 연결된 메시지엔 표시 안 함).

## v0.226.0 - 2026-08-25

- **업무 보드 3존 커맨드센터 개편.** ① 통합 인박스: 학부모 문의·출결내역·출결알림·선생님요청을 필터 탭 하나의 패널로(미완료 건수 배지, 선생님요청 배너→탭 흡수). ② 업무 흐름판에 [내 업무만|전체] 토글 — 내/전체 업무목록 위젯 2개를 흡수해 같은 업무가 세 번 그려지던 중복 제거, 칸반이 오른쪽 세로 전체 사용. ③ 부서메모 고정핀: 부서 공유 메모를 채팅 위에 상시 고정(접기/펼치기, 자동 저장). 모바일 탭도 채팅/흐름판/인박스로 정리.
- **업무 개요 페이지 제거.** 업무 보드 자체가 관제탑이므로 별도 개요 대시보드·탭·메뉴 항목을 제거.

## v0.225.0 - 2026-08-25

- **업무를 "관제탑" 개요+탭 구조로 개편(②).** 업무 개요 대시보드 신설 — 오늘 확인·처리할 것(내게 배정된 업무·미답변 학부모 문의·선생님 행정실 요청·오늘 픽업·검토대기 제안·발행대기 채택)을 클릭 가능한 타일로, 업무 현황(예정·진행중·보류·오늘 완료)과 최근 기록(사건·회의·행사)을 한 화면에. 각 업무 화면 상단에 WorkTabs(개요·보드·보고서·기록·제안·보관) 고정.
- **대분류 통합(7→5).** 예전 [기록] 대분류(등록사건목록·사건·회의·행사·회의 보고서·AI 매뉴얼·제안·채택)를 [업무] 안으로 흡수. 업무 메뉴 하나만 띄워두고도 확인·처리가 되도록.

## v0.224.0 - 2026-08-25

- **학기 준비(⑧) 강화.** 지난 학기 돌아보기(잘된점/개선점/제안)를 화면에서 바로 편집·저장(terms 반영). 준비 과정 회의록을 클릭하면 전체 내용 펼쳐 보기. **AI 분석·타 학교 참고 제안** 신설 — 지난 회차 돌아보기·회의록을 근거로 종합 분석·강점·개선점·준비 체크리스트를 만들고, 웹 검색으로 타 학교(국제학교·마이크로스쿨 등) 참고 아이디어를 출처와 함께 제안.

## v0.223.0 - 2026-08-25

- **셔틀 개요 개편.** 맨 위 지도를 크게(전체 폭·높이 600px) 띄우고 아래 전체 노선 리스트는 감춰, 지도를 누르면 그 지역 노선이 옆에 뜨게. 노선 정보는 아래 통합 표 하나(호차·지역(구·동)·기사·차량·오늘/정원·막차·지연·GPS)로 관리.
- **학교 개요 학년·반·학생 위젯.** 반별 현황을 학년 위젯 → 반 위젯 → 학생 이름 뱃지의 3중 위젯 구조로 재구성(한눈에 학사 현황).
- **교사 화면 전면 개편(요청 3·4).**
  - 교사 개요를 상단탭 고정 대시보드로. 담임: 우리 반 개요(학년·반·교실·담임, 학생 명단 위젯, 우리 반 주간 시간표, 지금 수업). 과목 선생님: 내 주간 시간표·주간 시수·지금 무슨 시간(수업 중이면 과목·장소, 아니면 프랩/대기).
  - i18n: 교사 개요의 학기 라벨·요일·상태를 화면 언어(한/영)에 맞춰 표시.
  - 주간 리포트는 담임 선생님만 작성(내 담당과목 메뉴 제거, 과목 선생님은 내 시간표 개요로 이동).
  - 담임/과목 → 행정실 문의·도움요청 창구 신설. 남긴 글은 업무 대시보드 상단에 실시간으로 뜨고, 해당 반에 빨간 느낌표로 표시(확인/완료 처리). ※ 반영하려면 마이그레이션 모음 SQL 실행 필요.
  - 담임은 상단탭에서 우리 반 픽업 체크로 바로 이동.

## v0.222.0 - 2026-08-25

- **과목반(과목 선생님) 데이터 입력 (⑦-2).** 26-27 명부(교직원)의 특별교과 담당을 `wr_subjects`에 넣음 — 중국어(박은지), 한국사(조진형), 코딩(Eamonn), 중국어·ASD(조주은), ASD(Teneqha Ford·Sophia Shim·Celine·Anna), ASD 보조(Crystal Jung). 계정이 없는 선생님도 이름으로 적을 수 있게 `wr_subjects.teacher_name` 칸 추가(과목 관리 화면·학교 개요에서 이름 표시). ※ 반영하려면 마이그레이션 모음 SQL 실행 필요.

## v0.221.0 - 2026-08-25

- **반 관리: 담임 미배정 반 정리 (⑦-1).** 상단에 담임 미배정 반 개수, "미배정만 보기" 필터, "미배정 반 모두 삭제"(삭제 전 대상 목록을 확인창에 보여줌) 추가. 표에서 미배정 반은 빨간 배경·배지로 강조.

## v0.220.0 - 2026-08-25

- **학생 명부 관리 재구성 (⑥).** 재학/졸업/퇴학·전출/보류/기타 상태 탭으로 나눠 보고, 각 행 맨 앞에 학생 고유코드(학번)를 조회 전용으로 표시(편집 불가·이 코드로 관련 자료 추적). 행 오른쪽에서 학적 상태(재학·졸업·퇴학·전출·보류·보관)를 바로 바꿀 수 있습니다. 전체 폭 사용은 이전 단계에서 적용됨. (status 값 '졸업'/'퇴학'/'전출' 지원 추가)

## v0.219.0 - 2026-08-25

- **셔틀 실시간·지역을 개요로 통합, 별도 탭 제거.** 요청. 셔틀 개요 맨 위에 지역·노선 지도를 넣고, 노선 상세표에 이미 지역(호차·지역·기사·차량·오늘/정원)이 함께 나오도록 유지. "실시간·지역" 탭과 좌측 메뉴의 실시간 위치·지역별 현황 항목을 없애 개요 하나로 합쳤습니다.

## v0.218.0 - 2026-08-25

- **구글챗 조회 일시 오류(500/503/429) 자동 재시도.** 요청: 구글챗 내용을 못 읽음. 구글 서버가 종종 "Internal error. Retry later" 500을 돌려주는데, 폴러가 재시도 없이 그 라운드를 통째로 실패시키고 있었습니다. 짧은 지수 백오프로 최대 4번 재시도하도록 하고, 그래도 실패하면 다음 크론이 마지막으로 읽은 시각부터 이어받아 메시지를 놓치지 않습니다(400/401/403 등 재시도 무의미한 오류는 즉시 표시).

## v0.217.0 - 2026-08-25

- **업무 대시보드 상단에 학교 요약.** 요청: 지금 무슨 학기·학기말까지 남은 기간 + 초등부 재학생 수를 맨 위에 배지로 표시.
- **수업·시간표에 학생 수 배지.** 선택 부서의 학년별 학생 수 요약 + 시간표 그리드 각 반 헤더에 "N명" 배지.

## v0.216.0 - 2026-08-25

- **수업·시간표 메뉴 신설 (③).** [학교 > 수업·시간표]에 ①공간 사용 현황(식당·체육관·컴퓨터실·미술실 등 지금 사용 중/비어 있음·누가 쓰는지) ②전체 시간표(부서·요일 선택, 반×교시 그리드로 과목·선생님·교실) ③지금 수업 없는 선생님·선생님별 주간 시수 ④담임 배정·과목 관리 바로가기를 한 화면에 모았습니다.
- **개발자 오류 로그 복사 버튼 (②).** 개발자 메뉴 오류 로그에 "전체 복사"와 개별 "복사" 버튼 추가(스택 포함). 붙여넣어 바로 공유할 수 있습니다.
- **학기 라벨 업데이트 (①).** 현재 진행 학기를 "26-27 1학기"로 바꾸는 SQL 제공(supabase/manual/2026_학기라벨_26-27_1학기.sql). 학년도는 26-27처럼 두 해를 잇고 1·2·3학기 + 여름/겨울 캠프 구조입니다.

## v0.215.0 - 2026-08-25

- **학교 개요 대폭 보강 + 모든 학교 페이지에 탭바.** 요청: 개요가 숫자만 있어 휑함 + 다른 페이지에서도 탭 이동. 개요에 ①현재 학기(무슨 학기·기간·학기말 D-day) ②부서별 재학생(유치·초등·중고등) ③반별 현황 표(반·담임·학생수, 반을 누르면 학생 명단 펼침) ④선생님 수·과목별 담당 선생님 ⑤학년별 분포·학사일정을 넣어 한 화면에서 학교 전체가 파악되게 함. 학교 전 페이지(개요·학생·교직원·명부·반·과목·학사일정·당번·학기준비)에 상단 탭바를 붙여 어디서든 탭으로 이동.

## v0.214.0 - 2026-08-25

- **학생 조회 재구성 (⑤).** 재학/졸업/퇴학 탭으로 나누고, 상단에 검색(이름·학번·영문이름)과 현재 탭의 학년별 분포 막대를 배치. 명단은 넓은 카드 그리드(학번 표시)로 전체 폭을 활용. 학교 탭바도 붙였습니다.

## v0.213.0 - 2026-08-25

- **개요 대시보드 정보 보강(셔틀·학교).** 요청: 숫자만 있어 휑함 → 실제 정보를 더. 학교 개요에 "반별 현황"표(반·담임·학생수, 담임 미배정 빨강) 추가. 셔틀 개요에 "오늘 픽업·결석 학생 명단"과 "지속 특이사항 목록"을 추가해 공간을 정보로 채웠습니다.

## v0.212.0 - 2026-08-25

- **학교 개요 대시보드 + 탭 구조 신설 (①).** 셔틀과 같은 "개요 + 탭" 방식으로 학교 메뉴를 재편. 학교를 누르면 개요가 먼저 열리고, 상단 탭(개요·학생 조회·교직원·명부 관리·반·과목·학사일정·당번표·학기 준비)으로 이동. 개요에는 재학생·졸업·퇴학, 반·담임 미배정, 이번주 위클리 리포트 KPI와 학년별 재학생 분포(막대), 다가오는 학사일정을 시각화(숫자 카운트업·막대 애니메이션). 다음 단계에서 학생조회 재학/졸업/퇴학 탭·명부관리·반관리·학기준비를 순서대로 개선합니다.

## v0.211.0 - 2026-08-25

- **셔틀 노선·배정·배차표: 상단 지도 + 정류장 클릭 이동.** 요청 ⑬. 노선을 고르면 상단에 노선 지도가 항상 뜨고, 아래 배차표에서 정류장 주소(📍)를 누르면 지도가 그 정류장으로 이동·확대됩니다. 배차표/노선도 토글은 없애고 지도+표를 한 화면에 함께 보여줍니다(인쇄본은 표만).

## v0.210.0 - 2026-08-25

- **픽업 인박스 학습.** 요청 ⑩. 픽업으로 확정하거나 "픽업 아님"으로 넘길 때마다 발신자(어머니·채널)별로 그 판단을 누적 학습합니다. 다음부터는 늘 픽업이던 발신자의 픽업은 더 빨리 자동확정되고, 대개 픽업이 아니던 발신자의 픽업은 신뢰도를 낮춰 사람이 한 번 더 확인하도록 유도합니다(분류 자체는 바꾸지 않아 오분류를 만들지 않는 안전한 방식). (DB 반영 필요: 이번배포 마이그레이션모음 SQL)

## v0.209.0 - 2026-08-25

- **셔틀 월별 운행 리포트 자동화.** 요청 채택. [셔틀 > 기록·분석] 상단에 이번 달 노선별 운행일수·막차 평균·정시/지연·정시율·평균 지연을 자동 집계해 표로 보여줍니다(전체 정시율 포함). 정시 기준은 막차 평균 ±5분, GPS 도착기록이 쌓일수록 정확해집니다.

## v0.208.0 - 2026-08-25

- **셔틀 기사·차량 변경 이력 자동 기록.** 요청 채택. 노선의 기사·연락처·차량번호·동승 선생님이 바뀌면 트리거가 자동으로 스냅샷을 남깁니다. [셔틀 > 링크·기기·GPS] 화면 하단의 "🧾 기사·차량 변경 이력"에서 언제 누가 어떤 차로 운행했는지 시간순으로 확인할 수 있습니다. (DB 반영 필요: 이번배포 마이그레이션모음 SQL)

## v0.207.0 - 2026-08-25

- **셔틀 개요: 결석·픽업 정류장 스킵 + 막차 ETA 재계산.** 요청 채택. 오늘 그 정류장의 탑승자가 전원 픽업/결석이면 "오늘 건너뜀"으로 세고, 건너뛴 정류장 수만큼 막차 도착을 앞당긴 조정 ETA(→ 시각)를 노선 상세표에 표시합니다.

## v0.206.0 - 2026-08-25

- **셔틀 개요: 정원 대비 실탑승 경고 + 노선별 막차 평균·지연 + 노선 상세 요약표(하단 여백 채움).** 요청 ⑨ + 벤치마킹 채택(정원 경고·예상 소요·지연). 노선 카드에 오늘 인원/정원(초과 시 빨간 ⚠)과 막차 평균·오늘 지연(+분/−분)을 표시하고, 하단에 기사·차량·정원·막차·지연·GPS를 한눈에 보는 노선 상세표를 추가. 막차 평균은 정류장 GPS 도착기록 누적으로 날마다 정확해집니다.

## v0.205.0 - 2026-08-25

- **셔틀 링크·기기·GPS 통합 + 개요 요약.** 요청 ⑰. 내 폰 GPS 테스트를 링크·기기 화면 안(접이식)으로 합치고, 상단에 등록 기기·연결 중·미설치·안내보드/도착 링크 수를 한눈에 보는 개요 요약을 추가. 전체 폭으로 넓힘. (메뉴 2→1)

## v0.204.0 - 2026-08-25

- **셔틀 노선·배정·배차표 통합 + 설정 페이지 여백 제거.** 요청 ⑯⑮. 노선 관리·탑승 배정·배차표 인쇄가 사실상 같은 종합 화면(/shuttle)이라 하나로 합치고(메뉴 3→1, 탭 "노선·배정·배차표"), 이 설정 화면들을 전체 폭으로 넓혀 좌우 여백 낭비를 없앴습니다.

## v0.203.0 - 2026-08-25

- **셔틀 전 페이지에 고정 탭바(공용 레이아웃).** 이제 셔틀 어느 화면에 있어도 상단 탭(개요·체크표·실시간·지역·노선·배정·링크·기기·기록)이 항상 떠 있고, 탭을 누르면 탭바(셸)는 그대로 둔 채 아래 내용만 바뀝니다. 실시간·지역이 개요와 같은 셸의 탭으로 들어와 오가기 편해졌습니다. (앞으로 학교·업무 개요도 같은 방식)

## v0.202.0 - 2026-08-25

- **셔틀 개요 대시보드 신설 (새 구조 1단계).** 요청: 메뉴 여러 개를 "개요 + 탭"으로 통합, 여백을 시각화로 채우기. 셔틀 메뉴를 누르면 개요가 먼저 열리고 상단 탭(개요·체크표·실시간·지역·노선·배정·기록)으로 이동. 오늘 하원의 탑승예정(도넛)·픽업·결석·운행중 차량·지속 특이사항을 한 화면에 집계하고, 노선별 카드 그리드에 인원·GPS 상태(펄스 점)를 표시. 숫자 카운트업·도넛 채움·hover 리프트 등 은은한 모션 적용. (실데이터 연동)

## v0.201.0 - 2026-08-25

- **PC 와이드 레이아웃 1단계.** 데이터가 많은 넓은 페이지(체크표·표·대시보드 등, max-w-6xl/7xl)를 큰 화면(≥1280px)에서 더 넓게(최대 1344~1536px, 초광폭에서 최대 1760px) 펼칩니다. 폼·읽기용 좁은 페이지는 가독성을 위해 그대로 두고, 인쇄본도 영향 없습니다. (카드·표·여백 통일과 노선관리 2단 재구성은 다음 단계)

## v0.200.0 - 2026-08-25

- **셔틀 하위메뉴 3그룹으로 분류.** ① 매일 하원 운영(하원 체크표·픽업 인박스·실시간 위치·지역별 현황) ② 기준정보·설정(노선 관리·탑승 배정·배차표·링크/기기·GPS 테스트) ③ 기록·분석(정류장 도착시간)으로 소제목·구분선을 넣어 정리(기능 삭제 없음).

## v0.199.0 - 2026-08-25

- **하원 체크표: 2호 김사랑(유치부) 숨김.** 초등부 동명이인 때문에 이름 대조로 잘못 딸려오던 유치부 김사랑을 콕 집어 제외.
- **중등부로 진급했지만 하원 셔틀은 계속 타는 7명(박준후·문수민·이도후·곽호율·박지음·강여명·정서안) 명단에 표시.** 요청대로 숨기지 않습니다.
- **오늘 안 타는 아이를 더 흐리게(회색조·투명도↑).** 오늘 타는 아이와 확실히 구분되도록.

## v0.198.0 - 2026-08-25

- **하원 체크표: 지속 특이사항 창구.** 요청: 왼쪽에 "이라엘 수요일 수영학원", "4호 김재이 당분간 개별하원(셔틀 안 탐)" 같은 지속 반영사항을 적는 창구를 만들고, 적으면 오른쪽 상단에 요약으로 계속 띄우며 셔틀도 자동 반영. 효과는 세 가지 - 메모만 / 특정 요일 셔틀 제외(예: 수영학원 가는 요일) / 개별하원(셔틀 전면 제외). 오른쪽 요약의 ⋯ → [삭제하고 원래 셔틀로]를 누르면 원래 배정으로 복귀합니다(파괴적 수정이 아니라 덧씌우기라 안전).
- **하원 체크표: 요일별 다른 셔틀 타는 학생을 같은 색 테두리로 묶음.** 요청: 요일마다 다른 차를 타는 아이(예: 임지효 4-2호 화수목 / 10호 월금)를 같은 색으로 묶고, 오늘 타는 셔틀은 선명하게·안 타는 날 셔틀은 옅게 전부 보이도록(갑자기 다른 날 셔틀을 태워달라는 요청에 대비). 실시간 반영(다른 직원이 적어도 즉시 보임).

## v0.197.0 - 2026-08-24

- **토들 수집기 "로그인 필요" 오탐 자동 복구 (수집기 v2.4.0).** 밤새 크롬이 탭을 잠재웠다 깨울 때 생기는 가짜 "로그인 필요"를, 토들 탭을 자동으로 한 번 새로고침하고 다시 시도해 스스로 복구합니다. 그래도 안 되면 진짜 만료이므로 "새로고침(F5) 후 다시 로그인" 안내를 더 분명히 띄웁니다. (PC에서 [업데이트] 실행 필요)

## v0.196.0 - 2026-08-24

- **"not here"(아이가 아직 안 왔다는 담임 문의)를 결석으로 처리하지 않습니다.** 요청대로 결석 판단 문구가 아니라 그냥 넘어갑니다(행정실이 학부모께 등원/지각/결석을 확인). 확실한 결석 표현(absent / not coming 등)만 결석으로 잡습니다.

## v0.195.0 - 2026-08-24

- **하원 체크표: 초등부 명부 + 알려주신 중고등부 탑승자 16명만** 표시(요청 정정). 그 외(유치부 등)는 숨깁니다.

## v0.194.0 - 2026-08-24

- **정류장 도착 시간 기록·평균** 화면을 새로 만들었습니다(셔틀 → 정류장 도착시간). 노선별로 각 정류장의 **평균 도착시각·오늘 도착·관측 일수**를 보여주고, 정류장을 누르면 **지도 위치와 지난 도착 기록**을 봅니다. 마지막 정류장 평균 도착시각도 함께 표시합니다.
- **하원 체크표를 우리 명부(초등부)만** 보이게 했습니다. 요청대로 유치부·중고등부 등 명부에 없는 이름은 전부 숨깁니다(배정의 학생 부서로 걸러냄).
- (자료) **27호_하원시간대만.sql** — 테스트라도 하원 시간대만 추적하도록 24시간 추적을 끄는 SQL(요청 변경). 관리자 화면의 [24h 테스트]를 꺼도 동일합니다.

## v0.193.0 - 2026-08-24

- **하원 체크표를 정규학기 기본으로.** 여름캠프2가 끝나 기본 학기를 정규학기로 바꿔 정규학기 명단이 바로 뜹니다.
- **하원 체크표에 토들 픽업·결석 자동 반영.** 요청대로 오늘 토들·전화·구글챗으로 들어온 픽업/결석을 명단에 자동으로 얹습니다(사람이 직접 누른 값이 우선).
- **안 타는 아이도 옅은 회색으로 표시 + 눌러서 탑승.** 오늘 안 타는 학생은 회색으로 흐리게 보이고, 🚌 탑승 버튼으로 오늘 탑승 처리할 수 있습니다.
- **하원 시작~다음날 아침에는 시간표 자리에 학교 정보·학사일정.** 요청대로 저녁엔 시간표 대신 오늘 학교 요약(재적·결석·지각·픽업·이번주 리포트)과 이번 달 **학사일정 달력**·다가오는 일정을 보여줍니다.
- **하원 지도 오른쪽을 위아래 2칸 순환으로** 바꿨습니다. 지금은 27호만 있어 한 칸에 27호가 뜹니다.
- (자료) **27호_테스트_자동추적.sql** — 테스트 동안 27호를 온오프 없이 자동 추적(always_on)으로 두는 SQL. 관리자 화면의 [24h 테스트] 토글과 동일합니다.

## v0.192.0 - 2026-08-24

- **기사님이 직접 켜고/끄는 방식으로 안내를 바꿨습니다.** 설정 페이지·안내를 "운행 전 켜고, 끝나면 끄기(운행 종료)"로 바꿔 개인정보 부담을 줄였습니다. 꺼두면 위치가 전혀 전송되지 않습니다.
- **GPS 미시작 경고.** 평일 15:50 이후 GPS 기기가 있는데 신호가 없는 노선을 하원 화면 상단에 빨간 배너로 띄워, 사무실에서 기사님께 전화로 안내할 수 있습니다.
- **하원 지도 오른쪽을 2×2(4개) 노선 순환으로.** 요청대로 오른쪽에 4개 지도를 띄워 여러 호차를 겹치지 않게 순환하며, 각 지도는 **현재 위치 주변을 도로가 보일 만큼 확대**하고 예상 경로·지나온 자취를 함께 보여줍니다.
- **전체화면 강제·안내창을 없앴습니다.** 요청대로 앱이 스스로 전체화면을 켜지 않습니다(브라우저 전체화면으로 쓰시면 됩니다). 하원 종료 버튼은 그대로입니다.
- **일정 시간 안 움직이면 마우스 커서가 사라집니다**(하원 운행·업무 대시보드 모두). 움직이면 다시 나타납니다.

## v0.191.0 - 2026-08-24

- **하원 지도를 좌우 7:3으로 나눴습니다.** 요청대로 **왼쪽은 전체 지도**(모든 노선), **오른쪽은 노선을 하나씩 순환하며 가까이** 보여줍니다(7초마다 전환). 지금은 27호만 신호가 있어 오른쪽에 27호가 뜹니다.
- **전체 지도(왼쪽)의 차량은 번호가 든 작은 점**으로 표시하고, **지도가 넓게 축소될수록 점도 작아집니다**.
- **오른쪽(순환) 화면은 위에서 내려다본 셔틀 밴**으로, 진행 방향·예상 경로·지나온 자취·정류장을 그 노선에 딱 맞게 확대해 보여줍니다.

## v0.190.0 - 2026-08-24

- **셔틀 밴을 "위에서 본" 모양으로 바꾸고 진행 방향을 표시합니다.** 위에서 본 밴이 실제 가는 방향으로 회전하고, 번호는 항상 똑바로 읽히게 올립니다.
- **밴 색을 노선 색으로 통일했습니다.** 노선 목록(대체로 지역 묶음) 순서대로 색상환을 고르게 나눠, 밴·예상경로·지나온 자취가 모두 같은 노선 색으로 이어집니다.
- **예상 경로도 실선으로 함께 그립니다.** 예상(계획) 경로는 연한 실선, 지나온 길은 진한 실선. 전체 노선이 보이도록 예상 경로까지 포함해 지도를 자동 축소합니다.

## v0.189.0 - 2026-08-24

- **지도 위 차량을 노란 셔틀 밴 그림으로 바꿨습니다.** "27호" 글자 대신 노란 밴 안에 번호(27)만 넣어, 실제 셔틀이 노선을 따라가는 느낌으로 표시합니다(노선 색은 밴 테두리로 구분).

## v0.188.0 - 2026-08-24

- **업무 대시보드 지도에 전체 노선의 실제 이동 자취를 각 노선 색 실선으로 그립니다.** 계획 경로는 연한 점선(참고), 오늘 실제 지나온 길은 노선 색 실선입니다. 지금은 27호만 신호가 있어 27호만 실선으로 이어집니다. 지도는 자취를 포함해 노선 전체가 보이게 맞춰집니다.

## v0.187.0 - 2026-08-24

- **정류장은 진짜 근처일 때만 인식합니다.** 정류장 도착 반경을 120m→80m로 좁혀, GPS가 흔들리거나 그냥 서 있을 때 엉뚱한 정류장으로 잡히지 않게 했습니다.
- **GPS 테스트 지도를 노선 트래킹용으로 새로 그렸습니다.**
  - **GIA(학교) → 지나온 길을 파란 실선**으로 그립니다. 계획 경로는 연한 회색 점선, 정류장은 점으로 함께 표시.
  - 지도는 **차량을 따라다니지 않고**, 노선 전체가 한눈에 들어오게 **자동으로 확대**됩니다(직접 확대·이동해도 되돌리지 않음. "노선 전체 다시 맞추기" 버튼으로 재정렬).
  - 지금 신호를 보내는 노선을 자동으로 골라 보여줍니다(우선 27호 테스트).

## v0.186.0 - 2026-08-24

- **기사님 위치가 안 들어오던 원인(307)을 고쳤습니다.** 27호차 앱 로그의 `Upload response 307`은 위치 전송 주소(/api/shuttle/track)가 로그인 검사에 걸려 /login으로 튕기던 것이었습니다. 이 주소를 로그인 없이 받도록 열어, 이제 앱이 보낸 위치가 정상 저장됩니다.
- **셔틀 기기 관리에 '앱 신호 진단'을 넣었습니다.** 앱이 서버로 보낸 마지막 요청을 그대로 보여줍니다 — "위치 저장됨 ✓" / "지금은 하원 시간대가 아니라 미저장" / "좌표 없음(권한 확인)" / "앱 신호 없음(설치·기기ID 확인)".
- **[24h 테스트] 토글**을 추가했습니다. 하원 시간대(평일 15:30~18:30)가 아니어도 지금 바로 위치가 저장되는지 확인할 수 있고, 테스트 후 끄면 평소대로 돌아갑니다.

## v0.185.0 - 2026-08-24

- **내 휴대폰 GPS 테스트 화면이 생겼습니다.**
  - 셔틀 메뉴 → **GPS 테스트(내 폰)** 에서 설치 QR·링크, **실시간 위치**, **오늘 이동 경로·히스토리**를 한 화면에서 봅니다. 다음 날 경로가 정확한지도 이 화면에서 확인합니다.
  - 이 기기(강경원)는 **24시간 기록(always_on)** 이라 하원 시간대가 아니어도 출퇴근 위치가 그대로 기록됩니다. 실제 노선과 분리된 숨김 기기라 정규 운영 화면엔 영향이 없습니다.
  - 4시 하원 **업무 대시보드 지도에도 내 위치가 초록 📍로 실시간** 표시됩니다.
- **기준점을 GIA마이크로랩(서울 강남구 논현로131길 45)으로 고정**했습니다. 도착·출발 감지가 모두 이 좌표 반경으로 계산됩니다(함께 드리는 세팅 SQL에서 재지오코딩).
- (세팅 SQL 갱신) **여름캠프2 잔여 노선 비활성화 + 기준점 재설정**을 정규학기 세팅 SQL에 추가했습니다.

## v0.184.0 - 2026-08-24

- **출결 영문 이름 대조를 크게 개선했습니다.**
  - 이제 **이름(first name)만 적어도** 명부의 영문명과 대조됩니다 — "Benecia will be absent" → 김사랑, "seojun in G2A is not here" → 이서준. 같은 이름이 여럿이면 문장의 학년(G2A 등)으로 좁힙니다.
  - "is not here / not coming" 같은 표현도 결석으로 인식합니다.
- **답글은 긁어오지 않습니다.** he/she/they로 시작하는 **대명사 답글**(이름 없는 답변)은 새 결석·지각으로 집계하지 않습니다.
- **업무 대시보드 출결을 결석/지각으로 반 나눴습니다.** 요청대로 **결석은 빨강, 지각은 주황**으로 좌우로 갈라 한눈에 구분되고, **하원 픽업은 파란색으로 맨 앞**에 놓았습니다(조퇴는 지각 쪽에 보라색으로 함께).

## v0.183.0 - 2026-08-24

- **하원 GPS: 정류장별 도착·하차가 운영 대시보드에 뜹니다.**
  - 기사님 휴대폰 GPS가 정류장 반경(120m)에 들어오면 그 정류장을 **도착으로 자동 기록**합니다(하루 한 정류장 한 번). 정류장 좌표는 GPS 학습값이 있으면 그것, 없으면 지오코딩 좌표를 씁니다.
  - 4시 하원 운영화면 아래에 **🛰️ 운행 상황(GPS)** 칸이 새로 생겨, 운행 중인 차마다 **방금 닿은 정류장·그 정류장 하차 명단·다음 정류장·진행(몇/몇 정류장)** 을 한 줄로 보여줍니다.
  - 정류장 좌표는 매일 운행할수록 정확해집니다(기존 GPS 정차 학습 크론이 관측값 평균을 매번 다시 계산). 좌표가 아직 없는 정류장도 며칠 운행하면 자동으로 채워집니다.

## v0.182.0 - 2026-08-24

- **교사 첫 화면에 "우리 반 현황" 대시보드가 생겼습니다.**
  - 담임 선생님이 로그인하면 위클리 리포트가 아니라 **우리 반 현황**이 먼저 뜹니다(과목 전담 교사는 예전대로 위클리 리포트).
  - **오늘 픽업**: 우리 반 학생 중 오늘 픽업 요청이 온 아이를, **시각이 적혀 있으면 그 시각과 함께** 큼직하게 보여줍니다("누가 몇 시에").
  - **우리 반 문의**: 담임 이메일로 걸러 우리 반 학부모 문의만, 미답변·급한 것을 위로 보여줍니다. 문의를 누르면 원문을 봅니다.
  - 담임 이메일로만 좁혀 다른 반 문의는 보이지 않습니다.
- **데모 계정으로 미리 볼 수 있습니다.** 오리엔테이션 데모 교사(gia-demo)에 예시 픽업 2건·문의 3건을 넣었습니다. 데모 문의는 `is_demo`로 격리되어 실제 행정실 문의 목록·운영 대시보드에는 전혀 섞이지 않습니다.

## v0.181.0 - 2026-08-24

- **모바일 도착체크에 GPS 상태가 보입니다.**
  - 기사님 휴대폰 GPS가 살아있으면 카드가 **초록(운행중)**, 학교 근처에 도착하면 **주황(도착함·GPS 감지)**, 멀어지면 **회색(출발함)** 으로 자동으로 바뀝니다. 자동으로 안 바뀌어도 눌러서 직접 표시할 수 있습니다(기존 방식 유지).
  - 카드 맨 아래 **📍 표시**로 GPS가 방금 신호를 보냈는지(초록) 끊겼는지(회색)를 눈으로 확인합니다. 기기 미설정이면 "GPS 미설정".
  - 탑승 명단·픽업·결석 표시는 그대로 유지됩니다.

- (함께 드리는 세팅 자료) **2026_정규1학기_셔틀세팅.sql** — 안내보드·도착체크를 정규학기로 전환하고, 48개 노선에 기사님 GPS 기기·설정링크를 발급합니다. **2026_기사님_GPS설정링크.xlsx** — 노선별로 기사님께 보낼 /s/링크 목록.

## v0.180.0 - 2026-08-24

- **운영 대시보드에 결석·지각·픽업이 제대로 집계됩니다.**
  - 원인: 결석 칸은 출결기록·구글챗만, 픽업 칸은 하원 체크표만 읽고 있었습니다. 토들로 들어온 학부모 연락은 문의(pickup_requests)에만 쌓여 두 숫자 어디에도 반영되지 않았습니다.
  - 이제 문의로 들어온 연락도 같은 규칙(키워드 분류 + 명부 대조)으로 함께 집계합니다. AI가 이미 명부와 이어둔 학생이 있으면 그걸 최우선으로 쓰고, 없으면 본문을 명부와 대조합니다. "내일·금요일 결석"처럼 날짜가 적힌 건 그날에 반영되고, 오늘 것만 대시보드에 뜹니다.
  - 하원 체크표 픽업과 문의 픽업이 겹치면 한 번만 셉니다(같은 이름 중복 제거).

- **영어로 온 이름을 더 정확히 한글로 바꿉니다.**
  - 'Soo J'처럼 잘려 들어온 이름이 명부의 여러 명(예: 'Soo Ji'=지수, 'Soo Jin'=수진)과 겹칠 때, '가장 가깝게 완성되는' 한 명을 골라 확정합니다('Soo J'는 지수). 단, 후보가 크게 벌어지는 한 단어(예: 'Min')만 온 경우는 잘못 고르지 않도록 그대로 둡니다.

## v0.179.0 - 2026-08-24

- **이름을 성까지 표시합니다.**
  - 하원 픽업 목록이 탑승표에 적힌 이름(성이 빠졌을 수 있음) 대신 **학생 번호로 명부의 전체 이름(성+이름)** 을 씁니다. 학부모 문의·출결은 이미 명부 전체 이름을 쓰고 있었습니다.
  - 혹시 아직 성이 안 나오는 화면이 있으면 알려주세요 - 그 자리도 같은 방식으로 명부와 잇겠습니다.

- **시간표: 지금 수업하는 선생님만 표시.** 요청대로 담임 폴백을 빼고, 지금 교시의 담당 선생님만 과목 아래에 적습니다.
- **시간표 아래 "지금 수업 없는 선생님" 로스터를 없앴습니다**(요청: 시간표와 겹쳐 보였습니다).



- **운영 대시보드 배치 조정.** 출결·픽업·오늘 업무 칸을 조금 줄이고, 위쪽(교실·학부모 문의)을 키웠습니다. 학부모 문의가 여섯 개까지 한 장에 들어갑니다.

- **수업 담당 선생님을 시간표에 표시.**
  - 각 반 과목 아래에 **담당 선생님**을 작게 적습니다. 수업이 없으면 **담임** 이름을 보여줍니다.
  - **지금 수업 없는 선생님**은 시간표 아래에 로스터로 모아 보여줍니다. 스페셜티(전담) 선생님도 시간표에 나오면 함께 잡힙니다 - 지금 손이 비는 분을 한눈에 봅니다.

- **직원 답글이 해결인지 진행중인지 판단합니다.**(수집기 v2.3.0).
  - 지금까지는 직원이 답글만 달면 무조건 처리 완료로 넘겼습니다. 그런데 "확인 후 연락드리겠습니다" 같은 답은 아직 안 끝난 것입니다.
  - 이제 답글 내용을 보고 **해결이면 목록에서 빼고, 진행중이면 [답변중]** 으로 남겨 계속 눈에 띄게 합니다. 판단이 애매하면 답이 달렸으니 해결로 봅니다.



- **업무 대시보드 터치 조작을 넓혔습니다.**
  - **학부모 문의를 짧게 누르면** 그 토들 원문이 작은 창으로 뜹니다. 토들에 다시 접속하지 않아도 내용을 바로 봅니다.
  - **길게(0.6초) 누르면** 목록에서 없앱니다(처리 완료). 지우지 않고 기록으로 남겨, 잘못 눌러도 [기록]에서 되살릴 수 있습니다. 로그인 없는 대시보드라 되돌릴 수 없는 삭제는 막았습니다.
  - **각 반을 누르면 일주일 시간표**가 팝업으로 뜹니다. 교시 × 요일(월~금) 표에 과목·교실이 채워집니다.
  - 토큰 인증 API 두 개를 새로 뒀습니다(문의 처리 / 주간 시간표). 대시보드 링크가 살아 있을 때만 동작합니다.



- **학부모 문의가 더 이상 잘리지 않습니다.** 한 장에 담는 개수를 고정값으로 정하니 이름·요약 길이에 따라 마지막 줄("백서아" 아래)이 잘렸습니다. 이제 **칸 높이를 실제로 재서** 딱 들어맞는 개수를 스스로 찾습니다. 넘치면 한 건씩 줄이고, 나머지는 다음 장으로 넘깁니다. 글자도 조금 키웠습니다.

- **시간표: 4교시 등 과목 글자를 키우고, 학년 높이를 맞췄습니다.** 반 개수가 학년마다 달라도 각 학년 행이 높이를 똑같이 나눠 갖고, 그만큼 생긴 공간으로 과목명을 크게 띄웁니다.

- **업무 대시보드를 터치로 조작할 수 있습니다.**
  - 학부모 문의를 누르면 **토들 원문으로 바로** 갑니다.
  - 화면 아래 점을 누르면 그 장으로 넘어갑니다. 문의를 한 번 누르면 자동 넘김이 20초간 멈춰, 읽는 중에 장이 넘어가지 않습니다.
  - 터치스크린에서 탭이 바로 먹도록 처리했습니다(더블탭 확대 지연 제거).



- **학부모 문의 이름을 한글로 바꿉니다.**
  - `Diane & Sunwoo Lim` → **임다이앤 & 임선우** 처럼, 형제방 이름도 각각 대조해 한글로 이어 붙입니다.
  - `Soo J` 처럼 잘려 온 이름은, 영문명이 그 값으로 시작하는 학생이 **딱 한 명일 때만** 바꿉니다(`Soo J` → 김수진). 여럿이면 누구인지 알 수 없으니 그대로 둡니다 - 엉뚱한 학생으로 바꾸는 게 영어로 두는 것보다 나쁩니다.
  - 이미 한글이거나 명부에서 못 찾으면 원래 값을 그대로 둡니다. 업무 화면과 운영 대시보드 양쪽에 적용됩니다. 실제 이름 형태로 검증했습니다.

- **대시보드 출결에 영어 이름도 반영됩니다.** 앞서 구글챗 출결을 붙였는데, 명부 대조에 **영문명을 빼먹어** 영어로 온 출결(예: "Diane Lim 결석")이 대시보드에서만 안 잡혔습니다. 업무 화면 출결내역과 똑같이 영문명까지 넣어 대조합니다.



- **운영 대시보드 학부모 문의 개선.**
  - **최근 문의가 맨 위로** 올라옵니다. 예전에는 급한 것 우선이라 최신 건이 아래로 밀리기도 했는데, 급한 건은 빨간 테두리로 이미 구분되므로 순서는 순수 최신순으로 뒀습니다.
  - **새로 올라온 문의는 강조**합니다. 3분 이내에 들어온 것은 배경을 밝히고 파란 테두리에 **NEW** 배지를 붙입니다. 3분이 지나면 강조가 저절로 풀립니다 - 계속 강조돼 있으면 어느 게 진짜 새 것인지 알 수 없습니다.
  - 한 장에 담는 건수를 줄여(6건) 두 줄 요약도 잘리지 않게 했습니다. 넘치면 8초마다 다음 장으로 넘어갑니다(스크롤 없음).

- **출결·지각이 대시보드에 안 뜨던 문제를 고쳤습니다.**
  - 원인: 대시보드는 교사가 출결 화면에서 **직접 입력한 것(attendance_records)만** 읽고 있었습니다. 구글챗 출결알림은 그 표에 들어가지 않아 화면에 뜨지 않았습니다.
  - 이제 구글챗 출결알림을 **업무 화면의 출결내역과 같은 규칙으로** 파싱해 함께 올립니다(결석·지각·조퇴). 오늘 것만, 이 부서 학생과 대조되는 것만 올리고, 교사가 이미 직접 입력한 학생은 그 값을 존중합니다.
  - 대시보드는 15초마다 갱신되므로, 구글챗에 알림이 오면 늦어도 15초 안에 반영됩니다.



- **소리 알림을 뺐습니다**. 관련된 코드와 [🔔 소리 켜기] 버튼을 전부 걷어냈습니다. 화면이 죽었던 원인이 여기였고, 브라우저 규칙 때문에 사람이 한 번 눌러줘야 하는 제약도 있어 공용 모니터에는 잘 맞지 않았습니다.
- 나머지는 그대로입니다 - 새 문의는 15초 안에 화면에 뜨고, 급한 것이 위로 올라오며, 여러 장이면 8초마다 넘어갑니다.



- **긴급 수정: 대시보드가 React 오류(#310)로 죽던 문제.** 제 잘못입니다.
  - 소리 알림을 넣으면서 관련 코드를 **"데이터가 없으면 여기서 멈춤" 아래에** 뒀습니다. 그러면 데이터를 기다리는 첫 순간과 데이터가 온 뒤의 상태 개수가 달라져 리액트가 화면을 통째로 포기합니다.
  - 소리 관련 부분을 전부 위로 올렸습니다. 화면은 그대로입니다.
  - 눈으로 다시 보는 대신 **기계로 확인했습니다** - 화면 파일 8개에서 "멈춤 아래에 있는 상태 선언"을 훑어 하나도 없음을 확인했고, 린트로도 확인했습니다. 같은 실수를 눈으로 잡으려다 놓친 것이라 같은 방법을 반복하지 않았습니다.



- **긴급 수정: 업무 대시보드 오류.** 제 잘못입니다.
  - 새 기능을 올리면 **코드가 먼저 배포되고 마이그레이션은 조금 뒤에** 걸립니다. 그 사이에 아직 없는 칸(`merged_sources`, `answered_via` 등)을 콕 집어 달라고 하면 **조회 자체가 실패해 화면이 통째로 깨집니다.**
  - 칸을 하나씩 적는 대신 전부 가져오도록 바꿨습니다. 있는 것만 돌아오고, 없는 칸은 비어 있는 채로 화면은 그대로 뜹니다.
  - 학부모 문의 목록, 운영 대시보드, 중복 판정 세 곳을 모두 고쳤습니다.
  - 새 칸을 쓸 때는 앞으로도 이 방식을 씁니다. 배포와 마이그레이션 사이의 틈은 늘 생기고, 그때 화면이 죽으면 안 됩니다.



- **[관리자 → 데이터베이스 점검]** 화면을 만들었습니다.
  - 지금까지는 **화면이 깨져야** 알 수 있었고, 그것도 "어느 기능이 안 되는지"만 알 뿐 왜 안 되는지는 짐작해야 했습니다.
  - 이제 기능별로 초록/빨강이 나옵니다. 빨간 줄에는 **없는 칸 이름 · 그래서 무엇이 안 되는지 · 실행해야 할 마이그레이션 파일**이 함께 적힙니다.
  - 확인 방식은 `information_schema`를 뒤지는 대신 **실제로 그 칸을 읽어봅니다.** 권한(RLS)까지 함께 확인되기 때문입니다 - 칸이 있어도 읽을 수 없으면 기능은 똑같이 안 됩니다. "분명히 있는데 왜 안 되지?"를 없애려면 앱과 같은 방식으로 확인해야 합니다.
  - 들어가면 바로 한 번 확인합니다. 버튼을 또 눌러야 결과가 나오면 확인하러 온 보람이 없습니다.
  - 앞으로 새 마이그레이션을 만들 때 `schemaChecks.ts`에 한 줄만 추가하면 그 기능도 자동으로 점검됩니다.



- **운영 대시보드 학부모 문의를 크게, 그리고 넘겨가며 보여줍니다.**
  - 이름을 크게 올리고 **그 아래 요약 한 줄**을 둡니다. 글자 크기를 전반적으로 키웠습니다.
  - 한 화면에 안 들어가면 **8초마다 다음 장으로 넘겼다가 마지막에서 처음으로 돌아옵니다.** 공용 모니터라 스크롤을 내릴 사람이 없어서, 지나가며 몇 초만 봐도 결국 전부 보게 했습니다. 아래 점으로 몇 번째 장인지 표시합니다.

- **문의를 체크로 넘길 수 있습니다.** 업무 화면 문의 목록에 체크박스가 생겼습니다.
  - 체크하면 대시보드와 문의 목록에서 빠집니다. **지우지 않고 기록으로 남깁니다** - 지난 문의 내용을 나중에 다시 찾을 수 있어야 하고, 같은 문의가 반복되면 그것 자체가 고쳐야 할 신호입니다.
  - **[기록]** 을 누르면 처리한 것까지 함께 보이고, **검색창**으로 학생 이름·내용을 찾을 수 있습니다. 검색이 빠르도록 색인도 걸었습니다.

- **다른 선생님이 답글을 달면 자동으로 처리됩니다**(수집기 v2.2.0).
  - 수집기가 각 방의 마지막 글이 선생님 것인지 확인해 알려줍니다. 이미 답한 건이 인박스에 남아 있으면 또 답하게 되고, 학부모는 같은 얘기를 두 번 듣게 됩니다.
  - 답글로 처리된 건은 **이름 뒤에 초록 체크(✓)** 가 붙습니다. 직원이 직접 체크한 것과 구분됩니다.
  - 답글보다 나중에 들어온 문의는 건드리지 않습니다 - 아직 답을 못 받은 것입니다.



- **토들과 구글챗으로 같은 일이 두 번 들어오던 것을 하나로 묶습니다.**
  - 쓴 사람이 다르니 문장이 전혀 다릅니다. 학부모는 `"선생님 안녕하세요. 유겸이가 어제부터 장염이라 오늘 결석하겠습니다"`, 담임은 `"@노유겸 결석 - 장염"`. 글자로 겹치는 건 몇 자뿐입니다.
  - 그래서 세 단계로 좁힙니다.
    1. **같은 학생·같은 날짜·같은 종류**인 것만 후보로 봅니다. 학생을 특정하지 못한 건은 아예 묶지 않습니다.
    2. **픽업은 후보가 있으면 같은 건**입니다. 같은 아이를 같은 날 두 번 데려갈 일은 없습니다.
    3. **문의는 글자가 겹치는 정도**를 보고, 애매한 구간(0.2~0.6)만 AI에게 한 번 더 묻습니다.
  - 겹침을 잴 때 **짧은 쪽을 기준**으로 삼습니다. 긴 인사말이 붙었다고 다른 일이 되는 건 아니어서, 흔히 쓰는 방식(자카드)으로는 같은 건을 놓칩니다.
  - **뜻이 어긋나면 무조건 따로 둡니다** - "결석"과 "지각"은 짧고 대부분 겹쳐서 닮은 정도만으로는 못 가립니다.
  - 애매하면 묶지 않습니다. **잘못 묶으면 한쪽 내용이 통째로 사라집니다.** 두 줄로 남는 편이 낫습니다.
  - 묶인 건에는 **`+구글챗` 같은 표시**가 붙습니다. "구글챗에도 올렸는데 왜 여기 없지?" 하고 찾게 되는 것을 막습니다.
  - 실제 문장 쌍 6가지로 판정을 확인했습니다.



- **GIA 로고**가 운영 대시보드 맨 위 가운데에 들어갔습니다. 왼쪽 글자 길이에 따라 로고가 흔들리지 않도록 세 칸 격자로 두어 늘 정확히 가운데에 옵니다.

- **점심시간을 눈으로 봅니다.**
  - 점심시간이 되면 시간표 자리에 **큰 고리**가 뜨고 시간이 갈수록 줄어듭니다. 이 시간에 반별 과목은 볼 것이 없고, 정작 궁금한 것은 "얼마나 남았나" 하나입니다.
  - **끝나갈수록 색이 바뀝니다** - 넉넉할 때 초록, 10분 남으면 노랑, 3분 남으면 빨강. 정리할 때가 됐다는 것을 색만 보고도 압니다.
  - 숫자는 고리 안에 작게만 뒀습니다. 숫자를 크게 하면 결국 숫자를 읽게 되고, 그러면 굳이 이렇게 띄울 이유가 없습니다.
  - 점심 여부는 **시간표에 등록된 교시 이름**으로 봅니다(점심·중식·Lunch). 시각을 코드에 박아두면 학기마다 시간이 바뀔 때 아무도 고칠 생각을 못 합니다.

- **새 문의가 오면 소리로 알립니다.** 짧은 "딩-동" 음을 브라우저가 직접 냅니다.
  - 브라우저는 사람이 한 번이라도 누르기 전에는 소리를 못 내게 막습니다. 그래서 화면에 **[🔔 소리 켜기]** 버튼을 뒀습니다. 지나가는 분이 한 번만 눌러주면 그 뒤로는 계속 울리고, 버튼은 사라집니다.
  - 화면을 처음 열 때는 울리지 않습니다 - 켜자마자 밀린 문의로 울리면 놀라기만 합니다.



- **운영 대시보드 화면 배치를 다시 짰습니다.** 공용 모니터에 맞춘 것들입니다.
  - **위쪽은 절반씩** - 왼쪽 교실 상황, 오른쪽 학부모 문의. 하루 중 가장 자주 보는 둘입니다. 문의는 한 번에 16건까지 올라옵니다.
  - **출결 · 픽업 · 오늘 업무는 아래로** 내려 나란히 놓았습니다.
  - **어디에서도 스크롤되지 않습니다.** 대신 각 칸에서 보여줄 개수를 미리 줄여 애초에 넘치지 않게 했습니다 - 아래에 뭔가 더 있는데 아무도 못 보는 것보다, 중요한 것부터 화면 안에 들어오게 하는 편이 낫습니다. 순서는 이미 급한 것이 위로 오게 되어 있습니다.
  - **반 칸을 키웠습니다.** 칸 너비를 넓히고 글자를 키웠으며, 긴 과목명은 자르지 않고 **두 줄까지 내려 씁니다.** `Comput…`으로 잘리면 무슨 수업인지 알 수 없습니다.
  - **오늘 업무는 오늘 것만.** 마감이 지난 것이 위에 쌓여 오늘 할 일을 가리던 것을 뺐습니다.



- **운영 대시보드가 새 버전을 스스로 알아채고 새로고침합니다.**
  - 이 화면은 며칠씩 켜둔 채로 둡니다. 그동안 새 버전을 배포해도 브라우저는 처음 받아둔 코드를 계속 써서, **고친 것이 화면에 하나도 반영되지 않았습니다.** 지금까지는 누군가 가서 F5를 눌러야 했습니다.
  - 이제 서버가 알려주는 버전과 지금 돌고 있는 코드의 버전을 견주어 보고, 다르면 3초 뒤 알아서 새로고침합니다.
  - **새 버전이 올라왔을 때만 딱 한 번입니다.** 시간이 됐다고 새로고침하지 않습니다 - 보고 있는 중에 화면이 깜빡이면 그것대로 방해가 됩니다.
  - 새로고침한 뒤에도 버전이 그대로면(배포 캐시 등) 무한히 반복될 수 있어, 한 번 한 뒤에는 10분 동안 다시 하지 않습니다. 새로고침이 계속 도는 화면은 아무것도 못 읽습니다.
  - **이번 배포가 마지막 수동 새로고침입니다.** 지금 켜져 있는 화면은 예전 코드라 이 기능이 없으니 한 번만 F5를 눌러주시고, 그 뒤로는 알아서 따라옵니다.



- **각 반의 위치가 항상 보입니다.** 반 이름 바로 옆에 붙습니다. 교실에 있으면 교실 이름이, 나가면 그 자리 글자만 **[교실 밖]** 으로 바뀝니다. 위치는 늘 같은 자리에 있어야 눈이 그 자리를 찾습니다 - 있다 없다 하면 매번 다시 훑게 됩니다.

- **토들 수집기가 살아 있는지 운영 대시보드에서 보입니다.** 문의 칸 오른쪽에 `수집 10:58` 처럼 마지막 신호 시각이 뜨고, 5분 넘게 소식이 없으면 칸 전체가 빨갛게 **[토들 수집기가 멈춰 있습니다]** 로 바뀝니다.
  - 문의가 안 뜨는 것이 "문의가 없어서"인지 "수집기가 멈춰서"인지는 전혀 다른 얘기인데 화면에는 똑같이 비어 보입니다. 조용히 아무것도 안 하면서 정상인 척하는 것이 가장 나쁜 실패라, 눈에 띄게 했습니다.
  - 로그인이 풀린 경우에는 무엇을 해야 하는지까지 적습니다.



- **업무 화면: 학부모 문의사항과 출결내역의 자리를 바꿨습니다.** 문의사항이 위쪽 넓은 칸의 **기본 탭**이 되어 목록이 통째로 펼쳐집니다. 하루 열 건 넘게 오는 것을 세 줄로 보는 건 사실상 안 보는 것과 같았습니다. 출결내역·출결알림은 옆 탭으로 그대로 있습니다 - 없앤 것이 아니라 자리를 바꾼 것입니다.

- **운영 대시보드**
  - **시간표를 줄이지 않습니다.** 반이 늘어나도 칸을 좁히지 않고, 시간표 칸이 남는 공간을 크게 가져갑니다.
  - **쉬는 시간**이면 `쉬는 시간 · 10:40에 3교시 시작` 이라고 뜨고, 각 반에는 **다음 교시 과목을 미리** 보여줍니다. 지금 수업으로 잘못 읽지 않도록 색을 죽이고 [다음] 표시를 붙였습니다. 수업이 다 끝난 뒤와도 구분해 적습니다.
  - **교시가 바뀌는 순간 화면이 같이 바뀝니다.** 평소 15초마다 받아오고, 더해서 지금 교시가 끝나는 시각에 맞춰 한 번 더 받아옵니다. 종이 쳤는데 화면은 지난 교시를 보여주면 보는 사람이 화면을 안 믿게 됩니다.



- **운영 대시보드에 학부모 문의사항**을 띄웁니다. 아직 답하지 않은 것만, 급한 것부터 올립니다. 처리된 것까지 섞이면 멀리서 훑어보는 의미가 없어집니다. 학생 이름·분류·요약 한 줄·받은 시각이 한 줄에 들어갑니다.

- **지금 어느 반이 교실을 비웠는지 보입니다.**
  - 체육·컴퓨터·음악·미술 시간이면 반 칸이 **주황색으로 바뀌고 [교실 밖]** 이 붙습니다. 어느 방인지는 아직 적지 않습니다.
  - 어느 방인지는 안에서 계산해 두었습니다(체육관·컴퓨터실·음악실·미술실). 나중에 켜기만 하면 됩니다.
  - 과목 표기가 학교마다 달라 넉넉히 잡았습니다(PE / P.E. / Physical Education / 체육, Computer Science / Coding / CS / 코딩 등). **못 알아본 과목은 교실에 있는 것으로 봅니다** - 확실하지 않은데 "교실 밖"이라고 하면 찾으러 갔다가 헛걸음하고, 그러면 이 표시를 아무도 안 믿게 됩니다.
  - 22가지 과목명으로 판정을 확인했습니다. `Language Arts`(영문학)를 미술로 잘못 보내지 않는 것도 함께 확인했습니다.



- **수집기가 목록은 제대로 읽습니다**(안 읽은 방 14개). 이제 "방은 있는데 보낼 것이 0건"인 이유를 화면에 적습니다(v2.1.1).
  - 실제 데이터를 확인해보니 안 읽은 방 14개 안에 **학부모 메시지가 7건** 있었습니다. 그런데 0건으로 나왔습니다. 보내기 직전 어딘가에서 걸러지고 있다는 뜻입니다.
  - 이제 `거른 것: 직원 15, 시스템 4` 처럼 **무엇을 왜 걸렀는지** 적습니다. "0건"만 보여주면 정상인지 막힌 건지 알 수가 없어서, 이걸 알아내려고 여러 번 헛짚었습니다.
  - **[보낸 기록 지우고 다시 보내기]** 를 넣었습니다. 수집기는 같은 메시지를 두 번 보내지 않으려고 보낸 것을 기억해두는데, 서버 쪽에서 지웠거나 시험 중이면 그 기억 때문에 막힌 것처럼 보입니다.



- **확장을 새로고침해도 예전 코드가 계속 돌던 문제를 고쳤습니다**(수집기 v2.1.0). "고쳤는데 그대로다"의 진짜 원인이었습니다.
  - 크롬은 확장을 새로고침하면 새 코드를 열려 있는 탭에 다시 넣어줍니다. 그런데 그 페이지에는 이전 스크립트가 이미 자리를 잡고 있어서, **"이미 설치됨" 표시에 걸려 새 코드가 곧장 빠져나갔습니다.** 확장 카드에는 새 버전이 찍히는데 동작은 하나도 안 바뀌는, 대단히 헷갈리는 상태였습니다. 제가 "F5 필요 없다"고 안내한 것도 틀렸습니다.
  - 이제 **버전이 다르면 낡은 것을 걷어내고 새로 답니다.** 확장이 새로 올라오면 토들 탭도 알아서 새로고침합니다. 사람이 F5를 기억할 필요가 없습니다.
  - 상태판에 **페이지 쪽 코드의 버전**(`p2.1.0`)을 함께 적습니다. 확장 카드의 버전과 다르면 낡은 코드가 아직 도는 것이고, 그걸 화면에서 바로 알 수 있어야 합니다.
  - 팝업 제목 옆에도 버전이 뜹니다.



- **수집기 팝업에 버전을 표시합니다**(v2.0.2). 이게 없어서 "고쳤는데 그대로다"를 여러 번 주고받았습니다.
  - 업데이트 스크립트가 파일을 내려받아도 크롬 확장을 새로고침하지 않으면 예전 코드가 계속 돕니다. 화면만 봐서는 어느 쪽인지 알 수가 없었습니다. 이제 팝업 제목 옆에 지금 돌고 있는 버전이 그대로 뜹니다.
  - 팝업의 주소 예시도 실제 주소(gia-ops.vercel.app)로 고쳤습니다. 예전 예시가 남아 있어 혼동을 줬습니다.



- **"안 읽은 방 0개"가 무슨 뜻인지 구분해 보여줍니다**(수집기 v2.0.1).
  - 토들의 '안 읽음'은 **계정마다 따로** 셉니다. 그래서 0개에는 서로 다른 상황이 섞여 있는데 화면에는 똑같이 보였습니다.
    - **방이 아예 안 보임** - 그 계정이 학부모 채팅방의 참여자가 아닙니다. 고쳐야 할 문제입니다.
    - **방은 보이는데 다 읽음** - 진짜 정상입니다.
  - 이제 앞의 경우에는 **[계정 확인 필요]** 로 뜨고, 정상일 때도 `안 읽은 방 0개 / 전체 12개`처럼 전체 방 수를 함께 적습니다.
  - 어느 계정으로 보고 있는지도 함께 표시합니다. 사무실 PC와 개인 PC의 계정이 다르면 보이는 것도 달라집니다.



- **앞날 픽업 예약**.
  - 연락 하나가 여러 날을 가리키는 경우가 많습니다. 지금까지는 오늘 것만 처리되고 나머지 날짜는 **사람이 기억해야 했습니다.** 기억에 기대는 부분이 사고가 나는 곳입니다.
  - 이제 AI가 "이번주 목금", "내일부터 금요일까지", "8/26, 8/27" 같은 표현에서 날짜를 모두 뽑아 각각 예약합니다. 오늘 것은 즉시 반영하고, 나머지는 그날 아침 자동으로 걸립니다.
  - AI가 날짜를 잘못 셀 수 있어 서버에서 다시 거릅니다 - 지난 날, 45일보다 먼 날, **주말**, 중복은 버립니다. 아이를 엉뚱한 날 태워 보내는 일은 되돌릴 수 없습니다.
  - "이번주 목금"처럼 해석이 들어간 건은 **[확인 필요]** 로 표시해 예약하되 눈에 띄게 합니다. 놓치는 것보다 낫습니다.

- **당일 아침 자동 반영 + 담임 알림** (매일 오전 7시).
  - 그날 예정된 픽업을 하원 체크표에 걸어줍니다. 운영 대시보드·안내보드에도 그대로 따라옵니다.
  - 담임 선생님께 **긴급** 업무로 알립니다. 확인이 필요한 건은 제목에 그렇게 적습니다.
  - 학생을 명부에서 못 찾았거나 차량 배정이 없으면 조용히 넘기지 않고 인박스에 드러냅니다.

- **픽업 인박스에 [앞으로 예정된 픽업]** 을 날짜별로 묶어 보여줍니다. 예약이 어딘가 잡혔는데 눈으로 확인할 곳이 없으면 아무도 믿지 않습니다. 여기서 [맞음]으로 확인하거나 취소할 수 있습니다.



- **수집기를 새로 짰습니다**(v2.0.0). 며칠간 실패한 "엿보고 배우는" 방식을 버렸습니다.
  - 기존 방식은 토들이 주고받는 통신을 옆에서 보고 요청 서식을 배우는 것이었습니다. 그런데 토들은 채팅 목록을 한 번만 받아 캐시해서 **배울 기회가 사실상 한 번뿐**이고, 그 순간을 놓치면 영영 못 배웁니다. 감시를 계속 다시 걸어 해결하려다 토들 전체를 멈추게 했습니다.
  - 엿보기를 그만두고 실제로 무엇이 필요한지 화면에서 하나씩 확인했습니다.
    - 인증은 **쿠키**가 합니다. 토큰을 헤더에 넣을 필요가 없습니다.
    - 필요한 헤더는 **딱 둘** - `content-type` 과 `x-tod-source: WEB`(고정값). 없으면 401, 있으면 200입니다.
    - 학교 번호는 브라우저에 저장되어 있습니다(`userInfo.org_id`).
    - 우리에게 필요한 네 개 필드만 담은 짧은 질의를 직접 써도 그대로 답해줍니다. 토들의 원래 질의는 1,900자가 넘지만 우리는 그럴 필요가 없습니다.
  - 그래서 이제 **엿볼 것도 배울 것도 없습니다.** 로그인만 되어 있으면 바로 물어봅니다. 실제로 확인한 결과 안 읽은 방 12개, 방별 메시지·보낸이·시각까지 모두 정상입니다.
  - 읽음 처리는 여전히 별도 요청이라, 조회만으로 토들의 '안 읽음' 표시가 사라지지 않습니다. 선생님들 업무는 그대로입니다.
  - 처음 켠 날 지난 몇 달치가 쏟아지지 않도록, 방의 안 읽은 개수만큼 최신 것만 보고 3일 이내 것만 보냅니다.



- **수집을 6초 만에 끊고 있었습니다**(수집기 v1.5.2). 일을 시작해놓고 끝나기 전에 포기하는 상태였습니다.
  - 화면을 왕복시켜 다시 배우는 데만 9초가 걸리고, 방 열두 개의 메시지를 받아오는 시간이 더해집니다. 그런데 기다리는 시간이 6초로 묶여 있어 **매번 도중에 잘렸습니다.** 이 6초는 예전에 "탭 응답 없음"을 잡으려고 20초에서 줄인 값인데, 지금은 오히려 방해가 되고 있었습니다.
  - 하는 일에 맞춰 나눴습니다 - 상태 확인은 6초(답이 없으면 페이지가 죽은 것), 수집은 90초.
  - 수집 한 번이 1분을 넘을 수 있어, 앞의 수집이 도는 중이면 이번 차례를 거릅니다. 겹쳐 돌면 같은 메시지를 두 번 보내고 화면도 두 번 왕복합니다.



- **긴급 수정: v1.5.0 수집기가 토들을 멈춰 세웠습니다.** 제가 넣은 코드의 잘못입니다.
  - "감시가 지워졌으면 다시 씌운다"며 원본 통신 함수를 계속 갱신하게 했는데, 토들이 우리 위에 덧씌운 것을 다시 원본으로 삼는 바람에 **우리 함수가 우리 함수를 부르는 고리**가 생겼습니다. 토들의 모든 통신이 막혀 "오프라인"이 뜨고 로그아웃도 되지 않았습니다.
  - 원본은 이제 처음 한 번만 붙잡고 **절대 바꾸지 않습니다**(고정). 다시 씌우는 동작은 통째로 뺐습니다.
  - 되짚어보니 "페이지 처음에 건 감시가 지워진다"는 것은 제가 확인하지 않은 짐작이었습니다. 확인한 것은 "화면을 왕복시키면 다시 배운다" 하나뿐이었고, 그것만 남겼습니다.
  - 재학습은 10분에 한 번으로 제한합니다. 못 배우는 동안 화면이 1분마다 왔다갔다 하지 않도록 했습니다.



- **수집기가 안 읽은 방을 못 읽던 진짜 원인을 잡았습니다**(수집기 v1.5.0). 브라우저에서 하나씩 실제로 돌려보고 확인한 것들입니다.
  - **토들 번들이 우리 감시를 지웁니다.** 페이지 맨 처음에 감시를 걸면 → 안 잡히고, 페이지가 다 뜬 뒤에 걸면 → 잡힙니다. 토들이 나중에 로드되면서 통신 함수를 통째로 갈아끼워 먼저 걸어둔 것을 버리기 때문입니다. **먼저 걸수록 오히려 지워지고 있었습니다.** 이제 0.5초마다 확인해 지워졌으면 다시 겁니다.
  - **아직 못 배웠으면 화면을 잠깐 왕복시켜 배웁니다.** 토들은 채팅 목록을 한 번 받아 캐시해서, 가만히 두면 요청이 다시 오지 않습니다. Overview로 갔다가 Messages로 돌아오면 목록을 새로 받아옵니다 - 실제로 해보니 11개를 다시 배웠습니다. 수집 전용 컴퓨터라 화면이 잠깐 바뀌어도 문제되지 않습니다.
  - 왼쪽 메뉴가 링크(`<a>`)일 거라 짐작하고 짰다가, 화면에서 확인해보니 링크가 0개였습니다. 실제로는 id가 붙은 칸(`#chat`, `#monitor`)이어서 그에 맞춰 고쳤습니다.



- **수집기가 배운 서식을 저장합니다**(수집기 v1.4.0). 브라우저로 실제 통신을 재현해보고서야 진짜 구조를 알았습니다.
  - **토들은 채팅 목록을 한 번만 받아 캐시합니다.** 화면을 아무리 눌러도 다시 요청하지 않습니다 — 확인해보니 탭 전환 때 fetch 1건, XHR 0건이었습니다. 즉 "Messages 화면에 처음 들어갈 때" 딱 한 번만 배울 기회가 있고, 그 순간을 놓치면(확장을 나중에 켰거나 크롬이 탭을 재웠다 깨웠거나) **영영 못 배웁니다.** 세 번 고쳤는데 모두 이 지점을 비껴갔습니다.
  - 이제 한 번 배우면 확장 저장소에 넣어두고 다음부터 그대로 씁니다. 인증 헤더는 새 요청을 볼 때마다 갱신합니다.
  - **재전송 방식 자체는 정상**임을 실제로 확인했습니다(재전송 결과 9개, 안 읽은 방 9개). 문제는 서식을 못 배우는 것 하나뿐이었습니다.

- **"정상 0개"라는 거짓 안심을 없앴습니다.** 아는 방이 하나도 없으면 **[목록 못 읽음]** 으로 표시하고, 채팅목록 서식·대화 서식을 각각 있음/없음으로 보여줍니다. 원격에서 무엇이 막혔는지 한 줄만 봐도 알 수 있어야 합니다.
  - 정상일 때도 `전체 N개 · 안 읽은 방 N개`를 함께 적습니다.



- **"안 읽은 방 0개" 진짜 원인을 잡았습니다**(수집기 v1.3.1). 브라우저로 토들 통신을 직접 들여다보고 확인했습니다 — 추측으로 두 번 고쳤는데 둘 다 빗나갔습니다.
  - **채팅 목록을 돌려주는 요청이 두 종류였습니다.** `getChatAndChannelList`(방 이름·안 읽은 수가 든 진짜 목록)와 `getChatUnreadCount`(안 읽은 "개수"만 세는 요청). 둘 다 응답 모양이 `data.node.chats`로 같아서, "응답에 chats가 있으면 목록"이라는 판단이 **나중에 오는 개수 세기 요청으로 진짜 목록을 덮어썼습니다.** 그래서 재사용할 때 알맹이 없는 응답을 받아 0개가 됐습니다.
  - 이제 **방 이름이 들어 있는 응답만** 목록으로 인정합니다. 요청 이름으로 고르지 않으므로 토들이 이름을 바꿔도 계속 맞습니다.
  - 개수 세기 응답이 들어와도 **이미 알아둔 방 이름을 덮어쓰지 않습니다.**

- **문의가 언제 왔는지 정확히 표시합니다**. 토들 목록과 같은 방식입니다 — 오늘 온 것은 **시각**(14:32), 이번 주면 **요일+시각**(화 09:15), 그 전이면 날짜. 마우스를 올리면 전체 날짜시각이 뜹니다.
  - "3시간 전"만으로는 오전인지 점심인지 가늠할 수 없습니다. 하원 픽업은 몇 시에 온 연락인지가 판단에 직접 영향을 줍니다.
  - 메시지 자체의 시각이 없으면 그 방의 마지막 활동 시각으로 대신합니다.



- **수집기가 "0개"를 정상이라고 말하던 문제를 고쳤습니다**(수집기 v1.3.0). 토들에 안 읽은 방이 37개인데 수집기는 "정상 · 새 메시지 없음 (안 읽은 방 0개)"라고 답했습니다. **조용히 아무것도 안 하면서 정상이라고 말하는 것**이 이 시스템에서 가장 나쁜 실패인데, 정확히 그 상태였습니다.
  - **원인 ① 서식을 요청 이름으로 골랐던 것.** `operationName === "getChatConversations"` 처럼 요청 쪽 글자를 보고 판단했는데, 확장이 나중에 끼어들면 페이지가 이미 보낸 요청을 못 봅니다. 이제 **응답을 보고 판단합니다** — 토들은 요청을 배열로 묶어 보내고 응답도 같은 순서로 오므로, "몇 번째 응답에 채팅 목록이 들어 있나"를 보면 그 자리의 요청이 곧 채팅 목록 요청입니다. 토들이 이름을 바꿔도 계속 맞습니다.
  - **원인 ② 못 배운 상태에서 빈 목록을 돌려준 것.** 이제 아무것도 못 배웠으면 `NEEDS_TRAINING`을 던져 "준비 중"으로 표시합니다.
  - **자동 복구.** 두 번 연속 준비 중이면 수집기가 토들 탭을 스스로 새로고침합니다. 매번 하면 담당자가 토들을 보는 중에 방해가 되므로, 정말 막혔을 때만 합니다.

## v0.149.1 - 2026-08-24

- **업데이트 스크립트가 실패 원인을 그대로 보여줍니다**(수집기 v1.2.1). "서버에 연결하지 못했습니다" 한 줄만 띄우면 무엇이 잘못됐는지 짐작할 수 없습니다. 이제 HTTP 응답 코드와 curl 오류를 그대로 적고, 404(미배포)·401/403(배포 보호)·로그인 화면이 돌아온 경우를 각각 구분해 안내합니다.

## v0.149.0 - 2026-08-24

- **토들 수집기 자동 업데이트**(수집기 v1.2.0). 폴더를 매번 끌어다 놓게 만든 건 설계 잘못이었습니다 — 드래그하면 "폴더 2" 같은 새 폴더가 생기고, 크롬은 여전히 옛 폴더를 보고 있어 갱신이 안 됩니다.
  - 수집기 파일을 `public/collector/`로 옮겨 운영앱이 직접 서빙합니다. 이제 확장이 최신 파일을 **자기가 있는 자리에 덮어씁니다.**
  - 폴더 안의 **업데이트.command**(맥) / **업데이트.bat**(윈도우)를 더블클릭하면 끝입니다. 처음 한 번만 운영앱 주소를 묻고 그 뒤로는 기억합니다.
  - 받을 파일 목록을 `files.json`에서 읽으므로, 나중에 파일이 늘어나도 사용자는 아무것도 다시 옮기지 않아도 됩니다.
  - 임시 폴더에 전부 받은 뒤 성공했을 때만 덮어씁니다. 중간에 끊겨 반쪽짜리 확장이 남는 것이 가장 나쁜 결과라서입니다.
  - 확장이 스스로 새 버전을 확인해 **팝업에 "새 버전 N이 있습니다"** 를 띄웁니다. 손으로 설치한 확장이라 크롬이 알아서 갱신해주지 않기 때문입니다.
  - `/collector` 경로를 로그인 없이 열리도록 열었습니다(확장은 브라우저 세션 없이 파일을 받습니다). 확장 코드에는 비밀값이 없고, 수집 키는 담당자가 그 PC에서 직접 넣어 그 PC에만 저장됩니다.



- **토들 수집기 자가 복구**(수집기 v1.1.0). 설치 직후 "탭 응답 없음"이 뜨던 문제를 고쳤습니다.
  - 원인은 크롬의 규칙입니다 — 확장을 설치해도 **이미 열려 있던 탭에는 코드를 넣지 않습니다.** 그래서 설치 직후에는 항상 이 상태가 됩니다. 사람에게 새로고침을 시키는 대신, 수집기가 `chrome.scripting`으로 **직접 코드를 심어 스스로 복구**하게 했습니다. 크롬이 오래된 탭을 잠재웠다 깨우는 경우에도 같은 길로 낫습니다.
  - 토들 탭이 여러 개면 **전부 차례로 시도**하고, 메시지 화면(`/messaging`)이 열린 탭을 먼저 씁니다. 예전에는 첫 번째 탭 하나만 보고 포기했습니다.
  - 코드가 두 번 심겨도 한 번만 설치되도록 막았습니다(안 막으면 fetch를 두 겹으로 감싸고 같은 질문에 두 번 답합니다).
  - 페이지 응답 대기를 20초 → 6초로 줄였습니다. 수집 주기가 1분인데 20초를 기다리면 그 회차를 통째로 날립니다.



- **학부모 문의사항 탭 신설**.
  - 이제 AI가 학부모 연락을 **픽업 / 문의 / 기타** 세 갈래로 나눕니다. 문의는 유형(출결·수업·학습·생활·교우·건강·안전·차량·하원·행사·일정·납부·행정)과 **긴급도**, 한 줄 요약까지 붙여 남깁니다.
  - 학생·담임은 토들 채널 이름(`G2_Reina Park_Office`)으로 자동 연결됩니다. **[내 반]** 버튼으로 담임이 자기 반 문의만 볼 수 있습니다.
  - 목록은 **미답변 먼저, 그 안에서 긴급한 것 먼저** 정렬됩니다. 손댈 것이 항상 위에 있어야 합니다.
  - 문의를 누르면 원문·분류가 뜨고 **[토들에서 열기]** 로 그 채팅방으로 바로 갑니다. 여는 사람의 토들 로그인으로 열리므로, 그 방 멤버인 선생님은 바로 열리고 아니면 토들이 막습니다.
  - **[답변 완료]** 로 처리 여부를 관리하고, **[+ 업무로 등록]** 으로 필요한 것만 업무로 넘깁니다. 자동 등록하지 않는 이유는 하루 수십 건이 업무 목록에 쏟아지면 원래 업무가 묻히기 때문입니다. 넘길 때 담당자는 담임으로, 부서는 학생 부서로, 긴급 문의는 우선순위 [긴급]으로 미리 채워집니다.

- **실시간 로그를 상단 부서 줄 가운데로 이동**. 최신 한 줄만 흐르고, 누르면 전체 목록이 팝업으로 열립니다. 세 줄을 차지하던 자리는 학부모 문의사항이 이어받았습니다.

- **'기타'로 분류된 연락은 본문을 저장하지 않습니다.** 인사·감사·단순 확인처럼 답이 필요 없는 것은 남길 이유가 없습니다. 같은 메시지를 다시 읽지 않기 위한 표시만 남깁니다.

- **수집기가 채팅방 링크를 함께 보냅니다.** 학교·과정 번호를 코드에 박지 않고 지금 보고 있는 화면 주소에서 떼어 쓰므로, 다른 과정(LES/UES)에서도 그대로 동작합니다.

## v0.146.0 - 2026-08-24

- **토들 수집기(크롬 확장) 제작** — `tools/toddle-collector/`. 사무실 CCTV용 윈도우 PC 크롬에 한 번 설치하면, 토들에 새 학부모 메시지가 올 때마다 1분 안에 읽어 픽업 인박스로 보냅니다. **채팅방 100개를 사람이 도는 일이 사라집니다.**
  - **읽음 처리를 하지 않습니다.** 토들에서 읽음으로 바꾸는 것은 `insertUserChatActivityLog`라는 별도 요청인데, 수집기는 조회만 흉내 내고 그 요청을 아예 만들지 않습니다. 그래서 직원 화면의 '안 읽음' 표시가 그대로 남고, 답장이 필요한 진짜 문의는 예전처럼 사람이 처리합니다.
  - **요청 서식을 코드에 박지 않고 그때그때 배웁니다.** 토들이 실제로 보내는 요청을 옆에서 보고 복사해 두었다가, 볼 채팅방 id만 바꿔서 재사용합니다. 질의문을 박아두면 토들이 필드 하나만 바꿔도 멈추는데, 이 방식은 토들 변화를 따라갑니다.
  - 교직원이 쓴 글, 이미 읽은 옛 메시지, 이미 보낸 메시지는 보내지 않습니다(처음 켠 날 과거를 몰아 보내는 사고 방지).
  - 확장 아이콘의 상태판에 **정상 / 준비 중 / 로그인 필요 / 토들 탭 없음 / 전송 실패**가 표시되고, 같은 상태가 픽업 인박스 상단에도 뜹니다.
  - 설치 방법은 `tools/toddle-collector/설치안내.md`에 화면 순서대로 적어두었습니다.

## v0.145.0 - 2026-08-24

- **픽업 인박스 신설**. [셔틀 → 픽업 인박스]
  - 토들·전화·교사·구글챗·직접입력 어디로 들어오든 한 곳(`pickup_requests`)에 모입니다.
  - AI가 "픽업인가 / 누구인가 / 몇 시인가"를 뽑아 명부와 대조합니다. **확신이 높고 학생이 한 명으로 특정되면 자동으로 하원 체크표에 픽업이 찍히고**, 둘 중 하나라도 아니면 [확인이 필요한 픽업]에 남아 담당자가 한 번 눌러 확정합니다. 픽업은 아이를 누구에게 보내느냐의 문제라 기준을 넉넉히(0.85) 잡았습니다.
  - 토들 채널 이름이 `G2_Reina Park_Office` 형식이라 **본문을 해석하지 않아도 학생이 특정됩니다.** 형제 방(`G2_Sophia & Bella Hwang_Office`, `G3&G6_Ije & Ryeomyeong Kang_Office`)만 본문에서 가려내고, 못 가리면 사람에게 넘깁니다.
  - [손으로 접수] — 전화로 받은 내용이나 선생님이 전달한 내용을 붙여넣으면 같은 AI가 학생·시각을 찾아냅니다. 여러 건을 한 번에 넣을 수 있습니다.
  - **개인정보 최소 수집** — 픽업이 아니라고 판단된 메시지는 본문을 저장하지 않고, 같은 메시지를 다시 읽지 않기 위한 출처 식별자만 남깁니다. 픽업 건의 본문도 30일이 지나면 매일 도는 크론이 비웁니다.

- **수집기 생존 신호**(`integration_heartbeats`). 토들 수집기가 조용히 멈추면 그날 픽업을 통째로 놓칩니다. 1분마다 신호를 받아 **10분 끊기면 인박스 상단에 빨간 경고**를 띄웁니다 — "죽으면 시끄럽게"가 원칙입니다. 로그인이 풀린 경우는 그 PC에서 재로그인만 하면 된다고 안내합니다.

- **하원 운영화면 개편**.
  - 지도 밑 호차 카드를 **한 줄짜리 요약 띠**로 줄였습니다(호차 · 상태 · 탑승 3/12 · GPS 점). 미탑승 학생 이름까지 넣어 세로로 길던 카드를 없애 자리를 확보했습니다.
  - 남는 자리를 전부 **오늘 픽업 학생 실시간 목록**에 줬습니다. 하원 중에 눈으로 좇아야 하는 건 "몇 호차가 있다"가 아니라 "누가 차를 안 타는가"입니다.
  - **차가 이미 출발한 뒤에 픽업으로 바뀐 건은 빨갛게 맨 위로** 올리고 "⚠ 출발 후 변경 — 차량에 연락하세요"를 띄웁니다. 그건 그 아이를 태우고 떠났다는 뜻이라, 몇 분만 늦게 알아도 학부모가 빈 학교에서 기다리게 됩니다. 최근 15분 안에 바뀐 건은 파랗게 NEW로 표시합니다.

## v0.144.0 - 2026-08-23

- **운영 대시보드가 창 크기에 맞춰 스스로 줄어듭니다**.
  - 그동안은 "화면 절반"을 가정하고 글자 크기를 숫자로 박아뒀습니다. CCTV가 절반보다 더 차지하니 아래쪽 내용이 화면 밖으로 밀려났고, 대시보드는 아무도 스크롤하지 않으므로 밀려난 정보는 없는 것과 같았습니다.
  - 이제 창 크기를 재서 글자·여백·칸 최소폭을 한 배율로 함께 줄입니다. 가로·세로 중 더 부족한 쪽을 기준으로 삼아, 폭은 넉넉한데 높이가 낮은 창에서도 아래가 잘리지 않습니다.
  - 화면 높이를 넘기지 않도록 각 패널이 남는 공간을 나눠 갖고(시간표 3 : 출결·픽업 2 : 업무 2), 넘치는 목록은 그 패널 안에서만 스크롤됩니다. 아래 패널이 화면 밖으로 밀려나지 않습니다.
  - 창이 좁으면(820px 미만) 출결·픽업을 두 칸에서 한 칸으로 합치고, 업무 줄의 부서 표시를 빼서 제목이 잘리지 않게 합니다.
  - 오른쪽 위 **[🔍 자동]** 버튼으로 글자 크기를 자동 → 크게 → 보통 → 작게로 바꿀 수 있습니다. 모니터·시력·보는 거리에 따라 적당한 크기가 다르므로, 한 번 고르면 그 컴퓨터에 저장됩니다.

- **하원 운행 화면도 같은 방식으로 조절됩니다.** 전체화면을 못 쓰는 날에는 이 화면도 같은 좁은 창에 들어갑니다.
  - 좁을 때는 지도 비중을 55%에서 42%로 줄입니다 — 폭이 좁으면 지도가 담는 범위가 작아 "어디쯤인지"를 읽기 어렵고, 그보다 아래 차량 카드가 다 보이는 편이 실제로 쓸모 있습니다.
  - 차량 카드의 미탑승 학생 이름을 좁을 때 8명 → 5명으로 줄입니다(나머지는 +N). 카드가 세로로 길어져 뒤쪽 호차가 안 보이는 것을 막습니다.

## v0.143.0 - 2026-08-23

- **기기 발급을 일괄 처리로 바꿨습니다**. 기사님이 오시기를 기다릴 이유가 없어졌습니다.
  - [기기 없는 N개 노선에 한 번에 발급] 버튼 하나로 하원 노선 전체에 기기 ID와 설정 링크가 만들어집니다. 노선을 고르고 발급을 누르는 반복 작업이 사라집니다.
  - 그 뒤 담당자가 할 일은 [문자로 보내기]뿐이고, 기사님은 학교에 오시지 않아도 댁에서 설정을 마치실 수 있습니다.

- **설정 현황을 노선 기준으로 다시 짰습니다.** 기존에는 "발급된 기기"만 목록에 떠서, 아직 발급하지 않은 노선은 화면에 아예 없었습니다. 그러면 무엇이 남았는지 담당자가 머리로 세어야 합니다.
  - 하원 노선 전부가 한 줄씩 나오고 상태가 붙습니다 — **기기 없음 / 링크 보내야 함 / 링크 열어보심·설정 미완료 / ✓ 연결됨 / 신호 끊김**.
  - 상단에 `연결 N · 대기 N · 전체 N개 노선` 요약을 뒀습니다.
  - **신호 끊김**(예전에 들어왔는데 지금 조용한 경우)을 빨간색으로 따로 구분했습니다. 아이폰의 "계속 허용하시겠습니까?"를 잘못 누르셨거나 절전 설정이 되돌아간 경우가 대부분이라, 그냥 넘기면 그날 하원 때 알게 됩니다.

- **기사님 등록 체크리스트(A4 한 장)** 추가 — `docs/셔틀/`. 기사님 앞에서 손에 들고 진행하는 현장용입니다. 사전 준비 → 대면 절차 → 미리 말씀드릴 것 → 그날 확인의 네 단계와 체크박스로 구성했습니다.

## v0.142.0 - 2026-08-23

- **위치 수집 시간을 하원 3시간으로 좁혔습니다**. 기존 평일 12:00~20:00 → **평일 15:30~18:30**.
  - 서버가 이 창 밖의 좌표를 받는 즉시 버립니다(`isWithinTrackingWindow`). 문서상의 약속이 아니라 코드로 강제되므로, 기사님이 앱을 끄지 않으셔도 그 시간 밖의 이동은 저장되지 않습니다.
  - 등원 시간대는 확정되면 `TRACK_WINDOWS`에 한 줄만 더하면 됩니다. 시간 판단을 시(hour) 단위에서 분(minute) 단위로 바꿔뒀습니다.

- **기사님 설정 링크 신설**. 기기마다 짧은 주소 `/s/{6자리}`를 하나씩 발급합니다.
  - **기사님 화면**(로그인 없음) — 아이폰·안드로이드를 자동으로 가려 스토어 버튼을 띄우고, 서버 주소와 기기 번호를 **눌러서 복사**하게 합니다. 타자를 한 글자도 치지 않으십니다. 6단계를 한 번에 하나씩만 펼치고, 마지막에 서버가 실제로 위치를 받았는지 확인해 초록 표시를 띄웁니다(10초마다 자동 재확인).
  - **관리자 화면** — 호차 옆 [설정 링크]를 누르면 큰 QR(학교에 오신 기사님이 휴대폰 카메라로 찍는 용도)과 [문자로 보내기]·[문구 통째로 복사(카톡용)]가 나옵니다. 문자는 별도 발송 API(유료 가입·심사 필요) 없이 담당자 휴대폰의 기본 문자 앱을 열어 내용을 채웁니다.
  - 링크 코드는 기기 ID와 **따로** 둡니다. 문자로 나가는 값이라 새어 나갈 가능성이 크기 때문에, 그때는 코드만 새로 발급하면 되고 이미 설정을 마친 휴대폰은 건드리지 않아도 됩니다.
  - 기사님이 링크를 여신 시각이 남으므로, 설정을 마치셨는지 전화로 확인하지 않아도 됩니다.

- **안내문·동의서 개정**.
  - 수집 시간을 **평일 15:30~18:30**으로 고쳤고, 등원은 이 동의서에 포함되지 않으며 확대 시 다시 동의를 받는다고 명시했습니다.
  - 목적 제한을 각 문서 앞머리에 못박았습니다 — 하원 차량 운영 점검 외의 용도(사적 이동 경로 파악·근무 감시·운전 습관 평가)로 쓰지 않습니다.
  - 제3자 제공 금지를 "학부모를 포함하여 **학교 밖의 어떠한 개인·기관에도** 제공하지 않는다"로 강화하고, 판매·위탁·광고/통계 목적 이전까지 명시했습니다.
  - 안내문에 링크 방식 설명을 넣었습니다("문자로 받으신 링크 누르기").

## v0.141.0 - 2026-08-23

- **기사님용 안내문·동의서 작성**. `docs/셔틀/` 폴더에 두 개의 워드 파일로 넣었습니다.
  - **안내문(4쪽)** — 왜 하는지, 무엇이 수집되고 무엇이 수집되지 않는지, 앱 설치·설정 6단계, 자주 묻는 질문 6가지. Traccar 공식 문서에서 설정 항목 이름을 확인해 실제 화면과 같은 표기로 적었습니다.
  - **동의서(2쪽)** — 위치정보 수집·이용 동의서. 수집 목적·항목·시간 제한·보유 기간·열람 범위·거부 권리·철회 방법을 담고 서명란을 넣었습니다.

- **위치 기록 90일 자동 삭제 크론 신설**(`/api/cron/purge-shuttle-locations`, 매일 23:30 KST). 동의서에 "90일 뒤 자동 삭제"라고 적으려면 실제로 그렇게 동작해야 합니다. 문서에만 적어두고 계속 쌓아두면 그 약속이 거짓이 됩니다.
  - 위치 원본(`shuttle_pilot_pings`)과 정차 관찰 기록(`shuttle_stop_observations`)을 지웁니다.
  - 정류장 좌표(`shuttle_stops.gps_lat/lng`)는 지우지 않습니다 — 개인의 이동 기록이 아니라 "이 정류장이 어디인가"라는 장소 정보이고, 원본에서 이미 평균만 뽑아낸 값입니다.

## v0.140.0 - 2026-08-23

- **초등부 명부를 확정으로 삼고 정리했습니다**.

  **학생 → 보류** (지우지 않음). 명부에 없다고 바로 퇴소로 찍으면, 사실은 중고등부라서 이 명부에 없었을 뿐인 학생까지 퇴소가 됩니다. 그래서 **"아직 판단하지 않았다"는 뜻의 보류 상태**를 새로 만들어 거기에 뒀습니다.
  - 나중에 **중고등부 명단을 주시면 이름으로 대조해서, 그 명단에도 없는 학생만 퇴소로 넘깁니다.**
  - 보류 학생은 관찰기록·픽업체크·대시보드에서 빠지지만 기록은 그대로 남습니다.
  - 부서 값은 일부러 건드리지 않았습니다 — 중고등부일지 유치부일지 확인되지 않았는데 지금 단정해두면 나중에 대조할 때 잘못 붙은 값이 판단을 방해합니다.
  - **[학생 관리] 화면 위쪽에 [⏸️ 보류] 상자**가 생겼습니다. 펼쳐서 이름을 확인하고, 초등부 학생인데 누락된 경우라면 [재학으로] 한 번이면 되돌아옵니다.

  **반 → 삭제.** 확정 명부의 8개 반이 아닌 반은 지웠습니다. 옛 학기의 반이 남아 있으면 반 관리 화면과 담임 배정에서 계속 헷갈립니다.
  - **학생·관찰기록·출결은 지워지지 않습니다.** 연결만 끊어지고, 학년 표기는 각 기록에 글자로 남습니다. 그 반의 옛 시간표만 함께 사라지는데 새 학기 시간표를 이미 넣었으므로 문제되지 않습니다.
  - 어떤 반이 사라졌는지 담임 이름까지 [명부 점검]에 기록해뒀습니다.

  **유치부는 건드리지 않았습니다**. 부서·학년 표기가 유치부인 학생과 반은 그대로입니다. 유치부 반 이름 목록을 주시면 반 표기로도 정확히 갈라낼 수 있습니다.

  운영 화면이 보여주는 부서를 **초등부 하나로** 좁혔습니다. 대시보드의 부서 선택 버튼은 고를 것이 하나뿐이라 자동으로 숨겨집니다.

- **하원 셔틀은 이 정리의 영향을 받지 않습니다**. 하원 체크표·실시간 셔틀은 셔틀 배정표를 이름으로 읽기 때문에, **보류 학생도 유치부 학생도 그대로 나옵니다.** 하원차량표를 주시면 그것으로 노선을 다시 짜겠습니다.

## v0.139.4 - 2026-08-22

- **3학기 데이터 마이그레이션에서 없는 표(`wr_terms`)를 쓰던 코드를 걷어냈습니다.**

  ```
  ERROR: relation "wr_terms" does not exist (SQLSTATE 42P01)
  ```

  `wr_terms`는 **없는 게 정상입니다** — v0.36에서 "연도 > 학기" 체계를 운영과 주간 관찰기록이 함께 쓰도록 통합하면서 삭제된 표입니다. 그런데 제가 3학기 학기 등록을 만들면서 그 표에도 넣는 코드를 잘못 넣었습니다. 지우고 `terms` 하나만 쓰도록 고쳤습니다.
  - 대신 **새 학기를 켤 때 이전 학기를 자동으로 종료로 내립니다.** 여러 학기가 동시에 "진행중"으로 남아 있으면 화면이 어느 학기를 기준으로 삼을지 애매해집니다.
  - 남은 두 파일이 건드리는 표를 전부 추출해 실제로 존재하는지 확인했습니다. `wr_terms` 외에는 없습니다.

## v0.139.3 - 2026-08-22

- **도서관 마이그레이션의 옛 직위 이름을 고쳤습니다.** 앞의 세 파일은 통과했고 도서관 파일에서 막혔습니다.

  ```
  ERROR: new row for relation "app_users" violates check constraint "app_users_position_check"
  Failing row contains (gia-library@giamicro.com, ..., 교직원, ...)
  ```

  도서관 가계정을 계정 목록에 등록할 때 직위를 `교직원`으로 넣고 있었는데, 이 이름은 v0.36에서 **`행정직원`으로 정리되면서 허용값에서 빠진 옛 표기**입니다. 도서관 기능이 다른 갈래에서 만들어지는 동안 옛 이름이 그대로 남아 있었습니다. `행정직원`으로 바꿨습니다.
  - 남은 마이그레이션 전체를 훑어 **제약이 걸린 값(직위·소속·성별·악기·탑승·학기·상태)이 허용 목록 밖으로 나가는 곳이 더 없는지 전수 확인**했습니다. 이 한 건 외에는 없습니다.

## v0.139.2 - 2026-08-22

- **자동 반영이 멈춘 진짜 원인을 찾아 고쳤습니다.** 오류 문구는 이것이었습니다.

  ```
  ERROR: column s.gender does not exist (SQLSTATE 42703)
  ```

  실서버 `wr_students`에 **`gender`(성별) 칸이 아예 없었습니다.** 자동화를 만들기 전까지는 채팅으로 드린 SQL을 SQL Editor에 직접 붙여넣는 방식이었는데, 그 과정에서 한 블록이 빠졌던 것으로 보입니다. 그런데 나중에 만든 교직원 공용 명부 뷰가 그 칸을 읽으려 하면서 멈췄고, **마이그레이션은 하나가 실패하면 뒤가 전부 멈추기 때문에** 3학기 학생·반·시간표가 통째로 들어가지 못했습니다.
  - **선행 보정 파일을 추가했습니다** — 뒤 마이그레이션이 읽거나 쓰는 칸을 전부 "없으면 만들기"로 한 번씩 훑습니다(학생 14칸, 반 2칸, 셔틀 3칸, 계정 7칸, 학기별 반 배정 표, 공통 트리거 함수). 이미 있으면 아무 일도 일어나지 않습니다.
  - 앞으로도 손으로 넣은 SQL과 어긋나는 부분이 있으면 여기서 먼저 메워지므로 같은 이유로 멈추지 않습니다.

## v0.139.1 - 2026-08-22

- **자동 반영이 멈춘 원인으로 가장 유력한 곳을 고쳤습니다.** 마이그레이션 안에 실시간 구독 등록(`alter publication supabase_realtime add table...`)이 있는데, 이 문장은 Supabase에서 **소유자 권한**을 요구합니다. 대시보드 SQL Editor에서는 통과하지만 GitHub Actions가 쓰는 연결에서는 `must be owner of publication`으로 막힙니다.
  - 문제는 이 한 줄이 실패하면 **그 뒤 파일이 전부 멈춘다**는 것이었습니다. 실시간 구독은 "새 공지가 새로고침 없이 뜨는" 편의 기능일 뿐인데, 그것 때문에 학생 명부까지 못 들어간 셈입니다.
  - 이제 이 문장이 실패해도 안내만 남기고 넘어갑니다. 정말 권한 문제였다면 다음 실행부터 자동 반영이 끝까지 돌아갑니다.
  - 실시간 구독이 꺼진 채로 남으면 Supabase 대시보드 > Database > Replication에서 켜주시면 됩니다(업무 공지·도서관 대출 화면의 즉시 갱신용).

## v0.139.0 - 2026-08-22

- **자동 반영(GitHub Actions)이 v0.127.0부터 계속 실패하고 있었습니다.** 그래서 그 뒤로 만든 스키마와 3학기 학교 데이터가 실서버에 **하나도 들어가지 않은 상태**였습니다(명부가 비어 보이던 원인). 실행 로그가 비공개라 원인을 바로 볼 수 없어, 두 가지를 함께 했습니다.
  - **손으로 넣을 수 있는 SQL을 만들었습니다** — `supabase/manual/` 폴더에 1(스키마) · 2(3학기 데이터) · 3(기록표 정리) 세 파일입니다. Supabase SQL Editor에 순서대로 붙여넣어 실행하면 밀린 것이 전부 들어갑니다. 모든 문장이 "없으면 만들고 있으면 갱신"이라 몇 번을 돌려도 안전하고, 3번 파일이 반영 기록을 남겨서 **다음부터는 자동 반영이 같은 파일을 다시 돌리지 않습니다.**
  - **워크플로가 실패 원인을 감추지 않게 고쳤습니다** — 예전에는 빨간 X와 `exit code 1`만 보이고 무엇이 잘못됐는지 알 수 없었습니다. 이제 실행 결과 요약(Summary) 화면에 **원격 DB의 반영 현황과 실패 문구가 그대로** 적힙니다. 로그를 펼쳐 찾을 필요가 없습니다.
  - [학교 > 명부 점검] 안내에도 숫자가 계속 0일 때 무엇을 해야 하는지 적어뒀습니다.

- **운영 대시보드 '지금 수업'을 단순하게**: 반 이름과 지금 과목 **두 줄만** 남기고 담임 이름·다음 교시·담당 교사를 뺐습니다. 멀리서 보는 화면에서는 글자가 많을수록 오히려 안 읽힙니다. 과목 글자도 키웠습니다.
  - 수업이 없는 시간에는 그 자리에 **교실 위치**(A동 2F 등)가 뜹니다.

## v0.138.0 - 2026-08-22

- **당번표 신설**: [학교 > 당번표]에서 급식 당번을 입력해둘 수 있습니다. 화면 어디에도 띄우지 않고 모아두기만 하며, 나중에 대시보드나 달력에 올리기로 하면 여기 적은 내용을 그대로 씁니다 — 그때 다시 입력하실 필요가 없습니다.
  - 급식 당번뿐 아니라 **체육관·코딩실 사용, 도서관 당번처럼 같은 모양의 표는 전부 여기에** 넣습니다. [종류] 칸에 이름만 새로 적으면 새 당번표가 하나 생깁니다. 종류마다 표를 따로 만들면 새 당번이 생길 때마다 화면과 메뉴가 늘어나기 때문입니다.
  - [언제]는 **매주 반복**(요일)과 **특정일**(날짜) 중에 고릅니다. 담당자는 이름만 적어도 되고, 앱에 있는 이름과 같으면 계정까지 자동으로 연결됩니다.
  - 조회는 교직원 누구나, 추가·삭제는 행정직원 이상입니다.

- **모든 메뉴에 ❓사용 가이드**: 가이드가 없던 8개 화면에 새로 붙였습니다 — 업무 보드, 지난 업무, 휴지통, 하원 체크표, 실시간 셔틀, 등록사건목록, 보고서 모음, 데이터 백업. 이제 **메뉴에 연결된 모든 화면**에서 오른쪽 위 ❓를 누르면 그 화면이 무엇을 하는 곳이고 어떻게 쓰는지 볼 수 있습니다.
  - 하원 체크표 가이드에는 담임·구글챗·행정실 세 경로가 어떻게 한 칸에 모이는지, 학생을 다른 차로 옮길 때 [계속 유지]와 [오늘만]이 어떻게 다른지 적었습니다.
  - 데이터 백업 가이드에는 **복원할 때 무엇을 잃게 되는지**를 먼저 확인하라는 경고를 넣었습니다.

- **운영 대시보드 최신화**:
  - 반 이름 옆에 **담임 이름**이 함께 나옵니다. 멀리서 보는 화면이라 "G3JU"보다 "G3JU · Ms. June"이 훨씬 빨리 읽힙니다. 담임 계정이 아직 연결되지 않은 반도 명부에 적어둔 이름으로 표시됩니다.
  - 수업 중이 아닐 때는 **교실 위치**(A동 2F 등)를 대신 보여줍니다.
  - 반이 하나도 없을 때 "이 부서에 등록된 반이 없습니다"에서 끝나지 않고 **어디로 가서 무엇을 해야 하는지**까지 안내합니다.
  - 관리 화면 가이드에 3학기 시간표가 이미 채워져 있다는 점과, "지금 수업"이 비어 보일 때 무엇을 확인해야 하는지를 추가했습니다.

## v0.137.0 - 2026-08-22

- **메뉴 전면 재분류 — 주메뉴 10개 → 6개**. 기능이 생길 때마다 최상위에 하나씩 붙인 결과, 성격이 같은 메뉴가 따로 떨어져 있고(실무자 매뉴얼 ↔ 학교 문서함) 항목이 두 개뿐인 메뉴가 최상위 한 칸을 차지하고 있었습니다(학사일정). **"무엇에 대한 일인가"를 기준으로 다시 묶었습니다.**

  | 새 메뉴 | 담긴 것 | 어디서 왔나 |
  |---|---|---|
  | **업무** | 업무 보드 · 업무 보고서 · 지난 업무 · 휴지통 | 하위 화면을 부메뉴로 꺼냈습니다(전에는 보드 안에서만 이동) |
  | **학교** | 학생·교직원 조회 / 학생·반·과목 관리 · 명부 점검 / 학기 · 학사일정 · 학기 준비 · 시간표 | 학교 관리 + 학사일정 + 운영 대시보드 |
  | **셔틀** | 하원 체크표 · 실시간 위치 / 노선 · 탑승 배정 · 배차표 · 링크 관리 | 순서를 "매일 쓰는 것 → 기준정보"로 뒤집었습니다 |
  | **기록** | 사건·회의·행사 · 회의 보고서 / AI 매뉴얼 · 제안함 · 채택예정 | 옛 "운영 관리". 실제로 하는 일이 "기록 → 제안 → 발행"이라 이름을 바꿨습니다 |
  | **문서 · 매뉴얼** | 실무자 매뉴얼 · 매뉴얼 · 운영계획안 / 문서함 · 서류함 · AI 서류 작성 · 보고서 모음 | 최상위였던 실무자 매뉴얼 + 학교 문서함을 합쳤습니다 |
  | **관리** (관리자) | 통합 대시보드 · GIA시스템 · 교육뉴스 / 사용자·공용 계정 / 구글시트 가져오기 · 백업 | 여기저기 흩어져 있던 설정을 모았습니다 |

  - **실무자 매뉴얼은 손이 더 가지 않습니다** — 전화 응대 중 급하게 여는 화면이라, [문서 · 매뉴얼] 카테고리를 **직접 누르면 바로 실무자 매뉴얼이 열립니다.**
  - 앞으로 생길 **급식 당번·시설 예약** 같은 학사 운영 항목은 [학교 > 학사] 아래에 붙습니다. 새 기능을 어디에 넣을지 매번 고민하지 않아도 됩니다.
  - 주간 학생 관찰기록은 요청대로 손대지 않았습니다.

- **명부 점검 화면 신설**: [학교 > 명부 점검]에서 Supabase를 열지 않고도 명부가 제대로 들어갔는지 확인합니다.
  - 위쪽에 **재학생·퇴원·반·교시·시간표·과목 수**가 명부 기준값과 나란히 뜹니다. 값이 맞으면 초록, 다르면 주황, 0이면 "아직 반영 전"입니다. 칸을 누르면 해당 관리 화면으로 갑니다.
  - 아래쪽에 **사람이 판단해야 하는 건**이 모입니다 — 동명이인, 이름은 같은데 생년월일이 다른 경우, 생년월일이 비어 있는 경우. 처리한 뒤 [확인 완료]로 내리면 됩니다.

## v0.136.0 - 2026-08-22

- **2026학년도 3학기 학교 데이터 일괄 적재**. 출처는 `26-27 GIA Primary Roster [Admin Version].pdf` 35쪽 전체입니다.
  - **학기** — 2026학년도 3학기(2026-08-24 ~ 2026-11-13)를 진행중으로 등록했습니다.
  - **반 8개** — G2J·G2C·G2A / G3JU(June)·G3JA(Janelle) / G4R·G4S / G5E. 교실 위치까지 함께 넣었습니다. 명부에 두 G3 반이 똑같이 "G3J"로 적혀 있어 구분되지 않던 문제를 요청대로 두 글자씩 따서 나눴습니다.
  - **학생 100명** — 한글·영문 이름, 성별, 생년월일, 보호자 연락처, 악기, 방과후 여부, 알러지, 형제자매 관계, 교복 사이즈, 입학일까지 전부 반영했습니다.
  - **전출·퇴원 26명** — 퇴원 상태로 넣고 마지막 등원일(`left_on`)을 남겼습니다. 화면에는 안 보이지만 지난 학기 관찰기록·출결이 주인 없는 데이터가 되지 않습니다.
  - **교시 7개 + 반별 시간표 280칸** — 1교시 08:50부터 7교시 16:00까지, 8개 반 × 5일 × 7교시 전부. 이제 운영 대시보드의 "지금 수업"이 실제로 채워집니다.
  - **과목 16개** — 시간표에 나오는 과목을 등록하고, 명부에서 담당이 확인되는 과목(Chinese·Korean History·Computer Science)에는 선생님 이름을 미리 적어뒀습니다.
  - **적재 방식** — 이름과 생년월일로 기존 학생을 찾아 갱신하고 없을 때만 새로 만듭니다. 두 번 적용해도 학생이 두 벌로 늘지 않습니다. **앱에서 손으로 채워둔 값은 지우지 않고**, 명부에 값이 있는 칸만 덮어씁니다(특이사항은 덮어쓰지 않고 뒤에 덧붙입니다).
  - **확인이 필요한 건은 조용히 넘어가지 않습니다** — 동명이인, 이름은 같은데 생년월일이 다른 경우, 명부에 생년월일이 없는 경우를 `wr_import_issues`에 남깁니다. 애매한 채로 덮어쓰면 다른 학생의 기록이 섞이는데 그건 되돌리기가 매우 어렵습니다.
  - 명부의 오류 3건을 정정해 넣었습니다: 곽호율 학생 칸에 겹쳐 들어간 Maya Amelia Dowding의 성, 김서연 입학일 칸의 오타, 김시아 입학연도(2024→2026).

- **가입 화면이 직위에 따라 달라집니다**. 세 직위의 질문을 한꺼번에 늘어놓으면 대부분 자기와 상관없는 칸이라 무엇을 채워야 할지 헷갈립니다.
  - **교사** — 담임반과 담당 과목을 직접 고릅니다. **담임이 아직 비어 있는 반만** 목록에 나오므로 남의 반을 고를 수 없고, 각 반 옆에 학교 명부의 담임 이름("Ms. Jaime")이 함께 보입니다. 입력한 이름과 명부 이름이 겹치면 그 반을 한 번에 선택하는 버튼이 뜹니다.
  - **행정직원 · 관리자** — 반/과목 대신 담당 업무를 적습니다. [내 계정 설정]에서 나중에 바꿀 수 있습니다.
  - 관리자는 지금처럼 [학교 관리 > 반 관리]와 [과목반 세팅]에서 언제든 바로잡을 수 있습니다.

## v0.135.0 - 2026-08-22

- **운영 대시보드 메뉴 이동 + 관리자 전용**: 사이드바 맨 위 독립 메뉴였던 [운영 대시보드]를 **[운영 관리] 안으로** 옮겼습니다. 매일 열어보는 화면이 아니라 한 번 설정해두고 두는 성격이라 상단 자리를 차지할 이유가 없었습니다. 이제 관리자·개발자에게만 보이고, 주소를 직접 입력해 들어오더라도 화면 자체에서 다시 막습니다. 이 화면은 **로그인 없이 열리는 주소를 만들어내는 곳**이라, 주소가 새어나가면 누구나 결석 명단과 업무 현황을 볼 수 있게 되기 때문입니다.

- **하원 시간 전체화면 전환 + 자동 복귀**:
  - 하원 시각(기본 16:00)이 되면 대시보드가 **전체화면으로 커져 옆의 CCTV를 덮습니다.** 종료 시각(기본 17:30)이 되면 **저절로 전체화면이 풀리고** 원래 반반 배치로 돌아옵니다.
  - 하원 화면 오른쪽 위에 **[하원 종료]** 버튼을 뒀습니다. 마지막 차가 일찍 복귀한 날에는 종료 시각까지 기다리지 않고 바로 되돌릴 수 있습니다. 오늘 하루만 종료되고, 잘못 눌렀으면 평소 화면 위쪽 [하원 화면 다시 열기]로 되돌아옵니다.
  - 종료 시각은 [운영 관리 > 운영 대시보드]의 링크 목록에서 시작 시각 옆에 함께 정합니다.
  - **한 가지 알아두실 점** — 전체화면으로 *들어가는* 것만은 브라우저 규칙상 사람이 한 번 눌러야 합니다(아무 사이트나 시간이 되면 마음대로 화면을 덮지 못하게 하는 안전장치입니다). 하원 시각이 되면 오른쪽 아래에 [전체화면] 버튼이 떠서 한 번만 눌러주시면 되고, 급할 때는 무시하고 반반 화면 그대로 쓰셔도 됩니다. 되돌아오는 것은 제약이 없어 완전히 자동입니다.
  - 예전에는 시작 시각만 있어서 한 번 하원 화면이 되면 **자정까지 그대로 남아 있었습니다.** 종료 시각이 생기면서 이 문제도 함께 해결됩니다.

## v0.134.1 - 2026-08-21

- **교사 화면에서 통합 검색창 제거**: 이 검색은 사건기록·회의·업무·학교문서를 훑는 통합 검색인데, 교사는 그 화면들에 애초에 들어갈 수 없어 무엇을 쳐도 결과가 나오지 않았습니다. 사이드바와 모바일 상단 양쪽에서 감췄고 달력만 남겼습니다.
- **짧은 주소 오류 메시지 개선**: 짧은 주소 칸이 아직 DB에 반영되기 전에 저장하면 알아볼 수 없는 영문 오류가 떴습니다. 이제 "아직 DB에 반영되지 않았습니다. 1~2분 뒤 새로고침하고 다시 시도해주세요."로 안내합니다. 같은 상황에서 [링크 만들기]가 통째로 막히던 문제도 고쳤습니다 — 짧은 주소 없이 먼저 만들어 두고 나중에 코드만 채우면 됩니다.

## v0.134.0 - 2026-08-21

- **운영 대시보드 짧은 주소**: 지금까지 대시보드 주소는 36자리 토큰이 붙어 있어 다른 컴퓨터에서 주소창에 직접 치는 것이 사실상 불가능했습니다. 이제 **`우리주소/d/코드`** 네 글자로 바로 열립니다(안내보드의 `/b/`와 같은 방식, d = dashboard).
  - [운영 대시보드 · 시간표 관리] 링크 목록의 `/d/` 칸에서 코드를 확인하고 원하는 이름(`office` 등)으로 바꿀 수 있습니다. [짧은 주소 복사] 버튼도 함께 있습니다.
  - 이미 만들어 둔 링크에도 코드가 자동으로 하나씩 채워집니다.
  - 코드 글자에서 `0`·`O`·`1`·`l`을 뺐습니다 — 눈으로 보고 옮겨 치는 값이라 가장 흔한 실수가 0과 O를 헷갈리는 것입니다.
  - 토큰을 짧게 바꾸는 대신 지름길을 하나 더 두는 방식입니다. 토큰 자체를 짧게 하면 아무나 몇 번 찍어보는 것만으로 열 수 있게 되기 때문입니다. 사용을 멈추려면 지금처럼 [중지] 한 번이면 됩니다.
- **공용 계정 비밀번호 길이 제한 해제**: 앱에서 걸어두었던 10자 이상 조건을 없앴습니다. 도서관 노트북처럼 여러 사람이 하루에도 몇 번씩 입력하는 계정은 짧고 외우기 쉬운 편이 실제로 더 안전합니다 — 길고 복잡하게 강제하면 결국 모니터에 붙여두게 되기 때문입니다. (Supabase 프로젝트 설정에 정해진 최소 길이보다 짧으면 Supabase가 거절하고 그 메시지가 화면에 그대로 표시됩니다.)

## v0.133.0 - 2026-08-21

- **화면 언어 전환(한국어 ↔ English)**: 지금까지는 한글 옆에 영어를 작게 병기했는데, 병기는 화면이 두 배로 길어지고 원어민 선생님께는 여전히 한글이 먼저 눈에 들어왔습니다. 이제 **화면 전체가 한 언어로 완전히 바뀝니다.**
  - 사이드바 맨 아래(모바일은 상단 메뉴 오른쪽) [한국어 / English] 버튼으로 누구나 직접 바꿉니다. 선택은 이 브라우저에 1년간 저장되어 다음 로그인에도 유지됩니다.
  - 바뀌는 범위 - 메뉴, 내 담임반, 내 담당과목, 리포트 작성 모달(뱃지·항목 이름·안내문 포함), 내 반 픽업 체크, 출석부, 문의및건의사항, 내 계정 설정, 각 화면의 ❓사용 가이드.
  - **영어 화면에서는 학생의 영어 이름이 크게 나옵니다**(한글 이름은 작게 아래로). 원어민 선생님이 한글 이름만 보고 학생을 찾기 어려웠던 문제를 없앴습니다.
  - 소속·직위·출결상태처럼 DB에 한글로 저장되는 값은 저장은 그대로 두고 화면 표시만 번역합니다. 영어로 가입해도 권한 판정은 언어와 무관하게 똑같이 동작합니다.
- **가입 절차 영어 대응**: 로그인·최초 정보 입력(이름/소속/직위)·승인 대기 화면 모두 언어 전환 버튼이 붙었습니다. 원어민 선생님이 앱을 처음 만나는 순간부터 영어로 읽힙니다.

- **아이디·비밀번호 로그인 신설**: 로그인 화면 아래 [공용 계정으로 로그인]을 열면 아이디+비밀번호로 들어갈 수 있습니다. 아이디만 입력하면 `@giamicro.com`이 자동으로 붙습니다. 개인 교직원 계정은 지금처럼 회사 구글 계정으로 로그인합니다.
- **공용 계정 관리 화면**(관리자 > 공용 계정 관리): 계정을 만들고 비밀번호를 정하거나 바꿉니다.
  - [자동 생성]을 누르면 브라우저가 예측 불가능한 16자리를 만듭니다. `0`과 `O`처럼 헷갈리는 글자는 빠져 있어 종이에 적어 전달하기 좋습니다.
  - **비밀번호는 한 방향으로만 저장되어 관리자도 다시 꺼내볼 수 없습니다.** 저장 직후 한 번만 크게 표시되니 그때 적어두셔야 하고, 잊어버리면 새로 정하면 됩니다.
  - 쓰지 않게 된 계정은 지우지 말고 [사용중지]로 잠급니다(지우면 그 계정이 남긴 기록의 연결이 끊깁니다).

- **신입교사 오리엔테이션용 데모 계정 + 더미 학생**: `gia-demo` 계정으로 로그인하면 **교사 화면이 실제와 완전히 똑같이 보이지만 학생은 전부 가짜**입니다. 마음껏 눌러보고 저장해도 실제 기록에는 아무 영향이 없습니다.
  - 3학년 Demo 반 학생 8명. 설명할 거리가 나오도록 상황을 섞었습니다 - 알러지가 있는 학생, 악기 수업을 듣는 학생, 방과후가 있는 학생, 형제자매가 함께 다니는 경우, 셔틀을 타지 않는 학생.
  - **미리 작성된 관찰기록 두 건**(발행 완료 1건 · 임시저장 1건)이 들어 있어, "어느 정도로 적으면 되는지"를 완성된 예시로 바로 보여줄 수 있습니다.
  - 픽업 체크 화면에는 6명이 데모 노선에 배정되어 있고 2명은 셔틀 미탑승이라, 두 경우가 어떻게 다르게 보이는지 함께 설명됩니다.
  - 화면 위에 **노란 띠로 "연습용 계정입니다"**가 항상 떠 있어, 설명을 듣는 분이 실제 기록을 건드리고 있다고 오해하지 않습니다.
  - **격리 방식**: 표를 새로 만들지 않고 기존 표에 `is_demo` 칸 하나만 붙이고 보안규칙(RLS)으로 갈랐습니다. 데모 계정에는 데모 행만, 나머지 모두에게는 실제 행만 보여 서로를 아예 볼 수 없습니다. 화면 코드는 하나 그대로라 "데모에서는 되는데 실제에서는 안 되는" 차이가 생기지 않습니다.
  - 학생 명부 관리 화면과 사무실 대형 모니터 대시보드에는 데모 학생이 나타나지 않습니다(대시보드는 보안규칙을 통과하는 토큰 링크라 조회 조건을 직접 붙였습니다).

## v0.132.0 - 2026-08-21

- **병합 시 정리**: 도서관 브랜치를 운영앱에 합치면서 `supabase/schema.sql`(전체 구조 참고 문서)에 붙여넣기 하다 남은 군더더기 두 줄(`end if; end $$;`)을 지웠습니다. 이 오류로 참고 문서 전체가 문법 검사에서 실패하던 상태였습니다. **실제로 DB에 적용되는 마이그레이션 파일에는 없던 문제라 데이터에는 영향이 없습니다.**

- **도서관 구역(책장 위치) 체계 신설**: 지금까지 책 위치는 자유 입력 글자 한 칸이라 사람마다 다르게 적으면 검색이 안 됐습니다. 구역을 별도 표(`lib_locations`)로 올리고 책에 연결합니다.
  - 구역 이름 체계는 학교가 정하는 대로 자유롭게 씁니다(`A-1` 같은 칸 번호도, `그림책` 같은 분류도 가능).
  - **도서관 배치도**: 구역마다 평면도 좌표(`map_x/y/w/h`)를 담아, 화면에서 네모를 끌어다 놓으면 배치도가 완성됩니다. 책을 찾을 때·반납한 책을 꽂을 때 그 자리가 반짝입니다.
  - **반납 정리**: `lib_loans.reshelved_at`을 두어 "반납받았지만 아직 안 꽂은 책"을 구역별로 묶어 보여줍니다. 반납함에 들어온 책도 같은 목록에 나타납니다.
  - 책장 칸에 붙이는 구역 라벨(`LOC-A-1`)을 찍고 책을 이어서 찍으면 한꺼번에 배정됩니다.
- **도서카드 배경 그림·사진**: 배경 그림과 학생 사진을 Supabase 저장소(`library` 버킷)에 올리고, `lib_settings`/`lib_student_photos`에 주소를 남깁니다. 바코드는 배경이 어떤 그림이든 **항상 흰 바탕 위에** 얹혀 인쇄되어 스캔이 됩니다.
- **이름으로도 대출 가능**: 도서관 앱 스캔칸에 학생 이름을 입력하면 명부에서 찾아주고, 동명이인이면 골라서 진행합니다. 표 변경 없이 도서관 앱에서만 처리합니다.

## v0.131.0 - 2026-08-20

- **도서관 시스템(gia-lib) 기반 신설**: 대출·반납 화면은 별도 앱(`gia-lib-web`)으로 만들되 **데이터는 이 프로젝트와 같은 DB를 씁니다**. 이 저장소에는 표와 보안규칙만 추가됩니다.
  - 새 표 - `lib_books`(장서) / `lib_loans`(대출·반납 기록) / `lib_visits`(도서관 입실, 2단계용으로 표만 미리 준비) / `lib_settings`(대출 규칙 - 2주 · 1인 3권 · 연장 +7일 1회 · 연체 시 대출 정지).
  - **학생은 이미 있는 `student_no`(GIA-2026-0001)를 그대로 카드 바코드로 씁니다.** 학년·반이 바뀌어도 번호는 그대로라 카드를 다시 만들 필요가 없고, 나중에 출결·행사입장·매점으로 확장해도 **같은 카드**를 씁니다.
  - **도서관용 공용 명부(`lib_students`) 신설**: 고유번호·이름·영어이름·학년·반·부서만 담았습니다. 보호자 연락처·주소·생년월일·알러지 등은 넘어가지 않습니다. 교직원 공용 명부(`wr_students_basic`)에는 개인정보 보호를 위해 학번이 빠져 있어 도서카드 조회가 불가능하므로 별도로 만들었습니다.
  - 책은 뒷면에 인쇄된 **ISBN 바코드를 그대로 식별번호로 씁니다**(라벨 부착 작업 없음). ISBN이 없는 책만 자체 라벨(GIA-B-00001)을 `lib_next_item_code()`로 발급합니다.
- **도서관 전용 가계정 권한 분리**: 도서관 노트북은 `gia-library@giamicro.com` 같은 전용 계정으로 하루 종일 로그인된 채 열려 있습니다. 이 계정이 운영앱 데이터까지 볼 수 있으면 사건기록·보호자 연락처가 그대로 노출되므로 두 겹으로 막았습니다.
  - `is_giamicro_user()`가 `gia-library`로 시작하는 계정을 제외하도록 바꿨습니다. 운영앱의 정책들이 전부 이 함수를 쓰고 있어서 **함수 한 곳만 고치면 모든 표가 자동으로 차단됩니다.** 도서관 표는 새로 만든 `is_lib_user()`를 씁니다.
  - 미들웨어에서도 운영앱 화면 진입 자체를 막고, 로그인 화면에 "도서관 전용 계정은 운영앱에 접속할 수 없습니다" 안내를 띄웁니다.
  - 계정을 더 만들 때는 `gia-library2@giamicro.com`처럼 `gia-library`로 시작하게만 지으면 같은 규칙이 자동 적용됩니다.
  - **구글 계정을 새로 만들 필요가 없습니다**. 도서관 노트북은 Supabase Auth의 아이디+비밀번호 가계정으로 들어옵니다. 이 가계정을 `app_users`에 미리 등록해 두어 **운영앱 계정 관리 화면에 "GIA 도서관(공용 단말)"로 함께 나타나고, 승인을 취소하면 도서관 노트북도 즉시 막힙니다.**

## v0.130.1 - 2026-08-21

- **운영 대시보드 시계에 초 표시**: 그동안은 서버에서 받은 시각을 그대로 보여줘서 갱신 주기(30초)마다만 바뀌었는데, 이제 화면에서 1초마다 도는 시계로 바꿨습니다. 하원 운행 화면 상단에도 같은 시계를 넣었습니다. 노트북 시간대 설정과 무관하게 항상 한국시간으로 표시되고, 숫자 폭이 고정되어 초가 바뀔 때 글자가 흔들리지 않습니다.

## v0.130.0 - 2026-08-20

- **교사 화면 한영 병기 원칙 적용**: 원어민 선생님도 통역 없이 쓸 수 있도록 교사가 보는 모든 화면에 영어를 함께 적었습니다.
  - 사이드바 메뉴(내 담임반 My Homeroom · 내 담당과목 My Subjects · 내 반 픽업 체크 My Class Pickup Check), 로그아웃 Sign out.
  - 화면 제목·안내문·가이드·빈 화면 문구까지 모두 병기했습니다. 예: "담임으로 배정된 반이 없습니다 / No homeroom class is assigned to you."
  - 픽업 체크 버튼도 "픽업 Pickup", "결석 Absent"처럼 병기하고, 셔틀을 타지 않는 학생은 "셔틀 미탑승 No shuttle"로 표시합니다.
- **교사도 문의 창구를 쓸 수 있게 개방 + 한영 병기**: 그동안 문의및건의사항은 교사에게 아예 보이지 않았는데, 이제 사이드바 아래(모바일은 상단 메뉴 오른쪽)에서 열 수 있습니다. 카테고리(🐞 오류 Bug · 💡 기능제안 Idea · 💬 기타 Other), 입력 항목(제목 Title · 내용 Details), 처리 상태(접수 Received · 처리중 In progress · 완료 Done)를 모두 병기했고, **한국어와 영어 아무 쪽으로 써도 된다**는 안내를 넣었습니다.

## v0.129.0 - 2026-08-20

- **교사용 '내 반 픽업 체크' 신설**: 학부모님께 전화·문자로 직접 픽업 연락을 받으셨을 때 담임 선생님이 바로 체크할 수 있는 화면입니다.
  - **담임에게만 보입니다.** 담임(또는 부담임)으로 배정된 반이 있는 선생님께만 메뉴가 나타나고, 과목만 맡으신 선생님께는 아예 뜨지 않습니다. 보이는 학생도 내 담임반 학생뿐입니다.
  - 이름 옆 버튼으로 **픽업·결석**을 누르면 즉시 저장되고, 한 번 더 누르면 취소됩니다. 셔틀을 타지 않는 학생은 회색으로 표시되고 버튼이 없습니다.
  - 저장되는 곳이 하원 체크표와 같아서, 누르는 즉시 **안내보드·차량 도착체크·하원 운행 화면에 반영**됩니다. 행정실에서 먼저 체크하면 선생님 화면에도 실시간으로 나타납니다.
  - **기존 구글챗 자동 반영과 병행합니다.** 같은 칸을 쓰기 때문에 두 방식이 충돌하지 않고, 구글챗으로 알리신 건은 지금처럼 자동으로 반영됩니다.
- **교사 메뉴에서 실시간 셔틀 제거**: 교사 사이드바에서 실시간 셔틀이 빠지고 그 자리에 픽업 체크가 들어갑니다. 화면 접근도 함께 막았습니다. 관리자·행정직원은 기존대로 실시간 셔틀을 볼 수 있습니다.

## v0.128.0 - 2026-08-20

- **하원 운행 화면 신설**: 운영 대시보드가 설정 시각(기본 16:00)이 되면 안내보드 대신 **하원 운행 전용 화면**으로 바뀝니다.
  - **위쪽 - 전체 셔틀 실시간 지도**: 모든 하원 노선을 한 지도에 올리고, 기사님 휴대폰(Traccar)에서 들어오는 최신 위치로 차량 마커가 움직입니다. 노선마다 색을 달리하고, **운행 중인 노선은 경로선을 진하게** 그려 지금 어디를 지나는지 한눈에 보입니다. 실도로 경로가 계산되어 있으면 그 경로를, 없으면 정류장을 이은 선을 씁니다.
  - **아래쪽 - 하원차량 체크**: 노선별로 대기·도착함·운행중 상태와 **탑승 진행률(탑승/예정)**, 아직 안 탄 학생 이름, 도착·출발 시각을 보여줍니다. 픽업·결석 학생은 탑승 대상에서 자동으로 빠집니다.
  - **자동 감지 표시**: 도착·출발이 사람이 눌러서 기록된 것인지 GPS가 자동으로 잡은 것인지 "·자동"으로 구분해 표시하고, 노선별 GPS 수신 여부도 함께 보여줍니다.
  - 10초마다 자동 갱신되며, 전환할 안내보드를 따로 고르지 않아도 시각만 지나면 바로 전환됩니다.

## v0.127.1 - 2026-08-20

- **셔틀 배정표의 보호자 연락처도 보호**: 학생 명부는 앞 버전에서 막았지만, **같은 보호자 연락처가 셔틀 배정표에도 한 벌 더 들어 있었습니다.** 이 표는 동승선생님이 교사 계정일 수 있어 조회를 열어둔 상태여서, 화면에는 안 보여도 데이터로는 교사가 읽을 수 있었습니다. 이제 원본 배정표는 행정직원·관리자·개발자만 읽을 수 있고, 하원 체크표·실시간 셔틀은 보호자 연락처가 빠진 공용 배정표를 보도록 바꿨습니다. 동승선생님 업무에는 변화가 없습니다.

## v0.127.0 - 2026-08-20

- **학생명부 권한 정리**: 지금까지 학생 명부는 giamicro.com 계정이면 **누구나 읽고 고칠 수 있는** 상태였습니다. 이제 원본 명부는 **행정직원·관리자·개발자만** 읽고 쓸 수 있고, 반 명부도 수정은 같은 권한으로 제한했습니다(반 이름 읽기는 수업·출결 화면이 필요해서 전체 허용).
- **교직원 공용 명부 신설**(요청 항목 그대로): 일반 교직원은 새로 만든 공용 명부에서 **이름·영어이름, 생년월일, 성별, 방과후 수업 여부, 악기, 셔틀 탑승 여부, 특이사항(알러지·메모), 형제자매 링크**만 볼 수 있습니다. **보호자 연락처·이메일·주소·좌표·학번은 빠졌습니다.** 관찰기록·출결·셔틀·검색 화면은 자동으로 이 공용 명부를 보도록 바꿔서 기존 사용에는 변화가 없습니다.
- **새 항목 추가**: 방과후 수업 여부, 악기(첼로·우쿨렐레·클라리넷·바이올린·플룻 중 하나), 형제자매 묶음. 형제자매 묶음은 같은 집 아이들에게 같은 값을 넣어두면 **부서를 넘나들어도(유치부 동생 ↔ 초등부 형) 한 가족으로 이어집니다.**
- **다른 프로그램 연결 대비**: 나중에 유치부 프로그램을 붙일 때 이 공용 명부만 보게 하면 개인정보를 넘기지 않고 학생을 연결할 수 있습니다.
- 개발자 계정은 `is_app_admin()`에 항상 포함되어 있어, 앞으로 만드는 권한에서도 별도 처리 없이 최상위로 동작합니다.

## v0.126.1 - 2026-08-20

- **부서 구분을 명시적으로 저장**: 지금까지는 학년 표기의 글자 모양을 보고 부서를 추측했는데, 표기가 조금만 달라져도 엉뚱하게 분류될 수 있었습니다. 학생·반에 **부서 칸(유치부/초등부/중고등부)을 따로 추가**해서 앞으로는 추측 없이 이 값만 보고 판단합니다. 기존 데이터는 예전 규칙대로 한 번 자동으로 채워집니다.
- **유치부 화면에서 분리**: 운영 대시보드와 시간표 관리 화면의 부서 선택에서 유치부가 빠지고 초등부·중고등부만 보입니다. **데이터는 지우지 않고 그대로 보관**하므로, 나중에 유치부 프로그램을 만드실 때 그대로 쓰실 수 있고 다시 보이게 하는 것도 한 줄 수정이면 됩니다.
- **대시보드 '지금 수업'을 학년별로 묶어 표시**: 학년이 왼쪽에 세로 라벨로 붙고 그 학년의 반들이 오른쪽에 나란히 놓여, 학년 단위로 훑어보기 쉬워졌습니다. 학년 정렬도 1·2·3학년 순서대로 맞췄습니다(중1은 초6 다음으로).

## v0.126.0 - 2026-08-20

- **사무실 대형 모니터용 통합 운영 대시보드 신설**. 화면 절반에 띄우는 것을 전제로, 멀리서도 읽히는 큰 글씨의 어두운 화면으로 만들었습니다. 30초마다 자동 갱신되어 하루 종일 켜두면 됩니다.
  - **지금 수업**: 부서별 교시 시각을 기준으로 "지금 몇 교시"를 판단해, 각 반이 무슨 수업 중인지와 다음 교시 과목을 함께 보여줍니다.
  - **오늘 출결·픽업**: 결석·지각·조퇴 학생을 상태별 색으로 표시하고, 보호자 연락 전인 학생은 따로 표시합니다. 하원 체크표에서 픽업으로 찍힌 학생도 함께 보여줍니다.
  - **오늘 업무**: 기한이 지난 업무 → 오늘 마감 → 오늘 등록 순으로 정렬하고 긴급은 위로 올립니다. 상단에 상태별 전체 건수도 함께 표시합니다.
  - **부서 전환**: 화면에서 유치부·초등부·중고등부를 눌러 바꿀 수 있습니다.
  - **하원 시간 자동 전환**: 설정한 시각(기본 16:00)이 되면 화면 전체가 하원 차량 안내보드로 바뀝니다. 전환 시각과 띄울 안내보드는 관리자 화면에서 고릅니다.
  - **로그인 불필요**: 안내보드와 같은 토큰 링크 방식이라 하루 종일 켜두어도 세션이 풀리지 않습니다.
- **시간표 기능 신설**: "지금 각 반이 무슨 수업인지"를 알려면 필요한 데이터가 시스템에 없어서 새로 만들었습니다. 왼쪽 메뉴 **운영 대시보드**에서 부서별 교시(시작·종료 시각)를 정하고, 반×요일×교시 표에 과목명을 채워 넣으면 됩니다. 칸을 비우면 지워집니다. **아직 데이터는 비어 있으니 시간표를 넣어주시면 대시보드에 바로 반영됩니다.**

## v0.125.1 - 2026-08-20

- **Supabase 스키마 자동 반영**: 그동안은 새 기능을 배포할 때마다 SQL을 Supabase SQL Editor에 직접 붙여넣어야 했는데, 이제 main에 머지되면 GitHub Actions가 `supabase/migrations/` 폴더에서 **아직 반영되지 않은 파일만** 골라 자동으로 실행합니다. 이미 실행한 파일은 DB에 기록이 남아 두 번 실행되지 않습니다.
  - 앞서 배포한 v0.122·v0.124·v0.125 SQL을 첫 마이그레이션 파일로 정리해뒀습니다. 모두 "없으면 만들기" 방식이라 이미 실행했더라도 다시 돌려도 문제가 생기지 않습니다.
  - `supabase/schema.sql`은 전체 구조를 한눈에 보는 참고 문서로 그대로 유지합니다.
  - **저장소 Settings → Secrets and variables → Actions 에 `SUPABASE_DB_URL` 시크릿을 등록해야 동작합니다.** 등록 전까지는 워크플로가 실패하지 않고 안내만 남기고 넘어갑니다.

## v0.125.0 - 2026-08-20

- **업무 보드 전체공지**:
  - **상단 배너**: 부서 탭 바로 아래, 업무 화면 맨 위에 📢 공지 배너가 뜹니다. 항상 **가장 최근 공지 하나만** 표시되므로, 새 공지를 올리면 이전 공지는 자동으로 내려갑니다(따로 내리는 조작이 필요 없습니다).
  - **개인별 접기**: 배너를 누르면 접혔다 펴집니다. 접은 상태는 **사람마다 따로** 저장되어 다른 분 화면에는 영향이 없고, 다른 기기에서 로그인해도 그대로 유지됩니다. 접어둔 뒤 새 공지가 올라오면 그 공지는 다시 펼쳐진 채로 보여서 새 공지를 놓치지 않습니다.
  - **히스토리**: 배너 오른쪽 끝 시계 아이콘을 누르면 지난 공지를 최신순으로 모아 봅니다. 현재 상단에 떠 있는 공지에는 '현재' 표시가 붙습니다. 이전 공지는 지워지지 않고 모두 보관됩니다.
  - **공지 올리기**: 관리자·행정직원에게만 "✏️ 공지" 버튼이 보입니다. 올릴 때 **전체 부서 공통**인지 **지금 보고 있는 부서만**인지 고를 수 있고, 제목과 내용(선택)을 적으면 됩니다. 잘못 올린 공지는 "내리기"로 감출 수 있습니다.
  - 다른 사람이 공지를 올리면 새로고침 없이 실시간으로 화면에 뜹니다.

## v0.124.0 - 2026-08-13

- **기사님 휴대폰 GPS 추적 도입 (Traccar Client 연동)**: 웹페이지는 아이폰 사파리 특성상 백그라운드에서 위치를 보낼 수 없어서, 무료 오픈소스 앱인 **Traccar Client**가 우리 서버로 위치를 직접 보내도록 연동했습니다(`/api/shuttle/track`). 기사님은 최초 1회 설정 뒤로는 아무 조작도 하지 않으시고, 네비 화면은 전혀 가려지지 않습니다. 관리자 화면(셔틀 → 실시간 위치 링크 관리)에서 노선별 기기 ID를 발급하고 설치 안내를 볼 수 있습니다.
- **개인정보 최소 수집**: 기사님 개인 휴대폰을 쓰는 방식이라, **하원 시간대(평일 12~20시) 밖의 위치는 서버가 아예 저장하지 않습니다.** 앱은 24시간 켜져 있어도 그 밖의 좌표는 받는 즉시 버립니다. 도입 전 기사님께 이 내용을 설명하고 개인정보 수집·이용 동의를 받아두시기 바랍니다.
- **학교 도착 자동감지**: 새 크론(`/api/cron/shuttle-auto-arrive`)이 차량이 학교 반경 100m 안에 들어와 1분 이상 머무르면 사람이 누르지 않아도 "현장도착"을 자동으로 기록합니다. 이어서 기존 출발 자동감지가 "출발"을 채워, 하원 한 바퀴가 조작 없이 완결됩니다. **외부 스케줄러(cron-job.org 등)에 `/api/cron/shuttle-auto-arrive`를 1분마다 호출하도록 하나 더 등록해주셔야 합니다**(기존 출발 자동감지와 동일한 방식, `Authorization: Bearer {CRON_SECRET}` 헤더 필요).
- **정류장 좌표 GPS 자동 학습**: 새 크론(`/api/cron/shuttle-learn-stops`, 매일 자동 실행)이 그날 주행 기록에서 차가 실제로 45초 이상 멈춰 있던 자리를 찾아내 기록합니다(신호 대기는 걸러집니다). 같은 자리가 반복 관측될수록 평균 좌표가 정확해지고, 관리자 화면에서 "기존 좌표와 몇 m 차이"까지 확인한 뒤 "반영" 버튼으로 실제 정류장 좌표에 옮길 수 있습니다. 기존 좌표를 임의로 덮어쓰지 않으므로 잘못 학습돼도 원래 값을 잃지 않습니다. 어느 정류장인지 자동으로 연결되지 않은 정차 지점은 따로 모아 보여주므로, 직접 지정하거나 누락된 정류장을 발견할 수 있습니다.

## v0.123.0 - 2026-08-12

- **동승선생님 화면: 위치 전송 완전 자동화**: 동승선생님 체크인 화면에서 더 이상 "이동 시작/운행 종료" 버튼을 누르지 않아도, 학교 '현장도착'이 확인되면 자동으로 위치 전송이 시작되고, 오늘 실제 학생이 있는 마지막 정류장 근처(약 100m, 3회 연속 확인)에 닿으면 자동으로 멈춥니다. 자동 감지가 늦거나 안 될 때만 쓰시라고 작은 예비 버튼은 남겨뒀습니다. 위치는 이전과 동일하게 학부모님께는 전달되지 않고, 담당 직원의 도착체크·안내보드·자동출발 감지에만 쓰입니다.
- **백그라운드(네비 앱 사용 중) 보완**: 완전한 백그라운드 위치 전송은 브라우저 특성상(특히 아이폰 사파리) 불가능하지만, 기사님·동승선생님이 네비 앱 등으로 전환했다가 화면으로 돌아오는 순간 즉시 한 번 더 전송해서 끊긴 공백을 최대한 빨리 메웁니다.
- **부수 정리**: 동승선생님 화면이 더 이상 "출발" 이벤트를 직접 기록하지 않도록 바꿔, v0.122.0에서 추가한 "노선당 하루 1개" 제약(학교 도착체크·GPS 자동감지 크론 전용)과 충돌하지 않게 했습니다. 혹시 모를 충돌에 대비해 관련 API도 중복 기록을 에러 대신 정상 처리하도록 보강했습니다.
- **도착체크(교직원용) 화면에 차량번호 표시**: 각 차량 카드에 노선명 아래 차량번호를 작은 글씨로 함께 표시합니다.

## v0.122.0 - 2026-08-11

- **도착체크: 출발 자동감지**: 새 크론(`/api/cron/shuttle-auto-depart`)이 두 가지 신호로 "출발"을 자동으로 채웁니다. ① 그 노선에서 파일럿(GPS) 체크인이 켜져 있으면, 학교에서 100m 이상 멀어진 최근 위치가 잡히는 순간 실제 출발로 보고 자동 처리합니다(도착 후 최소 90초는 기다려 오작동을 막습니다). ② GPS 핑이 아예 없는 노선은 "도착함" 후 20분이 지나면 화면 정리 차원에서 자동 처리합니다. 자동으로 처리된 카드에는 "자동·GPS 감지" 또는 "자동·시간 초과"로 작게 표시해 사람이 누른 것과 구분됩니다. **적용하려면 poll-chat-messages와 같은 방식으로 외부 무료 스케줄러(cron-job.org 등)에 `/api/cron/shuttle-auto-depart`를 1분마다 호출하도록 하나 더 등록해주셔야 합니다** (기존 가이드와 동일한 방식, `Authorization: Bearer {CRON_SECRET}` 헤더 필요).

## v0.121.3 - 2026-08-11

- **출결내역 @멘션 필터링 개선**: 구글챗 멘션이 "@Teneqha Form Jino Park will be absent tomorrow"처럼 영문 두 단어(이름+성)로 된 경우, 이전에는 "@" 뒤 첫 한 단어("Teneqha")만 지워져 두 번째 단어("Form")가 그대로 남아 있었습니다. 이제 대문자로 시작하는 영문 단어가 바로 이어지면 최대 두 단어까지 함께 지우고(뒤에 이어지는 진짜 이름은 건드리지 않음), 한글 멘션은 원래대로 한 단어만 지운 뒤 이름 유추를 진행합니다.

## v0.121.2 - 2026-08-11

- **안내보드, 전자칠판 내장 브라우저에서 스타일이 안 먹던 문제 대응**: 전자칠판 내장 브라우저는 오래된 엔진인 경우가 많아, 앱이 쓰는 최신 CSS 문법을 못 읽으면 스타일이 통째로 빠지고 글자만 남을 수 있습니다. 원인이 되는 정확한 브라우저 종류를 특정하기 어려워, 안내보드의 핵심 화면(배경·차량 도착 카드·학생 이름)에 어떤 브라우저든 항상 그대로 읽는 순수 인라인 스타일을 함께 넣어, 외부 스타일시트가 전혀 안 먹는 경우에도 최소한 배경·글자 크기·강조색은 보이도록 안전장치를 추가했습니다.

## v0.121.1 - 2026-08-11

- **홈 화면 추가 안내는 처음 한 번만**: 도착체크 화면의 홈 화면 추가 안내 배너가 며칠마다 다시 뜨던 것을, 기기당 처음 한 번만 보여주고 이후엔 뜨지 않도록 바꿨습니다.
- **전체 리셋 버튼을 리셋·안내 아이콘으로 분리**: "⟲ 전체 리셋" 글자 버튼을 절반씩 나눠, 왼쪽 ⟲ 아이콘은 누르면 바로 리셋되고 오른쪽 ? 아이콘은 화면 사용법(버튼 누르는 순서, 꾹 눌러 기사님께 전화, 리셋 의미 등)을 안내하는 팝업만 띄웁니다.
- **출결내역 형제자매 이름 대조 개선**: 형제자매 중 한 명만 성을 쓰고 나머지는 이름만 쓴 경우("박라온, 다온")와, 성 없이 이름끼리 붙여 쓴 경우("라온다온") 모두 명부와 대조해 각각 따로 인식하도록 로직을 보강했습니다. 명부에 실제로 있는 이름 조합일 때만 인정하므로, 엉뚱한 낱말이 이름으로 잘못 잡힐 위험은 낮습니다.

## v0.121.0 - 2026-08-11

- **교직원 도착체크 링크, 홈 화면에 더 쉽게 추가**:
  - **QR코드**: 링크 관리 화면(관리자)에서 "📱 QR코드" 버튼을 누르면 그 자리에서 QR코드가 생성되고, 이미지로 저장해 카톡방 등에 바로 공유할 수 있습니다. 상대방은 카메라로 스캔만 하면 주소를 직접 입력하거나 복사·붙여넣기 하지 않아도 링크가 바로 열립니다.
  - **홈 화면 추가 자동 안내**: 도착체크 화면을 처음 열면(아직 홈 화면에 추가하지 않은 경우) 아이폰은 "공유 버튼 → 홈 화면에 추가", 안드로이드는 "⋮ 메뉴 → 홈 화면에 추가"를 화면 상단에 자동으로 안내해줘서, 말로 하나하나 설명하지 않아도 화면을 보고 따라 할 수 있습니다. 이미 홈 화면 아이콘으로 실행 중이면 뜨지 않고, 닫으면 며칠간 다시 뜨지 않습니다.

## v0.120.4 - 2026-08-11

- **교직원 도착체크에도 픽업·결석 상태 반영**: 안내보드에는 이미 반영되고 있었지만, 도착체크 화면은 학생 이름만 보여줄 뿐 픽업·결석 여부를 몰라서 이미 픽업/결석 처리된 학생도 계속 "미도착" 빨간 뱃지로 남아 있었습니다. 이제 도착체크도 안내보드와 같은 방식으로 shuttle_boardings의 상태를 함께 조회해, 픽업·결석 학생은 명단에서 빠지고 카드 아래에 "픽업 N · 결석 N"으로만 작게 표시됩니다. 두 화면 모두 3초 폴링이라, 체크표에서 체크하면 몇 초 안에 자동으로 반영됩니다.

## v0.120.3 - 2026-08-11

- **버그 수정: 교직원 도착체크 단독링크를 아이폰 홈 화면에 추가하면 그 링크가 아니라 앱 메인이 등록되던 문제**: iOS Safari는 16.4부터 "홈 화면에 추가"할 때 페이지에 연결된 Web App Manifest의 start_url을 우선 따라가는데, 이 앱의 manifest(`start_url=/home`)가 전체 사이트 루트에 걸려 있어 안내보드·도착체크·파일럿 체크인 같은 로그인 없는 토큰 링크도 전부 앱 메인으로 등록되고 있었습니다. manifest를 로그인 사용자 영역((dashboard))에만 적용되도록 좁혀서, 토큰 링크는 이제 지금 보고 있는 그 주소 그대로 홈 화면에 등록됩니다.

## v0.120.2 - 2026-08-11

- **셔틀 실시간 반영 속도 최적화**:
  - **하원 체크표**: 실시간 이벤트가 오면 테이블을 통째로 다시 조회하던 것을, 이벤트에 이미 담겨 온 값을 서버 왕복 없이 곧바로 화면에 반영하도록 바꿨습니다.
  - **실시간 셔틀(/shuttle/live)**: 7초 폴링을 걷어내고 위치·도착체크·탑승체크 세 테이블 모두 Realtime 구독으로 전환해, 다른 사람의 체크가 사실상 즉시 반영됩니다.
  - **파일럿 모니터링**: 노선마다 따로 조회하던 쿼리(파일럿 10개면 30개 요청)를 노선 전체를 한 번에 묶어 조회하도록 고쳤고, 위치 데이터도 필요 이상으로 가져오지 않게 시간 범위로 제한했습니다.
  - **안내보드·도착체크 API**: 서로 관계없는 조회를 순서대로 기다리지 않고 동시에 실행하도록 바꿔, 3초마다 폴링하는 두 화면의 응답 속도를 앞당겼습니다.

## v0.120.1 - 2026-08-11

- **사이드바 픽업·결석 이름을 누르면 체크표에서 바로 찾기**: "오늘 픽업·결석" 위젯의 이름을 누르면 오른쪽 체크표에서 같은 이름 뱃지가 노란색으로 강조되며 그 위치로 스크롤됩니다. 기존 검색창과 같은 방식이라, 눌러도 자동으로 체크되지는 않고 위치만 바로 찾아줍니다 - 실제 픽업/결석 체크는 찾은 뱃지의 🚗/🚫 버튼을 눌러 확정해주세요.

## v0.120.0 - 2026-08-11

- **학생별 특이사항 메모**: 하원 체크표에서 이름 뱃지 오른쪽 위 작은 아이콘을 누르면 그 학생만의 특이사항(알레르기, 하차 시 주의사항 등)을 적을 수 있습니다. 적어두면 뱃지 아이콘이 주황색 "!"로 바뀌어 한눈에 보입니다.
- **사이드바에 "특이사항" 위젯 추가**: 메모를 적은 학생들이 "학생이름: 메모" 형태로 왼쪽 사이드바에 모아서 나타납니다.
- **학생 이름 검색 + 하이라이트**: 표 위 검색창에 이름을 치면 그 학생의 뱃지가 노란색으로 바뀌면서 화면이 그 위치로 스크롤됩니다.
- **버그 수정: "계속 유지" 노선 변경이 관리자·행정직원이 아니면 저장되지 않던 문제**: shuttle_assignments 테이블이 관리자·행정직원 전용 쓰기 권한이라, 다른 로그인 사용자가 "계속 유지"를 선택하면 조용히 실패하고 있었습니다. 이번에 학생별 메모와 함께 전용 API로 우회해 로그인한 사용자 누구나 정상적으로 저장되도록 고쳤습니다.

## v0.119.0 - 2026-08-11

- **하원 체크표 실시간 반영**: 폴링 대신 Supabase Realtime으로 전환해, 다른 사람이 픽업/결석/노선이동을 누르면 화면에 거의 즉시 반영됩니다.
- **왼쪽 사이드바 스크롤 고정 + "오늘 차량 변경" 위젯 추가**: 화면을 내려도 사이드바가 따라오고, 픽업·결석 아래에 오늘 노선이 바뀐 학생을 모아 보여주는 위젯을 새로 추가했습니다.
- **자유로운 차량 전환 + 계속/오늘만 선택**: 이름을 다른 노선으로 드래그하면 "계속 유지"할지 "오늘만" 적용할지 물어봅니다. 계속 유지로 바뀐 학생은 안내보드·실시간셔틀·도착체크 화면에도 그대로 반영됩니다.
- **상단에 날짜·탑승예정인원 표시 + 인쇄 기능**: 체크표 위에 오늘 날짜와 탑승예정인원(픽업·결석 제외)이 나타나고, "🖨 인쇄" 버튼으로 표만 깔끔하게 인쇄할 수 있습니다.
- **모바일 도착체크: 호차 꾹 누르면 기사님께 전화**: 도착체크 화면에서 차량 카드를 길게 누르면 기사님 전화번호로 바로 걸 수 있는 메뉴가 뜹니다.

## v0.118.1 - 2026-08-11

- **버그 수정: 출결내역 이름 추정에 "@"가 다시 섞여 나오던 문제**: 문장 맨 앞에 붙은 멘션만 지우던 것을, 문장 어디에 있든 모든 "@멘션"을 지우도록 넓혔습니다. 또한 이름 후보를 정해진 글자수로 무작정 잘라내던 방식 대신 공백·문장부호로 실제 낱말 단위를 나누고, 결석/픽업 같은 출결 키워드 바로 앞 낱말을 우선으로 보게 해서 "확인 부탁드려요 정서안 결석입니다"처럼 문장 앞쪽에 다른 말이 섞여 있어도 실제 이름(정서안)을 더 정확히 뽑아냅니다.

## v0.118.0 - 2026-08-11

- **출결내역 이름 추정 개선**: 학생 명부와 대조되지 않을 때 원문을 아무 데서나 잘라 보여주던 것을, 문장 안에서 이름일 가능성이 높은 한글 단어를 먼저 찾아 보여주도록 바꿨습니다. 이렇게 추정된 이름은 🔎 표시로, 명부에서 확실히 찾은 이름과 구분해서 보여줍니다.
- **하원 체크표에 픽업·결석·출결메모 사이드바 추가**: 체크표(`/shuttle/checklist`) 왼쪽에 좁은 사이드바를 새로 만들어, 업무 메뉴로 가지 않아도 오늘 픽업·결석 학생 목록과 출결 메모(초등부)를 바로 볼 수 있게 됐습니다. 출결 메모는 업무 메뉴와 실시간으로 함께 업데이트됩니다.

## v0.117.1 - 2026-08-11

- **버그 수정: 출발하는 차량이 목록 맨 아래로 옮겨진 뒤 출발하던 문제**: 도착 시각 순 목록과, 출발 애니메이션용 별도 목록이 나뉘어 있어서 차가 출발할 때 순서가 훌쩍 바뀌는 것처럼 보였습니다. 이제 하나의 목록·하나의 정렬 기준으로 합쳐서, 출발하는 차량이 원래 있던 자리에서 그대로 슬라이드아웃됩니다.

## v0.117.0 - 2026-08-11

- **안내보드 짧은 주소 추가**: 공용컴퓨터 주소창에 복잡한 토큰 링크를 직접 입력하기 어렵다는 요청에 따라, "gia-ops-web.vercel.app/b/코드" 형태의 짧은 주소로도 안내보드에 접속할 수 있게 됐습니다. 안내보드 관리 화면(`/shuttle/pilot`)에서 안내보드마다 만들어진 4자리 코드를 직접 원하는 값으로 바꿀 수도 있습니다. 로그인은 여전히 필요 없습니다.

## v0.116.1 - 2026-08-10

- **안내보드 "지금 도착한 차량" 정렬 방식 변경**: 호차 번호 순이 아니라 도착 시각 순으로 정렬해, 먼저 온 차가 맨 위에 오도록 바꿨습니다. 오래 기다리는 차가 한눈에 보입니다.

## v0.116.0 - 2026-08-10

- **도착체크: 출발함 → 미도착 되돌리기**: 상태 버튼이 출발함에서 멈춰 다시 되돌릴 수 없던 것을, 한 번 더 누르면 미도착으로 돌아오도록 바꿨습니다(미도착 → 도착함 → 출발함 → 미도착 순환). 매일 반복되는 체크라 실수해도 바로 고칠 수 있습니다.
- **전체 리셋 버튼 추가**: 화면 오른쪽 위 "⟲ 전체 리셋" 버튼으로, 오늘 체크한 모든 차량의 도착·출발 상태를 한 번에 미도착으로 되돌릴 수 있습니다(확인 문구 후 실행).

## v0.115.1 - 2026-08-10

- **버그 수정: 여러 대가 동시에 도착하면 안내보드에 명단이 영영 안 뜨던 문제**: 도착 인트로 애니메이션을 순서대로 재생하는 대기열이 React state로 만들어져 있어서, 인트로가 재생되는 도중 새 차량이 폴링으로 대기열에 추가되면 이미 걸려 있던 타이머가 취소되어 그 차량의 명단 공개가 영영 멈추는 버그가 있었습니다. 대기열을 타이머와 함께 직접 관리하는 방식으로 바꿔 고쳤습니다.
- **도착·출발 체크 ↔ 안내보드 연동 속도 개선**: 폴링 주기를 도착체크 5초→3초, 안내보드 6초→3초로 줄이고, 도착 인트로 애니메이션 길이도 1.6초→1.1초로 줄여 전체적으로 더 빠르게 반영됩니다.

## v0.115.0 - 2026-08-10

- **교직원 도착체크 모바일 레이아웃 수정**: 노선 개수에 따라 열 수가 들쭉날쭉 늘어나 박스가 무너지던 문제를 고치고, 한 줄에 항상 4대씩 고정으로 보이게 했습니다(화면은 아래로 스크롤). 노선 번호가 크고 또렷하게 보이고, 그 아래 학생 이름이 아주 작은 뱃지로 표시됩니다.
- **도착·출발 버튼을 상태 겸용 하나로 통합**: "도착"/"출발" 버튼이 따로 있던 것을, 노선당 버튼 하나가 누를 때마다 미도착(흰색) → 도착함(오렌지색) → 출발함(회색, 완료) 순서로 색과 글자가 바뀌도록 바꿨습니다.
- **안내보드 도착·출발 애니메이션을 "지금 도착한 차량" 패널 안으로 한정**: 화면 전체를 덮던 버스 인트로 연출을, 오른쪽(모바일은 아래) 패널 폭 안에서만 재생되도록 바꿨습니다. 유튜브 화면을 가리지 않습니다.

## v0.114.0 - 2026-08-10

- **교직원 도착체크 화면 전면 재구성**: 미도착/도착함/출발함으로 나뉘어 있던 목록을 하나의 그리드로 합쳐, 노선 개수에 맞춰 약 3줄이 되도록 자동으로 열을 나눠서 화면 너비를 가득 채웁니다. 각 차량 카드 아래에 그 차를 타는 학생 이름이 작은 뱃지로 항상 표시됩니다. 지금은 동승선생님 쪽에 개별 탑승 체크 기능이 없어서 모든 이름이 "미탑승" 빨간 뱃지로만 보이며, 나중에 탑승 체크 기능이 생기면 누른 학생만 초록색으로 바뀌도록 만들 예정입니다.
- **안내보드: 유튜브 화면과 "도착한 차량" 패널 크기 조절**: 두 화면 사이의 구분선을 드래그하면 비율을 원하는 대로 바꿀 수 있습니다(가로 배치에서는 좌우로, 세로로 쌓인 화면에서는 위아래로). 마지막으로 조절한 크기는 브라우저에 저장되어 새로고침해도 유지됩니다.

## v0.113.0 - 2026-08-10

- **셔틀 링크 관리에 "링크 열기" 버튼 추가**: 안내보드·교직원 도착체크·파일럿(GPS) 링크 관리 화면에서 "🔗 링크 복사" 옆에 "↗ 링크 열기" 버튼이 생겨, 복사해서 다른 곳에 붙여넣지 않아도 그 자리에서 바로 새 탭으로 열어 확인할 수 있습니다.

## v0.112.0 - 2026-08-10

- **안내보드 차량 도착 연출 개선**: 차가 도착하면 위젯이 뜨기 전에 화면 전체를 덮는 큰 노란 셔틀버스가 경적 소리와 함께 왼쪽에서 오른쪽으로 지나가는 인트로 애니메이션이 먼저 나오고, 이어서 그 차량 위젯이 "띵동" 소리와 함께 왼쪽에서 미끄러져 들어오며 타야 할 학생 이름이 자동으로 표시됩니다. 여러 대가 동시에 도착해도 인트로가 겹치지 않고 순서대로 재생됩니다.
- **오른쪽 패널 제목 변경**: "지금 탈 수 있는 차량" → "지금 도착한 차량"으로 바꿔, 아직 안 탄 아이가 있어도 도착 자체가 목록에 뜨는 기준임을 더 명확히 했습니다.
- 화면을 처음 열었을 때 이미 도착해 있던 차량은 인트로 없이 바로 표시됩니다(반복 소음 방지).

## v0.111.0 - 2026-08-10

- **하원 체크표: 학생 당일 한정 노선 이동**: 표 안에서 학생 이름을 드래그해서 다른 호차 칸에 놓으면, 오늘 하루만 그 차량을 타는 것으로 바뀝니다. 평소 배정은 그대로 유지되고, 다음날은 자동으로 원래 노선으로 돌아갑니다. 옮겨진 학생은 이름 앞에 "↔" 표시와 노란 테두리로 구분됩니다.
- **픽업(🚗)/결석(🚫) 버튼 추가**: 학생 이름 아래 두 버튼으로 픽업·결석 여부를 바로 표시할 수 있습니다. 같은 버튼을 다시 누르면 취소됩니다.
- **노선 이동이 안내보드·실시간 셔틀·도착체크 화면에도 즉시 반영**: 체크표에서 옮긴 학생은 원래 노선이 아니라 옮겨진 노선의 명단에 나타나며, 모든 화면(안내보드, 실시간 셔틀, 교직원 도착체크 단독 링크)이 폴링 때마다 이를 함께 반영합니다. 지금은 여름캠프처럼 짧은 기간에 수동으로 쓰는 기능이며, 정규학기에는 추후 자동화 예정입니다.

## v0.110.0 - 2026-08-10

- **안내보드에서 유튜브 직접 검색**: 관리자가 미리 걸어둔 링크만 재생하던 것에서, 화면 오른쪽 위 "🔍 유튜브 검색" 버튼으로 그 자리에서 바로 검색하고 결과 중 원하는 영상을 클릭해 재생할 수 있게 됐습니다. 실제 유튜브 사이트는 보안 정책상 화면 안에 통째로 넣을 수 없어서, 검색 결과를 우리 화면에 직접 그려주는 방식으로 만들었습니다. 유튜브 플레이어의 전체화면 버튼을 누르면 검색창 없이 영상만 화면 가득 재생됩니다. "↩ 기본 영상으로" 버튼으로 관리자가 설정해둔 영상으로 언제든 되돌릴 수 있습니다.
- 이 기능을 쓰려면 `YOUTUBE_API_KEY` 환경변수 설정이 필요합니다(무료).

## v0.109.0 - 2026-08-10

- **하원 체크표 신설**: 업로드해주신 하원차량 체크표(PDF)와 같은 형태로, 노선별 오늘의 학생 명단을 한 화면에서 볼 수 있는 표 화면(`/shuttle/checklist`, 왼쪽 메뉴 "하원 체크표")을 만들었습니다. 부모님이 직접 데리러 오셔서 셔틀을 타지 않는 학생은 이름을 눌러 "픽업"으로 표시할 수 있고, 다시 누르면 취소됩니다. 정규학기/여름캠프2 탭으로 구분됩니다.
- **픽업 상태가 실시간 셔틀·안내보드에 즉시 반영**: 체크표에서 "픽업"으로 표시한 학생은 안내보드의 "아직 안 탄 아이" 목록에서 바로 빠지고(탑승·결석과 동일하게 취급), 실시간 셔틀 화면에도 픽업 인원이 별도로 표시됩니다.

## v0.108.1 - 2026-08-10

- **안내보드 유튜브 - 재생목록 링크 없이 영상 여러 개 등록 가능**: 재생목록 공유 링크를 따로 찾을 필요 없이, 평소 보던 영상 링크를 한 줄에 하나씩 붙여넣으면 순서대로 이어서 반복 재생됩니다. 재생목록 링크를 붙여넣는 기존 방식도 그대로 지원합니다.

## v0.108.0 - 2026-08-10

- **정규학기 / 여름캠프2 셔틀 구분**: 노선에 구분(term) 값이 생겨, 기존 데이터는 모두 "정규학기"로 자동 분류됩니다. 실시간 셔틀·파일럿 관리자 화면은 정규학기 노선만 보여줘서 캠프 노선과 섞이지 않습니다.
- **교직원 도착·출발 체크 단독 링크 신설**: 로그인 없이 링크 하나로 접속해 노선별로 "도착"/"출발(다 태움)" 두 버튼만 누르는 화면(`/shuttle-arrival/{토큰}`)을 새로 만들었습니다. GPS 위치 전송이나 학생별 개별 탑승 체크가 필요 없는 여름캠프처럼 짧고 단순한 운영에 씁니다. 관리자 화면(`/shuttle/pilot`)에서 정규학기용/캠프용 링크를 각각 만들고 관리할 수 있습니다.
- **안내보드 도착·출발 애니메이션**: 안내보드도 이제 정규학기/캠프 구분(term)에 맞는 노선만 보여주고(안내보드 링크마다 구분을 지정), 차량이 도착하면 노란 버스 아이콘이 미끄러져 들어오는 애니메이션이, 출발하면 카드가 미끄러지며 사라지는 애니메이션이 나옵니다. 개별 탑승 체크 없이 도착한 차량의 전체 명단이 계속 보이다가, 출발하면 사라집니다.

## v0.107.2 - 2026-08-10

- **안내보드 20초 반복 알람 제거**: 여러 차량이 동시에 정차해 있을 때 20초마다 알람이 계속 울리면 정신없다는 의견에 따라, 이제 차량이 새로 도착한 순간에만 알람이 한 번 울립니다. 몇 호차가 도착했는지, 아직 타지 않은 학생이 누구인지는 계속 화면에 떠 있고, 탑승 체크가 되면 그 학생 이름이 바로 목록에서 빠져 남은 학생만 한눈에 보입니다.

## v0.107.1 - 2026-08-10

- **사건기록 새 사건 입력 화면 스크롤 안 되던 문제 수정**: 데스크톱에서 입력폼이 화면보다 길어지면(태그할 학생이 많거나 내용이 길 때) 아래쪽 저장 버튼까지 화면이 잘려서 안 내려가던 문제를 고쳤습니다. 이제 가운데 입력폼 칸도 양옆 목록/AI제안 칸처럼 그 칸 안에서 스크롤됩니다.

## v0.107.0 - 2026-08-10

- **안내보드 도착 알람 강화**: 차량이 도착해 탑승할 아이들 이름이 뜨는 순간 3음 알람 소리가 울립니다. 브라우저 자동재생 정책 때문에 화면을 처음 열면 "소리 켜고 시작하기"를 한 번 눌러야 하고(그 뒤로는 계속 소리가 납니다), 아직 타지 않은 아이가 남아있는 동안은 20초마다 알람이 다시 울려서 놓치지 않도록 했습니다.

## v0.106.0 - 2026-08-10

- **안내보드를 로그인 없는 링크로 전환 + 유튜브 재생**: `/shuttle-board`가 회사 계정 로그인 없이 링크(토큰) 하나로 접속하는 화면(`/shuttle-board/{토큰}`)으로 바뀌었습니다. 평소에는 유튜브 영상(관리자가 지정한 영상 또는 재생목록)이 화면 대부분을 채워 아이들이 기다리며 볼 수 있고, 오른쪽(모바일은 아래) 패널에는 "지금 탈 수 있는 차량"이 항상 함께 보입니다. 차량이 도착하면 몇 호차인지, 아직 타지 않은 학생이 누구인지 큰 글씨로 뜨고, 새로 도착한 차량은 잠깐 반짝이며 알림음이 울립니다.
- **안내보드 링크 관리 UI**: 관리자 화면(`/shuttle/pilot`)에서 안내보드를 여러 개 만들고(화면별로 이름 지정), 각각 재생할 유튜브 영상/재생목록을 지정하고, 링크를 복사하고, 켜고 끌 수 있습니다. 실시간 셔틀 화면(`/shuttle/live`)의 '안내보드 열기' 버튼이 실제 활성화된 안내보드로 연결됩니다.
- **동시 도착 대응**: 여러 차량이 거의 동시에 도착해 여러 교직원이 '현장도착'을 동시에 누르는 상황에 대비해, 같은 노선·같은 날짜에는 DB에서 중복 체크를 자동으로 막고(부분 유니크 인덱스), 화면에서도 중복 클릭을 에러 없이 조용히 넘어가도록 처리했습니다. 또한 실시간 셔틀 화면을 "미도착 · 빠른 체크"(큰 버튼만 있는 목록)와 "상세 현황"(위치·탑승현황이 있는 카드) 두 구역으로 나눠, 여러 차가 몰리는 순간에도 빠르게 훑고 누를 수 있게 했습니다.

## v0.105.0 - 2026-08-10

- **동승선생님 전용 + 하원 우선으로 범위 축소**: 이번 단계에서는 기사님이 아닌 동승선생님만 체크인 링크를 쓰고, 등원은 잠시 제외하고 하원 노선만 지원합니다. 관리자 링크 관리, 교직원 실시간 셔틀, 안내보드 모두 하원 노선만 보여줍니다.
- **체크인 화면 순서 재정렬**: 화면을 열면 ① 학생 탑승 확인(목록)이 먼저 보이고, 그 아래 ② '이동 시작(위치 전송 시작)' 버튼이 있습니다. 이동을 시작하면 실시간 위치가 전송되고, '운행 종료'를 누르는 즉시 위치 전송이 멈춥니다.

## v0.104.0 - 2026-08-10

- **안내보드(전광판) 화면 신설**: 복도·로비 화면에 띄워두는 전용 화면(`/shuttle-board`, 실시간 셔틀 화면의 '안내보드 열기'로 접근)입니다. 교직원이 현장도착을 체크한 하원 차량만, 아직 타지 않은 학생 이름을 큰 글씨로 자동 표시합니다. 동승선생님이 탑승을 체크하면 이 화면에도 실시간으로 반영되고, 전원 탑승하면 "전원 탑승 완료"로 바뀝니다.
- 이번 버전으로 하원 셔틀 자동화 로드맵(학부모 제외, 교직원 중심 재편)의 1차 구현을 마쳤습니다: 실시간 위치(교직원 전체) → 현장도착 체크 → 안내보드 → 동승선생님 탑승확인 → 교직원 모바일 확인까지 전 과정이 연결됩니다.

## v0.103.0 - 2026-08-10

- **교직원 전체용 실시간 셔틀 화면 신설**: 왼쪽 메뉴 '실시간 셔틀'(교사 포함 로그인한 전체 교직원이 볼 수 있음)에서 등원·하원 노선의 실시간 위치와 정류장별 탑승/결석/미탑승 현황을 바로 확인할 수 있습니다.
- **'현장도착' 체크 추가**: 하원 차량이 학교 승차 지점에 도착하면 교직원이 모바일에서 직접 '현장도착'을 체크할 수 있습니다(기사님 GPS 이벤트와 별개로, 눈으로 본 도착을 확인하는 절차). 동승선생님이 기존 체크인 화면에서 탑승을 체크하면 이 화면에도 실시간으로 반영됩니다.
- 관리자 전용 링크 관리 화면(/shuttle/pilot)은 그대로 유지하되, 메뉴 이름을 '링크 관리'로 명확히 구분했습니다.

## v0.102.0 - 2026-08-10

- **학부모 기능 전면 제거**: 노선이 지연되거나 교통상황으로 도착이 늦어질 때 학부모가 실시간 위치만 보고 오해해 문의가 상충할 수 있다는 판단에 따라, 학부모 테스트 화면·학부모 테스트 링크 발급·자동 푸시 알림(2단계-c) 기능을 전부 제거했습니다. 대신 교직원이 등원·하원 셔틀 실시간 위치와 탑승현황을 바로 확인하고, 하원 차량 탑승을 안내·확인하는 방향으로 시스템을 재편합니다(다음 버전부터 순차 반영).

## v0.101.0 - 2026-08-10

- **3단계-b: 운행일지 자동생성**: 셔틀 실시간 위치 화면의 노선 카드에 '오늘 운행일지' 버튼이 생겼습니다. 누르면 그날의 출발·5분전·도착 시각, 소요시간, 정류장별 탑승/결석/미탑승/하차 현황, 안전운행지수를 자동으로 정리한 PDF가 만들어집니다. 벤치마킹 제안서의 3단계 기능이 모두 반영되어(4단계 얼굴인식은 제외), 1~3단계 로드맵을 마쳤습니다.

## v0.100.0 - 2026-08-10

- **3단계-a: 안전운행지수(급가속·급감속)**: 기사님·동승선생님 체크인 화면이 운행 중 휴대폰 가속도 센서로 급가속·급감속 순간을 감지합니다. 관리자 모니터링 화면 노선 카드에 오늘 하루 안전운행지수(100점 만점, 급가속·급감속 1건당 5점 감점)가 표시됩니다.

## v0.99.0 - 2026-08-10

- **2단계-c: 학부모 자동 푸시 알림**: 학부모 테스트 화면에서 "탑승·하차 알림 받기"를 누르면, 그 학생이 탑승/미탑승/결석으로 체크되거나 하차가 확인될 때 브라우저 알림이 옵니다. 앱 설치나 외부 서비스 가입 없이 브라우저 표준 Web Push를 씁니다(발신용 VAPID 키는 서버 환경변수로만 관리, 로그인 없는 학부모 테스트 링크 구조는 그대로 유지).

## v0.98.0 - 2026-08-10

- **2단계-b: 도착예정시각(ETA)**: 학부모 테스트 화면에 "약 O분 후 도착 예정(HH:MM)"이 표시됩니다. 현재 위치에서 학생 정류장까지 카카오모빌리티 길찾기로 계산하며, 노선·정류장 조합별로 30초 캐시를 둬 API 호출 비용을 통제합니다(기존 KAKAO_REST_API_KEY 재사용, 새 키 발급 불필요).

## v0.97.0 - 2026-08-10

- **2단계-a: 학생별 탑승·하차 체크리스트**: 기사님·동승선생님 체크인 화면에서 오늘 그 노선에 배정된 학생 목록을 정류장 순서대로 보고, 탑승/결석/미탑승/하차를 터치 한 번으로 기록할 수 있습니다(다시 누르면 취소). 전부터 만들어져 있었지만 화면과 연결되지 않았던 `shuttle_boardings` 테이블을 처음으로 사용합니다.
- 학부모 테스트 화면에도 탑승·하차 확인 여부가 함께 표시됩니다.

## v0.96.0 - 2026-08-10

- **셔틀 실시간 위치 1단계 정식 도입**: 벤치마킹 제안서의 1단계(실시간 위치 전송, 운행 이벤트)를 파일럿에서 정식 기능으로 전환했습니다. 관리자가 고른 일부 노선에만 붙던 것을, 활성 노선 전체에 자동으로 적용하고(새 노선 등록 시 자동 포함) 사이드바 메뉴명도 "실시간 위치"로 바꿨습니다.
- **학부모 테스트 화면 신설**: 실제 학부모에게는 아직 배포하지 않지만, 관리자가 학생을 검색해 테스트용 링크를 만들고 직접 학부모 화면(자녀 셔틀 실시간 위치)을 확인해볼 수 있습니다. 도착예정시각·자동알림은 2단계에서 추가될 예정입니다.

## v0.95.0 - 2026-08-10

- **출결내역 이름 인식 개선**: 구글챗 출결알림의 "@멘션"이 학생 이름으로 잘못 표시되던 문제를 고쳤습니다(태그일 뿐 실제 이름이 아님). 영어로 적힌 출결알림("~will be absent" 등)도 문구 앞 이름을 학생 명부의 영어이름과 대조해 "한글이름(영어이름)"으로 표시합니다.
- **부서 메모 / 출결 메모 분리**: 부서 메모에 출결 내용을 적으면 자동으로 출결내역에 반영되던 동작을 없앴습니다(자유 메모로 못 쓰게 되는 문제). 대신 출결내역 화면을 좌:우 7:3으로 나눠, 오른쪽에 절대 자동 반영되지 않는 전용 "출결 메모"칸을 추가했습니다 - 픽업/결석/퇴소 메모나 며칠 뒤 출결을 미리 적어두는 용도입니다.
- **탑승 배정 화면 재구성**: 등원/하원 탭으로 나누고, 호차(노선) 단위로 묶어 "1호차" 아래 학생 목록이 뜨도록 바꿨습니다. 호차는 번호순, 호차 안에서는 학년순으로 정렬됩니다. 학생 추가도 이제 호차 단위로 합니다.
- **노선 관리 용어 변경**: 정류장 항목의 "게이트"를 "도착장소"로 바꿨습니다.
- **셔틀 메뉴 기본 화면 변경**: 사이드바 "셔틀"을 누르면 바로 지역별 현황이 뜨도록 바꾸고, 부메뉴에서는 뺐습니다.

## v0.94.0 - 2026-08-10

- **셔틀 앱 파일럿 검증 인프라 신설**: 정식 앱(앱스토어 배포) 전에, 실제 노선으로 위치 전송이 무리 없이 되는지 먼저 확인하는 단계를 추가했습니다. 학부모는 참여하지 않고 기사님·동승선생님만 참여하며, 앱 설치·개발자 계정·로그인 없이 링크 하나로 접속합니다(`/shuttle-pilot/[토큰]`) - "운행 시작"을 누르면 5초 간격으로 위치만 전송되고, "운행 종료"를 누르면 즉시 멈춥니다.
- **파일럿 모니터링 대시보드**: 관리자 메뉴에 "🧪 파일럿 검증"이 추가되어, 노선별 실시간 위치를 지도에서 보고 수신 성공률·평균 갱신 간격·완주 여부 같은 검증 지표를 확인할 수 있습니다. 노선을 골라 파일럿 링크를 바로 만들고 복사할 수 있습니다.
- 이 검증 데이터는 학생 개인정보와 무관하며(노선 좌표·시각만 저장), 검증이 끝나면 관련 테이블은 통째로 삭제해도 정식 서비스에 영향이 없습니다.

## v0.93.3 - 2026-08-07

- **지도는 노선이 가는 구만 색이 들어감**: 셔틀이 안 가는 구는 흰 바탕에 옅은 테두리만 남아, 색이 칠해진 구 = 실제로 셔틀이 가는 지역이라는 게 바로 보입니다.
- **동별 목록에서 등원/하원 구분**: 지역을 눌렀을 때 뜨는 동별 호차 목록이 등원·하원 줄로 나뉘어 표시됩니다. 아래 전체 노선 목록도 같은 지역 안에서는 등원이 먼저, 하원이 나중에 오도록 정렬했습니다.

## v0.93.2 - 2026-08-07

- **서울 지도 글자 안 보이는 문제 수정**: 종로구 등 일부 구는 이름표 위치가 도형 바깥으로 벗어나 있었습니다. 도형 경계에서 가장 먼 안쪽 지점을 다시 계산해 25개 구 전부 도형 안에 이름표가 찍히도록 고쳤고, 배경색과 겹쳐도 잘 보이도록 글자에 흰 테두리를 추가했습니다.
- **지역을 누르면 색이 확실히 바뀜**: 선택한 구는 테두리만 굵어지던 것에서, 채우기 색 자체가 주황색으로 바뀌도록 해 지금 어느 구를 보고 있는지 한눈에 보입니다.

## v0.93.1 - 2026-08-07

- **정류장 주소 저장 시 즉시 구/동 반영**: 노선 관리에서 정류장 주소를 추가하거나 고치면, 노선도 탭을 따로 열지 않아도 그 자리에서 바로 구/동·좌표가 채워집니다. 주소를 지우면 옛 좌표/구·동도 함께 지워집니다.
- **정류장이 바뀌면 실도로 경로 캐시 자동 삭제**: 정류장을 추가·삭제하거나 주소·순서·소속 노선이 바뀌면(차량 배정 변경 포함), 캐시된 실도로 경로가 자동으로 삭제되어 다음에 노선도를 열 때 새로 계산됩니다. 예전 경로가 화면에 남아있을 일이 없습니다.
- **정류장 구/동 일괄 채우기 스크립트 추가**: 기존에 등록된 정류장들의 구/동을 한 번에 채우는 관리자용 스크립트(`scripts/backfill-shuttle-geo.mjs`)를 추가했습니다.

## v0.93.0 - 2026-08-07

- **지역별 현황을 실제 행정구역(구/동) 기준으로 재구성**: 노선 이름 표기("청담1", "용산/이태원" 등)로 지역을 추정하던 방식은 표기가 제각각이라 믿을 수 없었습니다. 이제 정류장 주소를 지오코딩할 때 함께 받아오는 실제 행정구·행정동을 기준으로 지역을 분류합니다. 노선도 탭을 한 번 열면 그 노선 정류장들의 구/동이 자동으로 채워집니다.
- **서울 25개구 지도로 교체**: 카카오맵 대신 서울시 25개 자치구 경계를 그대로 그린 지도를 새로 만들었습니다. 구를 누르면 그 구 안의 동별로 몇 대(몇 호차)가 가는지 오른쪽에 바로 뜨고, 지도 위 숫자로 구별 셔틀 대수를 한눈에 볼 수 있습니다. 지명·아파트·도로명·차호수 검색도 계속됩니다.

## v0.92.0 - 2026-08-07

- **노선도에 정류장별 시간표 추가**: 노선도 아래에 GIA(또는 첫 정류장) 출발 시각부터 정류장을 순서대로, 구간별 소요시간과 누적 도착예정시각을 함께 보여줍니다. 실도로 경로가 계산돼야 정확한 시간이 나오고, 없으면 순서만 보여줍니다.
- **실도로 경로 자동 계산**: 이제 노선도를 열면(편집 권한이 있는 경우) 실도로 경로가 없거나 낡았을 때 자동으로 한 번 계산합니다. 손으로 버튼을 누르지 않아도 됩니다.
- **차량 정원 관리 + 초과 경고**: 노선 관리에서 차량 정원(인승)과 실제 탑승 가능 인원을 입력할 수 있고, 배정 인원이 이를 넘으면 셔틀 현황·노선 관리·지역별 현황 어디서나 ⚠️ 경고가 뜹니다.
- **지역별 현황 대시보드 신설**: 노선 번호가 지역을 묶어 매겨진 점에 착안해, 지도에서 지역을 고르면(또는 지명·아파트·도로명·차호수로 검색하면) 그 지역 가는 셔틀 목록이 뜨고, 아래에는 전체 노선이 지역순·호차순으로 정렬돼 나옵니다. 지역 태그는 기존 권역명에서 1차 자동 추정 후 노선 관리에서 다듬을 수 있습니다.

## v0.91.0 - 2026-08-07

- **학생 명부에 차량탑승 여부 + 노선 추천**: 학생 명부 관리에서 "차량탑승 여부"(없음/등원/하원/등하원)를 지정하면 [추천] 버튼으로 주소 기준 가장 가까운 노선·정류장 3곳을 거리·예상 시각과 함께 보여주고, 그 자리에서 바로 배정할 수 있습니다.
- **노선 지도에 실제 도로 경로 표시**: 노선도 탭에 [🛣️ 실제 도로 경로 계산] 버튼이 생겼습니다. 카카오모빌리티 길찾기로 정류장을 순서대로 잇는 실제 도로 경로를 계산해 캐시해두고, 직선 대신 그 경로를 그립니다(총 거리·예상 소요시간 함께 표시). 정류장 구성이 바뀌면 다시 계산이 필요하다고 안내합니다.

## v0.90.1 - 2026-08-07

- **노선 지도에 GIA 본원 지점 표시**: 정류장 DB에는 학생 주소만 있어 학교 자체가 지도에 안 보였습니다. 이제 등원 노선은 마지막에, 하원 노선은 맨 앞에 🏫 GIA 지점을 자동으로 붙여서, 실제 운행 순서(집→학교 / 학교→집)가 그대로 지도에 보이도록 했습니다.

## v0.90.0 - 2026-08-07

- **셔틀 현황에 노선도(지도) 탭 추가**: 노선을 고르면 배차표 옆에 [🗺️ 노선도] 탭이 생겨, 정류장을 순서대로 번호가 매겨진 지도 마커와 경로선으로 볼 수 있습니다. 카카오맵 주소검색으로 좌표를 자동으로 채우고, 동 이름만 있어 자동으로 못 찾는 주소는 지도를 직접 클릭해 위치를 지정할 수 있습니다.

## v0.89.0 - 2026-08-07

- **셔틀이 주메뉴로 독립**: 학교 관리 부메뉴 하나였던 셔틀을 주메뉴로 빼고 부메뉴를 나눴습니다 - 셔틀 현황 / 노선 관리 / 탑승 배정.
- **노선 관리 신설**: 노선을 추가·삭제하고 호차·권역명·기사님·차량번호·동승 선생님·출발시각을 수정합니다. 정류장도 추가·수정·삭제하고 ↑↓로 순서를 바꿀 수 있습니다. 정류장을 지울 때는 함께 사라지는 학생 배정 수를 먼저 알려줍니다.
- **탑승 배정 신설**: 학생 이름이나 노선으로 찾으면 그 아이의 등원·하원 배정이 한 줄씩 보이고, 요일 버튼(월화수목금)을 눌러 타는 날을 바로 지정합니다. 요일마다 내리는 곳이 다른 학생은 [+ 배정 추가]로 줄을 더 만들어 각각 다른 정류장·요일을 지정하면 됩니다.

## v0.88.2 - 2026-08-07

- **셔틀 배차표에 소속(유치부/초등부/중고등부) 표시**: 반 표기로 소속을 추정해 배지로 보여줍니다("5 Nightingale"처럼 나이+반이름은 유치부, "학교"는 초등부).
- **명부 미연결 경고를 꼭 필요한 곳만 표시**: 지금 학생 명부에는 초등부만 등록되어 있어 유치부·중고등부는 연결이 안 되는 게 정상인데, 585명 중 488명에 ⚠️가 떠서 정작 확인이 필요한 건이 묻혔습니다. 이제 "초등부인데 명부에 없는" 경우에만 표시하고, 노선 제목 옆에 그 건수를 함께 보여줍니다.

## v0.88.1 - 2026-08-07

- **셔틀 호차 중복 오류 수정**: 배차표 원본에 "등원 22호"가 청담1·청담4 두 노선에 중복으로 적혀 있어 데이터 입력이 실패했습니다. 호차 번호 유니크 제약을 없애 두 노선 모두 들어가도록 하고, 화면에서 보고 정리할 수 있게 했습니다.

## v0.88.0 - 2026-08-07

- **셔틀 관리 신설 (1단계)**: [학교 관리] 메뉴에 "🚌 셔틀 관리"가 생겼습니다. 등원·하원 노선과 정류장, 요일별 탑승 학생을 관리하고 배차표를 인쇄할 수 있습니다.
  - 2026BUS.pdf를 그대로 옮겼습니다: 등원 39개 / 하원 47개 노선, 정류장 595곳, 탑승 배정 585건, 학생 335명.
  - 요일별로 다른 노선을 타는 학생(월수는 학원, 화목은 집 등)을 그대로 반영했습니다.
  - 노선·기사님·학생 이름으로 검색됩니다(이 아이가 몇 호차인지 바로 확인 가능).
  - 지입차량이라 바뀌기 쉬운 기사님·차량번호·동승 선생님은 화면에서 바로 수정할 수 있습니다.
  - 학생 명부와 자동으로 연결되며, 동명이인이라 확정 못 한 건은 ⚠️로 표시됩니다.

## v0.87.3 - 2026-08-07

- **출결내역 날짜·요일 인식**: "조영윤 금요일 결석입니다"처럼 오늘이 아닌 날의 출결을 미리 알리는 경우, 이제 그 날짜를 읽어서 오늘 집계에 섞이지 않게 했습니다. 요일(금요일), 상대 표현(오늘/내일/모레), 날짜(8/12, 8월 12일, 12일)를 모두 인식합니다.
- **"📅 예정" 칸 추가**: 앞으로 예정된 결석·픽업은 오늘 칸 아래에 날짜와 함께 따로 모아 보여줍니다. 지난 날짜는 자동으로 빠집니다.
- **미리 알려준 오늘 결석도 잡음**: 예전에는 오늘 올라온 메시지만 봐서, 며칠 전에 미리 알려준 오늘 결석을 놓쳤습니다. 이제 최근 2주치를 훑어 각 메시지가 가리키는 날짜로 분류합니다.

## v0.87.2 - 2026-08-07

- **출결내역 동명이인 구분**: 같은 이름의 학생이 여러 명일 때, 문장에 적힌 학년 표기를 읽어 누구인지 구분합니다. "2학년 김재이", "김재이(2)", "김재이 2학년", "초2 김재이" 형태를 모두 인식하고, 화면에는 "김재이(2학년)"처럼 학년을 붙여 보여줍니다.
- **학년 표기가 없으면 임의로 고르지 않음**: 동명이인인데 학년 힌트가 없으면 한 명을 몰래 골라버리지 않고 ⚠️ 표시로 "학년 확인 필요"라고 알려줍니다.
- **중복 체크 기준 보완**: 중복 제거가 이름만이 아니라 학년까지 함께 보므로, 같은 이름의 다른 학년 학생이 한 명으로 합쳐지지 않습니다.

## v0.87.1 - 2026-08-07

- **출결내역 제목 옆 요약**: 픽업·결석·지각 순서로 건수를 한눈에 보여줍니다(조퇴는 있을 때만).
- **출결내역을 분류별 칸으로 분리**: 채팅처럼 한 줄로 흐르던 것을 위에서부터 픽업 / 결석 / 지각 칸으로 나눠, 각 칸 안에 누가 해당되는지와 원문을 작게 함께 보여줍니다.
- **출결내역 ↔ 출결알림 탭 전환**: 자리를 반으로 쪼개지 않고 탭으로 전환합니다. 기본은 정리본인 출결내역이고, 원문 확인이 필요할 때 출결알림 탭을 누르면 구글챗 채팅이 보입니다.
- **부서 메모도 출결내역에 반영**: 부서 메모에 적힌 결석·픽업 내용도 함께 올라옵니다. 구글챗과 메모에 같은 내용이 겹쳐 적혀도 같은 학생이 두 번 뜨지 않습니다.
- **학생 이름 오인식 수정**: 예전에는 문장에서 한글 2~4자를 그대로 이름으로 집어내서 "정서안만 픽업"의 "정서안만"이 이름처럼 잡혔습니다. 이제 실제 학생 명부와 대조해서, 명부에 있는 "정서안"만 잡히고 이름이 겹치는 경우(김민 / 김민준 등)에도 긴 이름을 우선해 한 명만 정확히 잡습니다.

## v0.87.0 - 2026-08-07

- **내 업무가 아닌데도 진행대기에 뜨던 문제 수정**: 진행대기/진행중/완료 칸이 부서 전체 업무를 보여주고 있었습니다. 이제 "내가 태그된 업무"만 뜹니다. 판정 기준도 태그 하나로 통일했습니다 - 어떤 방식([나]/[전체]/[공유])으로 등록하든 등록자 본인이 자동으로 태그되고, [전체]로 등록하면 부서원 전원이 태그됩니다. 부서 전체를 훑어보는 용도는 오른쪽 위 "전체 업무목록"이 계속 담당합니다.
- **업무 흐름판(이름 신설)**: 진행대기→진행중→완료 흐름을 보는 영역에 "🔀 업무 흐름판"이라는 이름을 붙였습니다.
- **업무바 접기/펼치기**: 등록된 업무는 평소엔 제목·확인여부·마감기한만 있는 얇은 바로 보이고, 마우스를 올리면 아래로 펼쳐지며 내용·담당자·확인현황·상태변경이 나타납니다. 옮길 때는 제목 부분을 잡고 드래그합니다.
- **출결내역 위젯 신설**: 출결알림 자리를 반으로 나눠 왼쪽은 구글챗 원문 그대로, 오른쪽은 그 원문에서 결석·픽업·지각·조퇴 키워드를 뽑아 누가 해당되는지 분류·정리해 보여줍니다(오늘 것만).
- **등록사건목록 대시보드 신설**: 사이드바에서 [운영 관리]를 누르면 바로 열립니다. 날짜별로 묶어 언제 어떤 사건이 등록됐는지와 개요(최대 5줄)를 함께 보여줍니다.
- **사이드바 숫자 배지 정리**: 주메뉴(운영 관리 등) 옆에 상시로 떠 있던 합계 숫자를 없앴습니다. 대신 등록사건목록 대시보드 제목 옆에 총 건수를 표시합니다(부메뉴 항목별 배지는 그대로 유지).

## v0.86.3 - 2026-08-06

- **학생 명부 관리 스크롤 안 되던 버그 수정**: 화면을 감싸는 영역에 높이 기준이 빠져 있어서, 목록을 아무리 늘려도(v0.86.2에서 페이지 넘김을 없앤 뒤) 표 안쪽 스크롤이 동작하지 않았습니다. `/students`(학생 정보 조회) 화면과 같은 구조로 맞춰 이제 표 영역이 화면 높이에 맞게 자체적으로 스크롤됩니다.
- **학생 명부 관리에 이름 검색창 추가**: "학생 추가" 버튼 줄과 표 사이에 이름/영어 이름 검색창을 넣었습니다. 학년/반 필터와 함께 쓸 수 있습니다.

## v0.86.2 - 2026-08-06

- **학생 명부 관리 화면 개선**: 페이지 단위(20명씩) 넘기던 방식을 없애고 전체 명단이 한 번에 아래로 스크롤되도록 했습니다. 상단에 학년/반 필터를 추가해 원하는 학년·반만 골라 볼 수 있고, 기존처럼 칼럼 제목(이름/영어 이름 등)을 클릭하면 그 칼럼 기준 오름차순/내림차순으로 정렬됩니다.

## v0.86.1 - 2026-08-06

- **통합검색으로 학생을 찾으면 관련 사건기록이 안 보이던 버그 수정**: 상단 검색(⌘K)에서 학생을 찾아 클릭하면 관리자·행정직원도 위클리 리포트 화면(사건기록 없음)으로 이동하고 있었습니다. 이제 관리자·행정직원은 사건기록·업무언급까지 함께 보이는 통합 프로필(학생 정보 조회와 동일한 화면)로 이동합니다. 교사는 기존과 동일합니다.
- **사건기록 입력 텍스트박스 자동 높이조절**: 상세 내용/잘된 점/부족했던 점/보완점/조치사항 칸이 이제 내용이 10줄 이내면 전문이 그대로 다 보이고, 10줄을 넘으면 그 높이에서 멈추고 안쪽 스크롤로 바뀝니다.

## v0.86.0 - 2026-08-06

- **교직원 통합기록 신설**: 학생 통합 프로필(입학부터 졸업까지 고유번호 유지)과 같은 방식으로, 교직원도 계정 이메일을 영구 식별자로 삼아 입사일·퇴사일과 연도/학기별 담당 반·역할 이력을 한 화면에서 관리합니다.
  - [학교 관리] 메뉴에 "교직원 정보 조회"가 새로 생겼습니다(관리자·행정직원). 이름/이메일로 검색해 들어가면 기본정보 + 담당 이력 + 관련 업무를 한 페이지에서 볼 수 있습니다.
  - 입사일/퇴사일 편집과 담당 이력 추가/삭제는 관리자만 할 수 있습니다. 퇴사해서 로그인 계정이 차단되어도 기록은 계정이 삭제되지 않는 한 그대로 남습니다.
  - [사용자 관리] 화면의 승인된 계정마다 "📋 근무기록" 바로가기가 추가됐습니다.
  - 다음 단계로 예정된 "사건 검색 시 비슷한 사건 정렬 표시" 기능은 이번 배포에는 포함되지 않았습니다(다음 라운드에서 진행).

## v0.85.8 - 2026-08-06

- **코드 정리**: 정적 분석(knip)으로 찾은 안쓰는 코드/파일을 정리했습니다.
  - 완전히 안쓰는 파일 삭제: `src/lib/ai/translate.ts`(제거된 "행정요청" 기능 전용 번역 헬퍼, v0.82.0에서 그 기능 자체가 삭제됨).
  - 어디서도 호출되지 않던 죽은 함수 삭제: `appendHtmlSection`, `isAuthOkCookieValid`, `CATEGORY_LABELS`/`parseBadges`/`hasWarningBadges`/`hasExcellentBadges`(위클리 리포트 통계 화면이 이미 같은 로직을 직접 계산하고 있어 중복이었습니다), `isInCurrentWeek`.
  - 같은 파일 안에서만 쓰이는데 불필요하게 외부로 열려있던 함수/상수 12개는 `export`만 제거(로직은 그대로 유지, 파일 밖 공개 API만 줄임).
  - 번들/의존성 점검 결과 사용하지 않는 npm 패키지는 없었고, 무거운 라이브러리(dnd-kit/tiptap/react-pdf)는 이미 각자 필요한 화면에만 자동으로 코드 분할되어 있어(react-pdf는 서버 전용 API 라우트에서만 쓰여 브라우저로는 아예 전송되지 않음) 추가 조치가 필요하지 않았습니다.
  - 동작 변화는 없고(tsc/build 통과 확인), 코드베이스만 가벼워졌습니다.

## v0.85.7 - 2026-08-06

- **슬랙 알림 실패 진단 로그 추가**: `SLACK_WEBHOOK_URL`을 등록했는데도 가입 알림이 오지 않는 문제를 조사하기 위해, Slack이 웹훅 요청을 거부해도 지금까지는 아무 흔적 없이 조용히 실패하던 부분을 고쳐 실패 시 상태코드/응답 내용을 Vercel 함수 로그에 남기도록 했습니다. 실제 원인(웹훅 URL 문제/채널 문제 등)은 로그를 보고 이어서 확인합니다.

## v0.85.6 - 2026-08-06

- **구글챗 미러링 내용 옆에 업무등록 아이콘을 작게 배치**: 아이콘이 있던 별도 줄을 없애고, 메시지 내용 오른쪽에 아주 작은 아이콘(🔧/✅)만 붙여서 내용이 더 깔끔하게 보이도록 했습니다.

## v0.85.5 - 2026-08-06

- **구글챗 미러링 "업무로 등록" 버튼을 아이콘 전용으로 축소**: 글씨 없이 🔧 아이콘만 표시하고, 마우스를 올리면 "업무등록"이라는 작은 말풍선만 뜨도록 했습니다.

## v0.85.4 - 2026-08-06

- **구글챗 미러링 패널 날짜별 구분선 추가**: 출결알림 목록에 날짜가 바뀔 때마다 "오늘/어제/8월 6일 (수)" 형태의 구분선이 표시되도록 했습니다.

## v0.85.3 - 2026-08-06

- **선생님요청 패널 임시 제거**: 선생님요청 방이 아직 만들어지지 않았는데 메시지가 출결알림/선생님요청 두 패널에 뒤섞여 나타나던 문제가 있었습니다. 방이 생기기 전까지는 선생님요청 패널 자체를 화면(PC/모바일)에서 빼고 출결알림 패널이 자리를 전부 쓰도록 했고, 폴링 크론도 더 이상 선생님요청 방을 조회하지 않도록 했습니다. 패널이 넘치면 스크롤되는 동작은 그대로 유지됩니다. 방이 만들어지고 환경변수(`GOOGLE_CHAT_SPACE_TEACHER_REQUESTS`)를 넣으면 다시 추가하면 됩니다.

## v0.85.2 - 2026-08-06

- **구글챗 폴링 크론 타임아웃 조정**: cron-job.org 무료 플랜의 요청 타임아웃 상한이 30초로 확인되어(이전 기준이던 60초보다 짧음), 폴링 라우트의 내부 반복 시간을 25초로 줄여 항상 제한 시간 안에 응답하도록 수정했습니다. 가이드의 STEP 8에도 반영했습니다.

## v0.85.1 - 2026-08-06

- **크론 라우트가 항상 로그인 화면으로 리다이렉트되던 버그 수정**: 로그인 세션 검사 미들웨어가 `/api/cron/*` 경로에도 적용되고 있어서, 외부 스케줄러(cron-job.org)나 Vercel Cron처럼 로그인 세션 없이 `Authorization: Bearer` 헤더로만 인증하는 요청이 항상 `/login`으로 307 리다이렉트되고 있었습니다(구글챗 폴링 크론에서 "307 Temporary Redirect" 오류로 발견). `/api/cron` 경로는 세션 검사를 건너뛰고 각 라우트 자체의 인증만 거치도록 수정했습니다 - 기존에 있던 다른 6개 크론(백업/학기전환/보관/휴지통정리 등)도 같은 문제를 겪고 있었을 가능성이 높아 함께 해결됩니다.

## v0.85.0 - 2026-08-06

- **구글챗 미러링을 Pub/Sub push → 폴링 방식으로 전환**: 실시간 push 알림에 필요한 Google Cloud Pub/Sub 권한 부여 단계가 조직의 "Domain Restricted Sharing" 정책에 막혀 GCP 조직 정책 관리자 권한이 있어야만 풀 수 있는 상황이었습니다(관리자 승인 없이 혼자 끝내기 원칙과 상충). 대신 앱이 구글챗 API를 주기적으로 직접 조회하는 방식으로 바꿔 IAM 권한 변경이 전혀 필요 없게 했습니다. 외부 무료 스케줄러(1분 간격)가 호출하면, 그 안에서 다시 짧은 간격으로 반복 조회해 수 초~수십 초 내에 새 메시지가 반영됩니다.

## v0.84.0 - 2026-08-06

- **구글챗 미러링 인증방식을 본인 계정 OAuth로 전환**: 관리자(도메인 위임) 승인 없이도, 구글챗 두 방 모두에 속한 본인 계정으로 한 번만 로그인하면 미러링이 동작하도록 바꿨습니다. 개발자 전용 링크(`/api/google-chat/oauth/start`)에서 로그인 한 번이면 설정이 끝납니다 - Google Workspace 관리자 콘솔에 들어갈 필요가 없어졌습니다.

## v0.83.0 - 2026-08-06

- **구글챗 미러링 수신/갱신 인프라 추가**: 구글챗에서 새 메시지가 오면 받아서 저장하는 웹훅 라우트(`/api/google-chat/webhook`)와, 연동이 끊기지 않게 매일 자동으로 구독을 갱신하는 크론(`/api/cron/renew-chat-subscriptions`)을 추가했습니다. 아직 Google Cloud 쪽 설정(다음 가이드 참고)을 마치기 전까지는 동작하지 않고, 화면에는 "연동 전입니다" 안내만 보입니다.

## v0.82.0 - 2026-08-06

- **업무탭 레이아웃 재구성**: 업무상황판을 "전체 업무목록" 제목 옆에 작은 숫자 배지로 옮겼습니다. 행정요청 기능(교사가 앱에서 직접 입력하던 방식)은 제거했습니다.
- **구글챗 미러링 자리 마련**: 예전 업무상황판/행정요청 자리에 구글챗 두 방(출결알림/선생님요청)을 읽기전용으로 실시간으로 보여주는 패널을 넣었습니다. 메시지마다 바로 "🔧 업무로 등록" 버튼으로 업무를 만들 수 있습니다. (실제 구글챗 연동은 별도 설정 후 동작합니다 - 연동 전에는 "연동 전입니다" 안내만 보입니다.)

## v0.81.0 - 2026-08-06

- **사건기록 "관련 학생" 입력을 검색-선택 방식으로 통일**: 자유 타이핑 칸을 없애고, 이름을 치면 학생 명부에서 검색되어 학년·반·학번까지 보고 정확히 골라 추가하는 방식 하나로 합쳤습니다. 이제 사건에 적히는 학생 이름은 항상 실제 학생 기록과 연결됩니다.

## v0.80.0 - 2026-08-06

- **학생 프로필 "관련 사건기록" 0건 표시 버그 수정**: 사건 입력 시 "관련 학생 연결" 선택을 안 했더라도(자유 텍스트 이름 칸에만 적힌 경우) 그 사건이 학생 프로필에 함께 표시되도록 수정했습니다. (아래 SQL 실행 시 기존 사건도 구조적으로 함께 연결됩니다.)
- **학생 정보 조회 검색범위 확대**: 재적생만 보이던 것을 비재적(전학/졸업 등) 학생도 포함해 검색되도록 넓혔습니다.

## v0.79.0 - 2026-08-06

- **사건·회의 항목 분류를 GIA시스템 기준으로 통일**: 매뉴얼항목(실무자용)/운영계획안항목(학부모용) 선택 목록과 AI 자동 분류가 이제 GIA시스템의 대분류>중분류>세부항목 체계를 그대로 사용합니다. 드롭다운도 대분류별로 묶어서 보여줍니다.
- **예상 문의/컴플레인 기능 제거**: 실무자매뉴얼의 "AI 예상 문의 제안받기" 기능과 제안함의 관련 탭을 없앴습니다.
- **새 사건 입력 시 처리상태 필드 제거**: 새로 입력할 때는 처리상태를 묻지 않고, 기존 사건을 수정할 때만 보이도록 정리했습니다.

## v0.78.0 - 2026-08-06

- **사건기록에 "조치사항" 추가**: 사건이 어떻게 마무리됐는지(실제로 취한 조치)를 남기는 칸을 새로 넣었습니다.
- **좁은 화면용 패널 접기 기능**: 사건기록·회의기록·AI 매뉴얼 작성·실무자매뉴얼 화면에서 목록/AI 제안 패널을 접고 펼 수 있습니다. 접은 상태는 로그인 계정별로 기억되어 다음에 다시 들어와도 그대로 유지됩니다.

## v0.77.1 - 2026-08-06

- **AI 서류 작성 다국어 지원**: 작성 언어를 한국어/영어/한국어+영어 중에서 고를 수 있습니다(국제학교 특성상 영문 서류 필요 대응). 예시 문구도 현장학습 동의서로 교체했습니다.

## v0.77.0 - 2026-08-06

- **AI 서류 작성**: 학교 문서함 > 서류함 옆에 새 메뉴가 생겼습니다. 필요한 서류 상황(예: 근로계약 조건)을 문장으로 설명하면 AI가 초안과 GIA시스템 분류를 함께 만들어주고, 검토 후 저장하면 서류함에 그 분류로 자동 등록됩니다.

## v0.76.0 - 2026-08-05

- **정책 항목·GIA시스템 항목 가나다순 정렬**: 항목 관리 화면, 사건/회의 항목 선택 드롭다운, GIA시스템 세부 항목이 모두 이름순으로 정렬됩니다.
- **학생 프로필 페이지 레이아웃 개선**: 요약 수치 + 바로가기 탭, 긴 목록 안쪽 스크롤 처리로 가독성을 높였습니다.
- **Slack 알림 연동**: 새 계정 등록 신청과 오류 발생 시 Slack으로 알림을 받을 수 있습니다(웹훅 설정 필요).

## v0.75.0 - 2026-08-05

- **제안함 "통합승인" 버튼 명칭 변경**: 학부모용·실무자용이 함께 묶여 나온 제안을 승인할 때 버튼 문구가 "승인 (둘 다)"였는데, 뜻이 더 명확한 "통합승인"으로 바꿨습니다(단일 문서 제안은 그대로 "승인").
- **"다시 분석하기" 기능 추가**: 지금까지는 사건·회의를 한 번 AI로 분석하면(제안함에 이미 들어간 뒤에는) 다시 분석할 방법이 없었습니다.
- **중복 분석 방지 유지**: 자동/일괄 분석은 지금처럼 고유번호 기준으로 한 번만 처리되고, 이미 분석된 기록을 다시 처리하려면 반드시 "다시 분석하기" 버튼을 직접 눌러야만 재분석이 일어납니다 (실수로 같은 기록이 중복 분석되는 일이 없습니다).
- **제안함에서 원본 기록 재분석 가능**: 제안함(검토대기) 화면에서도 사건·행사·회의 유형의 카드라면 바로 "🔄 다시 분석" 버튼으로 원본 기록을 다시 분석할 수 있습니다(원본 화면으로 돌아가지 않아도 됨).

## v0.74.0 - 2026-08-05

- **운영계획안·매뉴얼 고정 항목 체계 도입**: 지금까지는 AI가 사건/회의를 분류할 때마다 항목 이름을 그때그때 자유롭게 지어내서, 같은 주제인데 이름만 다른 항목이 계속 늘어날 수 있었습니다.
- **[정책 항목 관리] 화면 신설**: 학교 문서함 메뉴 안에 새 화면을 추가해, 관리자·행정직원이 운영계획안/매뉴얼 항목의 이름·설명·보유상태를 직접 추가·수정·삭제할 수 있습니다.
- **GIA시스템 편집 권한 확장**: 지금까지 관리자만 GIA시스템 화면을 보고 고칠 수 있었는데, 이제 행정직원도 볼 수 있고 항목명/설명을 직접 수정할 수 있습니다.
- **사건/회의 기록에 항목 선택 드롭다운 추가**: 사건기록·회의기록 작성/수정 화면에 "매뉴얼 항목(실무자용)"과 "운영계획안 항목(학부모용)" 드롭다운을 추가해, 작성자가 직접 위 고정 목록에서 골라 태그할 수 있습니다(비워두면 AI가 스캔할 때 자동으로 채웁니다).
- **기존 기록 소급 태깅**: [정책 항목 관리] 화면에 "🔄 기존 기록 소급 태깅" 버튼을 추가해, 새 항목 체계 도입 전에 이미 저장된 사건/회의도 AI가 훑어서 고정 목록 중 해당하는 항목으로 자동 태깅할 수 있습니다.

## v0.73.1 - 2026-08-05

- **출석부 5열 그리드**: 학생 목록을 화면 넓이에 맞춰 최대 5열 그리드로 배치했습니다.
- **출석부 메뉴 임시 비공개**: 사이드바 메뉴에서 출석부 항목을 뺐습니다.

## v0.73.0 - 2026-08-05

- **업무 삭제 버그 수정**: 업무를 삭제해도 등록자·담당자·관리자 화면에는 계속 목록에 남아있던 문제를 고쳤습니다.
- **업무 휴지통 영구삭제/비우기**: 휴지통에 "🗑️ 영구삭제"(개별)와 "🗑️ 휴지통 비우기"(일괄) 버튼을 추가했습니다.
- **행정요청 완료 처리자 표시**: 행정요청이 완료되면 누가 처리했는지(이름)가 요청 카드에 함께 표시됩니다.
- **학생 출석부 신설**: 담임교사가 매일 학생별 출결(출석/지각/결석/조퇴/기타)을 실시간으로 체크하고, 행정직원·관리자 등 다른 직원도 같은 화면을 실시간으로 보면서 결석·조퇴한 학생의 보호자에게 바로 전화/이메일로 연락하고 연락 완료 여부를 기록할 수 있는 "🗒️ 출석부" 메뉴를 새로 만들었습니다.
- **Toddle/ClassDojo 연동 조사**: 정규학기(Toddle)·계절학기(ClassDojo)에서 쓰는 학부모 소통 앱과 gia-ops 앱을 자동 연동할 방법을 조사했습니다.

## v0.72.0 - 2026-08-05

- **업무 코멘트/실시간 로그 분리**: 업무 상세창에서 코멘트를 남기면 상태변경·업무확인 등 시스템 로그가 코멘트창에도 함께 섞여 보이던 문제를 고쳤습니다.
- **GIA시스템 AI 제안 범위 제한**: "✨ AI로 추가/세분화 체크"(기존 "AI로 벤치마킹 제안받기")는 이제 기존에 정리해둔 항목을 절대 고치거나 지우지 않고, ①GIA에 아직 없는 시스템을 새로 추가하거나 ②이미 있는 항목 중 너무 뭉뚱그려진 것을 더 구체적인 하위 항목으로 쪼갤 수 있는지만 체크해서 제안하도록 프롬프트와 저장 로직을 다시 짰습니다.
- **GIA시스템 드롭다운 구조**: 대분류 안의 중분류 묶음이 전부 펼쳐진 채로 나열되던 것을, 기본은 접혀있고 눌러야 펼쳐지는 드롭다운으로 바꿨습니다.
- **학생 통합 조회 재확인**: "학생 정보 조회" 메뉴(관리자·행정직원)에서 이름/학번으로 검색해 들어가면 학적사항·학적이력·사건기록·주간 학생 관찰기록·업무/채팅 언급까지 한 화면에서 보이는 기존 기능을 재확인했습니다(신규 구현 아님).

## v0.71.0 - 2026-08-05

- **사건기록 채택·발행 통합**: 제안함에서 학부모용/실무자용을 따로 체크해 채택하던 방식을 없애고, 승인·보류·삭제가 항상 둘을 함께 처리하도록 통합했습니다(운영계획안 내용은 자동으로 실무자매뉴얼에도 들어가야 하므로).
- **채택 문구 톤 개편**: 운영계획안(학부모용)은 정중하고 친절한 안내 톤으로, 실무자매뉴얼 (실무자용)은 명확한 지시 톤으로 다듬되, 둘 다 법령 조문처럼 명시적이고 정확한 표현을 유지하면서 딱딱한 "~다"체 대신 정중한 "~하는 것으로 한다." 체로 종결하도록 AI 프롬프트를 손봤습니다.
- **행정요청 메뉴 개편**: 행정요청 메뉴는 이제 교사에게만 보이고, 관리자/행정직원은 업무 탭의 업무상황판에서 미처리 행정요청 건수를 바로 확인합니다(등록 시 자동으로 업무보드에도 올라가고 처리 상태가 자동 동기화되므로 별도 메뉴가 필요 없습니다).

## v0.70.0 - 2026-08-05

- **관리자 통합 대시보드 복구**: 반복 사건 유형, 학생 랭킹, 월별 운영 추이, 부서별 업무 완료율 같은 관리자 전용 운영 분석을 별도의 "관리자 통합 대시보드"(관리자 메뉴 → 통합 대시보드)로 다시 분리했습니다.
- **채택예정 화면 개편**: 제안함과 같은 방식으로 카테고리 탭(사건/행사/회의/AI매뉴얼/예상 문의/GIA시스템), 원본 사건 개요 표시, 학부모용·실무자용 카드 통합을 채택예정 화면에도 적용했습니다.
- **GIA시스템 항목 전면 재설계**: 기존 6개 항목(구비서류/내규/계약서/학생관리/교사관리/교직원관리)을 지우고, 다른 공립·사립·국제학교가 일반적으로 갖추는 시스템을 참고해 대분류(재정/인사·교직원/ 학사/운영/시설·안전/입학·홍보/행정·문서/정보보안·법무) → 중분류 → 세부 항목의 3단계로 44개 항목을 새로 구성했습니다.
- **AI 프롬프트 공유 캐시 블록 통합**: 기관 소개문과 참고 법령 목록을 쓰는 6개 AI 프롬프트(사건 분류, AI매뉴얼 초안 분류, 서류 추천, 서류 초안, 예상 문의/컴플레인, 채택예정 AI 검증)가 각자 전체 시스템 프롬프트를 하나의 문자열로 캐싱하던 것을, 공용 부분(SHARED_CACHE_CONTEXT)과 라우트별 부분으로 나눠 별도의 캐시 블록으로 보내도록 바꿨습니다.

## v0.69.1 - 2026-08-05

- **옵션 선택 시 최종 채택 내용 자동 기입**: 제안함에서 보완방안/학부모 안내멘트/학생 교육방법 옵션 앞에 동그라미(라디오) 선택을 추가했습니다.
- **재발방지 옵션을 실제 절차로 강화**: AI가 제안하는 보완/재발방지 방안이 추상적인 다짐이 아니라, 상황 발생 → 담당자의 즉시 조치 → 학부모에게 안내할 선택지 → 선택지별 후속 조치 → 단계별 학부모 고지 시점 → 최종 인계 확인까지, 실제로 그대로 따를 수 있는 순서와 책임 소재가 분명한 절차로 작성되도록 프롬프트를 강화했습니다.
- **즉시 채택 전 확인창**: 제안함 목록에서 학부모용/실무자용 체크박스를 눌러 바로 채택예정으로 보낼 때, 사건명과 최종 채택 내용을 다시 보여주고 확인을 받은 뒤에만 진행합니다.
- **채택예정 되돌리기**: 채택예정 화면에 "↩️ 되돌리기" 버튼을 추가했습니다.

## v0.69.0 - 2026-08-05

지난 통합 개선 제안서(v0.68.0)의 8개 항목을 전부 반영하고, AI 비용을 더 아끼는 방향으로 함께 설계했습니다.

- **① 원본 사건/회의 역참조 링크**: 매뉴얼 항목이 어떤 사건/행사/회의/GIA시스템 기록에서 왔는지 카드 하단에 칩으로 표시하고, 원본 목록 화면으로 바로 이동할 수 있습니다.
- **② 반복 사건 패턴 ↔ 매뉴얼 연결**: 홈 화면의 "최근 90일 반복 사건" 판정(AI 호출 없이 순수 집계, 비용 0)을 매뉴얼 화면에도 그대로 적용해, 관련 항목 카드에 "🔥 반복 사건 N건" 배지를 띄웁니다.
- **③ GIA시스템 자동 매칭**: 매뉴얼이 발행될 때, "미보유" 상태인 GIA시스템 항목의 이름이 발행 문구에 그대로 언급되어 있으면(AI 호출 없이 단순 포함 여부만 확인) 자동으로 연결해두고, GIA시스템 화면에서 안내 문구로 보여줍니다.
- **④ 정책영역(도메인) 분류**: 사건/회의/AI매뉴얼 작성 시 AI가 항목을 8개 정책영역(학사· 안전/보건·시설·인사·재무/환불·개인정보·민원대응·행정일반) 중 하나로 함께 분류합니다(기존 분류 호출에 슬쩍 얹어서 별도 AI 호출은 늘리지 않았습니다).
- **⑤ 매뉴얼 변경 이력 관리**: 항목이 수정될 때마다 이전 내용이 자동으로 이력에 남고, "변경 이력" 버튼으로 과거 버전을 언제든 확인할 수 있습니다.
- **⑥ AI매뉴얼 작성 시 유사 기록 추천**: 새 초안을 작성하면, AI 호출 없이 키워드 겹침만으로 과거 사건/회의/이미 발행된 매뉴얼 중 관련 있어 보이는 기록을 함께 보여줍니다.
- **⑦ 발행 전 학부모 관점 체크리스트**: 학부모용 항목을 AI 검증할 때는 "학부모가 이 문구만 읽고 오해 없이 이해할 수 있는가"를 최우선 관점으로 점검하도록 프롬프트를 강화하고, 버튼 라벨도 "학부모 관점 AI 검증"으로 구분했습니다.
- **⑧ 매뉴얼 정기 리뷰 사이클**: 매주 월요일, 6개월 이상 수정되지 않은 항목과 최근 90일 내 관련 사건이 5건 이상 급증한 항목을 자동으로 찾아 매뉴얼 화면 상단에 배너로 알립니다(AI 호출 없이 조건 판정만).
- **AI 과금 최소화**: 학부모용 승인 시 톤을 다듬는 AI 호출을 Sonnet에서 Haiku로 낮췄습니다 (이미 승인된 문구를 존댓말로만 바꾸는 단순 작업이라 저렴한 모델로 충분합니다).

## v0.68.0 - 2026-08-05

- **제안 카드에 사건 개요 노출**: 사건/행사/회의/AI매뉴얼 초안에서 나온 제안을 펼치면, 그 제안이 어떤 원본 기록에서 나온 건지 제목·내용 요약을 함께 보여줍니다.
- **체크박스로 즉시 채택**: 카드를 펼치지 않아도 목록에서 체크 한 번으로 바로 승인해 채택예정으로 옮길 수 있습니다.
- **"다음 옵션" 반복 문구 제거**: 보완방안/학부모 안내멘트/학생교육 옵션이 화면에 구분자 글자 없이 번호가 매겨진 카드 형태로 깔끔하게 표시됩니다.
- **제안서 형식으로 가독성 개선**: 사건 개요 / 채택 내용 / 제안 옵션 / 법령 근거를 구분선으로 나눠 문서처럼 읽기 쉽게 재구성했습니다.
- **학부모용·실무자용 통합 카드**: 같은 사건에서 나온 학부모용/실무자용 제안 두 건이 카드 하나로 합쳐지고, 안에서 탭으로 전환해서 봅니다(전에는 같은 사건이 카드 두 개로 따로 보였습니다).
- **학부모용 승인 시 톤 다듬기 확대**: 전에는 예상 문의(컴플레인) 제안만 승인 시 AI가 톤을 다듬었는데, 이제 모든 학부모용 승인 건이 정중하고 친절한 문구로 한 번 더 다듬어집니다.
- **AI가 기존 항목을 우선 재사용**: 사건/행사/회의/AI매뉴얼 제안을 만들 때, 운영계획안· 실무자매뉴얼에 이미 있는 항목 목록을 AI에게 실제로 전달해서, 비슷한 내용이 항목명만 다르게 계속 새로 생기는 문제를 줄였습니다.
- **사건·회의·AI매뉴얼 통합 개선 제안서**: 앞으로 더 유용하게 연결할 수 있는 방안(발행 후 원본 연결 유지, GIA시스템 자동 매칭, 매뉴얼 변경 이력 관리 등)을 정리한 제안서를 작성해 전달했습니다.

## v0.67.0 - 2026-08-05

- **행정요청 카테고리를 관리자가 직접 관리**: 사물함파손/물품구입 같은 요청 분류를 관리자가 화면에서 새로 추가하거나 숨길 수 있습니다(운영관리 화면이 아니라 행정요청 화면 안에 "카테고리 관리" 패널로 있습니다).
- **행정요청 등록 시 초등부 업무보드에 자동 등록**: 교사가 요청을 등록하면 초등부 전체 업무창에도 업무가 함께 생성됩니다.
  - 업무보드에서 담당자가 "확인" 체크를 하면 요청 목록의 빨간불이 초록불로 자동으로 바뀌고, 업무를 완료로 옮기면 요청도 자동으로 완료 처리됩니다.
  - 교사 화면은 "내가 등록한 요청"과 그 아래 "완료된 요청" 두 목록으로 나눠서, 진행 중인 요청과 끝난 요청을 한눈에 구분할 수 있습니다.
- **행정요청 코멘트 기능**: 요청 하나하나에 코멘트를 주고받을 수 있고, 새 코멘트는 실시간으로 반영됩니다.
- **한/영 자동 번역**: 요청 제목·내용과 코멘트를 한국어로 적든 영어로 적든 자동으로 번역되어 함께 표시됩니다(원어민 교사가 많은 점을 고려했습니다).
- **행정요청 화면 전체 영어 병기**: 버튼·라벨·안내 문구에 영어를 함께 표기했습니다.

## v0.66.1 - 2026-08-05

- **교사 메뉴 영어 병기**: 교사 계정에 원어민 교사가 있을 수 있어, 사이드바·모바일 메뉴의 "행정요청"에 영어("Staff Requests")를 함께 표기했습니다.
- **메뉴 줄바꿈 정리**: 한글/영어 라벨이 길어져도 어색하게 줄이 꺾이지 않고 한 줄로 깔끔하게 잘리도록(말줄임표) 사이드바·모바일 메뉴 스타일을 정리했습니다.

## v0.66.0 - 2026-08-05

- **교사 메뉴에서 학사일정 제거**: 교사 계정에는 더 이상 학사일정 메뉴가 보이지 않습니다.
- **행정요청 메뉴 신설**: 교사가 사물함 파손·물품 구입·아픈 학생 인계·출결 상황 문의처럼 행정직원에게 부탁할 일을 등록하면, 행정직원/관리자가 접수대기 → 처리중 → 완료로 상태를 바꾸고 처리 메모를 남길 수 있습니다.
- **권한 미리보기 전환 버그 수정**: 관리자가 "교사"로 미리보기 전환한 뒤 다른 권한으로 다시 바꿀 수 없던 문제를 고쳤습니다.
- **브라우저 탭 아이콘 교체**: Vercel 기본 아이콘 대신 로그인 화면의 GIA 크레스트 로고가 탭 아이콘으로 표시됩니다.

## v0.65.0 - 2026-08-05

- **운영관리 부메뉴 정리**: "매뉴얼" 항목을 뺐습니다.
- **화면 전체 크래시 방지 강화**: 검토대기 배지처럼 부가적인 정보 조회가 실패해도 로그인·사이드바를 포함한 화면 전체가 함께 죽지 않도록 서로 독립시켰습니다.
- **로그성 데이터 조회 속도 개선**: 계속 쌓이기만 하는 오류·AI 사용 기록, 미해결 문의 조회에 인덱스를 추가했습니다.
- **동시 사용 안전장치 확대**: 학기 전환과 채택예정 발행을 여러 명이 거의 동시에 눌러도 데이터가 꼬이지 않도록 서버에서 한 번에 처리하게 바꿨습니다.
- **관리자 대시보드에 시스템 상태 카드 추가**: 최근 24시간 오류 급증 여부와 마지막 백업 시각을 한눈에 볼 수 있습니다.
- **자동 일일 백업**: 매일 한 번 핵심 데이터를 자동으로 백업해두어, 수동 백업을 깜빡한 날에도 최소한의 안전망이 있습니다.

## v0.64.0 - 2026-08-05

- **업무탭 좌우 분할, 부서 메모장 추가**: "🔔 실시간 로그" 자리가 왼쪽 메모장 / 오른쪽 실시간 로그로 나뉩니다.
- **학교관리 부메뉴 정리**: 구글시트로 가져오기 항목을 부메뉴에서 뺐습니다.
- **부메뉴 구분선 추가**: 운영 관리 / 학교 관리 / 학교 문서함 / 주간 학생 관찰기록 / 관리자 부메뉴에 목적별 구분선을 넣어 항목이 많은 메뉴도 한눈에 구분되도록 했습니다.
- **신청서 가져오기 학기선택을 실제 등록된 학기로 연동**: 이전에는 연도를 직접 입력하고 학기 종류를 고정 목록에서 골랐는데, 이제 학기 관리 화면에 실제로 등록된 연도·학기 목록을 그대로 보여줍니다.
- **버전 기록 화면 정리**: 기술적인 DB 변경 안내를 빼고, 어떤 기능이 새로 생겼는지만 짧고 읽기 편하게 정리했습니다.
- **빌드 최적화**: 매뉴얼 리치 텍스트 편집기를 실제로 편집 화면을 열 때만 불러오도록 바꿔, 매뉴얼을 조회만 하는 방문에서는 불필요한 코드를 받지 않도록 했습니다.

## v0.63.0 - 2026-08-05

- **신청서 붙여넣기 전 학기/목적 선택**: 신청서(학기/행사) 탭에서 구글시트를 붙여넣기 전에 연도·학기(또는 캠프)·목적을 먼저 선택하도록 바꿨습니다(예: "26년 3학기 인원모집", "26년 여름캠프2 바자회 행사").
- **학사일정 → 학사일정달력 / 학기준비 두 화면으로 분리**: 기존 학사일정 화면은 "학사일정달력"으로 이름을 유지하고(체크리스트 그대로), 새로 "학기준비" 화면을 추가했습니다.
- **학기준비 화면 신설**: 연도와 학기(또는 캠프)를 선택하면 같은 학기 유형의 지난 회차 기록을 한 번에 모아 보여줍니다 - ①지난 학기 돌아보기(잘한 점/아쉬운 점/다음 제안), ②그때 가져온 신청서(구글폼) 기록(연도·목적별 건수), ③그 학기 준비 기간에 있었던 업무·회의 타임라인.
- **관리자 권한 미리보기(역할 전환)**: 관리자 계정 사이드바의 로그아웃 버튼 바로 위에 "🎭 권한 미리보기" 드롭다운이 생겼습니다.

## v0.62.0 - 2026-08-05

- **GIA/다크 테마 로고 가시성 수정**: 로고 이미지가 투명 배경 위에 남색 잉크로만 그려져 있어서 짙은 남색 사이드바(GIA 테마) 위에서 거의 안 보였습니다.
- **관리자 전용 이름/소속 편집**: 사용자 관리 화면에서 관리자 계정으로 로그인하면 이름 옆에 ✏️ 버튼이 나타나 이름/소속을 직접 정정할 수 있습니다.
- **신청서(구글폼) 가져오기**: 학교관리 → 구글시트로 가져오기 화면에 "📋 신청서(학기/행사)" 탭을 추가했습니다.

## v0.61.1 - 2026-08-05

- **GIA 테마 본문 배경 밝게 조정**: 바로 전 업데이트(v0.61.0)에서 GIA 테마를 고르면 사건기록· 매뉴얼 등 본문 화면까지 사이드바와 같은 짙은 남색 배경을 썼는데, 검은 글자와 대비가 안 맞아 읽기 힘들다는 피드백을 반영해 사이드바/헤더는 짙은 남색 그대로 두고, 본문 배경만 골드 톤과 어울리는 밝은 아이보리/샴페인 그라디언트로 바꿨습니다.

## v0.61.0 - 2026-08-05

- **페이지 배경도 테마 적용**: 지금까지는 사이드바/헤더 같은 공용 틀만 테마(라이트/다크/ 리퀴드글라스/GIA)를 따라가고, 사건기록·회의기록·제안함·매뉴얼·문서함·사용자관리·학사일정 등 실제 화면 배경은 항상 밝은 회색 고정이었습니다.
- **테마별 글자색 가독성 보정**: 배경이 어두워지는 다크·GIA 테마에서, 화면 안의 글자색이 기존처럼 어두운 톤 그대로면 안 보이는 문제가 있어 자동으로 밝은 톤으로 바뀌도록 손봤습니다.

## v0.60.0 - 2026-08-04

- **오류 화면**: 서버 쪽 오류가 나면 이제 Next.js 기본 크래시 화면 대신, 다시 시도/새로고침 버튼이 있는 우호적인 화면이 뜹니다.
- **삭제 확인/실패 알림**: 브라우저 기본 confirm/alert 대신 앱 안에서 자연스럽게 뜨는 확인창과 토스트 알림으로 20개 화면(사건/업무/채팅/제안함/문서/매뉴얼 등 46곳)을 전부 바꿨습니다.
- **업무 휴지통(7일 복구)**: 업무를 삭제해도 즉시 영구 삭제되지 않고, 사이드바 업무탭의 "🗑 휴지통"에서 7일 안에 복구할 수 있습니다(등록자·담당자·관리자만 볼 수 있음).
- **커맨드팔레트**: 어디서든 ⌘K/Ctrl+K를 누르면 메뉴 바로가기 + 통합검색을 한 번에 쓸 수 있는 창이 뜹니다.
- **연결 끊김 알림**: 인터넷이 끊기면 상단에 배너로 알리고, 다시 연결되면 자동으로 화면을 최신 내용으로 갱신합니다.
- **알림 묶어보기**: 프로필 옆 빨간 배지를 누르면 숫자만이 아니라 실제로 어떤 업무가 미확인인지 목록으로 바로 보여줍니다.
- **업무 선후관계**: 업무 상세에서 "선행 업무"를 지정할 수 있고, 아직 안 끝났으면 경고 배너로 알려줍니다(강제로 막지는 않습니다).
- **사건기록 템플릿**: 새 사건 입력 화면에 안전사고/교우관계/규칙위반/학부모민원 등 자주 쓰는 유형의 시작 틀(빠른 시작 버튼)을 추가했습니다.
- 참고: 알림을 매일 이메일로 받아보는 기능은 별도 이메일 발송 서비스(API 키) 연동이 필요해 이번 라운드에서는 빼고, 대신 위 "알림 묶어보기"로 인앱에서 확인할 수 있게 했습니다.

## v0.59.0 - 2026-08-04

- **버전 기록**: 사이드바 문의및건의사항 바로 아래에 현재 버전(v0.59.0)이 뜨고, 누르면 지금까지의 모든 업데이트 내역을 최신순으로 볼 수 있는 [버전 기록] 화면(`/changelog`)으로 이동합니다.
- **테마 정교화**: 리퀴드글라스 테마는 사이드바 메뉴 버튼이 실제로 반투명+블러 유리질감으로 뜨고, GIA 테마는 사이드바 배경이 로고 남색, 선택된 메뉴가 골드 톤으로 강조되도록 다듬었습니다.
- **모션**: 사이드바 부메뉴(플라이아웃)와 검색 결과가 아래로 살짝 미끄러지며 나타나고, 업무 알림 배지가 새로 생길 때 팝(pop)하듯 나타나며, 메뉴 버튼을 누르면 살짝 눌리는 느낌을 주는 등 절제된 모션을 셸 전반에 추가했습니다(동작이 불편한 분들을 위해 시스템의 "동작 줄이기" 설정을 따르도록 했습니다).

## v0.58.0 - 2026-08-04

- **실시간 로그 세 줄**: 업무 탭 하단 "실시간 로그" 고정 높이 영역이 5줄 → 3줄로 줄었습니다 (헤더 클릭 시 뜨는 "전체보기" 팝업은 그대로 전체를 보여줍니다).
- **테마 4종**: [내 계정 설정]에 테마 선택이 추가됐습니다 - 라이트(기존)/다크/리퀴드 글라스(반투명+블러)/GIA(로고 남색+골드).

## v0.57.7 - 2026-08-04

알림 표시를 두 곳으로 나눴습니다.

- **프로필 아이콘 모서리(빨간 원)**: 이제 "미확인" 업무 개수만 보여줍니다.
- **프로필 이름 오른쪽(회색 사각 배지)**: 확인 여부와 무관하게 "내 업무" 총 개수를 항상 보여줍니다.
- **즉시 반영 버그 수정**: "업무 확인" 체크박스를 누르면 Realtime 전파를 기다리지 않고 그 자리에서 바로 다시 세도록 고쳤습니다 - 예전에는 체크한 뒤 다른 페이지로 이동해야만 깜빡임이 사라졌습니다.

## v0.57.6 - 2026-08-04

- **숫자**: 이제 "새로 생긴 업무 수"가 아니라 "지금 내 업무함에 있는 업무 개수"입니다(내 업무 목록 위젯과 동일한 기준: 내가 등록자거나, 태그된 담당자거나, [전체]로 등록된 업무 중 아직 완료 안 된 것).
- **깜빡임**: 그중 내가 아직 "업무 확인" 체크를 안 한 업무가 하나라도 있으면 빨간색으로 깜빡입니다.

## v0.57.5 - 2026-08-04

알림 배지에서 "새 업무" 개수가 안 뜨는 문제를 고쳤습니다.

- 원인: 업무 배지는 "마지막으로 업무 탭을 연 시각 이후 등록된 업무"만 셌는데, 업무 탭을 아직 한 번도 연 적이 없으면(=task_list_reads에 내 기록이 아직 없으면) 그 기준 시각이 없어서 통째로 0으로 처리하고 있었습니다.

## v0.57.4 - 2026-08-04

알림 배지(안 읽은 채팅 + 새 업무) 기능을 다시 넣었습니다.

- **원인**: 배지가 데스크톱 사이드바 프로필 옆 + 모바일 헤더 계정 아이콘 옆, 두 군데에 동시에 렌더링되는데(화면 크기에 따라 CSS로만 숨기는 방식이라 화면 크기와 무관하게 둘 다 항상 마운트됨), 각 배지가 자기 안에서 데이터 조회 + Realtime 구독을 따로 했습니다.
- **수정**: 데이터 조회/실시간 구독 로직을 NotificationProvider 하나로 분리해서 화면 최상단(layout)에서 딱 한 번만 실행되게 하고, 실제 눈에 보이는 배지 두 개는 그 결과를 Context로 읽기만 하는 얇은 컴포넌트로 바꿨습니다.
- **DB**: task_list_reads 테이블을 다시 만듭니다.

## v0.57.3 - 2026-08-04

스테이징 주소가 "This page couldn't load"로 아예 안 뜨는 문제 진단 중 - v0.57.2(알림 배지)를 일단 되돌렸습니다.

- v0.57.2에서 추가한 사이드바 알림 배지(NotificationBell)와 관련 코드를 전부 되돌렸습니다 (task_list_reads 조회/구독, 프로필 옆 빨간 배지 UI).

- Vercel Authentication(배포 보호)은 이미 꺼져 있는 상태에서도 로드가 안 됐던 걸 확인했고, 서버로 직접 보낸 요청은 계속 정상 응답이 와서 배포 자체가 완전히 죽은 건 아니었습니다.

## v0.57.1 - 2026-08-04

사이드바 메뉴 하이라이트 버그를 고치고, AI 기능 on/off를 진짜 스위치 모양으로 바꿨습니다.

- **메뉴 하이라이트 버그 수정**: "학교관리"를 누르면 "관리자"까지 같이 색이 바뀌고, "학교문서함"을 누르면 "학교관리"·"관리자"가 같이 바뀌고, "관리자"를 눌러도 "학교관리"가 같이 바뀌던 문제를 고쳤습니다.
- **AI 기능 on/off를 스위치 버튼으로**: 관리자 대시보드의 "켜짐/일시정지중" 텍스트 버튼을 실제로 딸깍 켜고 끄는 토글 스위치 모양으로 바꿨습니다.

## v0.57.0 - 2026-08-04

학생 명부 관리(학생 관리 페이지)에 학생 인적사항 항목을 늘리고, 앞으로 필요한 항목을 직접 추가할 수 있는 "칼럼 추가" 기능과 구글시트 같은 정렬 기능을 넣었습니다.

- **기록 항목 추가**: 학생이름·영어이름은 이미 있었고, 이번에 보호자 이메일·성별·알러지를 새로 추가했습니다(보호자 연락처·주소·생일은 이미 있던 항목을 이 화면에서도 바로 입력/수정할 수 있게 했습니다).
- **칼럼 추가 기능**: "+ 칼럼 추가" 버튼으로 원하는 이름(예: 형제자매, 통학버스 노선)과 입력 형식(텍스트/숫자/날짜)을 정해 새 칼럼을 만들 수 있습니다.
- **학생 명부 표 + 정렬**: 학생 명부 관리(주간 학생 관찰기록 → 관리 → 학생 명부 관리)에서 전체 학생을 표로 보고, 학년 → 반 → 이름(가나다) 순으로 기본 정렬됩니다.
- 모든 칸은 클릭해서 바로 수정할 수 있습니다(기존 영어이름 인라인 수정 방식을 전체 칸으로 확장).
- 학생 통합 프로필(/students/[id]) 화면에도 보호자 이메일·성별·알러지·직접 추가한 칼럼 값이 함께 표시됩니다.

## v0.56.11 - 2026-08-04

- **접속자 배지 툴팁 개선**: 브라우저 기본 툴팁은 뜨기까지 시간이 걸리고 잘 안 보여서 놓치기 쉬웠습니다.

## v0.56.10 - 2026-08-04

새 업무가 "내 업무목록"에 들어오면(내가 태그되었거나 [전체]로 등록된 경우) 확인창(alert) 없이 화면 오른쪽 아래에 잠시 알림이 떴다가 자동으로 사라지도록 했습니다.

- 이미 있던 "업무 상태 변경 토스트"와 같은 방식으로, 새 업무 등록 시에도 알림이 뜨도록 확장했습니다.
- 내가 직접 등록한 업무는 이미 알고 있으니 알림에서 제외됩니다.

## v0.56.9 - 2026-08-04

접속중인 사람이 누구인지 마우스로 확인할 수 있게 하고, 업무 상세창의 코멘트 영역을 코멘트/처리사항 둘로 나누고, 처리사항이 업무기록과 보고서(화면+PDF)까지 이어지도록 만들었습니다.

- **접속자 호버 툴팁**: 업무 탭 상단 "🟢 N명 접속중" 배지에 마우스를 올리면 지금 이 페이지를 보고 있는 사람들의 이름이 뜹니다.
- **코멘트/처리사항 분할**: 업무를 클릭해 여는 상세창의 코멘트 영역을 위/아래로 나눴습니다.
- **처리사항이 보관함·보고서까지 이어짐**: 처리사항은 업무 자체에 저장되는 값이라 업무가 완료→보관함(업무기록)으로 넘어가도 그대로 남고, 업무기록 화면과 업무 보고서 화면·PDF에서도 "처리사항/처리결과"로 함께 보입니다.
- **코멘트가 실시간 로그에도 기록됨**: 업무에 코멘트를 남기면 상단 실시간 로그에도 "OOO님이 코멘트를 남겼습니다: ..." 한 줄로 함께 기록됩니다.
- **실시간 로그 5줄 제한**: 확인해보니 이 부분은 이전 작업에서 이미 반영되어 있었습니다 (실시간 로그는 최대 5줄까지만 보이고, 위로 스크롤하면 과거 로그를 더 불러옵니다).

## v0.56.8 - 2026-08-04

업무 확인목록에서 등록자 본인을 빼고, "내 업무목록"을 내 것/전체 두 칸으로 나누고, 데이터 백업/복원 기능을 새로 만들었습니다.

- **확인목록에서 등록자 제외**: [전체] 모드로 업무를 등록하면 부서원 전원(등록자 포함)이 담당자가 되는데, 예전에는 등록자 본인도 "확인 안 함"으로 남아있었습니다.
- **내 업무목록 / 전체 업무목록 좌우 분할**: 업무 탭 오른쪽 위 "내 업무목록" 자리를 반으로 나눠, 왼쪽은 내가 등록했거나 태그되거나 [전체] 모드인 업무만, 오른쪽은 지금 볼 수 있는 업무 전체를 보여줍니다.
- **데이터 백업/복원 신설**: 관리자 메뉴에 "데이터 백업" 화면을 추가했습니다.
- **보안 PIN 관련**: 이전 버전(v0.56.7)에서 PIN 2차 확인을 없앤 데 이어, 이번 백업 기능이 "데이터가 꼬여도 되돌릴 수 있는" 안전망 역할을 합니다.

## v0.56.7 - 2026-08-04

보안 PIN 2차 확인 기능을 없애고, 업무 등록 시 고른 공개범위([나]/[공유]/[전체])가 실제 조회 단계에서도 지켜지도록 손봤습니다.

- **PIN 2차 보안 제거**: 구글 로그인(회사 계정만 허용) + 관리자 승인 두 단계로도 충분히 안전하다고 판단해, 로그인 뒤 한 번 더 개인 PIN을 입력하던 화면을 없앴습니다.
- **업무 공개범위를 실제로 제한**: 지금까지는 [나](개인)/[공유](태그)로 등록해도 화면에서만 안 보이게 감춰뒀을 뿐, 데이터 자체는 giamicro.com 로그인 계정이면 누구나 조회할 수 있었습니다.
- **내 업무목록 위젯 보완**: "내 업무목록"이 이제 내가 등록한 업무와 나를 태그한 업무를 함께 보여줍니다(예전에는 담당자로 태그된 것만 보였음) - [공유]로 다른 사람만 태그해도 등록한 나에게 바로 보입니다.

## v0.56.6 - 2026-08-04

사이드바 왼쪽 아래 미니 달력을 누르면 학사일정 페이지로 바로 넘어가도록 바꿨습니다.

- **달력 클릭 → 학사일정 이동**: 날짜 칸이든 달력의 다른 빈 곳이든, 어디를 눌러도 `/academic-calendar`(학사일정)로 이동합니다.
- **OS 캘린더 앱 열기 기능 제거**: 날짜를 누르면 맥 캘린더 앱(또는 .ics 다운로드)을 여는 기존 기능이 실제로는 잘 뜨지 않는다는 제보를 받아 제거했습니다.
- **"학사일정 보기" 버튼 제거**: 달력 자체를 누르면 바로 이동하므로 별도 하단 버튼은 중복이라 없앴습니다.

## v0.56.5 - 2026-08-04

- **화면을 막던 조회 축소(5개 → 3개)**: 로그인 확인과 배지 숫자만 화면 렌더링 전에 꼭 필요하고, 학기 배지·AI 기능 정지 배너는 "장식성" 정보라 화면을 막을 이유가 없었습니다.
- 메뉴/페이지별 데이터 조회 구조를 점검해 대부분 이미 병렬 조회로 잘 구성돼 있음을 확인했습니다(사건기록/홈 등 - 이전 라운드에서 이미 최적화됨).

**추가로 확인해보시면 좋은 인프라 설정 2가지** (코드로는 손댈 수 없어 대시보드에서
직접 확인이 필요합니다):
1. Supabase 대시보드 → Settings → API → JWT Keys에서 "Legacy JWT secret" 대신
   "Asymmetric keys(비대칭키)"를 쓰고 있는지 확인해주세요. 비대칭키면 로그인 확인이
   외부 네트워크 왕복 없이 로컬에서 끝나 모든 페이지 이동이 한 단계 더 빨라집니다.
2. Vercel 프로젝트 리전과 Supabase 프로젝트 리전이 같은 지역(가까운 지역)인지
   확인해주세요. 두 서비스가 멀리 떨어져 있으면 DB 조회 한 번마다 그 거리만큼
   지연이 계속 누적됩니다.

## v0.56.4 - 2026-08-04

메뉴를 눌렀을 때 화면 전환이 느리다는 제보를 확인해 수정했습니다.

- **메뉴 hover 시 미리 불러오기 복구**: 마우스를 메뉴 위에 올리는 순간(부메뉴가 펼쳐지는 순간 포함) 그 메뉴가 갈 화면 데이터를 미리 받아두도록 했습니다.
- **최근 열어본 화면 재사용(30초)**: 같은 메뉴를 30초 안에 다시 열면 서버에 다시 묻지 않고 방금 받아둔 화면을 그대로 재사용하도록 설정했습니다.
- 상태표시줄에 링크 주소가 안 뜨는 기존 동작은 그대로 유지됩니다.

## v0.56.3 - 2026-08-04

여러 사람이 "AI 제안 만들기"(사건/행사/회의 새 기록 분석)를 거의 동시에 눌러도 같은 기록이 중복 처리되지 않도록 손봤습니다.

- **원자적 선점(claim) 처리**: AI를 부르기 전에 먼저 "이 기록은 내가 처리한다"고 DB에 원자적으로 표시합니다.
- **DB 이중 방어선**: 사건/행사에서 만들어지는 제안에 유니크 제약을 추가해, 혹시 위 로직에 틈이 있어도 같은 기록·같은 문서용 제안이 중복 저장되는 걸 DB가 한 번 더 막아줍니다.

## v0.56.2 - 2026-08-04

학사일정 달력에도 대한민국 공휴일이 표시됩니다(사이드바 메인 달력과 동일한 공휴일 데이터).

## v0.56.1 - 2026-08-04

사이드바 위쪽을 더 정돈하고, 메뉴가 스크롤 없이 한눈에 더 많이 보이도록 손봤습니다.

- **학기 표시 가운데 정렬**: 로고 아래 진행중인 학기 배지가 가운데로 정렬됩니다.
- **검색+달력 위젯 통합**: 따로 있던 통합 검색창과 축소 달력을 검색이 위, 달력이 아래에 오는 하나의 상자로 합치고 전체 크기를 더 줄였습니다 - 그만큼 메뉴 목록이 보이는 영역이 넓어집니다.
- **메뉴 목록 컴팩트화**: 사이드바 카테고리 각 줄의 위아래 여백을 줄여서, 같은 화면 높이에 더 많은 메뉴가 스크롤 없이 한눈에 들어옵니다.

## v0.56.0 - 2026-08-04

새 메뉴 "학사일정"을 추가했습니다.

- **학사일정 달력 화면 (`/academic-calendar`, 신규)**: 이번 달 달력에 학사 준비 업무가 날짜별로 표시됩니다(예: 학기 시작 14일 전 - 학생명단 확정, 7일 전 - 교실 물품 점검).
- **사이드바 메인 달력과 연동**: 사이드바에 항상 떠 있는 달력(시계 위젯)에도 학사일정이 있는 날짜에 작은 초록 점이 표시되고, 달력 아래 "📅 학사일정 보기" 링크로 바로 이동합니다.
- 여러 직원이 동시에 페이지를 열어도 학기별 항목은 중복 없이 한 번만 자동 생성됩니다(DB 고유 제약 + 안전한 재시도 처리, 반복 업무 자동 재등록 기능과 동일한 방식).

## v0.55.1 - 2026-08-04

채팅을 계속 치면 채팅창이 아래로 늘어나면서 브라우저 페이지 전체가 스크롤돼, 사이드바 메뉴가 화면 위로 밀려 올라가버리는 문제를 고쳤습니다.

- **화면 레이아웃 높이 고정**: 사이드바+본문을 감싸는 바깥 틀이 "최소 높이"(min-height)로만 잡혀 있어서, 채팅처럼 내용이 계속 늘어나는 화면에서는 그 틀 자체가 계속 커지며 전체 페이지가 스크롤됐습니다.

## v0.55.0 - 2026-08-04

전체 메뉴 구조를 점검해 중복되는 화면을 통합하고, 검토가 필요한 항목을 사이드바에서 바로 알 수 있도록 배지를 추가했습니다.

- **검토 대기 배지**: 사이드바 운영 관리 메뉴에 제안함(검토대기)·채택예정(발행대기) 건수가 빨간 숫자 배지로 표시됩니다.
- **대시보드 통합**: 예전에 따로 있던 "관리자 대시보드"(반복 사건·학생 랭킹·월별 추이·부서별 업무 완료율 분석)를 "학교 관리 대시보드"(/school) 하나로 합쳤습니다.

## v0.54.0 - 2026-08-04

학교관리 아래에 "학교 문서함" 메뉴를 새로 만들어, GIA에서 만들어지는 보고서·매뉴얼·운영계획안· 서류를 한곳에서 열람·검색·인쇄할 수 있도록 통합했습니다.

- **학교 문서함 (신규 메뉴)**: 사이드바에 🗄️ 학교 문서함을 새로 추가했습니다.
- **문서함 홈 (`/school/documents`)**: 매뉴얼·운영계획안·서류함에 등록된 문서를 최근 등록·수정 순으로 모아 보여주고, 이름/카테고리로 바로 검색할 수 있습니다.
- **보고서 통합 화면 (`/school/documents/reports`)**: 업무 보고서와 회의 보고서를 탭 하나로 오가며 볼 수 있습니다(기존 `/work/report`, `/meetings/report` 화면과 동일한 내용이며, 두 메뉴는 그대로 유지됩니다).
- **매뉴얼/운영계획안 바로가기**: 매뉴얼 화면이 `?doc=` 링크로 들어오면 실무자매뉴얼/운영계획안 탭이 바로 열리도록 했습니다.
- **서류함 이동**: 서류함 메뉴를 운영관리에서 학교 문서함으로 옮겼습니다(화면·기능은 그대로).

## v0.53.1 - 2026-08-04

사이드바 메뉴에 마우스를 올리면 브라우저 창 아래쪽 상태표시줄에 계속 링크 주소가 떠서 거슬리던 문제를 고쳤습니다.

- **메뉴 호버 시 주소 미리보기 제거**: 사이드바 메뉴(주메뉴/부메뉴)와 모바일 하단 메뉴를 실제 `<a href>` 링크 대신 클릭 시 이동하는 버튼 방식으로 바꿨습니다.

## v0.53.0 - 2026-08-04

사이드바 부메뉴가 화면 아래로 잘리는 문제를 고치고, 업무·회의를 일간/주간/월간 보고서로 정리해 바로 인쇄(PDF)할 수 있는 화면을 새로 만들었습니다.

- **부메뉴 잘림 수정**: 사이드바 아래쪽 메뉴에 마우스를 올리면 뜨는 부메뉴가 화면 밑으로 잘려나가 누르기 어려웠던 문제를 고쳤습니다.
- **업무 보고서 (신규, 🗂 업무 페이지 상단 아이콘 → 📊 업무 보고서, `/work/report`)**: 일간 ·주간·월간 단위로 그 기간에 완료된 업무(언제·누가·무엇을)와 아직 진행 중이던 업무 현황을 한 화면에 모아 보여줍니다.
- **회의 보고서 (신규, 운영 관리 → 회의 보고서, `/meetings/report`)**: 마찬가지로 일간·주간 ·월간 단위로 그 기간의 회의 기록을 날짜순으로 모아 보여주고, 인쇄용 PDF로 바로 열 수 있습니다.

## v0.52.0 - 2026-08-04

업무 페이지처럼 각 메뉴 화면 우상단에 ❓ 사용 가이드 버튼을 추가했습니다.

- **공용 가이드 컴포넌트**: 기존 업무 페이지 전용이던 가이드 모달을 `GuideModal`/`GuideButton` 공용 컴포넌트로 일반화해, 제목과 내용만 넘기면 어느 화면에서도 재사용할 수 있게 만들었습니다.
- **전체 메뉴에 적용**: 학교 관리, 학생 정보 조회, 구글시트 가져오기, 관리자 대시보드, 내 계정 설정, 학생/반/과목 관리, 내 담임반/담당과목, 리포트 프린트, 반별 작성 현황, 통계 대시보드, 사건기록, 회의기록, AI 매뉴얼, 행사기록, 제안함, 채택예정, 매뉴얼, 서류함, 실무자매뉴얼, 학기·캠프, 사용자 관리, 문의및 건의사항, 교육뉴스, GIA시스템, 관리자 대시보드까지 사이드바의 거의 모든 메뉴에 ❓ 버튼을 달았습니다.

## v0.51.1 - 2026-08-04

업무 채팅에 "전체 멘션" 기능을 추가하고, "채팅이 늦게 뜬다"는 신고에 대한 개선을 반영했습니다.

- **@전체 멘션**: 채팅 입력창에서 `@`를 누르면 개인 이름들과 함께 "📢 전체"가 항상 맨 위에 뜹니다.
- **채팅 지연 개선**: 메시지를 보내면 실시간 수신 이벤트를 기다리지 않고 전송 즉시 화면에 바로 붙도록 바꿨습니다(낙관적 업데이트).
- main에 배포한 뒤 채팅이 아예 안 올라온다는 신고가 있었는데, 코드 점검 결과 전송/수신 로직과 실제 DB 컬럼이 정확히 일치해 코드상 원인은 찾지 못했습니다.

## v0.51.0 - 2026-08-04

속도개선 제안서(Top 5) 중 실제로 효과가 있는 항목들을 반영했습니다.

- **학기 정보 중복 조회 제거**: 사이드바+각 페이지가 "현재 진행중인 학기"를 각자 따로 조회하던 것을 요청 단위로 한 번만 조회하도록 캐싱했습니다(`getCurrentTerm`을 `getCurrentAppUser`와 같은 방식으로 `cache()` 처리).
- **AI 단가표 갱신 리마인더 예약**: Sonnet 5 도입특가가 8월 31일 종료되는데, 단가표 (`pricing.ts`)를 갱신하지 않으면 관리자 대시보드의 AI 비용 표시가 조용히 부정확해지므로 8월 29일 자동 리마인더를 걸어뒀습니다.
- **매뉴얼/실무자매뉴얼/학기관리 캐시 완화는 보류**: 원래 제안에는 있었지만, 확인해보니 이 화면들은 로그인 세션(쿠키) 확인이 필요해 Next.js 구조상 `force-dynamic`을 빼도 실제로는 캐시되지 않습니다(쿠키를 읽으면 항상 요청마다 새로 렌더링됨).
- **대형 테이블 조회 컬럼 지정**: 주간 학생 관찰기록 보드, 업무 채팅(메시지/반응/읽음), 학생 정보 조회 화면에서 전체 컬럼(`select *`) 대신 실제 화면에 쓰는 컬럼만 지정하도록 정리해 전송량을 줄였습니다.

## v0.50.0 - 2026-08-04

파일럿 테스트 전 마지막 정비입니다.

**권한 재정비**
- 교사 계정은 이제 정말로 "자기 반/자기 담당과목"만 볼 수 있습니다 - 예전에는 URL을 직접 입력하면 다른 반 학생의 리포트·부모 연락처를 보거나 PDF로 출력하거나 통합검색으로 찾아낼 수 있었는데(`/weekly-report/students`, `/weekly-report/print`, `/weekly-report/students/[id]`, 검색, PDF 발급 라우트), 전부 소유권 검사를 추가해 막았습니다.
- 행정직원/관리자는 그대로 업무·실무자매뉴얼·운영관리·학교관리·주간학생관찰기록을 봅니다.
- 관리자만 보는 새 "관리자" 메뉴를 만들어 관리자 대시보드/학교 현황판/교육뉴스/ GIA시스템을 모았습니다.

**반 담임 이름 배정**

**신규: 교육뉴스 (관리자 전용)**
- AI가 web 검색으로 국제학교/교육정책/교육 트렌드 최신 소식을 찾아 정리하는 다이제스트입니다.

**신규: GIA시스템 (관리자 전용)**
- GIA가 이미 갖춘 운영 시스템(구비서류/내규/계약서/학생관리/교사관리/교직원관리/매뉴얼/ 운영계획안)을 카테고리별로 정리해 보여주고, AI가 다른 국제학교·공립학교 사례를 검색해 GIA에 없는 시스템을 제안합니다.

**가벼운 정리**
- 채팅/업무/구글시트가져오기에서 저장 실패 시 Postgres 원본 에러 문구(영어)가 그대로 보이던 것을 "일시적인 오류입니다.
- 안 쓰는 죽은 코드(`CompletionGauge.tsx`, `/weekly-report/admin/terms` 리다이렉트 페이지)를 정리했습니다.
- `manifest.json`을 추가해 "홈 화면에 추가"가 실제로 앱처럼(standalone) 열리도록 했습니다.

## v0.49.1 - 2026-08-03

실시간 채팅과 업무판을 정말 꼼꼼하게 다시 점검해서, 조용히 메시지/업무 내용이 사라지거나 잘못될 수 있는 지점들을 고쳤습니다.

**채팅**
- 메시지 전송이 실패해도(끊긴 와이파이 등) 알려주지 않고 입력창만 비워져서 메시지가 조용히 사라지는 문제, 그리고 실패 시 전송 버튼이 계속 잠긴 채로 남는 문제를 고쳤습니다 - 이제 실패하면 알림을 띄우고 입력 내용을 복원하며, 버튼도 다시 눌릴 수 있게 됩니다.
- 파일 첨부도 같은 이유로 캡션이 먼저 지워지던 것을 저장 성공 후로 미뤘고, 업로드는 됐는데 메시지 저장이 실패한 경우 남는 "고아 파일"을 자동 정리하도록 했습니다.
- 이모지 반응을 아주 빠르게 두 번 누르면(추가 요청이 아직 끝나기 전에 다시 클릭) 취소가 실제로는 아무 효과 없이 무시되던 버그를 고쳤습니다.
- 재연결(와이파이 순간 끊김 등) 시 최근 100건으로 화면 전체를 덮어써서, 검색으로 점프해 보고 있던 예전 메시지가 화면에서 사라지던 문제를 고쳤습니다 - 이제 기존 화면 내용을 유지한 채 최신 변경분만 얹습니다.
- 다른 사람이 메시지를 보낼 때마다 화면이 맨 아래로 튀어서, 위로 스크롤해 예전 대화를 읽던 중에도 계속 방해받던 문제를 고쳤습니다 - 이제 이미 맨 아래 근처에 있을 때만 자동으로 따라 내려갑니다.
- 채팅 검색이 메시지가 많이 쌓여도 느려지지 않도록 검색용 인덱스(pg_trgm)를 추가했습니다.

**업무판**
- 반복 업무를 업무 상세 팝업에서 완료 처리하면(칸반 드래그가 아니라) 반복이 조용히 끊기던 문제를 고쳤습니다 - 두 곳 모두 같은 로직을 쓰도록 정리했고, 두 사람이 거의 동시에 완료 처리해도 다음 회차가 중복 생성되지 않도록 DB에도 안전장치를 추가했습니다.
- 업무 상태 변경/업무 확인/담당자 태그/댓글 등록/업무 삭제가 실패했을 때 조용히 무시되던 것을 고쳐서, 실패하면 알림을 띄우고 화면을 원래 상태로 되돌리도록 했습니다(끊긴 와이파이 중에도 "저장된 것처럼 보이는데 실제로는 안 된" 상황이 생기지 않도록).
- 업무 상세 팝업에서 완료 처리해도 칸반 정렬 기준(position)이 함께 갱신되도록 맞췄습니다.

## v0.49.0 - 2026-08-03

여러 사람이 동시에 데이터를 입력할 때 생길 수 있는 두 가지 실제 위험을 점검해서 고쳤습니다 (실시간 채팅/칸반/전체 Realtime 채널은 점검 결과 이미 안전하게 정리되어 있었고, 아래 두 곳만 "조용한 데이터 유실" 위험이 있었습니다).

- 업무 "확인"(acknowledged_by)/담당자 태그(assignee_emails): 지금까지는 각자의 화면이 계산한 배열 전체를 그대로 저장해서, 전체공지 업무를 여러 명이 거의 동시에 "확인" 클릭하면 늦게 도착한 저장이 먼저 도착한 사람의 확인 기록을 덮어써 조용히 사라질 수 있었습니다.
- 주간 학생 관찰기록: 같은 학생·과목·주 리포트를 담임/과목교사가 동시에(또는 같은 사람이 두 탭으로) 처음 열어 저장하면 중복 리포트가 생길 수 있었습니다.
- 그 외 실시간 채팅(ChatPanel)/칸반 드래그앤드롭/Realtime 채널 정리(구독 해제) 상태를 전수 점검했습니다 - 13개 채널 모두 정상적으로 정리되고 있어 별도 수정은 필요 없었습니다.

## v0.48.1 - 2026-08-03

관리자 승인 목록, 사용자 관리, 학교 관리 대시보드 등 여러 화면의 계정 표시 로직을 정리했습니다.

## v0.48.0 - 2026-08-03

관리자 대시보드(/dev)에서 AI 기능별로 실제 얼마나 과금되고 있는지 달러로 확인하고, 과금이 부담스러운 기능은 항목별로 켜고 끌 수 있게 만들었습니다.

- "AI 사용량 & 예상 과금" 섹션에 Anthropic 공식 요금표(Sonnet 5 $2/$10, Haiku 4.5 $1/$5 · 백만 토큰당) 기준 예상 과금액을 추가했습니다.
- "AI 기능 on/off (과금 조절)" 섹션을 새로 만들어, 사건기록 정리 AI/회의록 다듬기 AI/매뉴얼 초안작성 AI 등 총 16개 AI 기능을 기록함·매뉴얼·문서·제안함·채택예정·업무 4개 그룹으로 묶어 각각 켜짐/일시정지중 스위치로 조절할 수 있게 했습니다.
- 새 `ai_feature_flags` 테이블을 만들고, 모든 Claude 호출이 거쳐가는 단일 지점인 `callClaudeJson()`(src/lib/ai/claude.ts)에 게이트를 추가했습니다 - 기능이 꺼져 있으면 Anthropic API를 호출하기 전에 바로 막아서 과금이 발생하지 않고, 사용자에게는 "현재 이 AI 기능은 관리자에 의해 일시정지되어 있습니다"라는 안내 메시지가 표시됩니다.
- 사이드바 프로필 블록(로고 아래, 이메일 위) 바로 밑에 빨간 배너를 추가해, 꺼둔 AI 기능이 있으면 모든 로그인 사용자에게 "⏸️ AI 기능 일시정지중"과 함께 어떤 기능인지 실시간으로 보여줍니다.
- 토글/배너는 giamicro.com 전체가 조회(SELECT)할 수 있고, 켜고 끄는 것(수정)은 관리자 계정만 가능하도록 RLS를 설정했습니다.

## v0.47.0 - 2026-08-03

구글시트로 학교정보를 일괄 등록하는 화면(/school/import)을 새로 만들었습니다.

- 구글시트에서 표를 CSV로 다운로드하거나(파일 > 다운로드 > 쉼표로 구분된 값), 셀 범위를 그대로 복사해서 붙여넣으면 자동으로 표를 읽어 미리보기를 보여줍니다.
- 3개 탭으로 나뉘어 있습니다.
  - 🧑‍🏫 교직원 명단: 이메일/이름/소속/직위 - 미리 등록해두면 선생님이 구글 계정으로 처음 로그인할 때 승인 대기 없이 바로 시스템을 쓸 수 있습니다.
  - 🏫 반 구성: 학년/반/담임 이메일/부담임 이메일 - 기존 반이면 담임·부담임을 갱신하고, 없는 조합이면 새 반을 만듭니다.
  - 🎓 학생 명부: 이름/영어이름/학년/반/보호자 연락처 - 학년+반이 일치하면 자동으로 그 반에 연결됩니다.
- 각 행마다 형식 오류(이메일 형식, 필수값 누락 등)를 미리보기에서 빨간색으로 표시해 잘못된 행은 제외하고 정상 행만 가져올 수 있습니다.

## v0.46.0 - 2026-08-03

전체 코드 최적화 점검 결과를 반영했습니다.

- 로고 이미지(사이드바/모바일 헤더/로그인/온보딩/승인대기 화면, 총 5곳)를 일반 `<img>`에서 Next.js `<Image>`로 전환했습니다.
- 주간 학생 관찰기록의 반별 위젯(StudentReportBoard)에서 "학생마다 전체 리포트 목록을 다시 훑어 이번 주 작성 상태를 찾는" 방식이었던 부분을, 리포트가 바뀔 때 한 번만 학생별로 미리 정리해두는 방식으로 바꿨습니다.
- 기존에 이미 적용되어 있던 최적화(미들웨어 승인상태 캐싱, 로그인 유저 정보 요청 단위 캐싱, Supabase 클라이언트 싱글턴, PDF/리치에디터 등 무거운 라이브러리의 서버 전용 격리)는 점검 결과 여전히 잘 동작 중임을 확인했습니다.

### 추가로 제안하는 속도 개선 방법 (코드 외 조치 필요)
- Vercel 프로젝트에 이미 연동된 Speed Insights 대시보드를 주기적으로 확인하면, 실제 사용자 기준으로 어느 화면이 느린지 데이터로 파악할 수 있습니다.
- Supabase에서 자주 필터링되는 컬럼(app_users.email, tasks.department, wr_reports.term_id/ student_id, chat messages.department 등)에 인덱스가 걸려 있는지 한 번 점검해보시는 걸 추천드립니다 - 데이터가 늘어날수록 체감 효과가 커집니다.
- 채팅창(ChatPanel)은 메시지 목록이 많아질수록 입력할 때마다 전체 목록을 다시 그리는 구조라, 다음 최적화 후보로 남겨뒀습니다(구조가 복잡해 이번엔 안전하게 손대지 않았습니다).

## v0.45.2 - 2026-08-03

채팅 입력창 안내문구를 짧게 줄였습니다.

- "메시지 보내기 (Enter 전송·Shift+Enter 줄바꿈, **굵게** *기울임* ~~취소선~~ `코드`, @개인호출, #부서공지)" → "메시지 보내기 (Enter 전송·Shift+Enter 줄바꿈, @개인호출, #부서공지)"로 정리.

## v0.45.1 - 2026-08-03

주간 학생 관찰기록 반별 대시보드(학생 카드) 이름 표시를 개선했습니다.

- 학생 이름 아래 영어이름이 한 줄로 잘리지 않고(예전엔 말줄임표로 잘림) 줄바꿈되며, 글자 크기를 더 줄여서(10px) 카드 안에 자연스럽게 들어가도록 했습니다.
- 담임/과목/관리자 화면에서 공통으로 쓰는 컴포넌트라 반별 작성 현황, 내 담임반, 내 담당과목 화면 전부에 동일하게 적용됩니다.

## v0.45.0 - 2026-08-03

- 사이드바/모바일 메뉴에서 "학교 관리" 카테고리 라벨 자체를 누르면(기존에는 하위 항목 중 첫 번째로 그냥 넘어갔음) 새 `/school` 대시보드가 열립니다.
- 대시보드에서 현재 학기, 개설된 반 수, 과목 수, 교사 수, 교직원 수, 재학생 수를 카드로 한눈에 보여주고, 그 아래에 반별 담임/부담임, 과목별 담당 교사·수강 인원, 교사 리스트, 교직원 리스트, 재학생 요약(24명까지 미리보기 + 더보기 안내)을 함께 보여줍니다.
- 각 카드에는 반 관리/과목반 세팅/사용자 관리/학생 정보 조회 등 해당 관리 화면으로 바로 이동하는 링크를 달아뒀습니다.
- 조회 권한은 기존과 동일하게 행정직원 이상(교사 계정은 접근 불가)입니다.
- (수정) 학기 카드는 연도를 작게 위에, 학기명(예: 여름캠프)은 줄바꿈해서 가운데 크게 보이도록 바꿨습니다.

## v0.44.0 - 2026-08-03

UI/UX 정리 6건을 반영했습니다.

- 업무상황판/주간관찰기록 등 화면 크기를 바꿔도 라벨·뱃지·부서탭 글자가 어중간하게 줄바꿈되며 밀리지 않도록 nowrap/줄임(ellipsis) 처리를 정리했습니다.
- 실시간 로그(🔔) 항목을 관리자이거나 그 로그를 남긴 본인이면 지울 수 있게 됐습니다(업무 상세 패널의 코멘트/로그 항목도 동일).
- 실시간 로그 컴팩트 뷰가 한 줄에 하나씩, 딱 5줄만 보이는 고정 높이 영역으로 바뀌었고, 위로 스크롤하면 그 이전 로그를 추가로 불러옵니다(캐시에 무한정 쌓이지 않도록 상한을 둠).
- 업무탭을 모바일(작은 화면)에서 열면, 마우스 드래그로 나누던 2단 레이아웃 대신 채팅/칸반/내 업무 탭으로 전체 화면을 하나씩 보여줍니다.
- 뱃지 평가 안내와 학생 리스트 표가 좁은 화면에서도 깨지지 않도록 줄바꿈/가로스크롤을 정리했습니다.
- 학생 영어 이름이 목록/카드에서 줄바꿈 없이 한 줄로 깔끔하게 보이도록 정리했습니다.

### 모바일 전용(m.) 서브도메인에 대한 안내
이 앱은 반응형 웹으로 만들어서, m.도메인을 따로 두지 않고 같은 주소(같은 코드)가 PC/모바일
화면 크기에 맞춰 자동으로 레이아웃을 바꿉니다. m.도메인 방식은 옛날 방식(모바일 전용 사이트를
완전히 별도로 만들어 운영)이라 유지보수 부담이 두 배가 되고, 요즘 대부분의 서비스(이 앱 포함)는
반응형 하나로 처리합니다. 이번 업데이트로 업무탭도 반응형으로 전환했고, 필요하면 "홈 화면에
추가"로 앱처럼 아이콘을 만들어 쓰실 수 있습니다(PWA는 이미 적용돼 있습니다).

리포트 작성 화면의 "뱃지 평가 안내" 문구에 영어 병기를 추가했습니다(원어민 교사 대상).

## v0.43.0 - 2026-08-03

지난 기능 제안 중 학부모 열람 관련 항목을 제외한 나머지 6건을 반영했습니다.

- 반복 업무: 업무등록 위젯/상세패널에서 매일·매주(요일)·매월(날짜) 반복을 지정하면, 완료 처리되는 순간 다음 회차가 자동으로 새로 등록됩니다(🔁 뱃지로 표시).
- 업무별 첨부파일: 업무 상세패널에서 파일을 첨부·다운로드·삭제할 수 있습니다.
- 채팅 공지 고정 배너: 이미 있는 메시지 고정(📌) 기능이 이 역할을 하고 있어 별도 개발 없이 그대로 사용하시면 됩니다.
- 학기 종합 PDF: 리포트 프린트 화면에서 학생+학기를 함께 고르면, 그 학기 동안 발행된 모든 리포트를 과목별로 모아 하나의 PDF로 볼 수 있습니다.
- 통합 검색: 사이드바(모바일은 상단)에서 학생·사건·회의·행사·업무·서류함을 한 번에 검색합니다.
- 모바일 칸반 상태변경: 업무카드에 드래그 없이 바로 상태를 바꿀 수 있는 드롭다운을 추가해 터치 환경에서도 편하게 옮길 수 있습니다.

## v0.42.0 - 2026-08-03

업무탭 편의성 + 주간 학생 관찰기록 개선 7건을 반영했습니다.

- **업무탭 레이아웃 크기 기억.** 채팅/상황판/칸반 폭·높이를 마우스로 조절하면 이 기기에 저장되어, 다음에 업무탭에 들어와도 조절한 그대로 유지됩니다.
- **채팅 업무등록 안내를 실시간 로그로 분리.** 채팅으로 업무를 등록하면 "등록됨" 안내와 확인 내역이 더 이상 채팅에 섞이지 않고, 칸반 오른쪽 위 "🔔 실시간 로그"에만 쌓입니다.
- **채팅 서식 툴바 아이콘화.** 굵게/기울임/취소선/코드 버튼을 글자(B/I/S) 대신 선으로 그린 작은 아이콘으로 바꿨습니다.
- **주간 학생 관찰기록 영어 병기.** 원어민 교사도 쓰는 화면이라, 메뉴(내 담임반/내 담당과목/ 반별 작성 현황 등)와 리포트 작성창의 평가 항목·뱃지·버튼에 영어를 함께 표시했습니다.
- **학생 이름 한글/영어 줄바꿈 표시.** 학생 카드·목록·리포트창에서 영어 이름이 등록되어 있으면 한글 이름 아래 줄바꿈으로 함께 보입니다.
- **관리자/행정직원의 리포트 읽기·수정·삭제 권한.** 학생 프로필 화면에서 관리자/행정직원은 이제 리포트를 열람만 하는 게 아니라 직접 수정하거나 삭제할 수 있습니다(삭제 버튼은 관리자/행정직원에게만 보입니다).

## v0.41.0 - 2026-08-03

업무 페이지 사용성 개선 10건을 한 번에 반영했습니다.

- **업무 삭제 권한 제한.** 이제 등록자 본인 또는 관리자만 업무를 삭제할 수 있습니다(상세 패널의 삭제 버튼이 다른 사람에게는 아예 보이지 않습니다).
- **확인 체크 시 취소선 제거.** 담당자가 업무 확인 체크박스를 누르면 제목에 줄이 그이지 않고, 흐리게(반투명)만 표시됩니다.
- **칸반 레이아웃 개편.** 진행대기·진행중·완료 3열이 항상 위에 쾌적하게 보이고, 보류/이슈는 "⏸️ 보류/이슈 (N)" 버튼을 눌러야 펼쳐지는 접이식 섹션으로 아래에 분리됐습니다.
- **업무를 보류로 옮기면 단순 보류인지 이슈인지 물어봅니다.** 이슈를 선택하면 메모를 남길 수 있고, 이 메모는 업무를 공유하는 모두에게 보이며 작성자도 함께 표시됩니다.
- **나/전체/공유 색상 시스템.** 빠른 업무등록 위젯에서 고른 뱃지가 업무에 저장되고, 그 색이 칸반 카드 테두리 색으로 그대로 쓰입니다.
- **업무 마감일 → OS 캘린더 연동.** 상세 패널의 📅 버튼을 누르면 마감 일정을 내 기기의 기본 캘린더 앱(Mac은 Calendar.app, 그 외는 .ics 다운로드)에 바로 추가할 수 있습니다.
- **빠른 업무등록에 날짜/시간 선택 추가.** 오늘·내일·이번주 뱃지를 누르거나 날짜·시간을 직접 입력할 수 있습니다.
- **사용 가이드 팝업 신설.** 업무 페이지 우상단 ❓ 아이콘을 누르면 빠른등록/채팅/칸반/ 업무기록/삭제·캘린더 사용법을 요약한 팝업이 뜹니다.
- **채팅 서식 툴바.** 입력창 위에 굵게(B)/기울임(I)/취소선(S)/코드(&lt;/&gt;) 버튼이 생겨, 마크다운 문법을 직접 타이핑하지 않아도 선택한 글자를 감싸서 서식을 넣을 수 있습니다.
- **반응(이모지) 체크 표시 + 목록 확장.** 반응 고르기 팝업에서 이미 남긴 이모지에 파란 체크 표시가 붙고, 자주 쓰는 이모지가 6개 → 12개로 늘었습니다.

## v0.40.0 - 2026-08-03

업무기록(archive) 기능을 새로 만들었습니다.

- **완료하는 순간 카드가 제목만 남기고 줄어듭니다.** 칸반에서 업무를 완료로 옮기면 그 즉시 설명/담당자/마감 뱃지 등이 사라지고 체크 표시 + 제목만 있는 얇은 줄로 바뀝니다.
- **다음날 자정 직후 자동으로 업무기록으로 이동합니다.** 매일 밤(학기 자동전환 크론 바로 뒤에) 완료 상태인 업무를 업무보드에서 빼서 보관 처리하는 크론을 새로 추가했습니다.
- **업무기록 화면 신설.** 업무 페이지 우측 상단에 "🗂 업무기록" 아이콘을 누르면 이동합니다.

- **참고: 이 기능은 매일 자정 직후(KST) 자동 실행되는 Vercel Cron에 의존합니다.** 기존 학기 자동전환 크론과 같은 환경변수(`CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`)를 그대로 재사용하므로 별도 설정 없이 바로 동작합니다.

## v0.39.2 - 2026-08-03

- **홈 메뉴 재확인.** v0.39.1에서 없앤 게 맞는데, 그 변경은 staging 브랜치에만 있고 아직 main(라이브)에는 병합되지 않아서 실제 화면에서는 계속 보이셨을 거예요.
- **메뉴 순서 변경.** 가장 자주 쓰는 "업무"를 맨 위로 올리고, 그 바로 아래에 "실무자 매뉴얼"을 뒀습니다.
- **"학교관리" → "학교 관리".** "운영 관리"처럼 띄어쓰기를 맞췄습니다.

## v0.39.1 - 2026-08-03

- **중복 "홈" 메뉴 제거.** 사이드바(PC)/상단바(모바일)의 로고를 누르면 이미 홈으로 이동하고 있어서, 메뉴 목록에 따로 있던 "홈" 항목은 없앴습니다.

## v0.39.0 - 2026-08-03

업무등록을 채팅과 분리했습니다.

- **빠른 업무등록 위젯 신설.** [나] 뱃지를 누르면 내 개인 업무로, [전체]를 누르면 부서원 전원에게 배정되는 팀 업무로(모두의 "내 업무목록"에 뜹니다), [공유]를 누르면 태그 목록이 펼쳐져서 원하는 사람만 골라 배정할 수 있습니다.
- **채팅창 높이 축소.** 위젯이 새로 들어간 만큼 채팅창 공간을 줄였습니다.
- **마감초과 시각적 강조 강화.** 마감이 지난 업무는 카드 테두리가 빨간색으로 바뀌고 "🔥 지연" 뱃지가 깜빡이며, 24시간 안에 마감인 업무는 "⏰ 임박" 뱃지가 노란색으로 뜹니다.
- **공유 업무 상태변경 실시간 알림.** 여러 명에게 태그된 업무를 한 명이 진행중/완료 등으로 옮기면, 그 업무의 등록자와 다른 담당자들에게 "OOO님이 '제목' 업무를 '진행중'으로 옮겼어요" 토스트 알림이 화면 우측 하단에 바로 뜹니다(본인이 직접 바꾼 경우는 알림이 뜨지 않습니다).

## v0.38.0 - 2026-08-03

지난 버전(v0.37.0) CHANGELOG에서 "저장소/조회 기능이 더 필요해서 이번 범위에는 넣지 않았다"고 미뤄뒀던 4가지 - 파일/이미지 첨부, 읽음 표시, 링크 미리보기, 메시지 검색·고정 - 를 마저 구현했습니다:

- **파일/이미지 첨부.** 입력창 왼쪽 📎 버튼으로 파일을 고르면(최대 20MB) 바로 업로드되어 메시지로 전송됩니다.
- **읽음 표시.** 내가 보낸 메시지 옆에 "아직 안 읽은 부서원 수"가 카카오톡의 숫자처럼 표시되고, 상대가 채팅방을 열면 실시간으로 줄어듭니다.
- **링크 미리보기.** 메시지에 http(s) 링크가 있으면 구글챗처럼 그 아래에 제목/설명/썸네일이 담긴 작은 카드가 자동으로 붙습니다.
- **메시지 검색.** 채팅창 헤더의 🔍를 누르면 검색창이 뜨고, 입력하는 대로 그 부서 채팅방의 메시지 내용을 검색합니다.
- **메시지 고정.** 메시지에 마우스를 올려 📌를 누르면 고정되어 채팅창 상단에 "고정된 메시지" 바로 모입니다(카카오톡 공지처럼 부서원 누구나 고정/해제 가능).

## v0.37.0 - 2026-08-03

업무 채팅에 구글챗 스타일 기능 대거 추가 + 삭제 UX를 카카오톡 방식으로 변경:

- **메시지 삭제 UX 변경.** 이전엔 이름 옆에 휴지통 아이콘이 있었는데, 카카오톡처럼 내가 보낸 메시지에 마우스를 올렸을 때만 말풍선 오른쪽에 작게 ✕가 나타나도록 바꿨습니다.
- **Enter로 전송, Shift+Enter로 줄바꿈.** 입력창이 한 줄짜리 input에서 여러 줄 입력 가능한 textarea로 바뀌었고, 내용에 맞춰 높이가 자동으로 늘어납니다(최대 5줄 정도, 그 이상은 스크롤).
- **텍스트 서식.** `**굵게**`, `*기울임*`, `~~취소선~~`, `` `코드` `` 문법을 메시지에 그대로 쓰면 서식이 적용되어 보입니다.
- **메시지 수정.** 내가 보낸 메시지에 마우스를 올리면 ✏️ 아이콘이 뜨고, 눌러서 고치고 저장하면 "(수정됨)" 표시와 함께 반영됩니다.
- **답장(인용).** 메시지에 마우스를 올려 ↩️를 누르면 입력창 위에 원본이 미리보기로 뜨고, 그 상태로 보내면 답장한 메시지 위에 원본이 작게 인용되어 표시됩니다.
- **이모지 반응.** 😀 아이콘을 누르면 👍❤️😂😮😢🙏 중 고를 수 있고, 메시지 아래에 반응 뱃지로 모입니다(같은 이모지를 다시 누르면 취소).
- **입력 중 표시.** 다른 사람이 타이핑하고 있으면 입력창 위에 "OOO님이 입력 중..."이 뜹니다 (3초 넘게 조용하면 사라집니다).
- **연속 메시지 그룹핑.** 같은 사람이 5분 안에 연달아 보낸 메시지는 이름/시간을 반복해서 보여주지 않고 말풍선만 이어서 보여줘 채팅창이 덜 복잡해졌습니다.
- **채팅 업무등록 버튼 실패 원인 수정.** 업무 저장이 실패해도 결과를 확인하지 않고 팝업만 닫아버려서 "눌렀는데 반응이 없다"로 보였던 문제를 고쳤습니다.
- **참고로 이번엔 넣지 않은 것들:** 파일/이미지 첨부, 읽음 표시, 링크 미리보기, 메시지 검색·고정은 구글챗에 있지만 저장소/조회 기능이 추가로 필요해서 이번 범위에는 넣지 않았습니다.

## v0.36.0 - 2026-08-03

업무 채팅 "업무등록" 버튼 실패 원인 수정 + 메시지 본인 삭제 기능:

- **채팅 "업무등록" 버튼을 눌러도 등록이 안 되던 문제 수정.** 원인은 등록 로직 자체가 아니라 오류 처리 방식이었습니다 - AI 분석이 실패해도 규칙 기반으로 대체되니 문제가 아니었고, 그 다음 단계인 실제 업무 저장(`tasks` 테이블 insert)이 실패했을 때 결과를 확인하지 않고 그냥 팝업을 닫아버려서, 저장이 안 됐는데도 화면에는 아무 표시가 없어 "눌렀는데 반응이 없다"로 보였던 것입니다.
- **채팅 메시지 본인 삭제 기능 추가.** 메시지에 마우스를 올리면(내가 보낸 메시지에만) 오른쪽에 🗑️ 아이콘이 나타나고, 눌러서 확인하면 삭제됩니다.

## v0.35.0 - 2026-08-02

내 계정 설정(프로필 사진/이름) + 직위(권한) 뱃지 표시 + 관리자의 직위 편집 + 담당자 자동 채움을 이름으로 전환:

- **내 계정 설정 화면 신설(`/account`).** 사이드바 하단 프로필을 누르면 이동하는 새 화면으로, 프로필 사진과 이름을 스스로 바꿀 수 있습니다.
- **사이드바 프로필 블록 개편.** 로고 아래 학기 배지 밑에 있던 한 줄짜리 이메일 표시를, 프로필 사진 + "이름(직위)" + 그 아래 작은 글씨로 로그인 이메일을 보여주는 카드로 바꿨습니다.
- **사건기록 담당자 자동 채움을 이메일 대신 이름으로.** 새 사건을 작성할 때 담당자 칸에 자동으로 채워지던 로그인 이메일을, 내 계정 설정에서 정한 이름(없으면 이메일)으로 바꿨습니다.

## v0.34.0 - 2026-08-02

- **"위클리 리포트" → "주간 학생 관찰기록"으로 개칭.** 사이드바 메뉴명, 페이지 제목, PDF 표지/푸터 등 화면에 보이는 텍스트를 전부 바꿨습니다.
- **"학교관리" 메뉴 신설(통합 관리).** 학생/반/학기/교직원 계정 관리가 운영관리·주간 학생 관찰기록·지원관리 세 군데에 흩어져 있었고, 특히 "학기"는 두 곳에 중복으로 있었습니다.
- **반별 작성 현황 위젯 화면.** "주간 학생 관찰기록" 메뉴에 들어가면 이제 맨 위에 현재 학기가 크게 표시되고, 그 아래 반마다 위젯 카드가 나옵니다.
- **연도-학기-학년-반 통합 검색.** 주간 학생 관찰기록 통계 화면 하단에 검색 패널을 추가해, 연도·학기 / 학년 / 반 조건으로 그동안 쌓인 리포트를 바로 찾아볼 수 있습니다.

## v0.33.1 - 2026-08-02

전체 빌드 점검 + 로고 이미지 최적화:

- **빌드 전수 점검.** 클린 환경에서 `npm install` + `npm run build`를 다시 실행해 타입 에러, 라우트 충돌(경로 중복 등), 머지 마커 잔재 여부를 확인했습니다 - 전부 정상이었고, 빌드는 약 11초, 클라이언트로 나가는 JS는 전체 라우트 합쳐서 1.7MB 수준으로 확인했습니다.
- **사이드바/로그인 화면 로고 이미지 용량 축소 (핵심 개선).** `logo-main.png`가 실제 화면 표시 크기(가로 40px)보다 훨씬 큰 원본(5907×1317px, 224KB)으로 들어가 있어서 대시보드 화면에 들어갈 때마다 불필요하게 큰 파일을 내려받고 있었습니다.
- 그 외 코드 전반(중복 쿼리, 실시간 구독 정리, 브라우저 Supabase 클라이언트 싱글턴 등)은 이전 점검(v0.33.0, 그 이전 성능 점검들)에서 이미 정리되어 있어 추가로 손댈 부분은 없었습니다.

## v0.33.0 - 2026-08-02

탭/메뉴 전환 속도 개선을 위한 코드 통합 및 최적화:

- **로그인 사용자 정보 조회를 요청 단위로 통합.** 지금까지는 사이드바(레이아웃)와 각 페이지가 탭을 옮길 때마다 "로그인 확인 + app_users에서 이름/직위 조회"를 각자 따로 실행해서, 같은 사람 정보를 같은 화면 전환 안에서 두 번 묻는 셈이었습니다.
- **탭 전환 시 즉시 반응하는 로딩 화면 추가.** 지금까지는 페이지를 클릭한 뒤 새 화면의 데이터가 도착하기까지 화면이 하얗게 비어 있었는데, 대시보드 전 영역에 공통 로딩 스켈레톤을 추가해 전환 순간부터 바로 반응하는 것처럼 느껴지도록 했습니다(실제 데이터 조회 시간 자체가 줄어드는 건 아니지만 체감 속도는 확실히 개선됩니다).
- 그 외 미들웨어 인증 캐싱, 라우트별 코드 스플리팅(리치 텍스트 에디터·PDF 생성 등)은 이전 점검에서 이미 최적화되어 있음을 재확인했고, 추가로 손댈 부분은 없었습니다.

## v0.32.1 - 2026-08-02

채팅 "업무등록" 팝업 위치/크기 개선:

- 메시지 아래로 뜨면서 바로 다음 메시지를 가리던 문제를 고쳐, 이제 메시지 오른쪽 옆에 작게 붙습니다(채팅창을 좁게 줄여 오른쪽 공간이 부족할 땐 자동으로 왼쪽에 붙습니다).
- 라벨을 "📋 업무로 등록 (AI 분석)"에서 "📋 업무등록"으로 간단하게 줄였습니다.
- 팝업 크기와 글씨(11px)를 채팅 말풍선(13px)보다 뚜렷하게 작게 만들어서, 실제 채팅 내용과 헷갈리지 않고 조작 버튼이라는 게 한눈에 구별되도록 했습니다.

## v0.32.0 - 2026-08-02

업무 상황판 위젯 축소 + 채팅 메시지 클릭 → AI 분석 업무등록 + 실시간 채팅 점검:

- **업무 상황판 위젯을 텍스트 크기에 맞게 축소.** 숫자 배지 한 줄뿐인데도 공간을 많이 차지하던 것을 더 작게 줄이고, 그만큼 채팅창을 키웠습니다(왼쪽 컬럼 기본 비율: 상황판 14% / 채팅 86%, 드래그로 계속 조절 가능).
- **채팅 자동 업무등록 → 클릭해서 AI가 분석하는 방식으로 전환.** "@담당자를 태그하면 자동으로 업무 등록"은 실시간 채팅이 활발해지면 잡담까지 전부 업무화될 위험이 있어 방식을 바꿨습니다.
  - 새 API: `POST /api/ai/analyze-task`.
- **실시간 채팅 점검.** messages 테이블의 realtime 발행/RLS(도메인 기반 giamicro.com 계정 전체 허용)는 정상이었습니다.

## v0.31.0 - 2026-08-02

전 화면 게시판형 페이지네이션 도입 + 사이드바 메뉴 구분선 + 달력 위젯 축소:

- **목록 화면들을 스크롤 대신 "1 2 3" 페이지 번호로.** 사건기록/회의기록/행사기록/제안함/ 채택예정/매뉴얼/서류함/문의및건의사항/사용자 관리/학생 검색/학기/위클리 리포트 관리(학생 명부·반 배정·과목반) 화면 모두, 목록이 아래로 계속 늘어나며 스크롤되던 것을 화면 높이에 맞춰 고정하고 목록 아래에 게시판형 페이지 번호(‹ 이전 · 1 2 3 … · 다음 ›)를 붙였습니다.
  - 서류함은 "확인 필요" 항목만 예외로 전체 노출(급한 항목은 페이지를 넘기지 않아도 바로 보여야 하므로), 나머지 목록만 페이지네이션됩니다.
  - 공용 `Pagination` 컴포넌트(`src/components/Pagination.tsx`)로 통일해서, 다음에 새 목록 화면을 만들 때도 같은 방식으로 바로 붙일 수 있습니다.
- **사이드바 메뉴에 얇은 구분선 복원.** 플라이아웃으로 평평하게 펼쳐지면서 사라졌던 메뉴 그룹 간 구분이 안 보이던 문제를, 각 메뉴 항목 사이에 얇은 실선을 넣어 다시 구분되게 했습니다.
- **사이드바 달력+시계 위젯을 더 작게, 위젯답게.** 셀 크기와 간격을 더 줄이고 테두리+옅은 배경을 넣어 카드형 위젯처럼 보이게 다듬었습니다.

## v0.30.0 - 2026-08-02

사이드바 달력 상시표시 + 실무자매뉴얼 독립 메뉴화 + 전화 응대용 매뉴얼·학생 통합 조회 화면:

- **달력+시계를 왼쪽 메뉴에 항상 표시.** 홈 화면에만 있던 큰 달력/시계 위젯을 축소판으로 만들어 왼쪽 사이드바(로고 아래, 메뉴 위)에 항상 떠 있게 옮겼습니다.
- **실무자매뉴얼을 독립 메뉴로 분리.** "운영 관리" 하위 플라이아웃에 묻혀 있던 실무자매뉴얼을 꺼내서 홈과 업무 사이에 바로 보이는 최상단 메뉴로 옮겼습니다.
- **실무자매뉴얼 화면을 반으로 나눠 매뉴얼+학생 조회를 동시에.** 왼쪽은 기존 매뉴얼 검색, 오른쪽은 새로 추가된 학생 검색 위젯입니다.

## v0.29.2 - 2026-08-02

업무 탭 레이아웃 2차 개편 - 상황판 초압축(숫자+팝업) + 채팅 3:7 + "내 업무목록" 위젯 + 가로 칸반:

- **상단 헤더 통합.** 부서탭 바 위/아래로 나뉘어 있던 "[부서명] 전용 업무 및 소통 공간" 안내 헤더를 없애고, 맨 위 부서탭 바 하나로 합쳐서 세로 공간을 줄였습니다.
- **업무 상황판을 숫자 배지 한 줄로 초압축.** 카드를 늘어놓던 기존 방식 대신, "전체/진행&대기/ 보류&이슈/완료"를 숫자 배지 한 줄로만 보여줍니다.
- **왼쪽 컬럼 세로 비율 3:7 고정(상황판:채팅).** 상황판은 작게, 채팅은 크게 - 경계선을 드래그 하면 비율을 자유롭게 조절할 수 있습니다.
- **오른쪽에 "내 업무목록" 위젯 신설.** 나에게 배정된, 완료되지 않은 업무만 마감 임박순으로 모아 보여줍니다.
- **그 아래 칸반보드(진행대기/진행중/보류·이슈/완료)를 가로로 나란히 배치.** 화면이 넓을 땐 4개 상태 컬럼이 옆으로 나란히 놓여 진짜 칸반보드처럼 카드를 끌어다 놓을 수 있고, 좁아지면 자동으로 1~2열로 줄어듭니다(드래그앤드롭 기능 자체는 기존과 동일).

## v0.29.1 - 2026-08-02

업무 탭 UX 개선 - 부메뉴 팝업화 + 채팅 중심 레이아웃 + 채팅만으로 업무등록(마감기한 자동 인식):

- **사이드바 부메뉴가 진짜 팝업으로 뜨도록 수정.** 부메뉴가 사이드바 내부에 그대로 펼쳐지면서 스크롤이 생기던 문제를 고쳤습니다.
- **업무 탭 레이아웃을 채팅 중심으로 재조정.** 업무 상황판과 칸반보드(업무목록)는 참고용이라 작게, 실제로 업무를 처리하고 소통하는 채팅창은 크게 배치했습니다(왼쪽 상황판+칸반 : 오른쪽 채팅 = 대략 3:7).
- **"+ 새 업무" 버튼/입력폼을 없앴습니다.** 업무 등록은 이제 채팅창에 메시지를 치는 것만으로 이루어집니다(기존에도 있던 기능이지만, 버튼식 입력창이 따로 있어 헷갈렸던 부분을 정리해 채팅 한 곳으로 통일했습니다).

## v0.29.0 - 2026-08-01

통합 인물관리 시스템 - 학생 영구 고유번호 도입 + 연도/학기 통합 + 사건기록-학생 구조적 연결 + 학생 정보 조회 화면 신규:

**동명이인 구분 방법**: 이름만으로는 같은 이름의 학생(예: 김재이가 1A/1C/2J 세 반에 있음)을
구분할 수 없어서, 실제 학교 현장에서 쓰는 "학번" 방식을 그대로 들여왔습니다. 학생이 처음
등록되는 순간 `GIA-2026-0001` 형식의 영구 고유번호(student_no)가 자동으로 한 번 부여되고,
이후 이름이 바뀌든 학년·반이 바뀌든 절대 바뀌지 않습니다. 업무·사건기록·위클리 리포트 세
영역 모두 이제 이 고유번호(정확히는 그것과 묶인 학생 레코드의 id)를 기준으로 같은 학생을
가리키게 됩니다 - "강여명 어머니께서"라는 문구가 나와도, 그 강여명이 어느 학생인지는 화면에서
직접 골라서 정확히 연결해야 합니다(이름만으로는 시스템이 자동으로 확신할 수 없기 때문입니다).

- **학생 영구 고유번호(student_no).** 기존 114명 전원에게 일괄로 GIA-2026-0001 ~ GIA-2026-0114 번호를 부여했습니다.
- **학생 인적/학적사항 확장.** 생년월일·학생 연락처·주소 컬럼을 추가했고, 반(wr_classes)과도 FK로 연결해 담임선생님을 안정적으로 조회할 수 있게 했습니다.
- **연도>학기(정규학기+캠프)>학생/교직원 통합 분류체계.** 위클리 리포트가 따로 쓰던 학기 테이블(wr_terms)을 없애고, 운영(gia-ops)이 쓰던 학기 테이블(terms - 연도+학기유형: 1학기/ 2학기/3학기/여름캠프1/여름캠프2/겨울캠프1/겨울캠프2)로 완전히 통합했습니다.
  - **재학 이력(wr_enrollments) 신설.** "몇년도 어느 학기에 이 학생이 몇학년 몇반, 담임은 누구였는지"를 스냅샷으로 남기는 이력 테이블입니다.
- **사건기록 ↔ 학생 구조적 연결.** 사건 입력/수정 화면에 "관련 학생(정확히 연결)" 검색창을 추가했습니다.
- **직위체계 정리.** '교직원'이라는 모호한 표현을 '행정직원'으로 명확히 했습니다(교사/행정직원/ 관리자 3단계로 표현을 정리했습니다(실제 권한 로직은 바뀌지 않았습니다).
- **학생 정보 조회(통합 프로필) 화면 신규 - `/students`.** 행정직원/관리자만 접근 가능합니다.

## v0.28.1 - 2026-08-01

위클리 리포트에 실제 학생 명단(114명, 10개 반) 이관 - 코드 변경 없이 DB 데이터만 추가:

- 업로드해주신 실제 GIA 명단(구 프로토타입 DB 덤프)을 현재 `wr_classes`/`wr_students` 스키마에 맞춰 옮겼습니다.
- 32번에서 넣어뒀던 테스트용 더미 데이터(이해린/김민지/팜하니/강해린/다니엘, 1a/2a 반)는 정확히 그 5명/2개 반만 지정해서 함께 정리했습니다 - 실제 명단과 섞이지 않습니다.
- ⚠️ **담임 선생님 이메일은 비워뒀습니다.** 원본 자료에 실제 `@giamicro.com` 이메일이 없고 구 시스템의 아이디/비밀번호만 있었습니다.
  - 1A=Aimie, 1C=Carina, 1J=Jamie, 2Y=Yunsang, 2J=Jandy, 2K=Katherine, 3J=Janelle, 3A=Anna, 4A=Sarah, 5E=Eamonn.
  - Crystal/Michelle/Celine 선생님은 원본 자료상 담임 배정이 없어(과목 전담으로 추정) [과목반 세팅] 화면에서 별도로 배정해주세요.

## v0.28.0 - 2026-08-01

사이드바 화이트 복귀 + 업무 탭 WorkFlatform 원본 재구현 + 플라이아웃 메뉴 + 통합 관리자 대시보드 + 3개 앱 테마 명확화:

- **사이드바를 다시 화이트로 되돌리고, 페이지 배경만 톤 조정했습니다.** 로고가 있는 사이드바가 계속 색이 바뀌다 보니 로고와 어울리지 않는다는 지적을 반영해, 사이드바/헤더는 다시 흰색으로 고정하고 각 화면의 콘텐츠 영역 배경만 앱별로 다르게 유지했습니다(가시성 저하 없이 톤만 구분).
- **업무 탭을 사용자가 지정한 "워크플랫폼" 참조 소스코드의 UI/UX 그대로 재구현했습니다.** 이전에 자체적으로 만들었던 남색 톤 칸반 대신, 참조 프로그램과 동일하게 밝은 블루 + 글래스모피즘 (반투명 블러) 스타일로 전면 교체했습니다.
  - 드래그앤드롭을 `@dnd-kit`(core/sortable/utilities) 라이브러리로 새로 도입해 카드를 마우스로 끌어다 상태(진행 대기/진행 중/보류·이슈/완료) 사이로 옮기면 실제로 붕 뜨는 미리보기가 따라오는, 참조와 동일한 드래그 경험을 구현했습니다.
  - 부서별 업무 상황판(완료율 도넛 게이지 + 전체/완료/진행&대기/보류&이슈 배지 목록), 부서 그룹채팅(@담당자 태그 시 자동 업무 등록, #부서명 태그 시 해당 부서 채팅방에 공유, 실시간 자동완성 메뉴), 실시간 로그(상태변경·업무확인 이벤트를 자동 기록)까지 참조의 구성 요소를 빠짐없이 옮겼습니다.
  - 참조 소스코드에는 있었지만 저희 앱에는 이미 메인 사이드바가 있어 중복되는 좌측 세로 부서 목록만, 상단 가로 탭(부서 색상 점 클릭 시 관리자 색상 변경) 형태로 의도적으로 바꿨습니다.
- **사이드바 메뉴를 플라이아웃(호버 시 오른쪽에 펼쳐지는) 구조로 재편했습니다.** 메뉴가 너무 길어 계속 스크롤해야 했던 문제를 해결하기 위해, 주메뉴는 홈/업무/운영 관리/위클리 리포트/지원·관리만 세로로 깔끔하게 두고, 세부 항목(사건기록·회의기록·매뉴얼 등)은 주메뉴에 마우스를 올렸을 때만 오른쪽에 펼쳐지는 부메뉴로 이동했습니다.
- **문의 및 사용자관리를 "지원 · 관리" 카테고리로 묶어 메뉴 맨 뒤로 옮겼습니다.** 사용자관리와 새로 만든 관리자 대시보드는 관리자만, 문의및건의사항은 교직원 이상 전원이 볼 수 있도록 카테고리 안에서 권한별로 항목을 다르게 구성했습니다.
- **통합 관리자 대시보드(`/admin/dashboard`)를 신규로 만들었습니다.** 교장/이사장/부이사장/부서장 등 "관리자" 직위를 가진 분들이 업무·운영·위클리 리포트 세 영역을 한 화면에서 확인할 수 있습니다.
  - 최근 30일 사건/행사/회의 건수, 전체 업무 완료율, 재적 학생 수, 이번주 평가 발행률 등 핵심 지표를 상단에 숫자로 요약했습니다.
  - **반복되는 사건 유형을 자동으로 짚어드립니다** - 최근 6개월간 같은 분류(manual_cat)로 3건 이상 반복된 사건 유형을 발생 빈도 순으로 보여줍니다.
  - **학생 평가(위클리 리포트) 현황도 함께 봅니다** - 이번 학기 동안 경고·미흡 배지가 3회 이상 반복된 "반복 지도 필요" 학생과, 우수 배지가 3회 이상 반복된 "반복 우수" 학생을 자동으로 뽑아 보여줘서, 반복되는 부진에는 추가 조치를, 계속 잘하는 학생에게는 보상을 검토할 수 있도록 했습니다.
  - **월별 추이를 그래프로 보여줍니다** - 사건·행사·회의 발생 건수 추이, 학생평가 경고/우수 배지 추이를 최근 6개월 막대그래프로, 부서별 업무 완료율을 비교 막대그래프로 표시합니다.
  - ⚠️ 앱의 직위 체계에는 "부장" 직급이 따로 없어(교사/교직원/관리자 3단계), 현재는 "관리자" 직위를 가진 모든 계정이 이 대시보드에 접근합니다.
- **업무/운영/위클리 리포트 세 화면의 테마를 명확히 구분했습니다.** 사이드바는 공통으로 흰색을 유지하되, 콘텐츠 영역 배경과 강조색을 화면마다 다르게 해서 지금 어느 앱에 들어와 있는지 한눈에 알 수 있게 했습니다.
  - 업무: 밝은 블루 + 글래스모피즘(반투명 블러) - WorkFlatform 원본 룩 그대로.
  - 운영(gia-ops): 남색 + 골드 - 기존 브랜드 톤 유지.
  - 위클리 리포트: 틸(청록) 계열 배경(`--color-wr-bg`) + 전용 강조색(`--color-wr-primary`, 사이드바 메뉴 강조색과 겹치지 않는 살짝 더 짙은 틸)으로 버튼·강조 요소를 새로 통일했습니다.

## v0.27.0 - 2026-08-01

- **디자인 시스템 도입(GIA 남색+골드).** 로고의 짙은 남색을 기준으로 남색/골드 팔레트를 `globals.css`에 정의하고, 페이지 배경을 흰색에서 살짝 톤 다운된 회색빛(`#eef1f6`)으로 바꿔서 흰색 카드가 배경과 구분되도록 했습니다 - "다 하얘서 가시성이 떨어진다"는 문제의 원인이 흰 카드가 흰 배경 위에 있었기 때문이라, 배경만 바꿔도 앱 전체(GIA ops·업무·위클리 리포트 공통 레이아웃)에 카드 구분이 생깁니다.
- **공용 셸(사이드바/헤더)을 남색 테마로 전환.** 대시보드 좌측 사이드바와 모바일 헤더가 남색 그라데이션이 되고, 로고는 남색 배경 위에서도 잘 보이도록 흰 칩(chip) 위에 올렸습니다.
- **업무/사건/행사/제안함/문의 등 앱 전반의 강조 버튼·선택 탭 색상을 슬레이트 계열에서 GIA 남색으로 통일**해서 세 앱이 같은 브랜드 색을 쓰도록 정리했습니다.
- **위클리 리포트 실데이터 시드.** 전달해주신 학생/반/과목/학기 더미 데이터를 실제 `wr_terms`/`wr_classes`/`wr_students`/`wr_subjects`/`wr_reports` 테이블 구조(uuid 기본키)에 맞춰 옮겼습니다: 2026년 가을학기, 1학년A·2학년A 반, 학생 5명(이해린/김민지/팜하니/강해린/ 다니엘, 학부모 연락처 포함), 수학(1학년)·영어(2학년) 과목, 이해린 담임 리포트 샘플 1건.
  - ⚠️ **원본 데이터의 teacher1/teacher2 계정은 실제 @giamicro.com 이메일이 아니라서 반/과목의 담당교사는 비워뒀습니다.** 위클리 리포트 관리 > 반/담임 배정, 과목반 세팅 화면에서 실제 선생님 이메일을 배정해주세요.
- **레거시 정리.** 홈 화면의 개인용 "할 일" 위젯이 팀 공유 업무 보드로 완전히 대체된 뒤 화면 어디에서도 쓰이지 않던 `todos` 테이블을 정리했습니다.

## v0.26.0 - 2026-08-01

GIA 통합 플랫폼 3번째 통합:

- 별도로 개발 중이던 학생 주간 리포트 앱(교사가 학업/보완점/참여도/태도/교우관계 5개 항목을 뱃지(🌟탁월/🟢양호/⚠️지도요망/🚨집중지도)와 서술형으로 매주 평가하는 앱)을 gia-ops-web 안에 새 메뉴 "위클리 리포트"로 통합했습니다.
- **교사(직위=교사)는 로그인하면 위클리 리포트 화면만 보이고, 사이드바에도 "내 담임반"· "내 담당과목" 두 메뉴만 나타납니다.** GIA ops(사건/회의/매뉴얼 등)와 업무 보드는 아예 접근 자체가 막힙니다(주소를 직접 입력해도 위클리 리포트로 돌아옵니다) - 계약직으로 짧게 근무할 수도 있는 교사에게 내부 문서 성격의 다른 메뉴를 보여주지 않기 위함입니다.
- **관리자/교직원은 기존 메뉴 전체 + 위클리 리포트를 함께 볼 수 있습니다.** 교직원은 "학생 현황"·"리포트 프린트"를, 관리자는 여기에 더해 "반/담임 배정"·"과목반 세팅"· "학생 명부"·"학기 관리"·"통계 대시보드"까지 볼 수 있습니다.
- 리포트 작성 화면은 원본 앱의 기능을 그대로 옮겼습니다: 뱃지 복수 선택, 3초 자동 임시저장, 종합 의견 상용구 저장(개인 브라우저에 저장), 지난주 기록 보기, 임시저장/발행 구분, 다른 과목 탭은 읽기 전용으로 열람.
- 학부모 배포용 PDF(발행된 리포트만 모아서 과목별로 정리)를 리포트 프린트 메뉴에서 바로 열람/다운로드할 수 있습니다.
- 참고: 원본 문서의 "AI 뱃지"는 실제로는 AI가 아니라 선생님이 직접 클릭해서 고르는 수동 평가였습니다(이름만 ai_tags였을 뿐 생성 로직은 없었음) - 그대로 수동 선택 방식으로 옮겼습니다.
- 원본 앱의 회원가입/비밀번호/사용자 승인/오류 로그/관리자 대시보드 화면은 옮기지 않았습니다 - 이미 gia-ops-web에 동일한 기능(구글 로그인 승인, 관리자 대시보드 등)이 있어서 그걸 그대로 씁니다.

## v0.25.0 - 2026-08-01

로그인 승인 방식 전면 개편:

- 처음 로그인하는 사람은 이제 이름, 소속(유치부/초등부/중고등부), 직위(교사/교직원/관리자)를 입력하는 온보딩 화면(`/onboarding`)을 먼저 거친 뒤에 승인 대기 화면으로 넘어감.
- 승인 권한이 "승인된 사람이면 누구나"에서 "직위가 관리자인 사람만"으로 좁혀짐.
- 이 기능이 추가되기 전 이미 승인됐던 기존 계정도, 아직 이름을 입력한 적이 없다면 다음 로그인 때 온보딩 화면을 한 번 거치게 됨(그 뒤로는 다시 뜨지 않음).
- 온보딩 중 자기 상태(status)를 스스로 승인으로 바꾸거나, 승인 기록(담당자/일시)을 조작하는 것은 DB 트리거로 원천 차단됨 - 오직 관리자만 승인/거절 처리 가능.
- 사용자 관리 화면에 이제 이메일뿐 아니라 이름·소속·직위가 함께 표시됨.
- 업무 보드(칸반 카드, 상세 패널, 담당자 태그, 접속자 목록)와 부서 채팅에서 이제 이메일 대신 이름이 표시되고, 채팅 멘션도 "@이메일앞부분" 대신 "@이름"으로 사람을 태그함.
- (참고) 사건/회의/행사의 담당자·작성자 표시, 문의사항 작성자 표시, 관리자 대시보드 로그는 이번 작업 범위에 포함되지 않아 여전히 이메일로 표시됩니다 - 필요하시면 다음 작업으로 이어서 처리해드릴게요.

## v0.24.0 - 2026-07-31

GIA WorkFlatform 통합 2단계:

- 업무 페이지에서 부서 탭을 선택하면(기본값 초등부) 오른쪽에 그 부서의 실시간 채팅창이 뜸.
- 채팅에서 "@사람이름"을 태그하면 그 메시지가 즉시 업무 카드로 등록되고(상태: 예정, 담당자: 태그된 사람), "✅ 업무로 등록됨" 안내 메시지가 채팅에 남아서 누가 봐도 바로 확인 가능.
- 채팅에서 "#부서명"을 태그하면 같은 메시지가 그 부서 채팅방에도 그대로 복사되어 들어감 (원본 부서 이름이 "~에서 공유됨"으로 표시됨) - 부서 간 교차 공유.
- 빠른 입력을 위해 채팅 입력창 위에 팀원 이름(@)과 다른 부서(#) 버튼을 눌러서 바로 태그를 삽입할 수 있게 함.
- 부서를 선택하면 그 부서 업무의 완료율을 보여주는 원형 게이지 위젯 추가.
- 부서 목록을 코드에 하드코딩하지 않고 departments 테이블로 관리하도록 변경.

이번 단계에서 기획서 대비 단순화한 부분: 부서 하나당 채널을 여러 개(공지/자유 등) 두는 기능은
아직 없고 부서=채널 1:1로 구현했습니다. 또 지금은 모든 승인된 팀원이 모든 부서 채팅을 볼 수
있어요(부서 소속에 따른 접근 제한은 아직 없음, 학교 규모상 전체 공유가 더 유용할 것으로 판단).
필요하시면 다음 단계로 추가하겠습니다.

## v0.23.0 - 2026-07-31

GIA WorkFlatform 통합 1단계:

- 사장님이 주신 "GIA WorkFlatform" 기획서(부서별 채팅+업무 자동할당+글래스모피즘 UI 협업툴)를 검토한 결과, 단계적으로 통합하기로 했고 1단계로 업무보드에 부서(department) 개념을 추가함.
- tasks에 부서 필드 추가(기본값 유치부/초등부/행정실 + 실제 쓰인 부서명이 자동으로 선택지에 추가됨 - 학기/행사명과 같은 방식).
- 디자인: 기획서의 vanilla CSS 대신, 기존 Tailwind 체계를 유지하면서 Tailwind 유틸리티만으로 글래스모피즘(반투명 + 블러 + 은은한 그라데이션)을 구현함.
- 다음 단계(부서별 실시간 채팅 + `@`/`#` 태그로 업무 자동 전환)는 별도로 진행 예정 - 실제 부서 전체 목록과 Supabase 요금제(Realtime 동시 연결 수) 확인이 먼저 필요함.

## v0.22.0 - 2026-07-31

- 이전에 만든 개인용 할 일(홈 위젯)/업무히스토리(달력)를 팀이 같이 보는 업무판인 새 "업무" 메뉴로 완전히 대체했습니다.
- 사이드바에 "업무" 메뉴 신설(홈 바로 아래).
- 지금 이 페이지에 접속해 있는 팀원을 실시간으로 보여줌(Supabase Presence).
- 업무를 등록하면 카드 형태로 "예정" 칸에 들어가고, 마우스로 카드를 끌어서 진행중/완료/보류 칸으로 옮길 수 있음(모바일 등 드래그가 불편한 환경을 위해 카드마다 상태를 바로 바꾸는 드롭다운도 함께 제공).
- 업무를 등록하거나 카드를 열었을 때, 지금 접속 중인 팀원을 포함한 승인된 팀원 전체를 담당자로 태그할 수 있음(여러 명 가능).
- 카드를 클릭하면 상세 패널이 열리고, 상태/우선순위/마감일/담당자를 바로 수정할 수 있음.
- 구글 시트 메모처럼, 각 업무 카드에 실시간 코멘트를 남길 수 있음 - 팀원이 그 업무를 보고 있으면 코멘트가 실시간으로 함께 뜸.
- 완료된 업무는 기본적으로 최근 14일치만 보여서 보드가 오래된 카드로 지저분해지지 않게 하고, 체크박스로 전체 완료 이력도 펼쳐볼 수 있음.
- 우선순위(보통/긴급) 태그, 마감일 지난 카드 빨간 테두리 강조 등 부가 기능도 함께 추가.
- 홈 화면은 "업무 현황"(예정/진행중/완료/보류 건수 + 나에게 태그된 업무 건수)만 요약으로 보여주고, 자세히 보려면 업무 메뉴로 이동하도록 정리.

## v0.21.0 - 2026-07-31

할 일 입력 확대 + 날짜별 기록 저장 + 업무 히스토리(달력) 메뉴 신설:

- 홈 화면 할 일 입력창을 더 크게(글자 크기/여백 확대).
- 할 일마다 "날짜(for_date)"가 함께 저장됨.
- 사이드바 "실무자매뉴얼" 아래에 "업무히스토리" 메뉴 신설.
- 알림 시간 설정 UI도 날짜+시간을 분리해서 더 명확하게 정리(선택한 날짜의 그 시간에 알림).

## v0.20.1 - 2026-07-31

- "홈" 제목 글씨를 없앰(학기 표시와 함께 있을 때 어울리지 않는다는 피드백 반영).
- 학기 표시를 파란 버튼(알약 모양) 스타일에서, 페이지 제목처럼 보이는 큰 글씨 + 아래 짧은 포인트 라인으로 변경 - 클릭 가능한 버튼처럼 보이지 않도록 정리.

## v0.20.0 - 2026-07-31

홈 화면 왼쪽에 개인 할 일 목록 + 시간 알림 기능 신설:

- 홈 화면 맨 왼쪽에 "✅ 할 일" 위젯 추가.
- 설정한 시간이 되면 팝업으로 알려줌: (1) 브라우저 알림 허용 시 OS 알림 센터에도 뜨는 네이티브 알림, (2) 앱을 켜둔 화면 오른쪽 아래에 뜨는 인앱 팝업(완료 처리/닫기 버튼 포함) - 홈 화면이 아니어도 앱의 다른 어떤 화면에 있든 뜸.
  - 주의: 이 알림은 브라우저 탭(앱)이 열려 있는 동안에만 동작함.

## v0.19.0 - 2026-07-31

- 홈 화면 학기 표시를 제목 옆 작은 글씨에서, 제목 아래 중앙의 눈에 띄는 파란 배지("📅 2026 여름캠프2")로 이동해 가장 눈에 띄게 강조했습니다.
- 달력 위젯에 대한민국 공휴일을 표시함.
- 달력의 날짜를 클릭하면 그 날짜로 OS 기본 캘린더 앱이 열리도록 연동:.
  - Mac/iOS: Calendar.app을 `calshow:` 링크로 즉시 열어서 해당 날짜로 이동.
  - Windows/Android 등: 해당 날짜의 .ics 파일을 다운로드함 - 열어보면(더블클릭) 기본 캘린더 앱(Outlook/캘린더/구글 캘린더 등)에 바로 추가할 수 있음(웹 표준 "캘린더에 추가" 방식).
- 행사기록 > 정규행사 탭의 "📊 연도별 비교 리포트" 버튼을 "🔍 행사분석 (AI)"로 이름 정비.

## v0.18.0 - 2026-07-31

홈 화면 학기 표시 단순화 + 달력/시계 위젯 + 학기 자동 전환(날짜 기반):

- 홈 화면 상단의 학기 안내 박스(클릭 가능한 카드, "현재 학기:" 라벨, 안내 문구)를 없애고, 제목 "홈" 옆에 "📅 2026 여름캠프2"처럼 값만 표시.
- 홈 화면 오른쪽에 실시간 시계 + 이번 달 달력(오늘 날짜 강조, 좌우 화살표로 다른 달도 확인 가능) 위젯 추가 - 오늘이 며칠인지 한눈에 확인 가능.
- 사이드바(로고-이메일 사이)/모바일 상단의 학기 배지 로직 자체는 정상이었음.
- **학기 자동 전환(신규)**: 학기 등록 폼의 시작일/종료일은 이미 달력(날짜 선택) 입력이었음.
  - 배포 후 한 번만 설정합니다: (1) 터미널에서 `openssl rand -hex 32`로 무작위 값 생성 → Vercel 프로젝트 Settings → Environment Variables에 `CRON_SECRET`으로 등록, (2) 같은 값으로 로컬 `.env.local`에도 `CRON_SECRET=...` 추가(선택, 로컬 테스트용), (3) `vercel.json`에 크론 설정을 이미 포함해서 커밋했으므로 GitHub Desktop으로 푸시하면 Vercel이 자동으로 매일 인식해서 실행함 (Vercel 프로젝트 Cron Jobs 탭에서 실행 이력 확인 가능).

## v0.17.3 - 2026-07-31

- 홈 화면, 사이드바(로고-이메일 사이) 배지, 학기 메뉴 상단 "현재 학기" 표시를 모두 `학기 (연도)` → `연도 학기` 형식으로 통일.
- 학기 메뉴의 "현재 학기" 변경 드롭다운에 연도 선택 셀렉트를 추가함.

## v0.17.2 - 2026-07-31

- "현재 학기" 변경 드롭다운이 이미 저장된 회차 기록에서만 목록을 만들다 보니, 아직 아무 학기도 등록 안 한 상태에서는 빈 목록으로 보였음.

## v0.17.1 - 2026-07-31

- 학기·캠프 화면 맨 위에 "현재 학기" 영역 추가: 지금 진행중으로 설정된 학기가 바로 보이고, "변경" 버튼으로 드롭다운에서 아무 회차나 골라 즉시 전환 가능.
- 코드 정리: 아무 데서도 안 쓰이던 중복 페이지(`/incidents`, `/records`와 완전히 같은 내용)를 제거.
- 성능: 사건기록/회의기록/AI매뉴얼 화면 오른쪽의 AI 제안 패널이 실시간 갱신을 구독할 때, 이제 Supabase 쪽에서 해당 화면과 관련 없는 변경 이벤트는 아예 걸러서 보내도록 필터를 걸어 불필요한 네트워크 트래픽을 줄이고, 처음 불러올 때도 화면에 실제로 쓰이는 컬럼만 가져오도록 줄여 응답 속도를 개선.

## v0.17.0 - 2026-07-31

- **문의및건의사항** 메뉴 신설(전 직원): 오류 신고/기능제안/기타를 자유롭게 남기고, 본인이 남긴 문의와 답변을 확인할 수 있음.
- **관리자 대시보드**(`/dev`): 전체 데이터 현황(테이블별 건수), 14일 넘게 방치된 제안/채택예정과 3일 넘게 미처리된 오류 문의 알림, 최근 30일 AI 사용량(라우트별 호출수·실패수·토큰수), 최근 오류 로그 20건을 한 화면에서 확인.
  - 관리자 메뉴에서는 문의사항도 전체 조회 + 상태변경(접수/처리중/완료) + 답변 작성 가능.
- **오류 로깅**: 지금까지 API 오류가 기록되지 않던 것을 모든 AI 관련 API 라우트(16개)에서 자동으로 `error_logs`에 남기도록 구현.
- **AI 사용량 로깅**: `callClaudeJson` 호출마다 라우트명·모델·입력/출력 토큰수·성공여부를 `ai_usage_logs`에 자동 기록(비용/사용 패턴 모니터링용).
- **Vercel Analytics + Speed Insights** 연동: 배포하면 별도 설정 없이 실제 사용자 페이지 로딩 속도가 Vercel 대시보드에 자동으로 수집됨(무료 도구).

## v0.16.0 - 2026-07-31

- **사건기록 AI 자동채우기**: 상세 내용(경위)란에 두서없이 적고 "🧹 AI로 채우기"를 누르면 날짜· 제목·잘된 점·부족했던 점·보완점을 AI가 원문에서 찾아 자동으로 채워줌(원문에 없는 내용은 억지로 지어내지 않고 비워둠 - 없어졌던 예전 기능을 새 화면 구조에 맞게 복원).
- **담당자 자동 입력**: 새 사건을 작성할 때 담당자(작성자) 칸에 로그인한 계정 이메일이 자동으로 채워짐(직접 수정 가능).
- **학기 자동 연결**: 학기 메뉴에서 어떤 학기/캠프를 "진행중"으로 등록해두면, 그 기간에 새로 작성하는 사건·회의 기록이 자동으로 그 학기에 연결됨.
- 홈 화면 상단에 "현재 학기" 카드 추가(진행중인 학기가 없으면 등록 안내 표시).
- 사이드바(PC)와 상단 헤더(모바일) 로고와 로그인 계정 사이에 현재 학기 배지 표시, 누르면 학기 메뉴로 이동.

## v0.15.0 - 2026-07-31

- 모니터가 넓을 때 본문이 가운데에 좁게 몰려 여백이 과하게 남던 문제 수정.
- **사건기록**: 기존에는 "+새로 입력"을 눌러야 입력칸이 나타났는데, 이제 왼쪽에 지금까지 기록한 사건 목록이 항상 보이고 가운데에 입력폼이 항상 떠 있어 바로 적고 저장할 수 있음.
- **회의기록**: 왼쪽에 날짜별 회의 목록, 가운데에 지금까지 쓰던 채팅 작성창(텍스트/음성파일 업로드/라이브 녹음 모두 그대로)이 항상 떠 있음.
- **AI 매뉴얼**: 왼쪽에 지금까지 작성한 이력이 쌓여서 클릭하면 다시 불러올 수 있고, 가운데 입력폼과 오른쪽 AI 제안 패널(검토대기/채택예정)이 함께 있음.
- 사건/회의를 저장하는 즉시 그 건만 AI가 바로 분석하도록 스캔 API를 확장(기존에는 5건 단위 일괄 분석만 있어서, 방금 쓴 글이 오른쪽에 뜨기까지 다른 미분석 기록이 쌓이길 기다려야 했음).
- 클릭 반응 속도: 지난 버전(미들웨어 캐싱, 브라우저 클라이언트 재사용)에 이어, 이번 3단 작업화면으로 사건/회의/AI매뉴얼-제안함-채택예정 사이 페이지 이동 자체가 크게 줄어 체감 반응속도가 개선됨.

## v0.14.2 - 2026-07-31

- 모델을 whisper-large-v3-turbo -> whisper-large-v3(전체 모델)로 변경 - 무료 한도는 동일한데 인식 정확도가 더 좋음.
- 언어를 자동감지 대신 한국어(ko)로 명시 - 특히 짧은 구간에서 자동감지가 흔들리는 문제 개선.
- GIA 관련 용어(부이사장, 실장, 플리마켓, 구글폼 등)를 미리 알려주는 프롬프트를 추가해 고유 용어 인식률 개선.
- 라이브 녹음의 마이크 설정에 에코 제거/노이즈 억제/자동 게인을 명시적으로 켜고, 녹음 비트레이트를 높여 음질 개선.
- 라이브 녹음 구간 길이를 45초 -> 60초로 늘려 문장이 구간 경계에서 잘리는 빈도를 줄임(대신 화면 갱신 주기는 약간 느려짐).

## v0.14.1 - 2026-07-31

- 음성 인식(STT) 서비스를 OpenAI(유료, 카드 등록 필요)에서 Groq 무료 티어로 변경 - 신용카드 등록 없이 가입만으로 하루 2,000건/시간당 오디오 7,200초까지 무료 사용 가능(학교 회의록 용도로는 충분한 한도).
- **필요 환경변수 변경**: `OPENAI_API_KEY` 대신 `GROQ_API_KEY`를 Vercel에 추가해야 함 (아래 가입 가이드 참고).

## v0.14.0 - 2026-07-31

- 회의를 "자료 기록" 그룹 안에서 독립 메뉴("회의기록")로 분리(기존 기록함은 이제 사건만 다룸, 라벨도 "사건기록"으로 명확히 함).
- 회의록 채팅에 "🔴 회의 시작" 라이브 녹음 기능 추가: 누르면 마이크 녹음이 시작되고, 약 45초 구간마다 자동으로 텍스트로 바뀌어 채팅에 전송되면서 정리본이 회의 진행 중에 계속 갱신됨.

## v0.13.0 - 2026-07-31

- 회의록 "새로 입력"이 채팅 형식으로 바뀜: 두서없이 적은 회의 메모를 그대로 붙여넣으면 AI가 애매한 부분(예: 시각을 바꾼다는 메모에 대해 "원래 몇 시였고 왜 바꾸나요?")을 되물으면서 정식 회의록을 실시간으로 정리.
- 채팅에 회의 녹음 음성 파일을 올릴 수 있음 - 자동으로 텍스트로 바뀐 뒤 같은 대화형 정리 과정을 시작함(OpenAI 음성 인식 API 사용, Claude API는 오디오 입력을 지원하지 않아 이 부분만 별도 서비스 이용 - 분당 약 0.003달러로 저렴함).
- **배포 전 필수**: Vercel 프로젝트 환경변수에 `OPENAI_API_KEY`를 새로 추가해야 음성 변환 기능이 동작합니다(OpenAI 플랫폼에서 발급).

## v0.12.0 - 2026-07-31

- AI 예상 문의/컴플레인 제안: 카테고리명만으로 중복을 걸러내던 것을, 기존 실무자매뉴얼에 이미 규정된 내용과 검토 대기 중인 예상 문의의 실제 본문까지 AI에게 함께 보여줘서, 항목명이 달라도 이미 다뤄진 주제는 자동으로 걸러내도록 개선.
- 채택예정에 "AI 검증" 기능 추가: 실무자가 GIA 실정에 맞게 구체화한 최종 문구를 AI가 비판적으로 분석해서 예상 후속 문의/컴플레인, 조항의 맹점·허점, 구체적인 보완 제안을 짚어줌.

## v0.11.0 - 2026-07-30

- 브라우저 Supabase 클라이언트를 페이지 로드당 1개만 만들어 재사용하도록 변경(기존에는 버튼을 누르거나 목록을 구독할 때마다 매번 새 클라이언트를 새로 만들어서, 그때마다 세션을 다시 읽고 내부 잠금을 새로 잡는 지연이 있었음) - 특히 저장/발행 등 버튼 클릭 반응 속도에 가장 크게 기여.
- 로그인 승인 여부 확인을 매 페이지 이동마다 DB에서 조회하던 것을, 한 번 확인되면 5분 동안은 서명된 쿠키만으로 빠르게 통과시키도록 변경(페이지를 옮길 때마다 있었던 네트워크 왕복 1회를 대부분의 경우 제거) - 관리자가 승인을 취소/차단해도 최대 5분 이내에는 반영됨(실제 데이터 접근은 항상 Supabase 행 단위 보안(RLS)이 별도로 검사하므로 안전에는 영향 없음).
- 세션 검증을 getUser()에서 getClaims()로 교체(프로젝트가 최신 비대칭 서명키를 쓰는 경우 매 요청 인증 서버 왕복 없이 로컬 검증되어 더 빠름, 그렇지 않아도 기존과 동일하게 동작).
- 홈/기록함 등 서버 페이지의 여러 DB 조회는 이미 병렬(Promise.all)로 처리되고 있어 추가로 손볼 부분이 없었음(점검만 진행).

## v0.10.0 - 2026-07-30

- 행사기록: 정규행사(매년 반복되는 행사)와 일시적행사(한 번뿐인 행사)를 구분해서 입력.
- 행사/학기 사진을 실제로 업로드해서 보관(비공개 Storage 버킷, giamicro.com 로그인 사용자만 조회/업로드/삭제 가능 - 아동이 포함된 사진일 수 있어 비공개로 유지).
- "학기" 메뉴 신설(행사 옆): 1~3학기, 여름캠프1·2, 겨울캠프1·2를 회차별로 기록.
- 회의록 AI 분류의 "행사/학기 참고" 카테고리를 확장해서, 특정 행사뿐 아니라 특정 학기/캠프에 대한 회고도 자동으로 구분해서 매칭되는 학기 기록에 붙여줌.

## v0.9.0 - 2026-07-30

- 행사기록을 기록함에서 분리해 독립 메뉴로 노출(기록함은 이제 사건·회의만 다룸) - 행사는 매뉴얼 제작용 자료가 아니라 반복 행사를 중복 없이 기록하고 다음 행사 준비에 참고하는 목적이 달라 별도로 뺐습니다.
- 사이드바 메뉴를 목적별로 그룹화: [홈/실무자매뉴얼] · [자료 기록: 기록함·AI매뉴얼] · [행사] · [제안·발행: 제안함·채택예정] · [문서함: 매뉴얼·서류함] · [관리: 사용자 관리].
- 회의록 AI 분석에 "행사/학기 참고" 분류 추가: 특정 행사나 학기에 대한 회고·개선 아이디어는 매뉴얼 제안으로 만들지 않고, 이름이 비슷한 행사 기록의 "개선 제안"에 자동으로 붙여서 다음번 같은 행사 열릴 때 AI 비교 리포트에 바로 반영되도록 함(매칭되는 행사가 없으면 회의록의 확정 기록에 메모로 남김).

## v0.8.0 - 2026-07-30

- "실무자매뉴얼" 전용 메뉴 신설(홈 바로 다음 위치) - 학부모 문의/컴플레인 응대용 매뉴얼을 검색해서 바로 찾아볼 수 있는 화면.
- AI가 학부모 문의/컴플레인을 미리 예상해서 권장 응대 문구와 함께 제안함에 자동 등록(제안함에 "예상 문의/컴플레인" 탭 추가), 학비/환불·안전·급식·소통 등 국제학교에서 흔한 주제를 폭넓게 고려.
- 워크플로우 정리: AI 제안 → 실무자 회의로 GIA 실정에 맞게 수정 후 승인(발행예정) → 이 단계에서 AI가 실무자가 수정한 문구를 다시 한번 깔끔한 규정 문구로 정리해서 채택예정에 반영 → 실무자 확인 후 발행 → 실무자매뉴얼에 카테고리로 반영 (기존 제안함/채택예정 화면 그대로 재사용).

## v0.7.0 - 2026-07-30

- 서류함 메뉴 신설: GIA 같은 대안교육기관이 갖추면 좋은 서류를 AI가 추천해서 목록으로 만들고, 각 서류별로 "AI 초안 만들기"로 바로 다듬어 쓸 수 있는 초안을 생성, 준비 상태(필요/준비중/ 보유/만료임박/해당없음)를 관리.
- 홈 대시보드에 "반복되는 사건 유형" 경고 추가: 최근 90일 내 같은 유형 사건이 3건 이상 발생하면 자동으로 표시(AI 호출 없이 순수 집계라 비용 없음).
- 행사 기록에 "연도별 비교 리포트" 추가: 같은 이름의 과거 행사 기록 2건 이상이 있으면 AI가 개선된 점/반복되는 문제/다음 행사 제안을 요약.
- 매뉴얼(학부모용 운영계획안)에 "FAQ 자동 생성" 추가: 전체 내용을 바탕으로 학부모가 궁금해할 질문/답변을 AI가 만들고, 검토 후 매뉴얼의 "자주 묻는 질문" 항목으로 반영.
- 매뉴얼 항목에 "서명 필요" 표시 기능 추가(환불 규정, 안전 수칙 등 학부모용 항목 한정) - 체크해두면 PDF로 출력할 때 그 항목 뒤에 학생명/보호자 성명/서명/날짜를 적을 수 있는 서명란이 자동으로 들어감(앱은 내부 인원만 쓰므로 전자서명 대신 배포용 문서에 서명란을 넣는 방식).

## v0.6.0 - 2026-07-30

- AI 매뉴얼 작성 시 어느 문서(학부모용 운영계획안/실무자매뉴얼)에 반영할지 사람이 미리 고르지 않고 AI가 내용을 보고 직접 판단(차량 탑승·아동 인계·환불 규정처럼 학부모도 알아야 하는 내용은 두 문서 모두에, 교사 채용 기준처럼 내부용 절차는 실무자매뉴얼에만 반영).
- 판단 이유를 화면에 함께 표시해서 왜 그렇게 분류됐는지 확인 가능.
- 사건/행사 AI 분석에서도 같은 로직 적용 - "두 문서 모두 해당"으로 판단되면 자동으로 두 건의 제안을 각각 만들도록 기존 버그 수정(이전에는 "둘다"라는 문자열이 그대로 저장되어 매뉴얼 화면 어디에도 반영되지 않는 상태였음).
- AI 비용 절감: 맞춤법 정리·회의 내용 분류처럼 실수해도 검토 단계에서 바로 잡을 수 있는 작업은 저렴한 모델(Haiku)로 전환, 학부모 공지·법적 판단처럼 리스크가 큰 작업만 고품질 모델(Sonnet) 유지 - 기존 prompt caching과 함께 적용되어 비용이 추가로 절감됨.

## v0.5.0 - 2026-07-30

- 로그인 승인제 도입: giamicro.com 계정이면 누구나 로그인은 되지만, 관리자가 승인해야만 대시보드에 들어갈 수 있음(승인 전에는 "승인 대기" 화면).
- 승인된 사람은 관리자로서 신규 신청 승인/거절과 기존 사용자 차단 가능.
- 새 메뉴 "사용자 관리" 추가: 승인 대기/승인됨/거절됨 목록을 실시간으로 확인하고 처리.
- 메뉴/버튼에 마우스를 올렸을 때 손가락 커서와 함께 색이 바뀌도록 통일(이전에는 아무 변화가 없어 클릭 가능한지 알기 어려웠음), 로고는 색 변화 없이 커서만 바뀌도록 유지.

## v0.4.0 - 2026-07-30

- 헤더/사이드바 로고 클릭 시 홈으로 이동하도록 복구(리스트 개편 때 빠졌던 링크 연결).
- 매뉴얼 화면을 "운영계획안"/"실무자매뉴얼" 탭 구조로 개편.
- 매뉴얼 항목을 구글독스처럼 서식(굵게·기울임·제목·목록) 적용하며 바로 보고 편집할 수 있는 리치 텍스트 에디터(Tiptap) 도입 - 새 항목 추가/기존 항목 수정 모두 적용.
- 발행(채택예정→매뉴얼) 시 기존 내용과 새 내용을 서식 깨지지 않게 이어붙이도록 병합 로직 개선.
- PDF 생성 시 리치 텍스트 내용을 태그 없는 읽기 좋은 텍스트로 자동 변환해서 출력.

## v0.3.0 - 2026-07-30

- AI 매뉴얼 메뉴 신설: 규정/매뉴얼 초안을 자유 텍스트로 작성하면 AI가 정식 문구 + 관련 법령까지 찾아 제안함(AI매뉴얼제안 탭)에 자동 등록, 승인 시 기존 채택예정→발행 흐름 그대로 매뉴얼에 반영.
- 회의록 작성/수정 화면에 "🧹 AI로 정리" 버튼 추가 - 두서없이 쓴 회의 메모를 AI가 맞춤법 교정 + 문어체로 정리(저장 전 미리보기/수정 가능).
- 제안함에 "AI매뉴얼제안" 탭 추가(전체/사건/행사/회의/AI매뉴얼 5개 탭).

## v0.2.0 - 2026-07-30

- 로고 반영: 사이드바/모바일 헤더에 GIA Micro Lab 로고(가로형), 로그인 화면에 크레스트 로고(정방형) 적용.
- 홈 대시보드 강화: "기록 현황"(사건/행사/회의) + "처리할 일"(제안함 대기·채택예정 대기·발행된 매뉴얼 항목) 두 그룹으로 확장.
- 기록함 메뉴 신설: 기존 사건/행사/회의 3개 메뉴를 "기록함" 하나로 통합하고 탭 + 건수 카드로 전환.
- 제안함에 카테고리 탭(전체/사건/행사/회의) 및 분류별 건수 표시 추가.

## v0.1.0 - 2026-07-30 (최초 커밋)

- Supabase(Postgres+Auth+Realtime) + Vercel 기반 독립 웹앱으로 최초 구축 (구글 시트/문서 미사용).
- 사건/행사/회의 기록 CRUD + 실시간 동기화.
- Google OAuth 로그인(giamicro.com 도메인 제한) + PIN 2차 보안.
- AI 제안 워크플로우(스캔 → 제안 → 승인/보류/삭제 → 채택예정 → 발행).
- 매뉴얼(운영계획안/실무자매뉴얼) 앱 내 직접 편집(CRUD) + PDF 생성.
- 모바일 반응형 전체 적용.

<!-- deploy: production 빌드 트리거(같은 SHA가 staging에 먼저 올라가면 Vercel이 main 빌드를 건너뛰므로, 앞으로는 main 먼저 푸시합니다) -->
