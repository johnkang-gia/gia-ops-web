# 구글챗 미러링 설정 가이드 (본인 계정 인증 방식)

이 문서는 gia 운영앱 업무탭에 구글챗 두 방(출결알림 / 선생님요청)의 메시지를 실시간으로 읽기전용 미러링하기 위해, 강경원님이 직접 해야 하는 부분을 순서대로 정리한 것입니다.

**이전 버전과 달라진 점:** 처음에는 "서비스 계정 + 도메인 전체 위임" 방식으로 설계했지만, 이 방식은 Google Workspace 관리자 콘솔 접근 권한이 필요해서 부이사장님께 매번 요청드려야 했습니다. 지금은 강경원님 본인 계정으로 딱 한 번 로그인해서 허용 버튼만 누르면 끝나는 방식으로 바꿨습니다 - **관리자 콘솔(admin.google.com)에는 전혀 들어갈 필요가 없습니다.** 다만 강경원님 계정이 출결알림, 선생님요청 두 방에 모두 속해 있어야 합니다(확인 완료).

코드/DB는 이미 staging에 반영되어 있습니다(v0.84.0). 아래 설정을 마치면 바로 동작합니다.

전체 흐름: 구글챗 메시지 → Google Workspace Events API → Google Cloud Pub/Sub → gia 운영앱 웹훅 → Supabase 저장 → 업무탭 실시간 표시.

예상 소요시간: 25~40분. 전부 강경원님 개인 Google 계정 권한 안에서 끝납니다.

---

## 사전 준비물

- 출결알림, 선생님요청 두 구글챗 방에 모두 속해 있는 본인 Google 계정 (확인 완료).
- Vercel에서 gia-ops-web 프로젝트 환경변수를 수정할 수 있는 권한.
- 터미널(가능하면) - 없어도 진행 가능하지만 있으면 STEP 9가 더 쉽습니다.

**만약 막히는 부분이 있다면:** 딱 한 곳, "STEP 1. Google Cloud 프로젝트 만들기"가 혹시 회사 정책으로 막혀 있을 수 있습니다(일부 조직은 아무나 새 프로젝트를 못 만들게 해둡니다). 이때만 "제 계정으로 Cloud 프로젝트 하나 만들 수 있게 해주실 수 있나요?" 정도의 아주 가벼운 요청만 하면 되고, 그 외에는 전혀 도움이 필요 없습니다.

---

## STEP 1. Google Cloud 프로젝트 만들기

1. 브라우저에서 **https://console.cloud.google.com** 접속 후, 본인 계정으로 로그인합니다.
2. 화면 상단의 프로젝트 선택 드롭다운을 클릭 → **새 프로젝트**
3. 프로젝트 이름을 입력합니다. 예: `gia-ops-chat-mirror`
4. **만들기** 클릭. 몇 초 기다리면 프로젝트가 생성되고, 상단 드롭다운에서 방금 만든 프로젝트를 선택합니다(이후 모든 단계는 이 프로젝트 안에서 진행).

---

## STEP 2. 필요한 API 3개 활성화

왼쪽 메뉴(≡) → **API 및 서비스** → **라이브러리**로 이동해서, 아래 3개를 각각 검색 → 클릭 → **사용 설정(Enable)** 버튼을 누릅니다.

1. **Google Chat API**
2. **Google Workspace Events API**
3. **Cloud Pub/Sub API**

---

## STEP 3. OAuth 동의 화면 설정

(Google Cloud 콘솔 UI가 최근 "Google Auth Platform"이라는 마법사 형태로 개편되었습니다 - 이름만 다를 뿐 절차는 같습니다.)

1. 왼쪽 메뉴 → **API 및 서비스**(또는 검색창에 "Google Auth Platform") → **OAuth Overview** 화면에서 **Get started** 클릭
2. **App Information**: 앱 이름 `GIA 운영앱 구글챗 미러링` (아무 이름이나 무방), User support email에 본인 이메일 입력 → **Next**
3. **Audience**: **Internal**이 선택 가능하면 그것을 선택합니다(회사 도메인 계정만 로그인 가능 - 더 안전하고 별도 심사도 없습니다). 안 보이면 **External**을 선택합니다 → **Next**
4. **Contact Information**: 본인 이메일 입력 → **Next**
5. **Finish**: "I agree to the Google API Services User Data Policy" 체크 → **Create**
6. 왼쪽 메뉴 **Data Access** 클릭 → **Add or remove scopes** → 검색창에 아래 두 값을 각각 입력해 체크 → **Update** → **Save**

   ```
   https://www.googleapis.com/auth/chat.spaces.readonly
   https://www.googleapis.com/auth/chat.messages.readonly
   ```

7. 3번에서 **External**을 선택했다면: 왼쪽 메뉴 **Audience** → 아래쪽 **Test users** → **Add users** → 본인 이메일 추가

---

## STEP 4. OAuth 클라이언트 ID 만들기

1. 왼쪽 메뉴 **Clients** 클릭 → **Create client**
2. Application type: **Web application**
3. 이름: `gia-ops-web` (아무 이름이나 무방)
4. **Authorized redirect URIs**에 아래 값을 정확히 추가합니다(오타 있으면 인증이 실패합니다):

   ```
   https://gia-ops.vercel.app/api/google-chat/oauth/callback
   ```

   (배포 주소는 `https://gia-ops.vercel.app`입니다.)
5. **Create** 클릭 → 화면에 뜨는 **Client ID**와 **Client secret** 두 값을 복사해둡니다. STEP 8에서 씁니다. (나중에 다시 보려면 **Clients** 탭에서 방금 만든 클라이언트를 클릭하면 됩니다.)

---

## STEP 5. Pub/Sub 주제(Topic) + 구독(Subscription) 만들기

(왼쪽 사이드바에 "Pub/Sub"이 안 보이면, 화면 맨 위 검색창을 클릭하거나 `/` 키를 누른 뒤 `Pub/Sub`을 검색해서 들어갑니다 - 직접 주소로 가려면 `https://console.cloud.google.com/cloudpubsub/topic/list?project=<프로젝트ID>`. 콘솔이 영어로 나오는 경우를 위해 아래 메뉴명을 한글(영어) 형태로 병기했습니다.)

1. Pub/Sub 화면 왼쪽 사이드바 **주제(Topics)** 클릭 → 위쪽 **주제 만들기(CREATE TOPIC)** 버튼
   - **주제 ID(Topic ID)**: `gia-chat-mirror`
   - **주제에 기본 구독 추가(Add a default subscription)** 체크는 **해제**합니다(아래에서 직접 만들 것이므로).
   - **만들기(CREATE)** 버튼 클릭
2. 방금 만든 주제를 클릭 → 위쪽 탭 중 **구독(Subscriptions)** 탭 → **구독 만들기(CREATE SUBSCRIPTION)** 버튼
   - **구독 ID(Subscription ID)**: `gia-chat-mirror-push`
   - **전송 유형(Delivery Type)**: **Push**
   - **Push 엔드포인트 URL(Endpoint URL)**: `https://gia-ops.vercel.app/api/google-chat/webhook?secret=<웹훅비밀값>`
     - `<웹훅비밀값>`은 STEP 7에서 만듭니다 - 지금은 임시 값을 넣고, STEP 7을 마친 뒤 이 구독을 열어 URL을 수정해도 됩니다(구독 상세 화면 → 수정 EDIT).
   - 나머지 옵션은 기본값 그대로 두고 **만들기(CREATE)** 버튼 클릭
3. Google Chat이 이 주제에 메시지를 발행할 수 있도록 권한을 줘야 합니다.
   - 주제 상세 화면 → 위쪽 탭 중 **권한(Permissions)** 탭 → **주 구성원 추가(ADD PRINCIPAL)** 버튼
   - **새 주 구성원(New principals)** 입력란: `chat-api-push@system.gserviceaccount.com` (구글챗 전용 고정 시스템 계정)
   - **역할 선택(Select a role)** 입력란에 `Pub/Sub Publisher`(한글 UI에서는 "Pub/Sub 게시자") 검색해서 선택
   - **저장(SAVE)** 버튼 클릭
   - **저장**

---

## STEP 6. 두 구글챗 방의 스페이스 ID 확인하기

1. 브라우저에서 **https://chat.google.com** 접속
2. "출결알림" 방을 클릭해서 엽니다
3. 주소창의 URL을 확인합니다. 끝에 영문+숫자로 된 부분(`.../room/AAAAxxxxxxxxx` 또는 `.../space/AAAAxxxxxxxxx`)이 있습니다.
4. 그 부분 앞에 `spaces/`를 붙인 값을 기록해둡니다. 예: `spaces/AAAAxxxxxxxxx`
5. "선생님요청" 방도 똑같이 열어서 ID를 확인하고 기록해둡니다.

---

## STEP 7. 웹훅 비밀값 만들기

이 값은 Pub/Sub가 우리 앱을 부를 때 진짜 Google이 보낸 요청인지 확인하는 용도입니다.

- 터미널이 있다면: `openssl rand -hex 32` 실행 → 나온 문자열을 복사
- 터미널이 없다면 아무 무작위 문자열 생성 웹사이트에서 32자 이상의 영문+숫자 조합을 만들어도 됩니다.

STEP 5-2에서 만든 Pub/Sub 구독의 URL 끝에 `?secret=이값`이 정확히 들어가 있는지 다시 한번 확인합니다.

---

## STEP 8. Vercel 환경변수 등록

1. **https://vercel.com** 접속 → gia-ops-web 프로젝트 선택 → **Settings** → **Environment Variables**
2. 아래 6개를 하나씩 추가합니다. **Production과 Preview 둘 다 체크**해주세요(staging 브랜치는 Preview로 배포됩니다).

| 이름 | 값 |
|---|---|
| `GOOGLE_OAUTH_CLIENT_ID` | STEP 4의 클라이언트 ID |
| `GOOGLE_OAUTH_CLIENT_SECRET` | STEP 4의 클라이언트 보안 비밀번호 |
| `GOOGLE_OAUTH_REDIRECT_URI` | `https://gia-ops.vercel.app/api/google-chat/oauth/callback` (STEP 4와 정확히 동일) |
| `GOOGLE_CHAT_SPACE_ATTENDANCE` | STEP 6의 출결알림 방 ID (`spaces/AAAA...`) |
| `GOOGLE_CHAT_SPACE_TEACHER_REQUESTS` | STEP 6의 선생님요청 방 ID (`spaces/BBBB...`) |
| `GOOGLE_PUBSUB_TOPIC` | `projects/<프로젝트ID>/topics/gia-chat-mirror` (프로젝트ID는 Cloud Console 상단에서 확인) |
| `GOOGLE_CHAT_WEBHOOK_SECRET` | STEP 7에서 만든 값 |

3. 저장 후 **Deployments** 탭 → 최신 배포 오른쪽 **⋯** → **Redeploy**로 재배포합니다.

---

## STEP 9. 본인 계정으로 최초 1회 로그인

재배포가 끝나면, gia 운영앱에 개발자 계정(강경원님)으로 로그인한 상태에서 브라우저 주소창에 아래 주소를 입력해 접속합니다.

```
https://gia-ops.vercel.app/api/google-chat/oauth/start
```

구글 로그인/동의 화면이 뜹니다 - "Google에서 확인하지 않은 앱입니다" 같은 경고가 보일 수 있는데, 본인이 방금 만든 앱이므로 **고급(Advanced)** → **[앱 이름]으로 이동(안전하지 않음)**을 눌러 계속 진행하면 됩니다. 두 스코프(구글챗 스페이스/메시지 읽기)에 대한 허용 버튼을 누르면, "연동 완료" 화면이 뜨면서 끝납니다.

이 단계가 끝나면 아래 명령(또는 브라우저 접속)으로 실제 구독을 만듭니다. 터미널에서:

```
curl -H "Authorization: Bearer <CRON_SECRET>" "https://gia-ops.vercel.app/api/cron/renew-chat-subscriptions"
```

정상이면 아래처럼 응답이 옵니다.

```
{"ok":true,"results":{"attendance":"created","teacher_requests":"created"}}
```

터미널이 없다면, 매일 자동으로 도는 크론이 다음날 대신 실행해주지만, 가능하면 직접 한 번 호출하는 걸 권장합니다.

---

## STEP 10. Supabase에 SQL 실행 (아직 안 하셨다면)

아래 SQL을 Supabase 프로젝트의 **SQL Editor**에 붙여넣고 실행합니다(이미 실행하셨다면 건너뛰세요 - `if not exists` 처리가 되어있어 다시 실행해도 안전합니다).

```sql
-- ===== 행정요청 제거 + 구글챗 미러링(출결알림/선생님요청) 도입 =====
drop trigger if exists tasks_sync_staff_request on tasks;
drop function if exists sync_staff_request_from_task();
drop trigger if exists staff_request_comments_bump_count on staff_request_comments;
drop function if exists bump_staff_request_comment_count();
drop function if exists create_staff_request(text, text, text, text, text, text, text, text, text[], text);
drop table if exists staff_request_comments cascade;
drop table if exists staff_requests cascade;
drop table if exists staff_request_categories cascade;

create table if not exists google_chat_mirror_messages (
  id uuid primary key default gen_random_uuid(),
  source_key text not null check (source_key in ('attendance', 'teacher_requests')),
  google_message_id text not null unique,
  google_space_id text,
  sender_display_name text,
  sender_email text,
  content text not null,
  created_at_google timestamptz not null,
  received_at timestamptz not null default now(),
  task_id uuid references tasks(id) on delete set null
);

create index if not exists google_chat_mirror_messages_source_created_idx
  on google_chat_mirror_messages(source_key, created_at_google desc);

alter table google_chat_mirror_messages enable row level security;

drop policy if exists "wr_manager_select_google_chat_mirror_messages" on google_chat_mirror_messages;
create policy "wr_manager_select_google_chat_mirror_messages" on google_chat_mirror_messages
  for select using (is_wr_manager());

drop policy if exists "wr_manager_update_google_chat_mirror_messages" on google_chat_mirror_messages;
create policy "wr_manager_update_google_chat_mirror_messages" on google_chat_mirror_messages
  for update using (is_wr_manager()) with check (is_wr_manager());

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'google_chat_mirror_messages'
  ) then
    alter publication supabase_realtime add table google_chat_mirror_messages;
  end if;
end $$;

create table if not exists google_chat_subscriptions (
  source_key text primary key check (source_key in ('attendance', 'teacher_requests')),
  subscription_name text,
  expire_time timestamptz,
  updated_at timestamptz not null default now()
);

alter table google_chat_subscriptions enable row level security;

drop policy if exists "wr_manager_select_google_chat_subscriptions" on google_chat_subscriptions;
create policy "wr_manager_select_google_chat_subscriptions" on google_chat_subscriptions
  for select using (is_wr_manager());

create table if not exists google_chat_oauth_tokens (
  id text primary key default 'default',
  refresh_token text not null,
  updated_by text,
  updated_at timestamptz not null default now()
);

alter table google_chat_oauth_tokens enable row level security;
```

---

## STEP 11. 동작 확인

1. 구글챗 "출결알림" 방에 아무 메시지나 하나 보내봅니다.
2. gia 운영앱 → 업무탭 → 왼쪽 위 "출결알림" 패널에 몇 초~1분 안에 그 메시지가 나타나면 성공입니다.
3. 메시지 옆 **🔧 업무로 등록** 버튼을 눌러서 업무보드에 잘 들어가는지도 확인합니다.
4. "선생님요청" 방도 똑같이 확인합니다.

---

## 문제가 생기면

- 메시지가 안 뜨는 경우 대부분 원인: 환경변수 오타(특히 스페이스 ID), 리디렉션 URI가 STEP 4/STEP 8에서 서로 다른 경우, Pub/Sub 구독 URL의 `secret` 값과 `GOOGLE_CHAT_WEBHOOK_SECRET`이 다른 경우, STEP 9 로그인을 아직 안 한 경우.
- "refresh_token을 받지 못했습니다" 화면이 뜨면: 이미 한 번 동의한 적이 있어서 그렇습니다. 본인 Google 계정 설정 → 보안 → 타사 앱 및 서비스에서 이 앱의 연결을 끊고, STEP 9를 다시 시도하면 됩니다.
- gia 운영앱의 개발자 전용 오류로그 화면(`/dev`)에서 `google-chat-webhook`, `cron:renew-chat-subscriptions`, `google-chat/oauth/callback` 라우트의 오류 메시지를 확인할 수 있습니다.
- 막히는 부분이 있으면 어느 STEP에서 무슨 화면이 나왔는지 캡처해서 알려주시면 바로 봐드릴게요.
