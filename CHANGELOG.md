# CHANGELOG

이 파일은 `gia-ops-web`의 버전별 변경 이력을 기록합니다. 버전 번호는 `package.json`의
`version` 값과 항상 일치시킵니다. 업데이트할 때마다 이 파일 맨 위에 새 항목을 추가하고,
같은 내용을 GitHub Desktop의 커밋 Summary/Description에도 그대로 사용하면 됩니다.

## v0.217.0 - 2026-08-25

- **업무 대시보드 상단에 학교 요약.** 요청: 지금 무슨 학기·학기말까지 남은 기간 + 초등부 재학생 수를 맨 위에 배지로 표시.
- **수업·시간표에 학생 수 배지.** 선택 부서의 학년별 학생 수 요약 + 시간표 그리드 각 반 헤더에 "N명" 배지.

## v0.216.0 - 2026-08-25

- **수업·시간표 메뉴 신설 (③).** [학교 > 수업·시간표]에 ①공간 사용 현황(식당·체육관·컴퓨터실·미술실 등 지금 사용 중/비어 있음·누가 쓰는지) ②전체 시간표(부서·요일 선택, 반×교시 그리드로 과목·선생님·교실) ③지금 수업 없는 선생님·선생님별 주간 시수 ④담임 배정·과목 관리 바로가기를 한 화면에 모았습니다.
- **개발자 오류 로그 복사 버튼 (②).** 개발자 메뉴 오류 로그에 "전체 복사"와 개별 "복사" 버튼 추가(스택 포함). 붙여넣어 바로 공유할 수 있습니다.
- **학기 라벨 업데이트 (①).** 현재 진행 학기를 "26-27 1학기"로 바꾸는 SQL 제공(supabase/manual/2026_학기라벨_26-27_1학기.sql). 학년도는 26-27처럼 두 해를 잇고 1·2·3학기 + 여름/겨울 캠프 구조입니다.

## v0.215.0 - 2026-08-25

- **학교 개요 대폭 보강 + 모든 학교 페이지에 탭바.** 요청: 개요가 숫자만 있어 휑함 + 다른 페이지에서도 탭 이동. 개요에 ①현재 학기(무슨 학기·기간·학기말 D-day) ②부서별 재학생(유치·초등·중고등) ③반별 현황 표(반·담임·학생수, 반을 누르면 학생 명단 펼침) ④선생님 수·과목별 담당 선생님 ⑤학년별 분포·학사일정을 넣어 한 화면에서 학교 전체가 파악되게 함. 학교 전 페이지(개요·학생·교직원·명부·반·과목·학사일정·당번·학기준비)에 상단 탭바를 붙여 어디서든 탭으로 이동.

## v0.214.0 - 2026-08-25

- **학생 조회 재구성 (⑤).** 재학/졸업/퇴학 탭으로 나누고, 상단에 검색(이름·학번·영문이름)과 현재 탭의 학년별 분포 막대를 배치. 명단은 넓은 카드 그리드(학번 표시)로 전체 폭을 활용. 학교 탭바도 붙였습니다.

## v0.213.0 - 2026-08-25

- **개요 대시보드 정보 보강(셔틀·학교).** 요청: 숫자만 있어 휑함 → 실제 정보를 더. 학교 개요에 "반별 현황"표(반·담임·학생수, 담임 미배정 빨강) 추가. 셔틀 개요에 "오늘 픽업·결석 학생 명단"과 "지속 특이사항 목록"을 추가해 공간을 정보로 채웠습니다.

## v0.212.0 - 2026-08-25

- **학교 개요 대시보드 + 탭 구조 신설 (①).** 셔틀과 같은 "개요 + 탭" 방식으로 학교 메뉴를 재편. 학교를 누르면 개요가 먼저 열리고, 상단 탭(개요·학생 조회·교직원·명부 관리·반·과목·학사일정·당번표·학기 준비)으로 이동. 개요에는 재학생·졸업·퇴학, 반·담임 미배정, 이번주 위클리 리포트 KPI와 학년별 재학생 분포(막대), 다가오는 학사일정을 시각화(숫자 카운트업·막대 애니메이션). 다음 단계에서 학생조회 재학/졸업/퇴학 탭·명부관리·반관리·학기준비를 순서대로 개선합니다.

## v0.211.0 - 2026-08-25

- **셔틀 노선·배정·배차표: 상단 지도 + 정류장 클릭 이동.** 요청 ⑬. 노선을 고르면 상단에 노선 지도가 항상 뜨고, 아래 배차표에서 정류장 주소(📍)를 누르면 지도가 그 정류장으로 이동·확대됩니다. 배차표/노선도 토글은 없애고 지도+표를 한 화면에 함께 보여줍니다(인쇄본은 표만).

## v0.210.0 - 2026-08-25

- **픽업 인박스 학습.** 요청 ⑩. 픽업으로 확정하거나 "픽업 아님"으로 넘길 때마다 발신자(어머니·채널)별로 그 판단을 누적 학습합니다. 다음부터는 늘 픽업이던 발신자의 픽업은 더 빨리 자동확정되고, 대개 픽업이 아니던 발신자의 픽업은 신뢰도를 낮춰 사람이 한 번 더 확인하도록 유도합니다(분류 자체는 바꾸지 않아 오분류를 만들지 않는 안전한 방식). (DB 반영 필요: 이번배포 마이그레이션모음 SQL)

## v0.209.0 - 2026-08-25

- **셔틀 월별 운행 리포트 자동화.** 요청 채택. [셔틀 > 기록·분석] 상단에 이번 달 노선별 운행일수·막차 평균·정시/지연·정시율·평균 지연을 자동 집계해 표로 보여줍니다(전체 정시율 포함). 정시 기준은 막차 평균 ±5분, GPS 도착기록이 쌓일수록 정확해집니다.

## v0.208.0 - 2026-08-25

- **셔틀 기사·차량 변경 이력 자동 기록.** 요청 채택. 노선의 기사·연락처·차량번호·동승 선생님이 바뀌면 트리거가 자동으로 스냅샷을 남깁니다. [셔틀 > 링크·기기·GPS] 화면 하단의 "🧾 기사·차량 변경 이력"에서 언제 누가 어떤 차로 운행했는지 시간순으로 확인할 수 있습니다. (DB 반영 필요: 이번배포 마이그레이션모음 SQL)

## v0.207.0 - 2026-08-25

- **셔틀 개요: 결석·픽업 정류장 스킵 + 막차 ETA 재계산.** 요청 채택. 오늘 그 정류장의 탑승자가 전원 픽업/결석이면 "오늘 건너뜀"으로 세고, 건너뛴 정류장 수만큼 막차 도착을 앞당긴 조정 ETA(→ 시각)를 노선 상세표에 표시합니다.

## v0.206.0 - 2026-08-25

- **셔틀 개요: 정원 대비 실탑승 경고 + 노선별 막차 평균·지연 + 노선 상세 요약표(하단 여백 채움).** 요청 ⑨ + 벤치마킹 채택(정원 경고·예상 소요·지연). 노선 카드에 오늘 인원/정원(초과 시 빨간 ⚠)과 막차 평균·오늘 지연(+분/−분)을 표시하고, 하단에 기사·차량·정원·막차·지연·GPS를 한눈에 보는 노선 상세표를 추가. 막차 평균은 정류장 GPS 도착기록 누적으로 날마다 정확해집니다.

## v0.205.0 - 2026-08-25

- **셔틀 링크·기기·GPS 통합 + 개요 요약.** 요청 ⑰. 내 폰 GPS 테스트를 링크·기기 화면 안(접이식)으로 합치고, 상단에 등록 기기·연결 중·미설치·안내보드/도착 링크 수를 한눈에 보는 개요 요약을 추가. 전체 폭으로 넓힘. (메뉴 2→1)

## v0.204.0 - 2026-08-25

- **셔틀 노선·배정·배차표 통합 + 설정 페이지 여백 제거.** 요청 ⑯⑮. 노선 관리·탑승 배정·배차표 인쇄가 사실상 같은 종합 화면(/shuttle)이라 하나로 합치고(메뉴 3→1, 탭 "노선·배정·배차표"), 이 설정 화면들을 전체 폭으로 넓혀 좌우 여백 낭비를 없앴습니다.

## v0.203.0 - 2026-08-25

- **셔틀 전 페이지에 고정 탭바(공용 레이아웃).** 요청: "다른 탭 누르면 페이지로 이동해 다시 개요로 와야 함 → 탭으로 이동". 이제 셔틀 어느 화면에 있어도 상단 탭(개요·체크표·실시간·지역·노선·배정·링크·기기·기록)이 항상 떠 있고, 탭을 누르면 탭바(셸)는 그대로 둔 채 아래 내용만 바뀝니다. 실시간·지역이 개요와 같은 셸의 탭으로 들어와 오가기 편해졌습니다. (앞으로 학교·업무 개요도 같은 방식)

## v0.202.0 - 2026-08-25

- **셔틀 개요 대시보드 신설 (새 구조 1단계).** 요청: 메뉴 여러 개를 "개요 + 탭"으로 통합, 여백을 시각화로 채우기. 셔틀 메뉴를 누르면 개요가 먼저 열리고 상단 탭(개요·체크표·실시간·지역·노선·배정·기록)으로 이동. 오늘 하원의 탑승예정(도넛)·픽업·결석·운행중 차량·지속 특이사항을 한 화면에 집계하고, 노선별 카드 그리드에 인원·GPS 상태(펄스 점)를 표시. 숫자 카운트업·도넛 채움·hover 리프트 등 은은한 모션 적용. (실데이터 연동)

## v0.201.0 - 2026-08-25

- **PC 와이드 레이아웃 1단계.** 요청: "PC 전용이니 페이지를 더 넓게". 데이터가 많은 넓은 페이지(체크표·표·대시보드 등, max-w-6xl/7xl)를 큰 화면(≥1280px)에서 더 넓게(최대 1344~1536px, 초광폭에서 최대 1760px) 펼칩니다. 폼·읽기용 좁은 페이지는 가독성을 위해 그대로 두고, 인쇄본도 영향 없습니다. (카드·표·여백 통일과 노선관리 2단 재구성은 다음 단계)

## v0.200.0 - 2026-08-25

- **셔틀 하위메뉴 3그룹으로 분류.** 요청: "셔틀 메뉴가 너무 복잡, 통합·분류". ① 매일 하원 운영(하원 체크표·픽업 인박스·실시간 위치·지역별 현황) ② 기준정보·설정(노선 관리·탑승 배정·배차표·링크/기기·GPS 테스트) ③ 기록·분석(정류장 도착시간)으로 소제목·구분선을 넣어 정리(기능 삭제 없음).

## v0.199.0 - 2026-08-25

- **하원 체크표: 2호 김사랑(유치부) 숨김.** 초등부 동명이인 때문에 이름 대조로 잘못 딸려오던 유치부 김사랑을 콕 집어 제외.
- **중등부로 진급했지만 하원 셔틀은 계속 타는 7명(박준후·문수민·이도후·곽호율·박지음·강여명·정서안) 명단에 표시.** 요청대로 숨기지 않습니다.
- **오늘 안 타는 아이를 더 흐리게(회색조·투명도↑).** 오늘 타는 아이와 확실히 구분되도록.

## v0.198.0 - 2026-08-25

- **하원 체크표: 지속 특이사항 창구.** 요청: 왼쪽에 "이라엘 수요일 수영학원", "4호 김재이 당분간 개별하원(셔틀 안 탐)" 같은 지속 반영사항을 적는 창구를 만들고, 적으면 오른쪽 상단에 요약으로 계속 띄우며 셔틀도 자동 반영. 효과는 세 가지 - 메모만 / 특정 요일 셔틀 제외(예: 수영학원 가는 요일) / 개별하원(셔틀 전면 제외). 오른쪽 요약의 ⋯ → [삭제하고 원래 셔틀로]를 누르면 원래 배정으로 복귀합니다(파괴적 수정이 아니라 덧씌우기라 안전).
- **하원 체크표: 요일별 다른 셔틀 타는 학생을 같은 색 테두리로 묶음.** 요청: 요일마다 다른 차를 타는 아이(예: 임지효 4-2호 화수목 / 10호 월금)를 같은 색으로 묶고, 오늘 타는 셔틀은 선명하게·안 타는 날 셔틀은 옅게 전부 보이도록(갑자기 다른 날 셔틀을 태워달라는 요청에 대비). 실시간 반영(다른 직원이 적어도 즉시 보임).

## v0.197.0 - 2026-08-24

- **토들 수집기 "로그인 필요" 오탐 자동 복구 (수집기 v2.4.0).** 요청: "로그인이 되어있는데 로그인 필요라고". 밤새 크롬이 탭을 잠재웠다 깨울 때 생기는 가짜 "로그인 필요"를, 토들 탭을 자동으로 한 번 새로고침하고 다시 시도해 스스로 복구합니다. 그래도 안 되면 진짜 만료이므로 "새로고침(F5) 후 다시 로그인" 안내를 더 분명히 띄웁니다. (PC에서 [업데이트] 실행 필요)

## v0.196.0 - 2026-08-24

- **"not here"(아이가 아직 안 왔다는 담임 문의)를 결석으로 처리하지 않습니다.** 요청대로 결석 판단 문구가 아니라 그냥 넘어갑니다(행정실이 학부모께 등원/지각/결석을 확인). 확실한 결석 표현(absent / not coming 등)만 결석으로 잡습니다.

## v0.195.0 - 2026-08-24

- **하원 체크표: 초등부 명부 + 알려주신 중고등부 탑승자 16명만** 표시(요청 정정). 그 외(유치부 등)는 숨깁니다.

## v0.194.0 - 2026-08-24

- **정류장 도착 시간 기록·평균** 화면을 새로 만들었습니다(셔틀 → 정류장 도착시간). 요청: "각 정류장 도착하는 시간 기록해서 평균을 내줘 (...) 어느 정류장에 어느 시간에 정차했고 위치가 어디인지 클릭해서". 노선별로 각 정류장의 **평균 도착시각·오늘 도착·관측 일수**를 보여주고, 정류장을 누르면 **지도 위치와 지난 도착 기록**을 봅니다. 마지막 정류장 평균 도착시각도 함께 표시합니다.
- **하원 체크표를 우리 명부(초등부)만** 보이게 했습니다. 요청대로 유치부·중고등부 등 명부에 없는 이름은 전부 숨깁니다(배정의 학생 부서로 걸러냄).
- (자료) **27호_하원시간대만.sql** — 테스트라도 하원 시간대만 추적하도록 24시간 추적을 끄는 SQL(요청 변경). 관리자 화면의 [24h 테스트]를 꺼도 동일합니다.

## v0.193.0 - 2026-08-24

- **하원 체크표를 정규학기 기본으로.** 요청: "정규학기 애들 체크가 하나도 안되어 있어". 여름캠프2가 끝나 기본 학기를 정규학기로 바꿔 정규학기 명단이 바로 뜹니다.
- **하원 체크표에 토들 픽업·결석 자동 반영.** 요청대로 오늘 토들·전화·구글챗으로 들어온 픽업/결석을 명단에 자동으로 얹습니다(사람이 직접 누른 값이 우선).
- **안 타는 아이도 옅은 회색으로 표시 + 눌러서 탑승.** 요청: "안타는 아이도 옅은 회색으로 (...) 갑자기 탑승하게 되면 눌러서 탑승으로". 오늘 안 타는 학생은 회색으로 흐리게 보이고, 🚌 탑승 버튼으로 오늘 탑승 처리할 수 있습니다.
- **하원 시작~다음날 아침에는 시간표 자리에 학교 정보·학사일정.** 요청대로 저녁엔 시간표 대신 오늘 학교 요약(재적·결석·지각·픽업·이번주 리포트)과 이번 달 **학사일정 달력**·다가오는 일정을 보여줍니다.
- **하원 지도 오른쪽을 위아래 2칸 순환으로** 바꿨습니다(요청). 지금은 27호만 있어 한 칸에 27호가 뜹니다.
- (자료) **27호_테스트_자동추적.sql** — 테스트 동안 27호를 온오프 없이 자동 추적(always_on)으로 두는 SQL. 관리자 화면의 [24h 테스트] 토글과 동일합니다.

## v0.192.0 - 2026-08-24

- **기사님이 직접 켜고/끄는 방식으로 안내를 바꿨습니다.** 요청: "기사님께서 온오프하실 수 있도록 (...) 운행 종료후에는 (...) 끄게끔". 설정 페이지·안내를 "운행 전 켜고, 끝나면 끄기(운행 종료)"로 바꿔 개인정보 부담을 줄였습니다. 꺼두면 위치가 전혀 전송되지 않습니다.
- **GPS 미시작 경고.** 요청: "3:50분부터 신호가 안오면 (...) 켜달라고 안내". 평일 15:50 이후 GPS 기기가 있는데 신호가 없는 노선을 하원 화면 상단에 빨간 배너로 띄워, 사무실에서 기사님께 전화로 안내할 수 있습니다.
- **하원 지도 오른쪽을 2×2(4개) 노선 순환으로.** 요청대로 오른쪽에 4개 지도를 띄워 여러 호차를 겹치지 않게 순환하며, 각 지도는 **현재 위치 주변을 도로가 보일 만큼 확대**하고 예상 경로·지나온 자취를 함께 보여줍니다.
- **전체화면 강제·안내창을 없앴습니다.** 요청대로 앱이 스스로 전체화면을 켜지 않습니다(브라우저 전체화면으로 쓰시면 됩니다). 하원 종료 버튼은 그대로입니다.
- **일정 시간 안 움직이면 마우스 커서가 사라집니다**(하원 운행·업무 대시보드 모두). 움직이면 다시 나타납니다.

## v0.191.0 - 2026-08-24

- **하원 지도를 좌우 7:3으로 나눴습니다.** 요청대로 **왼쪽은 전체 지도**(모든 노선), **오른쪽은 노선을 하나씩 순환하며 가까이** 보여줍니다(7초마다 전환). 지금은 27호만 신호가 있어 오른쪽에 27호가 뜹니다.
- **전체 지도(왼쪽)의 차량은 번호가 든 작은 점**으로 표시하고, **지도가 넓게 축소될수록 점도 작아집니다**(요청: "작아지는만큼 크기 작아지게").
- **오른쪽(순환) 화면은 위에서 내려다본 셔틀 밴**으로, 진행 방향·예상 경로·지나온 자취·정류장을 그 노선에 딱 맞게 확대해 보여줍니다.

## v0.190.0 - 2026-08-24

- **셔틀 밴을 "위에서 본" 모양으로 바꾸고 진행 방향을 표시합니다.** 요청: "가는 방향도 알수 있도록 (...) 옆에서보는 모양말고 위에서 본 모양으로". 위에서 본 밴이 실제 가는 방향으로 회전하고, 번호는 항상 똑바로 읽히게 올립니다.
- **밴 색을 노선 색으로 통일했습니다.** 요청: "셔틀색도 각 번호 색으로 (...) 각 번호는 지역으로 나눠져 있도록". 노선 목록(대체로 지역 묶음) 순서대로 색상환을 고르게 나눠, 밴·예상경로·지나온 자취가 모두 같은 노선 색으로 이어집니다.
- **예상 경로도 실선으로 함께 그립니다.** 요청: "예상노선과, 지나온 노선 실선으로". 예상(계획) 경로는 연한 실선, 지나온 길은 진한 실선. 전체 노선이 보이도록 예상 경로까지 포함해 지도를 자동 축소합니다.

## v0.189.0 - 2026-08-24

- **지도 위 차량을 노란 셔틀 밴 그림으로 바꿨습니다.** 요청: "노란색셔틀밴 그림으로 표시하고 그안에 숫자만 (...) 실제 셔틀이 가는느낌". "27호" 글자 대신 노란 밴 안에 번호(27)만 넣어, 실제 셔틀이 노선을 따라가는 느낌으로 표시합니다(노선 색은 밴 테두리로 구분).

## v0.188.0 - 2026-08-24

- **업무 대시보드 지도에 전체 노선의 실제 이동 자취를 각 노선 색 실선으로 그립니다.** 요청: "전체노선 다 실선으로 (...) GIA에서부터 실선으로 이어줘 각 노선 색으로". 계획 경로는 연한 점선(참고), 오늘 실제 지나온 길은 노선 색 실선입니다. 지금은 27호만 신호가 있어 27호만 실선으로 이어집니다. 지도는 자취를 포함해 노선 전체가 보이게 맞춰집니다.

## v0.187.0 - 2026-08-24

- **정류장은 진짜 근처일 때만 인식합니다.** 요청: "신호대기랑 정류장 정차 헷갈리지 않게 정류장 근처일때만". 정류장 도착 반경을 120m→80m로 좁혀, GPS가 흔들리거나 그냥 서 있을 때 엉뚱한 정류장으로 잡히지 않게 했습니다.
- **GPS 테스트 지도를 노선 트래킹용으로 새로 그렸습니다.** 요청: "지도 크게 (...) GIA에서부터 출발하면서 지나는 경로 선으로 트래킹 (...) 차량을 가운데 두지말고 노선이 보이도록".
  - **GIA(학교) → 지나온 길을 파란 실선**으로 그립니다. 계획 경로는 연한 회색 점선, 정류장은 점으로 함께 표시.
  - 지도는 **차량을 따라다니지 않고**, 노선 전체가 한눈에 들어오게 **자동으로 확대**됩니다(직접 확대·이동해도 되돌리지 않음. "노선 전체 다시 맞추기" 버튼으로 재정렬).
  - 지금 신호를 보내는 노선을 자동으로 골라 보여줍니다(우선 27호 테스트).

## v0.186.0 - 2026-08-24

- **기사님 위치가 안 들어오던 원인(307)을 고쳤습니다.** 27호차 앱 로그의 `Upload response 307`은 위치 전송 주소(/api/shuttle/track)가 로그인 검사에 걸려 /login으로 튕기던 것이었습니다. 이 주소를 로그인 없이 받도록 열어, 이제 앱이 보낸 위치가 정상 저장됩니다.
- **셔틀 기기 관리에 '앱 신호 진단'을 넣었습니다.** 요청: "앱에 로그기록있던데 이거 가져오게 못하나". 앱이 서버로 보낸 마지막 요청을 그대로 보여줍니다 — "위치 저장됨 ✓" / "지금은 하원 시간대가 아니라 미저장" / "좌표 없음(권한 확인)" / "앱 신호 없음(설치·기기ID 확인)".
- **[24h 테스트] 토글**을 추가했습니다. 하원 시간대(평일 15:30~18:30)가 아니어도 지금 바로 위치가 저장되는지 확인할 수 있고, 테스트 후 끄면 평소대로 돌아갑니다.

## v0.185.0 - 2026-08-24

- **내 휴대폰 GPS 테스트 화면이 생겼습니다.** 요청: "기사님께 드리기전에 내 핸드폰으로 등록 테스트 (...) 24시간 추적 (...) 강경원 이름으로 테스트 링크 (...) 히스토리처럼 남겨줘".
  - 셔틀 메뉴 → **GPS 테스트(내 폰)** 에서 설치 QR·링크, **실시간 위치**, **오늘 이동 경로·히스토리**를 한 화면에서 봅니다. 다음 날 경로가 정확한지도 이 화면에서 확인합니다.
  - 이 기기(강경원)는 **24시간 기록(always_on)** 이라 하원 시간대가 아니어도 출퇴근 위치가 그대로 기록됩니다. 실제 노선과 분리된 숨김 기기라 정규 운영 화면엔 영향이 없습니다.
  - 4시 하원 **업무 대시보드 지도에도 내 위치가 초록 📍로 실시간** 표시됩니다.
- **기준점을 GIA마이크로랩(서울 강남구 논현로131길 45)으로 고정**했습니다. 도착·출발 감지가 모두 이 좌표 반경으로 계산됩니다(함께 드리는 세팅 SQL에서 재지오코딩).
- (세팅 SQL 갱신) **여름캠프2 잔여 노선 비활성화 + 기준점 재설정**을 정규학기 세팅 SQL에 추가했습니다.

## v0.184.0 - 2026-08-24

- **출결 영문 이름 대조를 크게 개선했습니다.** 요청: "seojun lee / benecia (...) 일치하는 이름들 찾아서 매칭".
  - 이제 **이름(first name)만 적어도** 명부의 영문명과 대조됩니다 — "Benecia will be absent" → 김사랑, "seojun in G2A is not here" → 이서준. 같은 이름이 여럿이면 문장의 학년(G2A 등)으로 좁힙니다.
  - "is not here / not coming" 같은 표현도 결석으로 인식합니다.
- **답글은 긁어오지 않습니다.** 요청: "he will be late의 경우 (...) 답변인데 이걸 그냥 긁어왔더라고". he/she/they로 시작하는 **대명사 답글**(이름 없는 답변)은 새 결석·지각으로 집계하지 않습니다.
- **업무 대시보드 출결을 결석/지각으로 반 나눴습니다.** 요청대로 **결석은 빨강, 지각은 주황**으로 좌우로 갈라 한눈에 구분되고, **하원 픽업은 파란색으로 맨 앞**에 놓았습니다(조퇴는 지각 쪽에 보라색으로 함께).

## v0.183.0 - 2026-08-24

- **하원 GPS: 정류장별 도착·하차가 운영 대시보드에 뜹니다.** 요청: "정류장에 도착했다면 어디정류장에 도착했는지 (...) 누가 내리는지 (...) 출발했다면 어느정류장으로 가고있는지".
  - 기사님 휴대폰 GPS가 정류장 반경(120m)에 들어오면 그 정류장을 **도착으로 자동 기록**합니다(하루 한 정류장 한 번). 정류장 좌표는 GPS 학습값이 있으면 그것, 없으면 지오코딩 좌표를 씁니다.
  - 4시 하원 운영화면 아래에 **🛰️ 운행 상황(GPS)** 칸이 새로 생겨, 운행 중인 차마다 **방금 닿은 정류장·그 정류장 하차 명단·다음 정류장·진행(몇/몇 정류장)** 을 한 줄로 보여줍니다.
  - 정류장 좌표는 매일 운행할수록 정확해집니다(기존 GPS 정차 학습 크론이 관측값 평균을 매번 다시 계산). 좌표가 아직 없는 정류장도 며칠 운행하면 자동으로 채워집니다.

## v0.182.0 - 2026-08-24

- **교사 첫 화면에 "우리 반 현황" 대시보드가 생겼습니다.** 요청: "교사 권한으로 로그인했을때 (...) 자기반 아이들 어머님께서 문의하신 사항을 띄울 수 있게 (...) 픽업의 경우 시간을 명시한경우 (...) 누가 몇시에 픽업인지".
  - 담임 선생님이 로그인하면 위클리 리포트가 아니라 **우리 반 현황**이 먼저 뜹니다(과목 전담 교사는 예전대로 위클리 리포트).
  - **오늘 픽업**: 우리 반 학생 중 오늘 픽업 요청이 온 아이를, **시각이 적혀 있으면 그 시각과 함께** 큼직하게 보여줍니다("누가 몇 시에").
  - **우리 반 문의**: 담임 이메일로 걸러 우리 반 학부모 문의만, 미답변·급한 것을 위로 보여줍니다. 문의를 누르면 원문을 봅니다.
  - 담임 이메일로만 좁혀 다른 반 문의는 보이지 않습니다.
- **데모 계정으로 미리 볼 수 있습니다.** 오리엔테이션 데모 교사(gia-demo)에 예시 픽업 2건·문의 3건을 넣었습니다. 데모 문의는 `is_demo`로 격리되어 실제 행정실 문의 목록·운영 대시보드에는 전혀 섞이지 않습니다.

## v0.181.0 - 2026-08-24

- **모바일 도착체크에 GPS 상태가 보입니다.** 요청: "gps로 근처 도착하면 색이 바뀌던가하고 출발하면 또 색이 바뀌게 해서 모바일로 제대로 돌아가는지 체크".
  - 기사님 휴대폰 GPS가 살아있으면 카드가 **초록(운행중)**, 학교 근처에 도착하면 **주황(도착함·GPS 감지)**, 멀어지면 **회색(출발함)** 으로 자동으로 바뀝니다. 자동으로 안 바뀌어도 눌러서 직접 표시할 수 있습니다(기존 방식 유지).
  - 카드 맨 아래 **📍 표시**로 GPS가 방금 신호를 보냈는지(초록) 끊겼는지(회색)를 눈으로 확인합니다. 기기 미설정이면 "GPS 미설정".
  - 탑승 명단·픽업·결석 표시는 그대로 유지됩니다.

- (함께 드리는 세팅 자료) **2026_정규1학기_셔틀세팅.sql** — 안내보드·도착체크를 정규학기로 전환하고, 48개 노선에 기사님 GPS 기기·설정링크를 발급합니다. **2026_기사님_GPS설정링크.xlsx** — 노선별로 기사님께 보낼 /s/링크 목록.

## v0.180.0 - 2026-08-24

- **운영 대시보드에 결석·지각·픽업이 제대로 집계됩니다.** 요청: "문의에서도 결석이나, 픽업 등 다 올라오는데 결석은 아직도 1로 떠 픽업은 심지어 0이야 제대로 반영해줘".
  - 원인: 결석 칸은 출결기록·구글챗만, 픽업 칸은 하원 체크표만 읽고 있었습니다. 토들로 들어온 학부모 연락은 문의(pickup_requests)에만 쌓여 두 숫자 어디에도 반영되지 않았습니다.
  - 이제 문의로 들어온 연락도 같은 규칙(키워드 분류 + 명부 대조)으로 함께 집계합니다. AI가 이미 명부와 이어둔 학생이 있으면 그걸 최우선으로 쓰고, 없으면 본문을 명부와 대조합니다. "내일·금요일 결석"처럼 날짜가 적힌 건 그날에 반영되고, 오늘 것만 대시보드에 뜹니다.
  - 하원 체크표 픽업과 문의 픽업이 겹치면 한 번만 셉니다(같은 이름 중복 제거).

- **영어로 온 이름을 더 정확히 한글로 바꿉니다.** 요청: "아직도 soo j로 떠 지수라고 떠야하는데".
  - 'Soo J'처럼 잘려 들어온 이름이 명부의 여러 명(예: 'Soo Ji'=지수, 'Soo Jin'=수진)과 겹칠 때, '가장 가깝게 완성되는' 한 명을 골라 확정합니다('Soo J'는 지수). 단, 후보가 크게 벌어지는 한 단어(예: 'Min')만 온 경우는 잘못 고르지 않도록 그대로 둡니다.

## v0.179.0 - 2026-08-24

- **이름을 성까지 표시합니다.** 요청: "꼭 이름만 뜨지않고 성까지 (...) 같은이름을 가진아이들도 있어서 성을 꼭 같이".
  - 하원 픽업 목록이 탑승표에 적힌 이름(성이 빠졌을 수 있음) 대신 **학생 번호로 명부의 전체 이름(성+이름)** 을 씁니다. 학부모 문의·출결은 이미 명부 전체 이름을 쓰고 있었습니다.
  - 혹시 아직 성이 안 나오는 화면이 있으면 알려주세요 - 그 자리도 같은 방식으로 명부와 잇겠습니다.

- **시간표: 지금 수업하는 선생님만 표시.** 요청대로 담임 폴백을 빼고, 지금 교시의 담당 선생님만 과목 아래에 적습니다.
- **시간표 아래 "지금 수업 없는 선생님" 로스터를 없앴습니다**(요청: 시간표와 겹쳐 보였습니다).



- **운영 대시보드 배치 조정.** 출결·픽업·오늘 업무 칸을 조금 줄이고, 위쪽(교실·학부모 문의)을 키웠습니다. 학부모 문의가 여섯 개까지 한 장에 들어갑니다.

- **수업 담당 선생님을 시간표에 표시.** 요청: "수업하시는 선생님들은 각교실 과목아래에 작게 누구수업인지 적어주고, 담임이면 담임이름".
  - 각 반 과목 아래에 **담당 선생님**을 작게 적습니다. 수업이 없으면 **담임** 이름을 보여줍니다.
  - **지금 수업 없는 선생님**은 시간표 아래에 로스터로 모아 보여줍니다(요청). 스페셜티(전담) 선생님도 시간표에 나오면 함께 잡힙니다 - 지금 손이 비는 분을 한눈에 봅니다.

- **직원 답글이 해결인지 진행중인지 판단합니다.** 요청: "우리 직원이 쓴글도 분석 (...) 문의가 해결되었는지 안되었는지 표시"(수집기 v2.3.0).
  - 지금까지는 직원이 답글만 달면 무조건 처리 완료로 넘겼습니다. 그런데 "확인 후 연락드리겠습니다" 같은 답은 아직 안 끝난 것입니다.
  - 이제 답글 내용을 보고 **해결이면 목록에서 빼고, 진행중이면 [답변중]** 으로 남겨 계속 눈에 띄게 합니다. 판단이 애매하면 답이 달렸으니 해결로 봅니다.



- **업무 대시보드 터치 조작을 넓혔습니다.**
  - **학부모 문의를 짧게 누르면** 그 토들 원문이 작은 창으로 뜹니다. 토들에 다시 접속하지 않아도 내용을 바로 봅니다.
  - **길게(0.6초) 누르면** 목록에서 없앱니다(처리 완료). 지우지 않고 기록으로 남겨, 잘못 눌러도 [기록]에서 되살릴 수 있습니다. 로그인 없는 대시보드라 되돌릴 수 없는 삭제는 막았습니다.
  - **각 반을 누르면 일주일 시간표**가 팝업으로 뜹니다. 교시 × 요일(월~금) 표에 과목·교실이 채워집니다.
  - 토큰 인증 API 두 개를 새로 뒀습니다(문의 처리 / 주간 시간표). 대시보드 링크가 살아 있을 때만 동작합니다.



- **학부모 문의가 더 이상 잘리지 않습니다.** 한 장에 담는 개수를 고정값으로 정하니 이름·요약 길이에 따라 마지막 줄("백서아" 아래)이 잘렸습니다. 이제 **칸 높이를 실제로 재서** 딱 들어맞는 개수를 스스로 찾습니다. 넘치면 한 건씩 줄이고, 나머지는 다음 장으로 넘깁니다. 글자도 조금 키웠습니다.

- **시간표: 4교시 등 과목 글자를 키우고, 학년 높이를 맞췄습니다.** 요청: "4교시 좀더 글자 키워주고, 2,3,4,5 학년 위아래 동일하게 맞춰줘". 반 개수가 학년마다 달라도 각 학년 행이 높이를 똑같이 나눠 갖고, 그만큼 생긴 공간으로 과목명을 크게 띄웁니다.

- **업무 대시보드를 터치로 조작할 수 있습니다.** 요청: "업무 대시보드 터치가능하게".
  - 학부모 문의를 누르면 **토들 원문으로 바로** 갑니다.
  - 화면 아래 점을 누르면 그 장으로 넘어갑니다. 문의를 한 번 누르면 자동 넘김이 20초간 멈춰, 읽는 중에 장이 넘어가지 않습니다.
  - 터치스크린에서 탭이 바로 먹도록 처리했습니다(더블탭 확대 지연 제거).



- **학부모 문의 이름을 한글로 바꿉니다.** 요청: "영어이름으로 문의를 올렸다면 학생명부와 대조후에 한글이름으로 올려줘"
  - `Diane & Sunwoo Lim` → **임다이앤 & 임선우** 처럼, 형제방 이름도 각각 대조해 한글로 이어 붙입니다.
  - `Soo J` 처럼 잘려 온 이름은, 영문명이 그 값으로 시작하는 학생이 **딱 한 명일 때만** 바꿉니다(`Soo J` → 김수진). 여럿이면 누구인지 알 수 없으니 그대로 둡니다 - 엉뚱한 학생으로 바꾸는 게 영어로 두는 것보다 나쁩니다.
  - 이미 한글이거나 명부에서 못 찾으면 원래 값을 그대로 둡니다. 업무 화면과 운영 대시보드 양쪽에 적용됩니다. 실제 이름 형태로 검증했습니다.

- **대시보드 출결에 영어 이름도 반영됩니다.** 앞서 구글챗 출결을 붙였는데, 명부 대조에 **영문명을 빼먹어** 영어로 온 출결(예: "Diane Lim 결석")이 대시보드에서만 안 잡혔습니다. 업무 화면 출결내역과 똑같이 영문명까지 넣어 대조합니다.



- **운영 대시보드 학부모 문의 개선.**
  - **최근 문의가 맨 위로** 올라옵니다. 예전에는 급한 것 우선이라 최신 건이 아래로 밀리기도 했는데, 급한 건은 빨간 테두리로 이미 구분되므로 순서는 순수 최신순으로 뒀습니다.
  - **새로 올라온 문의는 강조**합니다. 3분 이내에 들어온 것은 배경을 밝히고 파란 테두리에 **NEW** 배지를 붙입니다. 3분이 지나면 강조가 저절로 풀립니다 - 계속 강조돼 있으면 어느 게 진짜 새 것인지 알 수 없습니다.
  - 한 장에 담는 건수를 줄여(6건) 두 줄 요약도 잘리지 않게 했습니다. 넘치면 8초마다 다음 장으로 넘어갑니다(스크롤 없음).

- **출결·지각이 대시보드에 안 뜨던 문제를 고쳤습니다.**
  - 원인: 대시보드는 교사가 출결 화면에서 **직접 입력한 것(attendance_records)만** 읽고 있었습니다. 구글챗 출결알림은 그 표에 들어가지 않아 화면에 뜨지 않았습니다.
  - 이제 구글챗 출결알림을 **업무 화면의 출결내역과 같은 규칙으로** 파싱해 함께 올립니다(결석·지각·조퇴). 오늘 것만, 이 부서 학생과 대조되는 것만 올리고, 교사가 이미 직접 입력한 학생은 그 값을 존중합니다.
  - 대시보드는 15초마다 갱신되므로, 구글챗에 알림이 오면 늦어도 15초 안에 반영됩니다.



- **소리 알림을 뺐습니다**(요청). 관련된 코드와 [🔔 소리 켜기] 버튼을 전부 걷어냈습니다. 화면이 죽었던 원인이 여기였고, 브라우저 규칙 때문에 사람이 한 번 눌러줘야 하는 제약도 있어 공용 모니터에는 잘 맞지 않았습니다.
- 나머지는 그대로입니다 - 새 문의는 15초 안에 화면에 뜨고, 급한 것이 위로 올라오며, 여러 장이면 8초마다 넘어갑니다.



- **긴급 수정: 대시보드가 React 오류(#310)로 죽던 문제.** 제 잘못입니다.
  - 소리 알림을 넣으면서 관련 코드를 **"데이터가 없으면 여기서 멈춤" 아래에** 뒀습니다. 그러면 데이터를 기다리는 첫 순간과 데이터가 온 뒤의 상태 개수가 달라져 리액트가 화면을 통째로 포기합니다.
  - 소리 관련 부분을 전부 위로 올렸습니다. 화면은 그대로입니다.
  - 눈으로 다시 보는 대신 **기계로 확인했습니다** - 화면 파일 8개에서 "멈춤 아래에 있는 상태 선언"을 훑어 하나도 없음을 확인했고, 린트로도 확인했습니다. 같은 실수를 눈으로 잡으려다 놓친 것이라 같은 방법을 반복하지 않았습니다.



- **긴급 수정: 업무 대시보드 오류.** 제 잘못입니다.
  - 새 기능을 올리면 **코드가 먼저 배포되고 마이그레이션은 조금 뒤에** 걸립니다. 그 사이에 아직 없는 칸(`merged_sources`, `answered_via` 등)을 콕 집어 달라고 하면 **조회 자체가 실패해 화면이 통째로 깨집니다.**
  - 칸을 하나씩 적는 대신 전부 가져오도록 바꿨습니다. 있는 것만 돌아오고, 없는 칸은 비어 있는 채로 화면은 그대로 뜹니다.
  - 학부모 문의 목록, 운영 대시보드, 중복 판정 세 곳을 모두 고쳤습니다.
  - 새 칸을 쓸 때는 앞으로도 이 방식을 씁니다. 배포와 마이그레이션 사이의 틈은 늘 생기고, 그때 화면이 죽으면 안 됩니다.



- **[관리자 → 데이터베이스 점검]** 화면을 만들었습니다. 요청: "마이그레이션 자동으로 해놨긴했는데 제대로 반영되는지 안되는지 편하게 확인할 수 있는 방법이 없을까?"
  - 지금까지는 **화면이 깨져야** 알 수 있었고, 그것도 "어느 기능이 안 되는지"만 알 뿐 왜 안 되는지는 짐작해야 했습니다.
  - 이제 기능별로 초록/빨강이 나옵니다. 빨간 줄에는 **없는 칸 이름 · 그래서 무엇이 안 되는지 · 실행해야 할 마이그레이션 파일**이 함께 적힙니다.
  - 확인 방식은 `information_schema`를 뒤지는 대신 **실제로 그 칸을 읽어봅니다.** 권한(RLS)까지 함께 확인되기 때문입니다 - 칸이 있어도 읽을 수 없으면 기능은 똑같이 안 됩니다. "분명히 있는데 왜 안 되지?"를 없애려면 앱과 같은 방식으로 확인해야 합니다.
  - 들어가면 바로 한 번 확인합니다. 버튼을 또 눌러야 결과가 나오면 확인하러 온 보람이 없습니다.
  - 앞으로 새 마이그레이션을 만들 때 `schemaChecks.ts`에 한 줄만 추가하면 그 기능도 자동으로 점검됩니다.



- **운영 대시보드 학부모 문의를 크게, 그리고 넘겨가며 보여줍니다.**
  - 이름을 크게 올리고 **그 아래 요약 한 줄**을 둡니다. 글자 크기를 전반적으로 키웠습니다.
  - 한 화면에 안 들어가면 **8초마다 다음 장으로 넘겼다가 마지막에서 처음으로 돌아옵니다.** 공용 모니터라 스크롤을 내릴 사람이 없어서, 지나가며 몇 초만 봐도 결국 전부 보게 했습니다. 아래 점으로 몇 번째 장인지 표시합니다.

- **문의를 체크로 넘길 수 있습니다.** 업무 화면 문의 목록에 체크박스가 생겼습니다.
  - 체크하면 대시보드와 문의 목록에서 빠집니다. **지우지 않고 기록으로 남깁니다** - "그때 그 학부모가 뭐라고 하셨더라"를 나중에 찾을 수 있어야 하고, 같은 문의가 반복되면 그것 자체가 고쳐야 할 신호입니다.
  - **[기록]** 을 누르면 처리한 것까지 함께 보이고, **검색창**으로 학생 이름·내용을 찾을 수 있습니다. 검색이 빠르도록 색인도 걸었습니다.

- **다른 선생님이 답글을 달면 자동으로 처리됩니다**(수집기 v2.2.0).
  - 수집기가 각 방의 마지막 글이 선생님 것인지 확인해 알려줍니다. 이미 답한 건이 인박스에 남아 있으면 또 답하게 되고, 학부모는 같은 얘기를 두 번 듣게 됩니다.
  - 답글로 처리된 건은 **이름 뒤에 초록 체크(✓)** 가 붙습니다. 직원이 직접 체크한 것과 구분됩니다.
  - 답글보다 나중에 들어온 문의는 건드리지 않습니다 - 아직 답을 못 받은 것입니다.



- **토들과 구글챗으로 같은 일이 두 번 들어오던 것을 하나로 묶습니다.** 요청: "문장이 완전히 똑같지 않을거라서 잘 분석해줘"
  - 쓴 사람이 다르니 문장이 전혀 다릅니다. 학부모는 `"선생님 안녕하세요. 유겸이가 어제부터 장염이라 오늘 결석하겠습니다"`, 담임은 `"@노유겸 결석 - 장염"`. 글자로 겹치는 건 몇 자뿐입니다.
  - 그래서 세 단계로 좁힙니다.
    1. **같은 학생·같은 날짜·같은 종류**인 것만 후보로 봅니다. 학생을 특정하지 못한 건은 아예 묶지 않습니다.
    2. **픽업은 후보가 있으면 같은 건**입니다. 같은 아이를 같은 날 두 번 데려갈 일은 없습니다.
    3. **문의는 글자가 겹치는 정도**를 보고, 애매한 구간(0.2~0.6)만 AI에게 한 번 더 묻습니다.
  - 겹침을 잴 때 **짧은 쪽을 기준**으로 삼습니다. 긴 인사말이 붙었다고 다른 일이 되는 건 아니어서, 흔히 쓰는 방식(자카드)으로는 같은 건을 놓칩니다.
  - **뜻이 어긋나면 무조건 따로 둡니다** - "결석"과 "지각"은 짧고 대부분 겹쳐서 닮은 정도만으로는 못 가립니다.
  - 애매하면 묶지 않습니다. **잘못 묶으면 한쪽 내용이 통째로 사라집니다.** 두 줄로 남는 편이 낫습니다.
  - 묶인 건에는 **`+구글챗` 같은 표시**가 붙습니다. "구글챗에도 올렸는데 왜 여기 없지?" 하고 찾게 되는 것을 막습니다.
  - 실제 문장 쌍 6가지로 판정을 확인했습니다.



- **GIA 로고**가 운영 대시보드 맨 위 가운데에 들어갔습니다. 왼쪽 글자 길이에 따라 로고가 흔들리지 않도록 세 칸 격자로 두어 늘 정확히 가운데에 옵니다.

- **점심시간을 눈으로 봅니다.** 요청: "숫자로 보기보단 시각적으로"
  - 점심시간이 되면 시간표 자리에 **큰 고리**가 뜨고 시간이 갈수록 줄어듭니다. 이 시간에 반별 과목은 볼 것이 없고, 정작 궁금한 것은 "얼마나 남았나" 하나입니다.
  - **끝나갈수록 색이 바뀝니다** - 넉넉할 때 초록, 10분 남으면 노랑, 3분 남으면 빨강. 정리할 때가 됐다는 것을 색만 보고도 압니다.
  - 숫자는 고리 안에 작게만 뒀습니다. 숫자를 크게 하면 결국 숫자를 읽게 되고, 그러면 굳이 이렇게 띄울 이유가 없습니다.
  - 점심 여부는 **시간표에 등록된 교시 이름**으로 봅니다(점심·중식·Lunch). 시각을 코드에 박아두면 학기마다 시간이 바뀔 때 아무도 고칠 생각을 못 합니다.

- **새 문의가 오면 소리로 알립니다.** 짧은 "딩-동" 음을 브라우저가 직접 냅니다.
  - 브라우저는 사람이 한 번이라도 누르기 전에는 소리를 못 내게 막습니다. 그래서 화면에 **[🔔 소리 켜기]** 버튼을 뒀습니다. 지나가는 분이 한 번만 눌러주면 그 뒤로는 계속 울리고, 버튼은 사라집니다.
  - 화면을 처음 열 때는 울리지 않습니다 - 켜자마자 밀린 문의로 울리면 놀라기만 합니다.



- **운영 대시보드 화면 배치를 다시 짰습니다.** 공용 모니터에 맞춘 것들입니다.
  - **위쪽은 절반씩** - 왼쪽 교실 상황, 오른쪽 학부모 문의. 하루 중 가장 자주 보는 둘입니다. 문의는 한 번에 16건까지 올라옵니다.
  - **출결 · 픽업 · 오늘 업무는 아래로** 내려 나란히 놓았습니다.
  - **어디에서도 스크롤되지 않습니다.** 요청: "공용모니터에 연결한거라 스크롤이 되면 내릴사람이 없어". 대신 각 칸에서 보여줄 개수를 미리 줄여 애초에 넘치지 않게 했습니다 - 아래에 뭔가 더 있는데 아무도 못 보는 것보다, 중요한 것부터 화면 안에 들어오게 하는 편이 낫습니다. 순서는 이미 급한 것이 위로 오게 되어 있습니다.
  - **반 칸을 키웠습니다.** 칸 너비를 넓히고 글자를 키웠으며, 긴 과목명은 자르지 않고 **두 줄까지 내려 씁니다.** `Comput…`으로 잘리면 무슨 수업인지 알 수 없습니다.
  - **오늘 업무는 오늘 것만.** 마감이 지난 것이 위에 쌓여 오늘 할 일을 가리던 것을 뺐습니다.



- **운영 대시보드가 새 버전을 스스로 알아채고 새로고침합니다.** 요청: "공용 모니터라서 내가 가서 새로고침 누르는것 보다 자체적으로 새로고침이 되었으면"
  - 이 화면은 며칠씩 켜둔 채로 둡니다. 그동안 새 버전을 배포해도 브라우저는 처음 받아둔 코드를 계속 써서, **고친 것이 화면에 하나도 반영되지 않았습니다.** 지금까지는 누군가 가서 F5를 눌러야 했습니다.
  - 이제 서버가 알려주는 버전과 지금 돌고 있는 코드의 버전을 견주어 보고, 다르면 3초 뒤 알아서 새로고침합니다.
  - **새 버전이 올라왔을 때만 딱 한 번입니다.** 시간이 됐다고 새로고침하지 않습니다 - 보고 있는 중에 화면이 깜빡이면 그것대로 방해가 됩니다.
  - 새로고침한 뒤에도 버전이 그대로면(배포 캐시 등) 무한히 반복될 수 있어, 한 번 한 뒤에는 10분 동안 다시 하지 않습니다. 새로고침이 계속 도는 화면은 아무것도 못 읽습니다.
  - **이번 배포가 마지막 수동 새로고침입니다.** 지금 켜져 있는 화면은 예전 코드라 이 기능이 없으니 한 번만 F5를 눌러주시고, 그 뒤로는 알아서 따라옵니다.



- **각 반의 위치가 항상 보입니다.** 반 이름 바로 옆에 붙습니다(요청: "교실명 옆에 나오게 해도 되 학년과 반이 우선이야"). 교실에 있으면 교실 이름이, 나가면 그 자리 글자만 **[교실 밖]** 으로 바뀝니다. 위치는 늘 같은 자리에 있어야 눈이 그 자리를 찾습니다 - 있다 없다 하면 매번 다시 훑게 됩니다.

- **토들 수집기가 살아 있는지 운영 대시보드에서 보입니다.** 문의 칸 오른쪽에 `수집 10:58` 처럼 마지막 신호 시각이 뜨고, 5분 넘게 소식이 없으면 칸 전체가 빨갛게 **[토들 수집기가 멈춰 있습니다]** 로 바뀝니다.
  - 문의가 안 뜨는 것이 "문의가 없어서"인지 "수집기가 멈춰서"인지는 전혀 다른 얘기인데 화면에는 똑같이 비어 보입니다. 조용히 아무것도 안 하면서 정상인 척하는 것이 가장 나쁜 실패라, 눈에 띄게 했습니다.
  - 로그인이 풀린 경우에는 무엇을 해야 하는지까지 적습니다.



- **업무 화면: 학부모 문의사항과 출결내역의 자리를 바꿨습니다.** 문의사항이 위쪽 넓은 칸의 **기본 탭**이 되어 목록이 통째로 펼쳐집니다. 하루 열 건 넘게 오는 것을 세 줄로 보는 건 사실상 안 보는 것과 같았습니다. 출결내역·출결알림은 옆 탭으로 그대로 있습니다 - 없앤 것이 아니라 자리를 바꾼 것입니다.

- **운영 대시보드**
  - **시간표를 줄이지 않습니다.** 반이 늘어나도 칸을 좁히지 않고, 시간표 칸이 남는 공간을 크게 가져갑니다.
  - **쉬는 시간**이면 `쉬는 시간 · 10:40에 3교시 시작` 이라고 뜨고, 각 반에는 **다음 교시 과목을 미리** 보여줍니다. 지금 수업으로 잘못 읽지 않도록 색을 죽이고 [다음] 표시를 붙였습니다. 수업이 다 끝난 뒤와도 구분해 적습니다.
  - **교시가 바뀌는 순간 화면이 같이 바뀝니다.** 평소 15초마다 받아오고, 더해서 지금 교시가 끝나는 시각에 맞춰 한 번 더 받아옵니다. 종이 쳤는데 화면은 지난 교시를 보여주면 보는 사람이 화면을 안 믿게 됩니다.



- **운영 대시보드에 학부모 문의사항**을 띄웁니다. 아직 답하지 않은 것만, 급한 것부터 올립니다. 처리된 것까지 섞이면 멀리서 훑어보는 의미가 없어집니다. 학생 이름·분류·요약 한 줄·받은 시각이 한 줄에 들어갑니다.

- **지금 어느 반이 교실을 비웠는지 보입니다.** 요청: "위치도 알 수 있게 표시해줘 (...) 일단 특수교실들은 장소를 바로 표시하지말고, 그냥 교실이 아닌곳에 있다는 표시만"
  - 체육·컴퓨터·음악·미술 시간이면 반 칸이 **주황색으로 바뀌고 [교실 밖]** 이 붙습니다. 어느 방인지는 아직 적지 않습니다.
  - 어느 방인지는 안에서 계산해 두었습니다(체육관·컴퓨터실·음악실·미술실). 나중에 켜기만 하면 됩니다.
  - 과목 표기가 학교마다 달라 넉넉히 잡았습니다(PE / P.E. / Physical Education / 체육, Computer Science / Coding / CS / 코딩 등). **못 알아본 과목은 교실에 있는 것으로 봅니다** - 확실하지 않은데 "교실 밖"이라고 하면 찾으러 갔다가 헛걸음하고, 그러면 이 표시를 아무도 안 믿게 됩니다.
  - 22가지 과목명으로 판정을 확인했습니다. `Language Arts`(영문학)를 미술로 잘못 보내지 않는 것도 함께 확인했습니다.



- **수집기가 목록은 제대로 읽습니다**(안 읽은 방 14개). 이제 "방은 있는데 보낼 것이 0건"인 이유를 화면에 적습니다(v2.1.1).
  - 실제 데이터를 확인해보니 안 읽은 방 14개 안에 **학부모 메시지가 7건** 있었습니다. 그런데 0건으로 나왔습니다. 보내기 직전 어딘가에서 걸러지고 있다는 뜻입니다.
  - 이제 `거른 것: 직원 15, 시스템 4` 처럼 **무엇을 왜 걸렀는지** 적습니다. "0건"만 보여주면 정상인지 막힌 건지 알 수가 없어서, 이걸 알아내려고 여러 번 헛짚었습니다.
  - **[보낸 기록 지우고 다시 보내기]** 를 넣었습니다. 수집기는 같은 메시지를 두 번 보내지 않으려고 보낸 것을 기억해두는데, 서버 쪽에서 지웠거나 시험 중이면 그 기억 때문에 막힌 것처럼 보입니다.



- **확장을 새로고침해도 예전 코드가 계속 돌던 문제를 고쳤습니다**(수집기 v2.1.0). "고쳤는데 그대로다"의 진짜 원인이었습니다.
  - 크롬은 확장을 새로고침하면 새 코드를 열려 있는 탭에 다시 넣어줍니다. 그런데 그 페이지에는 이전 스크립트가 이미 자리를 잡고 있어서, **"이미 설치됨" 표시에 걸려 새 코드가 곧장 빠져나갔습니다.** 확장 카드에는 새 버전이 찍히는데 동작은 하나도 안 바뀌는, 대단히 헷갈리는 상태였습니다. 제가 "F5 필요 없다"고 안내한 것도 틀렸습니다.
  - 이제 **버전이 다르면 낡은 것을 걷어내고 새로 답니다.** 확장이 새로 올라오면 토들 탭도 알아서 새로고침합니다. 사람이 F5를 기억할 필요가 없습니다.
  - 상태판에 **페이지 쪽 코드의 버전**(`p2.1.0`)을 함께 적습니다. 확장 카드의 버전과 다르면 낡은 코드가 아직 도는 것이고, 그걸 화면에서 바로 알 수 있어야 합니다.
  - 팝업 제목 옆에도 버전이 뜹니다.



- **수집기 팝업에 버전을 표시합니다**(v2.0.2). 이게 없어서 "고쳤는데 그대로다"를 여러 번 주고받았습니다.
  - 업데이트 스크립트가 파일을 내려받아도 크롬 확장을 새로고침하지 않으면 예전 코드가 계속 돕니다. 화면만 봐서는 어느 쪽인지 알 수가 없었습니다. 이제 팝업 제목 옆에 지금 돌고 있는 버전이 그대로 뜹니다.
  - 팝업의 주소 예시도 실제 주소(gia-ops.vercel.app)로 고쳤습니다. 예전 예시가 남아 있어 혼동을 줬습니다.



- **"안 읽은 방 0개"가 무슨 뜻인지 구분해 보여줍니다**(수집기 v2.0.1).
  - 토들의 '안 읽음'은 **계정마다 따로** 셉니다. 그래서 0개에는 서로 다른 상황이 섞여 있는데 화면에는 똑같이 보였습니다.
    - **방이 아예 안 보임** - 그 계정이 학부모 채팅방의 참여자가 아닙니다. 고쳐야 할 문제입니다.
    - **방은 보이는데 다 읽음** - 진짜 정상입니다.
  - 이제 앞의 경우에는 **[계정 확인 필요]** 로 뜨고, 정상일 때도 `안 읽은 방 0개 / 전체 12개`처럼 전체 방 수를 함께 적습니다.
  - 어느 계정으로 보고 있는지도 함께 표시합니다. 사무실 PC와 개인 PC의 계정이 다르면 보이는 것도 달라집니다.



- **앞날 픽업 예약**. 요청: "'이번주 목금 이라엘 픽업입니다' 이라던가 특정날짜가 보이면 이것도 분석해서 미리 예정으로 등록해주고, 지정한날이 되면 자동으로 리마인드도 해줘"
  - 연락 하나가 여러 날을 가리키는 경우가 많습니다. 지금까지는 오늘 것만 처리되고 나머지 날짜는 **사람이 기억해야 했습니다.** 기억에 기대는 부분이 사고가 나는 곳입니다.
  - 이제 AI가 "이번주 목금", "내일부터 금요일까지", "8/26, 8/27" 같은 표현에서 날짜를 모두 뽑아 각각 예약합니다. 오늘 것은 즉시 반영하고, 나머지는 그날 아침 자동으로 걸립니다.
  - AI가 날짜를 잘못 셀 수 있어 서버에서 다시 거릅니다 - 지난 날, 45일보다 먼 날, **주말**, 중복은 버립니다. 아이를 엉뚱한 날 태워 보내는 일은 되돌릴 수 없습니다.
  - "이번주 목금"처럼 해석이 들어간 건은 **[확인 필요]** 로 표시해 예약하되 눈에 띄게 합니다. 놓치는 것보다 낫습니다.

- **당일 아침 자동 반영 + 담임 알림** (매일 오전 7시).
  - 그날 예정된 픽업을 하원 체크표에 걸어줍니다. 운영 대시보드·안내보드에도 그대로 따라옵니다.
  - 담임 선생님께 **긴급** 업무로 알립니다. 확인이 필요한 건은 제목에 그렇게 적습니다.
  - 학생을 명부에서 못 찾았거나 차량 배정이 없으면 조용히 넘기지 않고 인박스에 드러냅니다.

- **픽업 인박스에 [앞으로 예정된 픽업]** 을 날짜별로 묶어 보여줍니다. 예약이 어딘가 잡혔는데 눈으로 확인할 곳이 없으면 아무도 믿지 않습니다. 여기서 [맞음]으로 확인하거나 취소할 수 있습니다.



- **수집기를 새로 짰습니다**(v2.0.0). 며칠간 실패한 "엿보고 배우는" 방식을 버렸습니다.
  - 기존 방식은 토들이 주고받는 통신을 옆에서 보고 요청 서식을 배우는 것이었습니다. 그런데 토들은 채팅 목록을 한 번만 받아 캐시해서 **배울 기회가 사실상 한 번뿐**이고, 그 순간을 놓치면 영영 못 배웁니다. 감시를 계속 다시 걸어 해결하려다 토들 전체를 멈추게 했습니다.
  - 엿보기를 그만두고 실제로 무엇이 필요한지 화면에서 하나씩 확인했습니다.
    - 인증은 **쿠키**가 합니다. 토큰을 헤더에 넣을 필요가 없습니다.
    - 필요한 헤더는 **딱 둘** - `content-type` 과 `x-tod-source: WEB`(고정값). 없으면 401, 있으면 200입니다.
    - 학교 번호는 브라우저에 저장되어 있습니다(`userInfo.org_id`).
    - 우리에게 필요한 네 개 필드만 담은 짧은 질의를 직접 써도 그대로 답해줍니다. 토들의 원래 질의는 1,900자가 넘지만 우리는 그럴 필요가 없습니다.
  - 그래서 이제 **엿볼 것도 배울 것도 없습니다.** 로그인만 되어 있으면 바로 물어봅니다. 실제로 확인한 결과 안 읽은 방 12개, 방별 메시지·보낸이·시각까지 모두 정상입니다.
  - 읽음 처리는 여전히 별도 요청이라, 조회만으로 토들의 '안 읽음' 표시가 사라지지 않습니다. 선생님들 업무는 그대로입니다.
  - 처음 켠 날 지난 몇 달치가 쏟아지지 않도록, 방의 안 읽은 개수만큼 최신 것만 보고 3일 이내 것만 보냅니다.



- **수집을 6초 만에 끊고 있었습니다**(수집기 v1.5.2). 일을 시작해놓고 끝나기 전에 포기하는 상태였습니다.
  - 화면을 왕복시켜 다시 배우는 데만 9초가 걸리고, 방 열두 개의 메시지를 받아오는 시간이 더해집니다. 그런데 기다리는 시간이 6초로 묶여 있어 **매번 도중에 잘렸습니다.** 이 6초는 예전에 "탭 응답 없음"을 잡으려고 20초에서 줄인 값인데, 지금은 오히려 방해가 되고 있었습니다.
  - 하는 일에 맞춰 나눴습니다 - 상태 확인은 6초(답이 없으면 페이지가 죽은 것), 수집은 90초.
  - 수집 한 번이 1분을 넘을 수 있어, 앞의 수집이 도는 중이면 이번 차례를 거릅니다. 겹쳐 돌면 같은 메시지를 두 번 보내고 화면도 두 번 왕복합니다.



- **긴급 수정: v1.5.0 수집기가 토들을 멈춰 세웠습니다.** 제가 넣은 코드의 잘못입니다.
  - "감시가 지워졌으면 다시 씌운다"며 원본 통신 함수를 계속 갱신하게 했는데, 토들이 우리 위에 덧씌운 것을 다시 원본으로 삼는 바람에 **우리 함수가 우리 함수를 부르는 고리**가 생겼습니다. 토들의 모든 통신이 막혀 "오프라인"이 뜨고 로그아웃도 되지 않았습니다.
  - 원본은 이제 처음 한 번만 붙잡고 **절대 바꾸지 않습니다**(고정). 다시 씌우는 동작은 통째로 뺐습니다.
  - 되짚어보니 "페이지 처음에 건 감시가 지워진다"는 것은 제가 확인하지 않은 짐작이었습니다. 확인한 것은 "화면을 왕복시키면 다시 배운다" 하나뿐이었고, 그것만 남겼습니다.
  - 재학습은 10분에 한 번으로 제한합니다. 못 배우는 동안 화면이 1분마다 왔다갔다 하지 않도록 했습니다.



- **수집기가 안 읽은 방을 못 읽던 진짜 원인을 잡았습니다**(수집기 v1.5.0). 브라우저에서 하나씩 실제로 돌려보고 확인한 것들입니다.
  - **토들 번들이 우리 감시를 지웁니다.** 페이지 맨 처음에 감시를 걸면 → 안 잡히고, 페이지가 다 뜬 뒤에 걸면 → 잡힙니다. 토들이 나중에 로드되면서 통신 함수를 통째로 갈아끼워 먼저 걸어둔 것을 버리기 때문입니다. **먼저 걸수록 오히려 지워지고 있었습니다.** 이제 0.5초마다 확인해 지워졌으면 다시 겁니다.
  - **아직 못 배웠으면 화면을 잠깐 왕복시켜 배웁니다.** 토들은 채팅 목록을 한 번 받아 캐시해서, 가만히 두면 요청이 다시 오지 않습니다. Overview로 갔다가 Messages로 돌아오면 목록을 새로 받아옵니다 - 실제로 해보니 11개를 다시 배웠습니다. 수집 전용 컴퓨터라 화면이 잠깐 바뀌어도 문제되지 않습니다.
  - 왼쪽 메뉴가 링크(`<a>`)일 거라 짐작하고 짰다가, 화면에서 확인해보니 링크가 0개였습니다. 실제로는 id가 붙은 칸(`#chat`, `#monitor`)이어서 그에 맞춰 고쳤습니다.



- **수집기가 배운 서식을 저장합니다**(수집기 v1.4.0). 브라우저로 실제 통신을 재현해보고서야 진짜 구조를 알았습니다.
  - **토들은 채팅 목록을 한 번만 받아 캐시합니다.** 화면을 아무리 눌러도 다시 요청하지 않습니다 — 확인해보니 탭 전환 때 fetch 1건, XHR 0건이었습니다. 즉 "Messages 화면에 처음 들어갈 때" 딱 한 번만 배울 기회가 있고, 그 순간을 놓치면(확장을 나중에 켰거나 크롬이 탭을 재웠다 깨웠거나) **영영 못 배웁니다.** 세 번 고쳤는데 모두 이 지점을 비껴갔습니다.
  - 이제 한 번 배우면 확장 저장소에 넣어두고 다음부터 그대로 씁니다. 인증 헤더는 새 요청을 볼 때마다 갱신합니다.
  - **재전송 방식 자체는 정상**임을 실제로 확인했습니다(재전송 결과 9개, 안 읽은 방 9개). 문제는 서식을 못 배우는 것 하나뿐이었습니다.

- **"정상 0개"라는 거짓 안심을 없앴습니다.** 아는 방이 하나도 없으면 **[목록 못 읽음]** 으로 표시하고, 채팅목록 서식·대화 서식을 각각 있음/없음으로 보여줍니다. 원격에서 무엇이 막혔는지 한 줄만 봐도 알 수 있어야 합니다.
  - 정상일 때도 `전체 N개 · 안 읽은 방 N개`를 함께 적습니다.



- **"안 읽은 방 0개" 진짜 원인을 잡았습니다**(수집기 v1.3.1). 브라우저로 토들 통신을 직접 들여다보고 확인했습니다 — 추측으로 두 번 고쳤는데 둘 다 빗나갔습니다.
  - **채팅 목록을 돌려주는 요청이 두 종류였습니다.** `getChatAndChannelList`(방 이름·안 읽은 수가 든 진짜 목록)와 `getChatUnreadCount`(안 읽은 "개수"만 세는 요청). 둘 다 응답 모양이 `data.node.chats`로 같아서, "응답에 chats가 있으면 목록"이라는 판단이 **나중에 오는 개수 세기 요청으로 진짜 목록을 덮어썼습니다.** 그래서 재사용할 때 알맹이 없는 응답을 받아 0개가 됐습니다.
  - 이제 **방 이름이 들어 있는 응답만** 목록으로 인정합니다. 요청 이름으로 고르지 않으므로 토들이 이름을 바꿔도 계속 맞습니다.
  - 개수 세기 응답이 들어와도 **이미 알아둔 방 이름을 덮어쓰지 않습니다.**

- **문의가 언제 왔는지 정확히 표시합니다**(요청: "토들메세지 옆에 보면 시간이 나와 요일도나오고 오늘이라면 시간도 나와... 언제문의가 온건지도 기록해줘"). 토들 목록과 같은 방식입니다 — 오늘 온 것은 **시각**(14:32), 이번 주면 **요일+시각**(화 09:15), 그 전이면 날짜. 마우스를 올리면 전체 날짜시각이 뜹니다.
  - "3시간 전"만으로는 오전인지 점심인지 가늠할 수 없습니다. 하원 픽업은 몇 시에 온 연락인지가 판단에 직접 영향을 줍니다.
  - 메시지 자체의 시각이 없으면 그 방의 마지막 활동 시각으로 대신합니다.



- **수집기가 "0개"를 정상이라고 말하던 문제를 고쳤습니다**(수집기 v1.3.0). 토들에 안 읽은 방이 37개인데 수집기는 "정상 · 새 메시지 없음 (안 읽은 방 0개)"라고 답했습니다. **조용히 아무것도 안 하면서 정상이라고 말하는 것**이 이 시스템에서 가장 나쁜 실패인데, 정확히 그 상태였습니다.
  - **원인 ① 서식을 요청 이름으로 골랐던 것.** `operationName === "getChatConversations"` 처럼 요청 쪽 글자를 보고 판단했는데, 확장이 나중에 끼어들면 페이지가 이미 보낸 요청을 못 봅니다. 이제 **응답을 보고 판단합니다** — 토들은 요청을 배열로 묶어 보내고 응답도 같은 순서로 오므로, "몇 번째 응답에 채팅 목록이 들어 있나"를 보면 그 자리의 요청이 곧 채팅 목록 요청입니다. 토들이 이름을 바꿔도 계속 맞습니다.
  - **원인 ② 못 배운 상태에서 빈 목록을 돌려준 것.** 이제 아무것도 못 배웠으면 `NEEDS_TRAINING`을 던져 "준비 중"으로 표시합니다.
  - **자동 복구.** 두 번 연속 준비 중이면 수집기가 토들 탭을 스스로 새로고침합니다. 매번 하면 담당자가 토들을 보는 중에 방해가 되므로, 정말 막혔을 때만 합니다.

## v0.149.1 - 2026-08-24

- **업데이트 스크립트가 실패 원인을 그대로 보여줍니다**(수집기 v1.2.1). "서버에 연결하지 못했습니다" 한 줄만 띄우면 무엇이 잘못됐는지 짐작할 수 없습니다. 이제 HTTP 응답 코드와 curl 오류를 그대로 적고, 404(미배포)·401/403(배포 보호)·로그인 화면이 돌아온 경우를 각각 구분해 안내합니다.

## v0.149.0 - 2026-08-24

- **토들 수집기 자동 업데이트**(수집기 v1.2.0). 폴더를 매번 끌어다 놓게 만든 건 설계 잘못이었습니다 — 드래그하면 "폴더 2" 같은 새 폴더가 생기고, 크롬은 여전히 옛 폴더를 보고 있어 갱신이 안 됩니다.
  - 수집기 파일을 `public/collector/`로 옮겨 운영앱이 직접 서빙합니다. 이제 확장이 최신 파일을 **자기가 있는 자리에 덮어씁니다.**
  - 폴더 안의 **업데이트.command**(맥) / **업데이트.bat**(윈도우)를 더블클릭하면 끝입니다. 처음 한 번만 운영앱 주소를 묻고 그 뒤로는 기억합니다.
  - 받을 파일 목록을 `files.json`에서 읽으므로, 나중에 파일이 늘어나도 사용자는 아무것도 다시 옮기지 않아도 됩니다.
  - 임시 폴더에 전부 받은 뒤 성공했을 때만 덮어씁니다. 중간에 끊겨 반쪽짜리 확장이 남는 것이 가장 나쁜 결과라서입니다.
  - 확장이 스스로 새 버전을 확인해 **팝업에 "새 버전 N이 있습니다"** 를 띄웁니다. 손으로 설치한 확장이라 크롬이 알아서 갱신해주지 않기 때문입니다.
  - `/collector` 경로를 로그인 없이 열리도록 열었습니다(확장은 브라우저 세션 없이 파일을 받습니다). 확장 코드에는 비밀값이 없고, 수집 키는 담당자가 그 PC에서 직접 넣어 그 PC에만 저장됩니다.



- **토들 수집기 자가 복구**(수집기 v1.1.0). 설치 직후 "탭 응답 없음"이 뜨던 문제를 고쳤습니다.
  - 원인은 크롬의 규칙입니다 — 확장을 설치해도 **이미 열려 있던 탭에는 코드를 넣지 않습니다.** 그래서 설치 직후에는 항상 이 상태가 됩니다. 사람에게 새로고침을 시키는 대신, 수집기가 `chrome.scripting`으로 **직접 코드를 심어 스스로 복구**하게 했습니다. 크롬이 오래된 탭을 잠재웠다 깨우는 경우에도 같은 길로 낫습니다.
  - 토들 탭이 여러 개면 **전부 차례로 시도**하고, 메시지 화면(`/messaging`)이 열린 탭을 먼저 씁니다. 예전에는 첫 번째 탭 하나만 보고 포기했습니다.
  - 코드가 두 번 심겨도 한 번만 설치되도록 막았습니다(안 막으면 fetch를 두 겹으로 감싸고 같은 질문에 두 번 답합니다).
  - 페이지 응답 대기를 20초 → 6초로 줄였습니다. 수집 주기가 1분인데 20초를 기다리면 그 회차를 통째로 날립니다.



- **학부모 문의사항 탭 신설**(요청: "이토들로 문의사항도 많이 넣어주셔... 지금 실시간 로그자리에 학부모 문의사항탭을 넣고 거기에서 토들에 문의한 내용을 학생과 대조해서 분류해서 뜨도록").
  - 이제 AI가 학부모 연락을 **픽업 / 문의 / 기타** 세 갈래로 나눕니다. 문의는 유형(출결·수업·학습·생활·교우·건강·안전·차량·하원·행사·일정·납부·행정)과 **긴급도**, 한 줄 요약까지 붙여 남깁니다.
  - 학생·담임은 토들 채널 이름(`G2_Reina Park_Office`)으로 자동 연결됩니다. **[내 반]** 버튼으로 담임이 자기 반 문의만 볼 수 있습니다.
  - 목록은 **미답변 먼저, 그 안에서 긴급한 것 먼저** 정렬됩니다. 손댈 것이 항상 위에 있어야 합니다.
  - 문의를 누르면 원문·분류가 뜨고 **[토들에서 열기]** 로 그 채팅방으로 바로 갑니다. 여는 사람의 토들 로그인으로 열리므로, 그 방 멤버인 선생님은 바로 열리고 아니면 토들이 막습니다.
  - **[답변 완료]** 로 처리 여부를 관리하고, **[+ 업무로 등록]** 으로 필요한 것만 업무로 넘깁니다(요청: "문의탭에서만 우선보이고 클릭해서 업무로 등록할 수 있도록"). 자동 등록하지 않는 이유는 하루 수십 건이 업무 목록에 쏟아지면 원래 업무가 묻히기 때문입니다. 넘길 때 담당자는 담임으로, 부서는 학생 부서로, 긴급 문의는 우선순위 [긴급]으로 미리 채워집니다.

- **실시간 로그를 상단 부서 줄 가운데로 이동**(요청: "실시간 로그를 맨위에 초등부 부서 나오는칸 가운데로 로그를 옮기고 한줄만 표시되도록해서 누르면 전체로그가 뜨도록"). 최신 한 줄만 흐르고, 누르면 전체 목록이 팝업으로 열립니다. 세 줄을 차지하던 자리는 학부모 문의사항이 이어받았습니다.

- **'기타'로 분류된 연락은 본문을 저장하지 않습니다.** 인사·감사·단순 확인처럼 답이 필요 없는 것은 남길 이유가 없습니다. 같은 메시지를 다시 읽지 않기 위한 표시만 남깁니다.

- **수집기가 채팅방 링크를 함께 보냅니다.** 학교·과정 번호를 코드에 박지 않고 지금 보고 있는 화면 주소에서 떼어 쓰므로, 다른 과정(LES/UES)에서도 그대로 동작합니다.

## v0.146.0 - 2026-08-24

- **토들 수집기(크롬 확장) 제작** — `tools/toddle-collector/`. 사무실 CCTV용 윈도우 PC 크롬에 한 번 설치하면, 토들에 새 학부모 메시지가 올 때마다 1분 안에 읽어 픽업 인박스로 보냅니다. **채팅방 100개를 사람이 도는 일이 사라집니다.**
  - **읽음 처리를 하지 않습니다.** 토들에서 읽음으로 바꾸는 것은 `insertUserChatActivityLog`라는 별도 요청인데, 수집기는 조회만 흉내 내고 그 요청을 아예 만들지 않습니다. 그래서 직원 화면의 '안 읽음' 표시가 그대로 남고, 답장이 필요한 진짜 문의는 예전처럼 사람이 처리합니다.
  - **요청 서식을 코드에 박지 않고 그때그때 배웁니다.** 토들이 실제로 보내는 요청을 옆에서 보고 복사해 두었다가, 볼 채팅방 id만 바꿔서 재사용합니다. 질의문을 박아두면 토들이 필드 하나만 바꿔도 멈추는데, 이 방식은 토들 변화를 따라갑니다.
  - 교직원이 쓴 글, 이미 읽은 옛 메시지, 이미 보낸 메시지는 보내지 않습니다(처음 켠 날 과거를 몰아 보내는 사고 방지).
  - 확장 아이콘의 상태판에 **정상 / 준비 중 / 로그인 필요 / 토들 탭 없음 / 전송 실패**가 표시되고, 같은 상태가 픽업 인박스 상단에도 뜹니다.
  - 설치 방법은 `tools/toddle-collector/설치안내.md`에 화면 순서대로 적어두었습니다.

## v0.145.0 - 2026-08-24

- **픽업 인박스 신설**(요청: "전체 학부모의 채팅을 하나하나 실시간으로 보면서 아이들의 픽업을 처리하는게 너무 힘든데... 전화로 픽업을 오는경우도 많고, 교사에게 픽업을 알리는 경우도 많아서 픽업체크가 정말 어려워"). [셔틀 → 픽업 인박스]
  - 토들·전화·교사·구글챗·직접입력 어디로 들어오든 한 곳(`pickup_requests`)에 모입니다.
  - AI가 "픽업인가 / 누구인가 / 몇 시인가"를 뽑아 명부와 대조합니다. **확신이 높고 학생이 한 명으로 특정되면 자동으로 하원 체크표에 픽업이 찍히고**, 둘 중 하나라도 아니면 [확인이 필요한 픽업]에 남아 담당자가 한 번 눌러 확정합니다. 픽업은 아이를 누구에게 보내느냐의 문제라 기준을 넉넉히(0.85) 잡았습니다.
  - 토들 채널 이름이 `G2_Reina Park_Office` 형식이라 **본문을 해석하지 않아도 학생이 특정됩니다.** 형제 방(`G2_Sophia & Bella Hwang_Office`, `G3&G6_Ije & Ryeomyeong Kang_Office`)만 본문에서 가려내고, 못 가리면 사람에게 넘깁니다.
  - [손으로 접수] — 전화로 받은 내용이나 선생님이 전달한 내용을 붙여넣으면 같은 AI가 학생·시각을 찾아냅니다. 여러 건을 한 번에 넣을 수 있습니다.
  - **개인정보 최소 수집** — 픽업이 아니라고 판단된 메시지는 본문을 저장하지 않고, 같은 메시지를 다시 읽지 않기 위한 출처 식별자만 남깁니다. 픽업 건의 본문도 30일이 지나면 매일 도는 크론이 비웁니다.

- **수집기 생존 신호**(`integration_heartbeats`). 토들 수집기가 조용히 멈추면 그날 픽업을 통째로 놓칩니다. 1분마다 신호를 받아 **10분 끊기면 인박스 상단에 빨간 경고**를 띄웁니다 — "죽으면 시끄럽게"가 원칙입니다. 로그인이 풀린 경우는 그 PC에서 재로그인만 하면 된다고 안내합니다.

- **하원 운영화면 개편**(요청: "아래에 차량호수는 필요없고... 혹시 하원차량운행중에 픽업으로 전환되는 경우 표시되도록").
  - 지도 밑 호차 카드를 **한 줄짜리 요약 띠**로 줄였습니다(호차 · 상태 · 탑승 3/12 · GPS 점). 미탑승 학생 이름까지 넣어 세로로 길던 카드를 없애 자리를 확보했습니다.
  - 남는 자리를 전부 **오늘 픽업 학생 실시간 목록**에 줬습니다. 하원 중에 눈으로 좇아야 하는 건 "몇 호차가 있다"가 아니라 "누가 차를 안 타는가"입니다.
  - **차가 이미 출발한 뒤에 픽업으로 바뀐 건은 빨갛게 맨 위로** 올리고 "⚠ 출발 후 변경 — 차량에 연락하세요"를 띄웁니다. 그건 그 아이를 태우고 떠났다는 뜻이라, 몇 분만 늦게 알아도 학부모가 빈 학교에서 기다리게 됩니다. 최근 15분 안에 바뀐 건은 파랗게 NEW로 표시합니다.

## v0.144.0 - 2026-08-23

- **운영 대시보드가 창 크기에 맞춰 스스로 줄어듭니다**(요청: "cctv프로그램이 너무 많이 차지해서 공간이 많이 없더라고 반은 될줄알았는데 보니까 반에서 좀더 줄어들더라고, 그래도 시간표랑함께 모든정보들이 뜰 수 있도록 조정해줘").
  - 그동안은 "화면 절반"을 가정하고 글자 크기를 숫자로 박아뒀습니다. CCTV가 절반보다 더 차지하니 아래쪽 내용이 화면 밖으로 밀려났고, 대시보드는 아무도 스크롤하지 않으므로 밀려난 정보는 없는 것과 같았습니다.
  - 이제 창 크기를 재서 글자·여백·칸 최소폭을 한 배율로 함께 줄입니다. 가로·세로 중 더 부족한 쪽을 기준으로 삼아, 폭은 넉넉한데 높이가 낮은 창에서도 아래가 잘리지 않습니다.
  - 화면 높이를 넘기지 않도록 각 패널이 남는 공간을 나눠 갖고(시간표 3 : 출결·픽업 2 : 업무 2), 넘치는 목록은 그 패널 안에서만 스크롤됩니다. 아래 패널이 화면 밖으로 밀려나지 않습니다.
  - 창이 좁으면(820px 미만) 출결·픽업을 두 칸에서 한 칸으로 합치고, 업무 줄의 부서 표시를 빼서 제목이 잘리지 않게 합니다.
  - 오른쪽 위 **[🔍 자동]** 버튼으로 글자 크기를 자동 → 크게 → 보통 → 작게로 바꿀 수 있습니다. 모니터·시력·보는 거리에 따라 적당한 크기가 다르므로, 한 번 고르면 그 컴퓨터에 저장됩니다.

- **하원 운행 화면도 같은 방식으로 조절됩니다.** 전체화면을 못 쓰는 날에는 이 화면도 같은 좁은 창에 들어갑니다.
  - 좁을 때는 지도 비중을 55%에서 42%로 줄입니다 — 폭이 좁으면 지도가 담는 범위가 작아 "어디쯤인지"를 읽기 어렵고, 그보다 아래 차량 카드가 다 보이는 편이 실제로 쓸모 있습니다.
  - 차량 카드의 미탑승 학생 이름을 좁을 때 8명 → 5명으로 줄입니다(나머지는 +N). 카드가 세로로 길어져 뒤쪽 호차가 안 보이는 것을 막습니다.

## v0.143.0 - 2026-08-23

- **기기 발급을 일괄 처리로 바꿨습니다**(요청: "오시는 분마다 내가 설정해 드리는것도 문제고 최대한 자동으로 설정되게 만들어줘"). 기사님이 오시기를 기다릴 이유가 없어졌습니다.
  - [기기 없는 N개 노선에 한 번에 발급] 버튼 하나로 하원 노선 전체에 기기 ID와 설정 링크가 만들어집니다. 노선을 고르고 발급을 누르는 반복 작업이 사라집니다.
  - 그 뒤 담당자가 할 일은 [문자로 보내기]뿐이고, 기사님은 학교에 오시지 않아도 댁에서 설정을 마치실 수 있습니다.

- **설정 현황을 노선 기준으로 다시 짰습니다.** 기존에는 "발급된 기기"만 목록에 떠서, 아직 발급하지 않은 노선은 화면에 아예 없었습니다. 그러면 무엇이 남았는지 담당자가 머리로 세어야 합니다.
  - 하원 노선 전부가 한 줄씩 나오고 상태가 붙습니다 — **기기 없음 / 링크 보내야 함 / 링크 열어보심·설정 미완료 / ✓ 연결됨 / 신호 끊김**.
  - 상단에 `연결 N · 대기 N · 전체 N개 노선` 요약을 뒀습니다.
  - **신호 끊김**(예전에 들어왔는데 지금 조용한 경우)을 빨간색으로 따로 구분했습니다. 아이폰의 "계속 허용하시겠습니까?"를 잘못 누르셨거나 절전 설정이 되돌아간 경우가 대부분이라, 그냥 넘기면 그날 하원 때 알게 됩니다.

- **기사님 등록 체크리스트(A4 한 장)** 추가 — `docs/셔틀/`. 담당자가 기사님 앞에서 손에 들고 진행하는 현장용입니다. 사전 준비 → 대면 절차 → 미리 말씀드릴 것 → 그날 확인의 네 단계와 체크박스로 구성했습니다.

## v0.142.0 - 2026-08-23

- **위치 수집 시간을 하원 3시간으로 좁혔습니다**(요청: "하원트래킹은 오후 3:30분 부터 오후 6시 30분까지 3시간이야 그 이후에는 위치추적안하고"). 기존 평일 12:00~20:00 → **평일 15:30~18:30**.
  - 서버가 이 창 밖의 좌표를 받는 즉시 버립니다(`isWithinTrackingWindow`). 문서상의 약속이 아니라 코드로 강제되므로, 기사님이 앱을 끄지 않으셔도 그 시간 밖의 이동은 저장되지 않습니다.
  - 등원 시간대는 확정되면 `TRACK_WINDOWS`에 한 줄만 더하면 됩니다. 시간 판단을 시(hour) 단위에서 분(minute) 단위로 바꿔뒀습니다.

- **기사님 설정 링크 신설**(요청: "웹앱으로 몇호차 기사님께 보내기하면 기사님 카톡이나 문자로 링크가 가서 누르면 웹앱으로 접속되고 거기에서 편하게 누르면...", "기기를 맡기면 오분이내에 설정이 완료되어야 하는데"). 기기마다 짧은 주소 `/s/{6자리}`를 하나씩 발급합니다.
  - **기사님 화면**(로그인 없음) — 아이폰·안드로이드를 자동으로 가려 스토어 버튼을 띄우고, 서버 주소와 기기 번호를 **눌러서 복사**하게 합니다. 타자를 한 글자도 치지 않으십니다. 6단계를 한 번에 하나씩만 펼치고, 마지막에 서버가 실제로 위치를 받았는지 확인해 초록 표시를 띄웁니다(10초마다 자동 재확인).
  - **관리자 화면** — 호차 옆 [설정 링크]를 누르면 큰 QR(학교에 오신 기사님이 휴대폰 카메라로 찍는 용도)과 [문자로 보내기]·[문구 통째로 복사(카톡용)]가 나옵니다. 문자는 별도 발송 API(유료 가입·심사 필요) 없이 담당자 휴대폰의 기본 문자 앱을 열어 내용을 채웁니다.
  - 링크 코드는 기기 ID와 **따로** 둡니다. 문자로 나가는 값이라 새어 나갈 가능성이 크기 때문에, 그때는 코드만 새로 발급하면 되고 이미 설정을 마친 휴대폰은 건드리지 않아도 됩니다.
  - 기사님이 링크를 여신 시각을 남겨, 담당자가 "보내드렸는데 하셨나?"를 전화로 묻지 않아도 됩니다.

- **안내문·동의서 개정**(요청: "위치추적의 데이터도 개인이 사적으로 사용하는것이아니라 오로지 하원차량이 제대로 운영되는지 체크용도이고 학원이외에 이 정보를 누구에게도 제공하지 않는다는 것을 명시해주고").
  - 수집 시간을 **평일 15:30~18:30**으로 고쳤고, 등원은 이 동의서에 포함되지 않으며 확대 시 다시 동의를 받는다고 명시했습니다.
  - 목적 제한을 각 문서 앞머리에 못박았습니다 — 하원 차량 운영 점검 외의 용도(사적 이동 경로 파악·근무 감시·운전 습관 평가)로 쓰지 않습니다.
  - 제3자 제공 금지를 "학부모를 포함하여 **학교 밖의 어떠한 개인·기관에도** 제공하지 않는다"로 강화하고, 판매·위탁·광고/통계 목적 이전까지 명시했습니다.
  - 안내문에 링크 방식 설명을 넣었습니다("문자로 받으신 링크 누르기").

## v0.141.0 - 2026-08-23

- **기사님용 안내문·동의서 작성**(요청: "기사님께 보낼 안내문과 동의서 만들어주고, 어떤 앱을 설치해서 어떻게 설정해야 할지 알려줘"). `docs/셔틀/` 폴더에 두 개의 워드 파일로 넣었습니다.
  - **안내문(4쪽)** — 왜 하는지, 무엇이 수집되고 무엇이 수집되지 않는지, 앱 설치·설정 6단계, 자주 묻는 질문 6가지. Traccar 공식 문서에서 설정 항목 이름을 확인해 실제 화면과 같은 표기로 적었습니다.
  - **동의서(2쪽)** — 위치정보 수집·이용 동의서. 수집 목적·항목·시간 제한·보유 기간·열람 범위·거부 권리·철회 방법을 담고 서명란을 넣었습니다.

- **위치 기록 90일 자동 삭제 크론 신설**(`/api/cron/purge-shuttle-locations`, 매일 23:30 KST). 동의서에 "90일 뒤 자동 삭제"라고 적으려면 실제로 그렇게 동작해야 합니다. 문서에만 적어두고 계속 쌓아두면 그 약속이 거짓이 됩니다.
  - 위치 원본(`shuttle_pilot_pings`)과 정차 관찰 기록(`shuttle_stop_observations`)을 지웁니다.
  - 정류장 좌표(`shuttle_stops.gps_lat/lng`)는 지우지 않습니다 — 개인의 이동 기록이 아니라 "이 정류장이 어디인가"라는 장소 정보이고, 원본에서 이미 평균만 뽑아낸 값입니다.

## v0.140.0 - 2026-08-23

- **초등부 명부를 확정으로 삼고 정리했습니다**(요청: "일단 지금 명단에 없으면 전부 보류하고... 우선은 반구성도 지금 명단에 없다면 아예 없애줘 일단 초등부는 이명단이 확정이라 다른 반이 있어서는 안돼").

  **학생 → 보류** (지우지 않음). 명부에 없다고 바로 퇴소로 찍으면, 사실은 중고등부라서 이 명부에 없었을 뿐인 학생까지 퇴소가 됩니다. 그래서 **"아직 판단하지 않았다"는 뜻의 보류 상태**를 새로 만들어 거기에 뒀습니다.
  - 나중에 **중고등부 명단을 주시면 이름으로 대조해서, 그 명단에도 없는 학생만 퇴소로 넘깁니다.**
  - 보류 학생은 관찰기록·픽업체크·대시보드에서 빠지지만 기록은 그대로 남습니다.
  - 부서 값은 일부러 건드리지 않았습니다 — 중고등부일지 유치부일지 확인되지 않았는데 지금 단정해두면 나중에 대조할 때 잘못 붙은 값이 판단을 방해합니다.
  - **[학생 관리] 화면 위쪽에 [⏸️ 보류] 상자**가 생겼습니다. 펼쳐서 이름을 확인하고, 초등부 학생인데 누락된 경우라면 [재학으로] 한 번이면 되돌아옵니다.

  **반 → 삭제.** 확정 명부의 8개 반이 아닌 반은 지웠습니다. 옛 학기의 반이 남아 있으면 반 관리 화면과 담임 배정에서 계속 헷갈립니다.
  - **학생·관찰기록·출결은 지워지지 않습니다.** 연결만 끊어지고, 학년 표기는 각 기록에 글자로 남습니다. 그 반의 옛 시간표만 함께 사라지는데 새 학기 시간표를 이미 넣었으므로 문제되지 않습니다.
  - 어떤 반이 사라졌는지 담임 이름까지 [명부 점검]에 기록해뒀습니다.

  **유치부는 건드리지 않았습니다**(요청: "유치부는 반을 보고 구별할 수 있고"). 부서·학년 표기가 유치부인 학생과 반은 그대로입니다. 유치부 반 이름 목록을 주시면 반 표기로도 정확히 갈라낼 수 있습니다.

  운영 화면이 보여주는 부서를 **초등부 하나로** 좁혔습니다. 대시보드의 부서 선택 버튼은 고를 것이 하나뿐이라 자동으로 숨겨집니다.

- **하원 셔틀은 이 정리의 영향을 받지 않습니다**(요청: "일단은 셔틀만 지금 준 명단에 없는 애들 나오도록 해주고"). 하원 체크표·실시간 셔틀은 셔틀 배정표를 이름으로 읽기 때문에, **보류 학생도 유치부 학생도 그대로 나옵니다.** 하원차량표를 주시면 그것으로 노선을 다시 짜겠습니다.

## v0.139.4 - 2026-08-22

- **3학기 데이터 마이그레이션에서 없는 표(`wr_terms`)를 쓰던 코드를 걷어냈습니다.**

  ```
  ERROR: relation "wr_terms" does not exist (SQLSTATE 42P01)
  ```

  `wr_terms`는 **없는 게 정상입니다** — v0.36에서 "연도 > 학기" 체계를 운영과 주간 관찰기록이 함께 쓰도록 통합하면서 삭제된 표입니다. 그런데 제가 3학기 학기 등록을 만들면서 그 표에도 넣는 코드를 잘못 넣었습니다. 지우고 `terms` 하나만 쓰도록 고쳤습니다.
  - 대신 **새 학기를 켤 때 이전 학기를 자동으로 종료로 내립니다.** 여러 학기가 동시에 "진행중"으로 남아 있으면 화면이 어느 학기를 기준으로 삼을지 애매해집니다.
  - 남은 두 파일이 건드리는 표를 전부 추출해 실제로 존재하는지 확인했습니다. `wr_terms` 외에는 없습니다.

## v0.139.3 - 2026-08-22

- **도서관 마이그레이션의 옛 직위 이름을 고쳤습니다.** 앞의 세 파일은 통과했고 도서관 파일에서 막혔습니다.

  ```
  ERROR: new row for relation "app_users" violates check constraint "app_users_position_check"
  Failing row contains (gia-library@giamicro.com, ..., 교직원, ...)
  ```

  도서관 가계정을 계정 목록에 등록할 때 직위를 `교직원`으로 넣고 있었는데, 이 이름은 v0.36에서 **`행정직원`으로 정리되면서 허용값에서 빠진 옛 표기**입니다. 도서관 기능이 다른 갈래에서 만들어지는 동안 옛 이름이 그대로 남아 있었습니다. `행정직원`으로 바꿨습니다.
  - 남은 마이그레이션 전체를 훑어 **제약이 걸린 값(직위·소속·성별·악기·탑승·학기·상태)이 허용 목록 밖으로 나가는 곳이 더 없는지 전수 확인**했습니다. 이 한 건 외에는 없습니다.

## v0.139.2 - 2026-08-22

- **자동 반영이 멈춘 진짜 원인을 찾아 고쳤습니다.** 오류 문구는 이것이었습니다.

  ```
  ERROR: column s.gender does not exist (SQLSTATE 42703)
  ```

  실서버 `wr_students`에 **`gender`(성별) 칸이 아예 없었습니다.** 자동화를 만들기 전까지는 채팅으로 드린 SQL을 SQL Editor에 직접 붙여넣는 방식이었는데, 그 과정에서 한 블록이 빠졌던 것으로 보입니다. 그런데 나중에 만든 교직원 공용 명부 뷰가 그 칸을 읽으려 하면서 멈췄고, **마이그레이션은 하나가 실패하면 뒤가 전부 멈추기 때문에** 3학기 학생·반·시간표가 통째로 들어가지 못했습니다.
  - **선행 보정 파일을 추가했습니다** — 뒤 마이그레이션이 읽거나 쓰는 칸을 전부 "없으면 만들기"로 한 번씩 훑습니다(학생 14칸, 반 2칸, 셔틀 3칸, 계정 7칸, 학기별 반 배정 표, 공통 트리거 함수). 이미 있으면 아무 일도 일어나지 않습니다.
  - 앞으로도 손으로 넣은 SQL과 어긋나는 부분이 있으면 여기서 먼저 메워지므로 같은 이유로 멈추지 않습니다.

## v0.139.1 - 2026-08-22

- **자동 반영이 멈춘 원인으로 가장 유력한 곳을 고쳤습니다.** 마이그레이션 안에 실시간 구독 등록(`alter publication supabase_realtime add table ...`)이 있는데, 이 문장은 Supabase에서 **소유자 권한**을 요구합니다. 대시보드 SQL Editor에서는 통과하지만 GitHub Actions가 쓰는 연결에서는 `must be owner of publication`으로 막힙니다.
  - 문제는 이 한 줄이 실패하면 **그 뒤 파일이 전부 멈춘다**는 것이었습니다. 실시간 구독은 "새 공지가 새로고침 없이 뜨는" 편의 기능일 뿐인데, 그것 때문에 학생 명부까지 못 들어간 셈입니다.
  - 이제 이 문장이 실패해도 안내만 남기고 넘어갑니다. 정말 권한 문제였다면 다음 실행부터 자동 반영이 끝까지 돌아갑니다.
  - 실시간 구독이 꺼진 채로 남으면 Supabase 대시보드 > Database > Replication에서 켜주시면 됩니다(업무 공지·도서관 대출 화면의 즉시 갱신용).

## v0.139.0 - 2026-08-22

- **자동 반영(GitHub Actions)이 v0.127.0부터 계속 실패하고 있었습니다.** 그래서 그 뒤로 만든 스키마와 3학기 학교 데이터가 실서버에 **하나도 들어가지 않은 상태**였습니다(명부가 비어 보이던 원인). 실행 로그가 비공개라 원인을 바로 볼 수 없어, 두 가지를 함께 했습니다.
  - **손으로 넣을 수 있는 SQL을 만들었습니다** — `supabase/manual/` 폴더에 1(스키마) · 2(3학기 데이터) · 3(기록표 정리) 세 파일입니다. Supabase SQL Editor에 순서대로 붙여넣어 실행하면 밀린 것이 전부 들어갑니다. 모든 문장이 "없으면 만들고 있으면 갱신"이라 몇 번을 돌려도 안전하고, 3번 파일이 반영 기록을 남겨서 **다음부터는 자동 반영이 같은 파일을 다시 돌리지 않습니다.**
  - **워크플로가 실패 원인을 감추지 않게 고쳤습니다** — 예전에는 빨간 X와 `exit code 1`만 보이고 무엇이 잘못됐는지 알 수 없었습니다. 이제 실행 결과 요약(Summary) 화면에 **원격 DB의 반영 현황과 실패 문구가 그대로** 적힙니다. 로그를 펼쳐 찾을 필요가 없습니다.
  - [학교 > 명부 점검] 안내에도 숫자가 계속 0일 때 무엇을 해야 하는지 적어뒀습니다.

- **운영 대시보드 '지금 수업'을 단순하게**(요청: "그냥 지금 어느학년 어느반이 무슨시간인지 한눈에 볼 수있도록만 해주고"): 반 이름과 지금 과목 **두 줄만** 남기고 담임 이름·다음 교시·담당 교사를 뺐습니다. 멀리서 보는 화면에서는 글자가 많을수록 오히려 안 읽힙니다. 과목 글자도 키웠습니다.
  - 수업이 없는 시간에는 그 자리에 **교실 위치**(A동 2F 등)가 뜹니다(요청: "수업중이 아닐때 교실위치 보여주는 것은 좋고").

## v0.138.0 - 2026-08-22

- **당번표 신설**(요청: "당번표는 대시보드에 필요없고, 일단은 데이터만 넣을 수 있게 해주고"): [학교 > 당번표]에서 급식 당번을 입력해둘 수 있습니다. 화면 어디에도 띄우지 않고 모아두기만 하며, 나중에 대시보드나 달력에 올리기로 하면 여기 적은 내용을 그대로 씁니다 — 그때 다시 입력하실 필요가 없습니다.
  - 급식 당번뿐 아니라 **체육관·코딩실 사용, 도서관 당번처럼 같은 모양의 표는 전부 여기에** 넣습니다. [종류] 칸에 이름만 새로 적으면 새 당번표가 하나 생깁니다. 종류마다 표를 따로 만들면 새 당번이 생길 때마다 화면과 메뉴가 늘어나기 때문입니다.
  - [언제]는 **매주 반복**(요일)과 **특정일**(날짜) 중에 고릅니다. 담당자는 이름만 적어도 되고, 앱에 있는 이름과 같으면 계정까지 자동으로 연결됩니다.
  - 조회는 교직원 누구나, 추가·삭제는 행정직원 이상입니다.

- **모든 메뉴에 ❓사용 가이드**(요청: "각 메뉴들 안내하는 물음표 전부 만들어주고"): 가이드가 없던 8개 화면에 새로 붙였습니다 — 업무 보드, 지난 업무, 휴지통, 하원 체크표, 실시간 셔틀, 등록사건목록, 보고서 모음, 데이터 백업. 이제 **메뉴에 연결된 모든 화면**에서 오른쪽 위 ❓를 누르면 그 화면이 무엇을 하는 곳이고 어떻게 쓰는지 볼 수 있습니다.
  - 하원 체크표 가이드에는 담임·구글챗·행정실 세 경로가 어떻게 한 칸에 모이는지, 학생을 다른 차로 옮길 때 [계속 유지]와 [오늘만]이 어떻게 다른지 적었습니다.
  - 데이터 백업 가이드에는 **복원할 때 무엇을 잃게 되는지**를 먼저 확인하라는 경고를 넣었습니다.

- **운영 대시보드 최신화**(요청: "운영 대시보드 최신화 해줘"):
  - 반 이름 옆에 **담임 이름**이 함께 나옵니다. 멀리서 보는 화면이라 "G3JU"보다 "G3JU · Ms. June"이 훨씬 빨리 읽힙니다. 담임 계정이 아직 연결되지 않은 반도 명부에 적어둔 이름으로 표시됩니다.
  - 수업 중이 아닐 때는 **교실 위치**(A동 2F 등)를 대신 보여줍니다.
  - 반이 하나도 없을 때 "이 부서에 등록된 반이 없습니다"에서 끝나지 않고 **어디로 가서 무엇을 해야 하는지**까지 안내합니다.
  - 관리 화면 가이드에 3학기 시간표가 이미 채워져 있다는 점과, "지금 수업"이 비어 보일 때 무엇을 확인해야 하는지를 추가했습니다.

## v0.137.0 - 2026-08-22

- **메뉴 전면 재분류 — 주메뉴 10개 → 6개**(요청: "이제 메뉴가 너무 많아져서 통합할거 통합하고 분류할건 분류하고 싶어... 주간학생관찰기록은 제외하고 나머지의 메뉴들을 최대한 통합하고 관리할 수 있도록"). 기능이 생길 때마다 최상위에 하나씩 붙인 결과, 성격이 같은 메뉴가 따로 떨어져 있고(실무자 매뉴얼 ↔ 학교 문서함) 항목이 두 개뿐인 메뉴가 최상위 한 칸을 차지하고 있었습니다(학사일정). **"무엇에 대한 일인가"를 기준으로 다시 묶었습니다.**

  | 새 메뉴 | 담긴 것 | 어디서 왔나 |
  |---|---|---|
  | **업무** | 업무 보드 · 업무 보고서 · 지난 업무 · 휴지통 | 하위 화면을 부메뉴로 꺼냈습니다(전에는 보드 안에서만 이동) |
  | **학교** | 학생·교직원 조회 / 학생·반·과목 관리 · 명부 점검 / 학기 · 학사일정 · 학기 준비 · 시간표 | 학교 관리 + 학사일정 + 운영 대시보드 |
  | **셔틀** | 하원 체크표 · 실시간 위치 / 노선 · 탑승 배정 · 배차표 · 링크 관리 | 순서를 "매일 쓰는 것 → 기준정보"로 뒤집었습니다 |
  | **기록** | 사건·회의·행사 · 회의 보고서 / AI 매뉴얼 · 제안함 · 채택예정 | 옛 "운영 관리". 실제로 하는 일이 "기록 → 제안 → 발행"이라 이름을 바꿨습니다 |
  | **문서 · 매뉴얼** | 실무자 매뉴얼 · 매뉴얼 · 운영계획안 / 문서함 · 서류함 · AI 서류 작성 · 보고서 모음 | 최상위였던 실무자 매뉴얼 + 학교 문서함을 합쳤습니다 |
  | **관리** (관리자) | 통합 대시보드 · GIA시스템 · 교육뉴스 / 사용자·공용 계정 / 구글시트 가져오기 · 백업 | 여기저기 흩어져 있던 설정을 모았습니다 |

  - **실무자 매뉴얼은 손이 더 가지 않습니다** — 전화 응대 중 급하게 여는 화면이라, [문서 · 매뉴얼] 카테고리를 **직접 누르면 바로 실무자 매뉴얼이 열립니다.**
  - 앞으로 생길 **급식 당번·시설 예약** 같은 학사 운영 항목은 [학교 > 학사] 아래에 붙습니다. 새 기능을 어디에 넣을지 매번 고민하지 않아도 됩니다.
  - 주간 학생 관찰기록은 요청대로 손대지 않았습니다.

- **명부 점검 화면 신설**(요청: "대시보드에서 확인 어떻게 할 수 있어?"): [학교 > 명부 점검]에서 Supabase를 열지 않고도 명부가 제대로 들어갔는지 확인합니다.
  - 위쪽에 **재학생·퇴원·반·교시·시간표·과목 수**가 명부 기준값과 나란히 뜹니다. 값이 맞으면 초록, 다르면 주황, 0이면 "아직 반영 전"입니다. 칸을 누르면 해당 관리 화면으로 갑니다.
  - 아래쪽에 **사람이 판단해야 하는 건**이 모입니다 — 동명이인, 이름은 같은데 생년월일이 다른 경우, 생년월일이 비어 있는 경우. 처리한 뒤 [확인 완료]로 내리면 됩니다.

## v0.136.0 - 2026-08-22

- **2026학년도 3학기 학교 데이터 일괄 적재**(요청: "이번정규학기(3학기)에 모든 데이터를 줄거야... 각 반 시간표 등 학교에 관한 모든 자료가 있으니 분석해서 데이터를 전부 넣어줘"). 출처는 `26-27 GIA Primary Roster [Admin Version].pdf` 35쪽 전체입니다.
  - **학기** — 2026학년도 3학기(2026-08-24 ~ 2026-11-13)를 진행중으로 등록했습니다.
  - **반 8개** — G2J·G2C·G2A / G3JU(June)·G3JA(Janelle) / G4R·G4S / G5E. 교실 위치까지 함께 넣었습니다. 명부에 두 G3 반이 똑같이 "G3J"로 적혀 있어 구분되지 않던 문제를 요청대로 두 글자씩 따서 나눴습니다.
  - **학생 100명** — 한글·영문 이름, 성별, 생년월일, 보호자 연락처, 악기, 방과후 여부, 알러지, 형제자매 관계, 교복 사이즈, 입학일까지 전부 반영했습니다.
  - **전출·퇴원 26명** — 퇴원 상태로 넣고 마지막 등원일(`left_on`)을 남겼습니다. 화면에는 안 보이지만 지난 학기 관찰기록·출결이 주인 없는 데이터가 되지 않습니다.
  - **교시 7개 + 반별 시간표 280칸** — 1교시 08:50부터 7교시 16:00까지, 8개 반 × 5일 × 7교시 전부. 이제 운영 대시보드의 "지금 수업"이 실제로 채워집니다.
  - **과목 16개** — 시간표에 나오는 과목을 등록하고, 명부에서 담당이 확인되는 과목(Chinese·Korean History·Computer Science)에는 선생님 이름을 미리 적어뒀습니다.
  - **적재 방식** — 이름과 생년월일로 기존 학생을 찾아 갱신하고 없을 때만 새로 만듭니다. 두 번 적용해도 학생이 두 벌로 늘지 않습니다. **앱에서 손으로 채워둔 값은 지우지 않고**, 명부에 값이 있는 칸만 덮어씁니다(특이사항은 덮어쓰지 않고 뒤에 덧붙입니다).
  - **확인이 필요한 건은 조용히 넘어가지 않습니다** — 동명이인, 이름은 같은데 생년월일이 다른 경우, 명부에 생년월일이 없는 경우를 `wr_import_issues`에 남깁니다. 애매한 채로 덮어쓰면 다른 학생의 기록이 섞이는데 그건 되돌리기가 매우 어렵습니다.
  - 명부의 오류 3건을 정정해 넣었습니다: 곽호율 학생 칸에 겹쳐 들어간 Maya Amelia Dowding의 성, 김서연 입학일 칸의 오타, 김시아 입학연도(2024→2026).

- **가입 화면이 직위에 따라 달라집니다**(요청: "교사와 교직원, 관리자를 선택했을 때 각각 다르게 아래에 나오도록"). 세 직위의 질문을 한꺼번에 늘어놓으면 대부분 자기와 상관없는 칸이라 무엇을 채워야 할지 헷갈립니다.
  - **교사** — 담임반과 담당 과목을 직접 고릅니다(요청: "교사가 가입을 할 때, 반이나 과목을 선택할 수있게"). **담임이 아직 비어 있는 반만** 목록에 나오므로 남의 반을 고를 수 없고, 각 반 옆에 학교 명부의 담임 이름("Ms. Jaime")이 함께 보입니다. 입력한 이름과 명부 이름이 겹치면 그 반을 한 번에 선택하는 버튼이 뜹니다.
  - **행정직원 · 관리자** — 반/과목 대신 담당 업무를 적습니다. [내 계정 설정]에서 나중에 바꿀 수 있습니다.
  - 관리자는 지금처럼 [학교 관리 > 반 관리]와 [과목반 세팅]에서 언제든 바로잡을 수 있습니다.

## v0.135.0 - 2026-08-22

- **운영 대시보드 메뉴 이동 + 관리자 전용**(요청: "운영 대시보드 운영 관리 메뉴에 넣어주고, 운영 대시 보드는 관리자,개발자만 보이도록 해줘"): 사이드바 맨 위 독립 메뉴였던 [운영 대시보드]를 **[운영 관리] 안으로** 옮겼습니다. 매일 열어보는 화면이 아니라 한 번 설정해두고 두는 성격이라 상단 자리를 차지할 이유가 없었습니다. 이제 관리자·개발자에게만 보이고, 주소를 직접 입력해 들어오더라도 화면 자체에서 다시 막습니다. 이 화면은 **로그인 없이 열리는 주소를 만들어내는 곳**이라, 주소가 새어나가면 누구나 결석 명단과 업무 현황을 볼 수 있게 되기 때문입니다.

- **하원 시간 전체화면 전환 + 자동 복귀**(요청: "cctv프로그램하고 화면을 분할해서 반반 쓰고 있는데 하원시간에는 전체화면으로 전환되고 하원종료버튼을 누르거나 종료시간이 되면 다시 화면 되돌리게 만들어줘"):
  - 하원 시각(기본 16:00)이 되면 대시보드가 **전체화면으로 커져 옆의 CCTV를 덮습니다.** 종료 시각(기본 17:30)이 되면 **저절로 전체화면이 풀리고** 원래 반반 배치로 돌아옵니다.
  - 하원 화면 오른쪽 위에 **[하원 종료]** 버튼을 뒀습니다. 마지막 차가 일찍 복귀한 날에는 종료 시각까지 기다리지 않고 바로 되돌릴 수 있습니다. 오늘 하루만 종료되고, 잘못 눌렀으면 평소 화면 위쪽 [하원 화면 다시 열기]로 되돌아옵니다.
  - 종료 시각은 [운영 관리 > 운영 대시보드]의 링크 목록에서 시작 시각 옆에 함께 정합니다.
  - **한 가지 알아두실 점** — 전체화면으로 *들어가는* 것만은 브라우저 규칙상 사람이 한 번 눌러야 합니다(아무 사이트나 시간이 되면 마음대로 화면을 덮지 못하게 하는 안전장치입니다). 하원 시각이 되면 오른쪽 아래에 [전체화면] 버튼이 떠서 한 번만 눌러주시면 되고, 급할 때는 무시하고 반반 화면 그대로 쓰셔도 됩니다. 되돌아오는 것은 제약이 없어 완전히 자동입니다.
  - 예전에는 시작 시각만 있어서 한 번 하원 화면이 되면 **자정까지 그대로 남아 있었습니다.** 종료 시각이 생기면서 이 문제도 함께 해결됩니다.

## v0.134.1 - 2026-08-21

- **교사 화면에서 통합 검색창 제거**(요청: "교사계정은 달력위에 검색창 없애주고"): 이 검색은 사건기록·회의·업무·학교문서를 훑는 통합 검색인데, 교사는 그 화면들에 애초에 들어갈 수 없어 무엇을 쳐도 결과가 나오지 않았습니다. 사이드바와 모바일 상단 양쪽에서 감췄고 달력만 남겼습니다.
- **짧은 주소 오류 메시지 개선**: 짧은 주소 칸이 아직 DB에 반영되기 전에 저장하면 알아볼 수 없는 영문 오류가 떴습니다. 이제 "아직 DB에 반영되지 않았습니다. 1~2분 뒤 새로고침하고 다시 시도해주세요."로 안내합니다. 같은 상황에서 [링크 만들기]가 통째로 막히던 문제도 고쳤습니다 — 짧은 주소 없이 먼저 만들어 두고 나중에 코드만 채우면 됩니다.

## v0.134.0 - 2026-08-21

- **운영 대시보드 짧은 주소**(요청: "운영안내 대시보드 주소 간결하게 만들어주고, 다른곳에서 바로 주소만쳐서 들어갈 수 있게"): 지금까지 대시보드 주소는 36자리 토큰이 붙어 있어 다른 컴퓨터에서 주소창에 직접 치는 것이 사실상 불가능했습니다. 이제 **`우리주소/d/코드`** 네 글자로 바로 열립니다(안내보드의 `/b/`와 같은 방식, d = dashboard).
  - [운영 대시보드 · 시간표 관리] 링크 목록의 `/d/` 칸에서 코드를 확인하고 원하는 이름(`office` 등)으로 바꿀 수 있습니다. [짧은 주소 복사] 버튼도 함께 있습니다.
  - 이미 만들어 둔 링크에도 코드가 자동으로 하나씩 채워집니다.
  - 코드 글자에서 `0`·`O`·`1`·`l`을 뺐습니다 — 눈으로 보고 옮겨 치는 값이라 가장 흔한 실수가 0과 O를 헷갈리는 것입니다.
  - 토큰을 짧게 바꾸는 대신 지름길을 하나 더 두는 방식입니다. 토큰 자체를 짧게 하면 아무나 몇 번 찍어보는 것만으로 열 수 있게 되기 때문입니다. 사용을 멈추려면 지금처럼 [중지] 한 번이면 됩니다.
- **공용 계정 비밀번호 길이 제한 해제**(요청: "공용계정 비밀번호 제한없게 해줘"): 앱에서 걸어두었던 10자 이상 조건을 없앴습니다. 도서관 노트북처럼 여러 사람이 하루에도 몇 번씩 입력하는 계정은 짧고 외우기 쉬운 편이 실제로 더 안전합니다 — 길고 복잡하게 강제하면 결국 모니터에 붙여두게 되기 때문입니다. (Supabase 프로젝트 설정에 정해진 최소 길이보다 짧으면 Supabase가 거절하고 그 메시지가 화면에 그대로 표시됩니다.)

## v0.133.0 - 2026-08-21

- **화면 언어 전환(한국어 ↔ English)**(요청: "교사권한이 볼 수 있는 페이지는 영/한 완전히 변환할 수 있게"): 지금까지는 한글 옆에 영어를 작게 병기했는데, 병기는 화면이 두 배로 길어지고 원어민 선생님께는 여전히 한글이 먼저 눈에 들어왔습니다. 이제 **화면 전체가 한 언어로 완전히 바뀝니다.**
  - 사이드바 맨 아래(모바일은 상단 메뉴 오른쪽) [한국어 / English] 버튼으로 누구나 직접 바꿉니다. 선택은 이 브라우저에 1년간 저장되어 다음 로그인에도 유지됩니다.
  - 바뀌는 범위 - 메뉴, 내 담임반, 내 담당과목, 리포트 작성 모달(뱃지·항목 이름·안내문 포함), 내 반 픽업 체크, 출석부, 문의및건의사항, 내 계정 설정, 각 화면의 ❓사용 가이드.
  - **영어 화면에서는 학생의 영어 이름이 크게 나옵니다**(한글 이름은 작게 아래로). 원어민 선생님이 한글 이름만 보고 학생을 찾기 어려웠던 문제를 없앴습니다.
  - 소속·직위·출결상태처럼 DB에 한글로 저장되는 값은 저장은 그대로 두고 화면 표시만 번역합니다. 영어로 가입해도 권한 판정은 언어와 무관하게 똑같이 동작합니다.
- **가입 절차 영어 대응**(요청: "가입할때 교사도 가입해야하니까 가입하는것도 영어병기 해주고"): 로그인·최초 정보 입력(이름/소속/직위)·승인 대기 화면 모두 언어 전환 버튼이 붙었습니다. 원어민 선생님이 앱을 처음 만나는 순간부터 영어로 읽힙니다.

- **아이디·비밀번호 로그인 신설**(요청: "로그인할때, 아이디랑 비번 넣고 들어갈 수 있도록 만들어서, 도서관이랑, 오리엔테이션용 가계정을 만들어서 관리하게 해줘"): 로그인 화면 아래 [공용 계정으로 로그인]을 열면 아이디+비밀번호로 들어갈 수 있습니다. 아이디만 입력하면 `@giamicro.com`이 자동으로 붙습니다. 개인 교직원 계정은 지금처럼 회사 구글 계정으로 로그인합니다.
- **공용 계정 관리 화면**(관리자 > 공용 계정 관리): 계정을 만들고 비밀번호를 정하거나 바꿉니다.
  - [자동 생성]을 누르면 브라우저가 예측 불가능한 16자리를 만듭니다. `0`과 `O`처럼 헷갈리는 글자는 빠져 있어 종이에 적어 전달하기 좋습니다.
  - **비밀번호는 한 방향으로만 저장되어 관리자도 다시 꺼내볼 수 없습니다.** 저장 직후 한 번만 크게 표시되니 그때 적어두셔야 하고, 잊어버리면 새로 정하면 됩니다.
  - 쓰지 않게 된 계정은 지우지 말고 [사용중지]로 잠급니다(지우면 그 계정이 남긴 기록의 연결이 끊깁니다).

- **신입교사 오리엔테이션용 데모 계정 + 더미 학생**(요청: "매번 신입선생님들이 오면 설명을 해야 하니 아예 가계정 하나와 더미데이터로 어떻게 써넣으면 될지 알려줄 수 있게"): `gia-demo` 계정으로 로그인하면 **교사 화면이 실제와 완전히 똑같이 보이지만 학생은 전부 가짜**입니다. 마음껏 눌러보고 저장해도 실제 기록에는 아무 영향이 없습니다.
  - 3학년 Demo 반 학생 8명. 설명할 거리가 나오도록 상황을 섞었습니다 - 알러지가 있는 학생, 악기 수업을 듣는 학생, 방과후가 있는 학생, 형제자매가 함께 다니는 경우, 셔틀을 타지 않는 학생.
  - **미리 작성된 관찰기록 두 건**(발행 완료 1건 · 임시저장 1건)이 들어 있어, "어느 정도로 적으면 되는지"를 완성된 예시로 바로 보여줄 수 있습니다.
  - 픽업 체크 화면에는 6명이 데모 노선에 배정되어 있고 2명은 셔틀 미탑승이라, 두 경우가 어떻게 다르게 보이는지 함께 설명됩니다.
  - 화면 위에 **노란 띠로 "연습용 계정입니다"**가 항상 떠 있어, 설명을 듣는 분이 실제 기록을 건드리고 있다고 오해하지 않습니다.
  - **격리 방식**: 표를 새로 만들지 않고 기존 표에 `is_demo` 칸 하나만 붙이고 보안규칙(RLS)으로 갈랐습니다. 데모 계정에는 데모 행만, 나머지 모두에게는 실제 행만 보여 서로를 아예 볼 수 없습니다. 화면 코드는 하나 그대로라 "데모에서는 되는데 실제에서는 안 되는" 차이가 생기지 않습니다.
  - 학생 명부 관리 화면과 사무실 대형 모니터 대시보드에는 데모 학생이 나타나지 않습니다(대시보드는 보안규칙을 통과하는 토큰 링크라 조회 조건을 직접 붙였습니다).

## v0.132.0 - 2026-08-21

- **병합 시 정리**: 도서관 브랜치를 운영앱에 합치면서 `supabase/schema.sql`(전체 구조 참고 문서)에 붙여넣기 하다 남은 군더더기 두 줄(`end if; end $$;`)을 지웠습니다. 이 오류로 참고 문서 전체가 문법 검사에서 실패하던 상태였습니다. **실제로 DB에 적용되는 마이그레이션 파일에는 없던 문제라 데이터에는 영향이 없습니다.**

- **도서관 구역(책장 위치) 체계 신설**(요청: "장서목록을 검색하고 볼 수 있게 해주고, 그 장서가 어느 구역에 있는지도 잘 찾을 수 있게... 반납하고나서도 아무데나 꽂아 넣는게 아니라 정해진 위치에 다시 넣을 수 있도록"): 지금까지 책 위치는 자유 입력 글자 한 칸이라 사람마다 다르게 적으면 검색이 안 됐습니다. 구역을 별도 표(`lib_locations`)로 올리고 책에 연결합니다.
  - 구역 이름 체계는 학교가 정하는 대로 자유롭게 씁니다(`A-1` 같은 칸 번호도, `그림책` 같은 분류도 가능).
  - **도서관 배치도**: 구역마다 평면도 좌표(`map_x/y/w/h`)를 담아, 화면에서 네모를 끌어다 놓으면 배치도가 완성됩니다(요청: "책장구조를 알려줄게 그러면 화면에 책장화면을 간단한 벡터로 넣어주고, 그 구역을 보여줘서 찾아 넣을 수 있게"). 책을 찾을 때·반납한 책을 꽂을 때 그 자리가 반짝입니다.
  - **반납 정리**: `lib_loans.reshelved_at`을 두어 "반납받았지만 아직 안 꽂은 책"을 구역별로 묶어 보여줍니다. 반납함에 들어온 책도 같은 목록에 나타납니다.
  - 책장 칸에 붙이는 구역 라벨(`LOC-A-1`)을 찍고 책을 이어서 찍으면 한꺼번에 배정됩니다.
- **도서카드 배경 그림·사진**(요청: "도서카드 배경 그림을 보내주면 거기에 학생의 이름과 학생고유바코드를 넣어서 인쇄... 사진을 넣을 수도 있고, 사진없이 이름만 넣어서 뽑을 수 있도록"): 배경 그림과 학생 사진을 Supabase 저장소(`library` 버킷)에 올리고, `lib_settings`/`lib_student_photos`에 주소를 남깁니다. 바코드는 배경이 어떤 그림이든 **항상 흰 바탕 위에** 얹혀 인쇄되어 스캔이 됩니다.
- **이름으로도 대출 가능**(요청: "종이카드로 사용하기 때문에 분실하거나 잊어버릴 수 있어서, 이름으로도 출입이나 대여를 할 수 있게"): 도서관 앱 스캔칸에 학생 이름을 입력하면 명부에서 찾아주고, 동명이인이면 골라서 진행합니다. 표 변경 없이 도서관 앱에서만 처리합니다.

## v0.131.0 - 2026-08-20

- **도서관 시스템(gia-lib) 기반 신설**(요청: "학생들이 도서관을 이용할때 바코드로 학생도서카드를 만들어서, 바코드를 찍으면 누가, 어떤 책을 이용하는지에 관한 데이터를 기록하고... 나중에 확장하면 디지털 학생증처럼 학생의 출결이나, 행사입장, 물건구입 등을 학생카드로 통합 관리"): 대출·반납 화면은 별도 앱(`gia-lib-web`)으로 만들되 **데이터는 이 프로젝트와 같은 DB를 씁니다**(요청: "앱은 별도로, 데이터는 하나로"). 이 저장소에는 표와 보안규칙만 추가됩니다.
  - 새 표 - `lib_books`(장서) / `lib_loans`(대출·반납 기록) / `lib_visits`(도서관 입실, 2단계용으로 표만 미리 준비) / `lib_settings`(대출 규칙 - 2주 · 1인 3권 · 연장 +7일 1회 · 연체 시 대출 정지).
  - **학생은 이미 있는 `student_no`(GIA-2026-0001)를 그대로 카드 바코드로 씁니다.** 학년·반이 바뀌어도 번호는 그대로라 카드를 다시 만들 필요가 없고, 나중에 출결·행사입장·매점으로 확장해도 **같은 카드**를 씁니다.
  - **도서관용 공용 명부(`lib_students`) 신설**: 고유번호·이름·영어이름·학년·반·부서만 담았습니다. 보호자 연락처·주소·생년월일·알러지 등은 넘어가지 않습니다. 교직원 공용 명부(`wr_students_basic`)에는 개인정보 보호를 위해 학번이 빠져 있어 도서카드 조회가 불가능하므로 별도로 만들었습니다.
  - 책은 뒷면에 인쇄된 **ISBN 바코드를 그대로 식별번호로 씁니다**(라벨 부착 작업 없음). ISBN이 없는 책만 자체 라벨(GIA-B-00001)을 `lib_next_item_code()`로 발급합니다.
- **도서관 전용 가계정 권한 분리**(요청: "도서관 데이터만 접근 허용"): 도서관 노트북은 `gia-library@giamicro.com` 같은 전용 계정으로 하루 종일 로그인된 채 열려 있습니다. 이 계정이 운영앱 데이터까지 볼 수 있으면 사건기록·보호자 연락처가 그대로 노출되므로 두 겹으로 막았습니다.
  - `is_giamicro_user()`가 `gia-library`로 시작하는 계정을 제외하도록 바꿨습니다. 운영앱의 정책들이 전부 이 함수를 쓰고 있어서 **함수 한 곳만 고치면 모든 표가 자동으로 차단됩니다.** 도서관 표는 새로 만든 `is_lib_user()`를 씁니다.
  - 미들웨어에서도 운영앱 화면 진입 자체를 막고, 로그인 화면에 "도서관 전용 계정은 운영앱에 접속할 수 없습니다" 안내를 띄웁니다.
  - 계정을 더 만들 때는 `gia-library2@giamicro.com`처럼 `gia-library`로 시작하게만 지으면 같은 규칙이 자동 적용됩니다.
  - **구글 계정을 새로 만들 필요가 없습니다**(요청: "실제 구글계정을 등록할 필요 없이 가계정으로 만들어서 운영앱에서 관리"). 도서관 노트북은 Supabase Auth의 아이디+비밀번호 가계정으로 들어옵니다. 이 가계정을 `app_users`에 미리 등록해 두어 **운영앱 계정 관리 화면에 "GIA 도서관(공용 단말)"로 함께 나타나고, 승인을 취소하면 도서관 노트북도 즉시 막힙니다.**

## v0.130.1 - 2026-08-21

- **운영 대시보드 시계에 초 표시**(요청: "대시보드 분말고 초까지 나오도록 해줘"): 그동안은 서버에서 받은 시각을 그대로 보여줘서 갱신 주기(30초)마다만 바뀌었는데, 이제 화면에서 1초마다 도는 시계로 바꿨습니다. 하원 운행 화면 상단에도 같은 시계를 넣었습니다. 노트북 시간대 설정과 무관하게 항상 한국시간으로 표시되고, 숫자 폭이 고정되어 초가 바뀔 때 글자가 흔들리지 않습니다.

## v0.130.0 - 2026-08-20

- **교사 화면 한영 병기 원칙 적용**(요청: "교사들이 사용하는 메뉴의 경우 무조건 한영 병기를 원칙으로 해주고"): 원어민 선생님도 통역 없이 쓸 수 있도록 교사가 보는 모든 화면에 영어를 함께 적었습니다.
  - 사이드바 메뉴(내 담임반 My Homeroom · 내 담당과목 My Subjects · 내 반 픽업 체크 My Class Pickup Check), 로그아웃 Sign out.
  - 화면 제목·안내문·가이드·빈 화면 문구까지 모두 병기했습니다. 예: "담임으로 배정된 반이 없습니다 / No homeroom class is assigned to you."
  - 픽업 체크 버튼도 "픽업 Pickup", "결석 Absent"처럼 병기하고, 셔틀을 타지 않는 학생은 "셔틀 미탑승 No shuttle"로 표시합니다.
- **교사도 문의 창구를 쓸 수 있게 개방 + 한영 병기**(요청: "문의할 수 있는 창구도 한글/영어 병기할 수 있게 해줘"): 그동안 문의및건의사항은 교사에게 아예 보이지 않았는데, 이제 사이드바 아래(모바일은 상단 메뉴 오른쪽)에서 열 수 있습니다. 카테고리(🐞 오류 Bug · 💡 기능제안 Idea · 💬 기타 Other), 입력 항목(제목 Title · 내용 Details), 처리 상태(접수 Received · 처리중 In progress · 완료 Done)를 모두 병기했고, **한국어와 영어 아무 쪽으로 써도 된다**는 안내를 넣었습니다.

## v0.129.0 - 2026-08-20

- **교사용 '내 반 픽업 체크' 신설**(요청: "교사가 전화나, 다른 메세지로 픽업을 받은 경우, 체크를 할 수 있도록... 담임교사는 자기 반만 보이고, 과목교사선생님은 보이지 않도록"): 학부모님께 전화·문자로 직접 픽업 연락을 받으셨을 때 담임 선생님이 바로 체크할 수 있는 화면입니다.
  - **담임에게만 보입니다.** 담임(또는 부담임)으로 배정된 반이 있는 선생님께만 메뉴가 나타나고, 과목만 맡으신 선생님께는 아예 뜨지 않습니다. 보이는 학생도 내 담임반 학생뿐입니다.
  - 이름 옆 버튼으로 **픽업·결석**을 누르면 즉시 저장되고, 한 번 더 누르면 취소됩니다. 셔틀을 타지 않는 학생은 회색으로 표시되고 버튼이 없습니다.
  - 저장되는 곳이 하원 체크표와 같아서, 누르는 즉시 **안내보드·차량 도착체크·하원 운행 화면에 반영**됩니다. 행정실에서 먼저 체크하면 선생님 화면에도 실시간으로 나타납니다.
  - **기존 구글챗 자동 반영과 병행합니다.** 같은 칸을 쓰기 때문에 두 방식이 충돌하지 않고, 구글챗으로 알리신 건은 지금처럼 자동으로 반영됩니다.
- **교사 메뉴에서 실시간 셔틀 제거**(요청: "교사화면에서 실시간 셔틀은 안보여도 되고"): 교사 사이드바에서 실시간 셔틀이 빠지고 그 자리에 픽업 체크가 들어갑니다. 화면 접근도 함께 막았습니다. 관리자·행정직원은 기존대로 실시간 셔틀을 볼 수 있습니다.

## v0.128.0 - 2026-08-20

- **하원 운행 화면 신설**(요청: "셔틀시작시간때(4:00)가 되면 화면이 전환되면서 실시간 셔틀 운행지도가 뜨고 지도에서 각 셔틀이 어떤 경로로 가고있는지 볼 수 있게 하면서, 아래쪽에는 아이들이 차량을 다 탑승했는지 하원차량 체크화면이 뜨고"): 운영 대시보드가 설정 시각(기본 16:00)이 되면 안내보드 대신 **하원 운행 전용 화면**으로 바뀝니다.
  - **위쪽 - 전체 셔틀 실시간 지도**: 모든 하원 노선을 한 지도에 올리고, 기사님 휴대폰(Traccar)에서 들어오는 최신 위치로 차량 마커가 움직입니다. 노선마다 색을 달리하고, **운행 중인 노선은 경로선을 진하게** 그려 지금 어디를 지나는지 한눈에 보입니다. 실도로 경로가 계산되어 있으면 그 경로를, 없으면 정류장을 이은 선을 씁니다.
  - **아래쪽 - 하원차량 체크**: 노선별로 대기·도착함·운행중 상태와 **탑승 진행률(탑승/예정)**, 아직 안 탄 학생 이름, 도착·출발 시각을 보여줍니다. 픽업·결석 학생은 탑승 대상에서 자동으로 빠집니다.
  - **자동 감지 표시**(요청: "기사님의 핸드폰을 통해서 추척하고 더 정확하게 매번 자동으로 수정"): 도착·출발이 사람이 눌러서 기록된 것인지 GPS가 자동으로 잡은 것인지 "·자동"으로 구분해 표시하고, 노선별 GPS 수신 여부도 함께 보여줍니다.
  - 10초마다 자동 갱신되며, 전환할 안내보드를 따로 고르지 않아도 시각만 지나면 바로 전환됩니다.

## v0.127.1 - 2026-08-20

- **셔틀 배정표의 보호자 연락처도 보호**(요청: "보호자 연락처, 이메일, 주소, 좌표, 학번의 경우 행정직원과 관리자만 볼 수 있도록해줘"): 학생 명부는 앞 버전에서 막았지만, **같은 보호자 연락처가 셔틀 배정표에도 한 벌 더 들어 있었습니다.** 이 표는 동승선생님이 교사 계정일 수 있어 조회를 열어둔 상태여서, 화면에는 안 보여도 데이터로는 교사가 읽을 수 있었습니다. 이제 원본 배정표는 행정직원·관리자·개발자만 읽을 수 있고, 하원 체크표·실시간 셔틀은 보호자 연락처가 빠진 공용 배정표를 보도록 바꿨습니다. 동승선생님 업무에는 변화가 없습니다.

## v0.127.0 - 2026-08-20

- **학생명부 권한 정리**(요청: "이명부에 관한 권한은 행정직원,관리자,개발자로 할게"): 지금까지 학생 명부는 giamicro.com 계정이면 **누구나 읽고 고칠 수 있는** 상태였습니다. 이제 원본 명부는 **행정직원·관리자·개발자만** 읽고 쓸 수 있고, 반 명부도 수정은 같은 권한으로 제한했습니다(반 이름 읽기는 수업·출결 화면이 필요해서 전체 허용).
- **교직원 공용 명부 신설**(요청 항목 그대로): 일반 교직원은 새로 만든 공용 명부에서 **이름·영어이름, 생년월일, 성별, 방과후 수업 여부, 악기, 셔틀 탑승 여부, 특이사항(알러지·메모), 형제자매 링크**만 볼 수 있습니다. **보호자 연락처·이메일·주소·좌표·학번은 빠졌습니다.** 관찰기록·출결·셔틀·검색 화면은 자동으로 이 공용 명부를 보도록 바꿔서 기존 사용에는 변화가 없습니다.
- **새 항목 추가**: 방과후 수업 여부, 악기(첼로·우쿨렐레·클라리넷·바이올린·플룻 중 하나), 형제자매 묶음. 형제자매 묶음은 같은 집 아이들에게 같은 값을 넣어두면 **부서를 넘나들어도(유치부 동생 ↔ 초등부 형) 한 가족으로 이어집니다.**
- **다른 프로그램 연결 대비**: 나중에 유치부 프로그램을 붙일 때 이 공용 명부만 보게 하면 개인정보를 넘기지 않고 학생을 연결할 수 있습니다.
- 개발자 계정은 `is_app_admin()`에 항상 포함되어 있어, 앞으로 만드는 권한에서도 별도 처리 없이 최상위로 동작합니다.

## v0.126.1 - 2026-08-20

- **부서 구분을 명시적으로 저장**(요청: "부서는 유치부는 통합하지말고 따로 만들어 달라고 하셨어... 앞으로도 저런형식을가진 아이들을 유치부로 분류하고"): 지금까지는 학년 표기의 글자 모양을 보고 부서를 추측했는데, 표기가 조금만 달라져도 엉뚱하게 분류될 수 있었습니다. 학생·반에 **부서 칸(유치부/초등부/중고등부)을 따로 추가**해서 앞으로는 추측 없이 이 값만 보고 판단합니다. 기존 데이터는 예전 규칙대로 한 번 자동으로 채워집니다.
- **유치부 화면에서 분리**(요청: "유치부는 우선 분리해서 표면적으로는 안보이게 해줘"): 운영 대시보드와 시간표 관리 화면의 부서 선택에서 유치부가 빠지고 초등부·중고등부만 보입니다. **데이터는 지우지 않고 그대로 보관**하므로, 나중에 유치부 프로그램을 만드실 때 그대로 쓰실 수 있고 다시 보이게 하는 것도 한 줄 수정이면 됩니다.
- **대시보드 '지금 수업'을 학년별로 묶어 표시**(요청: "각 학년과 반별로 어느수업이 진행되는지 뜨도록"): 학년이 왼쪽에 세로 라벨로 붙고 그 학년의 반들이 오른쪽에 나란히 놓여, 학년 단위로 훑어보기 쉬워졌습니다. 학년 정렬도 1·2·3학년 순서대로 맞췄습니다(중1은 초6 다음으로).

## v0.126.0 - 2026-08-20

- **사무실 대형 모니터용 통합 운영 대시보드 신설**(요청: "gia운영에 있는 업무 탭을 사무실 가운데에 큰 모니터에 띄워서 전체가 한눈에 보고 파악할 수 있는 통합 대시보드... 페이지를 반으로 나눠서 한쪽은 cctv 그리고 한쪽은 우리 gia운영 앱"). 화면 절반에 띄우는 것을 전제로, 멀리서도 읽히는 큰 글씨의 어두운 화면으로 만들었습니다. 30초마다 자동 갱신되어 하루 종일 켜두면 됩니다.
  - **지금 수업**: 부서별 교시 시각을 기준으로 "지금 몇 교시"를 판단해, 각 반이 무슨 수업 중인지와 다음 교시 과목을 함께 보여줍니다.
  - **오늘 출결·픽업**: 결석·지각·조퇴 학생을 상태별 색으로 표시하고, 보호자 연락 전인 학생은 따로 표시합니다. 하원 체크표에서 픽업으로 찍힌 학생도 함께 보여줍니다.
  - **오늘 업무**: 기한이 지난 업무 → 오늘 마감 → 오늘 등록 순으로 정렬하고 긴급은 위로 올립니다. 상단에 상태별 전체 건수도 함께 표시합니다.
  - **부서 전환**: 화면에서 유치부·초등부·중고등부를 눌러 바꿀 수 있습니다.
  - **하원 시간 자동 전환**(요청: "오후 4시쯤에 하원차량 픽업이 시작되기때문에 그 때에는 자동으로 차량 상황을 볼 수 있게끔"): 설정한 시각(기본 16:00)이 되면 화면 전체가 하원 차량 안내보드로 바뀝니다. 전환 시각과 띄울 안내보드는 관리자 화면에서 고릅니다.
  - **로그인 불필요**: 안내보드와 같은 토큰 링크 방식이라 하루 종일 켜두어도 세션이 풀리지 않습니다.
- **시간표 기능 신설**: "지금 각 반이 무슨 수업인지"를 알려면 필요한 데이터가 시스템에 없어서 새로 만들었습니다. 왼쪽 메뉴 **운영 대시보드**에서 부서별 교시(시작·종료 시각)를 정하고, 반×요일×교시 표에 과목명을 채워 넣으면 됩니다. 칸을 비우면 지워집니다. **아직 데이터는 비어 있으니 시간표를 넣어주시면 대시보드에 바로 반영됩니다.**

## v0.125.1 - 2026-08-20

- **Supabase 스키마 자동 반영**(요청: "supabase도 자동으로 업데이트 하게 만들수 있어?"): 그동안은 새 기능을 배포할 때마다 SQL을 Supabase SQL Editor에 직접 붙여넣어야 했는데, 이제 main에 머지되면 GitHub Actions가 `supabase/migrations/` 폴더에서 **아직 반영되지 않은 파일만** 골라 자동으로 실행합니다. 이미 실행한 파일은 DB에 기록이 남아 두 번 실행되지 않습니다.
  - 지금까지 안내드렸던 v0.122·v0.124·v0.125 SQL을 첫 마이그레이션 파일로 정리해뒀습니다. 모두 "없으면 만들기" 방식이라 이미 직접 실행하셨더라도 다시 돌아가며 문제가 생기지 않습니다.
  - `supabase/schema.sql`은 전체 구조를 한눈에 보는 참고 문서로 그대로 유지합니다.
  - **저장소 Settings → Secrets and variables → Actions 에 `SUPABASE_DB_URL` 시크릿을 등록해야 동작합니다.** 등록 전까지는 워크플로가 실패하지 않고 안내만 남기고 넘어갑니다.

## v0.125.0 - 2026-08-20

- **업무 보드 전체공지**(요청: "업무에서 전체공지가 있을경우 바로 상단으로 옮겨지고, 새로운 공지가 있으면 이전공지가 사라지고, 다음공지가 상단으로 옮겨지게 하고, 전체공지 히스토리를 상단오른쪽에 히스토리 아이콘을 눌러서 볼 수 있도록 만들어주고, 공지로 상단에 뜨는경우, 각각의 이용자들이 공지를 접을 수 있게 해줘"):
  - **상단 배너**: 부서 탭 바로 아래, 업무 화면 맨 위에 📢 공지 배너가 뜹니다. 항상 **가장 최근 공지 하나만** 표시되므로, 새 공지를 올리면 이전 공지는 자동으로 내려갑니다(따로 내리는 조작이 필요 없습니다).
  - **개인별 접기**: 배너를 누르면 접혔다 펴집니다. 접은 상태는 **사람마다 따로** 저장되어 다른 분 화면에는 영향이 없고, 다른 기기에서 로그인해도 그대로 유지됩니다. 접어둔 뒤 새 공지가 올라오면 그 공지는 다시 펼쳐진 채로 보여서 새 공지를 놓치지 않습니다.
  - **히스토리**: 배너 오른쪽 끝 시계 아이콘을 누르면 지난 공지를 최신순으로 모아 봅니다. 현재 상단에 떠 있는 공지에는 '현재' 표시가 붙습니다. 이전 공지는 지워지지 않고 모두 보관됩니다.
  - **공지 올리기**: 관리자·행정직원에게만 "✏️ 공지" 버튼이 보입니다. 올릴 때 **전체 부서 공통**인지 **지금 보고 있는 부서만**인지 고를 수 있고, 제목과 내용(선택)을 적으면 됩니다. 잘못 올린 공지는 "내리기"로 감출 수 있습니다.
  - 다른 사람이 공지를 올리면 새로고침 없이 실시간으로 화면에 뜹니다.

## v0.124.0 - 2026-08-13

- **기사님 휴대폰 GPS 추적 도입 (Traccar Client 연동)**(요청: "기사님들은 네비를 핸드폰으로 하시는 경우도 많아서, 핸드폰을 조작하는데 이 앱이 방해를 해서는 안돼, 즉, 백그라운드에서 돌아갈 수 있도록 만들어야 하고"): 웹페이지는 아이폰 사파리 특성상 백그라운드에서 위치를 보낼 수 없어서, 무료 오픈소스 앱인 **Traccar Client**가 우리 서버로 위치를 직접 보내도록 연동했습니다(`/api/shuttle/track`). 기사님은 최초 1회 설정 뒤로는 아무 조작도 하지 않으시고, 네비 화면은 전혀 가려지지 않습니다. 관리자 화면(셔틀 → 실시간 위치 링크 관리)에서 노선별 기기 ID를 발급하고 설치 안내를 볼 수 있습니다.
- **개인정보 최소 수집**: 기사님 개인 휴대폰을 쓰는 방식이라, **하원 시간대(평일 12~20시) 밖의 위치는 서버가 아예 저장하지 않습니다.** 앱은 24시간 켜져 있어도 그 밖의 좌표는 받는 즉시 버립니다. 도입 전 기사님께 이 내용을 설명하고 개인정보 수집·이용 동의를 받아두시기 바랍니다.
- **학교 도착 자동감지**(요청: "학교근처에 오면 자동으로 도착알림이 뜨고, 출발하게 되면 출발 표시가 뜨도록"): 새 크론(`/api/cron/shuttle-auto-arrive`)이 차량이 학교 반경 100m 안에 들어와 1분 이상 머무르면 사람이 누르지 않아도 "현장도착"을 자동으로 기록합니다. 이어서 기존 출발 자동감지가 "출발"을 채워, 하원 한 바퀴가 조작 없이 완결됩니다. **외부 스케줄러(cron-job.org 등)에 `/api/cron/shuttle-auto-arrive`를 1분마다 호출하도록 하나 더 등록해주셔야 합니다**(기존 출발 자동감지와 동일한 방식, `Authorization: Bearer {CRON_SECRET}` 헤더 필요).
- **정류장 좌표 GPS 자동 학습**(요청: "각 정류장도 우리는 지금 정확한 정보를 가지고 있지 않아서, gps를 통해서 정류장과, 도착 또한 gps를 계속 갱신해서 정확도를 높여서 정류장도 파악이 되도록 만들어줘"): 새 크론(`/api/cron/shuttle-learn-stops`, 매일 자동 실행)이 그날 주행 기록에서 차가 실제로 45초 이상 멈춰 있던 자리를 찾아내 기록합니다(신호 대기는 걸러집니다). 같은 자리가 반복 관측될수록 평균 좌표가 정확해지고, 관리자 화면에서 "기존 좌표와 몇 m 차이"까지 확인한 뒤 "반영" 버튼으로 실제 정류장 좌표에 옮길 수 있습니다. 기존 좌표를 임의로 덮어쓰지 않으므로 잘못 학습돼도 원래 값을 잃지 않습니다. 어느 정류장인지 자동으로 연결되지 않은 정차 지점은 따로 모아 보여줘, 담당자가 직접 지정하거나 누락된 정류장을 발견할 수 있습니다.

## v0.123.0 - 2026-08-12

- **동승선생님 화면: 위치 전송 완전 자동화**(요청: "기사님들 귀찮으시다면 우리가 그냥 자동으로 등/하원시간에 gps 켜고 끄도록 해드릴 수 있을까?... 기사님들이 버튼을 눌러달라고 하면 운행하느라 빼먹을 경우가 많아서 키고 끄는 걸 우리가 제어하게끔 해줘"): 동승선생님 체크인 화면에서 더 이상 "이동 시작/운행 종료" 버튼을 누르지 않아도, 학교 '현장도착'이 확인되면 자동으로 위치 전송이 시작되고(요청: "gia출발부터 마지막 정류장 도착까지 켜두고 계속 주기적으로 전달"), 오늘 실제 학생이 있는 마지막 정류장 근처(약 100m, 3회 연속 확인)에 닿으면 자동으로 멈춥니다. 자동 감지가 늦거나 안 될 때만 쓰시라고 작은 예비 버튼은 남겨뒀습니다. 위치는 이전과 동일하게 학부모님께는 전달되지 않고, 담당 직원의 도착체크·안내보드·자동출발 감지에만 쓰입니다.
- **백그라운드(네비 앱 사용 중) 보완**(요청: "핸드폰을 조작하는데 이 앱이 방해를 해서는 안돼... 백그라운드에서 돌아갈 수 있도록"): 완전한 백그라운드 위치 전송은 브라우저 특성상(특히 아이폰 사파리) 불가능하지만, 기사님·동승선생님이 네비 앱 등으로 전환했다가 화면으로 돌아오는 순간 즉시 한 번 더 전송해서 끊긴 공백을 최대한 빨리 메웁니다.
- **부수 정리**: 동승선생님 화면이 더 이상 "출발" 이벤트를 직접 기록하지 않도록 바꿔, v0.122.0에서 추가한 "노선당 하루 1개" 제약(학교 도착체크·GPS 자동감지 크론 전용)과 충돌하지 않게 했습니다. 혹시 모를 충돌에 대비해 관련 API도 중복 기록을 에러 대신 정상 처리하도록 보강했습니다.
- **도착체크(교직원용) 화면에 차량번호 표시**(요청: "차량도착 출발체크 교직원용에 차 번호도 나오게 해줘 작게"): 각 차량 카드에 노선명 아래 차량번호를 작은 글씨로 함께 표시합니다.

## v0.122.0 - 2026-08-11

- **도착체크: 출발 자동감지**(요청: "여러대가 한꺼번에 도착해서... 출발하는 것을 체크하는걸 까먹거나, 늦어져서 계속 화면에 차량이 뜨는 경우가 너무 많아"): 새 크론(`/api/cron/shuttle-auto-depart`)이 두 가지 신호로 "출발"을 자동으로 채웁니다. ① 그 노선에서 파일럿(GPS) 체크인이 켜져 있으면, 학교에서 100m 이상 멀어진 최근 위치가 잡히는 순간 실제 출발로 보고 자동 처리합니다(도착 후 최소 90초는 기다려 오작동을 막습니다). ② GPS 핑이 아예 없는 노선은 "도착함" 후 20분이 지나면 화면 정리 차원에서 자동 처리합니다. 자동으로 처리된 카드에는 "자동·GPS 감지" 또는 "자동·시간 초과"로 작게 표시해 사람이 누른 것과 구분됩니다. **적용하려면 poll-chat-messages와 같은 방식으로 외부 무료 스케줄러(cron-job.org 등)에 `/api/cron/shuttle-auto-depart`를 1분마다 호출하도록 하나 더 등록해주셔야 합니다** (기존 가이드와 동일한 방식, `Authorization: Bearer {CRON_SECRET}` 헤더 필요).

## v0.121.3 - 2026-08-11

- **출결내역 @멘션 필터링 개선**(요청: "구글챗 필터링할 때 @뒤에는 이름/성으로 이루어져있어서 무조건 제거해주고, 그 이후에 이름을 유추하는 필터링이 되었으면 좋겠어"): 구글챗 멘션이 "@Teneqha Form Jino Park will be absent tomorrow"처럼 영문 두 단어(이름+성)로 된 경우, 이전에는 "@" 뒤 첫 한 단어("Teneqha")만 지워져 두 번째 단어("Form")가 그대로 남아 있었습니다. 이제 대문자로 시작하는 영문 단어가 바로 이어지면 최대 두 단어까지 함께 지우고(뒤에 이어지는 진짜 이름은 건드리지 않음), 한글 멘션은 원래대로 한 단어만 지운 뒤 이름 유추를 진행합니다.

## v0.121.2 - 2026-08-11

- **안내보드, 전자칠판 내장 브라우저에서 스타일이 안 먹던 문제 대응**(요청: "전자칠판 내부 브라우저로 안내보드를 여니까 글만 나오고 적용이 안돼"): 전자칠판 내장 브라우저는 오래된 엔진인 경우가 많아, 앱이 쓰는 최신 CSS 문법을 못 읽으면 스타일이 통째로 빠지고 글자만 남을 수 있습니다. 원인이 되는 정확한 브라우저 종류를 특정하기 어려워, 안내보드의 핵심 화면(배경·차량 도착 카드·학생 이름)에 어떤 브라우저든 항상 그대로 읽는 순수 인라인 스타일을 함께 넣어, 외부 스타일시트가 전혀 안 먹는 경우에도 최소한 배경·글자 크기·강조색은 보이도록 안전장치를 추가했습니다.

## v0.121.1 - 2026-08-11

- **홈 화면 추가 안내는 처음 한 번만**(요청: "추가자동안내는 초기 한번만 해주고"): 도착체크 화면의 홈 화면 추가 안내 배너가 며칠마다 다시 뜨던 것을, 기기당 처음 한 번만 보여주고 이후엔 뜨지 않도록 바꿨습니다.
- **전체 리셋 버튼을 리셋·안내 아이콘으로 분리**(요청: "전체 리셋을 반으로 잘라서, 전체리셋은 그냥 원 화살표(리셋로고로 많이씀)와 물음표로 아이콘만 띄워서 누르면 리셋은 리셋되고, 물음표는 안내하도록"): "⟲ 전체 리셋" 글자 버튼을 절반씩 나눠, 왼쪽 ⟲ 아이콘은 누르면 바로 리셋되고 오른쪽 ? 아이콘은 화면 사용법(버튼 누르는 순서, 꾹 눌러 기사님께 전화, 리셋 의미 등)을 안내하는 팝업만 띄웁니다.
- **출결내역 형제자매 이름 대조 개선**(요청: "박라온, 박다온 같은 형제자매가 있을 경우 잘 안돼고... 박라온,다온/라온다온과 같이 한쪽만 성을 쓰거나 아니면 둘 다 안 쓰는 경우도 있어 이 경우도 각각 파악해서"): 형제자매 중 한 명만 성을 쓰고 나머지는 이름만 쓴 경우("박라온, 다온")와, 성 없이 이름끼리 붙여 쓴 경우("라온다온") 모두 명부와 대조해 각각 따로 인식하도록 로직을 보강했습니다. 명부에 실제로 있는 이름 조합일 때만 인정하므로, 엉뚱한 낱말이 이름으로 잘못 잡힐 위험은 낮습니다.

## v0.121.0 - 2026-08-11

- **교직원 도착체크 링크, 홈 화면에 더 쉽게 추가**(요청: "못하는 사람들 하나하나 찾아다니면서 설명할 수가 없어, 더 간단하게 자기 핸드폰에 추가하게 하는 방법 없을까?"):
  - **QR코드**: 링크 관리 화면(관리자)에서 "📱 QR코드" 버튼을 누르면 그 자리에서 QR코드가 생성되고, 이미지로 저장해 카톡방 등에 바로 공유할 수 있습니다. 상대방은 카메라로 스캔만 하면 주소를 직접 입력하거나 복사·붙여넣기 하지 않아도 링크가 바로 열립니다.
  - **홈 화면 추가 자동 안내**: 도착체크 화면을 처음 열면(아직 홈 화면에 추가하지 않은 경우) 아이폰은 "공유 버튼 → 홈 화면에 추가", 안드로이드는 "⋮ 메뉴 → 홈 화면에 추가"를 화면 상단에 자동으로 안내해줘서, 말로 하나하나 설명하지 않아도 화면을 보고 따라 할 수 있습니다. 이미 홈 화면 아이콘으로 실행 중이면 뜨지 않고, 닫으면 며칠간 다시 뜨지 않습니다.

## v0.120.4 - 2026-08-11

- **교직원 도착체크에도 픽업·결석 상태 반영**(요청: "하원체크표에서 결석이나, 픽업을 체크하면 실시간으로 교직원 차량 도착 출발체크에 반영이 되고 안내보드에도 반영되도록"): 안내보드에는 이미 반영되고 있었지만, 도착체크 화면은 학생 이름만 보여줄 뿐 픽업·결석 여부를 몰라서 이미 픽업/결석 처리된 학생도 계속 "미도착" 빨간 뱃지로 남아 있었습니다. 이제 도착체크도 안내보드와 같은 방식으로 shuttle_boardings의 상태를 함께 조회해, 픽업·결석 학생은 명단에서 빠지고 카드 아래에 "픽업 N · 결석 N"으로만 작게 표시됩니다. 두 화면 모두 3초 폴링이라, 체크표에서 체크하면 몇 초 안에 자동으로 반영됩니다.

## v0.120.3 - 2026-08-11

- **버그 수정: 교직원 도착체크 단독링크를 아이폰 홈 화면에 추가하면 그 링크가 아니라 앱 메인이 등록되던 문제**(요청: "교직원 도착체크 단독링크 아이폰에 아이콘으로 등록하려하니까 그냥 메인만 등록되고 이 링크 그대로는 아이콘 등록이 안돼"): iOS Safari는 16.4부터 "홈 화면에 추가"할 때 페이지에 연결된 Web App Manifest의 start_url을 우선 따라가는데, 이 앱의 manifest(`start_url=/home`)가 전체 사이트 루트에 걸려 있어 안내보드·도착체크·파일럿 체크인 같은 로그인 없는 토큰 링크도 전부 앱 메인으로 등록되고 있었습니다. manifest를 로그인 사용자 영역((dashboard))에만 적용되도록 좁혀서, 토큰 링크는 이제 지금 보고 있는 그 주소 그대로 홈 화면에 등록됩니다.

## v0.120.2 - 2026-08-11

- **셔틀 실시간 반영 속도 최적화** (요청: "실시간 반영 속도 더 개선"):
  - **하원 체크표**: 실시간 이벤트가 오면 테이블을 통째로 다시 조회하던 것을, 이벤트에 이미 담겨 온 값을 서버 왕복 없이 곧바로 화면에 반영하도록 바꿨습니다.
  - **실시간 셔틀(/shuttle/live)**: 7초 폴링을 걷어내고 위치·도착체크·탑승체크 세 테이블 모두 Realtime 구독으로 전환해, 다른 사람의 체크가 사실상 즉시 반영됩니다.
  - **파일럿 모니터링**: 노선마다 따로 조회하던 쿼리(파일럿 10개면 30개 요청)를 노선 전체를 한 번에 묶어 조회하도록 고쳤고, 위치 데이터도 필요 이상으로 가져오지 않게 시간 범위로 제한했습니다.
  - **안내보드·도착체크 API**: 서로 관계없는 조회를 순서대로 기다리지 않고 동시에 실행하도록 바꿔, 3초마다 폴링하는 두 화면의 응답 속도를 앞당겼습니다.

## v0.120.1 - 2026-08-11

- **사이드바 픽업·결석 이름을 누르면 체크표에서 바로 찾기**: "오늘 픽업·결석" 위젯의 이름을 누르면 오른쪽 체크표에서 같은 이름 뱃지가 노란색으로 강조되며 그 위치로 스크롤됩니다(요청: "왼쪽 위젯에 이름을 누르면 체크표에 있는 같은 이름으로 자동으로 이동하도록... 빠르게 체크할 수 있도록"). 기존 검색창과 같은 방식이라, 눌러도 자동으로 체크되지는 않고 위치만 바로 찾아줍니다 - 실제 픽업/결석 체크는 찾은 뱃지의 🚗/🚫 버튼을 눌러 확정해주세요.

## v0.120.0 - 2026-08-11

- **학생별 특이사항 메모**: 하원 체크표에서 이름 뱃지 오른쪽 위 작은 아이콘을 누르면 그 학생만의 특이사항(알레르기, 하차 시 주의사항 등)을 적을 수 있습니다(요청: "특이사항있는 아이들 아이들별로 뱃지 코너에 메모적을 수 있게"). 적어두면 뱃지 아이콘이 주황색 "!"로 바뀌어 한눈에 보입니다.
- **사이드바에 "특이사항" 위젯 추가**: 메모를 적은 학생들이 "학생이름: 메모" 형태로 왼쪽 사이드바에 모아서 나타납니다(요청: "학생이름: 메모 이렇게 정리되도록").
- **학생 이름 검색 + 하이라이트**: 표 위 검색창에 이름을 치면 그 학생의 뱃지가 노란색으로 바뀌면서 화면이 그 위치로 스크롤됩니다(요청: "이름을 치면 그 학생 이름뱃지 바로 찾을 수 있게... 색이 변해서 어디있는지 바로 알 수 있게끔").
- **버그 수정: "계속 유지" 노선 변경이 관리자·행정직원이 아니면 저장되지 않던 문제**: shuttle_assignments 테이블이 관리자·행정직원 전용 쓰기 권한이라, 다른 로그인 사용자가 "계속 유지"를 선택하면 조용히 실패하고 있었습니다. 이번에 학생별 메모와 함께 전용 API로 우회해 로그인한 사용자 누구나 정상적으로 저장되도록 고쳤습니다.

## v0.119.0 - 2026-08-11

- **하원 체크표 실시간 반영**: 폴링 대신 Supabase Realtime으로 전환해, 다른 사람이 픽업/결석/노선이동을 누르면 화면에 거의 즉시 반영됩니다(요청: "하원체크표에 표시하면 실시간으로 반영되도록").
- **왼쪽 사이드바 스크롤 고정 + "오늘 차량 변경" 위젯 추가**: 화면을 내려도 사이드바가 따라오고(요청: "화면스크롤을 내리면 따라오도록"), 픽업·결석 아래에 오늘 노선이 바뀐 학생을 모아 보여주는 위젯을 새로 추가했습니다.
- **자유로운 차량 전환 + 계속/오늘만 선택**: 이름을 다른 노선으로 드래그하면 "계속 유지"할지 "오늘만" 적용할지 물어봅니다(요청: "부모님이 개인사정으로... 태워달라고 요청할 수도 있기 때문에 자유롭게 전환할 수 있도록... 계속 수정이면 계속 바뀐그대로 고정해주고, 오늘만 타는거라면 오늘만 바뀌고 다음에는 원래대로"). 계속 유지로 바뀐 학생은 안내보드·실시간셔틀·도착체크 화면에도 그대로 반영됩니다.
- **상단에 날짜·탑승예정인원 표시 + 인쇄 기능**: 체크표 위에 오늘 날짜와 탑승예정인원(픽업·결석 제외)이 나타나고, "🖨 인쇄" 버튼으로 표만 깔끔하게 인쇄할 수 있습니다(요청: "표처럼 인쇄가능하도록").
- **모바일 도착체크: 호차 꾹 누르면 기사님께 전화**: 도착체크 화면에서 차량 카드를 길게 누르면 기사님 전화번호로 바로 걸 수 있는 메뉴가 뜹니다(요청: "모바일에서 호차 꾹누르면 기사님께 전화하기 메뉴가 떴으면 좋겠어").

## v0.118.1 - 2026-08-11

- **버그 수정: 출결내역 이름 추정에 "@"가 다시 섞여 나오던 문제**: 문장 맨 앞에 붙은 멘션만 지우던 것을, 문장 어디에 있든 모든 "@멘션"을 지우도록 넓혔습니다(요청: "결석에 @ 또 나왔다"). 또한 이름 후보를 정해진 글자수로 무작정 잘라내던 방식 대신 공백·문장부호로 실제 낱말 단위를 나누고, 결석/픽업 같은 출결 키워드 바로 앞 낱말을 우선으로 보게 해서(요청: "글을 해석해서 이름을 따오게 할 수 있어?") "확인 부탁드려요 정서안 결석입니다"처럼 문장 앞쪽에 다른 말이 섞여 있어도 실제 이름(정서안)을 더 정확히 뽑아냅니다.

## v0.118.0 - 2026-08-11

- **출결내역 이름 추정 개선**: 학생 명부와 대조되지 않을 때 원문을 아무 데서나 잘라 보여주던 것을, 문장 안에서 이름일 가능성이 높은 한글 단어를 먼저 찾아 보여주도록 바꿨습니다(요청: "대조하고 없으니까 그냥 아무거나 표시하는거 같아... 한글이름을 메인으로 뽑아줘"). 이렇게 추정된 이름은 🔎 표시로, 명부에서 확실히 찾은 이름과 구분해서 보여줍니다.
- **하원 체크표에 픽업·결석·출결메모 사이드바 추가**: 체크표(`/shuttle/checklist`) 왼쪽에 좁은 사이드바를 새로 만들어, 업무 메뉴로 가지 않아도 오늘 픽업·결석 학생 목록과 출결 메모(초등부)를 바로 볼 수 있게 됐습니다(요청: "업무쪽으로 가지 않아도 알수 있도록"). 출결 메모는 업무 메뉴와 실시간으로 함께 업데이트됩니다.

## v0.117.1 - 2026-08-11

- **버그 수정: 출발하는 차량이 목록 맨 아래로 옮겨진 뒤 출발하던 문제**: 도착 시각 순 목록과, 출발 애니메이션용 별도 목록이 나뉘어 있어서 차가 출발할 때 순서가 훌쩍 바뀌는 것처럼 보였습니다(요청: "셔틀이 출발할때는 정렬이 되고 출발하는게 아니라 그자리에서 그대로 출발할 수 있도록"). 이제 하나의 목록·하나의 정렬 기준으로 합쳐서, 출발하는 차량이 원래 있던 자리에서 그대로 슬라이드아웃됩니다.

## v0.117.0 - 2026-08-11

- **안내보드 짧은 주소 추가**: 공용컴퓨터 주소창에 복잡한 토큰 링크를 직접 입력하기 어렵다는 요청에 따라, "gia-ops-web.vercel.app/b/코드" 형태의 짧은 주소로도 안내보드에 접속할 수 있게 됐습니다. 안내보드 관리 화면(`/shuttle/pilot`)에서 안내보드마다 만들어진 4자리 코드를 직접 원하는 값으로 바꿀 수도 있습니다. 로그인은 여전히 필요 없습니다.

## v0.116.1 - 2026-08-10

- **안내보드 "지금 도착한 차량" 정렬 방식 변경**: 호차 번호 순이 아니라 도착 시각 순으로 정렬해, 먼저 온 차가 맨 위에 오도록 바꿨습니다. 오래 기다리는 차가 한눈에 보입니다.

## v0.116.0 - 2026-08-10

- **도착체크: 출발함 → 미도착 되돌리기**: 상태 버튼이 출발함에서 멈춰 다시 되돌릴 수 없던 것을, 한 번 더 누르면 미도착으로 돌아오도록 바꿨습니다(미도착 → 도착함 → 출발함 → 미도착 순환). 매일 반복되는 체크라 실수해도 바로 고칠 수 있습니다.
- **전체 리셋 버튼 추가**: 화면 오른쪽 위 "⟲ 전체 리셋" 버튼으로, 오늘 체크한 모든 차량의 도착·출발 상태를 한 번에 미도착으로 되돌릴 수 있습니다(확인 문구 후 실행).

## v0.115.1 - 2026-08-10

- **버그 수정: 여러 대가 동시에 도착하면 안내보드에 명단이 영영 안 뜨던 문제**: 도착 인트로 애니메이션을 순서대로 재생하는 대기열이 React state로 만들어져 있어서, 인트로가 재생되는 도중 새 차량이 폴링으로 대기열에 추가되면 이미 걸려 있던 타이머가 취소되어 그 차량의 명단 공개가 영영 멈추는 버그가 있었습니다(요청: "여러대 도착을 누르니까 반응을 아예 안하고, 도착후에 아이들 명단이 안떠"). 대기열을 타이머와 함께 직접 관리하는 방식으로 바꿔 고쳤습니다.
- **도착·출발 체크 ↔ 안내보드 연동 속도 개선**: 폴링 주기를 도착체크 5초→3초, 안내보드 6초→3초로 줄이고, 도착 인트로 애니메이션 길이도 1.6초→1.1초로 줄여 전체적으로 더 빠르게 반영됩니다.

## v0.115.0 - 2026-08-10

- **교직원 도착체크 모바일 레이아웃 수정**: 노선 개수에 따라 열 수가 들쭉날쭉 늘어나 박스가 무너지던 문제를 고치고, 한 줄에 항상 4대씩 고정으로 보이게 했습니다(화면은 아래로 스크롤). 노선 번호가 크고 또렷하게 보이고, 그 아래 학생 이름이 아주 작은 뱃지로 표시됩니다.
- **도착·출발 버튼을 상태 겸용 하나로 통합**: "도착"/"출발" 버튼이 따로 있던 것을, 노선당 버튼 하나가 누를 때마다 미도착(흰색) → 도착함(오렌지색) → 출발함(회색, 완료) 순서로 색과 글자가 바뀌도록 바꿨습니다.
- **안내보드 도착·출발 애니메이션을 "지금 도착한 차량" 패널 안으로 한정**: 화면 전체를 덮던 버스 인트로 연출을, 오른쪽(모바일은 아래) 패널 폭 안에서만 재생되도록 바꿨습니다. 유튜브 화면을 가리지 않습니다.

## v0.114.0 - 2026-08-10

- **교직원 도착체크 화면 전면 재구성**: 미도착/도착함/출발함으로 나뉘어 있던 목록을 하나의 그리드로 합쳐, 노선 개수에 맞춰 약 3줄이 되도록 자동으로 열을 나눠서 화면 너비를 가득 채웁니다(요청: "세줄정도로 해서 한눈에 모든 차량을 체크할 수 있게... 양쪽으로 반공간이 없도록"). 각 차량 카드 아래에 그 차를 타는 학생 이름이 작은 뱃지로 항상 표시됩니다(요청: "타는 아이들도 차량 아래에 표시되었으면 좋겠어"). 지금은 동승선생님 쪽에 개별 탑승 체크 기능이 없어서 모든 이름이 "미탑승" 빨간 뱃지로만 보이며, 나중에 탑승 체크 기능이 생기면 누른 학생만 초록색으로 바뀌도록 만들 예정입니다.
- **안내보드: 유튜브 화면과 "도착한 차량" 패널 크기 조절**: 두 화면 사이의 구분선을 드래그하면 비율을 원하는 대로 바꿀 수 있습니다(가로 배치에서는 좌우로, 세로로 쌓인 화면에서는 위아래로). 마지막으로 조절한 크기는 브라우저에 저장되어 새로고침해도 유지됩니다.

## v0.113.0 - 2026-08-10

- **셔틀 링크 관리에 "링크 열기" 버튼 추가**: 안내보드·교직원 도착체크·파일럿(GPS) 링크 관리 화면에서 "🔗 링크 복사" 옆에 "↗ 링크 열기" 버튼이 생겨, 복사해서 다른 곳에 붙여넣지 않아도 그 자리에서 바로 새 탭으로 열어 확인할 수 있습니다.

## v0.112.0 - 2026-08-10

- **안내보드 차량 도착 연출 개선**: 차가 도착하면 위젯이 뜨기 전에 화면 전체를 덮는 큰 노란 셔틀버스가 경적 소리와 함께 왼쪽에서 오른쪽으로 지나가는 인트로 애니메이션이 먼저 나오고(요청: "위젯이 나타나기 전에 소리와함께 노란색 셔틀차가 들어오는 애니메이션이 있었으면 좋겠어 아이들이 시각적으로 집중해서 탑승하도록"), 이어서 그 차량 위젯이 "띵동" 소리와 함께 왼쪽에서 미끄러져 들어오며 타야 할 학생 이름이 자동으로 표시됩니다. 여러 대가 동시에 도착해도 인트로가 겹치지 않고 순서대로 재생됩니다.
- **오른쪽 패널 제목 변경**: "지금 탈 수 있는 차량" → "지금 도착한 차량"으로 바꿔, 아직 안 탄 아이가 있어도 도착 자체가 목록에 뜨는 기준임을 더 명확히 했습니다.
- 화면을 처음 열었을 때 이미 도착해 있던 차량은 인트로 없이 바로 표시됩니다(반복 소음 방지).

## v0.111.0 - 2026-08-10

- **하원 체크표: 학생 당일 한정 노선 이동**: 표 안에서 학생 이름을 드래그해서 다른 호차 칸에 놓으면, 오늘 하루만 그 차량을 타는 것으로 바뀝니다(요청: "특정 학생이 특정 하루만 다른셔틀을 타는 경우도 있기때문에 표안에서 아이들의 이름을 자유롭게 끌어서 이동할 수 있게"). 평소 배정은 그대로 유지되고, 다음날은 자동으로 원래 노선으로 돌아갑니다. 옮겨진 학생은 이름 앞에 "↔" 표시와 노란 테두리로 구분됩니다.
- **픽업(🚗)/결석(🚫) 버튼 추가**: 학생 이름 아래 두 버튼으로 픽업·결석 여부를 바로 표시할 수 있습니다. 같은 버튼을 다시 누르면 취소됩니다.
- **노선 이동이 안내보드·실시간 셔틀·도착체크 화면에도 즉시 반영**: 체크표에서 옮긴 학생은 원래 노선이 아니라 옮겨진 노선의 명단에 나타나며, 모든 화면(안내보드, 실시간 셔틀, 교직원 도착체크 단독 링크)이 폴링 때마다 이를 함께 반영합니다. 지금은 여름캠프처럼 짧은 기간에 수동으로 쓰는 기능이며(요청: "나중에 정규학기에는 전부 자동화 할예정이지만 지금은 일주일뿐이라서, 수동으로 할 수있도록"), 정규학기에는 추후 자동화 예정입니다.

## v0.110.0 - 2026-08-10

- **안내보드에서 유튜브 직접 검색**: 관리자가 미리 걸어둔 링크만 재생하던 것에서, 화면 오른쪽 위 "🔍 유튜브 검색" 버튼으로 그 자리에서 바로 검색하고 결과 중 원하는 영상을 클릭해 재생할 수 있게 됐습니다(요청: "자유롭게 서치 해서 클릭할 수 있게"). 실제 유튜브 사이트는 보안 정책상 화면 안에 통째로 넣을 수 없어서, 검색 결과를 우리 화면에 직접 그려주는 방식으로 만들었습니다. 유튜브 플레이어의 전체화면 버튼을 누르면 검색창 없이 영상만 화면 가득 재생됩니다. "↩ 기본 영상으로" 버튼으로 관리자가 설정해둔 영상으로 언제든 되돌릴 수 있습니다.
- 이 기능을 쓰려면 `YOUTUBE_API_KEY` 환경변수 설정이 필요합니다(무료).

## v0.109.0 - 2026-08-10

- **하원 체크표 신설**: 업로드해주신 하원차량 체크표(PDF)와 같은 형태로, 노선별 오늘의 학생 명단을 한 화면에서 볼 수 있는 표 화면(`/shuttle/checklist`, 왼쪽 메뉴 "하원 체크표")을 만들었습니다. 부모님이 직접 데리러 오셔서 셔틀을 타지 않는 학생은 이름을 눌러 "픽업"으로 표시할 수 있고, 다시 누르면 취소됩니다. 정규학기/여름캠프2 탭으로 구분됩니다.
- **픽업 상태가 실시간 셔틀·안내보드에 즉시 반영**: 체크표에서 "픽업"으로 표시한 학생은 안내보드의 "아직 안 탄 아이" 목록에서 바로 빠지고(탑승·결석과 동일하게 취급), 실시간 셔틀 화면에도 픽업 인원이 별도로 표시됩니다.

## v0.108.1 - 2026-08-10

- **안내보드 유튜브 - 재생목록 링크 없이 영상 여러 개 등록 가능**: 재생목록 공유 링크를 따로 찾을 필요 없이, 평소 보던 영상 링크를 한 줄에 하나씩 붙여넣으면 순서대로 이어서 반복 재생됩니다(요청: "유튜브는 재생목록으로 띄우는 거라, 링크 바로 복사하기힘들어"). 재생목록 링크를 붙여넣는 기존 방식도 그대로 지원합니다.

## v0.108.0 - 2026-08-10

- **정규학기 / 여름캠프2 셔틀 구분**: 노선에 구분(term) 값이 생겨, 기존 데이터는 모두 "정규학기"로 자동 분류됩니다. 실시간 셔틀·파일럿 관리자 화면은 정규학기 노선만 보여줘서 캠프 노선과 섞이지 않습니다.
- **교직원 도착·출발 체크 단독 링크 신설**: 로그인 없이 링크 하나로 접속해 노선별로 "도착"/"출발(다 태움)" 두 버튼만 누르는 화면(`/shuttle-arrival/{토큰}`)을 새로 만들었습니다. GPS 위치 전송이나 학생별 개별 탑승 체크가 필요 없는 여름캠프처럼 짧고 단순한 운영에 씁니다. 관리자 화면(`/shuttle/pilot`)에서 정규학기용/캠프용 링크를 각각 만들고 관리할 수 있습니다.
- **안내보드 도착·출발 애니메이션**: 안내보드도 이제 정규학기/캠프 구분(term)에 맞는 노선만 보여주고(안내보드 링크마다 구분을 지정), 차량이 도착하면 노란 버스 아이콘이 미끄러져 들어오는 애니메이션이, 출발하면 카드가 미끄러지며 사라지는 애니메이션이 나옵니다. 개별 탑승 체크 없이 도착한 차량의 전체 명단이 계속 보이다가, 출발하면 사라집니다.

## v0.107.2 - 2026-08-10

- **안내보드 20초 반복 알람 제거**: 여러 차량이 동시에 정차해 있을 때 20초마다 알람이 계속 울리면 정신없다는 의견에 따라, 이제 차량이 새로 도착한 순간에만 알람이 한 번 울립니다. 몇 호차가 도착했는지, 아직 타지 않은 학생이 누구인지는 계속 화면에 떠 있고, 탑승 체크가 되면 그 학생 이름이 바로 목록에서 빠져 남은 학생만 한눈에 보입니다.

## v0.107.1 - 2026-08-10

- **사건기록 새 사건 입력 화면 스크롤 안 되던 문제 수정**: 데스크톱에서 입력폼이 화면보다 길어지면(태그할 학생이 많거나 내용이 길 때) 아래쪽 저장 버튼까지 화면이 잘려서 안 내려가던 문제를 고쳤습니다. 이제 가운데 입력폼 칸도 양옆 목록/AI제안 칸처럼 그 칸 안에서 스크롤됩니다.

## v0.107.0 - 2026-08-10

- **안내보드 도착 알람 강화**: 차량이 도착해 탑승할 아이들 이름이 뜨는 순간 3음 알람 소리가 울립니다. 브라우저 자동재생 정책 때문에 화면을 처음 열면 "소리 켜고 시작하기"를 한 번 눌러야 하고(그 뒤로는 계속 소리가 납니다), 아직 타지 않은 아이가 남아있는 동안은 20초마다 알람이 다시 울려서 놓치지 않도록 했습니다.

## v0.106.0 - 2026-08-10

- **안내보드를 로그인 없는 링크로 전환 + 유튜브 재생**: `/shuttle-board`가 회사 계정 로그인 없이 링크(토큰) 하나로 접속하는 화면(`/shuttle-board/{토큰}`)으로 바뀌었습니다. 평소에는 유튜브 영상(관리자가 지정한 영상 또는 재생목록)이 화면 대부분을 채워 아이들이 기다리며 볼 수 있고, 오른쪽(모바일은 아래) 패널에는 "지금 탈 수 있는 차량"이 항상 함께 보입니다. 차량이 도착하면 몇 호차인지, 아직 타지 않은 학생이 누구인지 큰 글씨로 뜨고, 새로 도착한 차량은 잠깐 반짝이며 알림음이 울립니다.
- **안내보드 링크 관리 UI**: 관리자 화면(`/shuttle/pilot`)에서 안내보드를 여러 개 만들고(화면별로 이름 지정), 각각 재생할 유튜브 영상/재생목록을 지정하고, 링크를 복사하고, 켜고 끌 수 있습니다. 실시간 셔틀 화면(`/shuttle/live`)의 '안내보드 열기' 버튼이 실제 활성화된 안내보드로 연결됩니다.
- **동시 도착 대응**: 여러 차량이 거의 동시에 도착해 여러 교직원이 '현장도착'을 동시에 누르는 상황에 대비해, 같은 노선·같은 날짜에는 DB에서 중복 체크를 자동으로 막고(부분 유니크 인덱스), 화면에서도 중복 클릭을 에러 없이 조용히 넘어가도록 처리했습니다. 또한 실시간 셔틀 화면을 "미도착 · 빠른 체크"(큰 버튼만 있는 목록)와 "상세 현황"(위치·탑승현황이 있는 카드) 두 구역으로 나눠, 여러 차가 몰리는 순간에도 빠르게 훑고 누를 수 있게 했습니다.

## v0.105.0 - 2026-08-10

- **동승선생님 전용 + 하원 우선으로 범위 축소**: 이번 단계에서는 기사님이 아닌 동승선생님만 체크인 링크를 쓰고, 등원은 잠시 제외하고 하원 노선만 지원합니다. 관리자 링크 관리, 교직원 실시간 셔틀, 안내보드 모두 하원 노선만 보여줍니다.
- **체크인 화면 순서 재정렬**: 화면을 열면 ① 학생 탑승 확인(목록)이 먼저 보이고, 그 아래 ② '이동 시작(위치 전송 시작)' 버튼이 있습니다. 이동을 시작하면 실시간 위치가 전송되고, '운행 종료'를 누르는 즉시 위치 전송이 멈춥니다.

## v0.104.0 - 2026-08-10

- **안내보드(전광판) 화면 신설**: 복도·로비 화면에 띄워두는 전용 화면(`/shuttle-board`, 실시간 셔틀 화면의 '안내보드 열기'로 접근)입니다. 교직원이 현장도착을 체크한 하원 차량만, 아직 타지 않은 학생 이름을 큰 글씨로 자동 표시합니다. 동승선생님이 탑승을 체크하면 이 화면에도 실시간으로 반영되고, 전원 탑승하면 "전원 탑승 완료"로 바뀝니다.
- 이번 버전으로 하원 셔틀 자동화 로드맵(학부모 제외, 교직원 중심 재편)의 1차 구현을 마쳤습니다: 실시간 위치(교직원 전체) → 현장도착 체크 → 안내보드 → 동승선생님 탑승확인 → 교직원 모바일 확인까지 전 과정이 연결됩니다.

## v0.103.0 - 2026-08-10

- **교직원 전체용 실시간 셔틀 화면 신설**: 왼쪽 메뉴 '실시간 셔틀'(교사 포함 로그인한 전체 교직원이 볼 수 있음)에서 등원·하원 노선의 실시간 위치와 정류장별 탑승/결석/미탑승 현황을 바로 확인할 수 있습니다.
- **'현장도착' 체크 추가**: 하원 차량이 학교 승차 지점에 도착하면 교직원이 모바일에서 직접 '현장도착'을 체크할 수 있습니다(기사님 GPS 이벤트와 별개로, 눈으로 본 도착을 확인하는 절차). 동승선생님이 기존 체크인 화면에서 탑승을 체크하면 이 화면에도 실시간으로 반영됩니다.
- 관리자 전용 링크 관리 화면(/shuttle/pilot)은 그대로 유지하되, 메뉴 이름을 '링크 관리'로 명확히 구분했습니다.

## v0.102.0 - 2026-08-10

- **학부모 기능 전면 제거**: 노선이 지연되거나 교통상황으로 도착이 늦어질 때 학부모가 실시간 위치만 보고 오해해 문의가 상충할 수 있다는 판단에 따라, 학부모 테스트 화면·학부모 테스트 링크 발급·자동 푸시 알림(2단계-c) 기능을 전부 제거했습니다. 대신 교직원이 등원·하원 셔틀 실시간 위치와 탑승현황을 바로 확인하고, 하원 차량 탑승을 안내·확인하는 방향으로 시스템을 재편합니다(다음 버전부터 순차 반영).

## v0.101.0 - 2026-08-10

- **3단계-b: 운행일지 자동생성**: 셔틀 실시간 위치 화면의 노선 카드에 '오늘 운행일지' 버튼이 생겼습니다. 누르면 그날의 출발·5분전·도착 시각, 소요시간, 정류장별 탑승/결석/미탑승/하차 현황, 안전운행지수를 자동으로 정리한 PDF가 만들어집니다. 벤치마킹 제안서의 3단계 기능이 모두 반영되어(4단계 얼굴인식은 제외), 1~3단계 로드맵을 마쳤습니다.

## v0.100.0 - 2026-08-10

- **3단계-a: 안전운행지수(급가속·급감속)**: 기사님·동승선생님 체크인 화면이 운행 중 휴대폰 가속도 센서로 급가속·급감속 순간을 감지합니다. 관리자 모니터링 화면 노선 카드에 오늘 하루 안전운행지수(100점 만점, 급가속·급감속 1건당 5점 감점)가 표시됩니다.

## v0.99.0 - 2026-08-10

- **2단계-c: 학부모 자동 푸시 알림**: 학부모 테스트 화면에서 "탑승·하차 알림 받기"를 누르면, 그 학생이 탑승/미탑승/결석으로 체크되거나 하차가 확인될 때 브라우저 알림이 옵니다. 앱 설치나 외부 서비스 가입 없이 브라우저 표준 Web Push를 씁니다(발신용 VAPID 키는 서버 환경변수로만 관리, 로그인 없는 학부모 테스트 링크 구조는 그대로 유지).

## v0.98.0 - 2026-08-10

- **2단계-b: 도착예정시각(ETA)**: 학부모 테스트 화면에 "약 O분 후 도착 예정(HH:MM)"이 표시됩니다. 현재 위치에서 학생 정류장까지 카카오모빌리티 길찾기로 계산하며, 노선·정류장 조합별로 30초 캐시를 둬 API 호출 비용을 통제합니다(기존 KAKAO_REST_API_KEY 재사용, 새 키 발급 불필요).

## v0.97.0 - 2026-08-10

- **2단계-a: 학생별 탑승·하차 체크리스트**: 기사님·동승선생님 체크인 화면에서 오늘 그 노선에 배정된 학생 목록을 정류장 순서대로 보고, 탑승/결석/미탑승/하차를 터치 한 번으로 기록할 수 있습니다(다시 누르면 취소). 전부터 만들어져 있었지만 화면과 연결되지 않았던 `shuttle_boardings` 테이블을 처음으로 사용합니다.
- 학부모 테스트 화면에도 탑승·하차 확인 여부가 함께 표시됩니다.

## v0.96.0 - 2026-08-10

- **셔틀 실시간 위치 1단계 정식 도입**: 벤치마킹 제안서의 1단계(실시간 위치 전송, 운행 이벤트)를 파일럿에서 정식 기능으로 전환했습니다. 관리자가 고른 일부 노선에만 붙던 것을, 활성 노선 전체에 자동으로 적용하고(새 노선 등록 시 자동 포함) 사이드바 메뉴명도 "실시간 위치"로 바꿨습니다.
- **학부모 테스트 화면 신설**: 실제 학부모에게는 아직 배포하지 않지만, 관리자가 학생을 검색해 테스트용 링크를 만들고 직접 학부모 화면(자녀 셔틀 실시간 위치)을 확인해볼 수 있습니다. 도착예정시각·자동알림은 2단계에서 추가될 예정입니다.

## v0.95.0 - 2026-08-10

- **출결내역 이름 인식 개선**: 구글챗 출결알림의 "@멘션"이 학생 이름으로 잘못 표시되던 문제를 고쳤습니다(태그일 뿐 실제 이름이 아님). 영어로 적힌 출결알림("~will be absent" 등)도 문구 앞 이름을 학생 명부의 영어이름과 대조해 "한글이름(영어이름)"으로 표시합니다.
- **부서 메모 / 출결 메모 분리**: 부서 메모에 출결 내용을 적으면 자동으로 출결내역에 반영되던 동작을 없앴습니다(자유 메모로 못 쓰게 되는 문제). 대신 출결내역 화면을 좌:우 7:3으로 나눠, 오른쪽에 절대 자동 반영되지 않는 전용 "출결 메모"칸을 추가했습니다 - 픽업/결석/퇴소 메모나 며칠 뒤 출결을 미리 적어두는 용도입니다.
- **탑승 배정 화면 재구성**: 등원/하원 탭으로 나누고, 호차(노선) 단위로 묶어 "1호차" 아래 학생 목록이 뜨도록 바꿨습니다. 호차는 번호순, 호차 안에서는 학년순으로 정렬됩니다. 학생 추가도 이제 호차 단위로 합니다.
- **노선 관리 용어 변경**: 정류장 항목의 "게이트"를 "도착장소"로 바꿨습니다.
- **셔틀 메뉴 기본 화면 변경**: 사이드바 "셔틀"을 누르면 바로 지역별 현황이 뜨도록 바꾸고, 부메뉴에서는 뺐습니다.

## v0.94.0 - 2026-08-10

- **셔틀 앱 파일럿 검증 인프라 신설**: 정식 앱(앱스토어 배포) 전에, 실제 노선으로 위치 전송이 무리 없이 되는지 먼저 확인하는 단계를 추가했습니다. 학부모는 참여하지 않고 기사님·동승선생님만 참여하며, 앱 설치·개발자 계정·로그인 없이 링크 하나로 접속합니다(`/shuttle-pilot/[토큰]`) - "운행 시작"을 누르면 5초 간격으로 위치만 전송되고, "운행 종료"를 누르면 즉시 멈춥니다.
- **파일럿 모니터링 대시보드**: 관리자 메뉴에 "🧪 파일럿 검증"이 추가되어, 노선별 실시간 위치를 지도에서 보고 수신 성공률·평균 갱신 간격·완주 여부 같은 검증 지표를 확인할 수 있습니다. 노선을 골라 파일럿 링크를 바로 만들고 복사할 수 있습니다.
- 이 검증 데이터는 학생 개인정보와 무관하며(노선 좌표·시각만 저장), 검증이 끝나면 관련 테이블은 통째로 삭제해도 정식 서비스에 영향이 없습니다.

## v0.93.3 - 2026-08-07

- **지도는 노선이 가는 구만 색이 들어감**: 셔틀이 안 가는 구는 흰 바탕에 옅은 테두리만 남아, 색이 칠해진 구 = 실제로 셔틀이 가는 지역이라는 게 바로 보입니다.
- **동별 목록에서 등원/하원 구분**: 지역을 눌렀을 때 뜨는 동별 호차 목록이 등원·하원 줄로 나뉘어 표시됩니다. 아래 전체 노선 목록도 같은 지역 안에서는 등원이 먼저, 하원이 나중에 오도록 정렬했습니다.

## v0.93.2 - 2026-08-07

- **서울 지도 글자 안 보이는 문제 수정**: 종로구 등 일부 구는 이름표 위치가 도형 바깥으로 벗어나 있었습니다. 도형 경계에서 가장 먼 안쪽 지점을 다시 계산해 25개 구 전부 도형 안에 이름표가 찍히도록 고쳤고, 배경색과 겹쳐도 잘 보이도록 글자에 흰 테두리를 추가했습니다.
- **지역을 누르면 색이 확실히 바뀜**: 선택한 구는 테두리만 굵어지던 것에서, 채우기 색 자체가 주황색으로 바뀌도록 해 지금 어느 구를 보고 있는지 한눈에 보입니다.

## v0.93.1 - 2026-08-07

- **정류장 주소 저장 시 즉시 구/동 반영**: 노선 관리에서 정류장 주소를 추가하거나 고치면, 노선도 탭을 따로 열지 않아도 그 자리에서 바로 구/동·좌표가 채워집니다. 주소를 지우면 옛 좌표/구·동도 함께 지워집니다.
- **정류장이 바뀌면 실도로 경로 캐시 자동 삭제**: 정류장을 추가·삭제하거나 주소·순서·소속 노선이 바뀌면(차량 배정 변경 포함), 캐시된 실도로 경로가 자동으로 삭제되어 다음에 노선도를 열 때 새로 계산됩니다. 예전 경로가 화면에 남아있을 일이 없습니다.
- **정류장 구/동 일괄 채우기 스크립트 추가**: 기존에 등록된 정류장들의 구/동을 한 번에 채우는 관리자용 스크립트(`scripts/backfill-shuttle-geo.mjs`)를 추가했습니다.

## v0.93.0 - 2026-08-07

- **지역별 현황을 실제 행정구역(구/동) 기준으로 재구성**: 노선 이름 표기("청담1", "용산/이태원" 등)로 지역을 추정하던 방식은 표기가 제각각이라 믿을 수 없었습니다. 이제 정류장 주소를 지오코딩할 때 함께 받아오는 실제 행정구·행정동을 기준으로 지역을 분류합니다. 노선도 탭을 한 번 열면 그 노선 정류장들의 구/동이 자동으로 채워집니다.
- **서울 25개구 지도로 교체**: 카카오맵 대신 서울시 25개 자치구 경계를 그대로 그린 지도를 새로 만들었습니다. 구를 누르면 그 구 안의 동별로 몇 대(몇 호차)가 가는지 오른쪽에 바로 뜨고, 지도 위 숫자로 구별 셔틀 대수를 한눈에 볼 수 있습니다. 지명·아파트·도로명·차호수 검색도 계속됩니다.

## v0.92.0 - 2026-08-07

- **노선도에 정류장별 시간표 추가**: 노선도 아래에 GIA(또는 첫 정류장) 출발 시각부터 정류장을 순서대로, 구간별 소요시간과 누적 도착예정시각을 함께 보여줍니다. 실도로 경로가 계산돼야 정확한 시간이 나오고, 없으면 순서만 보여줍니다.
- **실도로 경로 자동 계산**: 이제 노선도를 열면(편집 권한이 있는 경우) 실도로 경로가 없거나 낡았을 때 자동으로 한 번 계산합니다. 손으로 버튼을 누르지 않아도 됩니다.
- **차량 정원 관리 + 초과 경고**: 노선 관리에서 차량 정원(인승)과 실제 탑승 가능 인원을 입력할 수 있고, 배정 인원이 이를 넘으면 셔틀 현황·노선 관리·지역별 현황 어디서나 ⚠️ 경고가 뜹니다.
- **지역별 현황 대시보드 신설**: 노선 번호가 지역을 묶어 매겨진 점에 착안해, 지도에서 지역을 고르면(또는 지명·아파트·도로명·차호수로 검색하면) 그 지역 가는 셔틀 목록이 뜨고, 아래에는 전체 노선이 지역순·호차순으로 정렬돼 나옵니다. 지역 태그는 기존 권역명에서 1차 자동 추정 후 노선 관리에서 다듬을 수 있습니다.

## v0.91.0 - 2026-08-07

- **학생 명부에 차량탑승 여부 + 노선 추천**: 학생 명부 관리에서 "차량탑승 여부"(없음/등원/하원/등하원)를 지정하면 [추천] 버튼으로 주소 기준 가장 가까운 노선·정류장 3곳을 거리·예상 시각과 함께 보여주고, 그 자리에서 바로 배정할 수 있습니다.
- **노선 지도에 실제 도로 경로 표시**: 노선도 탭에 [🛣️ 실제 도로 경로 계산] 버튼이 생겼습니다. 카카오모빌리티 길찾기로 정류장을 순서대로 잇는 실제 도로 경로를 계산해 캐시해두고, 직선 대신 그 경로를 그립니다(총 거리·예상 소요시간 함께 표시). 정류장 구성이 바뀌면 다시 계산이 필요하다고 안내합니다.

## v0.90.1 - 2026-08-07

- **노선 지도에 GIA 본원 지점 표시**: 정류장 DB에는 학생 주소만 있어 학교 자체가 지도에 안 보였습니다. 이제 등원 노선은 마지막에, 하원 노선은 맨 앞에 🏫 GIA 지점을 자동으로 붙여서, 실제 운행 순서(집→학교 / 학교→집)가 그대로 지도에 보이도록 했습니다.

## v0.90.0 - 2026-08-07

- **셔틀 현황에 노선도(지도) 탭 추가**: 노선을 고르면 배차표 옆에 [🗺️ 노선도] 탭이 생겨, 정류장을 순서대로 번호가 매겨진 지도 마커와 경로선으로 볼 수 있습니다. 카카오맵 주소검색으로 좌표를 자동으로 채우고, 동 이름만 있어 자동으로 못 찾는 주소는 지도를 직접 클릭해 위치를 지정할 수 있습니다.

## v0.89.0 - 2026-08-07

- **셔틀이 주메뉴로 독립**: 학교 관리 부메뉴 하나였던 셔틀을 주메뉴로 빼고 부메뉴를 나눴습니다 - 셔틀 현황 / 노선 관리 / 탑승 배정.
- **노선 관리 신설**: 노선을 추가·삭제하고 호차·권역명·기사님·차량번호·동승 선생님·출발시각을 수정합니다. 정류장도 추가·수정·삭제하고 ↑↓로 순서를 바꿀 수 있습니다. 정류장을 지울 때는 함께 사라지는 학생 배정 수를 먼저 알려줍니다.
- **탑승 배정 신설**: 학생 이름이나 노선으로 찾으면 그 아이의 등원·하원 배정이 한 줄씩 보이고, 요일 버튼(월화수목금)을 눌러 타는 날을 바로 지정합니다. 요일마다 내리는 곳이 다른 학생은 [+ 배정 추가]로 줄을 더 만들어 각각 다른 정류장·요일을 지정하면 됩니다.

## v0.88.2 - 2026-08-07

- **셔틀 배차표에 소속(유치부/초등부/중고등부) 표시**: 반 표기로 소속을 추정해 배지로 보여줍니다("5 Nightingale"처럼 나이+반이름은 유치부, "학교"는 초등부).
- **명부 미연결 경고를 꼭 필요한 곳만 표시**: 지금 학생 명부에는 초등부만 등록되어 있어 유치부·중고등부는 연결이 안 되는 게 정상인데, 585명 중 488명에 ⚠️가 떠서 정작 확인이 필요한 건이 묻혔습니다. 이제 "초등부인데 명부에 없는" 경우에만 표시하고, 노선 제목 옆에 그 건수를 함께 보여줍니다.

## v0.88.1 - 2026-08-07

- **셔틀 호차 중복 오류 수정**: 배차표 원본에 "등원 22호"가 청담1·청담4 두 노선에 중복으로 적혀 있어 데이터 입력이 실패했습니다. 호차 번호 유니크 제약을 없애 두 노선 모두 들어가도록 하고, 화면에서 보고 정리할 수 있게 했습니다.

## v0.88.0 - 2026-08-07

- **셔틀 관리 신설 (1단계)**: [학교 관리] 메뉴에 "🚌 셔틀 관리"가 생겼습니다. 등원·하원 노선과 정류장, 요일별 탑승 학생을 관리하고 배차표를 인쇄할 수 있습니다.
  - 2026BUS.pdf를 그대로 옮겼습니다: 등원 39개 / 하원 47개 노선, 정류장 595곳, 탑승 배정 585건, 학생 335명.
  - 요일별로 다른 노선을 타는 학생(월수는 학원, 화목은 집 등)을 그대로 반영했습니다.
  - 노선·기사님·학생 이름으로 검색됩니다(이 아이가 몇 호차인지 바로 확인 가능).
  - 지입차량이라 바뀌기 쉬운 기사님·차량번호·동승 선생님은 화면에서 바로 수정할 수 있습니다.
  - 학생 명부와 자동으로 연결되며, 동명이인이라 확정 못 한 건은 ⚠️로 표시됩니다.

## v0.87.3 - 2026-08-07

- **출결내역 날짜·요일 인식**: "조영윤 금요일 결석입니다"처럼 오늘이 아닌 날의 출결을 미리 알리는 경우, 이제 그 날짜를 읽어서 오늘 집계에 섞이지 않게 했습니다. 요일(금요일), 상대 표현(오늘/내일/모레), 날짜(8/12, 8월 12일, 12일)를 모두 인식합니다.
- **"📅 예정" 칸 추가**: 앞으로 예정된 결석·픽업은 오늘 칸 아래에 날짜와 함께 따로 모아 보여줍니다. 지난 날짜는 자동으로 빠집니다.
- **미리 알려준 오늘 결석도 잡음**: 예전에는 오늘 올라온 메시지만 봐서, 며칠 전에 미리 알려준 오늘 결석을 놓쳤습니다. 이제 최근 2주치를 훑어 각 메시지가 가리키는 날짜로 분류합니다.

## v0.87.2 - 2026-08-07

- **출결내역 동명이인 구분**: 같은 이름의 학생이 여러 명일 때, 문장에 적힌 학년 표기를 읽어 누구인지 구분합니다. "2학년 김재이", "김재이(2)", "김재이 2학년", "초2 김재이" 형태를 모두 인식하고, 화면에는 "김재이(2학년)"처럼 학년을 붙여 보여줍니다.
- **학년 표기가 없으면 임의로 고르지 않음**: 동명이인인데 학년 힌트가 없으면 한 명을 몰래 골라버리지 않고 ⚠️ 표시로 "학년 확인 필요"라고 알려줍니다.
- **중복 체크 기준 보완**: 중복 제거가 이름만이 아니라 학년까지 함께 보므로, 같은 이름의 다른 학년 학생이 한 명으로 합쳐지지 않습니다.

## v0.87.1 - 2026-08-07

- **출결내역 제목 옆 요약**: 픽업·결석·지각 순서로 건수를 한눈에 보여줍니다(조퇴는 있을 때만).
- **출결내역을 분류별 칸으로 분리**: 채팅처럼 한 줄로 흐르던 것을 위에서부터 픽업 / 결석 / 지각 칸으로 나눠, 각 칸 안에 누가 해당되는지와 원문을 작게 함께 보여줍니다.
- **출결내역 ↔ 출결알림 탭 전환**: 자리를 반으로 쪼개지 않고 탭으로 전환합니다. 기본은 정리본인 출결내역이고, 원문 확인이 필요할 때 출결알림 탭을 누르면 구글챗 채팅이 보입니다.
- **부서 메모도 출결내역에 반영**: 부서 메모에 적힌 결석·픽업 내용도 함께 올라옵니다. 구글챗과 메모에 같은 내용이 겹쳐 적혀도 같은 학생이 두 번 뜨지 않습니다.
- **학생 이름 오인식 수정**: 예전에는 문장에서 한글 2~4자를 그대로 이름으로 집어내서 "정서안만 픽업"의 "정서안만"이 이름처럼 잡혔습니다. 이제 실제 학생 명부와 대조해서, 명부에 있는 "정서안"만 잡히고 이름이 겹치는 경우(김민 / 김민준 등)에도 긴 이름을 우선해 한 명만 정확히 잡습니다.

## v0.87.0 - 2026-08-07

- **내 업무가 아닌데도 진행대기에 뜨던 문제 수정**: 진행대기/진행중/완료 칸이 부서 전체 업무를 보여주고 있었습니다. 이제 "내가 태그된 업무"만 뜹니다. 판정 기준도 태그 하나로 통일했습니다 - 어떤 방식([나]/[전체]/[공유])으로 등록하든 등록자 본인이 자동으로 태그되고, [전체]로 등록하면 부서원 전원이 태그됩니다. 부서 전체를 훑어보는 용도는 오른쪽 위 "전체 업무목록"이 계속 담당합니다.
- **업무 흐름판(이름 신설)**: 진행대기→진행중→완료 흐름을 보는 영역에 "🔀 업무 흐름판"이라는 이름을 붙였습니다.
- **업무바 접기/펼치기**: 등록된 업무는 평소엔 제목·확인여부·마감기한만 있는 얇은 바로 보이고, 마우스를 올리면 아래로 펼쳐지며 내용·담당자·확인현황·상태변경이 나타납니다. 옮길 때는 제목 부분을 잡고 드래그합니다.
- **출결내역 위젯 신설**: 출결알림 자리를 반으로 나눠 왼쪽은 구글챗 원문 그대로, 오른쪽은 그 원문에서 결석·픽업·지각·조퇴 키워드를 뽑아 누가 해당되는지 분류·정리해 보여줍니다(오늘 것만).
- **등록사건목록 대시보드 신설**: 사이드바에서 [운영 관리]를 누르면 바로 열립니다. 날짜별로 묶어 언제 어떤 사건이 등록됐는지와 개요(최대 5줄)를 함께 보여줍니다.
- **사이드바 숫자 배지 정리**: 주메뉴(운영 관리 등) 옆에 상시로 떠 있던 합계 숫자를 없앴습니다. 대신 등록사건목록 대시보드 제목 옆에 총 건수를 표시합니다(부메뉴 항목별 배지는 그대로 유지).

## v0.86.3 - 2026-08-06

- **학생 명부 관리 스크롤 안 되던 버그 수정**: 화면을 감싸는 영역에 높이 기준이 빠져 있어서, 목록을 아무리 늘려도(v0.86.2에서 페이지 넘김을 없앤 뒤) 표 안쪽 스크롤이 동작하지 않았습니다. `/students`(학생 정보 조회) 화면과 같은 구조로 맞춰 이제 표 영역이 화면 높이에 맞게 자체적으로 스크롤됩니다.
- **학생 명부 관리에 이름 검색창 추가**: "학생 추가" 버튼 줄과 표 사이에 이름/영어 이름 검색창을 넣었습니다. 학년/반 필터와 함께 쓸 수 있습니다.

## v0.86.2 - 2026-08-06

- **학생 명부 관리 화면 개선**: 페이지 단위(20명씩) 넘기던 방식을 없애고 전체 명단이 한 번에 아래로 스크롤되도록 했습니다. 상단에 학년/반 필터를 추가해 원하는 학년·반만 골라 볼 수 있고, 기존처럼 칼럼 제목(이름/영어 이름 등)을 클릭하면 그 칼럼 기준 오름차순/내림차순으로 정렬됩니다.

## v0.86.1 - 2026-08-06

- **통합검색으로 학생을 찾으면 관련 사건기록이 안 보이던 버그 수정**: 상단 검색(⌘K)에서 학생을 찾아 클릭하면 관리자·행정직원도 위클리 리포트 화면(사건기록 없음)으로 이동하고 있었습니다. 이제 관리자·행정직원은 사건기록·업무언급까지 함께 보이는 통합 프로필(학생 정보 조회와 동일한 화면)로 이동합니다. 교사는 기존과 동일합니다.
- **사건기록 입력 텍스트박스 자동 높이조절**: 상세 내용/잘된 점/부족했던 점/보완점/조치사항 칸이 이제 내용이 10줄 이내면 전문이 그대로 다 보이고, 10줄을 넘으면 그 높이에서 멈추고 안쪽 스크롤로 바뀝니다.

## v0.86.0 - 2026-08-06

- **교직원 통합기록 신설**: 학생 통합 프로필(입학부터 졸업까지 고유번호 유지)과 같은 방식으로, 교직원도 계정 이메일을 영구 식별자로 삼아 입사일·퇴사일과 연도/학기별 담당 반·역할 이력을 한 화면에서 관리합니다.
  - [학교 관리] 메뉴에 "교직원 정보 조회"가 새로 생겼습니다(관리자·행정직원). 이름/이메일로 검색해 들어가면 기본정보 + 담당 이력 + 관련 업무를 한 페이지에서 볼 수 있습니다.
  - 입사일/퇴사일 편집과 담당 이력 추가/삭제는 관리자만 할 수 있습니다. 퇴사해서 로그인 계정이 차단되어도 기록은 계정이 삭제되지 않는 한 그대로 남습니다.
  - [사용자 관리] 화면의 승인된 계정마다 "📋 근무기록" 바로가기가 추가됐습니다.
  - 다음 단계로 예정된 "사건 검색 시 비슷한 사건 정렬 표시" 기능은 이번 배포에는 포함되지 않았습니다(다음 라운드에서 진행).

## v0.85.8 - 2026-08-06

- **코드 정리**: 정적 분석(knip)으로 찾은 안쓰는 코드/파일을 정리했습니다.
  - 완전히 안쓰는 파일 삭제: `src/lib/ai/translate.ts`(제거된 "행정요청" 기능 전용 번역 헬퍼, v0.82.0에서 그 기능 자체가 삭제됨).
  - 어디서도 호출되지 않던 죽은 함수 삭제: `appendHtmlSection`, `isAuthOkCookieValid`, `CATEGORY_LABELS`/`parseBadges`/`hasWarningBadges`/`hasExcellentBadges`(위클리 리포트 통계 화면이 이미 같은 로직을 직접 계산하고 있어 중복이었습니다), `isInCurrentWeek`.
  - 같은 파일 안에서만 쓰이는데 불필요하게 외부로 열려있던 함수/상수 12개는 `export`만 제거(로직은 그대로 유지, 파일 밖 공개 API만 줄임).
  - 번들/의존성 점검 결과 사용하지 않는 npm 패키지는 없었고, 무거운 라이브러리(dnd-kit/tiptap/react-pdf)는 이미 각자 필요한 화면에만 자동으로 코드 분할되어 있어(react-pdf는 서버 전용 API 라우트에서만 쓰여 브라우저로는 아예 전송되지 않음) 추가 조치가 필요하지 않았습니다.
  - 동작 변화는 없고(tsc/build 통과 확인), 코드베이스만 가벼워졌습니다.

## v0.85.7 - 2026-08-06

- **슬랙 알림 실패 진단 로그 추가**: `SLACK_WEBHOOK_URL`을 등록했는데도 가입 알림이 오지 않는 문제를 조사하기 위해, Slack이 웹훅 요청을 거부해도 지금까지는 아무 흔적 없이 조용히 실패하던 부분을 고쳐 실패 시 상태코드/응답 내용을 Vercel 함수 로그에 남기도록 했습니다. 실제 원인(웹훅 URL 문제/채널 문제 등)은 로그를 보고 이어서 확인합니다.

## v0.85.6 - 2026-08-06

- **구글챗 미러링 내용 옆에 업무등록 아이콘을 작게 배치**: 아이콘이 있던 별도 줄을 없애고, 메시지 내용 오른쪽에 아주 작은 아이콘(🔧/✅)만 붙여서 내용이 더 깔끔하게 보이도록 했습니다.

## v0.85.5 - 2026-08-06

- **구글챗 미러링 "업무로 등록" 버튼을 아이콘 전용으로 축소**: 글씨 없이 🔧 아이콘만 표시하고, 마우스를 올리면 "업무등록"이라는 작은 말풍선만 뜨도록 했습니다.

## v0.85.4 - 2026-08-06

- **구글챗 미러링 패널 날짜별 구분선 추가**: 출결알림 목록에 날짜가 바뀔 때마다 "오늘/어제/8월 6일 (수)" 형태의 구분선이 표시되도록 했습니다.

## v0.85.3 - 2026-08-06

- **선생님요청 패널 임시 제거**: 선생님요청 방이 아직 만들어지지 않았는데 메시지가 출결알림/선생님요청 두 패널에 뒤섞여 나타나던 문제가 있었습니다. 방이 생기기 전까지는 선생님요청 패널 자체를 화면(PC/모바일)에서 빼고 출결알림 패널이 자리를 전부 쓰도록 했고, 폴링 크론도 더 이상 선생님요청 방을 조회하지 않도록 했습니다. 패널이 넘치면 스크롤되는 동작은 그대로 유지됩니다. 방이 만들어지고 환경변수(`GOOGLE_CHAT_SPACE_TEACHER_REQUESTS`)를 넣으면 다시 추가하면 됩니다.

## v0.85.2 - 2026-08-06

- **구글챗 폴링 크론 타임아웃 조정**: cron-job.org 무료 플랜의 요청 타임아웃 상한이 30초로 확인되어(이전 기준이던 60초보다 짧음), 폴링 라우트의 내부 반복 시간을 25초로 줄여 항상 제한 시간 안에 응답하도록 수정했습니다. 가이드의 STEP 8에도 반영했습니다.

## v0.85.1 - 2026-08-06

- **크론 라우트가 항상 로그인 화면으로 리다이렉트되던 버그 수정**: 로그인 세션 검사 미들웨어가 `/api/cron/*` 경로에도 적용되고 있어서, 외부 스케줄러(cron-job.org)나 Vercel Cron처럼 로그인 세션 없이 `Authorization: Bearer` 헤더로만 인증하는 요청이 항상 `/login`으로 307 리다이렉트되고 있었습니다(구글챗 폴링 크론에서 "307 Temporary Redirect" 오류로 발견). `/api/cron` 경로는 세션 검사를 건너뛰고 각 라우트 자체의 인증만 거치도록 수정했습니다 - 기존에 있던 다른 6개 크론(백업/학기전환/보관/휴지통정리 등)도 같은 문제를 겪고 있었을 가능성이 높아 함께 해결됩니다.

## v0.85.0 - 2026-08-06

- **구글챗 미러링을 Pub/Sub push → 폴링 방식으로 전환**: 실시간 push 알림에 필요한 Google Cloud Pub/Sub 권한 부여 단계가 조직의 "Domain Restricted Sharing" 정책에 막혀 GCP 조직 정책 관리자 권한이 있어야만 풀 수 있는 상황이었습니다(관리자 승인 없이 혼자 끝내기 원칙과 상충). 대신 앱이 구글챗 API를 주기적으로 직접 조회하는 방식으로 바꿔 IAM 권한 변경이 전혀 필요 없게 했습니다. 외부 무료 스케줄러(1분 간격)가 호출하면, 그 안에서 다시 짧은 간격으로 반복 조회해 수 초~수십 초 내에 새 메시지가 반영됩니다.

## v0.84.0 - 2026-08-06

- **구글챗 미러링 인증방식을 본인 계정 OAuth로 전환**: 관리자(도메인 위임) 승인 없이도, 구글챗 두 방 모두에 속한 본인 계정으로 한 번만 로그인하면 미러링이 동작하도록 바꿨습니다. 개발자 전용 링크(`/api/google-chat/oauth/start`)에서 로그인 한 번이면 설정이 끝납니다 - Google Workspace 관리자 콘솔에 들어갈 필요가 없어졌습니다.

## v0.83.0 - 2026-08-06

- **구글챗 미러링 수신/갱신 인프라 추가**: 구글챗에서 새 메시지가 오면 받아서 저장하는 웹훅 라우트(`/api/google-chat/webhook`)와, 연동이 끊기지 않게 매일 자동으로 구독을 갱신하는 크론(`/api/cron/renew-chat-subscriptions`)을 추가했습니다. 아직 Google Cloud 쪽 설정(다음 가이드 참고)을 마치기 전까지는 동작하지 않고, 화면에는 "연동 전입니다" 안내만 보입니다.

## v0.82.0 - 2026-08-06

- **업무탭 레이아웃 재구성**: 업무상황판을 "전체 업무목록" 제목 옆에 작은 숫자 배지로 옮겼습니다. 행정요청 기능(교사가 앱에서 직접 입력하던 방식)은 제거했습니다.
- **구글챗 미러링 자리 마련**: 예전 업무상황판/행정요청 자리에 구글챗 두 방(출결알림/선생님요청)을 읽기전용으로 실시간으로 보여주는 패널을 넣었습니다. 메시지마다 바로 "🔧 업무로 등록" 버튼으로 업무를 만들 수 있습니다. (실제 구글챗 연동은 별도 설정 후 동작합니다 - 연동 전에는 "연동 전입니다" 안내만 보입니다.)

## v0.81.0 - 2026-08-06

- **사건기록 "관련 학생" 입력을 검색-선택 방식으로 통일**: 자유 타이핑 칸을 없애고, 이름을 치면 학생 명부에서 검색되어 학년·반·학번까지 보고 정확히 골라 추가하는 방식 하나로 합쳤습니다. 이제 사건에 적히는 학생 이름은 항상 실제 학생 기록과 연결됩니다.

## v0.80.0 - 2026-08-06

- **학생 프로필 "관련 사건기록" 0건 표시 버그 수정**: 사건 입력 시 "관련 학생 연결" 선택을 안 했더라도(자유 텍스트 이름 칸에만 적힌 경우) 그 사건이 학생 프로필에 함께 표시되도록 수정했습니다. (아래 SQL 실행 시 기존 사건도 구조적으로 함께 연결됩니다.)
- **학생 정보 조회 검색범위 확대**: 재적생만 보이던 것을 비재적(전학/졸업 등) 학생도 포함해 검색되도록 넓혔습니다.

## v0.79.0 - 2026-08-06

- **사건·회의 항목 분류를 GIA시스템 기준으로 통일**: 매뉴얼항목(실무자용)/운영계획안항목(학부모용) 선택 목록과 AI 자동 분류가 이제 GIA시스템의 대분류>중분류>세부항목 체계를 그대로 사용합니다. 드롭다운도 대분류별로 묶어서 보여줍니다.
- **예상 문의/컴플레인 기능 제거**: 실무자매뉴얼의 "AI 예상 문의 제안받기" 기능과 제안함의 관련 탭을 없앴습니다.
- **새 사건 입력 시 처리상태 필드 제거**: 새로 입력할 때는 처리상태를 묻지 않고, 기존 사건을 수정할 때만 보이도록 정리했습니다.

## v0.78.0 - 2026-08-06

- **사건기록에 "조치사항" 추가**: 사건이 어떻게 마무리됐는지(실제로 취한 조치)를 남기는 칸을 새로 넣었습니다.
- **좁은 화면용 패널 접기 기능**: 사건기록·회의기록·AI 매뉴얼 작성·실무자매뉴얼 화면에서 목록/AI 제안 패널을 접고 펼 수 있습니다. 접은 상태는 로그인 계정별로 기억되어 다음에 다시 들어와도 그대로 유지됩니다.

## v0.77.1 - 2026-08-06

- **AI 서류 작성 다국어 지원**: 작성 언어를 한국어/영어/한국어+영어 중에서 고를 수 있습니다(국제학교 특성상 영문 서류 필요 대응). 예시 문구도 현장학습 동의서로 교체했습니다.

## v0.77.0 - 2026-08-06

- **AI 서류 작성**: 학교 문서함 > 서류함 옆에 새 메뉴가 생겼습니다. 필요한 서류 상황(예: 근로계약 조건)을 문장으로 설명하면 AI가 초안과 GIA시스템 분류를 함께 만들어주고, 검토 후 저장하면 서류함에 그 분류로 자동 등록됩니다.

## v0.76.0 - 2026-08-05

- **정책 항목·GIA시스템 항목 가나다순 정렬**: 항목 관리 화면, 사건/회의 항목 선택 드롭다운, GIA시스템 세부 항목이 모두 이름순으로 정렬됩니다.
- **학생 프로필 페이지 레이아웃 개선**: 요약 수치 + 바로가기 탭, 긴 목록 안쪽 스크롤 처리로 가독성을 높였습니다.
- **Slack 알림 연동**: 새 계정 등록 신청과 오류 발생 시 Slack으로 알림을 받을 수 있습니다(웹훅 설정 필요).

## v0.75.0 - 2026-08-05

- **제안함 "통합승인" 버튼 명칭 변경**: 학부모용·실무자용이 함께 묶여 나온 제안을 승인할 때 버튼 문구가 "승인 (둘 다)"였는데, 뜻이 더 명확한 "통합승인"으로 바꿨습니다(단일 문서 제안은 그대로 "승인").
- **"다시 분석하기" 기능 추가**: 지금까지는 사건·회의를 한 번 AI로 분석하면(제안함에 이미 들어간 뒤에는) 다시 분석할 방법이 없었습니다.
- **중복 분석 방지 유지**: 자동/일괄 분석은 지금처럼 고유번호 기준으로 한 번만 처리되고, 이미 분석된 기록을 다시 처리하려면 반드시 "다시 분석하기" 버튼을 직접 눌러야만 재분석이 일어납니다 (실수로 같은 기록이 중복 분석되는 일이 없습니다).
- **제안함에서 원본 기록 재분석 가능**: 제안함(검토대기) 화면에서도 사건·행사·회의 유형의 카드라면 바로 "🔄 다시 분석" 버튼으로 원본 기록을 다시 분석할 수 있습니다(원본 화면으로 돌아가지 않아도 됨).

## v0.74.0 - 2026-08-05

- **운영계획안·매뉴얼 고정 항목 체계 도입**: 지금까지는 AI가 사건/회의를 분류할 때마다 항목 이름을 그때그때 자유롭게 지어내서, 같은 주제인데 이름만 다른 항목이 계속 늘어날 수 있었습니다.
- **[정책 항목 관리] 화면 신설**: 학교 문서함 메뉴 안에 새 화면을 추가해, 관리자·행정직원이 운영계획안/매뉴얼 항목의 이름·설명·보유상태를 직접 추가·수정·삭제할 수 있습니다.
- **GIA시스템 편집 권한 확장**: 지금까지 관리자만 GIA시스템 화면을 보고 고칠 수 있었는데, 이제 행정직원도 볼 수 있고 항목명/설명을 직접 수정할 수 있습니다.
- **사건/회의 기록에 항목 선택 드롭다운 추가**: 사건기록·회의기록 작성/수정 화면에 "매뉴얼 항목(실무자용)"과 "운영계획안 항목(학부모용)" 드롭다운을 추가해, 작성자가 직접 위 고정 목록에서 골라 태그할 수 있습니다(비워두면 AI가 스캔할 때 자동으로 채웁니다).
- **기존 기록 소급 태깅**: [정책 항목 관리] 화면에 "🔄 기존 기록 소급 태깅" 버튼을 추가해, 새 항목 체계 도입 전에 이미 저장된 사건/회의도 AI가 훑어서 고정 목록 중 해당하는 항목으로 자동 태깅할 수 있습니다.

## v0.73.1 - 2026-08-05

- **출석부 5열 그리드**: 학생 목록을 화면 넓이에 맞춰 최대 5열 그리드로 배치했습니다.
- **출석부 메뉴 임시 비공개**: 사이드바 메뉴에서 출석부 항목을 뺐습니다.

## v0.73.0 - 2026-08-05

- **업무 삭제 버그 수정**: 업무를 삭제해도 등록자·담당자·관리자 화면에는 계속 목록에 남아있던 문제를 고쳤습니다.
- **업무 휴지통 영구삭제/비우기**: 휴지통에 "🗑️ 영구삭제"(개별)와 "🗑️ 휴지통 비우기"(일괄) 버튼을 추가했습니다.
- **행정요청 완료 처리자 표시**: 행정요청이 완료되면 누가 처리했는지(이름)가 요청 카드에 함께 표시됩니다.
- **학생 출석부 신설**: 담임교사가 매일 학생별 출결(출석/지각/결석/조퇴/기타)을 실시간으로 체크하고, 행정직원·관리자 등 다른 직원도 같은 화면을 실시간으로 보면서 결석·조퇴한 학생의 보호자에게 바로 전화/이메일로 연락하고 연락 완료 여부를 기록할 수 있는 "🗒️ 출석부" 메뉴를 새로 만들었습니다.
- **Toddle/ClassDojo 연동 조사**: 정규학기(Toddle)·계절학기(ClassDojo)에서 쓰는 학부모 소통 앱과 gia-ops 앱을 자동 연동할 방법을 조사했습니다.

## v0.72.0 - 2026-08-05

- **업무 코멘트/실시간 로그 분리**: 업무 상세창에서 코멘트를 남기면 상태변경·업무확인 등 시스템 로그가 코멘트창에도 함께 섞여 보이던 문제를 고쳤습니다.
- **GIA시스템 AI 제안 범위 제한**: "✨ AI로 추가/세분화 체크"(기존 "AI로 벤치마킹 제안받기")는 이제 기존에 정리해둔 항목을 절대 고치거나 지우지 않고, ①GIA에 아직 없는 시스템을 새로 추가하거나 ②이미 있는 항목 중 너무 뭉뚱그려진 것을 더 구체적인 하위 항목으로 쪼갤 수 있는지만 체크해서 제안하도록 프롬프트와 저장 로직을 다시 짰습니다.
- **GIA시스템 드롭다운 구조**: 대분류 안의 중분류 묶음이 전부 펼쳐진 채로 나열되던 것을, 기본은 접혀있고 눌러야 펼쳐지는 드롭다운으로 바꿨습니다.
- **학생 통합 조회 재확인**: "학생 정보 조회" 메뉴(관리자·행정직원)에서 이름/학번으로 검색해 들어가면 학적사항·학적이력·사건기록·주간 학생 관찰기록·업무/채팅 언급까지 한 화면에서 보이는 기존 기능을 재확인했습니다(신규 구현 아님).

## v0.71.0 - 2026-08-05

- **사건기록 채택·발행 통합**: 제안함에서 학부모용/실무자용을 따로 체크해 채택하던 방식을 없애고, 승인·보류·삭제가 항상 둘을 함께 처리하도록 통합했습니다(운영계획안 내용은 자동으로 실무자매뉴얼에도 들어가야 하므로).
- **채택 문구 톤 개편**: 운영계획안(학부모용)은 정중하고 친절한 안내 톤으로, 실무자매뉴얼 (실무자용)은 명확한 지시 톤으로 다듬되, 둘 다 법령 조문처럼 명시적이고 정확한 표현을 유지하면서 딱딱한 "~다"체 대신 정중한 "~하는 것으로 한다." 체로 종결하도록 AI 프롬프트를 손봤습니다.
- **행정요청 메뉴 개편**: 행정요청 메뉴는 이제 교사에게만 보이고, 관리자/행정직원은 업무 탭의 업무상황판에서 미처리 행정요청 건수를 바로 확인합니다(등록 시 자동으로 업무보드에도 올라가고 처리 상태가 자동 동기화되므로 별도 메뉴가 필요 없습니다).

## v0.70.0 - 2026-08-05

- **관리자 통합 대시보드 복구**: 반복 사건 유형, 학생 랭킹, 월별 운영 추이, 부서별 업무 완료율 같은 관리자 전용 운영 분석을 별도의 "관리자 통합 대시보드"(관리자 메뉴 → 통합 대시보드)로 다시 분리했습니다.
- **채택예정 화면 개편**: 제안함과 같은 방식으로 카테고리 탭(사건/행사/회의/AI매뉴얼/예상 문의/GIA시스템), 원본 사건 개요 표시, 학부모용·실무자용 카드 통합을 채택예정 화면에도 적용했습니다.
- **GIA시스템 항목 전면 재설계**: 기존 6개 항목(구비서류/내규/계약서/학생관리/교사관리/교직원관리)을 지우고, 다른 공립·사립·국제학교가 일반적으로 갖추는 시스템을 참고해 대분류(재정/인사·교직원/ 학사/운영/시설·안전/입학·홍보/행정·문서/정보보안·법무) → 중분류 → 세부 항목의 3단계로 44개 항목을 새로 구성했습니다.
- **AI 프롬프트 공유 캐시 블록 통합**: 기관 소개문과 참고 법령 목록을 쓰는 6개 AI 프롬프트(사건 분류, AI매뉴얼 초안 분류, 서류 추천, 서류 초안, 예상 문의/컴플레인, 채택예정 AI 검증)가 각자 전체 시스템 프롬프트를 하나의 문자열로 캐싱하던 것을, 공용 부분(SHARED_CACHE_CONTEXT)과 라우트별 부분으로 나눠 별도의 캐시 블록으로 보내도록 바꿨습니다.

## v0.69.1 - 2026-08-05

- **옵션 선택 시 최종 채택 내용 자동 기입**: 제안함에서 보완방안/학부모 안내멘트/학생 교육방법 옵션 앞에 동그라미(라디오) 선택을 추가했습니다.
- **재발방지 옵션을 실제 절차로 강화**: AI가 제안하는 보완/재발방지 방안이 추상적인 다짐이 아니라, 상황 발생 → 담당자의 즉시 조치 → 학부모에게 안내할 선택지 → 선택지별 후속 조치 → 단계별 학부모 고지 시점 → 최종 인계 확인까지, 실제로 그대로 따를 수 있는 순서와 책임 소재가 분명한 절차로 작성되도록 프롬프트를 강화했습니다.
- **즉시 채택 전 확인창**: 제안함 목록에서 학부모용/실무자용 체크박스를 눌러 바로 채택예정으로 보낼 때, 사건명과 최종 채택 내용을 다시 보여주고 확인을 받은 뒤에만 진행합니다.
- **채택예정 되돌리기**: 채택예정 화면에 "↩️ 되돌리기" 버튼을 추가했습니다.

## v0.69.0 - 2026-08-05

지난 통합 개선 제안서(v0.68.0)의 8개 항목을 전부 반영하고, AI 비용을 더 아끼는 방향으로 함께 설계했습니다.

- **① 원본 사건/회의 역참조 링크**: 매뉴얼 항목이 어떤 사건/행사/회의/GIA시스템 기록에서 왔는지 카드 하단에 칩으로 표시하고, 원본 목록 화면으로 바로 이동할 수 있습니다.
- **② 반복 사건 패턴 ↔ 매뉴얼 연결**: 홈 화면의 "최근 90일 반복 사건" 판정(AI 호출 없이 순수 집계, 비용 0)을 매뉴얼 화면에도 그대로 적용해, 관련 항목 카드에 "🔥 반복 사건 N건" 배지를 띄웁니다.
- **③ GIA시스템 자동 매칭**: 매뉴얼이 발행될 때, "미보유" 상태인 GIA시스템 항목의 이름이 발행 문구에 그대로 언급되어 있으면(AI 호출 없이 단순 포함 여부만 확인) 자동으로 연결해두고, GIA시스템 화면에서 안내 문구로 보여줍니다.
- **④ 정책영역(도메인) 분류**: 사건/회의/AI매뉴얼 작성 시 AI가 항목을 8개 정책영역(학사· 안전/보건·시설·인사·재무/환불·개인정보·민원대응·행정일반) 중 하나로 함께 분류합니다(기존 분류 호출에 슬쩍 얹어서 별도 AI 호출은 늘리지 않았습니다).
- **⑤ 매뉴얼 변경 이력 관리**: 항목이 수정될 때마다 이전 내용이 자동으로 이력에 남고, "변경 이력" 버튼으로 과거 버전을 언제든 확인할 수 있습니다.
- **⑥ AI매뉴얼 작성 시 유사 기록 추천**: 새 초안을 작성하면, AI 호출 없이 키워드 겹침만으로 과거 사건/회의/이미 발행된 매뉴얼 중 관련 있어 보이는 기록을 함께 보여줍니다.
- **⑦ 발행 전 학부모 관점 체크리스트**: 학부모용 항목을 AI 검증할 때는 "학부모가 이 문구만 읽고 오해 없이 이해할 수 있는가"를 최우선 관점으로 점검하도록 프롬프트를 강화하고, 버튼 라벨도 "학부모 관점 AI 검증"으로 구분했습니다.
- **⑧ 매뉴얼 정기 리뷰 사이클**: 매주 월요일, 6개월 이상 수정되지 않은 항목과 최근 90일 내 관련 사건이 5건 이상 급증한 항목을 자동으로 찾아 매뉴얼 화면 상단에 배너로 알립니다(AI 호출 없이 조건 판정만).
- **AI 과금 최소화**: 학부모용 승인 시 톤을 다듬는 AI 호출을 Sonnet에서 Haiku로 낮췄습니다 (이미 승인된 문구를 존댓말로만 바꾸는 단순 작업이라 저렴한 모델로 충분합니다).

## v0.68.0 - 2026-08-05

- **제안 카드에 사건 개요 노출**: 사건/행사/회의/AI매뉴얼 초안에서 나온 제안을 펼치면, 그 제안이 어떤 원본 기록에서 나온 건지 제목·내용 요약을 함께 보여줍니다.
- **체크박스로 즉시 채택**: 카드를 펼치지 않아도 목록에서 체크 한 번으로 바로 승인해 채택예정으로 옮길 수 있습니다.
- **"다음 옵션" 반복 문구 제거**: 보완방안/학부모 안내멘트/학생교육 옵션이 화면에 구분자 글자 없이 번호가 매겨진 카드 형태로 깔끔하게 표시됩니다.
- **제안서 형식으로 가독성 개선**: 사건 개요 / 채택 내용 / 제안 옵션 / 법령 근거를 구분선으로 나눠 문서처럼 읽기 쉽게 재구성했습니다.
- **학부모용·실무자용 통합 카드**: 같은 사건에서 나온 학부모용/실무자용 제안 두 건이 카드 하나로 합쳐지고, 안에서 탭으로 전환해서 봅니다(전에는 같은 사건이 카드 두 개로 따로 보였습니다).
- **학부모용 승인 시 톤 다듬기 확대**: 전에는 예상 문의(컴플레인) 제안만 승인 시 AI가 톤을 다듬었는데, 이제 모든 학부모용 승인 건이 정중하고 친절한 문구로 한 번 더 다듬어집니다.
- **AI가 기존 항목을 우선 재사용**: 사건/행사/회의/AI매뉴얼 제안을 만들 때, 운영계획안· 실무자매뉴얼에 이미 있는 항목 목록을 AI에게 실제로 전달해서, 비슷한 내용이 항목명만 다르게 계속 새로 생기는 문제를 줄였습니다.
- **사건·회의·AI매뉴얼 통합 개선 제안서**: 앞으로 더 유용하게 연결할 수 있는 방안(발행 후 원본 연결 유지, GIA시스템 자동 매칭, 매뉴얼 변경 이력 관리 등)을 정리한 제안서를 작성해 전달했습니다.

## v0.67.0 - 2026-08-05

- **행정요청 카테고리를 관리자가 직접 관리**: 사물함파손/물품구입 같은 요청 분류를 관리자가 화면에서 새로 추가하거나 숨길 수 있습니다(운영관리 화면이 아니라 행정요청 화면 안에 "카테고리 관리" 패널로 있습니다).
- **행정요청 등록 시 초등부 업무보드에 자동 등록**: 교사가 요청을 등록하면 초등부 전체 업무창에도 업무가 함께 생성됩니다.
  - 업무보드에서 담당자가 "확인" 체크를 하면 요청 목록의 빨간불이 초록불로 자동으로 바뀌고, 업무를 완료로 옮기면 요청도 자동으로 완료 처리됩니다.
  - 교사 화면은 "내가 등록한 요청"과 그 아래 "완료된 요청" 두 목록으로 나눠서, 진행 중인 요청과 끝난 요청을 한눈에 구분할 수 있습니다.
- **행정요청 코멘트 기능**: 요청 하나하나에 코멘트를 주고받을 수 있고, 새 코멘트는 실시간으로 반영됩니다.
- **한/영 자동 번역**: 요청 제목·내용과 코멘트를 한국어로 적든 영어로 적든 자동으로 번역되어 함께 표시됩니다(원어민 교사가 많은 점을 고려했습니다).
- **행정요청 화면 전체 영어 병기**: 버튼·라벨·안내 문구에 영어를 함께 표기했습니다.

## v0.66.1 - 2026-08-05

- **교사 메뉴 영어 병기**: 교사 계정에 원어민 교사가 있을 수 있어, 사이드바·모바일 메뉴의 "행정요청"에 영어("Staff Requests")를 함께 표기했습니다.
- **메뉴 줄바꿈 정리**: 한글/영어 라벨이 길어져도 어색하게 줄이 꺾이지 않고 한 줄로 깔끔하게 잘리도록(말줄임표) 사이드바·모바일 메뉴 스타일을 정리했습니다.

## v0.66.0 - 2026-08-05

- **교사 메뉴에서 학사일정 제거**: 교사 계정에는 더 이상 학사일정 메뉴가 보이지 않습니다.
- **행정요청 메뉴 신설**: 교사가 사물함 파손·물품 구입·아픈 학생 인계·출결 상황 문의처럼 행정직원에게 부탁할 일을 등록하면, 행정직원/관리자가 접수대기 → 처리중 → 완료로 상태를 바꾸고 처리 메모를 남길 수 있습니다.
- **권한 미리보기 전환 버그 수정**: 관리자가 "교사"로 미리보기 전환한 뒤 다른 권한으로 다시 바꿀 수 없던 문제를 고쳤습니다.
- **브라우저 탭 아이콘 교체**: Vercel 기본 아이콘 대신 로그인 화면의 GIA 크레스트 로고가 탭 아이콘으로 표시됩니다.

## v0.65.0 - 2026-08-05

- **운영관리 부메뉴 정리**: "매뉴얼" 항목을 뺐습니다.
- **화면 전체 크래시 방지 강화**: 검토대기 배지처럼 부가적인 정보 조회가 실패해도 로그인·사이드바를 포함한 화면 전체가 함께 죽지 않도록 서로 독립시켰습니다.
- **로그성 데이터 조회 속도 개선**: 계속 쌓이기만 하는 오류·AI 사용 기록, 미해결 문의 조회에 인덱스를 추가했습니다.
- **동시 사용 안전장치 확대**: 학기 전환과 채택예정 발행을 여러 명이 거의 동시에 눌러도 데이터가 꼬이지 않도록 서버에서 한 번에 처리하게 바꿨습니다.
- **관리자 대시보드에 시스템 상태 카드 추가**: 최근 24시간 오류 급증 여부와 마지막 백업 시각을 한눈에 볼 수 있습니다.
- **자동 일일 백업**: 매일 한 번 핵심 데이터를 자동으로 백업해두어, 수동 백업을 깜빡한 날에도 최소한의 안전망이 있습니다.

## v0.64.0 - 2026-08-05

- **업무탭 좌우 분할, 부서 메모장 추가**: "🔔 실시간 로그" 자리가 왼쪽 메모장 / 오른쪽 실시간 로그로 나뉩니다.
- **학교관리 부메뉴 정리**: 구글시트로 가져오기 항목을 부메뉴에서 뺐습니다.
- **부메뉴 구분선 추가**: 운영 관리 / 학교 관리 / 학교 문서함 / 주간 학생 관찰기록 / 관리자 부메뉴에 목적별 구분선을 넣어 항목이 많은 메뉴도 한눈에 구분되도록 했습니다.
- **신청서 가져오기 학기선택을 실제 등록된 학기로 연동**: 이전에는 연도를 직접 입력하고 학기 종류를 고정 목록에서 골랐는데, 이제 학기 관리 화면에 실제로 등록된 연도·학기 목록을 그대로 보여줍니다.
- **버전 기록 화면 정리**: 기술적인 DB 변경 안내를 빼고, 어떤 기능이 새로 생겼는지만 짧고 읽기 편하게 정리했습니다.
- **빌드 최적화**: 매뉴얼 리치 텍스트 편집기를 실제로 편집 화면을 열 때만 불러오도록 바꿔, 매뉴얼을 조회만 하는 방문에서는 불필요한 코드를 받지 않도록 했습니다.

## v0.63.0 - 2026-08-05

- **신청서 붙여넣기 전 학기/목적 선택**: 신청서(학기/행사) 탭에서 구글시트를 붙여넣기 전에 연도·학기(또는 캠프)·목적을 먼저 선택하도록 바꿨습니다(예: "26년 3학기 인원모집", "26년 여름캠프2 바자회 행사").
- **학사일정 → 학사일정달력 / 학기준비 두 화면으로 분리**: 기존 학사일정 화면은 "학사일정달력"으로 이름을 유지하고(체크리스트 그대로), 새로 "학기준비" 화면을 추가했습니다.
- **학기준비 화면 신설**: 연도와 학기(또는 캠프)를 선택하면 같은 학기 유형의 지난 회차 기록을 한 번에 모아 보여줍니다 - ①지난 학기 돌아보기(잘한 점/아쉬운 점/다음 제안), ②그때 가져온 신청서(구글폼) 기록(연도·목적별 건수), ③그 학기 준비 기간에 있었던 업무·회의 타임라인.
- **관리자 권한 미리보기(역할 전환)**: 관리자 계정 사이드바의 로그아웃 버튼 바로 위에 "🎭 권한 미리보기" 드롭다운이 생겼습니다.

## v0.62.0 - 2026-08-05

- **GIA/다크 테마 로고 가시성 수정**: 로고 이미지가 투명 배경 위에 남색 잉크로만 그려져 있어서 짙은 남색 사이드바(GIA 테마) 위에서 거의 안 보였습니다.
- **관리자 전용 이름/소속 편집**: 사용자 관리 화면에서 관리자 계정으로 로그인하면 이름 옆에 ✏️ 버튼이 나타나 이름/소속을 직접 정정할 수 있습니다.
- **신청서(구글폼) 가져오기**: 학교관리 → 구글시트로 가져오기 화면에 "📋 신청서(학기/행사)" 탭을 추가했습니다.

## v0.61.1 - 2026-08-05

- **GIA 테마 본문 배경 밝게 조정**: 바로 전 업데이트(v0.61.0)에서 GIA 테마를 고르면 사건기록· 매뉴얼 등 본문 화면까지 사이드바와 같은 짙은 남색 배경을 썼는데, 검은 글자와 대비가 안 맞아 읽기 힘들다는 피드백을 반영해 사이드바/헤더는 짙은 남색 그대로 두고, 본문 배경만 골드 톤과 어울리는 밝은 아이보리/샴페인 그라디언트로 바꿨습니다.

## v0.61.0 - 2026-08-05

- **페이지 배경도 테마 적용**: 지금까지는 사이드바/헤더 같은 공용 틀만 테마(라이트/다크/ 리퀴드글라스/GIA)를 따라가고, 사건기록·회의기록·제안함·매뉴얼·문서함·사용자관리·학사일정 등 실제 화면 배경은 항상 밝은 회색 고정이었습니다.
- **테마별 글자색 가독성 보정**: 배경이 어두워지는 다크·GIA 테마에서, 화면 안의 글자색이 기존처럼 어두운 톤 그대로면 안 보이는 문제가 있어 자동으로 밝은 톤으로 바뀌도록 손봤습니다.

## v0.60.0 - 2026-08-04

- **오류 화면**: 서버 쪽 오류가 나면 이제 Next.js 기본 크래시 화면 대신, 다시 시도/새로고침 버튼이 있는 우호적인 화면이 뜹니다.
- **삭제 확인/실패 알림**: 브라우저 기본 confirm/alert 대신 앱 안에서 자연스럽게 뜨는 확인창과 토스트 알림으로 20개 화면(사건/업무/채팅/제안함/문서/매뉴얼 등 46곳)을 전부 바꿨습니다.
- **업무 휴지통(7일 복구)**: 업무를 삭제해도 즉시 영구 삭제되지 않고, 사이드바 업무탭의 "🗑 휴지통"에서 7일 안에 복구할 수 있습니다(등록자·담당자·관리자만 볼 수 있음).
- **커맨드팔레트**: 어디서든 ⌘K/Ctrl+K를 누르면 메뉴 바로가기 + 통합검색을 한 번에 쓸 수 있는 창이 뜹니다.
- **연결 끊김 알림**: 인터넷이 끊기면 상단에 배너로 알리고, 다시 연결되면 자동으로 화면을 최신 내용으로 갱신합니다.
- **알림 묶어보기**: 프로필 옆 빨간 배지를 누르면 숫자만이 아니라 실제로 어떤 업무가 미확인인지 목록으로 바로 보여줍니다.
- **업무 선후관계**: 업무 상세에서 "선행 업무"를 지정할 수 있고, 아직 안 끝났으면 경고 배너로 알려줍니다(강제로 막지는 않습니다).
- **사건기록 템플릿**: 새 사건 입력 화면에 안전사고/교우관계/규칙위반/학부모민원 등 자주 쓰는 유형의 시작 틀(빠른 시작 버튼)을 추가했습니다.
- 참고: 알림을 매일 이메일로 받아보는 기능은 별도 이메일 발송 서비스(API 키) 연동이 필요해 이번 라운드에서는 빼고, 대신 위 "알림 묶어보기"로 인앱에서 확인할 수 있게 했습니다.

## v0.59.0 - 2026-08-04

- **버전 기록**: 사이드바 문의및건의사항 바로 아래에 현재 버전(v0.59.0)이 뜨고, 누르면 지금까지의 모든 업데이트 내역을 최신순으로 볼 수 있는 [버전 기록] 화면(`/changelog`)으로 이동합니다.
- **테마 정교화**: 리퀴드글라스 테마는 사이드바 메뉴 버튼이 실제로 반투명+블러 유리질감으로 뜨고, GIA 테마는 사이드바 배경이 로고 남색, 선택된 메뉴가 골드 톤으로 강조되도록 다듬었습니다.
- **모션**: 사이드바 부메뉴(플라이아웃)와 검색 결과가 아래로 살짝 미끄러지며 나타나고, 업무 알림 배지가 새로 생길 때 팝(pop)하듯 나타나며, 메뉴 버튼을 누르면 살짝 눌리는 느낌을 주는 등 절제된 모션을 셸 전반에 추가했습니다(동작이 불편한 분들을 위해 시스템의 "동작 줄이기" 설정을 따르도록 했습니다).

## v0.58.0 - 2026-08-04

- **실시간 로그 세 줄**: 업무 탭 하단 "실시간 로그" 고정 높이 영역이 5줄 → 3줄로 줄었습니다 (헤더 클릭 시 뜨는 "전체보기" 팝업은 그대로 전체를 보여줍니다).
- **테마 4종**: [내 계정 설정]에 테마 선택이 추가됐습니다 - 라이트(기존)/다크/리퀴드 글라스(반투명+블러)/GIA(로고 남색+골드).

## v0.57.7 - 2026-08-04

알림 표시를 두 곳으로 나눴습니다.

- **프로필 아이콘 모서리(빨간 원)**: 이제 "미확인" 업무 개수만 보여줍니다.
- **프로필 이름 오른쪽(회색 사각 배지)**: 확인 여부와 무관하게 "내 업무" 총 개수를 항상 보여줍니다.
- **즉시 반영 버그 수정**: "업무 확인" 체크박스를 누르면 Realtime 전파를 기다리지 않고 그 자리에서 바로 다시 세도록 고쳤습니다 - 예전에는 체크한 뒤 다른 페이지로 이동해야만 깜빡임이 사라졌습니다.

## v0.57.6 - 2026-08-04

- **숫자**: 이제 "새로 생긴 업무 수"가 아니라 "지금 내 업무함에 있는 업무 개수"입니다(내 업무 목록 위젯과 동일한 기준: 내가 등록자거나, 태그된 담당자거나, [전체]로 등록된 업무 중 아직 완료 안 된 것).
- **깜빡임**: 그중 내가 아직 "업무 확인" 체크를 안 한 업무가 하나라도 있으면 빨간색으로 깜빡입니다.

## v0.57.5 - 2026-08-04

알림 배지에서 "새 업무" 개수가 안 뜨는 문제를 고쳤습니다.

- 원인: 업무 배지는 "마지막으로 업무 탭을 연 시각 이후 등록된 업무"만 셌는데, 업무 탭을 아직 한 번도 연 적이 없으면(=task_list_reads에 내 기록이 아직 없으면) 그 기준 시각이 없어서 통째로 0으로 처리하고 있었습니다.

## v0.57.4 - 2026-08-04

알림 배지(안 읽은 채팅 + 새 업무) 기능을 다시 넣었습니다.

- **원인**: 배지가 데스크톱 사이드바 프로필 옆 + 모바일 헤더 계정 아이콘 옆, 두 군데에 동시에 렌더링되는데(화면 크기에 따라 CSS로만 숨기는 방식이라 화면 크기와 무관하게 둘 다 항상 마운트됨), 각 배지가 자기 안에서 데이터 조회 + Realtime 구독을 따로 했습니다.
- **수정**: 데이터 조회/실시간 구독 로직을 NotificationProvider 하나로 분리해서 화면 최상단(layout)에서 딱 한 번만 실행되게 하고, 실제 눈에 보이는 배지 두 개는 그 결과를 Context로 읽기만 하는 얇은 컴포넌트로 바꿨습니다.
- **DB**: task_list_reads 테이블을 다시 만듭니다.

## v0.57.3 - 2026-08-04

스테이징 주소가 "This page couldn't load"로 아예 안 뜨는 문제 진단 중 - v0.57.2(알림 배지)를 일단 되돌렸습니다.

- v0.57.2에서 추가한 사이드바 알림 배지(NotificationBell)와 관련 코드를 전부 되돌렸습니다 (task_list_reads 조회/구독, 프로필 옆 빨간 배지 UI).

- Vercel Authentication(배포 보호)은 이미 꺼져 있는 상태에서도 로드가 안 됐던 걸 확인했고, 서버로 직접 보낸 요청은 계속 정상 응답이 와서 배포 자체가 완전히 죽은 건 아니었습니다.

## v0.57.1 - 2026-08-04

사이드바 메뉴 하이라이트 버그를 고치고, AI 기능 on/off를 진짜 스위치 모양으로 바꿨습니다.

- **메뉴 하이라이트 버그 수정**: "학교관리"를 누르면 "관리자"까지 같이 색이 바뀌고, "학교문서함"을 누르면 "학교관리"·"관리자"가 같이 바뀌고, "관리자"를 눌러도 "학교관리"가 같이 바뀌던 문제를 고쳤습니다.
- **AI 기능 on/off를 스위치 버튼으로**: 관리자 대시보드의 "켜짐/일시정지중" 텍스트 버튼을 실제로 딸깍 켜고 끄는 토글 스위치 모양으로 바꿨습니다.

## v0.57.0 - 2026-08-04

학생 명부 관리(학생 관리 페이지)에 학생 인적사항 항목을 늘리고, 앞으로 필요한 항목을 직접 추가할 수 있는 "칼럼 추가" 기능과 구글시트 같은 정렬 기능을 넣었습니다.

- **기록 항목 추가**: 학생이름·영어이름은 이미 있었고, 이번에 보호자 이메일·성별·알러지를 새로 추가했습니다(보호자 연락처·주소·생일은 이미 있던 항목을 이 화면에서도 바로 입력/수정할 수 있게 했습니다).
- **칼럼 추가 기능**: "+ 칼럼 추가" 버튼으로 원하는 이름(예: 형제자매, 통학버스 노선)과 입력 형식(텍스트/숫자/날짜)을 정해 새 칼럼을 만들 수 있습니다.
- **학생 명부 표 + 정렬**: 학생 명부 관리(주간 학생 관찰기록 → 관리 → 학생 명부 관리)에서 전체 학생을 표로 보고, 학년 → 반 → 이름(가나다) 순으로 기본 정렬됩니다.
- 모든 칸은 클릭해서 바로 수정할 수 있습니다(기존 영어이름 인라인 수정 방식을 전체 칸으로 확장).
- 학생 통합 프로필(/students/[id]) 화면에도 보호자 이메일·성별·알러지·직접 추가한 칼럼 값이 함께 표시됩니다.

## v0.56.11 - 2026-08-04

- **접속자 배지 툴팁 개선**: 브라우저 기본 툴팁은 뜨기까지 시간이 걸리고 잘 안 보여서 놓치기 쉬웠습니다.

## v0.56.10 - 2026-08-04

새 업무가 "내 업무목록"에 들어오면(내가 태그되었거나 [전체]로 등록된 경우) 확인창(alert) 없이 화면 오른쪽 아래에 잠시 알림이 떴다가 자동으로 사라지도록 했습니다.

- 이미 있던 "업무 상태 변경 토스트"와 같은 방식으로, 새 업무 등록 시에도 알림이 뜨도록 확장했습니다.
- 내가 직접 등록한 업무는 이미 알고 있으니 알림에서 제외됩니다.

## v0.56.9 - 2026-08-04

접속중인 사람이 누구인지 마우스로 확인할 수 있게 하고, 업무 상세창의 코멘트 영역을 코멘트/처리사항 둘로 나누고, 처리사항이 업무기록과 보고서(화면+PDF)까지 이어지도록 만들었습니다.

- **접속자 호버 툴팁**: 업무 탭 상단 "🟢 N명 접속중" 배지에 마우스를 올리면 지금 이 페이지를 보고 있는 사람들의 이름이 뜹니다.
- **코멘트/처리사항 분할**: 업무를 클릭해 여는 상세창의 코멘트 영역을 위/아래로 나눴습니다.
- **처리사항이 보관함·보고서까지 이어짐**: 처리사항은 업무 자체에 저장되는 값이라 업무가 완료→보관함(업무기록)으로 넘어가도 그대로 남고, 업무기록 화면과 업무 보고서 화면·PDF에서도 "처리사항/처리결과"로 함께 보입니다.
- **코멘트가 실시간 로그에도 기록됨**: 업무에 코멘트를 남기면 상단 실시간 로그에도 "OOO님이 코멘트를 남겼습니다: ..." 한 줄로 함께 기록됩니다.
- **실시간 로그 5줄 제한**: 확인해보니 이 부분은 이전 작업에서 이미 반영되어 있었습니다 (실시간 로그는 최대 5줄까지만 보이고, 위로 스크롤하면 과거 로그를 더 불러옵니다).

## v0.56.8 - 2026-08-04

업무 확인목록에서 등록자 본인을 빼고, "내 업무목록"을 내 것/전체 두 칸으로 나누고, 데이터 백업/복원 기능을 새로 만들었습니다.

- **확인목록에서 등록자 제외**: [전체] 모드로 업무를 등록하면 부서원 전원(등록자 포함)이 담당자가 되는데, 예전에는 등록자 본인도 "확인 안 함"으로 남아있었습니다.
- **내 업무목록 / 전체 업무목록 좌우 분할**: 업무 탭 오른쪽 위 "내 업무목록" 자리를 반으로 나눠, 왼쪽은 내가 등록했거나 태그되거나 [전체] 모드인 업무만, 오른쪽은 지금 볼 수 있는 업무 전체를 보여줍니다.
- **데이터 백업/복원 신설**: 관리자 메뉴에 "데이터 백업" 화면을 추가했습니다.
- **보안 PIN 관련**: 이전 버전(v0.56.7)에서 PIN 2차 확인을 없앤 데 이어, 이번 백업 기능이 "데이터가 꼬여도 되돌릴 수 있는" 안전망 역할을 합니다.

## v0.56.7 - 2026-08-04

보안 PIN 2차 확인 기능을 없애고, 업무 등록 시 고른 공개범위([나]/[공유]/[전체])가 실제 조회 단계에서도 지켜지도록 손봤습니다.

- **PIN 2차 보안 제거**: 구글 로그인(회사 계정만 허용) + 관리자 승인 두 단계로도 충분히 안전하다고 판단해, 로그인 뒤 한 번 더 개인 PIN을 입력하던 화면을 없앴습니다.
- **업무 공개범위를 실제로 제한**: 지금까지는 [나](개인)/[공유](태그)로 등록해도 화면에서만 안 보이게 감춰뒀을 뿐, 데이터 자체는 giamicro.com 로그인 계정이면 누구나 조회할 수 있었습니다.
- **내 업무목록 위젯 보완**: "내 업무목록"이 이제 내가 등록한 업무와 나를 태그한 업무를 함께 보여줍니다(예전에는 담당자로 태그된 것만 보였음) - [공유]로 다른 사람만 태그해도 등록한 나에게 바로 보입니다.

## v0.56.6 - 2026-08-04

사이드바 왼쪽 아래 미니 달력을 누르면 학사일정 페이지로 바로 넘어가도록 바꿨습니다.

- **달력 클릭 → 학사일정 이동**: 날짜 칸이든 달력의 다른 빈 곳이든, 어디를 눌러도 `/academic-calendar`(학사일정)로 이동합니다.
- **OS 캘린더 앱 열기 기능 제거**: 날짜를 누르면 맥 캘린더 앱(또는 .ics 다운로드)을 여는 기존 기능이 실제로는 잘 뜨지 않는다는 제보를 받아 제거했습니다.
- **"학사일정 보기" 버튼 제거**: 달력 자체를 누르면 바로 이동하므로 별도 하단 버튼은 중복이라 없앴습니다.

## v0.56.5 - 2026-08-04

- **화면을 막던 조회 축소(5개 → 3개)**: 로그인 확인과 배지 숫자만 화면 렌더링 전에 꼭 필요하고, 학기 배지·AI 기능 정지 배너는 "장식성" 정보라 화면을 막을 이유가 없었습니다.
- 메뉴/페이지별 데이터 조회 구조를 점검해 대부분 이미 병렬 조회로 잘 구성돼 있음을 확인했습니다(사건기록/홈 등 - 이전 라운드에서 이미 최적화됨).

**추가로 확인해보시면 좋은 인프라 설정 2가지** (코드로는 손댈 수 없어 대시보드에서
직접 확인이 필요합니다):
1. Supabase 대시보드 → Settings → API → JWT Keys에서 "Legacy JWT secret" 대신
   "Asymmetric keys(비대칭키)"를 쓰고 있는지 확인해주세요. 비대칭키면 로그인 확인이
   외부 네트워크 왕복 없이 로컬에서 끝나 모든 페이지 이동이 한 단계 더 빨라집니다.
2. Vercel 프로젝트 리전과 Supabase 프로젝트 리전이 같은 지역(가까운 지역)인지
   확인해주세요. 두 서비스가 멀리 떨어져 있으면 DB 조회 한 번마다 그 거리만큼
   지연이 계속 누적됩니다.

## v0.56.4 - 2026-08-04

메뉴를 눌렀을 때 화면 전환이 느리다는 제보를 확인해 수정했습니다.

- **메뉴 hover 시 미리 불러오기 복구**: 마우스를 메뉴 위에 올리는 순간(부메뉴가 펼쳐지는 순간 포함) 그 메뉴가 갈 화면 데이터를 미리 받아두도록 했습니다.
- **최근 열어본 화면 재사용(30초)**: 같은 메뉴를 30초 안에 다시 열면 서버에 다시 묻지 않고 방금 받아둔 화면을 그대로 재사용하도록 설정했습니다.
- 상태표시줄에 링크 주소가 안 뜨는 기존 동작은 그대로 유지됩니다.

## v0.56.3 - 2026-08-04

여러 사람이 "AI 제안 만들기"(사건/행사/회의 새 기록 분석)를 거의 동시에 눌러도 같은 기록이 중복 처리되지 않도록 손봤습니다.

- **원자적 선점(claim) 처리**: AI를 부르기 전에 먼저 "이 기록은 내가 처리한다"고 DB에 원자적으로 표시합니다.
- **DB 이중 방어선**: 사건/행사에서 만들어지는 제안에 유니크 제약을 추가해, 혹시 위 로직에 틈이 있어도 같은 기록·같은 문서용 제안이 중복 저장되는 걸 DB가 한 번 더 막아줍니다.

## v0.56.2 - 2026-08-04

학사일정 달력에도 대한민국 공휴일이 표시됩니다(사이드바 메인 달력과 동일한 공휴일 데이터).

## v0.56.1 - 2026-08-04

사이드바 위쪽을 더 정돈하고, 메뉴가 스크롤 없이 한눈에 더 많이 보이도록 손봤습니다.

- **학기 표시 가운데 정렬**: 로고 아래 진행중인 학기 배지가 가운데로 정렬됩니다.
- **검색+달력 위젯 통합**: 따로 있던 통합 검색창과 축소 달력을 검색이 위, 달력이 아래에 오는 하나의 상자로 합치고 전체 크기를 더 줄였습니다 - 그만큼 메뉴 목록이 보이는 영역이 넓어집니다.
- **메뉴 목록 컴팩트화**: 사이드바 카테고리 각 줄의 위아래 여백을 줄여서, 같은 화면 높이에 더 많은 메뉴가 스크롤 없이 한눈에 들어옵니다.

## v0.56.0 - 2026-08-04

새 메뉴 "학사일정"을 추가했습니다.

- **학사일정 달력 화면 (`/academic-calendar`, 신규)**: 이번 달 달력에 학사 준비 업무가 날짜별로 표시됩니다(예: 학기 시작 14일 전 - 학생명단 확정, 7일 전 - 교실 물품 점검).
- **사이드바 메인 달력과 연동**: 사이드바에 항상 떠 있는 달력(시계 위젯)에도 학사일정이 있는 날짜에 작은 초록 점이 표시되고, 달력 아래 "📅 학사일정 보기" 링크로 바로 이동합니다.
- 여러 직원이 동시에 페이지를 열어도 학기별 항목은 중복 없이 한 번만 자동 생성됩니다(DB 고유 제약 + 안전한 재시도 처리, 반복 업무 자동 재등록 기능과 동일한 방식).

## v0.55.1 - 2026-08-04

채팅을 계속 치면 채팅창이 아래로 늘어나면서 브라우저 페이지 전체가 스크롤돼, 사이드바 메뉴가 화면 위로 밀려 올라가버리는 문제를 고쳤습니다.

- **화면 레이아웃 높이 고정**: 사이드바+본문을 감싸는 바깥 틀이 "최소 높이"(min-height)로만 잡혀 있어서, 채팅처럼 내용이 계속 늘어나는 화면에서는 그 틀 자체가 계속 커지며 전체 페이지가 스크롤됐습니다.

## v0.55.0 - 2026-08-04

전체 메뉴 구조를 점검해 중복되는 화면을 통합하고, 검토가 필요한 항목을 사이드바에서 바로 알 수 있도록 배지를 추가했습니다.

- **검토 대기 배지**: 사이드바 운영 관리 메뉴에 제안함(검토대기)·채택예정(발행대기) 건수가 빨간 숫자 배지로 표시됩니다.
- **대시보드 통합**: 예전에 따로 있던 "관리자 대시보드"(반복 사건·학생 랭킹·월별 추이·부서별 업무 완료율 분석)를 "학교 관리 대시보드"(/school) 하나로 합쳤습니다.

## v0.54.0 - 2026-08-04

학교관리 아래에 "학교 문서함" 메뉴를 새로 만들어, GIA에서 만들어지는 보고서·매뉴얼·운영계획안· 서류를 한곳에서 열람·검색·인쇄할 수 있도록 통합했습니다.

- **학교 문서함 (신규 메뉴)**: 사이드바에 🗄️ 학교 문서함을 새로 추가했습니다.
- **문서함 홈 (`/school/documents`)**: 매뉴얼·운영계획안·서류함에 등록된 문서를 최근 등록·수정 순으로 모아 보여주고, 이름/카테고리로 바로 검색할 수 있습니다.
- **보고서 통합 화면 (`/school/documents/reports`)**: 업무 보고서와 회의 보고서를 탭 하나로 오가며 볼 수 있습니다(기존 `/work/report`, `/meetings/report` 화면과 동일한 내용이며, 두 메뉴는 그대로 유지됩니다).
- **매뉴얼/운영계획안 바로가기**: 매뉴얼 화면이 `?doc=` 링크로 들어오면 실무자매뉴얼/운영계획안 탭이 바로 열리도록 했습니다.
- **서류함 이동**: 서류함 메뉴를 운영관리에서 학교 문서함으로 옮겼습니다(화면·기능은 그대로).

## v0.53.1 - 2026-08-04

사이드바 메뉴에 마우스를 올리면 브라우저 창 아래쪽 상태표시줄에 계속 링크 주소가 떠서 거슬리던 문제를 고쳤습니다.

- **메뉴 호버 시 주소 미리보기 제거**: 사이드바 메뉴(주메뉴/부메뉴)와 모바일 하단 메뉴를 실제 `<a href>` 링크 대신 클릭 시 이동하는 버튼 방식으로 바꿨습니다.

## v0.53.0 - 2026-08-04

사이드바 부메뉴가 화면 아래로 잘리는 문제를 고치고, 업무·회의를 일간/주간/월간 보고서로 정리해 바로 인쇄(PDF)할 수 있는 화면을 새로 만들었습니다.

- **부메뉴 잘림 수정**: 사이드바 아래쪽 메뉴에 마우스를 올리면 뜨는 부메뉴가 화면 밑으로 잘려나가 누르기 어려웠던 문제를 고쳤습니다.
- **업무 보고서 (신규, 🗂 업무 페이지 상단 아이콘 → 📊 업무 보고서, `/work/report`)**: 일간 ·주간·월간 단위로 그 기간에 완료된 업무(언제·누가·무엇을)와 아직 진행 중이던 업무 현황을 한 화면에 모아 보여줍니다.
- **회의 보고서 (신규, 운영 관리 → 회의 보고서, `/meetings/report`)**: 마찬가지로 일간·주간 ·월간 단위로 그 기간의 회의 기록을 날짜순으로 모아 보여주고, 인쇄용 PDF로 바로 열 수 있습니다.

## v0.52.0 - 2026-08-04

업무 페이지처럼 각 메뉴 화면 우상단에 ❓ 사용 가이드 버튼을 추가했습니다.

- **공용 가이드 컴포넌트**: 기존 업무 페이지 전용이던 가이드 모달을 `GuideModal`/`GuideButton` 공용 컴포넌트로 일반화해, 제목과 내용만 넘기면 어느 화면에서도 재사용할 수 있게 만들었습니다.
- **전체 메뉴에 적용**: 학교 관리, 학생 정보 조회, 구글시트 가져오기, 관리자 대시보드, 내 계정 설정, 학생/반/과목 관리, 내 담임반/담당과목, 리포트 프린트, 반별 작성 현황, 통계 대시보드, 사건기록, 회의기록, AI 매뉴얼, 행사기록, 제안함, 채택예정, 매뉴얼, 서류함, 실무자매뉴얼, 학기·캠프, 사용자 관리, 문의및 건의사항, 교육뉴스, GIA시스템, 관리자 대시보드까지 사이드바의 거의 모든 메뉴에 ❓ 버튼을 달았습니다.

## v0.51.1 - 2026-08-04

업무 채팅에 "전체 멘션" 기능을 추가하고, "채팅이 늦게 뜬다"는 신고에 대한 개선을 반영했습니다.

- **@전체 멘션**: 채팅 입력창에서 `@`를 누르면 개인 이름들과 함께 "📢 전체"가 항상 맨 위에 뜹니다.
- **채팅 지연 개선**: 메시지를 보내면 실시간 수신 이벤트를 기다리지 않고 전송 즉시 화면에 바로 붙도록 바꿨습니다(낙관적 업데이트).
- main에 배포한 뒤 채팅이 아예 안 올라온다는 신고가 있었는데, 코드 점검 결과 전송/수신 로직과 실제 DB 컬럼이 정확히 일치해 코드상 원인은 찾지 못했습니다.

## v0.51.0 - 2026-08-04

속도개선 제안서(Top 5) 중 실제로 효과가 있는 항목들을 반영했습니다.

- **학기 정보 중복 조회 제거**: 사이드바+각 페이지가 "현재 진행중인 학기"를 각자 따로 조회하던 것을 요청 단위로 한 번만 조회하도록 캐싱했습니다(`getCurrentTerm`을 `getCurrentAppUser`와 같은 방식으로 `cache()` 처리).
- **AI 단가표 갱신 리마인더 예약**: Sonnet 5 도입특가가 8월 31일 종료되는데, 단가표 (`pricing.ts`)를 갱신하지 않으면 관리자 대시보드의 AI 비용 표시가 조용히 부정확해지므로 8월 29일 자동 리마인더를 걸어뒀습니다.
- **매뉴얼/실무자매뉴얼/학기관리 캐시 완화는 보류**: 원래 제안에는 있었지만, 확인해보니 이 화면들은 로그인 세션(쿠키) 확인이 필요해 Next.js 구조상 `force-dynamic`을 빼도 실제로는 캐시되지 않습니다(쿠키를 읽으면 항상 요청마다 새로 렌더링됨).
- **대형 테이블 조회 컬럼 지정**: 주간 학생 관찰기록 보드, 업무 채팅(메시지/반응/읽음), 학생 정보 조회 화면에서 전체 컬럼(`select *`) 대신 실제 화면에 쓰는 컬럼만 지정하도록 정리해 전송량을 줄였습니다.

## v0.50.0 - 2026-08-04

파일럿 테스트 전 마지막 정비입니다.

**권한 재정비**
- 교사 계정은 이제 정말로 "자기 반/자기 담당과목"만 볼 수 있습니다 - 예전에는 URL을 직접 입력하면 다른 반 학생의 리포트·부모 연락처를 보거나 PDF로 출력하거나 통합검색으로 찾아낼 수 있었는데(`/weekly-report/students`, `/weekly-report/print`, `/weekly-report/students/[id]`, 검색, PDF 발급 라우트), 전부 소유권 검사를 추가해 막았습니다.
- 행정직원/관리자는 그대로 업무·실무자매뉴얼·운영관리·학교관리·주간학생관찰기록을 봅니다.
- 관리자만 보는 새 "관리자" 메뉴를 만들어 관리자 대시보드/학교 현황판/교육뉴스/ GIA시스템을 모았습니다.

**반 담임 이름 배정**

**신규: 교육뉴스 (관리자 전용)**
- AI가 web 검색으로 국제학교/교육정책/교육 트렌드 최신 소식을 찾아 정리하는 다이제스트입니다.

**신규: GIA시스템 (관리자 전용)**
- GIA가 이미 갖춘 운영 시스템(구비서류/내규/계약서/학생관리/교사관리/교직원관리/매뉴얼/ 운영계획안)을 카테고리별로 정리해 보여주고, AI가 다른 국제학교·공립학교 사례를 검색해 GIA에 없는 시스템을 제안합니다.

**가벼운 정리**
- 채팅/업무/구글시트가져오기에서 저장 실패 시 Postgres 원본 에러 문구(영어)가 그대로 보이던 것을 "일시적인 오류입니다.
- 안 쓰는 죽은 코드(`CompletionGauge.tsx`, `/weekly-report/admin/terms` 리다이렉트 페이지)를 정리했습니다.
- `manifest.json`을 추가해 "홈 화면에 추가"가 실제로 앱처럼(standalone) 열리도록 했습니다.

## v0.49.1 - 2026-08-03

실시간 채팅과 업무판을 정말 꼼꼼하게 다시 점검해서, 조용히 메시지/업무 내용이 사라지거나 잘못될 수 있는 지점들을 고쳤습니다.

**채팅**
- 메시지 전송이 실패해도(끊긴 와이파이 등) 알려주지 않고 입력창만 비워져서 메시지가 조용히 사라지는 문제, 그리고 실패 시 전송 버튼이 계속 잠긴 채로 남는 문제를 고쳤습니다 - 이제 실패하면 알림을 띄우고 입력 내용을 복원하며, 버튼도 다시 눌릴 수 있게 됩니다.
- 파일 첨부도 같은 이유로 캡션이 먼저 지워지던 것을 저장 성공 후로 미뤘고, 업로드는 됐는데 메시지 저장이 실패한 경우 남는 "고아 파일"을 자동 정리하도록 했습니다.
- 이모지 반응을 아주 빠르게 두 번 누르면(추가 요청이 아직 끝나기 전에 다시 클릭) 취소가 실제로는 아무 효과 없이 무시되던 버그를 고쳤습니다.
- 재연결(와이파이 순간 끊김 등) 시 최근 100건으로 화면 전체를 덮어써서, 검색으로 점프해 보고 있던 예전 메시지가 화면에서 사라지던 문제를 고쳤습니다 - 이제 기존 화면 내용을 유지한 채 최신 변경분만 얹습니다.
- 다른 사람이 메시지를 보낼 때마다 화면이 맨 아래로 튀어서, 위로 스크롤해 예전 대화를 읽던 중에도 계속 방해받던 문제를 고쳤습니다 - 이제 이미 맨 아래 근처에 있을 때만 자동으로 따라 내려갑니다.
- 채팅 검색이 메시지가 많이 쌓여도 느려지지 않도록 검색용 인덱스(pg_trgm)를 추가했습니다.

**업무판**
- 반복 업무를 업무 상세 팝업에서 완료 처리하면(칸반 드래그가 아니라) 반복이 조용히 끊기던 문제를 고쳤습니다 - 두 곳 모두 같은 로직을 쓰도록 정리했고, 두 사람이 거의 동시에 완료 처리해도 다음 회차가 중복 생성되지 않도록 DB에도 안전장치를 추가했습니다.
- 업무 상태 변경/업무 확인/담당자 태그/댓글 등록/업무 삭제가 실패했을 때 조용히 무시되던 것을 고쳐서, 실패하면 알림을 띄우고 화면을 원래 상태로 되돌리도록 했습니다(끊긴 와이파이 중에도 "저장된 것처럼 보이는데 실제로는 안 된" 상황이 생기지 않도록).
- 업무 상세 팝업에서 완료 처리해도 칸반 정렬 기준(position)이 함께 갱신되도록 맞췄습니다.

## v0.49.0 - 2026-08-03

여러 사람이 동시에 데이터를 입력할 때 생길 수 있는 두 가지 실제 위험을 점검해서 고쳤습니다 (실시간 채팅/칸반/전체 Realtime 채널은 점검 결과 이미 안전하게 정리되어 있었고, 아래 두 곳만 "조용한 데이터 유실" 위험이 있었습니다).

- 업무 "확인"(acknowledged_by)/담당자 태그(assignee_emails): 지금까지는 각자의 화면이 계산한 배열 전체를 그대로 저장해서, 전체공지 업무를 여러 명이 거의 동시에 "확인" 클릭하면 늦게 도착한 저장이 먼저 도착한 사람의 확인 기록을 덮어써 조용히 사라질 수 있었습니다.
- 주간 학생 관찰기록: 같은 학생·과목·주 리포트를 담임/과목교사가 동시에(또는 같은 사람이 두 탭으로) 처음 열어 저장하면 중복 리포트가 생길 수 있었습니다.
- 그 외 실시간 채팅(ChatPanel)/칸반 드래그앤드롭/Realtime 채널 정리(구독 해제) 상태를 전수 점검했습니다 - 13개 채널 모두 정상적으로 정리되고 있어 별도 수정은 필요 없었습니다.

## v0.48.1 - 2026-08-03

관리자 승인 목록, 사용자 관리, 학교 관리 대시보드 등 여러 화면의 계정 표시 로직을 정리했습니다.

## v0.48.0 - 2026-08-03

관리자 대시보드(/dev)에서 AI 기능별로 실제 얼마나 과금되고 있는지 달러로 확인하고, 과금이 부담스러운 기능은 항목별로 켜고 끌 수 있게 만들었습니다.

- "AI 사용량 & 예상 과금" 섹션에 Anthropic 공식 요금표(Sonnet 5 $2/$10, Haiku 4.5 $1/$5 · 백만 토큰당) 기준 예상 과금액을 추가했습니다.
- "AI 기능 on/off (과금 조절)" 섹션을 새로 만들어, 사건기록 정리 AI/회의록 다듬기 AI/매뉴얼 초안작성 AI 등 총 16개 AI 기능을 기록함·매뉴얼·문서·제안함·채택예정·업무 4개 그룹으로 묶어 각각 켜짐/일시정지중 스위치로 조절할 수 있게 했습니다.
- 새 `ai_feature_flags` 테이블을 만들고, 모든 Claude 호출이 거쳐가는 단일 지점인 `callClaudeJson()`(src/lib/ai/claude.ts)에 게이트를 추가했습니다 - 기능이 꺼져 있으면 Anthropic API를 호출하기 전에 바로 막아서 과금이 발생하지 않고, 사용자에게는 "현재 이 AI 기능은 관리자에 의해 일시정지되어 있습니다"라는 안내 메시지가 표시됩니다.
- 사이드바 프로필 블록(로고 아래, 이메일 위) 바로 밑에 빨간 배너를 추가해, 꺼둔 AI 기능이 있으면 모든 로그인 사용자에게 "⏸️ AI 기능 일시정지중"과 함께 어떤 기능인지 실시간으로 보여줍니다.
- 토글/배너는 giamicro.com 전체가 조회(SELECT)할 수 있고, 켜고 끄는 것(수정)은 관리자 계정만 가능하도록 RLS를 설정했습니다.

## v0.47.0 - 2026-08-03

구글시트로 학교정보를 일괄 등록하는 화면(/school/import)을 새로 만들었습니다.

- 구글시트에서 표를 CSV로 다운로드하거나(파일 > 다운로드 > 쉼표로 구분된 값), 셀 범위를 그대로 복사해서 붙여넣으면 자동으로 표를 읽어 미리보기를 보여줍니다.
- 3개 탭으로 나뉘어 있습니다.
  - 🧑‍🏫 교직원 명단: 이메일/이름/소속/직위 - 미리 등록해두면 선생님이 구글 계정으로 처음 로그인할 때 승인 대기 없이 바로 시스템을 쓸 수 있습니다.
  - 🏫 반 구성: 학년/반/담임 이메일/부담임 이메일 - 기존 반이면 담임·부담임을 갱신하고, 없는 조합이면 새 반을 만듭니다.
  - 🎓 학생 명부: 이름/영어이름/학년/반/보호자 연락처 - 학년+반이 일치하면 자동으로 그 반에 연결됩니다.
- 각 행마다 형식 오류(이메일 형식, 필수값 누락 등)를 미리보기에서 빨간색으로 표시해 잘못된 행은 제외하고 정상 행만 가져올 수 있습니다.

## v0.46.0 - 2026-08-03

전체 코드 최적화 점검 결과를 반영했습니다.

- 로고 이미지(사이드바/모바일 헤더/로그인/온보딩/승인대기 화면, 총 5곳)를 일반 `<img>`에서 Next.js `<Image>`로 전환했습니다.
- 주간 학생 관찰기록의 반별 위젯(StudentReportBoard)에서 "학생마다 전체 리포트 목록을 다시 훑어 이번 주 작성 상태를 찾는" 방식이었던 부분을, 리포트가 바뀔 때 한 번만 학생별로 미리 정리해두는 방식으로 바꿨습니다.
- 기존에 이미 적용되어 있던 최적화(미들웨어 승인상태 캐싱, 로그인 유저 정보 요청 단위 캐싱, Supabase 클라이언트 싱글턴, PDF/리치에디터 등 무거운 라이브러리의 서버 전용 격리)는 점검 결과 여전히 잘 동작 중임을 확인했습니다.

### 추가로 제안하는 속도 개선 방법 (코드 외 조치 필요)
- Vercel 프로젝트에 이미 연동된 Speed Insights 대시보드를 주기적으로 확인하면, 실제 사용자 기준으로 어느 화면이 느린지 데이터로 파악할 수 있습니다.
- Supabase에서 자주 필터링되는 컬럼(app_users.email, tasks.department, wr_reports.term_id/ student_id, chat messages.department 등)에 인덱스가 걸려 있는지 한 번 점검해보시는 걸 추천드립니다 - 데이터가 늘어날수록 체감 효과가 커집니다.
- 채팅창(ChatPanel)은 메시지 목록이 많아질수록 입력할 때마다 전체 목록을 다시 그리는 구조라, 다음 최적화 후보로 남겨뒀습니다(구조가 복잡해 이번엔 안전하게 손대지 않았습니다).

## v0.45.2 - 2026-08-03

채팅 입력창 안내문구를 짧게 줄였습니다.

- "메시지 보내기 (Enter 전송·Shift+Enter 줄바꿈, **굵게** *기울임* ~~취소선~~ `코드`, @개인호출, #부서공지)" → "메시지 보내기 (Enter 전송·Shift+Enter 줄바꿈, @개인호출, #부서공지)"로 정리.

## v0.45.1 - 2026-08-03

주간 학생 관찰기록 반별 대시보드(학생 카드) 이름 표시를 개선했습니다.

- 학생 이름 아래 영어이름이 한 줄로 잘리지 않고(예전엔 말줄임표로 잘림) 줄바꿈되며, 글자 크기를 더 줄여서(10px) 카드 안에 자연스럽게 들어가도록 했습니다.
- 담임/과목/관리자 화면에서 공통으로 쓰는 컴포넌트라 반별 작성 현황, 내 담임반, 내 담당과목 화면 전부에 동일하게 적용됩니다.

## v0.45.0 - 2026-08-03

- 사이드바/모바일 메뉴에서 "학교 관리" 카테고리 라벨 자체를 누르면(기존에는 하위 항목 중 첫 번째로 그냥 넘어갔음) 새 `/school` 대시보드가 열립니다.
- 대시보드에서 현재 학기, 개설된 반 수, 과목 수, 교사 수, 교직원 수, 재학생 수를 카드로 한눈에 보여주고, 그 아래에 반별 담임/부담임, 과목별 담당 교사·수강 인원, 교사 리스트, 교직원 리스트, 재학생 요약(24명까지 미리보기 + 더보기 안내)을 함께 보여줍니다.
- 각 카드에는 반 관리/과목반 세팅/사용자 관리/학생 정보 조회 등 해당 관리 화면으로 바로 이동하는 링크를 달아뒀습니다.
- 조회 권한은 기존과 동일하게 행정직원 이상(교사 계정은 접근 불가)입니다.
- (수정) 학기 카드는 연도를 작게 위에, 학기명(예: 여름캠프)은 줄바꿈해서 가운데 크게 보이도록 바꿨습니다.

## v0.44.0 - 2026-08-03

UI/UX 정리 6건을 반영했습니다.

- 업무상황판/주간관찰기록 등 화면 크기를 바꿔도 라벨·뱃지·부서탭 글자가 어중간하게 줄바꿈되며 밀리지 않도록 nowrap/줄임(ellipsis) 처리를 정리했습니다.
- 실시간 로그(🔔) 항목을 관리자이거나 그 로그를 남긴 본인이면 지울 수 있게 됐습니다(업무 상세 패널의 코멘트/로그 항목도 동일).
- 실시간 로그 컴팩트 뷰가 한 줄에 하나씩, 딱 5줄만 보이는 고정 높이 영역으로 바뀌었고, 위로 스크롤하면 그 이전 로그를 추가로 불러옵니다(캐시에 무한정 쌓이지 않도록 상한을 둠).
- 업무탭을 모바일(작은 화면)에서 열면, 마우스 드래그로 나누던 2단 레이아웃 대신 채팅/칸반/내 업무 탭으로 전체 화면을 하나씩 보여줍니다.
- 뱃지 평가 안내와 학생 리스트 표가 좁은 화면에서도 깨지지 않도록 줄바꿈/가로스크롤을 정리했습니다.
- 학생 영어 이름이 목록/카드에서 줄바꿈 없이 한 줄로 깔끔하게 보이도록 정리했습니다.

### 모바일 전용(m.) 서브도메인에 대한 안내
이 앱은 반응형 웹으로 만들어서, m.도메인을 따로 두지 않고 같은 주소(같은 코드)가 PC/모바일
화면 크기에 맞춰 자동으로 레이아웃을 바꿉니다. m.도메인 방식은 옛날 방식(모바일 전용 사이트를
완전히 별도로 만들어 운영)이라 유지보수 부담이 두 배가 되고, 요즘 대부분의 서비스(이 앱 포함)는
반응형 하나로 처리합니다. 이번 업데이트로 업무탭도 반응형으로 전환했고, 필요하면 "홈 화면에
추가"로 앱처럼 아이콘을 만들어 쓰실 수 있습니다(PWA는 이미 적용돼 있습니다).

리포트 작성 화면의 "뱃지 평가 안내" 문구에 영어 병기를 추가했습니다(원어민 교사 대상).

## v0.43.0 - 2026-08-03

지난 기능 제안 중 학부모 열람 관련 항목을 제외한 나머지 6건을 반영했습니다.

- 반복 업무: 업무등록 위젯/상세패널에서 매일·매주(요일)·매월(날짜) 반복을 지정하면, 완료 처리되는 순간 다음 회차가 자동으로 새로 등록됩니다(🔁 뱃지로 표시).
- 업무별 첨부파일: 업무 상세패널에서 파일을 첨부·다운로드·삭제할 수 있습니다.
- 채팅 공지 고정 배너: 이미 있는 메시지 고정(📌) 기능이 이 역할을 하고 있어 별도 개발 없이 그대로 사용하시면 됩니다.
- 학기 종합 PDF: 리포트 프린트 화면에서 학생+학기를 함께 고르면, 그 학기 동안 발행된 모든 리포트를 과목별로 모아 하나의 PDF로 볼 수 있습니다.
- 통합 검색: 사이드바(모바일은 상단)에서 학생·사건·회의·행사·업무·서류함을 한 번에 검색합니다.
- 모바일 칸반 상태변경: 업무카드에 드래그 없이 바로 상태를 바꿀 수 있는 드롭다운을 추가해 터치 환경에서도 편하게 옮길 수 있습니다.

## v0.42.0 - 2026-08-03

업무탭 편의성 + 주간 학생 관찰기록 개선 7건을 반영했습니다.

- **업무탭 레이아웃 크기 기억.** 채팅/상황판/칸반 폭·높이를 마우스로 조절하면 이 기기에 저장되어, 다음에 업무탭에 들어와도 조절한 그대로 유지됩니다.
- **채팅 업무등록 안내를 실시간 로그로 분리.** 채팅으로 업무를 등록하면 "등록됨" 안내와 확인 내역이 더 이상 채팅에 섞이지 않고, 칸반 오른쪽 위 "🔔 실시간 로그"에만 쌓입니다.
- **채팅 서식 툴바 아이콘화.** 굵게/기울임/취소선/코드 버튼을 글자(B/I/S) 대신 선으로 그린 작은 아이콘으로 바꿨습니다.
- **주간 학생 관찰기록 영어 병기.** 원어민 교사도 쓰는 화면이라, 메뉴(내 담임반/내 담당과목/ 반별 작성 현황 등)와 리포트 작성창의 평가 항목·뱃지·버튼에 영어를 함께 표시했습니다.
- **학생 이름 한글/영어 줄바꿈 표시.** 학생 카드·목록·리포트창에서 영어 이름이 등록되어 있으면 한글 이름 아래 줄바꿈으로 함께 보입니다.
- **관리자/행정직원의 리포트 읽기·수정·삭제 권한.** 학생 프로필 화면에서 관리자/행정직원은 이제 리포트를 열람만 하는 게 아니라 직접 수정하거나 삭제할 수 있습니다(삭제 버튼은 관리자/행정직원에게만 보입니다).

## v0.41.0 - 2026-08-03

업무 페이지 사용성 개선 10건을 한 번에 반영했습니다.

- **업무 삭제 권한 제한.** 이제 등록자 본인 또는 관리자만 업무를 삭제할 수 있습니다(상세 패널의 삭제 버튼이 다른 사람에게는 아예 보이지 않습니다).
- **확인 체크 시 취소선 제거.** 담당자가 업무 확인 체크박스를 누르면 제목에 줄이 그이지 않고, 흐리게(반투명)만 표시됩니다.
- **칸반 레이아웃 개편.** 진행대기·진행중·완료 3열이 항상 위에 쾌적하게 보이고, 보류/이슈는 "⏸️ 보류/이슈 (N)" 버튼을 눌러야 펼쳐지는 접이식 섹션으로 아래에 분리됐습니다.
- **업무를 보류로 옮기면 단순 보류인지 이슈인지 물어봅니다.** 이슈를 선택하면 메모를 남길 수 있고, 이 메모는 업무를 공유하는 모두에게 보이며 작성자도 함께 표시됩니다.
- **나/전체/공유 색상 시스템.** 빠른 업무등록 위젯에서 고른 뱃지가 업무에 저장되고, 그 색이 칸반 카드 테두리 색으로 그대로 쓰입니다.
- **업무 마감일 → OS 캘린더 연동.** 상세 패널의 📅 버튼을 누르면 마감 일정을 내 기기의 기본 캘린더 앱(Mac은 Calendar.app, 그 외는 .ics 다운로드)에 바로 추가할 수 있습니다.
- **빠른 업무등록에 날짜/시간 선택 추가.** 오늘·내일·이번주 뱃지를 누르거나 날짜·시간을 직접 입력할 수 있습니다.
- **사용 가이드 팝업 신설.** 업무 페이지 우상단 ❓ 아이콘을 누르면 빠른등록/채팅/칸반/ 업무기록/삭제·캘린더 사용법을 요약한 팝업이 뜹니다.
- **채팅 서식 툴바.** 입력창 위에 굵게(B)/기울임(I)/취소선(S)/코드(&lt;/&gt;) 버튼이 생겨, 마크다운 문법을 직접 타이핑하지 않아도 선택한 글자를 감싸서 서식을 넣을 수 있습니다.
- **반응(이모지) 체크 표시 + 목록 확장.** 반응 고르기 팝업에서 이미 남긴 이모지에 파란 체크 표시가 붙고, 자주 쓰는 이모지가 6개 → 12개로 늘었습니다.

## v0.40.0 - 2026-08-03

업무기록(archive) 기능을 새로 만들었습니다.

- **완료하는 순간 카드가 제목만 남기고 줄어듭니다.** 칸반에서 업무를 완료로 옮기면 그 즉시 설명/담당자/마감 뱃지 등이 사라지고 체크 표시 + 제목만 있는 얇은 줄로 바뀝니다.
- **다음날 자정 직후 자동으로 업무기록으로 이동합니다.** 매일 밤(학기 자동전환 크론 바로 뒤에) 완료 상태인 업무를 업무보드에서 빼서 보관 처리하는 크론을 새로 추가했습니다.
- **업무기록 화면 신설.** 업무 페이지 우측 상단에 "🗂 업무기록" 아이콘을 누르면 이동합니다.

- **참고: 이 기능은 매일 자정 직후(KST) 자동 실행되는 Vercel Cron에 의존합니다.** 기존 학기 자동전환 크론과 같은 환경변수(`CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`)를 그대로 재사용하므로 별도 설정 없이 바로 동작합니다.

## v0.39.2 - 2026-08-03

- **홈 메뉴 재확인.** v0.39.1에서 없앤 게 맞는데, 그 변경은 staging 브랜치에만 있고 아직 main(라이브)에는 병합되지 않아서 실제 화면에서는 계속 보이셨을 거예요.
- **메뉴 순서 변경.** 가장 자주 쓰는 "업무"를 맨 위로 올리고, 그 바로 아래에 "실무자 매뉴얼"을 뒀습니다.
- **"학교관리" → "학교 관리".** "운영 관리"처럼 띄어쓰기를 맞췄습니다.

## v0.39.1 - 2026-08-03

- **중복 "홈" 메뉴 제거.** 사이드바(PC)/상단바(모바일)의 로고를 누르면 이미 홈으로 이동하고 있어서, 메뉴 목록에 따로 있던 "홈" 항목은 없앴습니다.

## v0.39.0 - 2026-08-03

업무등록을 채팅과 분리했습니다.

- **빠른 업무등록 위젯 신설.** [나] 뱃지를 누르면 내 개인 업무로, [전체]를 누르면 부서원 전원에게 배정되는 팀 업무로(모두의 "내 업무목록"에 뜹니다), [공유]를 누르면 태그 목록이 펼쳐져서 원하는 사람만 골라 배정할 수 있습니다.
- **채팅창 높이 축소.** 위젯이 새로 들어간 만큼 채팅창 공간을 줄였습니다.
- **마감초과 시각적 강조 강화.** 마감이 지난 업무는 카드 테두리가 빨간색으로 바뀌고 "🔥 지연" 뱃지가 깜빡이며, 24시간 안에 마감인 업무는 "⏰ 임박" 뱃지가 노란색으로 뜹니다.
- **공유 업무 상태변경 실시간 알림.** 여러 명에게 태그된 업무를 한 명이 진행중/완료 등으로 옮기면, 그 업무의 등록자와 다른 담당자들에게 "OOO님이 '제목' 업무를 '진행중'으로 옮겼어요" 토스트 알림이 화면 우측 하단에 바로 뜹니다(본인이 직접 바꾼 경우는 알림이 뜨지 않습니다).

## v0.38.0 - 2026-08-03

지난 버전(v0.37.0) CHANGELOG에서 "저장소/조회 기능이 더 필요해서 이번 범위에는 넣지 않았다"고 미뤄뒀던 4가지 - 파일/이미지 첨부, 읽음 표시, 링크 미리보기, 메시지 검색·고정 - 를 마저 구현했습니다:

- **파일/이미지 첨부.** 입력창 왼쪽 📎 버튼으로 파일을 고르면(최대 20MB) 바로 업로드되어 메시지로 전송됩니다.
- **읽음 표시.** 내가 보낸 메시지 옆에 "아직 안 읽은 부서원 수"가 카카오톡의 숫자처럼 표시되고, 상대가 채팅방을 열면 실시간으로 줄어듭니다.
- **링크 미리보기.** 메시지에 http(s) 링크가 있으면 구글챗처럼 그 아래에 제목/설명/썸네일이 담긴 작은 카드가 자동으로 붙습니다.
- **메시지 검색.** 채팅창 헤더의 🔍를 누르면 검색창이 뜨고, 입력하는 대로 그 부서 채팅방의 메시지 내용을 검색합니다.
- **메시지 고정.** 메시지에 마우스를 올려 📌를 누르면 고정되어 채팅창 상단에 "고정된 메시지" 바로 모입니다(카카오톡 공지처럼 부서원 누구나 고정/해제 가능).

## v0.37.0 - 2026-08-03

업무 채팅에 구글챗 스타일 기능 대거 추가 + 삭제 UX를 카카오톡 방식으로 변경:

- **메시지 삭제 UX 변경.** 이전엔 이름 옆에 휴지통 아이콘이 있었는데, 카카오톡처럼 내가 보낸 메시지에 마우스를 올렸을 때만 말풍선 오른쪽에 작게 ✕가 나타나도록 바꿨습니다.
- **Enter로 전송, Shift+Enter로 줄바꿈.** 입력창이 한 줄짜리 input에서 여러 줄 입력 가능한 textarea로 바뀌었고, 내용에 맞춰 높이가 자동으로 늘어납니다(최대 5줄 정도, 그 이상은 스크롤).
- **텍스트 서식.** `**굵게**`, `*기울임*`, `~~취소선~~`, `` `코드` `` 문법을 메시지에 그대로 쓰면 서식이 적용되어 보입니다.
- **메시지 수정.** 내가 보낸 메시지에 마우스를 올리면 ✏️ 아이콘이 뜨고, 눌러서 고치고 저장하면 "(수정됨)" 표시와 함께 반영됩니다.
- **답장(인용).** 메시지에 마우스를 올려 ↩️를 누르면 입력창 위에 원본이 미리보기로 뜨고, 그 상태로 보내면 답장한 메시지 위에 원본이 작게 인용되어 표시됩니다.
- **이모지 반응.** 😀 아이콘을 누르면 👍❤️😂😮😢🙏 중 고를 수 있고, 메시지 아래에 반응 뱃지로 모입니다(같은 이모지를 다시 누르면 취소).
- **입력 중 표시.** 다른 사람이 타이핑하고 있으면 입력창 위에 "OOO님이 입력 중..."이 뜹니다 (3초 넘게 조용하면 사라집니다).
- **연속 메시지 그룹핑.** 같은 사람이 5분 안에 연달아 보낸 메시지는 이름/시간을 반복해서 보여주지 않고 말풍선만 이어서 보여줘 채팅창이 덜 복잡해졌습니다.
- **채팅 업무등록 버튼 실패 원인 수정.** 업무 저장이 실패해도 결과를 확인하지 않고 팝업만 닫아버려서 "눌렀는데 반응이 없다"로 보였던 문제를 고쳤습니다.
- **참고로 이번엔 넣지 않은 것들:** 파일/이미지 첨부, 읽음 표시, 링크 미리보기, 메시지 검색·고정은 구글챗에 있지만 저장소/조회 기능이 추가로 필요해서 이번 범위에는 넣지 않았습니다.

## v0.36.0 - 2026-08-03

업무 채팅 "업무등록" 버튼 실패 원인 수정 + 메시지 본인 삭제 기능:

- **채팅 "업무등록" 버튼을 눌러도 등록이 안 되던 문제 수정.** 원인은 등록 로직 자체가 아니라 오류 처리 방식이었습니다 - AI 분석이 실패해도 규칙 기반으로 대체되니 문제가 아니었고, 그 다음 단계인 실제 업무 저장(`tasks` 테이블 insert)이 실패했을 때 결과를 확인하지 않고 그냥 팝업을 닫아버려서, 저장이 안 됐는데도 화면에는 아무 표시가 없어 "눌렀는데 반응이 없다"로 보였던 것입니다.
- **채팅 메시지 본인 삭제 기능 추가.** 메시지에 마우스를 올리면(내가 보낸 메시지에만) 오른쪽에 🗑️ 아이콘이 나타나고, 눌러서 확인하면 삭제됩니다.

## v0.35.0 - 2026-08-02

내 계정 설정(프로필 사진/이름) + 직위(권한) 뱃지 표시 + 관리자의 직위 편집 + 담당자 자동 채움을 이름으로 전환:

- **내 계정 설정 화면 신설(`/account`).** 사이드바 하단 프로필을 누르면 이동하는 새 화면으로, 프로필 사진과 이름을 스스로 바꿀 수 있습니다.
- **사이드바 프로필 블록 개편.** 로고 아래 학기 배지 밑에 있던 한 줄짜리 이메일 표시를, 프로필 사진 + "이름(직위)" + 그 아래 작은 글씨로 로그인 이메일을 보여주는 카드로 바꿨습니다.
- **사건기록 담당자 자동 채움을 이메일 대신 이름으로.** 새 사건을 작성할 때 담당자 칸에 자동으로 채워지던 로그인 이메일을, 내 계정 설정에서 정한 이름(없으면 이메일)으로 바꿨습니다.

## v0.34.0 - 2026-08-02

- **"위클리 리포트" → "주간 학생 관찰기록"으로 개칭.** 사이드바 메뉴명, 페이지 제목, PDF 표지/푸터 등 화면에 보이는 텍스트를 전부 바꿨습니다.
- **"학교관리" 메뉴 신설(통합 관리).** 학생/반/학기/교직원 계정 관리가 운영관리·주간 학생 관찰기록·지원관리 세 군데에 흩어져 있었고, 특히 "학기"는 두 곳에 중복으로 있었습니다.
- **반별 작성 현황 위젯 화면.** "주간 학생 관찰기록" 메뉴에 들어가면 이제 맨 위에 현재 학기가 크게 표시되고, 그 아래 반마다 위젯 카드가 나옵니다.
- **연도-학기-학년-반 통합 검색.** 주간 학생 관찰기록 통계 화면 하단에 검색 패널을 추가해, 연도·학기 / 학년 / 반 조건으로 그동안 쌓인 리포트를 바로 찾아볼 수 있습니다.

## v0.33.1 - 2026-08-02

전체 빌드 점검 + 로고 이미지 최적화:

- **빌드 전수 점검.** 클린 환경에서 `npm install` + `npm run build`를 다시 실행해 타입 에러, 라우트 충돌(경로 중복 등), 머지 마커 잔재 여부를 확인했습니다 - 전부 정상이었고, 빌드는 약 11초, 클라이언트로 나가는 JS는 전체 라우트 합쳐서 1.7MB 수준으로 확인했습니다.
- **사이드바/로그인 화면 로고 이미지 용량 축소 (핵심 개선).** `logo-main.png`가 실제 화면 표시 크기(가로 40px)보다 훨씬 큰 원본(5907×1317px, 224KB)으로 들어가 있어서 대시보드 화면에 들어갈 때마다 불필요하게 큰 파일을 내려받고 있었습니다.
- 그 외 코드 전반(중복 쿼리, 실시간 구독 정리, 브라우저 Supabase 클라이언트 싱글턴 등)은 이전 점검(v0.33.0, 그 이전 성능 점검들)에서 이미 정리되어 있어 추가로 손댈 부분은 없었습니다.

## v0.33.0 - 2026-08-02

탭/메뉴 전환 속도 개선을 위한 코드 통합 및 최적화:

- **로그인 사용자 정보 조회를 요청 단위로 통합.** 지금까지는 사이드바(레이아웃)와 각 페이지가 탭을 옮길 때마다 "로그인 확인 + app_users에서 이름/직위 조회"를 각자 따로 실행해서, 같은 사람 정보를 같은 화면 전환 안에서 두 번 묻는 셈이었습니다.
- **탭 전환 시 즉시 반응하는 로딩 화면 추가.** 지금까지는 페이지를 클릭한 뒤 새 화면의 데이터가 도착하기까지 화면이 하얗게 비어 있었는데, 대시보드 전 영역에 공통 로딩 스켈레톤을 추가해 전환 순간부터 바로 반응하는 것처럼 느껴지도록 했습니다(실제 데이터 조회 시간 자체가 줄어드는 건 아니지만 체감 속도는 확실히 개선됩니다).
- 그 외 미들웨어 인증 캐싱, 라우트별 코드 스플리팅(리치 텍스트 에디터·PDF 생성 등)은 이전 점검에서 이미 최적화되어 있음을 재확인했고, 추가로 손댈 부분은 없었습니다.

## v0.32.1 - 2026-08-02

채팅 "업무등록" 팝업 위치/크기 개선:

- 메시지 아래로 뜨면서 바로 다음 메시지를 가리던 문제를 고쳐, 이제 메시지 오른쪽 옆에 작게 붙습니다(채팅창을 좁게 줄여 오른쪽 공간이 부족할 땐 자동으로 왼쪽에 붙습니다).
- 라벨을 "📋 업무로 등록 (AI 분석)"에서 "📋 업무등록"으로 간단하게 줄였습니다.
- 팝업 크기와 글씨(11px)를 채팅 말풍선(13px)보다 뚜렷하게 작게 만들어서, 실제 채팅 내용과 헷갈리지 않고 조작 버튼이라는 게 한눈에 구별되도록 했습니다.

## v0.32.0 - 2026-08-02

업무 상황판 위젯 축소 + 채팅 메시지 클릭 → AI 분석 업무등록 + 실시간 채팅 점검:

- **업무 상황판 위젯을 텍스트 크기에 맞게 축소.** 숫자 배지 한 줄뿐인데도 공간을 많이 차지하던 것을 더 작게 줄이고, 그만큼 채팅창을 키웠습니다(왼쪽 컬럼 기본 비율: 상황판 14% / 채팅 86%, 드래그로 계속 조절 가능).
- **채팅 자동 업무등록 → 클릭해서 AI가 분석하는 방식으로 전환.** "@담당자를 태그하면 자동으로 업무 등록"은 실시간 채팅이 활발해지면 잡담까지 전부 업무화될 위험이 있어 방식을 바꿨습니다.
  - 새 API: `POST /api/ai/analyze-task`.
- **실시간 채팅 점검.** messages 테이블의 realtime 발행/RLS(도메인 기반 giamicro.com 계정 전체 허용)는 정상이었습니다.

## v0.31.0 - 2026-08-02

전 화면 게시판형 페이지네이션 도입 + 사이드바 메뉴 구분선 + 달력 위젯 축소:

- **목록 화면들을 스크롤 대신 "1 2 3" 페이지 번호로.** 사건기록/회의기록/행사기록/제안함/ 채택예정/매뉴얼/서류함/문의및건의사항/사용자 관리/학생 검색/학기/위클리 리포트 관리(학생 명부·반 배정·과목반) 화면 모두, 목록이 아래로 계속 늘어나며 스크롤되던 것을 화면 높이에 맞춰 고정하고 목록 아래에 게시판형 페이지 번호(‹ 이전 · 1 2 3 … · 다음 ›)를 붙였습니다.
  - 서류함은 "확인 필요" 항목만 예외로 전체 노출(급한 항목은 페이지를 넘기지 않아도 바로 보여야 하므로), 나머지 목록만 페이지네이션됩니다.
  - 공용 `Pagination` 컴포넌트(`src/components/Pagination.tsx`)로 통일해서, 다음에 새 목록 화면을 만들 때도 같은 방식으로 바로 붙일 수 있습니다.
- **사이드바 메뉴에 얇은 구분선 복원.** 플라이아웃으로 평평하게 펼쳐지면서 사라졌던 메뉴 그룹 간 구분이 안 보이던 문제를, 각 메뉴 항목 사이에 얇은 실선을 넣어 다시 구분되게 했습니다.
- **사이드바 달력+시계 위젯을 더 작게, 위젯답게.** 셀 크기와 간격을 더 줄이고 테두리+옅은 배경을 넣어 카드형 위젯처럼 보이게 다듬었습니다.

## v0.30.0 - 2026-08-02

사이드바 달력 상시표시 + 실무자매뉴얼 독립 메뉴화 + 전화 응대용 매뉴얼·학생 통합 조회 화면:

- **달력+시계를 왼쪽 메뉴에 항상 표시.** 홈 화면에만 있던 큰 달력/시계 위젯을 축소판으로 만들어 왼쪽 사이드바(로고 아래, 메뉴 위)에 항상 떠 있게 옮겼습니다.
- **실무자매뉴얼을 독립 메뉴로 분리.** "운영 관리" 하위 플라이아웃에 묻혀 있던 실무자매뉴얼을 꺼내서 홈과 업무 사이에 바로 보이는 최상단 메뉴로 옮겼습니다.
- **실무자매뉴얼 화면을 반으로 나눠 매뉴얼+학생 조회를 동시에.** 왼쪽은 기존 매뉴얼 검색, 오른쪽은 새로 추가된 학생 검색 위젯입니다.

## v0.29.2 - 2026-08-02

업무 탭 레이아웃 2차 개편 - 상황판 초압축(숫자+팝업) + 채팅 3:7 + "내 업무목록" 위젯 + 가로 칸반:

- **상단 헤더 통합.** 부서탭 바 위/아래로 나뉘어 있던 "[부서명] 전용 업무 및 소통 공간" 안내 헤더를 없애고, 맨 위 부서탭 바 하나로 합쳐서 세로 공간을 줄였습니다.
- **업무 상황판을 숫자 배지 한 줄로 초압축.** 카드를 늘어놓던 기존 방식 대신, "전체/진행&대기/ 보류&이슈/완료"를 숫자 배지 한 줄로만 보여줍니다.
- **왼쪽 컬럼 세로 비율 3:7 고정(상황판:채팅).** 상황판은 작게, 채팅은 크게 - 경계선을 드래그 하면 비율을 자유롭게 조절할 수 있습니다.
- **오른쪽에 "내 업무목록" 위젯 신설.** 나에게 배정된, 완료되지 않은 업무만 마감 임박순으로 모아 보여줍니다.
- **그 아래 칸반보드(진행대기/진행중/보류·이슈/완료)를 가로로 나란히 배치.** 화면이 넓을 땐 4개 상태 컬럼이 옆으로 나란히 놓여 진짜 칸반보드처럼 카드를 끌어다 놓을 수 있고, 좁아지면 자동으로 1~2열로 줄어듭니다(드래그앤드롭 기능 자체는 기존과 동일).

## v0.29.1 - 2026-08-02

업무 탭 UX 개선 - 부메뉴 팝업화 + 채팅 중심 레이아웃 + 채팅만으로 업무등록(마감기한 자동 인식):

- **사이드바 부메뉴가 진짜 팝업으로 뜨도록 수정.** 부메뉴가 사이드바 내부에 그대로 펼쳐지면서 스크롤이 생기던 문제를 고쳤습니다.
- **업무 탭 레이아웃을 채팅 중심으로 재조정.** 업무 상황판과 칸반보드(업무목록)는 참고용이라 작게, 실제로 업무를 처리하고 소통하는 채팅창은 크게 배치했습니다(왼쪽 상황판+칸반 : 오른쪽 채팅 = 대략 3:7).
- **"+ 새 업무" 버튼/입력폼을 없앴습니다.** 업무 등록은 이제 채팅창에 메시지를 치는 것만으로 이루어집니다(기존에도 있던 기능이지만, 버튼식 입력창이 따로 있어 헷갈렸던 부분을 정리해 채팅 한 곳으로 통일했습니다).

## v0.29.0 - 2026-08-01

통합 인물관리 시스템 - 학생 영구 고유번호 도입 + 연도/학기 통합 + 사건기록-학생 구조적 연결 + 학생 정보 조회 화면 신규:

**동명이인 구분 방법**: 이름만으로는 같은 이름의 학생(예: 김재이가 1A/1C/2J 세 반에 있음)을
구분할 수 없어서, 실제 학교 현장에서 쓰는 "학번" 방식을 그대로 들여왔습니다. 학생이 처음
등록되는 순간 `GIA-2026-0001` 형식의 영구 고유번호(student_no)가 자동으로 한 번 부여되고,
이후 이름이 바뀌든 학년·반이 바뀌든 절대 바뀌지 않습니다. 업무·사건기록·위클리 리포트 세
영역 모두 이제 이 고유번호(정확히는 그것과 묶인 학생 레코드의 id)를 기준으로 같은 학생을
가리키게 됩니다 - "강여명 어머니께서"라는 문구가 나와도, 그 강여명이 어느 학생인지는 화면에서
직접 골라서 정확히 연결해야 합니다(이름만으로는 시스템이 자동으로 확신할 수 없기 때문입니다).

- **학생 영구 고유번호(student_no).** 기존 114명 전원에게 일괄로 GIA-2026-0001 ~ GIA-2026-0114 번호를 부여했습니다.
- **학생 인적/학적사항 확장.** 생년월일·학생 연락처·주소 컬럼을 추가했고, 반(wr_classes)과도 FK로 연결해 담임선생님을 안정적으로 조회할 수 있게 했습니다.
- **연도>학기(정규학기+캠프)>학생/교직원 통합 분류체계.** 위클리 리포트가 따로 쓰던 학기 테이블(wr_terms)을 없애고, 운영(gia-ops)이 쓰던 학기 테이블(terms - 연도+학기유형: 1학기/ 2학기/3학기/여름캠프1/여름캠프2/겨울캠프1/겨울캠프2)로 완전히 통합했습니다.
  - **재학 이력(wr_enrollments) 신설.** "몇년도 어느 학기에 이 학생이 몇학년 몇반, 담임은 누구였는지"를 스냅샷으로 남기는 이력 테이블입니다.
- **사건기록 ↔ 학생 구조적 연결.** 사건 입력/수정 화면에 "관련 학생(정확히 연결)" 검색창을 추가했습니다.
- **직위체계 정리.** '교직원'이라는 모호한 표현을 '행정직원'으로 명확히 했습니다(교사/행정직원/ 관리자 3단계로 표현을 정리했습니다(실제 권한 로직은 바뀌지 않았습니다).
- **학생 정보 조회(통합 프로필) 화면 신규 - `/students`.** 행정직원/관리자만 접근 가능합니다.

## v0.28.1 - 2026-08-01

위클리 리포트에 실제 학생 명단(114명, 10개 반) 이관 - 코드 변경 없이 DB 데이터만 추가:

- 업로드해주신 실제 GIA 명단(구 프로토타입 DB 덤프)을 현재 `wr_classes`/`wr_students` 스키마에 맞춰 옮겼습니다.
- 32번에서 넣어뒀던 테스트용 더미 데이터(이해린/김민지/팜하니/강해린/다니엘, 1a/2a 반)는 정확히 그 5명/2개 반만 지정해서 함께 정리했습니다 - 실제 명단과 섞이지 않습니다.
- ⚠️ **담임 선생님 이메일은 비워뒀습니다.** 원본 자료에 실제 `@giamicro.com` 이메일이 없고 구 시스템의 아이디/비밀번호만 있었습니다.
  - 1A=Aimie, 1C=Carina, 1J=Jamie, 2Y=Yunsang, 2J=Jandy, 2K=Katherine, 3J=Janelle, 3A=Anna, 4A=Sarah, 5E=Eamonn.
  - Crystal/Michelle/Celine 선생님은 원본 자료상 담임 배정이 없어(과목 전담으로 추정) [과목반 세팅] 화면에서 별도로 배정해주세요.

## v0.28.0 - 2026-08-01

사이드바 화이트 복귀 + 업무 탭 WorkFlatform 원본 재구현 + 플라이아웃 메뉴 + 통합 관리자 대시보드 + 3개 앱 테마 명확화:

- **사이드바를 다시 화이트로 되돌리고, 페이지 배경만 톤 조정했습니다.** 로고가 있는 사이드바가 계속 색이 바뀌다 보니 로고와 어울리지 않는다는 지적을 반영해, 사이드바/헤더는 다시 흰색으로 고정하고 각 화면의 콘텐츠 영역 배경만 앱별로 다르게 유지했습니다(가시성 저하 없이 톤만 구분).
- **업무 탭을 사용자가 지정한 "워크플랫폼" 참조 소스코드의 UI/UX 그대로 재구현했습니다.** 이전에 자체적으로 만들었던 남색 톤 칸반 대신, 참조 프로그램과 동일하게 밝은 블루 + 글래스모피즘 (반투명 블러) 스타일로 전면 교체했습니다.
  - 드래그앤드롭을 `@dnd-kit`(core/sortable/utilities) 라이브러리로 새로 도입해 카드를 마우스로 끌어다 상태(진행 대기/진행 중/보류·이슈/완료) 사이로 옮기면 실제로 붕 뜨는 미리보기가 따라오는, 참조와 동일한 드래그 경험을 구현했습니다.
  - 부서별 업무 상황판(완료율 도넛 게이지 + 전체/완료/진행&대기/보류&이슈 배지 목록), 부서 그룹채팅(@담당자 태그 시 자동 업무 등록, #부서명 태그 시 해당 부서 채팅방에 공유, 실시간 자동완성 메뉴), 실시간 로그(상태변경·업무확인 이벤트를 자동 기록)까지 참조의 구성 요소를 빠짐없이 옮겼습니다.
  - 참조 소스코드에는 있었지만 저희 앱에는 이미 메인 사이드바가 있어 중복되는 좌측 세로 부서 목록만, 상단 가로 탭(부서 색상 점 클릭 시 관리자 색상 변경) 형태로 의도적으로 바꿨습니다.
- **사이드바 메뉴를 플라이아웃(호버 시 오른쪽에 펼쳐지는) 구조로 재편했습니다.** 메뉴가 너무 길어 계속 스크롤해야 했던 문제를 해결하기 위해, 주메뉴는 홈/업무/운영 관리/위클리 리포트/지원·관리만 세로로 깔끔하게 두고, 세부 항목(사건기록·회의기록·매뉴얼 등)은 주메뉴에 마우스를 올렸을 때만 오른쪽에 펼쳐지는 부메뉴로 이동했습니다.
- **문의 및 사용자관리를 "지원 · 관리" 카테고리로 묶어 메뉴 맨 뒤로 옮겼습니다.** 사용자관리와 새로 만든 관리자 대시보드는 관리자만, 문의및건의사항은 교직원 이상 전원이 볼 수 있도록 카테고리 안에서 권한별로 항목을 다르게 구성했습니다.
- **통합 관리자 대시보드(`/admin/dashboard`)를 신규로 만들었습니다.** 교장/이사장/부이사장/부서장 등 "관리자" 직위를 가진 분들이 업무·운영·위클리 리포트 세 영역을 한 화면에서 확인할 수 있습니다.
  - 최근 30일 사건/행사/회의 건수, 전체 업무 완료율, 재적 학생 수, 이번주 평가 발행률 등 핵심 지표를 상단에 숫자로 요약했습니다.
  - **반복되는 사건 유형을 자동으로 짚어드립니다** - 최근 6개월간 같은 분류(manual_cat)로 3건 이상 반복된 사건 유형을 발생 빈도 순으로 보여줍니다.
  - **학생 평가(위클리 리포트) 현황도 함께 봅니다** - 이번 학기 동안 경고·미흡 배지가 3회 이상 반복된 "반복 지도 필요" 학생과, 우수 배지가 3회 이상 반복된 "반복 우수" 학생을 자동으로 뽑아 보여줘서, 반복되는 부진에는 추가 조치를, 계속 잘하는 학생에게는 보상을 검토할 수 있도록 했습니다.
  - **월별 추이를 그래프로 보여줍니다** - 사건·행사·회의 발생 건수 추이, 학생평가 경고/우수 배지 추이를 최근 6개월 막대그래프로, 부서별 업무 완료율을 비교 막대그래프로 표시합니다.
  - ⚠️ 앱의 직위 체계에는 "부장" 직급이 따로 없어(교사/교직원/관리자 3단계), 현재는 "관리자" 직위를 가진 모든 계정이 이 대시보드에 접근합니다.
- **업무/운영/위클리 리포트 세 화면의 테마를 명확히 구분했습니다.** 사이드바는 공통으로 흰색을 유지하되, 콘텐츠 영역 배경과 강조색을 화면마다 다르게 해서 지금 어느 앱에 들어와 있는지 한눈에 알 수 있게 했습니다.
  - 업무: 밝은 블루 + 글래스모피즘(반투명 블러) - WorkFlatform 원본 룩 그대로.
  - 운영(gia-ops): 남색 + 골드 - 기존 브랜드 톤 유지.
  - 위클리 리포트: 틸(청록) 계열 배경(`--color-wr-bg`) + 전용 강조색(`--color-wr-primary`, 사이드바 메뉴 강조색과 겹치지 않는 살짝 더 짙은 틸)으로 버튼·강조 요소를 새로 통일했습니다.

## v0.27.0 - 2026-08-01

- **디자인 시스템 도입(GIA 남색+골드).** 로고의 짙은 남색을 기준으로 남색/골드 팔레트를 `globals.css`에 정의하고, 페이지 배경을 흰색에서 살짝 톤 다운된 회색빛(`#eef1f6`)으로 바꿔서 흰색 카드가 배경과 구분되도록 했습니다 - "다 하얘서 가시성이 떨어진다"는 문제의 원인이 흰 카드가 흰 배경 위에 있었기 때문이라, 배경만 바꿔도 앱 전체(GIA ops·업무·위클리 리포트 공통 레이아웃)에 카드 구분이 생깁니다.
- **공용 셸(사이드바/헤더)을 남색 테마로 전환.** 대시보드 좌측 사이드바와 모바일 헤더가 남색 그라데이션이 되고, 로고는 남색 배경 위에서도 잘 보이도록 흰 칩(chip) 위에 올렸습니다.
- **업무/사건/행사/제안함/문의 등 앱 전반의 강조 버튼·선택 탭 색상을 슬레이트 계열에서 GIA 남색으로 통일**해서 세 앱이 같은 브랜드 색을 쓰도록 정리했습니다.
- **위클리 리포트 실데이터 시드.** 전달해주신 학생/반/과목/학기 더미 데이터를 실제 `wr_terms`/`wr_classes`/`wr_students`/`wr_subjects`/`wr_reports` 테이블 구조(uuid 기본키)에 맞춰 옮겼습니다: 2026년 가을학기, 1학년A·2학년A 반, 학생 5명(이해린/김민지/팜하니/강해린/ 다니엘, 학부모 연락처 포함), 수학(1학년)·영어(2학년) 과목, 이해린 담임 리포트 샘플 1건.
  - ⚠️ **원본 데이터의 teacher1/teacher2 계정은 실제 @giamicro.com 이메일이 아니라서 반/과목의 담당교사는 비워뒀습니다.** 위클리 리포트 관리 > 반/담임 배정, 과목반 세팅 화면에서 실제 선생님 이메일을 배정해주세요.
- **레거시 정리.** 홈 화면의 개인용 "할 일" 위젯이 팀 공유 업무 보드로 완전히 대체된 뒤 화면 어디에서도 쓰이지 않던 `todos` 테이블을 정리했습니다.

## v0.26.0 - 2026-08-01

GIA 통합 플랫폼 3번째 통합:

- 별도로 개발 중이던 학생 주간 리포트 앱(교사가 학업/보완점/참여도/태도/교우관계 5개 항목을 뱃지(🌟탁월/🟢양호/⚠️지도요망/🚨집중지도)와 서술형으로 매주 평가하는 앱)을 gia-ops-web 안에 새 메뉴 "위클리 리포트"로 통합했습니다.
- **교사(직위=교사)는 로그인하면 위클리 리포트 화면만 보이고, 사이드바에도 "내 담임반"· "내 담당과목" 두 메뉴만 나타납니다.** GIA ops(사건/회의/매뉴얼 등)와 업무 보드는 아예 접근 자체가 막힙니다(주소를 직접 입력해도 위클리 리포트로 돌아옵니다) - 계약직으로 짧게 근무할 수도 있는 교사에게 내부 문서 성격의 다른 메뉴를 보여주지 않기 위함입니다.
- **관리자/교직원은 기존 메뉴 전체 + 위클리 리포트를 함께 볼 수 있습니다.** 교직원은 "학생 현황"·"리포트 프린트"를, 관리자는 여기에 더해 "반/담임 배정"·"과목반 세팅"· "학생 명부"·"학기 관리"·"통계 대시보드"까지 볼 수 있습니다.
- 리포트 작성 화면은 원본 앱의 기능을 그대로 옮겼습니다: 뱃지 복수 선택, 3초 자동 임시저장, 종합 의견 상용구 저장(개인 브라우저에 저장), 지난주 기록 보기, 임시저장/발행 구분, 다른 과목 탭은 읽기 전용으로 열람.
- 학부모 배포용 PDF(발행된 리포트만 모아서 과목별로 정리)를 리포트 프린트 메뉴에서 바로 열람/다운로드할 수 있습니다.
- 참고: 원본 문서의 "AI 뱃지"는 실제로는 AI가 아니라 선생님이 직접 클릭해서 고르는 수동 평가였습니다(이름만 ai_tags였을 뿐 생성 로직은 없었음) - 그대로 수동 선택 방식으로 옮겼습니다.
- 원본 앱의 회원가입/비밀번호/사용자 승인/오류 로그/관리자 대시보드 화면은 옮기지 않았습니다 - 이미 gia-ops-web에 동일한 기능(구글 로그인 승인, 관리자 대시보드 등)이 있어서 그걸 그대로 씁니다.

## v0.25.0 - 2026-08-01

로그인 승인 방식 전면 개편:

- 처음 로그인하는 사람은 이제 이름, 소속(유치부/초등부/중고등부), 직위(교사/교직원/관리자)를 입력하는 온보딩 화면(`/onboarding`)을 먼저 거친 뒤에 승인 대기 화면으로 넘어감.
- 승인 권한이 "승인된 사람이면 누구나"에서 "직위가 관리자인 사람만"으로 좁혀짐.
- 이 기능이 추가되기 전 이미 승인됐던 기존 계정도, 아직 이름을 입력한 적이 없다면 다음 로그인 때 온보딩 화면을 한 번 거치게 됨(그 뒤로는 다시 뜨지 않음).
- 온보딩 중 자기 상태(status)를 스스로 승인으로 바꾸거나, 승인 기록(담당자/일시)을 조작하는 것은 DB 트리거로 원천 차단됨 - 오직 관리자만 승인/거절 처리 가능.
- 사용자 관리 화면에 이제 이메일뿐 아니라 이름·소속·직위가 함께 표시됨.
- 업무 보드(칸반 카드, 상세 패널, 담당자 태그, 접속자 목록)와 부서 채팅에서 이제 이메일 대신 이름이 표시되고, 채팅 멘션도 "@이메일앞부분" 대신 "@이름"으로 사람을 태그함.
- (참고) 사건/회의/행사의 담당자·작성자 표시, 문의사항 작성자 표시, 관리자 대시보드 로그는 이번 작업 범위에 포함되지 않아 여전히 이메일로 표시됩니다 - 필요하시면 다음 작업으로 이어서 처리해드릴게요.

## v0.24.0 - 2026-07-31

GIA WorkFlatform 통합 2단계:

- 업무 페이지에서 부서 탭을 선택하면(기본값 초등부) 오른쪽에 그 부서의 실시간 채팅창이 뜸.
- 채팅에서 "@사람이름"을 태그하면 그 메시지가 즉시 업무 카드로 등록되고(상태: 예정, 담당자: 태그된 사람), "✅ 업무로 등록됨" 안내 메시지가 채팅에 남아서 누가 봐도 바로 확인 가능.
- 채팅에서 "#부서명"을 태그하면 같은 메시지가 그 부서 채팅방에도 그대로 복사되어 들어감 (원본 부서 이름이 "~에서 공유됨"으로 표시됨) - 부서 간 교차 공유.
- 빠른 입력을 위해 채팅 입력창 위에 팀원 이름(@)과 다른 부서(#) 버튼을 눌러서 바로 태그를 삽입할 수 있게 함.
- 부서를 선택하면 그 부서 업무의 완료율을 보여주는 원형 게이지 위젯 추가.
- 부서 목록을 코드에 하드코딩하지 않고 departments 테이블로 관리하도록 변경.

이번 단계에서 기획서 대비 단순화한 부분: 부서 하나당 채널을 여러 개(공지/자유 등) 두는 기능은
아직 없고 부서=채널 1:1로 구현했습니다. 또 지금은 모든 승인된 팀원이 모든 부서 채팅을 볼 수
있어요(부서 소속에 따른 접근 제한은 아직 없음, 학교 규모상 전체 공유가 더 유용할 것으로 판단).
필요하시면 다음 단계로 추가하겠습니다.

## v0.23.0 - 2026-07-31

GIA WorkFlatform 통합 1단계:

- 사장님이 주신 "GIA WorkFlatform" 기획서(부서별 채팅+업무 자동할당+글래스모피즘 UI 협업툴)를 검토한 결과, 단계적으로 통합하기로 했고 1단계로 업무보드에 부서(department) 개념을 추가함.
- tasks에 부서 필드 추가(기본값 유치부/초등부/행정실 + 실제 쓰인 부서명이 자동으로 선택지에 추가됨 - 학기/행사명과 같은 방식).
- 디자인: 기획서의 vanilla CSS 대신, 기존 Tailwind 체계를 유지하면서 Tailwind 유틸리티만으로 글래스모피즘(반투명 + 블러 + 은은한 그라데이션)을 구현함.
- 다음 단계(부서별 실시간 채팅 + `@`/`#` 태그로 업무 자동 전환)는 별도로 진행 예정 - 실제 부서 전체 목록과 Supabase 요금제(Realtime 동시 연결 수) 확인이 먼저 필요함.

## v0.22.0 - 2026-07-31

- 이전에 만든 개인용 할 일(홈 위젯)/업무히스토리(달력)를 팀이 같이 보는 업무판인 새 "업무" 메뉴로 완전히 대체했습니다.
- 사이드바에 "업무" 메뉴 신설(홈 바로 아래).
- 지금 이 페이지에 접속해 있는 팀원을 실시간으로 보여줌(Supabase Presence).
- 업무를 등록하면 카드 형태로 "예정" 칸에 들어가고, 마우스로 카드를 끌어서 진행중/완료/보류 칸으로 옮길 수 있음(모바일 등 드래그가 불편한 환경을 위해 카드마다 상태를 바로 바꾸는 드롭다운도 함께 제공).
- 업무를 등록하거나 카드를 열었을 때, 지금 접속 중인 팀원을 포함한 승인된 팀원 전체를 담당자로 태그할 수 있음(여러 명 가능).
- 카드를 클릭하면 상세 패널이 열리고, 상태/우선순위/마감일/담당자를 바로 수정할 수 있음.
- 구글 시트 메모처럼, 각 업무 카드에 실시간 코멘트를 남길 수 있음 - 팀원이 그 업무를 보고 있으면 코멘트가 실시간으로 함께 뜸.
- 완료된 업무는 기본적으로 최근 14일치만 보여서 보드가 오래된 카드로 지저분해지지 않게 하고, 체크박스로 전체 완료 이력도 펼쳐볼 수 있음.
- 우선순위(보통/긴급) 태그, 마감일 지난 카드 빨간 테두리 강조 등 부가 기능도 함께 추가.
- 홈 화면은 "업무 현황"(예정/진행중/완료/보류 건수 + 나에게 태그된 업무 건수)만 요약으로 보여주고, 자세히 보려면 업무 메뉴로 이동하도록 정리.

## v0.21.0 - 2026-07-31

할 일 입력 확대 + 날짜별 기록 저장 + 업무 히스토리(달력) 메뉴 신설:

- 홈 화면 할 일 입력창을 더 크게(글자 크기/여백 확대).
- 할 일마다 "날짜(for_date)"가 함께 저장됨.
- 사이드바 "실무자매뉴얼" 아래에 "업무히스토리" 메뉴 신설.
- 알림 시간 설정 UI도 날짜+시간을 분리해서 더 명확하게 정리(선택한 날짜의 그 시간에 알림).

## v0.20.1 - 2026-07-31

- "홈" 제목 글씨를 없앰(학기 표시와 함께 있을 때 어울리지 않는다는 피드백 반영).
- 학기 표시를 파란 버튼(알약 모양) 스타일에서, 페이지 제목처럼 보이는 큰 글씨 + 아래 짧은 포인트 라인으로 변경 - 클릭 가능한 버튼처럼 보이지 않도록 정리.

## v0.20.0 - 2026-07-31

홈 화면 왼쪽에 개인 할 일 목록 + 시간 알림 기능 신설:

- 홈 화면 맨 왼쪽에 "✅ 할 일" 위젯 추가.
- 설정한 시간이 되면 팝업으로 알려줌: (1) 브라우저 알림 허용 시 OS 알림 센터에도 뜨는 네이티브 알림, (2) 앱을 켜둔 화면 오른쪽 아래에 뜨는 인앱 팝업(완료 처리/닫기 버튼 포함) - 홈 화면이 아니어도 앱의 다른 어떤 화면에 있든 뜸.
  - 주의: 이 알림은 브라우저 탭(앱)이 열려 있는 동안에만 동작함.

## v0.19.0 - 2026-07-31

- 홈 화면 학기 표시를 제목 옆 작은 글씨에서, 제목 아래 중앙의 눈에 띄는 파란 배지("📅 2026 여름캠프2")로 이동해 가장 눈에 띄게 강조했습니다.
- 달력 위젯에 대한민국 공휴일을 표시함.
- 달력의 날짜를 클릭하면 그 날짜로 OS 기본 캘린더 앱이 열리도록 연동:.
  - Mac/iOS: Calendar.app을 `calshow:` 링크로 즉시 열어서 해당 날짜로 이동.
  - Windows/Android 등: 해당 날짜의 .ics 파일을 다운로드함 - 열어보면(더블클릭) 기본 캘린더 앱(Outlook/캘린더/구글 캘린더 등)에 바로 추가할 수 있음(웹 표준 "캘린더에 추가" 방식).
- 행사기록 > 정규행사 탭의 "📊 연도별 비교 리포트" 버튼을 "🔍 행사분석 (AI)"로 이름 정비.

## v0.18.0 - 2026-07-31

홈 화면 학기 표시 단순화 + 달력/시계 위젯 + 학기 자동 전환(날짜 기반):

- 홈 화면 상단의 학기 안내 박스(클릭 가능한 카드, "현재 학기:" 라벨, 안내 문구)를 없애고, 제목 "홈" 옆에 "📅 2026 여름캠프2"처럼 값만 표시.
- 홈 화면 오른쪽에 실시간 시계 + 이번 달 달력(오늘 날짜 강조, 좌우 화살표로 다른 달도 확인 가능) 위젯 추가 - 오늘이 며칠인지 한눈에 확인 가능.
- 사이드바(로고-이메일 사이)/모바일 상단의 학기 배지 로직 자체는 정상이었음.
- **학기 자동 전환(신규)**: 학기 등록 폼의 시작일/종료일은 이미 달력(날짜 선택) 입력이었음.
  - 배포 후 한 번만 설정하면 됨: (1) 터미널에서 `openssl rand -hex 32`로 무작위 값 생성 → Vercel 프로젝트 Settings → Environment Variables에 `CRON_SECRET`으로 등록, (2) 같은 값으로 로컬 `.env.local`에도 `CRON_SECRET=...` 추가(선택, 로컬 테스트용), (3) `vercel.json`에 크론 설정을 이미 포함해서 커밋했으므로 GitHub Desktop으로 푸시하면 Vercel이 자동으로 매일 인식해서 실행함 (Vercel 프로젝트 Cron Jobs 탭에서 실행 이력 확인 가능).

## v0.17.3 - 2026-07-31

- 홈 화면, 사이드바(로고-이메일 사이) 배지, 학기 메뉴 상단 "현재 학기" 표시를 모두 `학기 (연도)` → `연도 학기` 형식으로 통일.
- 학기 메뉴의 "현재 학기" 변경 드롭다운에 연도 선택 셀렉트를 추가함.

## v0.17.2 - 2026-07-31

- "현재 학기" 변경 드롭다운이 이미 저장된 회차 기록에서만 목록을 만들다 보니, 아직 아무 학기도 등록 안 한 상태에서는 빈 목록으로 보였음.

## v0.17.1 - 2026-07-31

- 학기·캠프 화면 맨 위에 "현재 학기" 영역 추가: 지금 진행중으로 설정된 학기가 바로 보이고, "변경" 버튼으로 드롭다운에서 아무 회차나 골라 즉시 전환 가능.
- 코드 정리: 아무 데서도 안 쓰이던 중복 페이지(`/incidents`, `/records`와 완전히 같은 내용)를 제거.
- 성능: 사건기록/회의기록/AI매뉴얼 화면 오른쪽의 AI 제안 패널이 실시간 갱신을 구독할 때, 이제 Supabase 쪽에서 해당 화면과 관련 없는 변경 이벤트는 아예 걸러서 보내도록 필터를 걸어 불필요한 네트워크 트래픽을 줄이고, 처음 불러올 때도 화면에 실제로 쓰이는 컬럼만 가져오도록 줄여 응답 속도를 개선.

## v0.17.0 - 2026-07-31

- **문의및건의사항** 메뉴 신설(전 직원): 오류 신고/기능제안/기타를 자유롭게 남기고, 본인이 남긴 문의와 답변을 확인할 수 있음.
- **관리자 대시보드**(`/dev`): 전체 데이터 현황(테이블별 건수), 14일 넘게 방치된 제안/채택예정과 3일 넘게 미처리된 오류 문의 알림, 최근 30일 AI 사용량(라우트별 호출수·실패수·토큰수), 최근 오류 로그 20건을 한 화면에서 확인.
  - 관리자 메뉴에서는 문의사항도 전체 조회 + 상태변경(접수/처리중/완료) + 답변 작성 가능.
- **오류 로깅**: 지금까지 API 오류가 기록되지 않던 것을 모든 AI 관련 API 라우트(16개)에서 자동으로 `error_logs`에 남기도록 구현.
- **AI 사용량 로깅**: `callClaudeJson` 호출마다 라우트명·모델·입력/출력 토큰수·성공여부를 `ai_usage_logs`에 자동 기록(비용/사용 패턴 모니터링용).
- **Vercel Analytics + Speed Insights** 연동: 배포하면 별도 설정 없이 실제 사용자 페이지 로딩 속도가 Vercel 대시보드에 자동으로 수집됨(무료 도구).

## v0.16.0 - 2026-07-31

- **사건기록 AI 자동채우기**: 상세 내용(경위)란에 두서없이 적고 "🧹 AI로 채우기"를 누르면 날짜· 제목·잘된 점·부족했던 점·보완점을 AI가 원문에서 찾아 자동으로 채워줌(원문에 없는 내용은 억지로 지어내지 않고 비워둠 - 없어졌던 예전 기능을 새 화면 구조에 맞게 복원).
- **담당자 자동 입력**: 새 사건을 작성할 때 담당자(작성자) 칸에 로그인한 계정 이메일이 자동으로 채워짐(직접 수정 가능).
- **학기 자동 연결**: 학기 메뉴에서 어떤 학기/캠프를 "진행중"으로 등록해두면, 그 기간에 새로 작성하는 사건·회의 기록이 자동으로 그 학기에 연결됨.
- 홈 화면 상단에 "현재 학기" 카드 추가(진행중인 학기가 없으면 등록 안내 표시).
- 사이드바(PC)와 상단 헤더(모바일) 로고와 로그인 계정 사이에 현재 학기 배지 표시, 누르면 학기 메뉴로 이동.

## v0.15.0 - 2026-07-31

- 모니터가 넓을 때 본문이 가운데에 좁게 몰려 여백이 과하게 남던 문제 수정.
- **사건기록**: 기존에는 "+새로 입력"을 눌러야 입력칸이 나타났는데, 이제 왼쪽에 지금까지 기록한 사건 목록이 항상 보이고 가운데에 입력폼이 항상 떠 있어 바로 적고 저장할 수 있음.
- **회의기록**: 왼쪽에 날짜별 회의 목록, 가운데에 지금까지 쓰던 채팅 작성창(텍스트/음성파일 업로드/라이브 녹음 모두 그대로)이 항상 떠 있음.
- **AI 매뉴얼**: 왼쪽에 지금까지 작성한 이력이 쌓여서 클릭하면 다시 불러올 수 있고, 가운데 입력폼과 오른쪽 AI 제안 패널(검토대기/채택예정)이 함께 있음.
- 사건/회의를 저장하는 즉시 그 건만 AI가 바로 분석하도록 스캔 API를 확장(기존에는 5건 단위 일괄 분석만 있어서, 방금 쓴 글이 오른쪽에 뜨기까지 다른 미분석 기록이 쌓이길 기다려야 했음).
- 클릭 반응 속도: 지난 버전(미들웨어 캐싱, 브라우저 클라이언트 재사용)에 이어, 이번 3단 작업화면으로 사건/회의/AI매뉴얼-제안함-채택예정 사이 페이지 이동 자체가 크게 줄어 체감 반응속도가 개선됨.

## v0.14.2 - 2026-07-31

- 모델을 whisper-large-v3-turbo -> whisper-large-v3(전체 모델)로 변경 - 무료 한도는 동일한데 인식 정확도가 더 좋음.
- 언어를 자동감지 대신 한국어(ko)로 명시 - 특히 짧은 구간에서 자동감지가 흔들리는 문제 개선.
- GIA 관련 용어(부이사장, 실장, 플리마켓, 구글폼 등)를 미리 알려주는 프롬프트를 추가해 고유 용어 인식률 개선.
- 라이브 녹음의 마이크 설정에 에코 제거/노이즈 억제/자동 게인을 명시적으로 켜고, 녹음 비트레이트를 높여 음질 개선.
- 라이브 녹음 구간 길이를 45초 -> 60초로 늘려 문장이 구간 경계에서 잘리는 빈도를 줄임(대신 화면 갱신 주기는 약간 느려짐).

## v0.14.1 - 2026-07-31

- 음성 인식(STT) 서비스를 OpenAI(유료, 카드 등록 필요)에서 Groq 무료 티어로 변경 - 신용카드 등록 없이 가입만으로 하루 2,000건/시간당 오디오 7,200초까지 무료 사용 가능(학교 회의록 용도로는 충분한 한도).
- **필요 환경변수 변경**: `OPENAI_API_KEY` 대신 `GROQ_API_KEY`를 Vercel에 추가해야 함 (아래 가입 가이드 참고).

## v0.14.0 - 2026-07-31

- 회의를 "자료 기록" 그룹 안에서 독립 메뉴("회의기록")로 분리(기존 기록함은 이제 사건만 다룸, 라벨도 "사건기록"으로 명확히 함).
- 회의록 채팅에 "🔴 회의 시작" 라이브 녹음 기능 추가: 누르면 마이크 녹음이 시작되고, 약 45초 구간마다 자동으로 텍스트로 바뀌어 채팅에 전송되면서 정리본이 회의 진행 중에 계속 갱신됨.

## v0.13.0 - 2026-07-31

- 회의록 "새로 입력"이 채팅 형식으로 바뀜: 두서없이 적은 회의 메모를 그대로 붙여넣으면 AI가 애매한 부분(예: "시간 아침에 바꿔줘야 함" -> "원래 몇 시였고 왜 바꾸나요?")을 대화로 되물으면서 정식 회의록을 실시간으로 정리.
- 채팅에 회의 녹음 음성 파일을 올릴 수 있음 - 자동으로 텍스트로 바뀐 뒤 같은 대화형 정리 과정을 시작함(OpenAI 음성 인식 API 사용, Claude API는 오디오 입력을 지원하지 않아 이 부분만 별도 서비스 이용 - 분당 약 0.003달러로 저렴함).
- **배포 전 필수**: Vercel 프로젝트 환경변수에 `OPENAI_API_KEY`를 새로 추가해야 음성 변환 기능이 동작합니다(OpenAI 플랫폼에서 발급).

## v0.12.0 - 2026-07-31

- AI 예상 문의/컴플레인 제안: 카테고리명만으로 중복을 걸러내던 것을, 기존 실무자매뉴얼에 이미 규정된 내용과 검토 대기 중인 예상 문의의 실제 본문까지 AI에게 함께 보여줘서, 항목명이 달라도 이미 다뤄진 주제는 자동으로 걸러내도록 개선.
- 채택예정에 "AI 검증" 기능 추가: 실무자가 GIA 실정에 맞게 구체화한 최종 문구를 AI가 비판적으로 분석해서 예상 후속 문의/컴플레인, 조항의 맹점·허점, 구체적인 보완 제안을 짚어줌.

## v0.11.0 - 2026-07-30

- 브라우저 Supabase 클라이언트를 페이지 로드당 1개만 만들어 재사용하도록 변경(기존에는 버튼을 누르거나 목록을 구독할 때마다 매번 새 클라이언트를 새로 만들어서, 그때마다 세션을 다시 읽고 내부 잠금을 새로 잡는 지연이 있었음) - 특히 저장/발행 등 버튼 클릭 반응 속도에 가장 크게 기여.
- 로그인 승인 여부 확인을 매 페이지 이동마다 DB에서 조회하던 것을, 한 번 확인되면 5분 동안은 서명된 쿠키만으로 빠르게 통과시키도록 변경(페이지를 옮길 때마다 있었던 네트워크 왕복 1회를 대부분의 경우 제거) - 관리자가 승인을 취소/차단해도 최대 5분 이내에는 반영됨(실제 데이터 접근은 항상 Supabase 행 단위 보안(RLS)이 별도로 검사하므로 안전에는 영향 없음).
- 세션 검증을 getUser()에서 getClaims()로 교체(프로젝트가 최신 비대칭 서명키를 쓰는 경우 매 요청 인증 서버 왕복 없이 로컬 검증되어 더 빠름, 그렇지 않아도 기존과 동일하게 동작).
- 홈/기록함 등 서버 페이지의 여러 DB 조회는 이미 병렬(Promise.all)로 처리되고 있어 추가로 손볼 부분이 없었음(점검만 진행).

## v0.10.0 - 2026-07-30

- 행사기록: 정규행사(매년 반복되는 행사)와 일시적행사(한 번뿐인 행사)를 구분해서 입력.
- 행사/학기 사진을 실제로 업로드해서 보관(비공개 Storage 버킷, giamicro.com 로그인 사용자만 조회/업로드/삭제 가능 - 아동이 포함된 사진일 수 있어 비공개로 유지).
- "학기" 메뉴 신설(행사 옆): 1~3학기, 여름캠프1·2, 겨울캠프1·2를 회차별로 기록.
- 회의록 AI 분류의 "행사/학기 참고" 카테고리를 확장해서, 특정 행사뿐 아니라 특정 학기/캠프에 대한 회고도 자동으로 구분해서 매칭되는 학기 기록에 붙여줌.

## v0.9.0 - 2026-07-30

- 행사기록을 기록함에서 분리해 독립 메뉴로 노출(기록함은 이제 사건·회의만 다룸) - 행사는 매뉴얼 제작용 자료가 아니라 반복 행사를 중복 없이 기록하고 다음 행사 준비에 참고하는 목적이 달라 별도로 뺐습니다.
- 사이드바 메뉴를 목적별로 그룹화: [홈/실무자매뉴얼] · [자료 기록: 기록함·AI매뉴얼] · [행사] · [제안·발행: 제안함·채택예정] · [문서함: 매뉴얼·서류함] · [관리: 사용자 관리].
- 회의록 AI 분석에 "행사/학기 참고" 분류 추가: 특정 행사나 학기에 대한 회고·개선 아이디어는 매뉴얼 제안으로 만들지 않고, 이름이 비슷한 행사 기록의 "개선 제안"에 자동으로 붙여서 다음번 같은 행사 열릴 때 AI 비교 리포트에 바로 반영되도록 함(매칭되는 행사가 없으면 회의록의 확정 기록에 메모로 남김).

## v0.8.0 - 2026-07-30

- "실무자매뉴얼" 전용 메뉴 신설(홈 바로 다음 위치) - 학부모 문의/컴플레인 응대용 매뉴얼을 검색해서 바로 찾아볼 수 있는 화면.
- AI가 학부모 문의/컴플레인을 미리 예상해서 권장 응대 문구와 함께 제안함에 자동 등록(제안함에 "예상 문의/컴플레인" 탭 추가), 학비/환불·안전·급식·소통 등 국제학교에서 흔한 주제를 폭넓게 고려.
- 워크플로우 정리: AI 제안 → 실무자 회의로 GIA 실정에 맞게 수정 후 승인(발행예정) → 이 단계에서 AI가 실무자가 수정한 문구를 다시 한번 깔끔한 규정 문구로 정리해서 채택예정에 반영 → 실무자 확인 후 발행 → 실무자매뉴얼에 카테고리로 반영 (기존 제안함/채택예정 화면 그대로 재사용).

## v0.7.0 - 2026-07-30

- 서류함 메뉴 신설: GIA 같은 대안교육기관이 갖추면 좋은 서류를 AI가 추천해서 목록으로 만들고, 각 서류별로 "AI 초안 만들기"로 바로 다듬어 쓸 수 있는 초안을 생성, 준비 상태(필요/준비중/ 보유/만료임박/해당없음)를 관리.
- 홈 대시보드에 "반복되는 사건 유형" 경고 추가: 최근 90일 내 같은 유형 사건이 3건 이상 발생하면 자동으로 표시(AI 호출 없이 순수 집계라 비용 없음).
- 행사 기록에 "연도별 비교 리포트" 추가: 같은 이름의 과거 행사 기록 2건 이상이 있으면 AI가 개선된 점/반복되는 문제/다음 행사 제안을 요약.
- 매뉴얼(학부모용 운영계획안)에 "FAQ 자동 생성" 추가: 전체 내용을 바탕으로 학부모가 궁금해할 질문/답변을 AI가 만들고, 검토 후 매뉴얼의 "자주 묻는 질문" 항목으로 반영.
- 매뉴얼 항목에 "서명 필요" 표시 기능 추가(환불 규정, 안전 수칙 등 학부모용 항목 한정) - 체크해두면 PDF로 출력할 때 그 항목 뒤에 학생명/보호자 성명/서명/날짜를 적을 수 있는 서명란이 자동으로 들어감(앱은 내부 인원만 쓰므로 전자서명 대신 배포용 문서에 서명란을 넣는 방식).

## v0.6.0 - 2026-07-30

- AI 매뉴얼 작성 시 어느 문서(학부모용 운영계획안/실무자매뉴얼)에 반영할지 사람이 미리 고르지 않고 AI가 내용을 보고 직접 판단(차량 탑승·아동 인계·환불 규정처럼 학부모도 알아야 하는 내용은 두 문서 모두에, 교사 채용 기준처럼 내부용 절차는 실무자매뉴얼에만 반영).
- 판단 이유를 화면에 함께 표시해서 왜 그렇게 분류됐는지 확인 가능.
- 사건/행사 AI 분석에서도 같은 로직 적용 - "두 문서 모두 해당"으로 판단되면 자동으로 두 건의 제안을 각각 만들도록 기존 버그 수정(이전에는 "둘다"라는 문자열이 그대로 저장되어 매뉴얼 화면 어디에도 반영되지 않는 상태였음).
- AI 비용 절감: 맞춤법 정리·회의 내용 분류처럼 실수해도 검토 단계에서 바로 잡을 수 있는 작업은 저렴한 모델(Haiku)로 전환, 학부모 공지·법적 판단처럼 리스크가 큰 작업만 고품질 모델(Sonnet) 유지 - 기존 prompt caching과 함께 적용되어 비용이 추가로 절감됨.

## v0.5.0 - 2026-07-30

- 로그인 승인제 도입: giamicro.com 계정이면 누구나 로그인은 되지만, 관리자가 승인해야만 대시보드에 들어갈 수 있음(승인 전에는 "승인 대기" 화면).
- 승인된 사람은 관리자로서 신규 신청 승인/거절과 기존 사용자 차단 가능.
- 새 메뉴 "사용자 관리" 추가: 승인 대기/승인됨/거절됨 목록을 실시간으로 확인하고 처리.
- 메뉴/버튼에 마우스를 올렸을 때 손가락 커서와 함께 색이 바뀌도록 통일(이전에는 아무 변화가 없어 클릭 가능한지 알기 어려웠음), 로고는 색 변화 없이 커서만 바뀌도록 유지.

## v0.4.0 - 2026-07-30

- 헤더/사이드바 로고 클릭 시 홈으로 이동하도록 복구(리스트 개편 때 빠졌던 링크 연결).
- 매뉴얼 화면을 "운영계획안"/"실무자매뉴얼" 탭 구조로 개편.
- 매뉴얼 항목을 구글독스처럼 서식(굵게·기울임·제목·목록) 적용하며 바로 보고 편집할 수 있는 리치 텍스트 에디터(Tiptap) 도입 - 새 항목 추가/기존 항목 수정 모두 적용.
- 발행(채택예정→매뉴얼) 시 기존 내용과 새 내용을 서식 깨지지 않게 이어붙이도록 병합 로직 개선.
- PDF 생성 시 리치 텍스트 내용을 태그 없는 읽기 좋은 텍스트로 자동 변환해서 출력.

## v0.3.0 - 2026-07-30

- AI 매뉴얼 메뉴 신설: 규정/매뉴얼 초안을 자유 텍스트로 작성하면 AI가 정식 문구 + 관련 법령까지 찾아 제안함(AI매뉴얼제안 탭)에 자동 등록, 승인 시 기존 채택예정→발행 흐름 그대로 매뉴얼에 반영.
- 회의록 작성/수정 화면에 "🧹 AI로 정리" 버튼 추가 - 두서없이 쓴 회의 메모를 AI가 맞춤법 교정 + 문어체로 정리(저장 전 미리보기/수정 가능).
- 제안함에 "AI매뉴얼제안" 탭 추가(전체/사건/행사/회의/AI매뉴얼 5개 탭).

## v0.2.0 - 2026-07-30

- 로고 반영: 사이드바/모바일 헤더에 GIA Micro Lab 로고(가로형), 로그인 화면에 크레스트 로고(정방형) 적용.
- 홈 대시보드 강화: "기록 현황"(사건/행사/회의) + "처리할 일"(제안함 대기·채택예정 대기·발행된 매뉴얼 항목) 두 그룹으로 확장.
- 기록함 메뉴 신설: 기존 사건/행사/회의 3개 메뉴를 "기록함" 하나로 통합하고 탭 + 건수 카드로 전환.
- 제안함에 카테고리 탭(전체/사건/행사/회의) 및 분류별 건수 표시 추가.

## v0.1.0 - 2026-07-30 (최초 커밋)

- Supabase(Postgres+Auth+Realtime) + Vercel 기반 독립 웹앱으로 최초 구축 (구글 시트/문서 미사용).
- 사건/행사/회의 기록 CRUD + 실시간 동기화.
- Google OAuth 로그인(giamicro.com 도메인 제한) + PIN 2차 보안.
- AI 제안 워크플로우(스캔 → 제안 → 승인/보류/삭제 → 채택예정 → 발행).
- 매뉴얼(운영계획안/실무자매뉴얼) 앱 내 직접 편집(CRUD) + PDF 생성.
- 모바일 반응형 전체 적용.
