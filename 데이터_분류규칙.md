# 데이터 분류 규칙 — 새 데이터를 어디에 둘 것인가

> 앞으로 이 앱에 데이터를 하나 더 붙일 때마다 **먼저 이 문서로 갈래를 정하고 시작합니다.**
> 갈래가 정해지면 스키마 규칙이 자동으로 따라옵니다.

현재 표 80개를 전부 분류한 결과입니다.

---

## 왜 두 갈래로는 부족한가

처음 생각은 "학생에 붙는 것 / 안 붙는 것" 두 갈래였습니다. 그런데 실제로 나눠보니 **셋째 무리**가
나왔고, 그게 지금 문제의 진원지였습니다.

구글챗 메시지, 토들 문의, 사건 접수 — 이것들은 **들어오는 순간에는 누구 것인지 모릅니다.**
"내일 결석합니다"라는 문장 하나가 전부일 때가 있습니다. 여기에 학생 연결을 강제하면 저장 자체가
안 되므로, 지금은 `student_id`를 비워둘 수 있게 해놨습니다. 그런데 비워둘 수 있게 해놓으면
**끝까지 비어 있는 행이 쌓입니다.** 그게 지금 상태입니다.

즉 문제는 "연결이 없다"가 아니라 **"연결되지 않은 채로 끝나도 아무도 모른다"** 입니다.

그래서 셋으로 나눕니다.

---

## 세 갈래

### ① 학생 기록 — 학생이 확정된 것

**규칙: `student_id uuid NOT NULL REFERENCES wr_students(id)`**

학생과 연결되지 않은 행은 **저장 자체가 안 됩니다.** 앱이 잊을 여지도, SQL로 직접 넣을 여지도,
나중에 다른 사람이 기능을 붙일 여지도 없습니다. 규칙이 아니라 구조입니다.

이름 칸(`student_name`)은 남기되 **원문 보존용**으로만 씁니다 — "학부모가 이렇게 적었다"는
기록이지, 학생을 가리키는 수단이 아닙니다.

| 표 | 현재 상태 |
| --- | --- |
| `wr_students` / `wr_students_basic` | 본체 |
| `wr_enrollments` | ✅ NOT NULL + FK — **유일하게 제대로 된 표** |
| `wr_reports` (주간 관찰기록) | 확인 필요 |
| `wr_comments` | 확인 필요 |
| `attendance_records` | 확인 필요 |
| `attendance_entries` | ⚠️ FK 없음, nullable |
| `pickup_schedules` | ⚠️ nullable |
| `shuttle_assignments` / `_basic` | 확인 필요 |
| `shuttle_boardings` | 확인 필요 |
| `shuttle_persistent_notes` | ⚠️ 이름 문자열만 |
| `incident_students` | 확인 필요 |
| `wr_import_issues` | 확인 필요 |
| **(앞으로) `student_events`** | 작품·성과·활동 — **여기가 원본** |

### ② 입구 — 아직 학생이 정해지지 않은 원문

**규칙 1: 학생 연결을 강제하지 않습니다.** 원문은 원문 그대로 보존합니다.
**규칙 2: ①로 올라가는 경로가 반드시 있어야 합니다.** 이게 없으면 만들지 않습니다.
**규칙 3: 못 올라간 것은 사람 눈에 띄어야 합니다.** 조용히 남으면 안 됩니다.

| 표 | ①로 가는 경로 |
| --- | --- |
| `google_chat_mirror_messages` | → `attendance_entries` (✅ 만듦) |
| `pickup_requests` | → `attendance_entries` (✅ 만듦) |
| `inquiries` | ⚠️ 경로 없음 |
| `incidents` | → `incident_students` |
| `form_submissions` | ⚠️ 경로 없음 |
| `teacher_office_requests` | 학생 건이면 경로 필요 |

어제 만든 출결 ❓ 배지가 규칙 3의 구현입니다. 자동으로 학생을 못 찾으면 물음표로 남아 사람이
보게 되고, 사람이 눌러야 ①로 올라갑니다. **입구 표는 전부 이 모양을 갖춰야 합니다.**

> 입구 데이터는 **오래 두지 않습니다.** ①로 올라갔으면 원문의 역할은 끝났습니다.
> 보관 기간(예: 1년)을 정해 정리합니다.

### ③ 학생과 무관

학생에 붙일 이유가 없는 것들입니다. 여기에 억지로 `student_id`를 만들지 않습니다.

**교직원에 붙는 것** — `app_users` `staff_assignments` `duty_roster` `tasks` `task_comments`
`task_attachments` `messages` `message_reads` `message_reactions` `meetings` `manual_drafts`
`manual_review_flags`

**학교 자산·편성** — `shuttle_routes` `shuttle_stops` `shuttle_route_paths`
`shuttle_campus_locations` `shuttle_tracker_devices` `shuttle_pilot_routes`
`shuttle_route_vehicle_history` `wr_classes` `wr_periods` `wr_subjects` `wr_timetable`
`terms` `departments` `gia_systems`

**기관 문서·지식** — `documents` `school_documents` `policy_categories` `proposals` `adopted`
`manual_sections` `manual_section_history` `academic_checklist_items` `_templates`
`education_news` `work_notices` `work_notice_collapses` `department_memos`

**기계가 남기는 것** — `shuttle_pilot_pings` `shuttle_stop_observations` `shuttle_stop_arrivals`
`shuttle_run_events` `shuttle_safety_events` `error_logs` `ai_usage_logs`
`integration_heartbeats` `backups` `google_chat_oauth_tokens`
`google_chat_event_subscriptions` `ops_board_links` `shuttle_board_links`
`shuttle_arrival_links` `ai_feature_flags` `task_mode_colors` `form_import_templates`
`attendance_learning_rules` `pickup_sender_feedback` `wr_student_field_defs`

> ③에는 **보관 기간을 반드시 정합니다.** 특히 기계가 남기는 것들은 사람이 지우지 않으면
> 무한히 쌓입니다. GPS 위치, 오류 로그, 하트비트가 그렇습니다.

---

## 앞으로 새 데이터를 붙일 때

```
새 데이터가 생긴다
      │
      ├─ 이 줄 하나가 "어떤 학생의 일"인가?
      │        │
      │        ├─ 예, 저장 시점에 학생이 확정된다
      │        │     → ① 학생 기록.  student_id NOT NULL + FK.  끝.
      │        │
      │        └─ 예, 그런데 들어올 땐 누군지 모른다
      │              → ② 입구.  ①로 올리는 경로와 ❓ 큐를 **같이** 만든다.
      │                 경로를 못 만들겠으면 그 기능은 아직 설계가 덜 된 것.
      │
      └─ 아니오
             → ③ 무관.  student_id 만들지 않는다.  보관 기간을 정한다.
```

### ①이면 함께 결정할 것

- **지금 상태인가, 지나간 일인가**
  - 지금 상태(출결·배차·알러지) → 기존 표. 바뀌고 지워지는 게 맞음
  - 지나간 일(작품·성과·상담) → `student_events`. **지우지 않음**
- 학부모에게 보여줄 것인가 (`visibility`)
- 포트폴리오에 실을 것인가

---

## 이렇게 하면 무엇이 달라지나

**타임라인을 위해 표를 하나 더 만들 필요가 없습니다.** ①의 모든 표가 같은 `student_id`로
묶여 있으면, 학생 타임라인은 그냥 뷰(VIEW)입니다. 복제도, 동기화도, 덧붙임도 없습니다.

```sql
create view student_timeline as
  select student_id, date,        '출결' as kind, ... from attendance_entries
  union all
  select student_id, occurred_at, kind,           ... from student_events
  union all ...
```

새 기능이 ①에 표를 하나 더 만들면, 뷰에 한 줄이 늘어날 뿐입니다.

**그리고 "학생 기준으로 기록한다"가 관례가 아니라 제약조건이 됩니다.** 지키려고 노력할 필요가
없어집니다 — 어길 수가 없으니까요.

---

## 다음 할 일

1. ①로 분류된 표들의 **미연결 행 수 실측** — "확인 필요"로 적힌 것들 실제 스키마 확인
2. 매칭 가능한 것 채우기 / 안 되는 건 ❓ 큐로
3. 다 채워진 표부터 `NOT NULL` + FK로 잠그기
4. `student_timeline` 뷰
5. ②의 경로 없는 표(`inquiries`, `form_submissions`)에 경로 만들기

1번이 나와야 이게 며칠짜리인지 알 수 있습니다.
