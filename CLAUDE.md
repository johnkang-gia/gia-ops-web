# 이 저장소에서 지켜야 할 것

이 파일은 이 저장소에서 작업하는 사람과 AI 모두가 먼저 읽는 규칙입니다.
한 번 정한 것이 다음에 또 어긋나지 않도록, **기억이 아니라 검사로** 지킵니다.

---

## 1. CHANGELOG.md — 대화를 옮기지 않습니다

체인지로그는 **나중에 읽는 사람**을 위한 기록입니다. 몇 달 뒤 이 줄을 읽는 사람에게는
그때의 대화가 없습니다. 그 사람에게 필요한 것은 **무엇이 문제였고, 무엇을 어떻게
바꿨고, 왜 그렇게 정했는가**뿐입니다.

### 쓰지 않는 것

| 쓰지 않음 | 이유 |
|---|---|
| `담당자: "…해줘."` | 요청을 그대로 옮긴 인용 |
| `요청: "…"` / `(요청: "…")` | 같음 |
| `사용자 원문`, `요청자` | 같음 |
| `…라고 하셨거든`, `말씀하셨` | 들은 말을 옮기는 투 |
| 대화 문장을 60자 넘게 통째로 옮긴 따옴표 | 말투가 사실을 가림 |

### 쓰는 것

문제 → 원인 → 조치 순서로, **사실만** 씁니다.

```
나쁨:
- 담당자: "탑승 학생 맨 앞에 약간 여백 주고, 뒤쪽 숫자 폰트 키워주고 굵게 해줘."

좋음:
- 차번호의 뒤 네 자리를 13pt 굵게로 키웠습니다. 사람이 외우고 부르는 것은 뒤 네 자리인데
  앞 글자와 같은 크기라 멀리서 구별되지 않았습니다.
- 탑승 학생 칸 왼쪽에 여백 9px. 격자선에 첫 아이 이름이 붙어 먹히고 있었습니다.
```

요청 내용이 판단의 근거라면, **말이 아니라 그 말이 알려준 사실**을 적습니다.

```
나쁨: 형제할인 같은 부분 원래는 있는데 없애신다고 하셨거든.
좋음: 형제할인은 운영하지 않기로 정해졌습니다. 그래서 할인 항목 목록은 고정될 수 없습니다.
```

### 따옴표를 써도 되는 경우

- 오류 메시지 원문 (`must be owner of publication`)
- 화면에 실제로 뜨는 문구 (`"차번호 없음"`)
- 코드 블록 안의 로그·SQL

즉 **기계가 낸 말**은 인용해도 되고, **사람이 한 말**은 인용하지 않습니다.

### 검사

`npm run build` 가 `scripts/check-changelog.mjs` 를 먼저 돌립니다. 위 규칙을 어기면
**빌드가 실패합니다.** 따로 확인하려면:

```bash
npm run check:changelog
```

검사기가 헛걸리면 규칙을 느슨하게 고치되, 끄지는 않습니다. 자꾸 헛걸리는 검사기는
사람이 무시하게 되고, 무시당하는 검사기는 없는 것과 같습니다.

---

## 2. 코드 주석

주석은 **왜 이렇게 했는가**를 적는 자리입니다. 무엇을 하는지는 코드가 이미 말합니다.

- 어떤 판단을 왜 그렇게 내렸는지, 무엇을 일부러 하지 않았는지를 적습니다.
- 그 판단의 계기가 운영 현장의 사실이라면 그 사실을 적습니다.
  (예: "요일마다 다른 차를 타는 아이가 12명 있어서 호차만으로는 답이 안 됩니다.")
- 체인지로그와 같은 이유로, **대화를 옮긴 인용은 피합니다.**

## 2-1. 데모(연습용) 학생과 실제 학생

오리엔테이션용 가짜 학생·가짜 반이 **실제 표에 함께** 들어 있습니다. `is_demo` 칸 하나로만
갈립니다. 표를 따로 두지 않은 이유는 화면을 두 벌 만들지 않기 위해서였고, 그 대가로
**읽는 곳마다 is_demo 를 빠뜨리면 안 된다**는 조건이 생겼습니다.

`wr_students` · `wr_classes` 를 읽을 때는 반드시 조건을 겁니다.

```ts
.eq("is_demo", false)                    // 행정·관리자 화면
.eq("is_demo", isDemoAccount(me.email))  // 교사 화면(데모 계정도 들어옵니다)
```

넣을 때는 필터가 아니라 **값으로** 못박습니다: `is_demo: false`.

정말 걸면 안 되는 자리라면 바로 위 줄에 `// demo-ok: 이유` 를 적습니다. 이유를 적게 하는
것이 요점입니다 — 끄는 것은 쉬워야 하지만 왜 껐는지는 남아야 합니다.

`npm run build` 가 `scripts/check-demo-isolation.mjs` 를 돌려 빠뜨린 곳을 찾습니다. 따로
확인하려면 `npm run check:demo`.

데이터 쪽도 잠가두었습니다. `is_demo` 는 **만든 뒤에 바꿀 수 없습니다**(트리거). 실제 학생을
데모로 바꾸면 명부에서 조용히 사라지고, 반대로 바꾸면 가짜 학생이 실제 명단에 나타나는데
둘 다 화면에는 오류로 보이지 않습니다. 섞였는지는 `select * from demo_isolation_check;` 로
언제든 확인합니다.

---

## 2-2. 부서(초등부/중고등부) 판정

**6학년은 중고등부입니다.** 이 판정을 화면마다 다시 만들지 않습니다 — 세 곳에 흩어져 있어서
기준이 바뀌었을 때 한 곳만 고치고 나머지를 잊었고, 학생 조회에서는 6학년이 계속 초등부로
나왔습니다. 화면마다 다른 답이 나오면 어느 쪽이 맞는지 아무도 모릅니다.

판정은 `src/lib/department.ts` 의 `departmentOf` 하나만 씁니다.

```ts
departmentOf({ department: s.department, grade: s.grade }); // → "초등부" | "중고등부" | ...
```

**학년이 읽히면 학년이 먼저입니다.** 명부의 부서 칸은 예전 자료에서 들어온 값이라 기준이
바뀌면 어긋난 채 남습니다. 부서 칸은 학년을 읽을 수 없는 줄에서만 씁니다.

`npm run build` 가 `scripts/check-department.mjs` 를 돌려, 규칙을 다시 만든 코드를 찾습니다.
정말 따로 판정해야 하는 자리라면 바로 위 줄에 `// dept-ok: 이유` 를 적습니다.

---

## 2-3. 로그인 없이 불리는 창구

스크립트·기기·외부 서비스가 부르는 API는 **미들웨어 통과 목록(`isAuthRoute`)에 있어야
합니다.** 없으면 미들웨어가 `/login` 으로 넘기는데, 부르는 쪽은 리다이렉트를 그냥 따라가고
로그인 화면은 **HTTP 200** 으로 돌아옵니다. 보내는 쪽에는 «성공», 우리 쪽에는 «아무것도 안
들어옴». 어디에도 오류가 남지 않습니다.

이 저장소에서 네 번 났습니다 — 구글챗 크론(307), Traccar 위치 수신(307), 구글챗 Pub/Sub
푸시, 구글시트 명부 수신. 같은 실수가 네 번 났으면 다음에도 납니다.

`npm run build` 가 `scripts/check-open-api.mjs` 를 돌려, 세션을 확인하지 않으면서 토큰·
비밀값으로 스스로 확인하는 라우트가 통과 목록에 있는지 봅니다. 정말 막아야 하는 자리라면
라우트 안에 `// open-api-ok: 이유` 를 적습니다. 따로 확인하려면 `npm run check:openapi`.

**보내는 쪽도 리다이렉트를 따라가지 않게 합니다.** 앱스크립트는 `followRedirects: false`,
답이 JSON 이 아니면 실패로 던집니다. 조용한 성공은 조용한 실패보다 나쁩니다 - 아무도
찾아보지 않게 만들기 때문입니다.

---

## 2-4. 동명이인 (김재이가 셋입니다)

이름만 띄우면 보는 사람은 **이미 정해진 이름이라고 믿습니다.** 그 상태로 셔틀·출결에
반영하면 엉뚱한 아이가 처리되고, 그건 되돌릴 수 없습니다.

가르는 규칙은 `src/lib/pickupParse.ts` 한 곳입니다 — 생일 → 반 → 학년 순으로 좁히고,
못 좁히면 `markIfAmbiguous` 로 「김재이(G2C·G2A·G3JA 중 누구?)」라고 적습니다.

**힌트는 문장 전체에서 찾습니다.** 「g2c 김재이」처럼 이름 앞에 적어 오시는 경우가 더
많습니다. `matchStudent` 를 부를 때 원문을 함께 넘기세요.

이 문제를 세 번 고쳤는데 세 번 다 다시 났습니다. 규칙은 매번 멀쩡했고, **화면이
`class_name` · `birth_date` 를 안 읽어온 것**이 매번의 원인이었습니다. 없는 값으로는
아무것도 가를 수 없는데 화면에는 오류가 아니라 그냥 「김재이」로 보입니다.

`npm run build` 가 `scripts/check-roster-columns.mjs` 를 돌려, 가르는 코드를 쓰는 파일이
그 두 칸을 읽는지 봅니다. 따로 확인하려면 `npm run check:roster`.

---

## 2-5. 짝지어 저장하는 칸 (반 이름 ↔ 반 연결)

학생의 반이 두 칸에 나뉘어 있습니다.

- `wr_students.class_name` — 반 **이름**(글자). 화면 대부분이 이걸 읽어 목록을 그립니다
- `wr_students.class_id` — 반 **연결**. 반 배정·시간표·교실 태블릿·담임 판정이 이걸 읽습니다

**한쪽만 채우면 화면에는 반 이름이 잘 보이는데 배정은 안 됩니다.** 오류가 아니라 «되어
보이는 것»이라 아무도 못 찾고, 반 배정 화면에 가서야 「이 아이가 미배정에 있네」를
발견합니다. 실제로 학생 추가와 구글시트 반영 두 곳이 그랬습니다.

반을 정하는 일은 `src/lib/classAssign.ts` 의 `assignClass()` 하나만 합니다.

```ts
.insert({ name, ...assignClass(className, classes, grade) })
```

이름이 명부의 반과 안 맞으면 **연결을 비워 둡니다.** 없는 반을 만들어내지 않습니다 -
오타로 생긴 반이 명부에 늘면 아무도 못 지웁니다. 대신 화면이 「그런 반이 없습니다」를
사람에게 알려줍니다.

`npm run build` 가 `scripts/check-paired-columns.mjs` 를 돌려 한쪽만 쓰는 자리를 찾습니다.
남아 있는 학생은 `select * from students_without_class;` 로 언제든 확인합니다.

---

## 3. 버전과 배포

- 버전은 `package.json` 한 곳에만 적습니다. 화면 표시는 `src/lib/version.ts` 가
  `package.json` 에서 읽어옵니다. **두 곳에 같은 값을 손으로 맞추지 않습니다.**
- 배포 순서: `git push origin staging:main` → 잠시 대기 → `git push origin staging:staging`
  → `git branch -f main staging`.
- 배포 전 `npx tsc --noEmit` 과 `npm run build` 를 모두 통과시킵니다.

## 4. 날짜·시간

- 한국 날짜는 `src/lib/kst.ts` 의 도구를 씁니다.
- `new Date().toISOString().slice(0,10)` 은 **쓰지 않습니다.** 세계표준시라 한국 시간
  오전 9시 이전에는 어제 날짜가 나옵니다. 이 실수는 이미 다섯 곳에서 났습니다.

## 5. 조용한 실패 금지

저장·삭제·전송이 실패했으면 **사람이 볼 수 있는 자리에** 이유를 남깁니다.
`catch {}` 로 삼키거나 `return null` 로 끝내지 않습니다. 화면에는 아무 일도 없었던 것처럼
보이는데 실제로는 저장되지 않는 상황이 이 저장소에서 반복해서 나왔습니다.
