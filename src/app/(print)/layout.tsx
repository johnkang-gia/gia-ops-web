/**
 * **종이 한 장만 나오는 자리.**
 *
 * ── 무엇이 문제였나 ──────────────────────────────────────────────────
 *
 * 청구서 인쇄 화면이 로그인 영역(`(dashboard)`) 안에 있었습니다. 그 자리에 있으면 사이드바·
 * 상단탭·검색창이 함께 그려집니다. 사람이 직접 열 때는 그러려니 하지만, **미리보기 창은 이
 * 화면을 그대로 안에 띄웁니다** - 청구서 한 장을 보려고 연 창에 메뉴가 통째로 한 벌 더
 * 들어앉았습니다.
 *
 * ── 왜 이렇게 고치나 ─────────────────────────────────────────────────
 *
 * 「인쇄할 때는 숨기기」(`print:hidden`)로 가리는 방법도 있지만, 그건 **종이에서만** 사라집니다.
 * 화면으로 보는 미리보기에는 그대로 남습니다.
 *
 * 주소는 그대로 두고 **껍데기만 벗깁니다.** 괄호로 묶은 폴더는 주소에 안 들어가므로
 * `/finance/invoices/…/print` 는 바뀌지 않고, 바깥 틀만 루트 것으로 돌아갑니다.
 * 로그인 확인은 미들웨어와 화면 안에서 그대로 합니다 - 껍데기를 벗긴 것이지 문을 연 것이
 * 아닙니다.
 */
export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen bg-white">{children}</div>;
}
